# Hall of Fame Studio — 开发路线图

> Current backend-mock replacement note: project initiation/kickoff approval, Agent evidence-search/submission/draft routes plus Manager command/walkthrough/action, chat/meeting/change, transcript channel creation, autonomous-cycle, due-worker, security membership/session, and production-control receipt routes now support `includeReadModels: false`, returning lightweight backend receipts plus explicit project/transcript/Manager/Agent/security/launch-control read-model refresh routes. The Harness verifies this protocol across initiation, Agent, Manager, worker, security, and production receipt write paths; `npm run agents:scenario:contract` now aggregates the low-write Manager command, Agent message, marketplace Agent contract, Workbench, and timeline-action proof slices, while the long `npm run agents:scenario` covers the full legacy scenario path. React now consumes receipt-first initiation approval, backend project-catalog/load, sample/dev-only browser snapshot seed, transcript-read/channel-create, timeline/event-read, Ready Package proof-submodel, launch/control-submodel, provider-eval command, production-control rehearsal commands, and real-command failure gates through `GET /projects`, `GET /projects/:id`, guarded `Seed Sample/Dev`, `GET|POST /projects/:id/transcripts`, channel transcript routes, Readiness Proof Map `transcriptChannelRoutes` and `agentMessageRoutes`, `GET /projects/:id/timeline`, `GET /projects/:id/events`, standalone brainstorm/artifact/review/evidence/custody routes, full pilot/launch/production-control/provider/operations/persistence/queue/security proof-model sync, fail-closed chat/meeting/change/assignment/manual-pulse controls, route-backed Manager Use Case Audit proof through `managerUseCaseAuditRoutes`, failed Workbench/run controls that clear stale prior receipts, and backend-required missing states for absent production launch/control/evidence/receipt submodels on backend-online real projects. The production-control UI commands deliberately stamp evidence as `local-rehearsal`; public production still requires explicit managed-production evidence integrity. The Manager Demo is now loaded through a visible `sample-fixture` path instead of being presented as a real product path; remaining frontend work should move lower-priority browser-first mutation paths away from local synthesized rows and keep every demo/sample fallback visibly separated.
> Marketplace Agent contracting has moved onto the same backend-first track through `POST /projects/:id/agents/contract`: real projects now get `agent-contract/v1`, roster, Agent Dashboard, timeline/event, Readiness Proof Map route, and Manager Flow Graph proof instead of browser-only team mutation. `npm run agents:agent-contract` pins this path as a low-write gate.
> Per-Agent workers can now opt into autonomous work submissions, Reviewer processing, and Submitter revision responses: completed worker tasks can become standard `progress-brief` Agent submission nodes through `submitWorkArtifact` / `submitAgentWorkArtifacts`, Reviewer workers can process assigned pending submissions through `reviewPendingSubmission` / `reviewPendingSubmissions`, and Submitter workers can answer `changes-requested` obligations through `respondToReviewObligation` / `respondToReviewObligations` with linked `revision-note` or `final-deliverable` submissions. Harness coverage now includes service, API, task evidence, HTTP scheduler proof, and targeted scheduler filtering so manager-directed pulses are not crowded out by unrelated due projects. The behavior remains explicit until production artifact volume, review policy, and cost controls are ready.
> Agent workers now have a backend `agent-autonomous-strategy-decision/v1` layer. When enabled, the worker records why it chose to respond to a requested-change obligation, review a pending teammate submission, submit completed owned work, answer management signals, continue owned work, or monitor the project. The decision is persisted into worker ledger/timeline proof and returned by Agent work-cycle and due-worker APIs, moving the product closer to free/autonomous B-side behavior without pretending the system has literal consciousness.
>
> The next bridge is now in place: `GET|POST /projects/:id/agent-autonomous-action-queue` exposes those Agent strategy decisions as executable backend queue rows. Agent due-worker and HTTP scheduler tick/start responses now return the post-run queue, Manager Dashboard embeds and renders/runs it in React, Readiness Proof Map exposes the route, Manager Flow Graph renders auditable Agent action nodes, and `npm run agents:scenario` plus `npm run ui:manager-backend` verify service/API/HTTP/browser read-run coverage. This moves A/C-side orchestration closer to the intended 24/7 product-team loop, while managed queue infrastructure, production storage, cost limits, and release policy remain M2/M3 hardening work.
> Scheduler status now also exposes `scheduler-agent-controls/v1` for startup, preserved scheduled, and latest tick controls, and the React Backend Worker Station renders whether the backend is running Agent strategy, submissions, and review-response loops. Deployment Preflight includes the matching `AGENT_AUTONOMOUS_AGENT_*` env controls. This closes a C/A observability gap: the backend can run autonomously, and the manager can see the control mode rather than inferring it from logs.
> Backend Worker Station `Start` is now backend-owned end to end: the browser starts the scheduler only after an injected or saved backend target exists, the backend performs the immediate `manager-ui-scheduler-start-pulse` and `http-autonomous-scheduler-startup-agents` sweep, and the UI only reads back project/Manager/queue state after proof exists. Workspace catalog sync, worker status checks, scheduler start/stop, aggregate Manager/Proof/Cockpit/Queue syncs, `Server Pulse`, and `Sync State` share that configured-target guard so first-run users are not shown failed default `127.0.0.1:8787` probes. `npm run ui:manager-backend` verifies the browser path so C-side Start no longer races the A-side worker ledger with a parallel local pulse.
> Dashboard missing-model recovery buttons now follow the same first-run boundary: Next Recommendation, Agent Status, Manager Command Center, Scenario Walkthrough/Trail, Action Playbook, Action Run Ledger, Use Case Audit, Requirement Matrix, Event Ledger, Governance, Group Chat collaboration proof, channel transcript sync, Change Flow, Agent Management Mesh, Agent State Summary, Continuous/Fixed Work Routines, Scenario Readiness, Proof Map, Collaboration Health, Assignment Timeline, Recent Commit Line, Proof Model, and route sync controls stay disabled until a backend target is configured and the active project is backend-writable. `npm run ui:mock-boundaries` and the local MVP release checklist pin this so recovery affordances do not become fake backend actions.
> Autonomous Run Control is now the unified backend read model and C-side UI surface for "what can run next": `GET /projects/:id/autonomous-run-control` exposes Manager queue, Agent autonomous queue, scheduler tick, worker queue proof, proof ids, next runnable actions, and production blockers as `autonomous-run-control/v1`; `POST /projects/:id/autonomous-run-control/:actionId/run` executes a listed action through the delegated queue/scheduler lane and persists an `autonomous-run-control-action-run/v1` receipt. `POST /projects/:id/autonomous-run-control/run-loop` now runs a bounded loop over the same surface, capped at five steps, and persists `autonomous-run-control-loop-run/v1` with child run receipts and stop reason. Manager Dashboard, Manager Ready Package, Manager Flow Graph, Readiness Proof Map, lightweight receipt refresh routes, React Backend Worker Station, and the delegated Agent Dashboard now point at this surface; executed control actions now surface as Dashboard run rows, Agent control-run rows, loop rows, Flow Graph receipt nodes, and Proof Map routes.
> The next C/A control layer is now a bounded Autopilot session: `GET /projects/:id/autonomous-run-control/sessions`, `POST /sessions/start`, `POST /sessions/:sessionId/tick`, and `POST /sessions/:sessionId/pause` persist `autonomous-run-control-session/v1` and `autonomous-run-control-session-tick/v1` receipts. A Manager can create a budgeted autonomous session, advance it by explicit ticks, and pause it; each tick links to the child loop/run receipts, carries Agent/task/action/lane ownership, attaches an `autopilot-delivery-target/v1` snapshot of the generic Product Team Delivery Trace, converts the next missing stage into `autopilot-delivery-target-control/v1`, injects the corresponding artifact/evidence/review intent into the delegated execution payload, and records `autonomous-run-control-target-selection/v1` for the chosen Manager/Agent/scheduler lane. Sessions and ticks appear in Manager Dashboard, Manager Ready Package, Manager Flow Graph, Readiness Proof Map, and the React Backend Worker Station. This is still private-MVP/local autonomy, not production daemon infrastructure.
> That bounded Autopilot layer now has a controlled backend due-worker: `POST /workers/autopilot/due` scans active `autonomous-run-control-session/v1` rows, advances due sessions through the normal tick receipt, returns lightweight refresh routes when requested, and appears in Worker Queue Snapshot as `autopilot-session` rows with idempotency/lease/direct-tick contracts. Completed session ticks now also export worker execution receipts into `worker_runs`; file-backed restart can reload and resume a running session through the due-worker; and queue adapter plan/dry-run plus the private adapter gateway import, lease, dispatch, acknowledge, and parity-check the Autopilot lane alongside project and Agent workers. `/workers/autonomous/tick` can opt into this lane with `tickAutopilotSessions`, and scheduler status exposes `scheduler-autopilot-controls/v1` plus Autopilot processed/skipped counters. React Backend Worker Station now exposes this as the main `Scheduler Tick` Autopilot continuation path and keeps direct session tick as a diagnostic fallback. This is the current bridge from manual Autopilot ticks toward 24/7 operation; production still needs managed queue/cron, quotas, incident ownership, and durable audit storage.
> Operations Readiness now treats external model/search autonomy as a recovery contract, not just a Provider page. `GET /projects/:id/operations-readiness` exposes provider usage ledger counts, provider eval replay counts, source snapshot/provider receipt checksum coverage, event proof counts, persistence export counts, secret-vault boundary status, a provider ledger alert rule, a recovery runbook step, and an incident-drill receipt. This keeps autonomous search/writing tied to auditable recovery before public production BYOK/provider rollout.
> Settings provider setup now has a backend-owned readiness contract. `GET /settings/provider-readiness` and `GET /projects/:id/settings-provider-readiness` expose `settings-provider-readiness/v1`, telling React whether API fields can be typed, whether `Seal` can run, what backend action is required, and which Vault/provider routes prove the state. Settings Keys renders `settings-provider-api-entry-state` so first-run users can distinguish editable API fields from backend-only Vault persistence instead of reading a missing-Vault state as a disabled input. The Vault status badge now separates `sync required`, `backend unreachable`, and `vault required`, so an unsynced panel is not presented as a missing backend API. React automatically syncs provider/runtime readiness only after a backend URL has been injected or saved through Settings Deployment, and unsaved Deployment input remains draft-only rather than a runtime target. Manual Sync/Test controls follow the same guard, so first-run users are not shown a failed default `127.0.0.1:8787` probe before they choose the active backend. Manager Ready Package embeds `settingsProviderReadiness`, Readiness Proof Map exposes `settingsProviderReadinessRoutes`, and `npm run agents:settings-provider-readiness` verifies blocked/ready Vault states, project-scoped routing, formal proof-surface inclusion, and secret redaction without launching a browser.

> Local MVP startup readiness now has a global backend preflight. `GET /local-mvp-startup-readiness` exposes `local-mvp-startup-readiness/v1`, aggregating backend reachability, Secret Vault readiness, model/search runtime status, provider-vault redaction, project catalog readability, next action, validation commands, and production blockers before the UI claims a local product-team session can start. Workspace Hub renders that preflight beside Start Initiation and auto-syncs it once per configured backend target, Settings renders the same route in the provider setup flow, and Project Initiation requires the current configured backend target plus `readyForFirstProjectRun=true` before Start Kickoff or Approve/Create can run. Startup readiness refresh no longer probes the default backend URL before the user saves/injects a target, and stale readiness from another target cannot approve a real kickoff. Manager Ready Package embeds `localMvpStartupReadiness`, Readiness Proof Map exposes `localMvpStartupReadinessRoutes`, and `npm run agents:local-mvp-startup-readiness` verifies the route without launching a browser.
> Public production startup readiness now has a global backend preflight. `GET /public-production-startup-readiness` exposes `public-production-startup-readiness/v1`, checking enforced signed access, replay protection, audit fail-closed mode, managed identity provider/service identity evidence, managed Secret Manager/KMS, managed persistence, managed worker queue, provider redaction, production provider cost controls, production data governance, adapter gateway attestation, production traffic/health/rollback controls, centralized observability, alert routing, on-call ownership, managed incident records, restore drill proof, and centralized audit retention. It embeds `managed-identity-startup-readiness/v1` so prototype HMAC access headers cannot be mistaken for a production identity system, embeds `production-cost-control-startup-readiness/v1` so project-local provider budgets cannot be mistaken for centralized production cost control, embeds `production-data-governance-startup-readiness/v1` so project-local privacy settings cannot be mistaken for customer data governance, and embeds `production-traffic-startup-readiness/v1` so configured URLs cannot be mistaken for controlled customer traffic with health checks, release approval, and rollback proof. It also embeds `production-environment-setup/v1`, a redacted setup matrix that tells operators which production env var groups, routes, and validation commands remain missing without exposing values. Settings Runtime, Manager Ready Package, Readiness Proof Map, the project-level Production Launch Control Center, and Manager UI link this route, and `npm run agents:public-production-startup-readiness` verifies that local/private-MVP readiness cannot be presented as public-production readiness. `npm run agents:public-production-readiness-report` now exposes the same data as a redacted operator report and embeds the managed environment preflight summary, and `npm run agents:public-production-readiness-report:validate` verifies that partial/env-only production configuration stays blocked, managed environment readiness stays false, and configured values are not printed.
> Public-production customer acceptance is now an explicit P2 startup sub-contract: `production-customer-acceptance-startup-readiness/v1` and the `customer-production-acceptance-policy` gate require policy, success criteria, threshold, approval authority, rollback criteria, and signed managed-production customer acceptance evidence before any public traffic claim can pass. Private-pilot customer acceptance remains P1 proof, not a production launch certificate.

> Settings Health Quick Check now reads a backend-owned contract instead of browser-synthesized rows. `GET /settings/health-readiness` exposes `settings-health-readiness/v1`, covering backend API, local MVP startup, Secret Vault, model/search provider status, explicit provider test routes, worker status route, project catalog, validation commands, and production blockers without spending provider calls or creating probe projects. Manager Ready Package embeds `settingsHealthReadiness`, Readiness Proof Map exposes `settingsHealthReadinessRoutes`, and `npm run agents:settings-health-readiness` verifies the route without launching a browser.

> Settings Deployment and Models now read a backend-owned runtime contract instead of frontend-inferred route rows. `GET /settings/runtime-readiness` and `GET /projects/:id/settings-runtime-readiness` expose `settings-runtime-readiness/v1`, covering model/search runtime status, provider-vault binding proof, worker status route, persistence adapter, worker queue adapter, deployment preflight route, validation commands, and production blockers. Manager Ready Package embeds `settingsRuntimeReadiness`, Readiness Proof Map exposes `settingsRuntimeReadinessRoutes`, and `npm run agents:settings-runtime-readiness` verifies the route plus formal proof-surface inclusion without launching a browser or calling providers.

> Settings Integrations now reads a backend-owned aggregate readiness contract instead of only showing capability-route rows. `GET /projects/:id/settings-integration-readiness` exposes `settings-integration-readiness/v1`, aggregating provider budget, Agent tool grants, Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting into one project-scoped Settings packet with action-required rows, checksums, validation commands, Manager Ready Package `settingsIntegrationReadiness`, and Readiness Proof Map `settingsIntegrationReadinessRoutes`. `npm run agents:settings-integration-readiness` verifies that the UI can rely on the aggregate route while production proxy/webhook, managed MCP, managed vector memory, centralized alerting, and centralized observability remain blocked.
> Settings policy editing now has a browser-backed regression gate for real backend persistence. Workspace, Privacy, provider budget, and tool-grant controls use per-project pending drafts, complete `project-settings/v1` writes, backend receipt rollback, and disabled-in-flight controls so rapid user edits cannot silently become browser-only state. `npm run ui:settings-agents-server` verifies the full browser path through project settings, timeline/event proof, provider controlled-run consumption, and Vault-sealed provider credentials.
> Settings backend-backed controls now share the first-run configured-target boundary: Privacy, workspace policy, workspace bind, memory readiness, meeting summaries, integration readiness, integration capability sync, provider budget, and Agent tool grants stay disabled until a backend API target is saved/injected and the active project is backend-writable. The underlying sync/write functions also fail closed instead of probing the default backend URL or writing browser-local drafts for real projects.

> Settings Vector Store has been downgraded from fake editable UI to a backend Evidence Index readiness contract. `GET /projects/:id/evidence-index-readiness` exposes `evidence-index-readiness/v1`, proving local evidence searches, source snapshots, provider receipts, Agent submissions, and artifact storage proofs can be indexed for MVP inspection while managed vector memory stays production-blocked. `npm run agents:evidence-index-readiness` verifies the route without launching a browser.
> Settings Budget Alerts has been downgraded from fake editable UI to a backend Budget Alert readiness contract. `GET /projects/:id/budget-alert-readiness` exposes `budget-alert-readiness/v1`, proving local daily budget and hourly request headroom can be computed from backend project settings and provider usage ledger while centralized alert routing/on-call ownership stay production-blocked. Manager Ready Package now includes `budgetAlertReadiness`, and Readiness Proof Map exposes `budgetAlertReadinessRoutes`, so cost-control readiness is part of the formal proof surface. `npm run agents:budget-alert-readiness` verifies the route without launching a browser.
> Settings Error Reporting has been downgraded from fake editable UI to a backend Error Reporting readiness contract. `GET /projects/:id/error-reporting-readiness` exposes `error-reporting-readiness/v1`, proving local log streams, alert rules, recovery runbook, incident-drill status, and error-signal counts are route-backed from Operations Readiness while centralized logs/traces/incident ownership stay production-blocked. Manager Ready Package embeds `errorReportingReadiness`, Readiness Proof Map exposes `errorReportingReadinessRoutes`, and `npm run agents:error-reporting-readiness` verifies the route without launching a browser.
> Autonomous Agent worker evidence now reaches the configured search provider path when explicitly enabled. `runAgentWorkCycleWithProviderEvidence` and async Agent work-cycle API calls with `useProviderEvidenceSearch: true` run through provider policy, tool grants, budget/rate limits, retry/circuit state, and provider usage ledger proof, then attach provider-backed sources and receipts to the same evidence-search/submission nodes the Manager already inspects. Agent Autonomous Action Queue rows now mark provider-planned evidence work when the search provider is enabled, async queue runs persist the provider-backed evidence/search/submission/run receipt chain, and evidence-quality Autopilot session ticks plus `/workers/autopilot/due` can use the same async provider lane while linking provider usage and evidence-search ids into action, loop, and session tick receipts. The HTTP scheduler now calls the async API for Autopilot due-work and forwards provider-evidence controls; React Backend Worker Station renders the provider evidence receipt next to the scheduler-owned Autopilot worker receipt; Manager Flow Graph attaches that receipt to the Autopilot tick node and links to the evidence-search node; Readiness Proof Map exposes the evidence-search/provider-usage routes on the tick route. Provider-backed Autopilot ticks now also publish an `autopilot-provider-evidence` group-chat coordination message, store its message id on run/loop/tick/session receipts, expose the transcript route in Flow Graph and Proof Map, and include it in transcript proof coverage. Explicit evidence targets can also create project-level evidence/submission nodes when no single task owns the target. `npm run agents:scenario` verifies the service/API/queue/Autopilot due-worker/Flow/Proof/Transcript/UI source bridge with deterministic provider proof. Production still needs real BYOK provider eval, managed audit storage, cost enforcement, and incident ownership.
> Autonomous Run Control receipt refresh is now route-specific: action, loop, session start/tick/pause, and Autopilot due-worker writes return session, active tick/pause, scheduler tick, `/workers/autopilot/due`, Agent queue, operating-loop, cycle-consistency, Flow, Proof, timeline, event, and transcript routes when `includeReadModels: false`. React consumes those routes after Autopilot commands, reducing C-side dependence on browser-local session state while preserving public-production blockers.
> The HTTP scheduler validation now also proves a compact generic product-team chain: evidence-oriented autonomous work becomes an evidence packet with local evidence-search proof, Reviewer requests changes, the submitter posts a linked revision, the Reviewer accepts it, and a later due task becomes an accepted final deliverable with Manager Flow Graph and Readiness Proof Map routes.
> Product Team Delivery Trace now turns that chain into one Manager-readable backend contract. `GET /projects/:id/product-team-delivery-trace` and Manager Ready Package expose `product-team-delivery-trace/v1`, covering kickoff, Agent self-marketing, brainstorm, evidence, generated draft, review/revision, accepted final deliverable, and proof surfaces. This is the shortest path for replacing scattered frontend interpretation with one C-side truth surface for the A-side autonomous team.
> Manager Proof Map now renders the submission-review workflow and product-team delivery trace as first-class backend proof cards. The browser Harness verifies chat/timeline proof exits for requested-change closure, revision response, accepted final delivery, and delivery-chain completion, so the Manager can inspect the autonomous product-team loop without trusting summary-only UI.
> Generic Product-Team Acceptance Chain now sits on top of those proof cards as the C/A acceptance index. Readiness Proof Map emits `productTeamAcceptanceChainRoutes` / `productTeamAcceptanceChainSummary` with kickoff, self-marketing, brainstorm, evidence, draft, review/revision, final deliverable, and proof-surface stage rows, plus `readyForGenericProductTeamAcceptance`, `readyForBsideProductTeamRun`, proof ids, timeline ids, event ids, and production blockers. React Manager Proof Map renders `proof-map-product-team-acceptance-chain` with chat/timeline exits, and the product-team plus browser Harnesses reject research-specific drift.
> Group Chat Transcript Index now also renders backend collaboration proof rows for evidence searches, evidence source reviews, Agent submissions, Reviewer decisions, revisions, and final deliverables, and the live Group Chat stream renders those backend work messages as collaboration node cards rather than ordinary text bubbles. The cards now prefer backend-provided resource routes (`submissionRoute`, `evidenceSearchRoute`, `evidenceSourceReviewRoute`, `submissionReviewRoute`) before fallback URL construction. The browser Harness verifies those rows and cards can open exact chat/timeline proof, jump from a final-deliverable chat card to the matching Manager Flow Graph node after a real initiated project completes the review/revision/final-acceptance loop, expose the selected node's backend submission route, and return to Manager Proof Map from the node detail.
> Product Team Operating Loop is now the backend C/A coordination summary: `GET /projects/:id/product-team-operating-loop` exposes `product-team-operating-loop/v1`, joining Product Team Delivery Trace, Autonomous Run Control, Agent Autonomous Action Queue, Manager Action Queue, Worker Queue Snapshot, Readiness Proof Map, timeline ids, and event ids into one local-autonomy status. It now also projects Product Team Mission Runner handoff execution as `customerSide.handoffExecution`, distinguishing A-side session start, tick proof, run receipts, result messages, and Agent output submissions from the original C-side approval packet. The Research validation sample now reads it through the real HTTP server before passing, so the system proves not only delivery trace closure but also Manager continuation, Agent strategy selection, proof routes, and explicit production autonomy blockers.
> Team Collaboration Diagnostics now turns "overall scheduling" into a route-backed backend contract. `GET /projects/:id/team-collaboration-diagnostics` exposes `team-collaboration-diagnostics/v1`, diagnosing whether kickoff, Lead/Reviewer governance, self-marketing, group chat transcripts, owned tasks, brainstorm/evidence handoff, artifact review, revision, final delivery, Flow Graph, Proof Map, timeline/event proof, and Agent initiative rows are connected. Manager Ready Package, Manager Flow Graph, Readiness Proof Map, React proof sync, and the Research validation sample now consume it while keeping public production blocked for real provider/BYOK, managed queue/cron, durable audit, cost controls, customer acceptance thresholds, and incident recovery.
> Planner / Executor / Reviewer responsibility is now a route-backed state machine instead of an inferred dashboard label. `GET /projects/:id/planner-executor-reviewer-state-machine` exposes `planner-executor-reviewer-state-machine/v1`, proving the Planner kickoff/role lane, Executor brainstorm/evidence/draft lane, Reviewer requested-change/revision/final-acceptance lane, and four handoff transitions (`plan-to-execute`, `execute-to-review`, `review-to-revision`, `revision-to-final`). Manager Ready Package embeds it, Manager UI renders a compact source-labeled state-machine panel, Manager Flow Graph renders a state-machine node, Readiness Proof Map exposes `plannerExecutorReviewerStateMachineRoutes`, React proof sync fetches it, and `npm run agents:product-team:smoke` verifies the complete generic product-team sample without making Research a special case.
> Collaboration Intent Queue now turns meeting intent, group-chat action routing, and product-readiness targets into a backend contract and a consumed Manager surface. `GET /projects/:id/collaboration-intent-queue` exposes `collaboration-intent-queue/v1`, ranking kickoff self-marketing, role negotiation, group-chat attention, Manager next action, Agent autonomous initiatives, MVP readiness targets, and review/final closure into queue rows with runnable routes and proof ids; `POST /projects/:id/collaboration-intent-queue/:intentId/run` delegates runnable rows into the existing backend execution surfaces, records `collaboration-intent-run/v1`, and returns delegated Agent submission/evidence/review outputs when present. React now clears stale prior intent receipts/output and renders `Intent Run Failed` when that backend run fails, so a failed C-side click cannot look like a previous successful A-side action. Manager Ready Package, Manager Flow Graph, Readiness Proof Map, React Backend Worker Station, the product-team Harness, and the browser backend Harness now consume it while public 24/7 autonomy remains blocked by managed queue/cron, durable audit, cost controls, acceptance thresholds, incident recovery, and provider/BYOK policy.
> Runtime Contract Freeze now makes the local MVP contract set explicit. `GET /projects/:id/runtime-contracts` exposes `runtime-contract-freeze/v1`, freezing the route/schema coverage for Agent submissions, artifact storage proof, evidence search/source/provider proof, Reviewer review, revision/final delivery, transcripts, timeline, event ledger, Flow Graph, Proof Map, Operating Loop, Planner / Executor / Reviewer State Machine, and Team Collaboration Diagnostics. Manager Ready Package, Manager Flow Graph, Readiness Proof Map, React proof sync, and the product-team gates verify it while public production remains blocked by real provider/BYOK, managed database, managed queue/cron, durable audit, cost controls, acceptance policy, and incident recovery.
> Autonomous Cycle Consistency now proves the local MVP can keep an autonomous product-team loop coherent across multiple steps. `GET /projects/:id/autonomous-cycle-consistency` exposes `autonomous-cycle-consistency/v1`, joining child action receipts, bounded loop receipts, Flow Graph nodes, Proof Map routes, Worker Queue recovery, Product Team Operating Loop, Team Collaboration Diagnostics, and Runtime Contract Freeze. `npm run agents:product-team:cycle-consistency` runs the HTTP three-step autonomous loop through signed membership access; public 24/7 autonomy remains blocked by managed queue/cron, distributed leases, centralized audit, cost controls, and incident recovery.
> Runtime Autonomy Status now gives the Manager one backend answer for "can this C/A runtime keep going and be recovered?": `GET /projects/:id/runtime-autonomy-status` aggregates Product Team Mission Runner startup, Autopilot session/tick receipts, action/loop receipts, Worker Queue Snapshot recovery, queue adapter rehearsal, persistence snapshot, Operating Loop, Run Control, Flow Graph, Proof Map, scheduler status/tick routes, and `/workers/autopilot/due`. Product Team Mission Runner receipts now link this recovery route directly, and React project-start refresh pulls it immediately alongside Mission Runner, Operating Loop, and Run Control. The product-team core Harness and Research validation sample verify the standalone route, Readiness Proof Map `runtimeAutonomyStatusRoutes`, Manager Flow Graph `runtime-autonomy-status`, Ready Package summary, Security Boundary policy, and the explicit `readyForUnattendedProduction: false` boundary.
> `npm run agents:product-team:research-sample` now provides the low-write Research-as-validation-sample gate. It reuses the in-memory generic product-team backend chain, statically guards the Research-as-sample contract, and asserts the proven stages remain kickoff, self-marketing, Leader campaign, discovery, brainstorm, evidence/source review, Reviewer source decisions with no pending source-review queue, backend artifact draft, visible Agent Workbench draft proof receipt routes, requested-changes review, revision, accepted final deliverable, Artifact Quality Audit, Submission Review Workflow, Flow Graph, Proof Map, Manager governance/action Proof Map routes, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, ready Project Evidence Archive handoff, artifact storage/workspace proof, memory-readiness proof, and production blockers instead of paper/thesis/manuscript-specific protocol fields. It also requires Product Team Delivery Trace to expose the eight generic stage ids from kickoff through proof surfaces and evidence every ready stage through proof, timeline, or event ids, so Research remains a validation sample instead of a custom workflow. `npm run agents:product-team:cycle-consistency` remains the focused gate for the separate three-step autonomous consistency proof.
> `npm run agents:real-user-zero-to-autonomy` now provides the low-write real-backend API gate for the same zero-to-autonomy journey. It starts the real `agents:server`, seals model/search credentials through Secret Vault against local mock providers, creates a generic product-team mission, binds a local/private MVP workspace through the backend local runtime, writes and reads a workspace proof file, runs the C/A handoff, then forces Agent Autonomous Action Queue to prove an Agent continues independently with `agent-autonomous-action-run/v1`, action-decision, chat, timeline, event, and submission proof. It then drives evidence search, Reviewer source decisions, brainstorm submission, model-backed draft, Reviewer change request, all linked revisions needed to close open change requests, accepted final deliverable, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, ready Project Evidence Archive storage/workspace proof coverage, and Product Team Delivery Trace through HTTP APIs. `npm run agents:real-user-zero-to-autonomy-report` reuses that same backend chain to emit a redacted operator stage report, and `npm run agents:real-user-zero-to-autonomy-report:validate` verifies full stage/artifact coverage, search/model provider usage proof rows, provider receipt proof rows, no provider secret leakage, and no public-production overclaim. This bridges the in-memory smoke gate and the heavier browser `ui:real-user-zero-to-autonomy` gate without claiming browser UX or production-managed infrastructure is finished.
> `npm run ui:real-user-zero-to-autonomy` is now the browser proof for replacing the most important frontend mock path. It opens the built React app against the real `agents:server`, lets a fresh user set the backend API target in Settings Deployment, type and seal model/search credentials in Settings, verifies Workspace Hub startup readiness before Start Initiation, starts the generic mission from Workspace Hub, binds a local/private MVP workspace from Settings Workspace and verifies backend local-runtime readback, executes the C/A handoff from Manager UI, proves an Agent continues via Agent Autonomous Action Queue with a substantive autonomous submission, records provider-backed evidence with corroborating sources, records Reviewer source decisions, closes all requested-change reviews with linked revisions, and verifies Manager UI readback for Artifact Quality Audit, ready Project Evidence Archive storage/workspace proof coverage, Product Team Delivery Trace, Submission Review Workflow, Evidence Index readiness, core Proof Map routes, and the backend Flow Graph final-deliverable node. This proves a private-MVP real-user path, not public production readiness.

> Timeline detail actions now use a backend receipt instead of a local/no-op footer. `POST /projects/:id/timeline/actions` records Manager notes, acknowledgements, completion marks, and edit notes as `timeline-action-receipt/v1`, appends timeline/event-ledger proof, and is covered by `npm run agents:timeline-action`. Completion/edit actions are audit annotations; they do not mutate original timeline evidence.
> Private-pilot C-side handoff is now tested from launch approvals, not only final closeout receipts. `npm run ui:manager-private-pilot` prepares the generic product-team project at `private-pilot-launch-handoff`, records Manager/security launch approvals, requests/approves the evidence export, records the download audit, then records release candidate, launch run, health, and acceptance receipts from the browser and verifies Flow Graph plus Readiness Proof Map proof.
> Manager Ready Package now includes a Launch Operations Overview above the detailed launch panels, backed by `GET /projects/:id/launch-operations-overview`. It summarizes Private MVP Launch Package status, private-pilot acceptance, launch approvals, evidence package/download audit, next action, public-production no-go, production blocker count, Public Production Next Steps, and proof routes from backend read models, so non-technical operators do not have to infer the launch state from separate cards. The next-step rows can now be recorded as `launch-operations-next-step-run/v1` receipts for audit/proof without changing public-production readiness.
> P2 hardening now has focused gates instead of only one long full rehearsal. `npm run agents:product-team:production-ops-controls`, `production-deployment-controls`, `production-security-controls`, and `production-provider-controls` verify receipt write/read/proof/evidence-integrity projection without replaying the full private-pilot chain. `npm run agents:product-team:production-evidence-integrity` verifies local/test receipts remain rehearsal-only, unsigned managed-production claims remain unattested, then signed managed-production receipts close the evidence-integrity gap. `npm run agents:managed-environment-preflight` now classifies whether the configured adapter gateway, persistence adapter, queue adapter, and attestation signing are real managed-environment candidates instead of localhost/local-shadow rehearsal, with an explicit `--check-network` mode for live gateway `/health` and `/state` probes. `npm run adapters:gateway-postgres-store:validate` now proves the adapter gateway can generate an HMAC-signed control-plane attestation only after query-bound Postgres readback parity, and `npm run agents:managed-infrastructure-cutover-attestations` proves the project API can request those attestations only after project dry-run receipt evidence exists, persist managed persistence/queue cutover operations receipts, and project them into infrastructure rehearsal without closing unrelated public-production gates. `npm run agents:product-team:production-launch-governance` verifies signed Manager, security-admin, and operations-owner production approvals plus approval proof projection into Launch Audit, Control Center, Proof Map, and Flow Graph while the broader production launch remains `no-go` until every real managed control is accepted.
> C-side Agent Pulse controls now call those backend switches directly: normal manager Agent pulses can create real autonomous submission nodes, Reviewer pulses can request changes or accept linked revisions, and Submitter pulses can close open review obligations with revision notes. HTTP scheduler start and server autostart now forward the full startup/tick controls, including Agent autonomous strategy, submission, Reviewer, and revision-response controls, into Agent sweeps through explicit backend/env switches.
> C-side Server Pulse, Hour Pulse, Day Report, and Manager Action Playbook `fixed-continuous-routines` now use the real scheduler tick boundary with current-project filters and Agent worker controls. A manager pulse or Action Queue run can now advance project work and Agent worker output together instead of only running a project-level autonomous cycle; Hour/Day preserve cadence through `projectCadence`, and Action Queue runs persist `manager-action-scheduler-tick/v1` receipts with project/Agent processed counts.
> Manager readiness now treats Agent inbox rows as consumable work, while durable change ledger/timeline/event receipts preserve governance state. This closes a practical C/A gap: autonomous Agents can continue working freely without making the manager view lose already-audited team sync proof.
> Event-ledger readiness now survives normal local private-MVP pressure better: the retained local event window is larger, Agent-to-Agent messages write timeline/event proof, and replay summaries can recover core kickoff/change/handoff/autonomous coverage from durable project records after older events are pruned. This fixes a concrete restart-readiness gap while keeping public production blocked on managed immutable audit storage and retention policy.
> `docs/LAUNCH_READINESS_GATES.md` is now the explicit launch convergence table: P0 local backend MVP, P1 customer private pilot, and P2 public production each have required commands, allowed claims, and remaining blocker domains. `npm run launch:gates` keeps that table tied to package scripts so the roadmap cannot drift into vague "almost ready" language; `npm run launch:infra` runs the public-production startup blocker contract plus the P2 managed persistence / queue adapter rehearsal, including Agent HTTP server dry-runs through an env-configured gateway endpoint and the managed-infrastructure cutover attestation bridge, without claiming full production cutover. The same infrastructure rehearsal now appears as `production-infrastructure-rehearsal/v1`, Readiness Proof Map `productionInfrastructureRehearsalRoutes`, and the Manager Flow Graph `production-infrastructure-rehearsal` node.

> **当前状态：Private MVP（公开生产 blocked）**
> 本项目已能做本地后端 MVP 验证与受控私有试点 rehearsal，**尚未达到公开生产上线标准**。
> 请勿用于无人值守生产、未批准敏感数据或客户生产流量。

---

## 一、给 Visitor / Contributor 的 30 秒说明

| 问题 | 答案 |
|------|------|
| 现在能用吗？ | **可以用于本地后端 MVP 验证。** 已有从 Settings backend target、provider seal、立项、C/A handoff、Agent 自主行动、证据、草稿、评审、修改到 final deliverable 的真实后端/browser gate；不能当作公开生产服务。 |
| 能部署给团队用吗？ | **只能做受控私有试点 rehearsal。** 需要 operator supervision、非敏感或已批准试点数据，并通过 P1 launch/evidence/acceptance gates；不能无人值守生产运行。 |
| 能接 API Key 跑真实任务吗？ | **只建议在受控试点中使用。** 本地 Secret Vault、provider readiness、provider policy/usage ledger、project tool grants 和 redaction 已有合同；生产仍需要 managed KMS/Secret Manager、密钥轮换/撤销、集中审计、真实 provider eval、成本告警和事故流程。 |
| 现在适合做什么？ | 跑 P0/P1 gate、继续替换低优先级前端 fallback、完善私有试点操作路径、补 P2 managed controls。 |
| 我们卡在哪？ | **P2 公开生产。** 主要缺口是 managed database、managed queue/cron、managed KMS/Secret Manager、集中 observability/audit、incident recovery、真实 BYOK/provider 生产策略与客户生产验收策略。 |

---

## 二、总体进程规划

完整交付路径分为 **六个里程碑**。前三个是当前核心研发主线；后三个是发布前必做项。

### 验收样例：Research Project

Research Project 是早期用于验收通用产品团队能力的样例场景，而不是单独的垂直产品方向。它的价值在于同时覆盖一个产品团队应具备的核心动作：开会讨论、主动搜集信息、头脑风暴、写作产出、评审修订、群聊协作、节点提交和最终交付。

开发时应避免把逻辑写死为“论文工具”或“研究工具”。Research Project 跑通后，相关能力应能迁移到产品策划、市场分析、技术方案、内容生产、品牌策略等一般项目形态。

```
M0 原型壳层          ████████████████████  已完成
M1 人物 Skill 设计   ████████░░░░░░░░░░░░  ← 当前主战场
M2 Agent 协作机制    ██████░░░░░░░░░░░░░░  进行中（骨架已有）
M3 运行算法 Runtime  ████░░░░░░░░░░░░░░░░  原型级（非生产）
M4 安全与 BYOK       ██░░░░░░░░░░░░░░░░░░  原型启动
M5 公开发布          ░░░░░░░░░░░░░░░░░░░░  未开始
```

---

## 三、各阶段说明

### M0 · 原型壳层 ✅ 已完成

**目标：** 验证产品形态与交互叙事，而非交付可用系统。

**已交付：**
- React + Vite 本地原型 UI（Dashboard、Pantheon、圆桌、项目工作区）
- 40 个人物卡片与雷达档案展示
- Manager Demo 一键种子数据
- 浏览器 `localStorage` 持久化（仅演示）

**不代表：** 系统已可托管真实协作。

**相关路径：** `src/App.jsx`、`docs/assets/`、`scripts/validate-agent-manager-scenario.mjs`

---

### M1 · 每个人单独的 Skill 设计 🔄 **当前主战场**

**目标：** 每个人物是可独立调用、可验证、可 dist 的标准 Skill 包，而非一句 prompt 或一张卡片。

**进行中：**
- `skills/hall-of-fame-personas/source/personas/{slug}/` 源包结构（`persona.json`、`prompt.md`、`memory.md`、`examples.md`、`regression.json`）
- Schema 校验、回归用例、mindframe 生成、registry 编译流水线
- 人物任务匹配与圆桌发言排序（`persona_runtime.py`）

**尚未完成：**
- 全部 40 个人物的认知深度与行为回归达标
- 跨人物风格差异在真实 LLM 调用下的一致性验证
- Skill 版本化与发布策略

**Contributor 可参与：**
- 按 [`references/persona-authoring-template.md`](skills/hall-of-fame-personas/references/persona-authoring-template.md) 完善单人物包
- 运行 `npm run skills:check` 确保 schema + regression 通过
- 阅读 [`人物Skill系统.md`](人物Skill系统.md) 与 [`skills/hall-of-fame-personas/SKILL.md`](skills/hall-of-fame-personas/SKILL.md)

**退出标准（进入 M2 硬门槛）：**
- [x] 核心人物 roster 通过 regression（`npm run skills:check`：40 persona packages / 55 regression cases）
- [x] 单人物 Skill 可独立 dist，无外部工具指纹残留（`npm run skills:dist` / `npm run skills:audit`：40 dist packages，privatize audit passed）
- [x] 任务匹配 Top1/Top3 行为符合 baseline cases（`npm run skills:check`：55/55 regression cases）

---

### M2 · Agent 协作机制 🔄 进行中

**目标：** 多 Agent 在圆桌、群聊、分工、Lead/Reviewer 治理下可预测地协作，而非随机对话。

**已有骨架：**
- 五层 Agent 模型（Identity / Mind / Relations / Communication / Work Cycle）
- 会议话术框架（Kickoff / Sync / Change / Escalation）
- Lead 指派、Reviewer 挑战、attention-scored 发言路由
- 协作健康检查 `evaluateCollaborationState`

**尚未完成：**
- 协作不变量在复杂场景下的压力测试
- 跨会议状态一致性（charter → obligations → ledger）
- Agent 自我说明 / 自荐 / Leader campaign 在流程图中的一等节点表达
- 与真实 LLM 输出结合的容错与重试策略
- 多人贡献时的协议版本管理

**Contributor 可参与：**
- 阅读 [`src/agents/README.md`](src/agents/README.md) 与 [`src/agents/ARCHITECTURE_AUDIT.md`](src/agents/ARCHITECTURE_AUDIT.md)
- 补充 scenario 验证脚本与边界用例
- 在 Issue 中讨论「谁该说话、何时升级、如何记录决策」规则

**退出标准：**
- [ ] Manager Demo scenario 覆盖主要协作路径且可重复
- [x] Private-pilot acceptance is split into staged validation gates (`npm run agents:product-team:private-pilot:release`, `:launch`, `:health`, `:acceptance`) so launch work can be scheduled by the first failing customer-visible receipt instead of rerunning the whole long gate.
- [x] Manager Ready Package now exposes gated C-side backend commands for launch approvals, evidence export handoff, and the four private-pilot receipts, so approval, package handoff, release, launch, health, and acceptance can be recorded from UI only when backend workflow gates allow it.
- [x] Manager Ready Package now exposes gated C-side backend commands for provider eval plus production operations/deployment/security/provider control rehearsals. These commands write backend receipts and refresh Manager/Flow/Proof models, but mark production-control evidence as `local-rehearsal` so they do not overclaim managed-production readiness.
- [x] `npm run ui:manager-private-pilot` now proves the C-side private-pilot handoff/closeout loop: a backend-prepared generic product-team project is opened in the browser, Manager/security launch approvals are recorded, evidence export request/approval/download audit is recorded, the four receipt buttons are clicked, and backend Flow Graph / Proof Map acceptance evidence is verified.
- [x] Research Project 作为通用产品团队验收样例，覆盖会议、脑暴、群聊协作与角色分工，不引入 research-only 协议（`npm run agents:product-team:smoke` / `npm run agents:product-team:research-sample` / `npm run agents:product-team:core` / `npm run agents:product-team:private-pilot`；旧 file-backed core 通过 `agents:product-team:core:file-backed` 显式运行）
- [ ] 会议 → 任务 → 群聊 → 自治周期 全链路无断点
- [x] Agent 自我说明、角色自荐、Leader campaign 能作为流程图证据节点查看（Manager Flow Graph role/self-marketing nodes，`npm run agents:product-team:core` / `npm run agents:product-team:research-sample`）
- [ ] 协作诊断信息对 Contributor 可读、可调试

---

### M3 · 整个运行算法 🔄 原型级

**目标：** Hour Pulse / Day Report 自治循环、项目账本、变更 ledger 在统一 runtime 下稳定推进。

**已有原型：**
- `planAutonomousWorkCycle` + `advanceAutonomousProjectCycle`
- Manager Flow Graph emits proofed role-clarification nodes and first-class `self-marketing` nodes for role self-nominations and Leader campaign pitches.
- Readiness Proof Map exposes `roleNegotiationRoutes` and `selfMarketingRoutes`, covered by `npm run agents:product-team`.
- Artifact revision lineage links requested-changes reviews to revision-note/final-deliverable submissions, superseded drafts, resolved obligations, Flow Graph `revision` edges, and Readiness Proof Map `revisionRoutes`.
- Evidence searches now compute source `qualityScore`/`qualitySignals` plus aggregate `evidenceJudgement`, visible in Manager Dashboard, Flow Graph, Task Evidence, and Readiness Proof Map.
- Evidence quality audit is exposed by `GET /projects/:id/evidence-quality-audit` and Manager Ready Package as `evidence-quality-audit/v1`, aggregating evidence rows, per-source quality/safety signals, provider provenance, Proof Map routes, decision gates, production controls, and checksum. The product-team Harness now requires it through in-process API, real HTTP backend, project archive manifest, security route policy, and Manager UI snapshot.
- Evidence source review workflow is exposed by `GET|POST /projects/:id/evidence-source-review-workflow` and Manager Ready Package as `evidence-source-review-workflow/v1`, deriving reviewer-visible source review items and accepting `evidence-source-review/v1` Reviewer source decisions from the evidence quality audit. Provider-backed evidence now writes local `evidence-source-snapshot/v1` and `evidence-provider-receipt/v1` checksum records. The product-team Harness now requires source-review POST approvals, Flow Graph source-review nodes, Proof Map source-review routes, archive decision records, source snapshot/provider receipt coverage, enforced reviewer identity matching, `evidence_source_reviews`, `evidence_source_snapshots`, and `evidence_provider_receipts` persistence rows, real HTTP backend coverage, production launch evidence routes, security route policy, and Manager UI decision counts while production human review/managed immutable source-provider storage remains blocked.
- Real Node HTTP scheduler status/tick coverage is now part of `npm run agents:product-team`, proving the same generic product-team acceptance sample can be advanced by backend project and Agent workers without a browser tab.
- MVP readiness is exposed by `GET /projects/:id/mvp-readiness` and Manager Ready Package, separating local-pilot readiness from production blockers. It now emits routed `operatorActions` and a method/owner/path-backed `nextShortestPath`, and `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run` records those Manager choices as `mvp-readiness-operator-action-run/v1` proof in timeline, event ledger, Flow Graph, and Proof Map. Failed operator actions now render `Action failed` with no local operator receipt, clearing the risk that a previous receipt is mistaken for the current click. Runnable core-gap receipts now carry `autopilot-delivery-target-control/v1`, appear in Collaboration Intent Queue, create a `run-mvp-readiness-target` Autonomous Run Control action, and can execute through `POST /projects/:id/collaboration-intent-queue/:intentId/run` into the requested Agent submission/review node until the targeted gap closes. The target is step-sensitive across brainstorm, evidence, draft product brief, requested-changes review, linked revision, final deliverable, and final acceptance. `npm run agents:mvp-readiness:operator-actions` is now the focused low-write real Node HTTP server gate for this C/A readiness-action handoff, so the chain can be verified without replaying the full acceptance harness. This lets the C-side move from local MVP status to A-side execution, private-pilot handoff, or production hardening without inventing frontend guidance or pretending the production blocker is closed.
- Product Team Mission Runner is now exposed by `POST /product-team-missions` plus `GET /projects/:id/product-team-missions`. It is the first one-call C-side startup path for a generic customer mission: create/approve kickoff or reuse the meeting just approved in React initiation, confirm Leader/Reviewer/next actions, start bounded Autopilot, optionally run the first A-side tick, and project `product-team-mission-run/v1` into Manager Dashboard, Flow Graph, Readiness Proof Map, Product Team Delivery Trace, Operating Loop, Collaboration Intent Queue, and Autonomous Run Control routes. The receipt now carries `product-team-customer-agent-handoff/v1`, making the C-side approved roster, Leader/Reviewer, A-side session/tick, target stage, action lanes, and next run routes explicit instead of inferred from separate panels. Product Team Operating Loop consumes that handoff as `customerSide.handoff` and now derives `customerSide.handoffExecution` from Autopilot session/tick/run/submission receipts; Collaboration Intent Queue exposes the same handoff as a runnable Mission Handoff intent, links any observed A-side receipts/submissions into the row, and can now run the row through `POST /projects/:id/collaboration-intent-queue/:intentId/run` with a persisted `collaboration-intent-run/v1` receipt. `npm run agents:mission-runner:startup` is the focused low-write real Node HTTP gate for this startup and handoff path, including first A-side Agent submission, Flow Graph, and Proof Map evidence. React approval now uses this route for real backend projects, so the old kickoff/initiate path is no longer the primary C-side start path. It is local/private-MVP mission startup proof, not public-production autonomy.
- Private pilot launch readiness is exposed by `GET /projects/:id/pilot-launch-readiness` and Manager Ready Package. It returns `pilot-launch-readiness/v1` with a private-pilot go/no-go decision, production no-go decision, launch packet checksum, evidence routes, failed gates, and production blockers aggregated from MVP readiness, proof routes, security boundary, provider readiness, managed persistence, queue adapter, operations readiness, and incident drill receipts.
- Deployment preflight is exposed by `GET /projects/:id/deployment-preflight` and Manager Ready Package. It returns `deployment-preflight/v1` with private-pilot deployment blocker gates, warnings, production controls, backend store/scheduler status, access-control hardening flags, secret-vault readiness, provider policy status, adapter status, adapter gateway preflight, operations readiness, and a checksum while keeping `productionDeploymentReady: false`.
- Production launch audit is exposed by `GET /projects/:id/production-launch-audit` and Manager Ready Package. It returns `production-launch-audit/v1` with unified private-pilot and production decisions, private-pilot gates, private-pilot handoff gates, production gates, audit-integrity gates, evidence routes, project evidence handoff summary, production evidence integrity, production blockers, next-shortest-path guidance, and a checksum. The product-team Harness now requires private-pilot `go` for the completed acceptance project, points next-shortest-path to `private-pilot-handoff` until the local evidence export package is download-audited, keeps production `no-go`, and verifies that the managed-production evidence-integrity gate only passes after signed `managed-production` receipts.
- Production infrastructure rehearsal is exposed by `GET /projects/:id/production-infrastructure-rehearsal` and Manager Ready Package as `production-infrastructure-rehearsal/v1`. It aggregates adapter gateway, managed persistence dry-run, worker queue dry-run, deployment preflight, production deployment receipts, operations readiness, production operations readiness, and production launch gaps into one local/private infrastructure rehearsal boundary; the same contract now carries `managedCutoverGates` / `managedCutoverSummary` so C-side operators can see which managed-production cutover gates have receipt proof and which gate blocks public production next. Readiness Proof Map exposes `productionInfrastructureRehearsalRoutes`, Manager Flow Graph emits `production-infrastructure-rehearsal`, Manager UI renders the route, cutover gates, and a managed cutover attestation action that calls `POST /projects/:id/managed-infrastructure-cutover-attestations`, and product-team Harness keeps public production blocked until real managed-production cutover evidence exists.
- Production launch gap register is exposed by `GET /projects/:id/production-launch-gap-register` and Manager Ready Package as `production-launch-gap-register/v1`. It normalizes production blockers from launch audit, production evidence integrity audit, deployment preflight, production operations, provider, evidence custody, artifact quality, and review governance into owner/domain/action rows with proof ids, timeline/event ids, upstream checksums, next action, Flow Graph node, Readiness Proof Map `productionLaunchGapRoutes`, Security Boundary route policy, Manager UI panel, and `npm run agents:product-team` coverage while keeping `readyForProduction: false`.
- Production launch control center is exposed by `GET /projects/:id/production-launch-control-center` and Manager Ready Package as `production-launch-control-center/v1`. It aggregates the launch audit, gap register, private-pilot go-live state, global public-production startup readiness, production operations receipts, production deployment receipts, production security receipts, production provider receipts, production evidence integrity audit, launch approvals, deployment preflight, provider readiness, security boundary, custody, and artifact quality into release gate rows, owner rows, stage rows, next action, proof ids, timeline/event ids, upstream checksums, Flow Graph node, Readiness Proof Map `productionLaunchControlCenterRoutes`, Security Boundary route policy, Manager UI panel, and product-team Harness coverage while keeping public production `no-go`.
- Production launch evidence dossier is exposed by `GET /projects/:id/production-launch-evidence-dossier` and Manager Ready Package as `production-launch-evidence-dossier/v1`. It packages the launch audit, gap register, control center, evidence integrity audit, private-pilot go-live state, deployment/security/provider/operations readiness, and production receipt workflows into a route manifest, four production control domains, open gaps, proof/timeline/event ids, checksums, Flow Graph dossier node, Readiness Proof Map `productionLaunchEvidenceDossierRoutes`, Security Boundary route policy, Manager UI panel, and product-team Harness coverage while keeping public production `no-go` until real managed-production evidence and all launch controls close.
- Production evidence integrity audit is exposed by `GET /projects/:id/production-evidence-integrity-audit` and Manager Ready Package as `production-evidence-integrity-audit/v1`. It reads operations, deployment, security, and provider control receipts, classifies each required control as `missing`, `local-rehearsal`, `external-unattested`, or `managed-production`, and exposes domain rows, control rows, proof ids, timeline/event ids, backend routes, summary counts, checksum, Manager Flow Graph node, Readiness Proof Map `productionEvidenceIntegrityRoutes`, Security Boundary route policy, Manager UI panel, and product-team Harness coverage. The Harness now verifies that local/test receipt evidence does not become production evidence, that unsigned `evidenceEnvironment: "managed-production"` claims remain external-unattested, that signed managed-production receipts can upgrade the audit, and that this upgrade closes the matching launch-audit/gap-register blocker without turning the broader product into a public-production overclaim.
- Launch approval workflow is exposed by `GET|POST /projects/:id/launch-approvals` and Manager Ready Package. It returns checksummed `launch-approval-workflow/v1`, persists `launch-approval/v1` records for private-pilot and production release modes, requires Manager + security-admin approval for private pilot, requires Manager + security-admin + operations-owner for production, and mirrors approval proof into Manager Flow Graph, Readiness Proof Map, event ledger, timeline, production launch audit, and the `launch_approvals` persistence/migration seed path. `npm run agents:product-team:production-launch-governance` verifies the production approval path through signed membership access without turning the broader public-production decision to go.
- Project evidence archive is exposed by `GET /projects/:id/project-evidence-archive` and summarized in Manager Ready Package. The standalone route returns full `project-evidence-archive/v1` redacted contents with manifest checksums, integrity gates, final-deliverable/transcript/evidence/review/Flow-Graph proof, readiness summaries, persistence summary, worker recovery summary, and raw-secret scan status; Manager Ready Package keeps a manifest-only snapshot with the same route, status, gates, counts, and checksums for dashboard responsiveness. This closes the private-pilot/customer handoff proof surface while keeping production-grade export/download infrastructure as M4/M5 work.
- Project evidence export governance is exposed by `GET|POST /projects/:id/project-evidence-exports` and summarized in Manager Ready Package. It returns `project-evidence-export-workflow/v1`, persists `project-evidence-export/v1` request/approval/download-audit rows, pins requests to archive checksums, requires Manager + security-admin approval for private-pilot handoff, mirrors proof into Manager Flow Graph, Readiness Proof Map, timeline, event ledger, route policy, and `project_evidence_exports` persistence rows, and now returns a local `project-evidence-export-package/v1` descriptor after download audit with archive manifest checksums, watermark metadata, retention/data-residency metadata, package gates, and a receipt. It keeps `readyForProductionExport: false` until encrypted object storage, signed download URLs, watermark enforcement, retention deletion, centralized download audit, and data-residency controls exist.
- Production persistence snapshot is exposed by `GET /projects/:id/persistence-snapshot`, giving the future managed database migration a normalized table/checksum/integrity contract while the app still uses the local JSON file store.
- Managed persistence migration plan is exposed by `GET /projects/:id/persistence-migration-plan`, deriving Postgres-compatible table plans, seed batches, RLS guidance, critical-table coverage, verification gates, and cutover steps from the current persistence snapshot.
- Managed persistence dry-run verification is exposed by `GET /projects/:id/persistence-migration-dry-run`, simulating the adapter import contract against the snapshot to check seed coverage, row counts, checksums, primary-key uniqueness, RLS guidance, and migration-plan gates before a real managed database adapter is wired in.
- Managed persistence adapter plan and dry-run are exposed by `GET /projects/:id/persistence-adapter-plan` and `GET /projects/:id/persistence-adapter-dry-run`, turning the database cutover blocker into a concrete adapter contract with critical table coverage for membership/replay/audit/provider/worker/read-model records plus a configurable adapter status facade. The default `local-shadow` driver produces local execution receipts for shadow-read parity, transaction rollback, backup/restore, RLS coverage, audit-stream continuity, and read-model checkpoint gates. `http-json` now has an async gateway dry-run path through the project API when `MANAGED_PERSISTENCE_HTTP_ENDPOINT` or `ADAPTER_GATEWAY_HTTP_ENDPOINT` is configured; `postgres` remains a future real-driver target.
- Worker queue snapshot is exposed by `GET|POST /workers/queue-snapshot` and `GET|POST /projects/:id/worker-queue`, giving future queue/cron infrastructure a due-row, priority, idempotency, lease, retry/dead-letter, execution receipt, and recovery-route contract while the app still uses the local Node scheduler.
- Worker queue adapter plan and dry-run are exposed by `GET /projects/:id/worker-queue-adapter-plan` and `GET /projects/:id/worker-queue-adapter-dry-run`, turning the queue/cron blocker into a configurable adapter contract for project, Agent, and Autopilot-session worker lanes. The default `local-shadow` driver produces local execution receipts for enqueue, durable lease acquisition, dispatch, receipt acknowledgement, retry import, dead-letter recovery, queue inspection, `worker-queue-adapter-snapshot-parity/v1` queue/lease/ack/dead-letter parity, and adapter gate checks. `http-json` now has an async gateway dry-run path through the project API when `WORKER_QUEUE_HTTP_ENDPOINT` or `ADAPTER_GATEWAY_HTTP_ENDPOINT` is configured; `managed-queue` remains a future real queue adapter target.
- Adapter gateway contract validation is exposed by `npm run adapters:gateway`, which spins up local mock `http-json` gateways, verifies shared health, managed persistence execution receipt, and worker queue execution receipt shapes, then proves the project API dry-run routes can execute through that gateway before a real private adapter gateway exists.
- A runnable private adapter gateway reference process is exposed by `npm run adapters:gateway-server`. It serves `GET /health`, `GET /state`, `POST /persistence/dry-run`, and `POST /worker-queue/dry-run`, supports bearer auth through `ADAPTER_GATEWAY_AUTH_TOKEN`, persists imported shadow table records, queue rows, queue leases, dead-letter rows, and receipt summaries through the `ADAPTER_GATEWAY_STORAGE_DRIVER` adapter contract, and is verified by `npm run adapters:gateway-server:validate` against the shared client contract, backend project API dry-run routes, and `GET /projects/:id/adapter-gateway-preflight` live health/state/capability checks. The current drivers are `json-file`, `memory`, and `postgres` / `postgres-compatible`; `npm run adapters:gateway-postgres-store:validate` proves the Postgres-compatible schema plan, query-bound write operations, and snapshot/count readback parity with a fake query shim. A real managed database/queue driver with real database readback remains the next production gap.
- Operations readiness is exposed by `GET /projects/:id/operations-readiness`, combining worker proof, queue contract, queue adapter dry-run, execution receipts, retry/dead-letter recovery metrics, audit stream ordering plus hash-chain verification, persistence integrity, migration dry-run status, proof surfaces, alert-rule drafts, a recovery runbook, and `operations-incident-drill/v1` rehearsal receipts into one local operations contract; production still needs centralized observability, incident ownership, backup/restore drills against real infrastructure, durable queue leases, managed dead-letter storage, and real alert routing.
- Provider readiness is exposed by `GET /projects/:id/provider-readiness` and Manager Ready Package, turning the real-provider rollout blocker into a local contract with redacted model/search status, deterministic validation-provider proof, provider-backed evidence provenance, source-safety review, proof routes, leak scanning, provider control policy, allowlists, budgets/rate limits, Agent tool grants, retry policy, circuit-breaker policy, local secret-vault seal/open/rotation receipt status, provider usage/cost ledger rows, and explicit remaining production controls for managed KMS, revocation, managed provider audit storage, centralized alerting, and real-provider incident handling.
- Provider controlled run planning is exposed by `GET /projects/:id/provider-controlled-run` and Manager Ready Package as `provider-controlled-run/v1`. It evaluates the model/search operations needed for a private-pilot BYOK run without issuing provider calls: provider health checks, kickoff/intent model support, model artifact drafting, and evidence search. The plan checks provider allowlists, Agent tool grants, budget/rate headroom, retry/circuit state, usage-ledger proof, model human-review boundary, evidence governance, redaction, production controls, Proof Map route coverage, access policy, and Manager UI visibility. Production remains blocked until real provider eval runs, managed provider audit storage, centralized cost alerting, production incident runbooks, and calibrated human release policy exist.
- Settings Integrations now writes local MVP Agent tool grants through `project-settings/v1` as `project-tool-grant-policy/v1`. The External Tools panel controls provider-test, model kickoff/intent/artifact-draft, and search-evidence defaults; Provider Readiness and Provider Controlled Run consume the same policy, so removing `search:evidence` produces an `agent-tool-grant-missing` denial before any provider call. This replaces the browser-only tool-grant placeholder while production task-scoped authorization, managed runtime identity, centralized provider audit, and managed policy storage remain open.
- Settings Integrations now also reads `project-integration-capabilities/v1` from `project-settings/v1`. The UI shows provider budget and Agent tool grants as backend-backed editable rows, while Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting are backend-backed read-only route contracts pointing to Adapter Gateway preflight, Provider Readiness, Evidence Index readiness, Budget Alert readiness, and Error Reporting readiness instead of missing-backend placeholders. `npm run agents:project-settings:integrations` validates this contract without claiming those integrations are production-ready.
- Settings Integrations now also reads `settings-integration-readiness/v1` from `GET /projects/:id/settings-integration-readiness`. That aggregate route turns the capability rows, route-backed readiness models, action-required states, checksums, and production blockers into one Settings-visible project contract; Manager Ready Package embeds `settingsIntegrationReadiness`, Readiness Proof Map exposes `settingsIntegrationReadinessRoutes`, and `npm run agents:settings-integration-readiness` validates the formal proof surface.
- Settings Workspace now reads `project-workspace-policy/v1` and `project-workspace-capabilities/v1` from `project-settings/v1`. The UI shows global interface language as browser-local; project language, interface density, default visibility, autosave cadence, local workspace binding, runtime contract rules, long-term memory readiness, and meeting summaries as backend-backed. Local workspace binding writes through `POST /projects/:id/workspace/bind` and reads back through `GET /projects/:id/local-runtime`, giving the local/private MVP a real filesystem workspace entry instead of a browser-only path. Runtime contract rules point to `GET /projects/:id/runtime-contracts`, long-term memory points to `GET /projects/:id/memory-readiness`, and both remain read-only in Settings. Manager Ready Package now includes `projectMemoryReadiness`, and Readiness Proof Map exposes `projectMemoryReadinessRoutes`, so memory readiness is part of the formal proof surface. `npm run agents:project-settings:workspace` validates the workspace policy write, workspace bind/local-runtime readback, runtime-contract route, memory-readiness route, Manager Ready Package proof, Readiness Proof Map proof, meeting-summary transcript/timeline/event proof, audit, timeline, event-ledger, and file-backed persistence without claiming those workspace controls are production-ready.
- Provider eval shadow replay is exposed by `GET|POST /projects/:id/provider-eval-runs` and Manager Ready Package as `provider-eval-run-workflow/v1`. It records no-call `provider-eval-run/v1` receipts that replay the controlled-run operation plan from provider usage-ledger proof, require critical `model:artifact-draft` and `search:evidence` proof, write timeline/event proof, add Manager Flow Graph monitoring nodes, expose Readiness Proof Map `providerEvalRunRoutes`, and persist `provider_eval_runs` rows. Provider eval writes support `includeReadModels: false`, returning lightweight provider/Manager refresh routes instead of embedding large Manager snapshots. This proves local private-pilot provider-eval rehearsal readiness while keeping public production blocked until real provider eval datasets, managed eval storage, centralized provider cost alerts, incident runbooks, and calibrated release policy exist.
- Production provider control receipts are exposed by `GET|POST /projects/:id/production-provider-control-receipts` and Manager Ready Package as `production-provider-control-receipt-workflow/v1`. Runtime-platform or security admins can record checksummed `production-provider-control-receipt/v1` rows for provider allowlists, budgets/rate limits, Agent tool grants, retry/circuit breakers, provider audit/cost ledger, encrypted secret-vault proof, source safety, snapshots/provider receipts, model-output quality review, real-provider eval, managed audit/eval storage, centralized cost alerting, release policy, and incident runbooks. Receipts write timeline/event proof, add Manager Flow Graph nodes, expose Readiness Proof Map routes, appear in Security Boundary, render in Manager UI, export `production_provider_control_receipts` persistence/migration rows, and let the provider rollout domain become ready while the broader production launch control center can still remain `no-go`.
- Private pilot go-live readiness is exposed by `GET /projects/:id/private-pilot-go-live-readiness` and Manager Ready Package as `private-pilot-go-live-readiness/v1`. It aggregates delivery proof, release approvals, evidence handoff, provider eval, deployment/operations/security preflight, release candidate, launch run, post-launch health, customer acceptance, and production operations hardening receipts into stage rows, active phase, next action, proof ids, and checksum. Manager Flow Graph adds a `private-pilot-go-live-readiness` command node, Readiness Proof Map exposes `privatePilotGoLiveRoutes`, Security Boundary lists the route policy, Manager UI renders the stage panel, and `npm run agents:product-team` verifies in-process API, real HTTP, Ready Package summary, Flow Graph, Proof Map, enforced access, and UI source coverage while public production remains blocked by the broader launch audit.
- Private pilot release candidate freeze is exposed by `GET|POST /projects/:id/private-pilot-release-candidates` and Manager Ready Package as `private-pilot-release-candidate-workflow/v1`. It records checksummed `private-pilot-release-candidate/v1` receipts only after launch approvals, production launch audit private-pilot gates, evidence handoff package audit, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage are ready. The receipt freezes readiness/audit/evidence/provider/adapter checksums into timeline, event ledger, Manager Flow Graph, Readiness Proof Map, route policy, Manager UI, and `private_pilot_release_candidates` persistence rows. It closes the private-pilot release-candidate proof gap while keeping production blocked until real managed identity, database, queue, KMS, provider eval, centralized audit, deployment, and operations controls exist.
- Private pilot launch run activation is exposed by `GET|POST /projects/:id/private-pilot-launch-runs` and Manager Ready Package as `private-pilot-launch-run-workflow/v1`. It records checksummed `private-pilot-launch-run/v1` receipts from a frozen release candidate, requiring launch audit private-pilot go, audited evidence handoff package, archive/provider/proof/deployment/operations/security readiness, and runbook/incident-drill coverage. The receipt freezes release-candidate, launch audit, evidence, provider eval, deployment, operations, persistence adapter, and queue adapter checksums into timeline, event ledger, Manager Flow Graph, Readiness Proof Map, route policy, Manager UI, and `private_pilot_launch_runs` persistence rows. This turns the release candidate into a controlled private-pilot activation record without claiming public production go-live.
- Private pilot post-launch health is exposed by `GET|POST /projects/:id/private-pilot-launch-health-checks` and Manager Ready Package as `private-pilot-launch-health-check-workflow/v1`. It records checksummed `private-pilot-launch-health-check/v1` receipts after a launch run exists and operations, security, provider eval, evidence archive, persistence adapter, queue adapter, Flow Graph, and Proof Map gates remain healthy. The receipt freezes launch-run and health checksums into timeline, event ledger, Manager Flow Graph monitoring nodes, Readiness Proof Map, route policy, Manager UI, and `private_pilot_launch_health_checks` persistence rows. This closes the private-pilot monitoring proof gap while keeping public production blocked until centralized observability, alert routing, on-call ownership, managed incidents, and restore drills exist.
- Private pilot customer acceptance closeout is exposed by `GET|POST /projects/:id/private-pilot-acceptance-reports` and Manager Ready Package as `private-pilot-acceptance-report-workflow/v1`. It records checksummed `private-pilot-acceptance-report/v1` receipts after release candidate, launch run, post-launch health, evidence handoff, generic product-team delivery proof, operations/security/provider proof, Flow Graph, and Proof Map gates remain ready. The receipt freezes release/launch/health/evidence/Flow Graph/Proof Map checksums into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map, route policy, Manager UI, and `private_pilot_acceptance_reports` persistence rows. This closes the customer private-pilot acceptance proof gap while keeping public production blocked until managed infrastructure, centralized observability, on-call ownership, incident systems, and restore drills exist.
- Production operations readiness is exposed by `GET /projects/:id/production-operations-readiness` and Manager Ready Package as `production-operations-readiness/v1`. It aggregates private-pilot acceptance, post-launch health, local operations incident-drill proof, security audit-stream proof, provider eval replay, persistence adapter dry-run, worker queue adapter dry-run, and production launch audit into local proof gates plus production control gates. It can mark `readyForPrivatePilotOperations: true` after the customer acceptance proof, but keeps production operations blocked until centralized observability, alert routing, on-call ownership, managed incident systems, real restore-drill receipts, centralized audit retention, and managed database/queue cutover approval exist. `npm run agents:product-team:private-pilot:ops-readiness` now exposes this P1-to-P2 bridge as a focused Harness stage, and private-pilot receipt POST routes can return lightweight receipts plus refresh routes instead of embedding the full Manager Ready Package on every write.
- Production operations control receipts are exposed by `GET|POST /projects/:id/production-operations-control-receipts` and Manager Ready Package as `production-operations-control-receipt-workflow/v1`. Security admins or operations owners can record checksummed `production-operations-control-receipt/v1` rows for centralized logs, metrics, traces, alert routing, on-call, incident, restore, centralized audit, managed persistence, and managed queue controls. Receipts write timeline/event proof, add Manager Flow Graph nodes, expose Readiness Proof Map routes, appear in Security Boundary, render in Manager UI, and export `production_operations_control_receipts` persistence/migration rows. This moves operations hardening from a checklist to auditable evidence while still allowing the broader production launch audit to remain `no-go` for other unmanaged controls.
- Production deployment control receipts are exposed by `GET|POST /projects/:id/production-deployment-control-receipts` and Manager Ready Package as `production-deployment-control-receipt-workflow/v1`. Runtime-platform, operations-owner, or security-admin actors can record checksummed `production-deployment-control-receipt/v1` rows for enforced access, replay protection, audit fail-closed behavior, scheduler autostart, real persistence adapter, managed evidence custody, real queue adapter, environment promotion audit, rollback/smoke test, deployment change approval, and production domain/TLS. Receipts write timeline/event proof, add Manager Flow Graph nodes, expose Readiness Proof Map routes, appear in Security Boundary, render in Manager UI, and export `production_deployment_control_receipts` persistence/migration rows. This moves deployment hardening from a preflight checklist to auditable evidence while still allowing the broader production launch audit/control center to remain `no-go`.
- Production security control receipts are exposed by `GET|POST /projects/:id/production-security-control-receipts` and Manager Ready Package as `production-security-control-receipt-workflow/v1`. Security admins can record checksummed `production-security-control-receipt/v1` rows for managed identity provider, service identity boundary, managed KMS/Secret Manager, database-backed RBAC, centralized security audit, and session replay hardening. Receipts write timeline/event proof, add Manager Flow Graph nodes, expose Readiness Proof Map routes, update Security Boundary production-security status, render in Manager UI, and export `production_security_control_receipts` persistence/migration rows. This moves security hardening from a checklist to auditable evidence while still allowing the broader production launch audit/control center to remain `no-go` for provider, deployment, approval, or other unmanaged controls.
- Security boundary snapshot is exposed by `GET /projects/:id/security-boundary` and Manager Ready Package, giving the secret-vault/RBAC blocker a route policy, enforced access-policy, optional signed access-header contract, optional signed-request replay contract, optional audit fail-closed contract, optional project membership contract, local encrypted secret-vault contract, sensitive-field, redaction-scan, and production hardening contract while the prototype still lacks real identity/session infrastructure.
- `accessControl.js` provides prototype enforced-mode decisions for Manager, Agent, Reviewer, runtime, security-admin, and observer roles, including Agent self-scope, Reviewer identity match, sensitive export denial, optional HMAC-signed identity headers when `AGENT_ACCESS_SIGNING_SECRET` is configured, optional file-backed signed request id replay checks when `requireSignedRequestIds` / `AGENT_ACCESS_REPLAY_PROTECTION` is enabled, optional audit fail-closed rejection when `failClosedOnAuditError` / `AGENT_ACCESS_AUDIT_FAIL_CLOSED` is enabled, and persisted project membership policy checks with runtime bindings, revocations, revision audit, and `project_membership_policies` / `project_membership_grants` persistence rows when the API enables `requireProjectMembership`; `access_replay_records`, migration-plan verification gates, and migration dry-run gates are also covered by `npm run agents:product-team`.
- Security access audit is exposed by `GET /projects/:id/security-access-audit` and `GET /projects/:id/security-audit-stream`, written into project state, linked into the event ledger, persisted into the backend store-level audit stream with sequence/checksum plus tamper-evident hash-chain proof, mirrored to an append-only JSONL audit sink, and exported through the persistence snapshot as `security_access_audit` plus `security_audit_stream`; production still needs immutable centralized audit storage.
- Evidence custody readiness is exposed by `GET /projects/:id/evidence-custody-readiness` and Manager Ready Package, turning source snapshots, provider receipts, and Reviewer source decisions into checksummed local custody rows with Flow Graph custody nodes, Proof Map custody routes, archive coverage, and an explicit managed-storage production blocker.
- Agent artifact draft generation is exposed by `POST /projects/:id/agents/:agentId/artifact-drafts` as `agent-artifact-draft/v1`. The backend can generate a generic product-team draft from task context, evidence searches, prior submissions, and reviews, then submit it through the standard Agent submission contract with `submit: true`. Manager Dashboard, Agent Dashboard, archive, and persistence rows preserve generated-draft provenance plus `artifact-draft-quality/v1` status. `npm run agents:product-team` now requires both local fallback and deterministic model-backed `progress-brief` drafts to appear in Flow Graph, Proof Map, Task Evidence, artifact files, archive, UI-visible dashboard rows, provider usage ledger, and persistence rows; model-backed drafts must carry a local quality-ready gate and explicit human-review requirement.
- Kickoff meeting generation now exposes `kickoff-generation-provenance/v1` on backend meeting sessions and initiation proof cards. Deterministic validation, model-provider rehearsal, provider-backed model generation, and development fallback are separated, and all modes keep production claims blocked until production provider controls are approved. `npm run agents:scenario` and `npm run agents:product-team` both assert that deterministic kickoff generation is validation-only.
- Brainstorm Layer is exposed by `GET /projects/:id/brainstorm-layer` and Manager Ready Package as `brainstorm-layer/v1`. It aggregates generic `brainstorm-board` submissions, parses visible alternatives, links discovery/evidence/downstream decision and delivery artifacts, adds a Flow Graph `brainstorm-layer` aggregate node, exposes Readiness Proof Map `brainstormLayerRoutes`, renders in Manager UI, and is covered by `npm run agents:product-team` without turning the validation sample into a research-only workflow.
- Agent brainstorm contribution is now visible in `GET /projects/:id/agents/:agentId/dashboard` as `agent-brainstorm-contribution/v1`. The submitting Agent sees their own brainstorm boards, parsed alternatives, proof/timeline/event ids, project evidence/downstream follow-through counts, and route back to the Manager brainstorm layer. This strengthens the Agent initiative/self-positioning loop while keeping cross-team synthesis in the Manager aggregate.
- Artifact quality audit is exposed by `GET /projects/:id/artifact-quality-audit` and Manager Ready Package as `artifact-quality-audit/v1`. It audits all generic Agent submissions for artifact type coverage, title/summary/body readiness, chat/timeline/event/artifact proof, review/revision/final-deliverable closure, generated-draft quality status, redaction, production controls, and checksum. The product-team Harness now requires it through in-process API, real HTTP backend, Ready Package summary, Readiness Proof Map, Project Evidence Archive manifest/contents, Pilot/Production launch evidence routes, security route policy, and Manager UI snapshot. Production content quality remains blocked until calibrated rubrics, eval datasets, human release policy, managed output audit storage, and retention controls exist.
- Submission review workflow is exposed by `GET /projects/:id/submission-review-workflow` and Manager Ready Package as `submission-review-workflow/v1`. It aggregates generic submission review rounds, requested changes, linked revision responses, final-deliverable acceptance, proof routes, timeline/event ids, local closure gates, and a production review-governance blocker. Manager Flow Graph now adds a `submission-review-workflow` closure node, Readiness Proof Map exposes `submissionReviewWorkflowRoutes`, Security Boundary lists the route policy, Manager UI renders the workflow panel, and Manager Dashboard submission rows can now POST Reviewer verdicts/comments/requested changes through the backend review route with lightweight receipts. The workflow panel and Dashboard submission-review rows now expose per-review backend review/submission/response routes plus chat and timeline proof buttons, selected Flow Graph submission/final-deliverable nodes expose backend-provided `submissionId` / `submissionRoute`, and evidence/search plus review nodes expose backend-provided `evidenceSearchRoute`, `evidenceSourceReviewRoute`, and `submissionReviewRoute`, so the evidence -> review -> revision -> final acceptance loop can be inspected record by record. Failed Reviewer writes now replace temporary pending UI receipts with `submission-review-failed` and keep the Reviewer draft instead of displaying a fake or stuck pending proof. The Agent Workbench can submit linked revision-note and final-deliverable records with `respondsToReviewId`, `revisesSubmissionId`, and final `supersedesSubmissionIds`. `npm run agents:product-team` verifies standalone API, real HTTP, Flow Graph, Proof Map, enforced access, and Ready Package summary coverage; `npm run ui:manager-backend` now verifies the same kickoff -> brainstorm-board -> evidence search -> generated product-brief draft -> Reviewer decision -> revision-note -> final-deliverable -> accepted review loop on a real initiated browser project while production reviewer policy and immutable output audit remain blocked.
- Product Team Delivery Trace is exposed by `GET /projects/:id/product-team-delivery-trace` and Manager Ready Package as `product-team-delivery-trace/v1`. It joins kickoff, Agent self-marketing, brainstorm layer, evidence quality, generated draft, review/revision closure, accepted final deliverable, and proof surfaces into one generic C-side delivery chain. Autopilot sessions/ticks now snapshot this trace as `autopilot-delivery-target/v1`, making each autonomous run show ready/missing delivery stages instead of only step counts. The product-team Harness verifies API, HTTP, Ready Package summary, Proof Map route, and Manager UI coverage, so Research Project remains a validation sample rather than a custom workflow.
- Project Zero-to-Autonomy Report is exposed by `GET /projects/:id/zero-to-autonomy-report` and Manager Ready Package as `project-zero-to-autonomy-report/v1`. It aggregates Settings/BYOK startup, kickoff/self-marketing, workspace binding, C/A handoff, autonomous Agent output, provider evidence/source review, brainstorm, draft/review/revision/final delivery, generic artifact coverage, proof surfaces, Project Evidence Archive, and public-production blockers into one operator-readable route. The provider evidence/source review stage now requires provider-backed sources, Reviewer source decisions, search/model provider usage proof rows, and provider receipt proof rows before the report can represent the local run as complete. Manager Flow Graph renders `zero-to-autonomy-report`, Readiness Proof Map exposes `zeroToAutonomyReportRoutes` with provider usage proof ids, provider receipt proof ids, and provider audit routes, and React must fail closed when the route is missing instead of computing a local readiness claim. This strengthens P0/P1 validation while keeping public production blocked.
- Product Team Operating Loop is exposed by `GET /projects/:id/product-team-operating-loop` as `product-team-operating-loop/v1`. It is the backend C/A coordination summary over delivery trace, Autonomous Run Control, Agent and Manager queues, worker queue, Proof Map, timeline, and event evidence. It now includes `product-team-customer-agent-handoff-execution/v1`, a backend read-model summary of the latest Mission Runner handoff session, tick ids, run/loop receipt ids, result message ids, Agent submission ids, output rows, proof ids, timeline ids, and event ids. The local gate requires Manager continuation, Agent strategy selection, generic delivery closure, artifact/review/revision/final submission coverage, proof surfaces, and scheduler/worker continuation while keeping production autonomy blocked for managed queue/cron, durable audit, provider/BYOK policy, cost controls, customer acceptance policy, and incident recovery. The Research validation sample now verifies the route through the real HTTP server.
- Team Collaboration Diagnostics is exposed by `GET /projects/:id/team-collaboration-diagnostics` as `team-collaboration-diagnostics/v1`. It is the backend break-diagnostics surface for overall scheduling: kickoff, role governance, self-marketing, transcript proof, task ownership, brainstorm/evidence handoff, artifact/review/revision/final closure, operating-loop continuation, Flow Graph, Proof Map, timeline, and event proof. Manager Ready Package embeds it, Flow Graph adds `team-collaboration-diagnostics`, Readiness Proof Map exposes `teamCollaborationDiagnosticRoutes`, React proof sync renders the panel, and the Research validation sample verifies API, HTTP, Flow Graph, and Proof Map coverage while production autonomy stays blocked.
- Runtime Contract Freeze is exposed by `GET /projects/:id/runtime-contracts` as `runtime-contract-freeze/v1`. It freezes the local MVP route/schema coverage for Agent submissions, artifact storage proof, evidence search/source/provider proof, Reviewer review, revision/final delivery, transcripts, timeline, event ledger, Flow Graph, Proof Map, Operating Loop, and Team Collaboration Diagnostics. Manager Ready Package embeds it, Flow Graph adds `runtime-contract-freeze`, Readiness Proof Map exposes `runtimeContractFreezeRoutes`, React proof sync renders the panel, and product-team gates verify API, HTTP, Flow Graph, and Proof Map coverage while production runtime approval stays blocked.
- 确定性 runtime（`agentRuntime.js`）用于 UI 演示
- 自治 ledger、agentStates、obligation 持久化契约
- 通用 Agent submission / artifact / evidence-search / submission-review 后端契约，已由 `npm run agents:product-team` 覆盖 discovery-report、brainstorm-board、evidence-packet、product-brief、decision-proposal、risk-review、revision-note、implementation-plan 和 final-deliverable 的 Research Project 验收样例后端链路

**尚未完成：**
- LLM 驱动的真实工作产出（当前已有 artifact draft 后端契约、local fallback、deterministic model-backed provider 验收、`artifact-draft-quality/v1` 本地质量 gate、`artifact-quality-audit/v1` 通用产物质量审计、`provider-controlled-run/v1` 受控运行 dry-run、`provider-eval-run/v1` 影子回放收据与模型草稿人工复核证据，但仍需真实 BYOK model provider、真实 eval datasets、校准后的输出质量评估、生产级重试/限流、审批策略和成本控制才能用于生产）
- 生产级 managed database adapter 替代当前 JSON/file store，并把现有 `postgres` / `postgres-compatible` gateway schema/upsert/readback rehearsal 升级为真实数据库连接下的 shadow-read、backup/restore、rollback、RLS 与 cutover 演练
- 生产级 queue/cron scheduler 替代当前本地 Node interval runner，并实现 `http-json` 或 `managed-queue` driver，把本地 queue shadow execution receipt 升级为真实 durable lease store、managed dead-letter storage 与恢复演练
- 将 `adapterGatewayStore.js` 的 `json-file` / `memory` / `postgres-compatible` storage adapter rehearsal 替换为真实私有 adapter gateway 后端：托管数据库真实 readback parity、shadow-read、backup/restore、rollback、RLS 验证，以及真实 durable queue lease / managed dead-letter storage
- 将 `npm run adapters:gateway`、`npm run adapters:gateway-server:validate`、`GET /projects/:id/adapter-gateway-preflight` 和项目级 dry-run API 纳入针对真实私有 adapter gateway 的部署前验收
- Agent 提交系统的生产级 UI 操作、多轮评审版本管理、真实外部搜索适配与生产级持久化
- 证据保管从本地 checksum custody rows 升级为托管不可变对象存储、签名访问、保留/删除任务和集中 custody audit
- 运行时与 Skill 包的热更新策略
- 性能与成本控制（token、并发、缓存）

**Contributor 可参与：**
- 扩展 `npm run agents:scenario` 覆盖更多项目形态
- 文档化 runtime 输入/输出契约，便于未来替换 LLM backend

**退出标准：**
- [x] Runtime 契约已以本地 MVP 合同清单冻结并文档化（`GET /projects/:id/runtime-contracts` / `runtime-contract-freeze/v1`，由 product-team gates 验证 API、HTTP、Flow Graph、Proof Map 与 UI source coverage）
- [x] 自治周期在 N 轮模拟中状态一致（local MVP: `GET /projects/:id/autonomous-cycle-consistency` / `autonomous-cycle-consistency/v1`; `npm run agents:product-team:cycle-consistency` proves the HTTP three-step loop through signed membership access; verification covers API, Flow Graph, Proof Map, Ready Package, persistence, and UI source coverage; production remains blocked by managed queue/cron, distributed leases, centralized audit, cost controls, and incident recovery）
- [x] Private-pilot Harness exits can stop after release candidate, launch run, post-launch health, customer acceptance, or post-acceptance production-operations readiness, making the Research/Product acceptance sample usable as a staged scheduling instrument rather than a single opaque marathon.
- [x] Research Project 验收样例能跑通信息搜集、内容撰写、脑暴节点、群聊讨论、产出提交、正式评审与最终交付，但底层 artifact / submission / evidence-search / review 契约保持通用（`npm run agents:product-team:smoke` / `npm run agents:product-team:research-sample` / `npm run agents:product-team:core` / `npm run agents:product-team:private-pilot`；旧 file-backed core 通过 `agents:product-team:core:file-backed` 显式运行）
- [x] Agent submission / evidence-search / review node 契约已在本地 MVP 范围冻结，并能从任务、群聊、事件账本、工作区文件、Flow Graph 与 Proof Map 追溯证据（生产级不可变存储、真实 provider/BYOK、managed database/queue 仍在 M4/M5）
- [x] Pluggable LLM adapter contract is covered for the local MVP: `model-provider-adapter-manifest/v1` plus `npm run agents:model-provider-adapter` verify OpenAI-compatible/OpenAI/Anthropic/Gemini adapter selection, fake-fetch request mapping, missing-key fail-closed behavior, and secret redaction. Production LLM rollout still needs managed KMS, centralized provider audit, calibrated eval datasets, retry/circuit policy, cost alerts, and incident runbooks.

---

### M4 · 安全与 BYOK 🔐 原型启动

**目标：** 用户自带 API Key 时，密钥与数据不出用户边界；满足最低安全基线后再讨论对外使用。

**已有原型：**
- 模型 provider 与搜索 provider 的后端状态接口，状态响应不返回原始 API Key
- evidence search 可通过 deterministic provider 跑通，未来可替换为私有 `http-json` 搜索网关
- `GET /projects/:id/provider-readiness` 已将真实 provider rollout 的剩余差距显式化，包含 provider control policy、allowlist、预算/限流、Agent 工具授权、retry policy、circuit-breaker policy、本地 secret-vault status、provider usage/cost ledger、provider-backed evidence source-safety summary，MVP readiness 的 `production-real-providers` blocker 会指向该路由；`GET /projects/:id/provider-controlled-run` 进一步把私有试点 BYOK 运行所需的模型/搜索操作做成 policy dry-run，不发起真实调用但显示每个操作的策略、预算、熔断、proof、人审和生产 blocker；`GET|POST /projects/:id/provider-eval-runs` 会把该计划回放为可审计的 shadow replay 收据并持久化到 `provider_eval_runs`；`GET|POST /projects/:id/production-provider-control-receipts` 会把 provider 上线控制从 checklist 推进为可提交、可落图、可迁移的生产证据收据并持久化到 `production_provider_control_receipts`
- `.env.example` 记录模型/搜索 provider 配置边界，默认关闭搜索 provider
- `secretRedaction.js` 已覆盖 provider 状态/错误、evidence source、Agent submission/sourceRefs、review comments、task sourceRefs、workspace artifact draft 与 ledger payload 的原型级写入前脱敏
- `secretVault.js` 已提供本地 AES-GCM envelope vault 契约，可通过 `SECRET_VAULT_ENABLED` / `SECRET_VAULT_KEY` 启用；Provider Readiness 与 Security Boundary 会展示 vault ready、encrypted record count 与 raw secret record count；`public-production-startup-readiness/v1` 现在还暴露 `managed-secret-manager-readiness/v1`，要求 managed provider、endpoint/key 配置和成对 attestation id/checksum，并证明只填 Secret Manager/KMS env 不能让本地 vault 变成生产可用。生产仍需托管 KMS/Secret Manager、轮换、撤销与访问审计
- `public-production-startup-readiness/v1` 现在也暴露 `managed-infrastructure-cutover-readiness/v1`，把 managed database / worker queue 的配置、`*_REQUIRE_REAL_ADAPTER`、local-shadow 状态和 managed-production cutover proof 分开显示；只填 database URL 或 queue endpoint 不能让 P2 public production 通过，仍需真实托管 cutover receipt。
- `public-production-startup-readiness/v1` 现在还暴露 `production-operations-startup-readiness/v1`，把 observability、audit retention、incident response、restore drill 的配置和 signed managed-production evidence receipts 分开显示；只填 logs/metrics/traces、paging、incident、restore、audit endpoint 或未签名 receipt id/checksum 不能让 P2 public production 通过。
- `public-production-startup-readiness/v1` 现在还暴露 `managed-identity-startup-readiness/v1`，把 managed identity provider metadata、service identity audience/subject 和 signed managed-production identity receipt 分开显示；只启用 `AGENT_ACCESS_SIGNING_SECRET` 或只填身份 provider URL 不能让 P2 public production 通过。
- `public-production-startup-readiness/v1` 现在还暴露 `production-cost-control-startup-readiness/v1`，把 production provider budget/rate policy、usage/cost audit sink、cost alert routing 和 signed managed-production cost-control receipt 分开显示；只设置项目预算或只填成本审计/告警 endpoint 不能让 P2 public production 通过。
- `public-production-startup-readiness/v1` 现在还暴露 `production-data-governance-startup-readiness/v1`，把 retention policy、deletion job、export storage、data residency 和 signed managed-production data-governance receipt 分开显示；只设置项目隐私偏好或只填导出/删除 endpoint 不能让 P2 public production 通过。
- `public-production-startup-readiness/v1` 现在还暴露 `production-traffic-startup-readiness/v1`，把 production domain/TLS、traffic gateway、health check、release approval、rollback smoke test 和 signed managed-production traffic-control receipt 分开显示；只填公网 URL、健康检查或回滚 endpoint 不能让 P2 public production 通过。
- `public-production-startup-readiness/v1` 现在还暴露 `production-customer-acceptance-startup-readiness/v1`，把 customer production acceptance policy、success criteria、acceptance threshold、approval authority、rollback criteria 和 signed managed-production customer acceptance receipt 分开显示；private-pilot acceptance 或只填客户验收策略 env 不能让 P2 public production 通过。
- `npm run agents:product-team` 会注入假密钥并扫描 file store 与 workspace artifact files，验证 BYOK 风格秘密不会以原文持久化
- `GET /projects/:id/security-boundary` 会导出路由策略、敏感字段清单、provider 状态边界、脱敏扫描摘要和生产安全 blocker，并由 `npm run agents:product-team` 验证
- `AGENT_ACCESS_SIGNING_SECRET` 可开启原型级签名身份头校验，强制访问模式会先验签再执行角色策略；这降低本地/内测部署中的头部伪造风险，但仍不是正式身份系统
- `AGENT_ACCESS_REPLAY_PROTECTION` 可要求签名请求携带 `x-hofs-request-id`，本地文件后端会在签名有效期内持久记录已接受的 request id，并在 API 重启后继续拒绝重复 request id；生产多实例仍需要共享 replay store
- `AGENT_ACCESS_AUDIT_FAIL_CLOSED` 可要求强制访问在审计写入失败时直接拒绝；生产仍需要集中不可变审计、告警和恢复流程
- `GET|PUT /projects/:id/membership-policy` 可把 `project-membership-policy/v1` 存为项目状态，包含 manager/security/observer/runtime/Agent/Reviewer 授权、Agent runtime 绑定、撤销列表、revision 和事件账本审计；`requireProjectMembership` 会在角色/签名之后读取该持久策略，拒绝不属于该项目或已撤销的 manager、Agent runtime 或 Reviewer；生产仍需要数据库成员表、邀请/撤销流程和行级权限
- `GET|POST /projects/:id/identity-sessions` 可签发、列出和撤销本地 `identity-session/v1` 运行凭证；token 只在签发响应中返回一次，项目状态只保存 token hash/checksum/status/expiry/revocation proof，`x-hofs-session-token` 可在 MVP 验收中替代重复签名头访问项目 proof route。会话使用会进入 security access audit、backend audit stream、event ledger、persistence snapshot 和 Manager Ready Package 安全边界摘要；生产仍需要正式 IdP、持久 session store、服务身份签发、轮换、audience binding、集中审计和多实例 replay 防护

**尚未完成：**
- API Key 加密存储与 scope 隔离
- 沙箱化 tool / file 访问
- Prompt 注入与跨 Agent 污染防护
- 生产级审计日志、密钥生命周期管理与 redaction bypass 安全评审
- 依赖与供应链扫描

> **在此阶段完成前，请勿将本项目用于任何含敏感信息的场景。**

---

### M5 · 公开发布 ⏳ 未开始

**目标：** 文档、License、安装路径、升级策略齐备，Community 可安全试用。

**前置条件：** M1–M4 全部达标 + 明确 License。

---

## 四、当前所处阶段（结论）

```
┌─────────────────────────────────────────────────────────┐
│  你现在在这里：P0 本地后端 MVP 已可验证                 │
│              P1 私有试点 rehearsal 已有门禁             │
│              P2 公开生产仍 blocked                      │
└─────────────────────────────────────────────────────────┘
```

**三条并行研发主线（用户可见优先级）：**

| 优先级 | 主线 | 状态 | 说明 |
|--------|------|------|------|
| P0 | 本地后端 MVP / 通用产品团队闭环 | **已可验证** | `skills:check`、`agents:product-team:local-mvp`、`agents:real-user-zero-to-autonomy` 与 `ui:real-user-zero-to-autonomy` 覆盖从 Settings 到最终交付的本地链路；`agents:product-team:local-mvp` 是当前低写入核心聚合证明，并包含 zero-to-autonomy operator report、Private MVP Launch Package validation 与 Launch Operations Private MVP visibility |
| P1 | 客户私有试点 rehearsal | **有门禁，可受控演练** | launch approvals、evidence archive/export、private-pilot release/launch/health/acceptance receipts 与 UI handoff 已有验证入口；仍需要 operator supervision |
| P2 | 公开生产上线 | **blocked** | 仍缺真实 managed database、managed queue/cron、managed KMS/Secret Manager、集中审计/观测/告警、事故恢复、真实 BYOK/provider 生产策略与客户生产验收策略 |

---

## 五、本地运行 ≠ 公开生产上线

README 中的 Quick Start 用于 **开发者本地预览 UI、启动本地后端与跑验证脚本**：

```bash
npm install
npm run dev          # 预览界面
npm run skills:check # 校验 Skill 包
npm run agents:scenario:contract
npm run agents:scenario
```

当前可以正确声明的是：
- 可做本地后端 MVP 验证
- 可做受控私有试点 rehearsal
- Research Project 只能作为通用产品团队能力验收样例，不是 research-only 产品

这 **不表示** 产品已可用于：
- 公开生产服务
- 无人值守 24/7 生产 daemon
- 未经批准的敏感数据或真实生产 API Key 业务处理
- 缺少 managed database / managed queue / managed KMS / centralized observability 的客户生产环境

---

## 六、Contributor 快速入口

| 想贡献… | 从这里开始 |
|---------|------------|
| 人物 Skill | [`人物Skill系统.md`](人物Skill系统.md) → `skills/hall-of-fame-personas/` |
| Agent 协议 | [`src/agents/README.md`](src/agents/README.md) |
| 产品全貌 | [`PRD.md`](PRD.md) |
| 架构审计 | [`src/agents/ARCHITECTURE_AUDIT.md`](src/agents/ARCHITECTURE_AUDIT.md) |
| 提 Issue / PR | 请先说明针对 M1 / M2 / M3 哪一阶段 |

---

## 七、变更记录

| 日期 | 说明 |
|------|------|
| 2026-07-08 | Added the Private MVP Launch Package operator report: `npm run agents:private-mvp-launch-package` emits `private-mvp-launch-package/v1`, combining the real-user zero-to-autonomy operator report with the public-production no-go report into one customer/investor-readable package. `npm run agents:private-mvp-launch-package:validate` passes, proving the package keeps the expected `private-mvp-ready-public-production-blocked` status, preserves generic product-team artifact/stage coverage, lists allowed/forbidden claims, carries public-production blocker actions and validation commands, and does not leak provider secrets or vault ciphertext. |
| 2026-07-08 | Added Manager-visible Private MVP Launch Package boundary: `npm run agents:launch-operations:private-mvp` validates that `launch-operations-overview/v1`, Manager Ready Package, Readiness Proof Map, and the API route expose `private-mvp-launch-package-boundary/v1`, package/validation commands, the controlled-private-MVP claim, the forbidden public-production claim, Manager-readable Public Production Next Steps, and `launch-operations-next-step-run/v1` receipt proof. `agents:product-team:local-mvp` now includes this focused gate. |
| 2026-07-08 | Added the operator-readable production access-control policy route: `GET /access-control-policy` now returns `access-control-policy/v1` from the backend service with signed-header, replay request-id, project membership, identity-session, audit fail-closed, and related route contracts. `npm run agents:production-access-control` now verifies the route and confirms it does not leak `AGENT_ACCESS_SIGNING_SECRET`; this makes the first public-production blocker executable for deployment wiring while public production remains blocked by later managed controls. |
| 2026-07-08 | Added the operator-readable managed identity policy route: `GET /managed-identity-policy` now returns `managed-identity-policy/v1` with provider metadata env names, service identity env names, managed-production evidence env names, attestation payload shape, validation command, and related routes. `npm run agents:production-managed-identity` verifies the route and confirms it does not leak issuer/JWKS URL values, receipt checksums, signatures, access signing secrets, or attestation signing secrets; this makes the second public-production blocker executable for IdP/service-identity wiring while public production remains blocked by managed secrets and later infrastructure/operations controls. |
| 2026-07-08 | Added the operator-readable managed Secret Manager/KMS policy route: `GET /managed-secret-manager-policy` now returns `managed-secret-manager-policy/v1` with provider capability requirements, endpoint/key env groups, attestation id/checksum pairs, validation command, and related routes. `npm run agents:production-managed-secrets` verifies the route and confirms it does not leak endpoint values, key ids, attestation ids/checksums, raw secrets, access signing secrets, or attestation signing secrets; this makes the third public-production blocker executable for Secret Manager/KMS wiring while public production remains blocked by persistence, queue, provider, governance, traffic, and operations controls. |
| 2026-07-08 | Added the operator-readable managed persistence policy route: `GET /managed-persistence-policy` now returns `managed-persistence-policy/v1` with adapter env names, database/gateway env groups, signed cutover evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-managed-persistence` verifies the route and confirms it does not leak database URLs, endpoint tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the fourth public-production blocker executable for managed database cutover while public production remains blocked by durable queue, provider, governance, traffic, operations, and customer acceptance controls. |
| 2026-07-08 | Added the operator-readable managed worker queue policy route: `GET /managed-worker-queue-policy` now returns `managed-worker-queue-policy/v1` with queue adapter env names, queue/gateway env groups, scheduler lease/retry/dead-letter capability requirements, signed cutover evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-managed-worker-queue` verifies the route and confirms it does not leak queue endpoint tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the fifth public-production blocker executable for durable queue/scheduler cutover while public production remains blocked by provider, governance, traffic, operations, and customer acceptance controls. |
| 2026-07-08 | Added the operator-readable production provider controls policy route: `GET /production-provider-controls-policy` now returns `production-provider-controls-policy/v1` with budget/rate-limit env names, usage audit env groups, cost alert env groups, signed cost-control evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-provider-controls` verifies the route and confirms it does not leak audit endpoint tokens, alert routing tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the provider/BYOK cost-control blocker executable while public production remains blocked by governance, traffic, operations, and customer acceptance controls. |
| 2026-07-08 | Added the operator-readable production data governance policy route: `GET /production-data-governance-policy` now returns `production-data-governance-policy/v1` with retention/residency env names, deletion/export env groups, signed data-governance evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-data-governance` verifies the route and confirms it does not leak deletion/export endpoint tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the data-governance blocker executable while public production remains blocked by traffic, operations, and customer acceptance controls. |
| 2026-07-08 | Added the operator-readable production traffic/rollback policy route: `GET /production-traffic-policy` now returns `production-traffic-policy/v1` with production domain/TLS env names, traffic gateway and health check env names, release approval env names, rollback runbook/smoke-test env names, signed traffic-control evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-traffic-controls` verifies the route and confirms it does not leak traffic/health/rollback tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the traffic cutover blocker executable while public production remains blocked by operations and customer acceptance controls. |
| 2026-07-08 | Added the operator-readable production customer acceptance policy route: `GET /production-customer-acceptance-policy` now returns `production-customer-acceptance-policy/v1` with customer policy env names, success criteria env names, threshold env names, approver/approval env names, rollback criteria env names, signed customer-acceptance evidence env names, attestation payload shape, validation command, readiness booleans, and related routes. `npm run agents:production-customer-acceptance` verifies the route and confirms it does not leak approval values, receipt checksums, access signing secrets, or attestation signing secrets; this makes the customer production acceptance blocker executable while public production remains blocked by operations and final managed launch controls. |
| 2026-07-08 | Added the operator-readable production operations policy route: `GET /production-operations-policy` now returns `production-operations-policy/v1` with observability env groups, alert/on-call/incident env names, restore-drill env names, centralized audit-retention env names, signed operations evidence env names, validation command, readiness booleans, and related routes. `npm run agents:production-operations-startup` verifies the route and confirms it does not leak observability, alert, incident, or audit endpoint tokens, receipt checksums, access signing secrets, or attestation signing secrets; this makes the operations blocker executable after customer production acceptance while public production remains blocked by final managed launch controls. |
| 2026-07-08 | Reconfirmed the Settings and real-user browser path in both dev preflight and packaged UI modes: `npm run ui:settings-agents-server:dev`, `npm run ui:real-user-zero-to-autonomy:dev`, `npm run build`, `node scripts/validate-settings-agents-server-ui.mjs`, and `node scripts/validate-real-user-zero-to-autonomy-agents-server-ui.mjs` pass with `HOFS_PLAYWRIGHT_CHROMIUM` pointing to local Microsoft Edge because the sandbox cannot read the default Playwright browser cache. This proves the current UI can save the backend target, keep Settings footer blocked until Health runs, keep Health blocked before provider secrets are sealed, seal model/search credentials through Secret Vault, run Settings Workflow Smoke, then start the generic product-team zero-to-autonomy path through C/A handoff, Agent output, evidence, review/revision, final delivery, Flow Graph, Proof Map, transcript, timeline, event ledger, and Agent Dashboard. Public production remains blocked by managed identity, KMS/Secret Manager, durable persistence/queue, centralized audit/observability, real provider rollout, and launch-governance evidence. |
| 2026-07-07 | Reconfirmed the current built Settings and real-user browser path with one controlled production-style pass: `npm.cmd run build`, `node scripts/validate-settings-agents-server-ui.mjs`, and `node scripts/validate-real-user-zero-to-autonomy-agents-server-ui.mjs` pass against the rebuilt `dist` bundle. This proves the current UI can enter the backend URL, type and seal model/search credentials through Secret Vault, edit backend-backed Settings policies, run `/search/test`, start a generic product-team mission, execute kickoff, C/A handoff, autonomous Agent output, provider-backed evidence, source decisions, generic artifact submissions, review/revision/final delivery, and read back Flow Graph, Proof Map, Product Team Delivery Trace, Group Chat transcript, timeline, event ledger, Agent Dashboard, Evidence Index, Project Memory Readiness, and Project Evidence Archive proof. Public production remains blocked by managed identity, KMS/Secret Manager, durable persistence/queue, centralized audit/observability, real provider rollout, and launch-governance evidence. |
| 2026-07-06 | Reconfirmed the low-write real-user zero-to-autonomy API gate after adding workspace bind proof, Reviewer source decisions, and ready Project Evidence Archive proof coverage: `npm run agents:real-user-zero-to-autonomy` passes. This verifies Secret Vault, model/search provider setup, generic mission startup, local workspace bind/read/write/readback, C/A handoff, autonomous Agent output, provider-backed evidence, source decisions, generic artifact submissions, review/revision, final deliverable, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, Project Evidence Archive storage/workspace proof coverage, and Product Team Delivery Trace through the real backend API. |
| 2026-07-07 | Added the real-user zero-to-autonomy operator report: `npm run agents:real-user-zero-to-autonomy-report:validate` passes. This turns the same real `agents:server` Settings-to-final-delivery API run into a redacted stage report covering Settings/BYOK, startup readiness, kickoff/self-marketing, workspace binding, C/A handoff, provider evidence/source review, search/model provider usage proof rows, provider receipt proof rows, brainstorm/draft/review/revision/final delivery, artifact coverage, proof surfaces, Project Evidence Archive, and public-production blockers without printing provider secrets or vault ciphertext. |
| 2026-07-06 | Reconfirmed the built real-user zero-to-autonomy browser gate after the Settings Workspace bind fix and Project Evidence Archive Manager readback: `npm run ui:real-user-zero-to-autonomy` passes. This verifies the customer-facing path from Settings provider seal through kickoff, Settings Workspace local workspace bind/readback, C/A handoff, autonomous Agent output, provider-backed evidence, source decisions, generic artifact submissions, review/revision, final deliverable, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, and ready Project Evidence Archive storage/workspace proof readback on the built app. |
| 2026-07-06 | Reconfirmed the P1 Manager private-pilot browser path against the existing built app: `node scripts/validate-manager-private-pilot-ui.mjs` passes. This verifies C-side launch approvals, project evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Flow Graph, and Readiness Proof Map evidence after the P0 real-user build confirmation, while public production remains blocked by P2 managed controls. |
| 2026-07-06 | Reconfirmed low-write persona and generic product-team foundations: `npm run skills:validate`, `npm run agents:scenario:contract`, and `npm run agents:product-team:smoke` pass. This verifies the persona package supply chain, Manager chat/Agent message/marketplace contract/Workbench/timeline contracts, and the generic backend product-team loop without running the long scenario or staged acceptance regressions. |
| 2026-07-06 | Reconfirmed P2 launch-hardening blockers: `npm run agents:public-production-startup-readiness`, `npm run agents:product-team:production-evidence-integrity`, `npm run agents:product-team:production-launch-governance`, and `npm run launch:infra` pass. This proves public-production startup remains blocked without managed runtime controls, local/rehearsal evidence cannot masquerade as managed-production evidence, production approvals route through signed governance, and adapter/persistence/queue infrastructure remains rehearsal evidence until real managed services are connected. |
| 2026-07-07 | Added and validated the redacted public-production operator report: `npm run agents:public-production-readiness-report:validate` passes. This turns the `public-production-startup-readiness/v1`, `production-environment-setup/v1`, and embedded managed environment preflight contracts into a CLI-readable blocked-domain/action/env-name report without exposing configured production values or claiming public or managed-environment readiness. |
| 2026-07-07 | Added managed environment preflight: `npm run agents:managed-environment-preflight:validate` passes and `npm run launch:infra` includes it. This distinguishes real external adapter gateway / managed persistence / durable queue / attestation signing candidates from localhost or local-shadow rehearsal without printing endpoints, tokens, database credentials, queue tokens, or signing secrets. |
| 2026-07-06 | Reconfirmed local MVP startup and autonomy continuity gates: `npm run agents:local-mvp-startup-readiness`, `npm run agents:server:validate`, and `npm run agents:product-team:cycle-consistency` pass. This proves the backend startup/Vault boundary and the longer staged autonomous-cycle consistency path for C/A continuation, Flow Graph, Proof Map, Ready Package, and persistence proof. |
| 2026-07-06 | Reconfirmed the full persona check and the then-current generic product-team file-backed core backend gate: `npm run skills:check` passed 40 persona packages and 55 regression cases; the longer file-backed core chain covered generic mission startup, C/A handoff, Agent submissions, evidence, review/revision, final deliverable, Manager Flow Graph, Readiness Proof Map, security boundary, persistence, worker queue, and pre-HTTP runtime checks. The default `agents:product-team:core` command now points to the low-write local-MVP aggregate; use `agents:product-team:core:file-backed` for the older staged file-backed runner. Research remains a validation sample, not a research-only product direction. |
| 2026-07-06 | Tightened Timeline proof-node actions: `npm run agents:timeline-action` now verifies Manager note, acknowledgement, completion mark, and edit-note actions through `POST /projects/:id/timeline/actions`, including receipt anchors, redacted note text, timeline logs, event-ledger rows, and Manager Flow Graph proof nodes. This keeps proof review actions backend-backed instead of browser-only comments, fake completion state, or raw log mutation. |
| 2026-07-06 | Reconfirmed Group Chat transcript backend contracts: `npm run agents:transcript-contracts` passes. This aggregates transcript search, channel pin, message pin, reply, mention, attachment, and member presence, proving the collaboration surface uses backend transcript receipts, timeline/event proof, Proof Map routes, Flow Graph nodes, and Agent inbox delivery instead of browser-local chat state. |
| 2026-07-06 | Reconfirmed Settings project-policy backend gates: `npm run agents:project-settings:privacy`, `npm run agents:project-settings:provider-budget`, `npm run agents:project-settings:tool-grants`, `npm run agents:project-settings:integrations`, and `npm run agents:project-settings:workspace` pass. This proves Settings privacy, provider budget, Agent tool grants, integration capability rows, workspace policy, runtime-contract links, memory-readiness links, meeting summaries, audit, timeline/event proof, and file-backed persistence are backend receipt/readback paths rather than browser-only settings. |
| 2026-07-06 | Strengthened the Settings backend aggregate gate: `npm run agents:settings-contracts` now runs local MVP startup readiness, Settings health, runtime, provider-readiness, integration-readiness, search endpoint/key Vault binding, privacy, provider budget, Agent tool-grant, integration capability, and workspace capability contracts in one low-write sequence. This gives future Settings work one backend-first regression entry before heavier browser checks, including the first-project readiness and search-provider setup paths, without claiming public-production readiness. |
| 2026-07-06 | Tightened the P1 browser persistence/runtime fallback boundary: startup and runtime filters now treat cached backend sync status, backend-managed data-source labels, and embedded Manager Dashboard / Ready Package / Proof Map / Flow Graph read models as backend-managed markers. Older cached backend projects are dropped from browser project/chat cache, and current backend-marked projects cannot run local autonomous fallback or local transcript/timeline/Agent Dashboard proof recovery when they lack the newer kickoff-source marker. |
| 2026-07-06 | Tightened the `Seed Sample/Dev` development switch: `isDevelopmentLocalRuntimeFallbackEnabled()` no longer permits browser snapshot seeding for arbitrary projects. Snapshot seed is limited to Manager Demo or explicitly tagged development-fallback projects with no backend kickoff, backend-managed, or backend-sync evidence, protecting real receipt ledgers from stale browser snapshots. |
| 2026-07-06 | Tightened Settings first-run backend targeting: Deployment now exposes an editable local backend API target (`settings-deployment-backend-url-input`) that reuses the Backend Worker Station URL store, and saving a new target clears stale Provider, Secret Vault, startup, and Health read models before syncing the new backend. |
| 2026-07-07 | Promoted Settings contracts into the local MVP product-team aggregate gate: `npm run agents:product-team:local-mvp` / `agents:product-team:core` now start with `npm run agents:settings-contracts`, so backend URL/API entry, Secret Vault, provider/runtime readiness, search endpoint/key binding, and project Settings policy receipts must pass before the Research-as-sample, Mission Runner, MVP readiness action, and zero-to-autonomy report gates can represent local MVP readiness. |
| 2026-07-05 | Tightened the Manager Dashboard Agent Current Work Status boundary: `agent-state-summary/v1` now carries backend task counts and current-task fields, and React uses browser-local task rows only in explicit frontend-fallback mode. |
| 2026-07-05 | Tightened initiation development fallback labeling: explicit fallback projects are now tagged `development-fallback`, excluded from backend write commands, and documented as non-real launch evidence. |
| 2026-07-05 | Added an in-flow Project Initiation startup gate: Start Kickoff and Approve/Create now require `local-mvp-startup-readiness/v1` `readyForFirstProjectRun=true`, with Sync and Settings actions visible inside initiation; the underlying handlers repeat the same guard for hidden/legacy entry points. |
| 2026-07-05 | Added `npm run agents:scenario:contract` as the low-write Manager scenario aggregate gate. It chains Manager chat command, Agent-to-Agent message, marketplace Agent contract, Agent Workbench, and timeline-action contract slices so C/A scenario proof can be checked quickly before running the long legacy `agents:scenario` regression. |
| 2026-07-05 | Reconfirmed real-user zero-to-autonomy and production build gates after the cockpit route-sync and research-sample gate changes: `npm run agents:real-user-zero-to-autonomy`, `npm run ui:real-user-zero-to-autonomy:dev`, and `npm run build` pass. This proves the real local backend plus low-write browser preflight can still run Settings provider seal, kickoff, C/A handoff, autonomous Agent output, provider-backed evidence, generic artifacts, review/revision, final deliverable, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, and Evidence Index readback without relying on frontend-only mock state; the Vite build still reports a large chunk warning as a performance follow-up. |
| 2026-07-05 | Changed `agents:product-team:research-sample` from the long staged acceptance runner to a low-write Research-as-validation-sample gate over the generic in-memory product-team backend chain. It now proves kickoff, self-marketing, brainstorm, evidence, draft, review/revision, final deliverable, Flow Graph, Proof Map, Manager governance/action Proof Map routes and Flow Graph nodes, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, and artifact storage proof without launching the long staged HTTP runner; cycle consistency remains covered by `agents:product-team:cycle-consistency`. |
| 2026-07-05 | React Manager Dashboard now explicitly syncs standalone cockpit read models through deferred receipt routes, dashboard auto-sync, and the visible `Sync Cockpit` control. Agent State, Assignment Timeline Matrix, Change Flow, and Continuous Work Loop panels prefer `backendStation` route results before the aggregate Manager Dashboard, so real backend projects no longer need browser-derived rows to look operational. |
| 2026-07-05 | Added standalone Manager cockpit read routes for `agent-state-summary/v1`, `assignment-timeline-matrix/v1`, and `change-flow/v1` through `GET /projects/:id/agent-state-summary`, `/assignment-timeline-matrix`, and `/change-flow`, plus Readiness Proof Map route collections, Security Boundary policy, and low-write Manager chat command validation so Agent state, assignment handoff, and change-flow proof no longer depend only on the aggregate Manager Dashboard payload. |
| 2026-07-05 | Added standalone `continuous-work-loop/v1` read route through `GET /projects/:id/continuous-work-loop`, Readiness Proof Map `continuousWorkLoopRoutes`, Security Boundary policy, and the low-write Manager chat command contract so continuous Agent work proof no longer depends only on the aggregate Manager Dashboard payload; public 24/7 production remains blocked by managed queue/cron, observability, on-call, incident recovery, and provider/BYOK controls. |
| 2026-07-05 | Reconfirmed P1 private-pilot gates in the current workspace: `npm run agents:product-team:private-pilot` and `npm run ui:manager-private-pilot` both pass. This proves backend receipts plus browser Manager handoff for launch approvals, evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Flow Graph, and Readiness Proof Map while public production remains blocked. |
| 2026-07-04 | 更新当前阶段结论：P0 本地后端 MVP 与 P1 私有试点 rehearsal 已有验证门禁，P2 公开生产仍因 managed database、managed queue/cron、managed KMS/Secret Manager、集中观测审计、事故恢复与真实 BYOK/provider 生产策略保持 blocked |
| 2026-06-27 | 补齐私有 adapter gateway 项目级预检：Manager Ready Package / deployment preflight / API / Harness 可以读取 live health、state 与 capability proof，仍保持 production cutover 阻塞 |
| 2026-06-27 | Added `project-evidence-export-workflow/v1` plus local `project-evidence-export-package/v1` receipt for private-pilot evidence handoff governance across API, Manager Ready Package, UI, Flow Graph, Proof Map, access policy, persistence snapshot, and product-team Harness while keeping production export blocked |
| 2026-06-28 | Production Launch Control Center now treats `production-evidence-integrity-audit/v1` as a first-class launch gate, so local/test production-control receipts can verify domain workflows without closing public-production go/no-go until explicit managed-production evidence is present |
| 2026-06-28 | Added `production-evidence-integrity-audit/v1` across API, Manager Ready Package, Manager UI, Security Boundary route policy, and product-team Harness. It separates missing/local-rehearsal/external-unattested evidence from signed `managed-production` receipt evidence so local production-control rehearsals cannot be mistaken for public-production launch proof |
| 2026-06-28 | Added `production-deployment-control-receipt-workflow/v1` plus persisted `production-deployment-control-receipt/v1` rows for deployment hardening controls across API, Manager Ready Package, UI, Flow Graph, Proof Map, access policy, persistence snapshot, and product-team Harness while keeping broader public production blocked |
| 2026-06-27 | Added `production-provider-control-receipt-workflow/v1` plus persisted `production-provider-control-receipt/v1` rows for provider allowlists, budgets, tool grants, retry/circuit breakers, managed audit/eval storage, centralized cost alerting, release policy, and incident runbooks across API, Manager Ready Package, UI, Flow Graph, Proof Map, access policy, persistence snapshot, and product-team Harness while keeping broader public production blocked |
| 2026-06-27 | Added `evidence-quality-audit/v1` as the Manager decision-readiness audit for evidence rows/source quality/source safety/proof routes across API, Manager Ready Package, archive manifest, UI, access policy, product-team Harness, and docs; production search still requires real gateway, calibrated source policy, and managed audit storage |
| 2026-06-27 | Added `evidence-source-review-workflow/v1` as the reviewer-visible source governance workflow across API, Manager Ready Package, archive manifest, launch evidence routes, UI, access policy, product-team Harness, and docs; production still requires human source-review approval policy, calibrated quality policy, immutable source snapshots, and managed reviewer/provider audit storage |
| 2026-06-27 | Upgraded evidence source review from a read-only queue to a Reviewer decision loop: `POST /projects/:id/evidence-source-review-workflow` persists `evidence-source-review/v1`, updates chat/timeline/event proof, Flow Graph, Proof Map, Agent/Manager dashboards, archive contents, access policy, and `evidence_source_reviews` persistence rows |
| 2026-06-27 | Added local checksummed source snapshots and provider receipts for provider-backed evidence: evidence quality audit, Provider Readiness, Flow Graph attachments, Task Evidence, Project Evidence Archive, Proof Map, persistence snapshot, migration/dry-run/adapter plans, UI, and product-team Harness now cover `evidence-source-snapshot/v1`, `evidence-provider-receipt/v1`, `evidence_source_snapshots`, and `evidence_provider_receipts` |
| 2026-06-27 | Added `project-evidence-archive/v1` as the Manager/customer handoff proof bundle across API, Manager Ready Package manifest summary, UI, access policy, product-team Harness, and docs; production export/download controls remain blocked |
| 2026-06-27 | 补齐本地 identity-session / runtime credential 合同：签发、使用、撤销、审计、持久化、迁移种子和 Manager 安全边界证明面，仍保持生产身份系统阻塞可见 |
| 2026-05-31 | 首版路线图：明确 Pre-alpha 状态，锁定 M1 为当前主战场 |

---

## Frontend Mock Replacement Tracker

- `docs/FRONTEND_MOCK_REPLACEMENT_REGISTER.md` tracks frontend-only mock/fallback surfaces and their backend-backed replacements.
- Current first gate: Manager Command Center, Scenario Walkthrough, Action Playbook, Use Case Audit, Sync Protocol, Manager Flow Graph, production launch/control/evidence/receipt subpanels, production operations/deployment/security/provider receipt writes, and Project Initiation approval no longer silently synthesize frontend rows or local projects for real backend-online paths; governance and launch panels show `backend model missing` when the required read model is absent, initiation approval shows a backend failure instead of creating a local fallback project, kickoff meeting generation labels deterministic/model/development provenance instead of hiding validation fallback, the Agent Focus Workbench now sends brainstorm, evidence, submission, draft, revision-note, and final-deliverable writes through backend lightweight receipts before refreshing authoritative read models, Manager Dashboard submission rows now send Reviewer review writes through backend lightweight receipts, `npm run ui:manager-backend` verifies that same product-team loop through accepted final delivery on a real initiated browser project, production-control receipt writes can return lightweight receipts with launch-control refresh routes, the dashboard can sync/load backend projects from `GET /projects` / `GET /projects/:id`, Active Portfolios visibly labels `backend-backed`, `sample-fixture`, and `frontend-fallback` rows, browser snapshot writes are guarded to sample/dev fallback projects, and chat/meeting/change/Leader assignment/manual pulse controls fail closed instead of using browser-local runtime success for backend-online real projects.
- Group Chat transcript controls now have a low-write aggregate gate: `npm run agents:transcript-contracts` runs backend transcript search, channel pin, message pin, reply, mention, attachment, and member-presence contracts together while the individual scripts remain available for debugging.
- Agent Focus Workbench now requires a backend Agent Dashboard before backend-online real projects can use task, review, submission, or evidence dependency options as Agent-owned context; missing Agent Dashboards show `agent-workbench-backend-dashboard-required-*`, disable writes, and produce failed non-proof receipts instead of falling back to local project snapshot rows.
- Next replacement targets: lower-priority browser-first mutation paths and explicit sample/demo fixture separation.

---

## English summary

**Hall of Fame Studio is a local/private-MVP system, not a public-production service.** The generic product-team loop is now backend-backed and validated for local MVP plus controlled private-pilot rehearsal, but public production remains blocked until managed secrets, persistence, queue/cron, observability, audit, incident response, provider/BYOK controls, and customer production policy are proven.
