# Hall of Fame Studio Technical Overview

> Status: pre-alpha technical design and prototype implementation notes.
>
> This document explains the technical direction behind Hall of Fame Studio. For product requirements, see `PRD.md`. For the detailed Agent runtime API and verification notes, see `src/agents/README.md` and `src/agents/ARCHITECTURE_AUDIT.md`.

## 1. System Principle

Hall of Fame Studio is designed around a simple technical thesis:

An Agent is not a passive prompt response. An Agent is an independently addressable work node with identity, memory, obligations, communication policy, work cadence, and evidence output.

In the current prototype this is implemented through the five-layer Agent model:

1. Identity: name, persona skill, capability lane, project role.
2. Mind: current goal, open obligations, confidence, attention state.
3. Relations: Leader, Reviewer, managed peers, peer dependencies.
4. Communication Policy: when to read, stay silent, respond, escalate, or ask for evidence.
5. Work Cycle: Hour Pulse, Day Report, task progress, private worklog, public group-chat update.

The Director starts or changes intent. After that, Agents should be able to coordinate through meetings, group chat, task ownership, and worker cycles without requiring the Director to manually drive every step.

### 1.1 General Product-Team System

The system is not a research-only tool, paper-writing tool, or single vertical workflow.

The target is a general AI product-team operating system. A project may be a market research brief, product strategy, technical plan, content system, brand campaign, prototype specification, or research paper. The same primitives should apply:

- clarify intent
- recruit a suitable team
- hold a kickoff meeting
- negotiate ownership
- brainstorm alternatives
- collect and evaluate evidence
- write or build artifacts
- review and revise
- submit work nodes to the manager flow graph
- preserve proof in chat, timeline, task evidence, and event ledger

Research Project is an early validation scenario because it exercises many of these primitives at once. It should test whether the general system can produce a product-quality outcome, not create a separate research-specific runtime.

### 1.2 Acceptance Gates

The product-team validation now has three levels:

- `npm run agents:product-team:smoke` is the low-write in-memory backend smoke. It uses `createAgentProjectService` / `createAgentProjectApi` without a file-backed store, browser, HTTP server, or workspace artifact writer, then drives the generic C/A chain through mission kickoff, role self-marketing, backend meeting turns, evidence search, brainstorm submission, product-brief draft submission, requested-changes review, linked revision, accepted final deliverable, Manager Flow Graph, Readiness Proof Map, Agent Dashboard, transcript, timeline, event ledger, and Evidence Index readiness. Use it before heavier gates when disk write volume matters.
- `npm run agents:product-team:core` is the stable low-write alias for the current local MVP product-team proof. It runs the same aggregate as `agents:product-team:local-mvp`: Settings/local startup contracts, Research-as-sample generic chain, Mission Runner HTTP startup/C/A handoff, marketplace Agent contract roster proof, Agent Workbench artifact/review/final-delivery proof, MVP readiness operator-action execution, zero-to-autonomy operator report / proof package validation, and Launch Operations Private MVP visibility.
- `npm run agents:product-team:core:file-backed` keeps the older file-backed staged core runner for broader persistence, security-boundary, provider-readiness, and worker-queue coverage when that heavier proof is needed.
- `npm run agents:product-team:research-sample` is the low-write Research-as-validation-sample gate. It reuses the generic in-memory product-team backend chain, statically guards the Research-as-sample contract, and proves kickoff, self-marketing, Leader campaign, discovery report, brainstorm, evidence/source review, Reviewer source decisions with no pending source-review queue, backend artifact draft, requested-changes review, revision, accepted final deliverable, Artifact Quality Audit, Submission Review Workflow, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, ready Project Evidence Archive handoff, artifact storage/workspace proof, memory-readiness proof, and production blockers rather than paper/thesis/manuscript-specific protocol fields. The gate now also asserts `product-team-delivery-trace/v1` exposes the eight generic delivery stages from kickoff through proof surfaces, every ready stage has proof/timeline/event evidence, and the trace does not introduce paper/thesis/manuscript-only protocol fields. Use `npm run agents:product-team:cycle-consistency` for the separate three-step autonomous consistency proof.
- `npm run agents:product-team:local-mvp` is the low-write aggregate proof for the current local MVP product-team path. It runs the Settings backend aggregate, which includes local startup readiness, provider/runtime/readiness/Vault/project-settings contracts, then the Research-as-sample generic chain, Mission Runner HTTP startup/C/A handoff, marketplace Agent contract roster proof, Agent Workbench artifact/review/final-delivery proof, MVP readiness operator-action execution, zero-to-autonomy operator report / proof package validation, and Launch Operations Private MVP visibility in sequence so the team has one stable command for the current local-MVP launch question without running browser/build or full production-hardening gates.
- `npm run agents:mission-runner:startup` is the focused low-write HTTP Mission Runner startup gate. It starts the real Node Agent project HTTP server over an in-memory service, posts `/product-team-missions`, verifies kickoff self-marketing, Leader/Reviewer selection, `product-team-mission-run/v1`, `product-team-customer-agent-handoff/v1`, Runtime Autonomy Status, Collaboration Intent Queue handoff execution, the first A-side Agent submission output, Manager Flow Graph, and Readiness Proof Map without running the browser/build gate.
- `npm run agents:mvp-readiness:operator-actions` is the focused low-write HTTP C/A readiness-action gate. It starts the real Node Agent project HTTP server over an in-memory service, records `mvp-readiness-operator-action-run/v1`, projects the target into Collaboration Intent Queue and Autonomous Run Control, executes brainstorm, evidence, draft, review, revision, final-deliverable, and final-acceptance target steps into Agent output nodes, and verifies Manager Flow Graph plus Readiness Proof Map proof without running the long acceptance harness.
- `npm run ui:manager-provider-proof` is the focused C-side proof gate for provider-backed autonomous evidence. It starts the real backend with the deterministic search provider, advances a bounded Autopilot session through the scheduler, verifies the provider-evidence receipt, opens the Manager Flow Graph node, and clicks the attachment back into the backend Group Chat transcript proof. Use it when changing Flow Graph attachment rendering, transcript proof routing, provider evidence receipts, or Autopilot evidence UI.
- `npm run ui:manager-mission-runner` is the focused C/A startup gate. It starts its local backend with Secret Vault plus local model/search provider fixtures so `/local-mvp-startup-readiness` is ready before the first approval, drives the browser through kickoff meeting, Manager clarification, Leader/next-action decisions, and real project approval through `POST /product-team-missions`, then verifies the reused-kickoff Mission Runner receipt, Autopilot tick, chat proof, timeline proof, and Manager Flow Graph mission node. Use it when changing initiation approval, Mission Runner receipts, proof exits, or frontend mock replacement around project startup.
- Settings Health Workflow Smoke is the smallest user-visible proof that backend Settings can create Agent output and consume the configured provider boundary. `POST /settings/workflow-smoke` returns `settings-workflow-smoke/v1`; when the UI requests provider proof, the response also includes `settings-workflow-smoke-provider-evidence/v1`, a provider-backed evidence-search route, and a provider usage route. React stores that packet and renders `settings-health-workflow-smoke-output` with the probe project, product-brief submission, provider evidence, evidence-search route, provider usage route, Flow Graph route, Proof Map route, Group Chat transcript route, Timeline route, and Event Ledger route. This is a local MVP smoke proof only; it does not replace the full zero-to-autonomy gates.
- `npm run agents:real-user-zero-to-autonomy` is the low-write real-backend startup gate. It starts `scripts/agent-project-server.mjs` with Secret Vault plus local mock model/search endpoints, seals model key plus search endpoint/key through `/secret-vault/seal`, proves `/search/test`, verifies `/local-mvp-startup-readiness` reports `readyForFirstProjectRun=true` and routes the user into `/product-team-missions`, then drives product-team mission startup, binds a local/private MVP workspace through `/projects/:id/workspace/bind`, verifies `/projects/:id/local-runtime`, writes/reads a workspace proof file, runs C/A handoff, direct Agent Autonomous Action Queue continuation, provider-backed Agent evidence, Reviewer source decisions, brainstorm-board submission, model-backed product-brief draft, requested-changes review, linked revision-note closure for every open change request, accepted final-deliverable, Manager Flow Graph, Readiness Proof Map, Product Team Delivery Trace, Group Chat transcript, timeline, event ledger, Agent Dashboard, Evidence Index readiness, and ready Project Evidence Archive storage/workspace proof coverage through HTTP API calls without launching a browser or running a build. The gate fails if startup readiness is not proven before mission creation, or if the handoff only produces control receipts and no substantive `agent-autonomous-action-run/v1` submission with autonomous action decision, chat, timeline, and event proof.
- `npm run agents:real-user-zero-to-autonomy-report` runs the same real-backend startup gate with `--report` and emits `real-user-zero-to-autonomy-operator-report/v1`. The same backend run must also expose `GET /projects/:id/zero-to-autonomy-report` as `project-zero-to-autonomy-report/v1`, embedded in Manager Ready Package and linked from Manager Flow Graph plus Readiness Proof Map. The report is redacted and read-only: it exposes stage readiness, generic artifact coverage, source-review counts, proof routes, Project Evidence Archive coverage, and public-production blocker categories without provider secrets, endpoints, or vault ciphertext. `npm run agents:real-user-zero-to-autonomy-report:validate` verifies that the report covers every required zero-to-autonomy stage, keeps `readyForPublicProduction: false`, and preserves generic product-team positioning.
- `npm run agents:private-mvp-launch-package` is the operator launch package for the current private MVP boundary. It emits `private-mvp-launch-package/v1`, composes the real-backend zero-to-autonomy operator report with the redacted public-production readiness operator report, and must return `private-mvp-ready-public-production-blocked`: private MVP rehearsal evidence is acceptable for controlled private testing, while public production remains an explicit no-go. The package is not a backend route, production approval, or UI-derived readiness claim. Its contract is allowed to claim local/private MVP evidence, proof routes, validation commands, generic artifact coverage, provider usage/receipt proof, and public-production blocker categories; it is forbidden to claim public-production readiness or expose provider secrets, endpoints, vault ciphertext, or configured infrastructure values. `npm run agents:private-mvp-launch-package:validate` protects that contract.
- `npm run agents:launch-operations:private-mvp` verifies the backend `launch-operations-overview/v1`, Manager Ready Package embedding, and Readiness Proof Map route expose the same Private MVP Launch Package boundary for Manager UI consumption. It is the low-write proof that the launch package is visible to the operator, not only available as a CLI report.
- `npm run ui:real-user-zero-to-autonomy` is the stronger customer-facing startup gate. It starts the real `scripts/agent-project-server.mjs` process with Secret Vault plus local mock model/search endpoints; opens the built React app; sets the active backend API target through Settings Deployment; seals model key plus search endpoint/key through Settings; proves `/search/test` returns provider evidence; verifies Workspace Hub renders `workspace-local-mvp-startup-readiness`, then reads `/local-mvp-startup-readiness` directly to require `readyForFirstProjectRun=true`, `nextAction=/product-team-missions`, model/search runtime readiness, and no provider key/ciphertext leakage before clicking Start Initiation; then starts from Workspace Hub, runs kickoff, approves the mission, binds a local/private MVP workspace from Settings Workspace and verifies `/projects/:id/local-runtime`, executes the C/A handoff intent from the Manager UI, runs Agent Autonomous Action Queue against the same backend to prove post-handoff Agent initiative, records a provider-backed Agent evidence search through the same configured endpoint, records Reviewer source decisions for every provider-backed source, closes all requested-change reviews with linked revisions, and verifies the resulting brainstorm, evidence, model-backed product brief, review, revision, and accepted final-deliverable nodes are traceable through Manager Flow Graph, Readiness Proof Map, Group Chat transcript, timeline, event ledger, Project Evidence Archive, and Product Team Delivery Trace proof rows. Its Manager UI readback must show complete `9/9` generic artifact coverage through backend submissions or Artifact Quality Audit plus ready Project Evidence Archive storage/workspace proof coverage, not only backend JSON success.
- That browser gate now treats evidence quality as corroborated evidence instead of a single-source happy path: the local search gateway returns two independent source rows, so `evidence-quality-audit/v1` must become decision-ready through the same quality rule used by normal product-team evidence. The final Manager UI readback must expose Artifact Quality Audit, Product Team Delivery Trace, Submission Review Workflow, Evidence Index readiness, core Proof Map routes (`/manager-flow-graph`, `/submissions`, `/submission-review-workflow`, `/product-team-delivery-trace`, `/readiness-proof-map`), and the backend Flow Graph final-deliverable node before the gate passes.
- `npm run ui:real-user-zero-to-autonomy:dev` runs that same browser script with `--ui-base-url=http://127.0.0.1:5173` against an already running Vite dev server. The validator probes the configured local URL and its `localhost` / `127.0.0.1` counterpart, so it remains a lower-write preflight while replacing frontend mocks without requiring `vite build`. It still starts the real backend and mock providers, so it does not replace the built UI launch gate.
- `npm run ui:manager-backend:core:dev` runs the Manager backend core browser gate with `--ui-base-url=http://127.0.0.1:5173` against an already running Vite dev server. Use it for lower-write C/A control-loop preflight work around sample backend adoption, Autonomous Run Control, Agent autonomous action receipts, and Autopilot scheduler receipts; it does not replace the built `ui:manager-backend:core` gate.
- `npm run ui:manager-backend:real-user-chain:dev` is the Manager-backend alias for that same lower-write real-user browser preflight. Use it while replacing C/A Manager surfaces so the command name matches the Manager backend validation family while still reusing the zero-to-autonomy agents-server UI harness.
- `npm run ui:mock-boundaries` is the no-browser/no-build static guard for frontend mock replacement. It pins the fail-closed transcript/timeline proof boundary, real-project browser seed suppression, backend-required read-model states, and visible source labels that prevent offline/demo fallback rows from looking like A-side backend proof.
- `npm run agents:product-team:cycle-consistency` is the focused signed autonomy gate. It continues into membership-enforced backend access, runs a bounded three-step autonomous loop, and verifies `autonomous-cycle-consistency/v1`, Manager Flow Graph, Readiness Proof Map, Manager Ready Package, and persisted loop/run receipts without running the full launch-hardening chain.
- `npm run agents:product-team:private-pilot:ops-readiness` is the focused P1-to-P2 bridge. It continues through customer acceptance, verifies `production-operations-readiness/v1`, and confirms the project is locally ready for private-pilot operations while public production remains blocked by centralized observability, on-call, incident, restore, audit-retention, and managed infrastructure controls.
- `npm run agents:product-team` remains the full launch-hardening gate. It continues past the core loop into the real HTTP runtime, private-pilot controls, provider/security/persistence/queue/operations checks, and production no-go evidence.

This split keeps day-to-day development focused on whether the autonomous product-team loop still works, while preserving the longer public-production safety gate for release decisions.

Each staged product-team acceptance run now uses its own `.tmp/product-team-acceptance/<stage>-<pid>` workspace, so file-backed stores, audit logs, runtime files, and replay records stay isolated when focused gates run in parallel. Focused Manager browser gates also use process-scoped backend store and Vault record files, so concurrent UI validation cannot reuse stale Manager project state. Those workspaces are deleted by default when the stage exits; set `HOFS_PRODUCT_TEAM_PRESERVE_TMP=1` only when a failed run must be inspected. Browser private-pilot validation temporarily preserves the handoff checkpoint for the browser phase, then deletes it unless `HOFS_MANAGER_PRIVATE_PILOT_PRESERVE_TMP=1` is set. Progress logs for long/browser validations are opt-in through `HOFS_PROGRESS_LOG=1`; normal validation should rely on console output and bounded temp artifacts.

### 1.3 Autonomous Run Control

`GET /projects/:id/autonomous-run-control` exposes the current autonomous operating surface as `autonomous-run-control/v1`. `POST /projects/:id/autonomous-run-control/:actionId/run` executes one listed next action through the delegated Manager queue, Agent autonomous queue, or project-scoped scheduler lane and returns `autonomous-run-control-action-run/v1`. `POST /projects/:id/autonomous-run-control/run-loop` runs the same control surface as a bounded loop, defaulting to three steps and capped at five, then returns `autonomous-run-control-loop-run/v1` with each step receipt and the stop reason. `GET /projects/:id/autonomous-run-control/sessions`, `POST /projects/:id/autonomous-run-control/sessions/start`, `POST /projects/:id/autonomous-run-control/sessions/:sessionId/tick`, and `POST /projects/:id/autonomous-run-control/sessions/:sessionId/pause` add the project-scoped Autopilot session layer: a Manager can create a bounded session budget, advance it by explicit ticks, and pause it without turning local autonomy into an unbounded daemon. `POST /workers/autopilot/due` now scans active bounded sessions and advances due ones through the same session tick contract, so the HTTP scheduler can opt into Autopilot progress without giving the local prototype an unlimited background daemon.

It is the C-side answer to "what can the A-side team do next without me manually stitching panels together?" The model aggregates Manager action queue state, Agent autonomous action queue state, worker queue snapshot proof, scheduler routes, proof ids, timeline ids, event ids, next runnable actions, and explicit production blockers. Manager Dashboard embeds a light version; Manager Ready Package embeds the worker-queue-backed version; Manager Flow Graph and Readiness Proof Map expose it as a proofed control node/route.

The run receipt is persisted into `autonomousRunControlRunLedger`, timeline logs, and the event ledger, then exposed by Manager Dashboard, Manager Ready Package, Manager Flow Graph, Readiness Proof Map, and the delegated Agent's own Dashboard. Loop receipts are persisted into `autonomousRunControlLoopLedger` and exposed through the same Manager proof surfaces. Autopilot sessions use `autonomous-run-control-session/v1` and session ticks use `autonomous-run-control-session-tick/v1`; they persist into `autonomousRunControlSessionLedger` and `autonomousRunControlSessionTickLedger`, link to child loop/run receipt ids, carry direct `agentIds`, `taskIds`, `actionIds`, and `actionLanes`, and surface as Dashboard rows, Ready Package summary fields, Flow Graph nodes, and Proof Map routes. Each session and tick also carries an `autopilot-delivery-target/v1` snapshot of the generic Product Team Delivery Trace, including ready/missing stage counts and the next missing stage, so autonomous work is measured against the same kickoff -> self-marketing -> brainstorm -> evidence -> draft -> review/revision -> final deliverable -> proof-surface chain used by private-MVP validation. Session ticks convert that next missing stage into `autopilot-delivery-target-control/v1`, inject the resulting artifact/evidence/review controls into the delegated execution request body, and each child loop/run records `autonomous-run-control-target-selection/v1` so the C-side can audit why a Manager, Agent, or scheduler lane was selected and what work type it was asked to execute. Worker Queue Snapshot now includes `autopilot-session` rows with idempotency/lease keys, direct tick routes, and `/workers/autopilot/due`, and scheduler status exposes `scheduler-autopilot-controls/v1` plus Autopilot processed/skipped counters when that lane is enabled. This closes the C-side/A-side proof loop: the Manager can run one control action, a bounded autonomous loop, or a bounded Autopilot session, and the Agents who executed steps can also show the same step receipts, timeline ids, event ids, delivery-target state, and route back to Run Control. The contract keeps local/private-pilot autonomy separate from production autonomy. A scheduler tick, bounded loop, or session tick can be runnable while `readyForProduction` remains false until managed queue/cron, cost and volume policy, customer acceptance thresholds, incident ownership, and durable audit storage are proven.

Autonomous Run Control write responses now have a dedicated deferred read-model contract. When action, loop, session start/tick/pause, or Autopilot due-worker calls use `includeReadModels: false`, the API returns routes for the project snapshot, Manager Dashboard, Ready Package, Flow Graph, Proof Map, sessions, active tick/pause, scheduler tick, `/workers/autopilot/due`, Agent autonomous queue, Product Team Operating Loop, Autonomous Cycle Consistency, timeline, events, and transcripts. React consumes these routes after Autopilot commands via `refreshAutonomousRunControlReadModels`, so the C-side scheduler UI refreshes A-side session proof from backend reads instead of browser-local session memory.

`POST /product-team-missions` is the backend Mission Runner contract for turning a customer goal into a running generic product-team project. The service builds a durable kickoff meeting from the brief/team or reuses an existing kickoff meeting via `kickoffMeetingId` / `reuseExistingKickoffMeeting`, confirms the Leader/Reviewer/next-action packet, approves the meeting into a backend project using the Manager-confirmed `selectedTeamIds`, starts `autonomous-run-control-session/v1`, optionally runs the first `autonomous-run-control-session-tick/v1`, and then records `product-team-mission-run/v1` in `productTeamMissionRuns`. The receipt freezes whether the kickoff meeting was reused, the kickoff meeting id, selected Leader/Reviewer, task ids, Agent ids, session/tick ids, child loop/run receipt ids, target snapshot state, proof/timeline/event ids, and refresh routes for transcripts, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, Product Team Delivery Trace, Product Team Operating Loop, Collaboration Intent Queue, Runtime Autonomy Status, and Autonomous Run Control. It now also embeds `product-team-customer-agent-handoff/v1` as `customerAgentHandoff`, the C/A handoff packet that records the Manager-approved roster, Leader/Reviewer, A-side session status, first tick status, target stage, action lanes, and next A-side routes; the same handoff checksum is projected into Manager Dashboard rows, Readiness Proof Map mission routes, Manager Flow Graph mission attachments, Product Team Operating Loop `customerSide.handoff`, and the `customer-agent-handoff-intent` row in Collaboration Intent Queue. That queue row uses the unified Autonomous Run Control route (`/projects/:id/autonomous-run-control/run-backend-scheduler-tick/run`) so the handoff can continue through the same auditable A-side surface as other autonomous work. Reused-kickoff mission receipts also carry kickoff transcript turn ids in `proofIds`, so the Manager Dashboard can open exact chat proof from the Mission Runner receipt before jumping to timeline or Flow Graph proof. `GET /projects/:id/product-team-missions` and `GET /projects/:id/product-team-missions/:missionId` expose the persisted receipts. React initiation approval now posts to this route for real backend projects and then refreshes mission, autonomy, operating-loop, collaboration, Runtime Autonomy Status, transcript, timeline, and event read models. Manager Dashboard adds `productTeamMissionRuns`, Readiness Proof Map adds `productTeamMissionRunRoutes`, and Manager Flow Graph renders a `product-team-mission-run` node linked to kickoff, Autopilot, operating-loop, collaboration, runtime-autonomy, and delivery-trace surfaces. The contract is deliberately generic; Research Project is only a mission brief used by the Harness.

The customer-visible C/A handoff output must be inspectable before the full Manager Ready Package catches up. The standalone Manager Dashboard Collaboration Intent panel renders the runnable `customer-agent-handoff-intent` first, and after execution renders `backend-collaboration-intent-handoff-output-routes` with the delegated Agent submission route, timeline id, event id, chat proof button, and timeline proof button. That panel consumes the backend `collaboration-intent-run-output/v1` receipt; it must not invent a local handoff result.

Lightweight write receipts now expose proof refresh routes by default. `includeReadModels: false` responses include project, Manager Dashboard, Ready Package, Flow Graph, Readiness Proof Map, transcript, timeline, event, and relevant Agent routes, so React can refresh proof surfaces directly after Agent submissions, evidence searches, draft submission, Reviewer decisions, and worker actions instead of relying on browser-local reconstruction. Private-pilot lifecycle receipts and production operations/deployment/security/provider control receipts now use the same contract: write responses carry routes for launch, go-live, operations, production-infrastructure rehearsal, production control center, production evidence integrity, deployment/preflight, adapter gateway, provider/security, timeline, event, transcript, Flow, and Proof refreshes, and React consumes those routes before falling back to full Ready Package sync.

Autopilot session ticks now cross the same worker execution boundary as project and Agent workers. `autonomousRunControlSessionTickLedger` rows export `worker-execution-receipt/v1` records with `sessionId`, idempotency key, lease key, direct tick recovery route, and `worker_runs` persistence columns. The file-backed store persists active sessions and tick receipts, so a restarted service can reload a running Autopilot session and resume it through `/workers/autopilot/due`. Worker Queue Snapshot, queue adapter plan/dry-run, Operations Readiness metrics, and the private adapter gateway all import the `autopilot-session` lane, acquire leases, dispatch `/workers/autopilot/due`, acknowledge the tick receipts, and parity-check the resulting queue snapshot. This is the current bridge from C-side control to a future managed queue/cron runtime.

Agent autonomous initiative now crosses the same worker boundary. Agent worker queue rows carry `agent-autonomous-initiative/v1`, `agentInitiativeId`, target artifact type, strategy intent, and the Agent action run route into scheduler/import/lease request bodies. Autonomous Run Control action and loop receipts freeze the executed initiative when an Agent lane runs, while Autopilot session tick receipts always freeze the candidate Agent initiative ids and artifact targets seen before lane selection. This gives the C-side a backend audit trail from "Agent proposes work" to "worker queue leases it" to "Autopilot chose or skipped it this tick."

Agent worker evidence can now cross the real provider boundary as well. `runAgentWorkCycleWithProviderEvidence` and the async Agent work-cycle API path (`useProviderEvidenceSearch: true`) evaluate provider policy, Agent tool grants, budget/rate limits, retry/circuit state, and provider readiness before calling the configured search provider. A successful call injects provider sources, findings, and `evidence-provider-receipt/v1` into the normal worker evidence-search and submission path, then records `providerUsageLedger` proof. Agent Autonomous Action Queue rows also project this intent: when an enabled search provider is available and the strategy-selected row is an evidence-search/evidence-packet action, the row carries `providerEvidenceSearchPlanned` plus `useProviderEvidenceSearch`; the async queue run path persists the provider-backed evidence, provider usage, and `agent-autonomous-action-run/v1` links together. Autopilot session ticks and `/workers/autopilot/due` can now take the same async provider-evidence lane for evidence-quality targets, persist `autonomous-run-control-action-run/v1`, loop, and session tick receipts with provider/evidence links, publish an `autopilot-provider-evidence` group-chat coordination message, and allow explicitly requested evidence packets to land as project-level evidence/submission nodes when no single task owns the target. The HTTP scheduler calls the async API for Autopilot due-work and forwards provider-evidence controls, so scheduler-owned continuation can use the same proof chain as a manual tick. React Backend Worker Station now extracts those provider-evidence receipts from direct tick, scheduler tick, and scheduler status payloads, stores them in backend station state, and renders the provider/evidence/search/usage ids beside the Autopilot worker receipt. Manager Flow Graph Autopilot tick nodes now attach the same provider evidence receipt plus transcript route and link to the evidence-search node, while Readiness Proof Map session tick routes expose the evidence-search, provider-usage, and transcript routes. `autonomous-provider-preflight/v1` now runs before autonomous provider-backed work and records whether the scheduler should call the provider, degrade to local worker proof, wait for a queue row, or pause because a required provider boundary is unavailable; the same checksum/action is copied into provider usage, Agent Queue run receipts, Autopilot action/loop/tick receipts, and `/workers/autopilot/due` summaries. This is the current shortest backend bridge from A-side autonomous research/evidence work to C-side Flow Graph, Proof Map, Task Evidence, Group Chat Transcript, Agent Queue receipts, Autopilot receipts, and Manager inspection without creating a research-only protocol.

### 1.4 Product Team Operating Loop

`GET /projects/:id/product-team-operating-loop` exposes `product-team-operating-loop/v1`, the backend summary for the C-side/A-side collaboration contract. It does not execute work. It joins Product Team Delivery Trace, Autonomous Run Control, Agent Autonomous Action Queue, Manager Action Queue, Worker Queue Snapshot, Readiness Proof Map, timeline ids, and event ids into one redacted status object.

Manager Flow Graph also exposes this contract as a `product-team-operating-loop` aggregate node connected to Product Team Delivery Trace, Autonomous Run Control, and Agent-selected strategy nodes. Readiness Proof Map exposes the same contract through `productTeamOperatingLoopRoutes`, so the UI can prove the loop from routes, proof ids, timeline ids, and event ids instead of inferring it from scattered panels.

`GET /projects/:id/continuous-work-loop` exposes `continuous-work-loop/v1`, a narrower read-only contract for continuous Agent work. It returns scheduler state, next project pulse, scheduled/proofed Agent counts, per-Agent continuous-work rows, backend refresh routes, and explicit `readyForUnattendedProduction: false` blockers. Readiness Proof Map exposes the same surface through `continuousWorkLoopRoutes`, so local/private-pilot scheduler proof can be inspected independently without implying public 24/7 production readiness.

The A-side portion now includes `agent-autonomous-initiative/v1` rows. These rows are derived from each Agent's backend strategy decision and expose the Agent, selected action, intent, rationale, target artifact type, review/evidence target ids, run route, and proof/timeline/event ids. The Agent Autonomous Action Queue, Product Team Operating Loop, and Manager Flow Graph all consume the same initiative row shape, so "Agent initiative" is inspectable as a backend contract rather than a frontend interpretation of status text.

Product Team Mission Runner handoff is also projected into the same route as `customerSide.handoff` plus `customerSide.handoffExecution`. The execution summary is a read model, not a new runner: it aggregates the approved C-side roster/Leader/Reviewer handoff with the A-side Autopilot session, session tick receipts, loop/action run receipts, result message ids, and Agent submissions when they exist. This lets the Manager distinguish `a-side-session-started` from `a-side-tick-recorded` or `a-side-run-receipts-recorded` without relying on a frontend button state. Manager Flow Graph exposes the same data on the `operating-loop-customer-agent-handoff` attachment, Collaboration Intent Queue uses it to enrich the runnable Mission Handoff row, and React renders Handoff Exec/Runs/Outputs from the backend Operating Loop.

### 1.5 Planner / Executor / Reviewer State Machine

`GET /projects/:id/planner-executor-reviewer-state-machine` exposes `planner-executor-reviewer-state-machine/v1`. This is a read-only responsibility handoff model, not a new executor. It joins kickoff planning, role self-marketing, Leader selection, Agent brainstorm/evidence/draft submissions, Reviewer requested-change rounds, linked revisions, accepted final deliverable proof, Product Team Delivery Trace, Product Team Operating Loop, Team Collaboration Diagnostics, Flow Graph, Proof Map, timeline ids, and event ids.

The model exposes three role lanes (`planner`, `executor`, `reviewer`) and four transition rows (`plan-to-execute`, `execute-to-review`, `review-to-revision`, `revision-to-final`). Manager Ready Package embeds it, Manager UI renders a compact source-labeled state-machine panel, Manager Flow Graph renders `planner-executor-reviewer-state-machine`, Readiness Proof Map exposes `plannerExecutorReviewerStateMachineRoutes`, Security Boundary lists the route policy, and React proof sync fetches the standalone route. The state machine can mark local/private-MVP handoff readiness, but `readyForProduction` remains false until managed queue/cron, provider/BYOK controls, durable audit, customer acceptance policy, and incident recovery exist.

### 1.6 Team Collaboration Diagnostics

`GET /projects/:id/team-collaboration-diagnostics` exposes `team-collaboration-diagnostics/v1`, the backend break-diagnostics surface for C-side/A-side scheduling. It does not execute work. It diagnoses whether kickoff, Lead/Reviewer governance, role self-marketing, group chat transcripts, owned tasks, brainstorm/evidence handoff, artifact review, revision, final delivery, Flow Graph, Proof Map, timeline, event ledger, and Agent initiative evidence are connected.

Manager Ready Package can embed the same model, Manager Flow Graph exposes it as `team-collaboration-diagnostics`, and Readiness Proof Map exposes `teamCollaborationDiagnosticRoutes` / `teamCollaborationDiagnosticsSummary`. This gives the Manager one route-backed answer to "where is the collaboration chain broken?" without synthesizing a frontend health state. The contract may prove local/private-MVP collaboration readiness, but it keeps `readyForProduction: false` until real provider/BYOK policy, managed queue/cron, durable audit, cost controls, customer acceptance thresholds, and incident recovery exist.

Manager Dashboard also owns coordination display contracts used by the main cockpit. `GET /projects/:id/agent-state-summary` exposes `agent-state-summary/v1` from backend Agent state rows, `GET /projects/:id/assignment-timeline-matrix` exposes `assignment-timeline-matrix/v1` from assignment handoff rows, and `GET /projects/:id/change-flow` exposes `change-flow/v1` from change intake / owner-plan / team-sync rows. These are read-only inspection APIs, not new executors. React can render those rows with the same backend-backed/backend-required/demo source badge used by the routed read models, and Readiness Proof Map exposes matching route collections so real projects do not fall back to browser-derived rows for "which Agent is active", "did an assignment become Agent work", or "did a change request reach owner work". `agent-state-summary/v1` also carries backend-owned `openTaskCount`, `currentTask`, and `currentTaskText` for the Dashboard Agent Current Work Status panel; React only reads browser `activeProject.tasks` in the explicit `agent-state-summary/frontend-fallback` mode. Scenario Control Center's `Kickoff Decisions` status/proof reads backend `managerDashboard.kickoffExecutionFlow`, `Agent Management Sync` reads backend `agent-management-mesh/v1` rows, and `Mid-project Change Intake` reads backend `change-flow/v1` rows for backend-online real projects; local kickoff-charter counts, management timeline logs, and `changeLedger` counts remain offline/demo/development fallback only. The Agent Management Mesh panel now renders `agent-management-mesh-backend-required` plus `agent-management-mesh-sync-cockpit` when the backend mesh is missing, and local management / peer-proof rows are suppressed until Manager Dashboard supplies `agent-management-mesh/v1`. The Manager Dashboard Open Tasks stat and Active Threads list follow the same boundary by preferring `managerDashboard.tasks.openCount` / `managerDashboard.tasks.rows` and showing `backend required` for backend-online real projects when the backend task model is missing. The Project Dashboard header renders `project-dashboard-snapshot-source`, `project-dashboard-snapshot-source-detail`, `project-dashboard-progress-source`, and `project-dashboard-progress-source-detail`, so project status/progress is visibly marked as backend-backed, sample fixture, or frontend fallback. The Project Dashboard stat cards render `project-dashboard-stat-source-focus`, `project-dashboard-stat-source-active-channels`, `project-dashboard-stat-source-open-tasks`, `project-dashboard-stat-source-timeline-logs`, `project-dashboard-stat-source-event-ledger`, `project-dashboard-stat-source-autonomous-cycles`, and `project-dashboard-stat-source-agent-runs`, so top-level kickoff, transcript, task, timeline, event, scheduler-cycle, and run counts show whether they are backend-backed, backend-required, or frontend fallback. Project Dashboard Next Recommendation renders `project-dashboard-next-recommendation-source`; backend-online real projects require `managerDashboard.kickoffExecutionFlow` before recommendation text, next-action resolution, or Agent receipt counts can appear confirmed, and the missing state exposes `project-dashboard-next-recommendation-sync-manager-dashboard` to refresh `/manager-dashboard`. The Active Threads missing-model state exposes `active-threads-sync-manager-dashboard`, which refreshes `/manager-dashboard` instead of falling back to local `activeProject.tasks`. Deferred write receipts now include `agentStateSummaryRoute`, `assignmentTimelineMatrixRoute`, `changeFlowRoute`, and `continuousWorkLoopRoute`; React receipt refresh, dashboard auto-sync, and `Sync Cockpit` fetch those standalone read models into backend station state before falling back to aggregate Manager Dashboard rows.

The Project Dashboard Focus stat follows the same kickoff execution boundary as Next Recommendation: backend-online real projects require `managerDashboard.kickoffExecutionFlow` before Focus can show kickoff consensus, otherwise it displays `backend required` with a backend-required source badge.

The main Manager Proof Map consumes `/projects/:id/readiness-proof-map` as `manager-proof-map/v1` by reading `readiness.checks` from the backend Proof Map. Offline/demo projects can still render the local `evaluateManagerScenarioReadiness` fallback, but backend-online real projects show a backend-required proof-map state when the route is absent or returns no checks. Both the Scenario Readiness blocker and the Proof Map missing row expose `syncBackendReadinessProofMap` actions, so a Manager can retry the route without accepting frontend readiness rows.

Those in-panel recovery actions are not browser-only probes. Next Recommendation, Agent Status, Manager Command Center, Scenario Walkthrough/Trail, Action Playbook, Action Run Ledger, Use Case Audit, Requirement Matrix, Event Ledger, Governance, Group Chat collaboration proof, channel transcript sync, Change Flow, Agent Management Mesh, Agent State Summary, Continuous/Fixed Work Routines, Manager Scenario Readiness, Manager Proof Map, Collaboration Health, Assignment Timeline Matrix, Recent Commit Line, Proof Model, and Proof Map route sync buttons reuse the Backend Worker Station configured-project guard or the equivalent transcript sync guard. They are disabled until the UI has an injected or saved backend API target and `shouldAttemptBackendProjectWrite(activeProject)` confirms a backend-writable real project.

Manager Proof Map renders C/A autonomy route cards for Product Team Operating Loop, Planner / Executor / Reviewer State Machine, Team Collaboration Diagnostics, Runtime Contract Freeze, Autonomous Cycle Consistency, and Runtime Autonomy Status. The cards consume the corresponding backend Proof Map route arrays, expose source badges plus `Sync Proof Map`, can refresh the standalone proof models, link timeline/event proof, and treat summary-only readiness as incomplete route proof.

Manager Proof Map renders Cockpit dispatch route cards for Agent State Summary, Assignment Timeline Matrix, Change Flow, and Continuous Work Loop. The cards consume `agentStateSummaryRoutes`, `assignmentTimelineMatrixRoutes`, `changeFlowRoutes`, and `continuousWorkLoopRoutes`, expose source badges plus `Sync Proof Map`, can refresh the standalone Cockpit read models through `Sync Cockpit`, link timeline/event proof, and treat summary-only readiness as incomplete route proof.

Manager Proof Map renders Output chain route cards for Agent Submissions, Evidence Search, Evidence Quality Audit, Evidence Source Review Workflow, Evidence Index Readiness, Brainstorm Layer, Artifact Quality Audit, Evidence Custody Readiness, and Project Evidence Archive. The cards consume the backend Proof Map route arrays, expose source badges plus `Sync Proof Map`, can refresh standalone proof models, and link chat/timeline proof exits. Per-record submission/search collections fail closed only when records exist without matching route proof, so an empty project is not mistaken for a backend route failure. Readiness Proof Map also projects project-level `evidenceQualityRoutes`, `evidenceSourceReviewWorkflowRoutes`, `evidenceIndexReadinessRoutes`, and `projectEvidenceArchiveRoutes`, giving the Manager direct route proof for evidence judgement, Reviewer source decisions, index readiness, and customer handoff archive coverage.

### 1.7 Collaboration Intent Queue

`GET /projects/:id/collaboration-intent-queue` exposes `collaboration-intent-queue/v1`, the backend meeting and group-chat intent queue for C-side/A-side collaboration. It does not execute work. It ranks kickoff self-marketing, role negotiation, group-chat attention, Manager next action, Agent autonomous initiatives, and review/revision/final-deliverable closure into one proofed queue with `collaboration-intent-protocol/v1`, queue positions, runnable backend routes, proof ids, timeline ids, and event ids.

`POST /projects/:id/collaboration-intent-queue/:intentId/run` is the thin execution bridge for those rows. It selects the queue row, validates the row is runnable, merges the row `requestBodyTemplate` with caller overrides, delegates to the existing Autonomous Run Control, Agent Autonomous Action Queue, or Manager Action Queue route named by `runApiPath`, then persists `collaboration-intent-run/v1` into `collaborationIntentRunLedger`. The Mission Handoff row now carries the required A-side execution controls itself: use Agent autonomous strategy, force the project/Agent handoff scope, submit Agent work artifacts, review pending submissions, respond to review obligations, and target the next generic delivery stage. The receipt records the source/stage/lane, delegated run kind, delegated receipt id, result message ids, proof ids, timeline ids, event ids, output `workSubmissionId` / `artifactId` / `evidenceSearchId` anchors when present, and checksum. The response also returns delegated outputs such as Agent work submissions, artifacts, evidence searches, submission reviews, and review-response submissions when the delegated route produced them; Autonomous Run Control scheduler delegates lift representative due-Agent sweep outputs to the top-level response for this purpose. This lets a C-side `Run intent` click immediately inspect the A-side node it created while keeping queue execution auditable without creating a second runner or a research-specific path. React gives scheduler-backed intent runs a longer backend wait budget so real Agent output can return, and treats failed intent runs as non-proof: it clears the previous run receipt/output from the panel, renders `Intent Run Failed`, and records `localProofCreated: false` instead of reusing stale delegated output.

Manager Ready Package embeds the same model, Manager Flow Graph exposes `collaboration-intent-queue`, and Readiness Proof Map exposes `collaborationIntentQueueRoutes` / `collaborationIntentQueueSummary`. React Backend Worker Station consumes it through `Sync Intent Queue` and renders the queue rows, next runnable intent, route, chat proof action, and timeline proof action. Manager Dashboard also consumes `backendStation.collaborationIntentQueue` as a standalone scoped read model: when `backendManagerReadyPackage` is not available yet, it still renders `backend-collaboration-intent-queue-snapshot`, prioritizes the `customer-agent-handoff-intent` row, runs it through the same queue endpoint, and displays delegated Agent Submission output plus chat/timeline proof exits. Manager Flow Graph node detail renders the protocol, Agent initiative, and production-boundary attachments, and Manager Proof Map renders a dedicated Collaboration Intent Queue proof card with transcript and timeline exits. Proof Map collaboration-intent route cards fail closed with source badges and `Sync Proof Map` actions when `collaborationIntentQueueRoutes` is absent from an otherwise loaded backend Proof Map. This is the current backend answer to the meeting intent protocol and speaker/action queue: who wants to act, why, whether the row can run, and which transcript/timeline/event proof supports it. It can mark local/private-MVP intent routing ready, but `readyForProduction` remains false until managed queue/cron, durable audit, cost controls, customer acceptance thresholds, incident recovery, and provider/BYOK policy are proven.

### 1.8 Runtime Contract Freeze

`GET /projects/:id/runtime-contracts` exposes `runtime-contract-freeze/v1`, the local MVP runtime contract manifest. It freezes the schema/route coverage for Agent submissions and artifact storage proof, evidence search and source/provider proof, Reviewer review and revision closure, transcripts, timeline, event ledger, Manager Flow Graph, Readiness Proof Map, Product Team Operating Loop, Planner / Executor / Reviewer State Machine, and Team Collaboration Diagnostics.

The model returns `contractRows`, `failedLocalContracts`, frozen schema versions, proof ids, timeline ids, event ids, backend routes, and production blockers. Manager Ready Package embeds it, Manager Flow Graph exposes `runtime-contract-freeze`, and Readiness Proof Map exposes `runtimeContractFreezeRoutes` / `runtimeContractFreezeSummary`. It can mark the local/private-MVP contract set frozen, but public production still requires real provider/BYOK, managed database, managed queue/cron, durable audit, cost controls, customer acceptance policy, and incident recovery.

### 1.9 Autonomous Cycle Consistency

`GET /projects/:id/autonomous-cycle-consistency` exposes `autonomous-cycle-consistency/v1`, the read model that proves a bounded autonomous product-team loop can run for N steps without losing backend consistency. The local MVP requirement is three observed steps. The model joins Autonomous Run Control action receipts, bounded loop receipts, Autopilot session ticks, Worker Queue Snapshot recovery state, Product Team Operating Loop, Team Collaboration Diagnostics, Runtime Contract Freeze, Manager Flow Graph, and Readiness Proof Map.

The route returns `consistencyRows`, failed local rows, missing run receipt ids, proof ids, timeline ids, event ids, backend routes, production blockers, and a checksum. It specifically checks that loop receipts reference real child action receipts, action runs have checksum/timeline/event proof, Flow Graph and Proof Map expose the same continuity, operating-loop/diagnostics/runtime-contract routes remain linked after the run, and worker recovery has no dead letters. Manager Ready Package embeds the model, Manager Flow Graph adds `autonomous-cycle-consistency`, Readiness Proof Map exposes `autonomousCycleConsistencyRoutes`, React proof-model sync renders the panel with backend-required missing state, the Research validation sample runs an HTTP three-step loop, and `npm run agents:product-team:cycle-consistency` repeats the same proof through signed membership access before accepting the route. `readyForProduction` remains false until managed durable queue/cron, distributed leases/idempotency, centralized audit, cost controls, and incident recovery are proven.

The Worker Queue Snapshot and Autopilot receipts consume that same row shape. The operating-loop summary can therefore answer not only whether Agents have selected next actions, but whether those intentions are eligible for backend scheduling and whether the latest scheduler tick preserved the candidate initiative set for audit.

The object answers four launch-critical questions:

- What can the Manager run next through a backend route?
- What has the Agent side selected as its autonomous strategy?
- Which generic delivery stage is ready or missing?
- Which proof, timeline, and event routes prove the loop, and which production autonomy controls remain blocked?

The local gate passes only when C-side control, A-side autonomy, the generic delivery trace, submission/review/revision/final artifact coverage, proof surfaces, and scheduler/worker continuation are all present. `readyForProduction` remains false because this route intentionally requires managed queue/cron, durable persistence and audit, real provider/BYOK policy, cost controls, customer acceptance policy, and incident recovery before public autonomy can be claimed. React `Sync Proof Models` now fetches and renders the route in Manager Ready Package with backend-required missing state for backend-online real projects; `npm run agents:product-team:cycle-consistency` remains the focused route-level proof for the three-step autonomous loop.

`GET /projects/:id/runtime-autonomy-status` exposes `runtime-autonomy-status/v1`, the C/A runtime aggregation for the current project. It joins Product Team Mission Runner startup, Autopilot session/tick receipts, action/loop receipts, Worker Queue Snapshot recovery, worker queue adapter dry-run, persistence snapshot, Product Team Operating Loop, Autonomous Run Control, Manager Flow Graph, Readiness Proof Map, `/workers/autonomous/status`, `/workers/autonomous/tick`, and `/workers/autopilot/due`. It can mark local/private-MVP autonomy ready when the C-side mission handoff, A-side receipts, proof ids, queue recovery, adapter rehearsal, and persistence snapshot are connected. It always keeps `readyForUnattendedProduction: false` until managed queue/cron, distributed leases, managed persistence restore proof, BYOK/provider cost controls, centralized observability, immutable audit, and incident recovery drills exist. Readiness Proof Map exposes `runtimeAutonomyStatusRoutes`, Manager Flow Graph renders `runtime-autonomy-status`, and the product-team core plus Research validation gates verify the API, Proof Map route, Flow Graph node, Ready Package embedding, scheduler recovery links, and production boundary.

## 2. Agent Subjective Agency

Each Agent should behave as an independent project participant, not as a decorative persona.

### 2.1 Existing Prototype Behavior

The prototype already supports several forms of Agent agency:

- Agents ask role-clarification questions during kickoff.
- Agents self-nominate for work lanes before Leader confirmation.
- Leader candidates present campaign-style claims before the Director confirms the Leader.
- Agents receive direct group-chat mentions into their inbox and obligations.
- Agents publish autonomous work updates through project cycles.
- Agents can send Agent-to-Agent messages through the backend route.
- Agents can be contracted into a project roster through a backend Agent contract route, producing timeline, event-ledger, Flow Graph, and Agent Dashboard proof.
- Agent state is queryable through per-Agent dashboards.

Relevant implementation:

- `src/agents/agentRuntime.js`
- `src/agents/agentProjectService.js`
- `src/agents/agentProjectApi.js`
- `src/agents/agentProjectHttpServer.js`
- `src/skills/personSkillSystem.js`

### 2.2 Agent Self-Marketing

Agent self-marketing means an Agent can actively explain why it should own a role, lead a work lane, review a risk, or submit a particular artifact.

This is not advertising copy. It is an operational signal.

Examples:

- "I should own the market-sizing node because my evidence discipline fits the uncertainty."
- "I should review this decision because the plan depends on assumptions that need stress testing."
- "I am submitting a discovery memo node and asking the Reviewer to validate source quality."

In the current prototype, this appears in:

- role clarification
- self-nomination
- Leader campaign turns
- Agent worklog summaries
- management-priority reasons

Current implementation:

- `Manager Flow Graph` now emits `self-marketing` nodes for role self-nominations and Leader campaign pitches.
- The kickoff meeting UI now renders each Leader candidate's campaign claim before the Director confirms the Leader, so self-marketing is visible during the decision rather than only after project creation.
- Role-clarification turns are emitted as proofed kickoff conversation nodes.
- `Readiness Proof Map` exposes `roleNegotiationRoutes`, `roleNegotiationSummary`, `selfMarketingRoutes`, and `selfMarketingSummary` so API clients can inspect who pitched, who heard it, and which transcript/event proof supports it.
- Product-team acceptance validation fails if those self-marketing nodes or proof routes disappear.

## 3. Flow Graph Submission System

The flow graph should not only visualize system state. It should become the team's submission surface.

Each Agent should be able to submit work products into the graph under a typed node contract. The manager can then inspect submissions, compare them, approve them, request changes, or open a meeting around them.

### 3.1 Current Prototype Basis

The project already has several evidence streams that can support this:

- `kickoffCharter`: approved startup agreement.
- `agentContracts`: backend roster contract records for Agents added from the marketplace into a project.
- `tasks`: owned work items with assignment and evidence ids.
- `logs`: project timeline records.
- `eventLedger`: append-only event stream with sequence numbers.
- `changeLedger`: accepted project changes and owner sync.
- `agentStates`: inbox, obligations, worklog, current plan.
- `evidenceSearches`: Agent-recorded evidence/source searches with query, source quality, findings, proof ids, and task/submission links.
- `submissionReviews`: Reviewer verdict records with accepted / changes-requested state, comments, requested changes, proof ids, and task/submission links.
- `managerFlowGraph`: manager-readable graph projection.
- `managerDashboard`: aggregate manager read model.
- `readinessProofMap`: proof routes back to chat, timeline, tasks, events, and Agent dashboards.

The prototype now supports explicit Agent roster contracts, Agent submissions, evidence searches, and submission reviews through backend service/API routes. Agents added from the marketplace are stored as first-class `agentContracts` and project team members, Agent-submitted artifacts are stored as first-class `agentSubmissions` project records, evidence/source searches are stored as `evidenceSearches`, and Reviewer verdicts are stored as `submissionReviews`. Manager Flow Graph submission, evidence-search, evidence-source-review, and submission-review nodes plus the matching Readiness Proof Map routes expose direct resource ids/routes (`submissionId` / `submissionRoute`, `evidenceSearchId` / `evidenceSearchRoute`, `evidenceSourceReviewId` / `evidenceSourceReviewRoute`, `submissionReviewId` / `submissionReviewRoute`), so the UI can jump to backend resources without inferring identity from log text or node ids.

Every Agent-submitted artifact now carries `agent-artifact-storage-proof/v1`. The proof records backend storage type/status, workspace-relative path or URL, disk-existence state for the local Harness, content/path checksums, and a proof checksum. The same proof checksum is copied into submission evidence, Flow Graph attachments, Readiness Proof Map routes, Artifact Quality Audit rows, Project Evidence Archive manifests/contents, and production persistence snapshot rows, so a C-side node can prove it came from the backend artifact pipeline instead of a frontend mock. Project Evidence Archive also exposes `artifactStorageProofCoverageReady`, `artifactStorageProofCount`, `workspaceFileProofCount`, and an `artifact-storage-proof-coverage` integrity gate, making one-to-one submission-to-artifact proof coverage a private-pilot handoff invariant.

#### Receipt-First C/A Refresh Contract

Agent write routes, Manager write routes, kickoff approval, launch/security receipts, and worker controls can return lightweight receipts with `includeReadModels: false`. The receipt must include explicit read-model routes. For Agent-side writes, the base route set now includes `projectRoute` as well as Manager Dashboard, Manager Ready Package, Manager Flow Graph, Agent Dashboard, Product Team Delivery Trace, Planner / Executor / Reviewer State Machine, diagnostics, runtime contracts, Autonomous Run Control, and Agent Autonomous Action Queue routes.

React Agent write handling consumes `projectRoute` first, then refreshes the Manager/Agent read models and schedules transcript, timeline, event, and Ready Package proof-model sync. This is the C-side/A-side truth boundary: the browser may display local state as cache, but a backend-online project is considered updated only after the authoritative backend project snapshot and proof models have been read back.

#### Event Ledger Retention and Replay Recovery

The local event ledger is append-only, sequence-numbered, and retained in a bounded project snapshot window. The current local window is `EVENT_LEDGER_RETAINED_LIMIT = 5000`, which is intentionally larger than the full manager/product-team Harness path so Agent-to-Agent messages, worker ticks, reviews, and production rehearsal receipts do not evict early kickoff proof during a normal private-MVP run.

Readiness must not depend only on the currently retained event window. `summarizeProjectEventLedger` and `projectEventReplayProjection` can recover core replay coverage from durable project records such as `kickoffCharter`, `changeLedger`, `peerHandoffs`, and `autonomousSchedulerLedger` when older ledger events have been pruned. This keeps restarted file/HTTP backends manager-ready after long A-side activity while still preserving the retained sequence window and total event count. Public production still needs managed immutable audit storage and retention policy; this is a local/private-pilot reliability bridge, not the final audit system.

### 3.2 Artifact Draft Generation Contract

Agent work should not depend on an external caller hand-writing every artifact body. The backend exposes a draft-generation step that lets an Agent compose a generic product-team artifact from the current project state, then optionally submit it through the same first-class submission path.

Route:

- `POST /projects/:id/agents/:agentId/artifact-drafts`

Inputs:

- `artifactType`: generic product-team type such as `progress-brief`, `discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `implementation-plan`, `revision-note`, or `final-deliverable`
- `taskId`: optional task context
- `instruction`: the Agent's purpose for the draft
- `evidenceSearchIds`: evidence rows to summarize or cite
- `priorSubmissionIds`: earlier artifacts to build from
- `reviewIds`: Reviewer signals or requested changes to answer
- `submit`: when true, the generated draft is immediately persisted as a standard Agent submission
- `useModel` / `requireModel`: controls whether BYOK model execution is attempted or required

Output:

```json
{
  "schemaVersion": "agent-artifact-draft/v1",
  "artifactType": "progress-brief",
  "title": "Manager-readable title",
  "summary": "One sentence summary",
  "body": "Markdown artifact body",
  "source": "local-artifact-draft-generator",
  "modelUsed": false,
  "proofContext": {
    "taskId": "task_id",
    "evidenceSearchIds": ["evidence_search_id"],
    "submissionIds": ["prior_submission_id"],
    "reviewIds": ["review_id"]
  },
  "artifactDraftQuality": {
    "schemaVersion": "artifact-draft-quality/v1",
    "status": "local-quality-ready",
    "readyForLocalPilot": true,
    "readyForProduction": false,
    "humanReviewRequired": false
  },
  "checksum": "sha256:..."
}
```

If `submit: true`, the response includes the normal `submission`, `artifact`, `log`, and `task` proof surfaces. By default it also includes Manager read models for compatibility. When the caller sends `includeReadModels: false`, the response becomes a lightweight action receipt with `readModels.included: false` plus the routes to refresh Manager Dashboard, Manager Ready Package, Manager Flow Graph, and Agent Dashboard separately. The same generated body is then visible through Flow Graph submission nodes, Task Evidence, Agent Dashboard, Manager Dashboard, Readiness Proof Map, timeline, event ledger, workspace artifact files, project archive, and persistence snapshot rows. The persisted submission carries an `artifactDraft` link with draft id, source, checksum, proof context, model-provider metadata, local/model generation status, and the same artifact storage proof checksum as manual submissions so generated work can survive Manager UI reads, project archive handoff, and database migration.

Model-backed generation uses provider policy operation `model:artifact-draft`. When the model provider is disabled or a request sets `useModel: false`, the local fallback generator keeps the Harness deterministic. Every draft now carries `artifact-draft-quality/v1`, including title/summary/body coverage, generic product-team language, task/evidence/review context, handoff clarity, artifact-type alignment, redaction status, and model provenance. Model-authored drafts must also mark `humanReviewRequired: true`; Provider Readiness exposes this as the `model-artifact-draft-quality` gate and `model-output-quality-review` production control. The product-team Harness runs both local fallback and deterministic OpenAI-compatible model-provider drafts, requiring provider-usage ledger proof, quality-gate proof, human-review proof, and model provenance on the submitted artifact. This proves the BYOK route shape and local pilot quality boundary without claiming production-quality LLM authorship.

### 3.2.1 Deferred Read-Model Refresh Contract

Frontend mock replacement should use backend action receipts and backend read models as two separate steps.

The following first-wave Agent write routes support `includeReadModels: false`:

- `POST /projects/:id/agents/:agentId/submissions`
- `POST /projects/:id/agents/contract`
- `POST /projects/:id/agents/:agentId/evidence-searches`
- `POST /projects/:id/agents/:agentId/artifact-drafts`

With the flag disabled, the route still persists the work, writes chat/timeline/event proof, returns the direct receipt, and exposes:

```json
{
  "readModels": {
    "included": false,
    "reason": "deferred-by-request",
    "managerDashboardRoute": "/projects/:id/manager-dashboard",
    "managerReadyPackageRoute": "/projects/:id/manager-ready-package",
    "managerFlowGraphRoute": "/projects/:id/manager-flow-graph",
    "agentDashboardRoute": "/projects/:id/agents/:agentId/dashboard"
  }
}
```

The same deferred protocol now covers the main Manager and worker write paths used while replacing frontend mocks:

- `POST /projects/:id/manager-command-center/run-next`
- `POST /projects/:id/manager-scenario-walkthrough/:stepId/run`
- `POST /projects/:id/manager-action-queue/:actionId/run`
- `POST /projects/:id/chat`
- `POST /projects/:id/meeting`
- `POST /projects/:id/change-request`
- `POST /projects/:id/autonomous-cycle`
- `POST /projects/:id/agents/:agentId/message`
- `POST /projects/:id/agents/:agentId/work-cycle`
- `POST /projects/:id/agent-autonomous-action-queue/:agentId/run`
- `POST /workers/autonomous/due`
- `POST /workers/agents/due`
- `POST /workers/autopilot/due`

The HTTP async wrapper respects `includeReadModels: false`, and scheduler ticks now propagate that flag into project, Agent, and opt-in Autopilot due-worker calls. The UI should then fetch the standalone read-model routes. It should not synthesize Manager rows while waiting. The product-team acceptance Harness requires this deferred contract for evidence search, explicit submissions, and generated artifact drafts, then verifies the authoritative Manager Dashboard, Manager Ready Package, Flow Graph, Proof Map, Agent Dashboards, archive, and persistence rows afterward. The manager scenario Harness now also requires lightweight Manager/worker/chat/change receipts and their refresh routes; `npm run agents:scenario:contract` checks the low-write Manager command, Agent message, marketplace Agent contract, Workbench, and timeline action proof slices before the long `npm run agents:scenario` regression is needed.

`POST /projects/:id/meeting` now returns backend-authored `meeting-agent-turn/v1` rows as `meetingAgentTurns` for ordinary War Room discussion, meeting startup, and meeting change requests. The service uses the shared Agent roundtable runtime, persists those turns as `war-room-meeting-agent-turn` messages, writes `meeting-agent-turn` timeline logs, applies them to Agent inbox/worklog state, and records event-ledger proof ids (`evt_chat_*`). React War Room playback consumes those returned turns first; legacy War Room `Start Meeting` also calls this backend command before opening the session for real projects. Local meeting simulation remains only an offline/demo/development fallback. If a backend-required real project receives a successful meeting response without Agent turns, the UI now records `Backend meeting returned no Agent turns; local simulation blocked` and does not synthesize browser-only speech. Legacy terminal directives are restored to the input if the backend write fails or returns no Agent turns, and a failed backend close keeps the War Room open instead of presenting an unproven close as complete. This closes the gap where a backend-online project could show Agent meeting speech or meeting closure that never entered transcripts, timeline, or the event ledger.

`POST /projects/:id/transcripts/:channelId/channel-pin` records `transcript-channel-pin/v1` for a whole transcript channel. It appends a channel-visible system message, writes `transcript-channel-pinned` timeline/event-ledger proof, exposes channel `channelPins`, adds Readiness Proof Map `transcriptChannelPinRoutes`, and gives Manager Flow Graph a proofed `transcript-channel-pin` node. React Group Chat uses this route for the header pin control, so channel pin state is backend-backed instead of a browser-only header flag. `npm run agents:transcript-channel-pin` verifies the route without browser/server startup or workspace artifact writes.

`GET /projects/:id/artifact-quality-audit` exposes the aggregate `artifact-quality-audit/v1` read model for submitted product-team artifacts. It is a Manager-facing artifact readiness contract, not a replacement for formal human review. The audit checks required generic artifact type coverage, title/summary/body readiness, chat/timeline/event/artifact proof links, checksummed artifact storage proof, review/revision/final-deliverable closure, generated draft quality status, redaction status, local decision gates, production controls, and checksum. Manager Ready Package embeds it; Readiness Proof Map exposes `artifactQualityRoutes`; Project Evidence Archive includes it in readiness models, manifest, integrity gates, and full contents; Pilot Launch Readiness and Production Launch Audit link it as an evidence route. The local rubric can support private-pilot handoff, while production remains blocked for calibrated artifact rubrics, eval datasets, human release policy, managed output audit storage, and retention controls.

Agent artifact storage paths are intentionally bounded. The backend keeps long user-facing titles, task text, submission ids, and content in the submission contract and checksums, but local workspace filenames use short hash-suffixed names so validation and cleanup remain reliable on Windows path limits. `npm run agents:artifact-paths` verifies autonomous work-cycle artifacts, direct Agent submissions, and generated draft submissions still write real files with `agent-artifact-storage-proof/v1` while keeping relative and absolute paths bounded.

`GET /projects/:id/submission-review-workflow` exposes the aggregate `submission-review-workflow/v1` read model for submitted artifact review closure. It is a Manager-facing workflow contract for generic product-team review rounds, not a research-specific paper review feature. The workflow aggregates submission review rows, requested changes, linked revision responses, final-deliverable acceptance, proof routes, timeline/event ids, local closure gates, production controls, and checksum. Manager Ready Package embeds it; Manager Flow Graph adds a `submission-review-workflow` closure node; Readiness Proof Map exposes `submissionReviewWorkflowRoutes`; Security Boundary and enforced access policy cover the route; Manager UI renders the workflow snapshot. The local contract can prove private-pilot review closure, while production remains blocked for calibrated reviewer policy, durable Reviewer identity lifecycle, immutable output audit storage, and customer-specific acceptance thresholds.

### 3.3 Proposed Submission Node Contract

A submitted node should follow a stable shape:

```json
{
  "id": "submission_project_agent_timestamp",
  "projectId": "project_id",
  "agentId": "agent_id",
  "nodeType": "agent-submission",
  "artifactType": "discovery-report",
  "title": "Market map for segment A",
  "summary": "Short manager-readable summary",
  "body": "Inline content or pointer to a workspace file",
  "workspacePath": "reports/market-map.md",
  "artifactChecksum": "sha256:...",
  "artifactStorageStatus": "local-file-written",
  "artifactStorageProofChecksum": "sha256:...",
  "artifactStorageProof": {
    "schemaVersion": "agent-artifact-storage-proof/v1",
    "relativePath": "artifacts/submission_project_agent_timestamp.md",
    "contentChecksum": "sha256:..."
  },
  "status": "submitted",
  "reviewStatus": "pending-review",
  "taskId": "linked_task_id",
  "dependsOn": ["node_or_task_id"],
  "revisesSubmissionId": "previous_submission_id_or_null",
  "respondsToReviewId": "submission_review_id_or_null",
  "supersedesSubmissionIds": ["superseded_submission_id"],
  "evidenceIds": ["chat_message_id", "timeline_log_id", "event_id"],
  "requestedReviewAgentId": "reviewer_agent_id",
  "createdAt": "ISO timestamp",
  "updatedAt": "ISO timestamp"
}
```

Initial artifact types:

- `discovery-report`
- `research-report`
- `evidence-packet`
- `brainstorm-board`
- `product-brief`
- `decision-proposal`
- `risk-review`
- `implementation-plan`
- `progress-brief`
- `revision-note`
- `final-deliverable`

Initial node statuses:

- `drafting`
- `submitted`
- `under-review`
- `accepted`
- `changes-requested`
- `superseded`

Revision lineage:

- `revisesSubmissionId`: the prior submission this artifact revises.
- `respondsToReviewId`: the Reviewer verdict this artifact answers.
- `supersedesSubmissionIds`: submissions replaced by this artifact.

When a linked revision is submitted, the previous submission is marked `superseded`, the submitter's matching review obligation is resolved, Manager Flow Graph emits `revision` edges, and Readiness Proof Map exposes `revisionRoutes` / `revisionSummary`.

### 3.4 Evidence Search Contract

Evidence search is a generic product-team primitive, not a research-only feature. An Agent can record a source search, link it to a task or submission, and make it visible through group chat, timeline, event ledger, task evidence, Agent Dashboard, Manager Dashboard, Manager Flow Graph, and Readiness Proof Map.

```json
{
  "id": "evidence_search_project_agent_timestamp",
  "agentId": "agent_id",
  "query": "market / product / technical evidence question",
  "purpose": "why this search matters",
  "provider": "search provider or harness source",
  "searchMode": "agent-directed",
  "status": "completed",
  "confidence": "high",
  "taskId": "linked_task_id",
  "submissionId": "optional_submission_id",
  "evidenceJudgement": "strong-evidence",
  "qualityScore": 82,
  "qualitySummary": {
    "judgement": "strong-evidence",
    "decisionUse": "decision-ready",
    "strongSourceCount": 2
  },
  "sources": [{
    "title": "Source title",
    "kind": "runtime-proof",
    "confidence": "high",
    "qualityScore": 90,
    "qualityLevel": "strong",
    "qualitySignals": ["confidence:high", "internal-runtime-proof"]
  }],
  "findings": ["short finding"],
  "evidenceIds": ["chat_message_id", "timeline_log_id", "event_id"]
}
```

Evidence judgement is deterministic and provider-agnostic in the prototype. The service scores each source from confidence, kind, URL/source trace, summary presence, and internal runtime-proof status, then stores an aggregate `qualitySummary`. Manager Dashboard, Manager Flow Graph, Task Evidence, and Readiness Proof Map all expose this judgement so the Reviewer can distinguish decision-ready evidence from material that needs corroboration.

`GET /projects/:id/evidence-quality-audit` exposes the aggregate `evidence-quality-audit/v1` read model. It is a Manager-facing decision-readiness contract, not a replacement for the raw evidence-search rows. The audit includes:

- redacted evidence-search rows with query/purpose previews, provider provenance, quality summary, source-safety summary, proof route, and checksum
- flattened source rows with quality score/level/signals and source-safety level/score/signals
- decision gates for evidence presence, source count, quality judgement, source safety, and proof-route coverage
- production controls for a real search gateway, calibrated source-quality policy, human review policy, and managed provider/source audit storage

The model can return `readyForDecision: true` for local/private-pilot acceptance while always keeping `readyForProduction: false` until production evidence controls exist. Manager Ready Package embeds this audit, `project-evidence-archive/v1` carries it in readiness models and manifest, and launch evidence routes point to it so a Manager can verify why a product decision is evidence-backed.

`GET|POST /projects/:id/evidence-source-review-workflow` exposes `evidence-source-review-workflow/v1` and writes `evidence-source-review/v1` decisions. `GET` is the reviewer-facing governance layer above the raw source rows: the service derives one review item per source, attaches reviewer handoff, quality/source-safety signals, local `evidence-source-snapshot/v1` ids/checksums, `evidence-provider-receipt/v1` ids, latest Reviewer decision, local decision use, production decision use, proof route, gates, required production controls, and checksum. Required source decisions must be submitted before the local/private-pilot workflow becomes ready; the `source-review-decisions-ready` gate fails while required sources are pending, under review, quarantined, rejected, or waiting for corroboration. `POST` records a Reviewer decision for `{ evidenceSearchId, sourceId }`, publishes group-chat proof, writes timeline and event-ledger proof, updates the evidence search, task evidence, Agent dashboards, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, and normalized `evidence_source_reviews` persistence rows. The workflow always keeps `readyForProduction: false` until human source-review approvals, calibrated source-quality policy, immutable managed source/provider receipt storage, and reviewer audit storage exist. Enforced access policy allows Manager/security-admin/observer reads and Manager/reviewer-agent/security-admin writes, with reviewer-agent writes scoped by `reviewerAgentId`.

### 3.5 Submission Review Contract

Submission review is also a first-class project resource. A Reviewer can accept a submission or request changes. The result updates the linked submission and task, creates a review node in the flow graph, publishes a review message into group chat, and appends a ledger event.

```json
{
  "id": "submission_review_project_submission_reviewer_timestamp",
  "submissionId": "agent_submission_id",
  "reviewerAgentId": "reviewer_agent_id",
  "submitterAgentId": "submitter_agent_id",
  "verdict": "changes-requested",
  "comments": "manager-readable review note",
  "requestedChanges": ["revision request"],
  "evidenceIds": ["chat_message_id", "timeline_log_id", "event_id"]
}
```

### 3.6 Flow Graph Node Categories

The manager flow graph should distinguish:

- intent nodes: Director brief, meeting intent, change request
- self-marketing nodes: role pitch, Leader campaign, ownership proposal
- coordination nodes: assignment, handoff, blocker, queue item
- execution nodes: work pulse, artifact draft, submission
- review nodes: Reviewer challenge, evidence request, approval
- evidence nodes: evidence search, source packet, chat proof, timeline proof, workspace file, ledger event

The important principle is that the graph should answer:

- who proposed this work
- who accepted ownership
- what was submitted
- who reviewed it
- where the proof lives
- what changed after review

## 4. Working Mode

### 4.1 Continuous Autonomous Work

After kickoff approval, Agents should continue working without the Director repeatedly prompting them.

The current runtime supports this through:

- project-level autonomous cycles
- per-Agent work cycles
- due-worker scans
- scheduler status/tick/start/stop controls
- project and Agent worker ledgers

Core functions:

- `planAutonomousWorkCycle`
- `advanceAutonomousProjectCycle`
- `publishAutonomousCycleChat`
- `evaluateAutonomousSchedule`
- `runProjectAutonomousCycle`
- `evaluateAgentWorkSchedule`
- `runAgentWorkCycle`
- `runDueProjectAutonomousCycles`
- `runDueAgentWorkCycles`

### 4.2 Group Chat Coordination

Group chat is not only a visual chat surface. It is the collaboration bus.

Messages can:

- mention one Agent or all Agents
- create inbox entries
- create obligations
- trigger Leader assignments
- trigger peer handoffs
- trigger feature-change discussions
- create evidence for readiness and dashboard proof routes

Every important message should retain:

- source channel
- author
- direct targets
- receipts / heard-by evidence
- linked task or event ids
- proof route back to the exact transcript item

Meeting turns follow the same rule. A Director War Room command is written as `war-room-meeting-message`; the backend then emits `meeting-agent-turn/v1` replies with speaker, role, rank, proof ids, timeline log ids, and event ids. Frontend meeting animation must render those backend turns for real projects instead of generating a browser-only roundtable.

### 4.3 Leader, Reviewer, and Meetings

The system uses governance rather than free-for-all conversation.

Leader responsibilities:

- frame agenda
- assign owners
- track deadlines
- resolve handoffs
- decide whether a question needs Director input
- maintain execution continuity

Reviewer responsibilities:

- challenge weak evidence
- inspect risk
- request verification
- prevent decisions from becoming durable without support

Meeting protocols:

- Kickoff: role clarification, self-nomination, Leader campaign, first assignments.
- Sync: progress, blockers, deadline pressure, next delivery.
- Review: evidence, severity, fix owner, verification.
- Working Discussion: handoff, interpretation, action, request.
- Change Meeting: new intent, team discussion, owner confirmation, plan sync.

### 4.4 Queue System

There are three queue-like layers:

1. Project due queue: which projects need an autonomous cycle.
2. Agent due queue: which Agents need an independent work cycle.
3. Manager action queue: which manager-readable actions can be run or verified next.

The queue system should be observable. A manager should be able to see:

- why an Agent is due
- why an Agent was skipped
- which management-priority reasons moved an Agent forward
- which queue item produced which chat/timeline/event proof

Current local backend contract:

- `GET|POST /workers/queue-snapshot` and `GET|POST /projects/:id/worker-queue` return `worker-queue-snapshot/v1`.
- Project and Agent queue rows include stable `idempotencyKey`, `leaseKey`, due-worker route, direct recovery route, retry state, and an expected execution receipt marker.
- Completed project scheduler and Agent worker runs expose `worker-execution-receipt/v1` rows with receipt checksums, message proof ids, timeline proof ids, run status, idempotency key, and lease key.
- The snapshot includes `worker-queue-retry-policy/v1` and `worker-dead-letter-policy/v1` so local MVP validation can prove retry/dead-letter semantics before a production queue adapter exists.
- `GET /projects/:id/worker-queue-adapter-plan` returns `worker-queue-adapter-plan/v1`, the minimum production queue/cron adapter method contract for enqueue, lease acquisition, dispatch, receipt ack, retry, dead-letter, recovery, and inspection.
- `GET /projects/:id/worker-queue-adapter-dry-run` returns `worker-queue-adapter-dry-run/v1`, executing the queue cutover checks through `src/agents/workerQueueAdapter.js` by default. The adapter facade exposes `worker-queue-adapter-status/v1` from `WORKER_QUEUE_ADAPTER_DRIVER`, `WORKER_QUEUE_HTTP_ENDPOINT`, `ADAPTER_GATEWAY_HTTP_ENDPOINT`, and `WORKER_QUEUE_REQUIRE_REAL_ADAPTER`. The default `local-shadow` driver emits `worker-queue-adapter-execution-receipt/v1` rows for enqueue, lease acquisition, dispatch, execution receipt ack, retry import, dead-letter storage/recovery, queue inspection, and `worker-queue-adapter-snapshot-parity/v1`. When `WORKER_QUEUE_ADAPTER_DRIVER=http-json` and an endpoint is configured, the API async handler calls `POST /worker-queue/dry-run` on the private gateway and returns `adapterExecution.schemaVersion = worker-queue-adapter-gateway-execution/v1` with the gateway `worker-queue-adapter-execution-receipt/v1`. `managed-queue` is still a future real driver; all dry-run paths keep `productionCutoverReady` false until real queue cutover is separately approved.
- `GET /projects/:id/operations-readiness` checks that worker runs, receipts, retry state, dead-letter summary, queue adapter dry-run, managed persistence adapter dry-run, alert rules, persistence export, recovery runbook, and `operations-incident-drill/v1` rehearsal receipts are visible in Manager Ready Package.

## 5. Engineering Implementation

### 5.1 Runtime Layer

The runtime layer is deterministic and model-agnostic. It owns collaboration behavior:

- Agent network creation
- meeting frames
- communication routing
- attention scoring
- Leader assignments
- peer handoffs
- feature changes
- autonomous cycles
- event ledger generation

Main file:

- `src/agents/agentRuntime.js`

### 5.2 Service Boundary

The service layer turns runtime behavior into backend-style commands that React, tests, workers, or HTTP routes can call.

Main file:

- `src/agents/agentProjectService.js`

Important capabilities:

- create durable kickoff meeting sessions
- approve kickoff sessions into projects
- submit project chat
- submit meeting startup, meeting input, and meeting changes
- run project cycles
- run per-Agent cycles
- evaluate readiness
- build manager dashboards
- build manager flow graphs
- expose Agent dashboards

### 5.3 Storage Adapters

Current storage is prototype-grade but has a replaceable boundary.

Adapters:

- `createAgentProjectMemoryStore`: in-memory test/prototype store.
- `createAgentProjectFileStore`: JSON file-backed store for Node local backend.
- Browser `localStorage`: React offline/cache fallback for the prototype. It is no longer the intended primary source for backend-online project list/load/read flows; real project writes should go through backend receipt routes.

Initiation Roundtable free input follows the same backend boundary as explicit role-question clarification. When a durable kickoff meeting session exists, React posts free-form Manager meeting input to `POST /kickoff-meetings/:id/clarify`, updates the saved meeting transcript/evidence, and blocks local mock meeting turns if the backend session is missing or the write fails. The project approval step then carries that clarification evidence into the kickoff charter, Manager Dashboard, Flow Graph, transcript, timeline, and event proof routes.

Production direction:

- append-only project event table
- durable message transcript table
- task/submission table
- Agent state table
- worker queue table
- workspace artifact storage

### 5.4 Frontend Mock Replacement Boundary

`docs/FRONTEND_MOCK_REPLACEMENT_REGISTER.md` tracks the remaining frontend mock and fallback surfaces. The UI now distinguishes:

- `backend-backed`: rendering backend read models or backend action receipts
- `backend-required`: backend is online but a required read model is missing
- `frontend-fallback`: local/offline/demo rows synthesized by React
- `sample-fixture`: explicit demo/sample project data
- `validation-fallback`: deterministic provider or generator used only by Harnesses

The Workspace Active Portfolios list uses the same boundary at project-row level. Each project row renders a visible `backend-backed`, `sample-fixture`, or `frontend-fallback` badge; backend-backed requires backend catalog/detail/receipt evidence, while frontend fallback means the row came from browser cache and must be synced or recreated through backend routes before it can be treated as a real project. Cached Product Team Delivery Trace, Operating Loop, Planner/Executor/Reviewer, Runtime Autonomy, Autonomy/Intent queue, Zero-to-Autonomy Report, Project Evidence Archive, acceptance-chain, and Settings provider/runtime/integration readiness read models are also backend-managed markers, so older backend snapshots with newer proof models are filtered out of browser project/chat cache instead of rehydrating as local projects. If the backend is online and `/projects` has not synced, the list renders `workspace-portfolio-catalog-required` with a Sync Backend Projects action instead of showing "No projects yet" as if the backend catalog were known. The Workspace Active Projects and Backend Projects stats also follow `/projects`: when the backend is online they render the synced catalog count or `backend required`, not browser `projects.length` or a misleading unsynced backend `0`, with `workspace-stat-source-active-projects` and `workspace-stat-source-backend-projects` exposing the source.

Workspace Hub aggregate task and message counts follow the backend catalog boundary as well. When the backend is online, the Open Tasks stat reads synced backend catalog task summaries or task rows, and Stored Messages reads synced catalog transcript summaries, message counts, or message rows. If the catalog has not synced or lacks the relevant task/message evidence, React shows `backend required` instead of summing browser `projects[].tasks` or `chatMessages.length`. Browser task/message totals remain an offline/demo fallback only. The Open Tasks and Stored Messages stat cards render visible source labels through `workspace-stat-source-open-tasks` and `workspace-stat-source-stored-messages`. Active Portfolios row progress bars use the same project provenance through `project-progress-source-*`, so browser-cached progress cannot look like backend catalog truth.

For real projects with an online backend, Manager governance surfaces must not silently synthesize frontend rows. `src/App.jsx` now gates Manager Command Center, Scenario Walkthrough, Use Case Audit, Action Queue, Sync Protocol, Manager Flow Graph, and production launch/control/evidence/receipt fallback rows behind demo/offline mode. If the backend is online and a read model is absent, the UI renders `backend model missing` instead of pretending the model is complete. Project initiation approval also fails closed by default when backend approval fails: the user stays in the initiation decision step, sees `Backend initiation approval failed`, and no local project is created. Local kickoff project creation after backend failure is only available through an explicit development fallback flag (`window.__HOF_ALLOW_DEV_INITIATION_FALLBACK__`, `hall_of_fame_studio.dev_initiation_fallback.v1`, or `VITE_HOFS_ALLOW_LOCAL_INITIATION_FALLBACK=true` in dev). Those development fallback projects are tagged `development-fallback`, are visibly non-real, and are excluded from backend write commands so they cannot masquerade as launch evidence. Chat, Roundtable meeting, legacy War Room terminal, Manager change intake, Leader assignment, Manager Flow Graph confirmation, marketplace Agent contract, project language settings, project privacy policy settings, and manual autonomous pulse controls now follow the same fail-closed rule for backend-synced real projects: C-side write commands attempt the configured backend route even if the local station status display is stale; only an actual backend command failure on an offline-only project can enter local fallback, while projects already seen in the backend catalog, project sync, or Manager Dashboard sync stay backend-required. Real-project failures set a visible backend error and stop local mutation; chat and War Room meeting failures also restore the unsent draft instead of discarding user input or fabricating a local message. `PUT /projects/:id/project-settings` persists `project-settings/v1` language settings plus `project-privacy-policy/v1` retention/provider-log/training/export-approval policy with audit, timeline, and event-ledger proof so Agent/read-model language and local MVP privacy posture are backend project facts instead of browser-only preferences. Local runtime fallback is limited to offline-only projects, explicit demo/sample projects, explicit development-fallback projects, or development-build fallback for projects with no backend kickoff/managed/sync evidence (`window.__HOF_ALLOW_LOCAL_RUNTIME_FALLBACK__`, `hall_of_fame_studio.dev_local_runtime_fallback.v1`, or `VITE_HOFS_ALLOW_LOCAL_RUNTIME_FALLBACK=true` in dev); production builds ignore those flags. The development flag no longer changes backend-managed project classification: cached backend projects are still dropped from browser project/chat cache, and backend transcript, timeline, and Agent Dashboard proof remain required once backend ownership evidence exists. The browser-local one-minute autonomous loop and direct local autonomous-cycle helper use the same gate, so a stale offline status or legacy UI path cannot create local work for a backend-synced project. The UI also syncs a backend project catalog from `GET /projects`, opens backend-online projects through `GET /projects/:id`, disables browser snapshot seeding for real backend projects, refreshes group-chat transcript read models from `GET /projects/:id/transcripts` plus channel transcript routes, refreshes workflow proof from `GET /projects/:id/timeline` plus `GET /projects/:id/events`, and refreshes core Ready Package proof submodels from `GET /projects/:id/brainstorm-layer`, `GET /projects/:id/artifact-quality-audit`, `GET /projects/:id/submission-review-workflow`, `GET /projects/:id/evidence-quality-audit`, `GET /projects/:id/evidence-source-review-workflow`, and `GET /projects/:id/evidence-custody-readiness` after project load, chat/meeting commands, Manager control actions, worker pulses, and Agent write receipts. Backend requests use `cache: 'no-store'` so proof checks read the current local backend rather than browser HTTP cache. Chat proof navigation now uses those backend transcript routes as authoritative for backend-online real projects: it waits for the project/channel transcript sync before focusing a proof id and suppresses both `recoverProofMessages` and cached browser-chat proof if the backend transcript is missing the proof, failing closed instead of entering proof focus. Local recovery remains only for offline, sample, imported legacy, or explicit development fallback projects. Manager Dashboard submission rows, Manager Flow Graph node-detail evidence rows, and Manager governance-panel chat proof exits now filter to backend transcript message ids; timeline/event/artifact/checksum proof ids stay on their own proof surfaces. The browser Harness verifies real submission proof exits and includes a negative backend transcript fixture requiring the `backend proof transcript required` state when a real submission proof is absent from the backend transcript. When Manager Ready Package is synced or `Sync Proof Models` is clicked, React now performs a full proof-model sync that also fetches standalone MVP, pilot, deployment, evidence archive/export, private-pilot lifecycle, production launch audit/gap/control/dossier/evidence-integrity, launch approval, operations, production control receipt, provider, persistence adapter, worker queue adapter, and security boundary read models, then prefers those standalone models over embedded package snapshots. The core C/A Ready Package panels now expose source tags for Product Team Delivery Trace, Product Team Operating Loop, Team Collaboration Diagnostics, Runtime Contracts, and Autonomous Cycle Consistency, so backend-backed data and backend-required placeholders are visually distinct in the Manager UI. The remaining replacement work is to continue clearing lower-priority browser-first mutation paths and separate demo/sample fixtures from real project flow.

Unified Event Ledger uses the same route-backed recovery rule. When `event-ledger/v1` is missing for a backend-online real project, React shows `event-ledger-backend-required` with `event-ledger-sync-timeline-events`, which calls `GET /projects/:id/timeline` and `GET /projects/:id/events` through `syncBackendTimelineAndEvents` instead of displaying browser snapshot event rows.

Backend sync evidence is scoped by project id. `projectHasBackendSyncEvidence` no longer treats any `lastProjectSyncAt` or `lastManagerDashboardSyncAt` timestamp as proof for the active project; it requires a backend catalog match, `lastProjectSyncProjectId`, or a current backend read model with the same `projectId`. `applyBackendProjectSnapshot` now records `lastProjectSyncProjectId` and stamps real backend snapshots as `backendSyncStatus: "online"` and `dataSource: "backend-backed"` unless the project is an explicit sample or development fallback, so cached project rows cannot lose backend ownership evidence between renders.

Reviewer composer writes are fail-closed as well. `runBackendSubmissionReview` may show `submission-review-pending` while the backend request is in flight, but a failed request replaces that draft receipt with `submission-review-failed`, keeps the Reviewer comments and requested changes in project-scoped UI state, and renders `Review write failed` instead of a backend receipt route. No local submission-review node, Flow Graph proof, transcript proof, or review receipt is created unless `POST /projects/:id/submissions/:submissionId/reviews` returns successfully.

A-side run buttons follow the same stale-proof boundary. Failed Agent Autonomous Action Queue runs clear `agentAutonomousActionRun`, render `Agent Action Failed`, and set `localProofCreated: false`; failed Collaboration Intent Queue runs clear `collaborationIntentRun`, render `Intent Run Failed`, and set `localProofCreated: false`; failed MVP readiness operator actions render `Action failed` with no local operator receipt. The UI must not leave the previous successful run receipt visible after a failed click on a backend-online real project.

Per-Agent Dashboard caches are scoped by `projectId + agentId`. The same persona can appear in multiple projects, so React must not reuse a previous project's `/projects/:id/agents/:agentId/dashboard` read model when the Manager switches context. Agent Focus workspace, review/revision defaults, and post-write Agent refreshes now resolve dashboards through that scoped key before displaying obligations, evidence, submissions, reviews, or autonomous control receipts.

Agent Workbench, Agent Message, and Reviewer composer drafts follow the same project-scoped UI-state boundary. Those drafts are still local form state, but they can become backend evidence searches, artifact submissions, generated drafts, Agent-to-Agent messages, or submission reviews. React therefore keys them by project plus Agent or submission before any backend write reads them, preventing one reusable persona's previous project draft or receipt from becoming another project's C/A proof.

Manager Change Intake and Leader Assignment composer drafts are also scoped by project. They are C-side command inputs that can write backend chat/change records, assignment inbox items, timeline logs, and event-ledger proof, so React resolves them through the active project key before `submitManagerChangeIntake` or `submitManagerLeaderAssignment` can read them.

Group Chat input, War Room meeting input, and the active chat channel follow the same boundary. React stores those UI states by active project before `submitChatInput` or `submitRoomInput` can write transcript, meeting, timeline, or event proof, preventing unsent chat/meeting text or a previous project's channel selection from leaking into another project.

Manager Flow Graph read models follow the same project boundary. The graph scene accepts `backendStation.managerFlowGraph` only when its `projectId` matches the active project, accepts Manager Ready Package embedded graph data only when the package `projectId` matches, and resolves selected-node Proof Map routes from its own active-project-scoped `backendReadinessProofMapForFlowGraph`. For backend-online real projects this prevents a stale graph or Proof Map from a previously viewed project from becoming the current project's visible proof surface.

Manager Ready Package aggregate data is also project-scoped before consumption. React resolves `backendManagerReadyPackage` through the active project's scoped Ready Package, then derives embedded Manager Dashboard, Readiness Proof Map, production controls, and Ready Package submodel fallbacks from that scoped package only. A stale `backendStation.managerReadyPackage` for another project is treated as absent for the active project.

Standalone backendStation read models follow the same rule before Manager Dashboard or Ready Package panels consume them. React now filters project-level station models through the active `projectId` before using Product Team Operating Loop, Team Collaboration Diagnostics, Runtime Contracts, Autonomous Cycle Consistency, Runtime Autonomy Status, Manager command/governance queues, Agent State Summary, Continuous Work Loop, Assignment Timeline Matrix, Change Flow, or Sync Protocol Audit. For backend-managed real projects, a project-scoped station read model without a `projectId` is suppressed instead of treated as current proof; no-project-id compatibility data remains offline/demo fallback only.

Latest backendStation operation receipts follow a stricter boundary because they drive visible proof and follow-up actions. Product Team Mission, Manager Command Center, Manager Action output, Scenario Walkthrough, Collaboration Intent, Autonomous Run Control, Agent Autonomous Action, MVP readiness, managed cutover, and Timeline action receipts must carry the active `projectId` before React displays them or uses an Autopilot session id for a follow-up tick/pause. Unscoped receipts are not treated as real-project evidence.

Group Chat channel creation is now backend-backed for real projects. `POST /projects/:id/transcripts` creates a generic transcript channel through `transcript-channel-created/v1`, publishes a system message into that channel, stores `transcriptChannelReceipts` on the project, writes `transcript-channel-created` timeline and event-ledger proof, and returns deferred transcript/timeline/event/Flow/Proof refresh routes when `includeReadModels: false` is used. `GET /projects/:id/transcripts` includes backend-created channel metadata, Manager Flow Graph renders those transcript channels as collaboration proof nodes, and Readiness Proof Map exposes `transcriptChannelRoutes` / `transcriptChannelSummary` with receipt, chat, timeline, event, and channel route proof. React's group-chat `+` control calls this route for backend projects, consumes the returned `transcriptChannelRoute` through the lightweight receipt refresher, renders the route in Manager Proof Map, and only uses local channel creation for offline/demo/explicit fallback projects. Proof Map transcript channel route cards fail closed with source badges and `Sync Proof Map` actions when the summary exists but the route array is absent.

Agent-to-Agent messages now have the same proof-map boundary. `POST /projects/:id/agents/:agentId/message` still writes the Agent-authored chat message, target inbox/obligation state, sender worklog, an `agent-message` timeline log, and an `agent-to-agent-message` event-ledger entry; `GET /projects/:id/readiness-proof-map` now projects those messages as `agentMessageRoutes` / `agentMessageSummary`. Each route carries the transcript path, message proof id, timeline log ids, event ids, sender/target Agent ids, receipt/inbox/worklog state, and `readyForAgentMessageDelivery`, while Manager Proof Map renders direct chat and timeline/event proof exits. Proof Map Agent-to-Agent message route cards fail closed with source badges and `Sync Proof Map` actions when `agentMessageSummary` exists but `agentMessageRoutes` is absent from an otherwise loaded backend Proof Map. This keeps A-side peer collaboration inspectable from the same B-side evidence map as submissions, reviews, autonomous actions, and transcript channels.

Marketplace Agent contracts use that same proof-map boundary. `POST /projects/:id/agents/contract` writes `agent-contract/v1`, roster, Agent worklog, timeline, and event proof, while `GET /projects/:id/readiness-proof-map` projects `agentContractRoutes` / `agentContractSummary`. Proof Map marketplace Agent contract route cards fail closed with source badges, `Sync Proof Map`, Agent Dashboard, and timeline proof actions when the summary exists but the route array is absent.

Chat proof focus, Timeline proof focus, and selected Timeline detail are also project-scoped. Selection/focus state does not write backend facts, but `POST /projects/:id/timeline/actions` lets the Manager record a note, acknowledgement, completion mark, or edit note against a timeline node as `timeline-action-receipt/v1`, with a persisted timeline log and event-ledger row. Completion/edit actions are audit annotations and do not mutate original timeline evidence. React stores focus/draft state by active project to avoid carrying one project's evidence focus or action draft into another project's monitor view.

Manager Flow Graph has a dedicated negative browser fixture for that rule. `ui:manager-backend` creates a real backend project, intercepts `GET /projects/:id/manager-flow-graph` with an empty backend read model, clicks `Sync Graph`, and requires `manager-flow-backend-required`, `manager-flow-backend-required-sync`, `backend model missing`, explanatory suppression copy, and zero `manager-flow-node-*` fallback nodes before returning to the normal positive flow. The missing-model overlay uses the same `syncBackendManagerFlowGraph` path as the header control, so the Manager can retry the authoritative backend route without accepting frontend fallback nodes.

The generic product-team acceptance chain follows the same Ready Package source boundary. Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Evidence Quality Audit, Evidence Source Review Workflow, and Evidence Custody Readiness expose source tags in the Manager UI. For backend-online real projects, absent standalone or embedded read models render backend-required missing models; Brainstorm Layer summary-shaped fallback is limited to offline/demo mode.

Acceptance-chain missing models expose the same in-panel `Sync Proof Models` action used by the top-level backend toolbar. The button calls `syncBackendReadyPackageSubmodels({ includeLaunchControls: true })` and keeps local/demo fallback disabled for backend-online real projects.

Group Chat Transcript Index follows the same boundary. For backend-online real projects, it exposes `group-chat-transcript-source`, renders `backend-transcript-index-required` if the transcript read model is absent, and clears browser-local recovered message/proof counts until `GET /projects/:id/transcripts` succeeds. Offline, sample, imported legacy, and explicit development fallback projects still render local recovery as a visible `frontend-fallback` or `sample-fixture` source. The index also renders `group-chat-collaboration-proof-rows`, derived from the current project's backend Manager Dashboard evidence searches, evidence source reviews, Agent submissions, submission reviews, linked revision submissions, and final deliverables. React first verifies the Manager Dashboard `projectId` and requires the collaboration submodels `evidenceSearches.rows`, `submissions.rows`, `submissionReviews.rows`, and `evidenceSourceReviews.rows`; otherwise a real backend project stays in `backend-required` instead of reading browser-local project arrays. Each row keeps chat proof, timeline proof, event ids, and the relevant backend route visible so the Manager can inspect collaboration nodes from the group-chat surface without trusting a summary-only card. Transcript index rows and live work-node cards prefer backend-provided resource fields (`evidenceSearchRoute`, `evidenceSourceReviewRoute`, `submissionRoute`, `submissionReviewRoute`, and `resourceRoute`) before any fallback route construction. The live Group Chat renderer uses the same backend message types (`evidence-search`, `submission`, `submission-review`, and `evidence-source-review`) to render `chat-collaboration-node-*` cards with route text, proof counts, receipt coverage, timeline proof actions, and Flow Graph node actions; ordinary text bubbles remain for non-work-node conversation. The Flow Graph action syncs the backend graph when needed, selects the matching backend node id, and enters Manager Flow Graph with the same proof focus. Selected Flow Graph node details match the node's route/proof ids against Readiness Proof Map route collections, render the resulting Proof Map/API route, and provide a return action to the Manager Proof Map; if no backend route matches, the detail says so instead of synthesizing a fake route.

The transcript index recovery counter is also backend-scoped. Once backend transcript evidence is required or already present, React does not rebuild archived proof ids from `activeProject.logs`; it uses backend channel proof ids and backend recoverable-proof counts. Local kickoff/log recovery is limited to offline/demo/legacy fallback.

Project Dashboard Active Channels uses that same transcript boundary. Backend-online real projects read the current project's backend transcript index `channelCount` or `channels`; if the transcript index is absent, the stat shows `backend required` instead of counting browser `chatChannels.length`. Offline/demo paths can still use local channel counts as visible fallback.

`npm run agents:transcript-contracts` is the low-write aggregate contract for backend Group Chat controls. It runs transcript channel creation, search, channel pin, message pin, reply, mention, attachment, and member-presence gates in one command while keeping the individual scripts available for focused debugging.

`GET /projects/:id/transcripts/search?query=...&channelId=...` exposes `transcript-search/v1`, a read-only backend search model over persisted channel transcript messages plus archived proof messages recovered from project logs. React Group Chat uses this route for the header search control, renders result rows with transcript proof anchors, and focuses returned proof message ids instead of relying on browser-local chat history. `npm run agents:transcript-search` validates the route through the in-memory backend API without launching a browser/server or writing workspace artifact files. This is local MVP transcript inspection, not production transcript retention/search; managed retention, tenant policy, and audit controls remain production blockers.

`POST /projects/:id/transcripts/:channelId/pins` records a `transcript-pin/v1` receipt for a real backend transcript message. The write path stores one active pin per channel/message, appends a system message to the channel, writes a `transcript-message-pinned` timeline log and event-ledger row, exposes `pinnedMessages` through `GET /projects/:id/transcripts/:channelId`, and projects `transcriptPinRoutes` / `transcriptPinSummary` into Readiness Proof Map plus `transcript-pin` nodes into Manager Flow Graph. React Group Chat uses the route for the per-message pin button and renders pinned state from the backend transcript read model. `npm run agents:transcript-pin` validates the contract with the in-memory API without browser/server startup or workspace artifact writes.

`POST /projects/:id/transcripts/:channelId/replies` records a `transcript-reply/v1` receipt and appends a real `transcript-reply-message/v1` message to the backend transcript. The reply targets the parent message author when the author maps to an Agent, writes `transcript-message-replied` timeline and event-ledger proof, exposes channel `replies`, projects `transcriptReplyRoutes` / `transcriptReplySummary` into Readiness Proof Map, and adds `transcript-reply` nodes to Manager Flow Graph. React Group Chat uses the current input text as the reply body; an empty input only selects the reply target and does not create backend proof. `npm run agents:transcript-reply` validates the contract with the in-memory API without browser/server startup or workspace artifact writes.

`POST /projects/:id/transcripts/:channelId/mentions` records a `transcript-mention/v1` receipt and appends a real `transcript-mention-message/v1` message to the backend transcript. The mention targets the selected Agent, the source message author when possible, parsed `@Agent` text, or `@all` as a fallback; it enters the target Agent inbox through the existing message receipt path, writes `transcript-message-mentioned` timeline and event-ledger proof, exposes channel `mentions`, projects `transcriptMentionRoutes` / `transcriptMentionSummary` into Readiness Proof Map, and adds `transcript-mention` nodes to Manager Flow Graph. React Group Chat uses the current input text as the mention body; an empty input only selects/prefills the mention target and does not create backend proof. `npm run agents:transcript-mention` validates the contract with the in-memory API without browser/server startup or workspace artifact writes.

`POST /projects/:id/transcripts/:channelId/attachments` records a `transcript-attachment/v1` receipt and appends a real `transcript-attachment-message/v1` file message to the backend transcript. The local MVP contract stores bounded metadata/text preview plus content checksum proof, writes `transcript-file-attached` timeline and event-ledger proof, exposes channel `attachments`, projects `transcriptAttachmentRoutes` / `transcriptAttachmentSummary` into Readiness Proof Map, and adds `transcript-attachment` nodes to Manager Flow Graph. React Group Chat opens a file picker and sends filename/type/size plus at most a bounded text preview; production object storage, malware scanning, retention policy, and tenant audit remain blocked. `npm run agents:transcript-attachment` validates the contract with the in-memory API without browser/server startup or workspace artifact writes.

`GET /projects/:id/transcripts/:channelId/members` returns `transcript-member-presence/v1`, a backend read model derived from transcript receipts, direct targets, Agent inbox/obligations, and authored messages. It exposes per-Agent sent/seen/direct/todo counts, proof ids, timeline/event ids where available, `transcriptMemberPresenceRoutes` / `transcriptMemberPresenceSummary` in Readiness Proof Map, and `transcript-member-presence` nodes in Manager Flow Graph. React Group Chat uses this route for the header members button and renders the backend presence panel. `npm run agents:transcript-member-presence` validates the contract without browser/server startup or workspace artifact writes.

Proof Map transcript action route cards fail closed when the backend summary exists but the route array is absent. Channel-pin, per-message pin/reply/mention, attachment, and member-presence cards render backend-required source badges plus in-panel `Sync Proof Map` actions instead of treating summary-only data as traceable route proof.

The Manager Demo is explicitly marked as a `sample-fixture`: the dashboard entry reads `Load Sample Fixture`, the generated project carries `dataSource: 'sample-fixture'`, and project list/detail surfaces show sample-only labels. This keeps sample validation useful without letting it masquerade as the real C-side project path or A-side autonomous backend capability.

Manager-facing command availability is intentionally not the same as the current station status label. For an active project with a configured backend command target, Manager Live Command Center, Scenario Walkthrough, Action Playbook, Use Case Audit, Manager Flow Graph Sync/Confirm/Supersede controls, Agent Focus Workbench, Reviewer composer, Agent-to-Agent message, per-Agent Pulse, Autonomous Run Control, bounded Autopilot session controls, and Agent Autonomous Queue run buttons remain visible and attempt the backend route even if the last status read marked the station offline. A real route failure still fails closed for backend-synced projects; the UI records the backend error rather than creating browser-local proof. `npm run agents:agent-message-contract` is the low-write gate for the Agent-to-Agent path: it validates targeted inbox delivery, sender worklog proof, transcript, timeline, event ledger, Manager Dashboard delivery rows, Readiness Proof Map routes, Flow Graph node coverage, and target Agent Dashboard readback. `npm run agents:agent-contract` pins the marketplace Agent contract path: it validates `agent-contract/v1`, roster mutation, Agent Dashboard worklog, timeline/event proof, Flow Graph node coverage, and Proof Map route visibility.

Manager group-chat commands use the same backend boundary. `POST /projects/:id/chat` can route a Director/Manager message into ordinary group chat, Leader assignment, peer handoff, or feature/change intake, and `includeReadModels: false` returns refresh routes instead of requiring the UI to synthesize rows. `npm run agents:manager-chat-command-contract` validates the two customer-critical command paths without browser/build cost: a Leader `@assign` becomes an owned task, direct assignment message, acknowledgement, assignee Agent Dashboard proof, Assignment Timeline Matrix row, timeline logs, and event proof; a Google Chat change request becomes a change-ledger row, owner confirmation, team sync, owner work pulse, Change Flow row, transcript proof, timeline proof, event proof, Flow Graph proof, and Proof Map proof.

React Group Chat now applies the same fail-closed rule before a send is submitted. If a backend-backed real project cannot use a backend command route because the Backend URL is not configured and local runtime fallback is not allowed, `submitChatInput` returns before clearing the draft or creating browser-local chat proof. The composer keeps the text field editable, disables `project-chat-send`, shows `backend-chat-send-required`, and links `backend-chat-send-open-deployment` to Settings Deployment.

Group Chat collaboration proof rows are Manager Dashboard read-model consumers. For backend-online real projects, the dashboard requires `evidenceSearches.rows`, `submissions.rows`, `submissionReviews.rows`, and `evidenceSourceReviews.rows` before it can render transcript proof rows or enrich work-node chat cards from submission/evidence metadata. If those rows are missing, React shows `group-chat-collaboration-proof-backend-required` with `group-chat-collaboration-proof-sync-manager-dashboard`, which calls the existing Manager Dashboard sync instead of falling back to browser-local evidence, submission, or review arrays.

The live Group Chat view owns the same current-project Manager Dashboard collaboration resolver for work-node cards. It scopes `backendStation.managerDashboard` to the active project before rendering backend evidence-search, submission, submission-review, or evidence-source-review chat cards, so the room can render independently of Dashboard-local variables. These cards use explicit backend node and route fields from transcript messages plus Manager Dashboard rows instead of inferring resource identity from display text.

Autopilot scheduler continuation requires a durable backend session. Before React calls `/workers/autonomous/tick` from `Scheduler Tick`, it reads `GET /projects/:id/autonomous-run-control/sessions`; if no running or waiting session exists, it creates one through `POST /projects/:id/autonomous-run-control/sessions/start` and then lets the scheduler-owned due-worker produce the tick receipt. This keeps the 24/7 control loop tied to backend session/tick ledgers instead of a frontend-only session receipt.

Browser snapshot project writes are restricted to sample fixtures and explicit development fallback. `ensureBackendProjectSeed` still reads `GET /projects/:id` before commands, but if that read returns missing for a real project, React fails closed and records `Backend project missing; local seed suppressed` rather than PUT-ing the active browser snapshot. The visible `Seed Sample/Dev` action is disabled for real backend projects, records `Browser snapshot seeding is disabled for real backend projects.` if called directly, and is asserted disabled in `npm run ui:manager-backend` after real initiation approval. This prevents stale C-side state from overwriting backend Agent submissions, Autopilot sessions, Flow Graph confirmations, and ledger receipts.

Backend kickoff approval now hydrates the C-side project from `GET /projects/:id` and syncs `/manager-dashboard` before the real project enters the dashboard. Legacy seed ids such as `p_roundtable_001` are treated as sample/demo only when their source is not backend kickoff. This keeps kickoff generation provenance visible in the first dashboard screen and keeps Agent Focus Workspace available for backend-online real projects instead of relying on the old demo-id exception.

Agent work writes now have a frontend consumption path, not just a backend contract. The Agent Focus Workspace renders an Agent Workbench that calls `POST /projects/:id/agents/:agentId/evidence-searches`, `POST /projects/:id/agents/:agentId/submissions`, and `POST /projects/:id/agents/:agentId/artifact-drafts` with `includeReadModels: false`. After the lightweight receipt returns, React refreshes Manager Dashboard, Manager Ready Package, Manager Flow Graph, and the submitting Agent Dashboard through standalone backend GET routes. The refresh path intentionally avoids writing the stale browser project snapshot back to the backend, so newly created submission/evidence/draft nodes remain authoritative. If an evidence, submission, or draft write fails, React replaces the previous Workbench receipt with a failed non-proof receipt, sets `localProofCreated: false`, and renders `Agent Workbench Write Failed`; the UI must not leave the last successful Workbench receipt visible after a failed click. The Workbench typed submission control exposes the required generic artifact set (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`) so a product-team run can submit discovery, brainstorm, evidence, decision, risk, plan, revision, and final-delivery nodes without research-specific logic. The Workbench also exposes review/revision linkage controls: revision-note and final-deliverable submissions can carry `respondsToReviewId`, `revisesSubmissionId`, and final `supersedesSubmissionIds` through the same backend route. `npm run agents:agent-workbench-contract` is the low-write API gate for this path; it validates provider evidence, every required generic artifact submission, generated product brief, review, revision, final delivery, and proof-surface readback without browser or workspace artifact writes.

Agent Workbench, Reviewer composer, Agent pulse/message, and Flow Graph confirmation failures preserve backend-required station status for backend-managed real projects. Failed writes still replace stale receipts with non-proof failure state or visible backend errors, but they do not downgrade the station to offline/local fallback; backend read models remain authoritative until a backend receipt or refresh succeeds. Manager Command Center, Manager Action Queue, Autonomous Run Control, Autopilot session controls, Agent Autonomous Queue, Collaboration Intent Queue, MVP readiness receipts, private-pilot receipts, managed cutover attestations, and Scenario Walkthrough steps use the same failure-status boundary. Backend health checks and pre-project kickoff failures can still report `offline`; already backend-managed project commands cannot use that status change as a path into browser-local proof.

For backend-online real projects, Agent Focus treats `GET /projects/:id/agents/:agentId/dashboard` as the required personal Agent read model. The UI now exposes `agent-focus-dashboard-source-*`; if the route is missing it renders `agent-focus-backend-dashboard-required-*` plus a `Sync Agent Dashboard` action. The collapsed Team row follows the same rule: local `agentStates`, local `agentWorkerLedger` priority, local next-run, local management relation chips, peer-management counts, owned-task fallback rows, and the status dot are suppressed or downgraded behind `agent-team-dashboard-required-*` until the standalone route returns. The opened Agent Focus status chip also shows a red `backend required` state while that route is missing instead of defaulting to `monitoring`.

Agent Focus Workspace summary metrics use that same required read model. On backend-online real projects, Current Plan, Open Inbox, Obligations, Owned Tasks, status chips, and backend routine labels are read from `/agents/:agentId/dashboard`; if that route is missing they show `backend required` or the backend-required banner instead of deriving values from `activeProject.agentStates`, `activeProject.tasks`, or previous local worker ledgers. Latest inbox, obligation, and worklog cues follow the same boundary: without the backend Agent Dashboard they clear to null instead of reading browser-local Agent state.

Peer Management Matrix uses the backend Manager Dashboard boundary. For backend-online real projects, React reads `managerDashboard.peerManagementMatrix` and its timeline proof ids; if the backend matrix is absent, the Agent Management Mesh source remains backend-required and React does not count local `activeProject.logs` peer-management check-ins as proof. Local log-derived peer proof remains offline/demo fallback only.

Scheduler start, scheduler tick, and Server/Hour/Day pulse failures preserve the previous backend station status for backend-managed real projects. A failed 24/7 command records the backend error but does not downgrade a real backend project into an offline/local-fallback state; backend-required proof surfaces remain authoritative until a backend receipt or read model refresh succeeds.

Reviewer work now has the same C-side consumption path. Manager Dashboard submission rows render a review composer that posts `includeReadModels: false` to `POST /projects/:id/submissions/:submissionId/reviews`, then refreshes Manager Dashboard, Manager Ready Package, Manager Flow Graph, Timeline/Event, and the relevant Reviewer/submitter Agent dashboards from backend routes. This lets a real project close the review -> requested changes -> revision -> final acceptance loop without inventing frontend review rows. `npm run ui:manager-backend` now verifies this on a real initiated backend project: Turing submits a brainstorm-board, runs evidence search, generates a product-brief draft from that context, Curie requests changes from Manager Dashboard, Turing submits a linked revision-note and final-deliverable, Curie accepts the final deliverable, and Manager/Flow/Proof/Agent read models show the closed review workflow. The product-team acceptance Harness also pins those browser-chain markers, so backend acceptance fails if the real browser path drops brainstorm, evidence, generated draft, review, revision, final delivery, or accepted closure coverage.

Manager and worker controls follow the same receipt-first direction. Scheduler start, Server Pulse, Agent pulse/message, Manager Command Center, Scenario Walkthrough, Action Queue, and Manager Flow Graph confirmation now avoid pre-writing the browser project snapshot; when they need a backend write response they send `includeReadModels: false` and refresh authoritative read models afterward. The browser-local one-minute autonomous loop is suppressed for backend-online real projects, so server-backed 24/7 worker routes own real autonomous progress while offline, demo/sample, and explicit development fallback projects can still use local cycles. Per-Agent worker cycles can now be explicitly asked to submit completed autonomous work through the standard Agent submission contract: `submitWorkArtifact: true` on `POST /projects/:id/agents/:agentId/work-cycle`, or `submitAgentWorkArtifacts: true` on an HTTP scheduler tick, turns a completed owned task into a submission with group-chat, timeline, event-ledger, task-evidence, Agent Dashboard, Manager Dashboard, and Manager Flow Graph proof. When the caller sets `workArtifactType: "auto"` or the scheduler submits without an explicit type, the backend infers a generic product-team artifact type from the task intent, including `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `implementation-plan`, and `final-deliverable`, while preserving `progress-brief` as the fallback. Evidence-oriented autonomous tasks also record a standard `evidence-search` row through `recordAgentEvidenceSearch`, using worker task, group-chat, timeline, and event proof as local sources, then link that evidence-search route into the submitted `evidence-packet`. Reviewer worker processing is also explicit: `reviewPendingSubmission: true` on a work-cycle, or `reviewPendingSubmissions: true` on scheduler ticks, makes the Reviewer Agent pick a pending submission assigned to it and call the standard submission-review contract, returning a review receipt plus chat/timeline/event proof. Submitter response processing is explicit as well: `respondToReviewObligation: true` on a work-cycle, or `respondToReviewObligations: true` on scheduler ticks, makes the submitting Agent find its open `changes-requested` review obligation and submit a linked `revision-note` or `final-deliverable` through the same submission contract. The C-side Agent Pulse now sends these work/review/revision-response controls by default, deriving the Reviewer from the team roster and using first-pass `changes-requested` versus revision/final `accepted` verdicts so a manager can drive the full loop through existing Agent controls. Server Pulse, Hour Pulse, and Day Report now call `/workers/autonomous/tick` with current-project force filters, `forceAgentRun: true`, `submitAgentWorkArtifacts`, `reviewPendingSubmissions`, `agentReviewVerdict: "auto"`, and `respondToReviewObligations`, so one manager pulse advances both project-level work and Agent worker output through the scheduler contract. Hour/Day pass a `projectCadence` override so the project worker keeps the requested cadence semantics while sharing the same Agent worker path. Scenario Control Center's 24/7 pulse proof display also reads backend Manager Dashboard / Operations Board cycle and next-run evidence for backend-online real projects; local `activeProject.autonomousLedger` and local schedule text remain offline/demo/development fallback only. The 24/7 Operations Board and Continuous Work Loop summary tiles use that same backend-required boundary: if `agent-state-summary/v1` or `continuous-work-loop/v1` is missing, project next/last pulse and cadence labels show `backend required` instead of using `activeProject.nextAutonomousRunAt`, `activeProject.lastAutonomousRunAt`, or a frontend schedule estimate. HTTP scheduler start now forwards the complete request body into the immediate Agent sweep, including `useAgentAutonomousStrategy`, `submitAgentWorkArtifacts`, artifact type/reviewer controls, and revision-response controls; the React Start button no longer fires a parallel browser autonomous-cycle after starting the scheduler, and instead waits for backend-owned tick proof before syncing project/Manager/queue state back into the UI. Server autostart preserves those startup controls for later interval ticks and `npm run agents:server` exposes them through explicit `AGENT_AUTONOMOUS_AGENT_*` environment switches. Scheduler status also exposes `scheduler-agent-controls/v1` summaries for startup controls, preserved scheduled controls, and the latest tick controls, while Deployment Preflight exposes the configured Agent autonomy env switches and the React Backend Worker Station renders strategy/submission/revision-response state directly. The manager scenario Harness now verifies this scheduler contract through an isolated generic product-team chain: evidence-oriented work becomes an `evidence-packet` with `worker-local-evidence-search`, the Reviewer requests changes, the submitter posts a linked `revision-note`, the Reviewer accepts it, a later due task becomes an accepted `final-deliverable`, the scheduler-start path creates a startup `brainstorm-board` submission from forwarded strategy controls, and the autostart path does the same for an unattended startup server; Manager Flow Graph plus Readiness Proof Map expose the final proof routes. `npm run ui:manager-backend` also verifies the browser Start path writes both the `manager-ui-scheduler-start-pulse` project proof and `http-autonomous-scheduler-startup-agents` Agent ledger proof without a stale C-side snapshot overwrite. When scheduler calls include `forceProjectIds` or `forceAgentProjectIds`, non-target projects are filtered before max-project limits are applied, so a manager-directed project pulse cannot be crowded out by unrelated due work. These switches are intentionally opt-in while production cost, review policy, artifact-volume controls, and customer acceptance rules are still being hardened. Production operations, deployment, security, and provider control receipt POST routes also honor `includeReadModels: false`: the response keeps the auditable receipt/log and returns refresh routes for Manager Dashboard, Manager Ready Package, Manager Flow Graph, production launch audit/gap/control/dossier, production evidence integrity, the specific receipt workflow, and the upstream readiness/preflight/provider/security model instead of embedding large Manager read models in the write response. A small compatibility seed remains for local-only projects: it first tries `GET /projects/:id`, then uses `PUT /projects/:id` only if the backend does not know the project yet and the project has not already been seen through a successful backend project/read-model sync. React tracks `lastProjectSyncProjectId` plus backend read-model project ids to suppress repeat seeding after backend proof exists; `ui:manager-backend:core` allows at most one Manager Demo seed and asserts later Manager Action, Run Control, Agent Queue, and Autopilot scheduler controls do not issue another browser snapshot `PUT`. Backend-online real projects remain fail-closed and must not be silently overwritten from browser state.

Settings provider/runtime caches also follow active-project scope. React records the project id used for project-scoped Settings syncs, clears stale Provider/Runtime readiness when a new project sync starts, and renders Settings Provider Readiness, Settings Runtime Readiness, Settings Integration Readiness, and Provider Vault Binding proof only when the cached model belongs to the active project or is a global route. This prevents another project's API entry, Seal availability, provider-vault proof, or first-run status from appearing in the current customer's Settings panel.

The Autonomous Work Loop title is part of the same backend-required scheduler boundary. For backend-online real projects, it shows `backend required` until the Manager Dashboard operations board or continuous-work read model returns, rather than reading `activeProject.autonomy` as proof that cadence is active or paused.

Settings write receipts use the same stale-proof discipline. A new Seal attempt clears the previous Secret Vault receipt before the request runs, every provider key/endpoint edit clears the old receipt, and Seal failures leave `lastReceipt` empty while showing the backend error. Settings Integration sync also clears the previous readiness snapshot before the route is fetched and after failures. This keeps API key entry, endpoint entry, and integration status from displaying a previous successful proof after the current action failed or changed input. Provider secret fields remain locked until a backend URL is saved and the backend Vault is ready; both input and Seal persistence are gated by backend readiness.

Settings runtime status also fails closed across backend targets. React normalizes the target backend URL, clears model/search provider status, Secret Vault status/records, provider-vault bindings, startup readiness, and provider test receipts before a Provider sync or Settings Health Quick Check reads the current backend, preserves Integration readiness only when the backend/project scope still matches, and clears those runtime models again on failure. Saving a different backend API target through Settings Deployment uses the same stale-status reset before checking the new server, and Secret Vault Seal success refreshes provider readiness against the same backend URL used for the seal request instead of a later default or stale target. This prevents an old local backend or previous project from making Settings look configured for the current user target.

The Settings footer `Test Connection` is a visible backend health entry point. Clicking it switches to the Health tab and runs the Quick Check against `settings-health-readiness/v1`; the heavier Workflow Smoke remains a separate Health-tab action that can create a probe project and Agent pulse.

Settings project-settings writes also apply backend returned policy readbacks directly to React project state. Privacy, provider budget, workspace policy, and Agent tool-grant saves merge the returned `projectSettings` policy into the visible project before any later full project refresh, so saved policy counts and values cannot remain stale after a successful backend receipt.

Project entry commands now follow that same boundary. `POST /projects/initiate` and `POST /kickoff-meetings/:id/approve` honor `includeReadModels: false`, returning the created project, kickoff meeting/charter/assignment proof, first-pulse messages, and explicit refresh routes for project snapshot, transcripts, timeline/events, tasks, Manager Dashboard, Ready Package, Flow Graph, and Readiness Proof Map instead of embedding large Manager read models in the write response. React initiation approval now sends that flag, applies the project receipt, and refreshes those backend read models from the returned routes before entering the project dashboard.

Manager Action Playbook's `fixed-continuous-routines` row now uses that same scheduler boundary instead of a project-only autonomous cycle. Its run request targets `/workers/autonomous/tick`, forces the current project and Agent project, enables autonomous strategy/submission/review/revision-response controls, and records a `manager-action-scheduler-tick/v1` receipt with project and Agent processed counts plus Agent worker timeline/event proof. Manager Action Queue and Scenario Walkthrough write responses also return delegated scheduler/Agent output objects when present, and React renders `Manager Action Output Nodes` for transcript result messages, task nodes, scheduler ticks, submissions, evidence searches, reviews, and review responses with chat/timeline proof exits. This makes the C-side "start continuous work" or "run this scenario step" action a visible collaboration result, not a mock Action Queue success.

Readiness also distinguishes durable governance evidence from consumable Agent queues. `team-received-change-sync` no longer requires the change-sync item to remain in every Agent inbox after workers run; it accepts change ledger, timeline log, and event-ledger receipts for the sync, while still honoring live inbox/worklog evidence. That allows A-side Agents to freely consume their work queue without making the C-side manager view lose already-audited change ownership.

Manager governance panels now expose their read-model source in the UI. Live Command Center, Scenario Walkthrough, main Dashboard Scenario Trail, main Dashboard Requirement Matrix, Action Playbook, Use Case Audit, and Sync Protocol Audit show `backend-backed`, `demo data`, or `backend model missing` based on the same `backendOrAllowedFallback` gate. This is a C-side safety contract: real backend-online projects must not present frontend fallback rows as backend Agent/Manager capability. The Command Center missing-model stats also stay pure backend-required placeholders: if `manager-command-center/v1` is absent, `openTasks` and `changeRequests` render `backend required` rather than local browser counts. Governance & Speech Protocol reads `GET /projects/:id/governance-protocol` for Kickoff Charter Leader/Reviewer proof, evidence ids, timeline ids, event ids, Flow Graph / Proof Map links, and production blockers; real backend projects do not use project-snapshot Leader/Reviewer inference as proof. The Use Case Audit, Requirement Matrix, and Sync Protocol Audit missing-model blockers render in their own panels, so operators see whether `manager-use-case-audit/v1`, `manager-requirement-matrix/v1`, or `sync-protocol-audit/v1` is the route still missing. Control-panel missing states expose in-panel sync actions: Command Center uses `/manager-command-center`, Scenario Walkthrough uses `/manager-scenario-walkthrough`, Action Playbook uses `/manager-action-queue`, Action Run Ledger refreshes Manager Dashboard / Ready Package `manager-action-runs/v1`, Use Case Audit uses `/manager-use-case-audit`, Sync Protocol Audit uses `/sync-protocol-audit`, and Requirement Matrix uses `/manager-requirement-matrix`. Cockpit missing states expose the same direct recovery path: 24/7 Operations Board, Assignment Timeline Matrix, Change Flow, Continuous Work Loop, and Fixed Work Routines call `Sync Cockpit`; Governance & Speech Protocol calls `Sync Governance`; Recent Commit Line calls `Sync Timeline`; Collaboration Health calls `Sync Proof Models` for Team Collaboration Diagnostics. The local release checklist includes a static guard so backend-required/missing-backend Manager panels cannot regress into non-actionable dead ends.

Manager Dashboard route rows follow the same missing-model boundary. The command-center route row shows `backend required` until `manager-command-center/v1` exists; the generic `monitoring` label is reserved for a returned backend Command Center model with no runnable next action. Scenario Trail, Scenario Walkthrough, Requirement Matrix, Manager Action Queue, Agent Autonomous Queue, and Autonomous Run Control route rows also show `backend required` until their matching backend models exist, rather than rendering `0-0 ready`, `0-0 complete`, `0 ready`, or `0 runnable actions` as if an empty backend model had returned.

Backend Manager Snapshot summary cards use the same rule for standalone and embedded C/A models. Scenario Trail, Walkthrough, Standalone Trail, Action Queue, Agent Queue, and Run Control show `backend required` until the relevant backend read model exists, rather than summarizing missing read models as `0`, `0/0`, or `0 runnable`.

Manager Ready Package delivery summary cards also distinguish missing backend fields from valid zero counts. Trail Ready, Walkthrough, Requirements, Kickoff Board, Work Loop Board, Collaboration Board, Change Protocol, Change Owners, Use Cases, and Action Queue use summary field-presence checks; if the backend omitted those counts, the UI shows `backend required` instead of `0/0`.

The same field-presence rule applies to Ready Package single-count C/A summaries. Unresolved Routes, Transcript Channels, Ops Agents, Assignments, and Changes show `backend required` when the backend omits those summary fields, rather than treating missing evidence as a zero count.

Ready Package boolean status summaries use the same distinction. Queue Parity, Worker Recovery, and Incident Drill show `backend required` when the backend omits the boolean field; `blocked` means the backend returned `false`, not merely that the field was absent.

Provider controlled-run and provider eval summaries follow the same contract. Run Ready, Run Ops, Run Cost, Eval Ready, Eval Runs, and Eval Critical read the standalone provider models or embedded summary fields; if neither exists, the UI shows `backend required` instead of `0c`, `0/0`, `blocked`, or `record`.

Evidence/source-review/custody Ready Package summaries also treat `backend-model-missing` placeholders as missing evidence. Source Queue, Source Decisions, Source Pending, Custody Ready, Custody Records, and Custody Storage show `backend required` unless a real standalone model or embedded summary field exists, so placeholder summary zeros from missing-model wrappers do not appear as proof.

Product-team loop, collaboration, runtime-contract, autonomy, evidence-quality, and evidence-index Ready Package summaries follow the same real-model gate. Delivery Trace, Operating Loop, Collab Diagnostics, Intent Queue, Runtime Contracts, Cycle Consistency, Runtime Autonomy, Evidence Audit, Evidence Quality, Evidence Index, Source Review, and Evidence Custody require either a real standalone read model or a matching embedded Ready Package summary field; otherwise React renders `backend required` instead of fallback labels such as `unknown`, `0`, `0/0`, `blocked`, `review`, or `*-missing`.

Launch, private-pilot handoff, artifact/review, provider/security, and operations summaries also use that gate. Pilot Launch, Preflight, Gateway, Infrastructure Rehearsal, Launch Approval/Audit, Evidence Archive/Export, Go-Live, Release Candidate, Launch Run, Health, Acceptance, Production Ops, Artifact Audit, Review Workflow, Security, Provider Readiness, Controlled Run, Provider Eval, persistence adapter, and queue adapter cards require a real standalone read model or an explicit Ready Package summary field; otherwise React renders `backend required` instead of fallback labels such as `unknown`, `0`, `0/0`, `blocked`, `record`, `pending`, or `*-needed`. The detailed panels for Launch Audit, Launch Approval, Pilot Launch Readiness, Deployment Preflight, Operations Readiness, Provider Readiness, Provider Controlled Run, Provider Eval, Evidence Custody, and Security Boundary also require `readyPackageModelAvailable(...)`, so `backend-model-missing` wrappers cannot render as detailed operational results.

The Manager Scenario Walkthrough and Manager Action Playbook panels use that boundary in their own summary headers and top stat cards. When `manager-scenario-walkthrough/v1` or `manager-action-queue/v1` is suppressed for a backend-online real project, the panel summary shows `backend required` instead of zero-count completion, "All covered", "None", or "All complete" labels.

Settings Health Workflow Smoke is a stronger first-run backend probe than the quick readiness check. `POST /settings/workflow-smoke` creates a generic product-team probe project, sends a Manager group-chat assignment, runs the assigned Agent's backend `work-cycle` with `submitWorkArtifact=true`, `workArtifactType=product-brief`, `submitWorkArtifactOn=always`, and `includeReadModels=false`, then reads `/transcripts/:channelId`, `/timeline`, `/events`, `/manager-flow-graph`, and `/readiness-proof-map` separately. When `useProviderEvidenceSearch` and `requireProviderEvidenceSearch` are true, the async backend path also records provider-backed evidence through `recordAgentEvidenceSearchWithProvider`, returns `settings-workflow-smoke-provider-evidence/v1`, and exposes the evidence-search plus provider usage routes. The UI only calls this route and marks the workflow row passed when the backend returns a standard `product-brief` Agent submission, provider-backed evidence, provider usage, Group Chat transcript proof, Timeline proof, Event Ledger proof, a Flow Graph node, and the matching Proof Map submission route. This keeps Settings Health from claiming autonomous collaboration readiness from a worklog-only pulse, a browser-composed probe, or a provider status that was never consumed by an Agent workflow.

Sync Protocol Audit is also independently queryable through `GET /projects/:id/sync-protocol-audit`. The embedded Manager Dashboard and Ready Package audit remains useful for the aggregate view, but Backend Worker Station now has a direct `Sync Protocol` control and snapshot that read the standalone route. The Harness verifies Leader assignment and change-request protocol rows through API, HTTP, and browser checks, so C-side collaboration governance can audit A-side delivery state without depending on a bundled dashboard refresh.

Manager Live Command Center is independently queryable through `GET /projects/:id/manager-command-center`. The embedded dashboard panel remains the main operator view, but Backend Worker Station now has a direct `Sync Command` control and route-backed snapshot for the next C-side action, attention queue, Agent readiness, and run route. This keeps the C-side control room refreshable as a standalone backend read model while frontend mock replacement continues.

Manager Scenario Walkthrough is independently queryable through `GET /projects/:id/manager-scenario-walkthrough`. Backend Worker Station now has `Sync Walkthrough`, a route-backed snapshot for completed/runnable steps, next gap, and next run route, while execution remains delegated to Action Queue receipts. This gives the Manager a backend-owned acceptance-story map instead of requiring the UI to infer the step path from the aggregate dashboard.

Manager Use Case Audit is independently queryable through `GET /projects/:id/manager-use-case-audit`. Readiness Proof Map now exposes the same customer-story coverage through `managerUseCaseAuditRoutes` and `managerUseCaseAuditSummary`, linking Manager Dashboard, Manager Action Queue, Manager Flow Graph, transcript/timeline/event proof, and an explicit production-blocker boundary. This gives the C-side a route-backed answer to "is the product-team story covered?" instead of relying on frontend fallback audit rows.

Manager Proof Map renders Governance/action route cards for the same C-side control plane. Readiness Proof Map now exposes `managerCommandCenterRoutes`, `managerScenarioTrailRoutes`, `managerScenarioWalkthroughRoutes`, `managerRequirementMatrixRoutes`, `syncProtocolAuditRoutes`, `managerUseCaseAuditRoutes`, and `managerActionQueueRoutes` with matching summaries, proof ids, timeline ids, event ids, task ids, Agent ids, upstream routes, run templates where applicable, and production-blocker text. React renders these as source-badged Proof Map cards with `Sync Proof Map`, direct governance read-model sync, chat proof, and timeline proof exits, so the Manager cannot treat local command-center or action-queue interpretation as backend proof. Manager Flow Graph also projects the same C-side routes as `manager-command-center`, `manager-scenario-trail`, `manager-scenario-walkthrough`, `manager-requirement-matrix`, `sync-protocol-audit`, `manager-use-case-audit`, and `manager-action-queue` nodes with route/proof/production-boundary attachments and edges into the execution loop.

`POST /workers/autonomous/start` is intentionally idempotent for C-side operation. If the scheduler is already running and the caller still sends `runImmediately: true`, the backend now executes the same immediate current-project pulse instead of returning status only. This preserves `manager-ui-scheduler-start-pulse` and `http-autonomous-scheduler-startup-agents` proof when an operator presses Start again during a live local/private-pilot session.

Agent workers now support a service-side autonomous strategy contract. `useAutonomousStrategy: true` on `POST /projects/:id/agents/:agentId/work-cycle`, or `useAgentAutonomousStrategy: true` on `/workers/autonomous/tick` and scheduler start, derives an `agent-autonomous-strategy-decision/v1` before the worker executes side effects. The decision ranks open requested-change obligations, pending teammate submissions, owned-task completion, management signals, open owned work, and monitor-only states; it then resolves the concrete controls for submission, review, or revision response. The decision is redacted, persisted into `agentWorkerLedger`, attached to the worker timeline log/event payload, exposed in Agent work-cycle and due-worker API responses, and summarized in Manager Dashboard continuous-work rows so Flow Graph worker-loop nodes can show the Agent's chosen next step instead of only showing a pulse happened.

`GET /projects/:id/agent-autonomous-action-queue` exposes those decisions as a Manager-readable queue with one row per Agent, including selected action, rationale, resolved body template, delegated work-cycle route, proof ids, schedule state, and checksum. `POST /projects/:id/agent-autonomous-action-queue/:agentId/run` executes the selected row through the normal Agent work-cycle path with `useAutonomousStrategy: true`, then returns `agent-autonomous-action-queue-item-run` plus strategy/run evidence, delegated output objects, and lightweight read-model refresh routes when requested. Each queue run now also emits `autonomous-action-decision/v1`, a general action decision wrapper over the selected strategy, initiative, queue row, target artifact/review/evidence intent, and optional provider preflight. The decision checksum/action is copied into Agent Queue run receipts, Autonomous Run Control delegated receipts, Collaboration Intent run receipts, due-worker summaries, Manager Flow Graph attachments, and Readiness Proof Map routes so C-side operators can audit why an Agent ran, submitted, reviewed, revised, waited, or used provider evidence. Direct queue runs now lift returned `workSubmission`, `artifact`, `evidenceSearch`, `review`, `reviewedSubmission`, `reviewResponseSubmission`, and `reviewResponseArtifact` objects to the write response so C-side surfaces can inspect the product node that the A-side action created. Agent due-worker responses, HTTP scheduler ticks, and scheduler startup sweeps now also return the post-run queue list and retain it in scheduler status, so the C-side scheduler result can hydrate "who acts next" without a separate local inference step. Manager Dashboard embeds the queue, Readiness Proof Map exposes `agentAutonomousActionRoutes`, and Manager Flow Graph renders queue rows as auditable Agent action nodes. This is the current engineering bridge from "Agent has a subjective next-step judgement" to "A/C-side backend can schedule, inspect, and run it." It still depends on local prototype storage/worker loops until managed queue infrastructure and production policy controls are cut over.

React now consumes the same contract in the Manager Dashboard: `Sync Agent Queue` fetches the standalone backend queue, the `Agent Autonomous Queue` panel displays Agent-selected rows and run routes, and each enabled row runs through `POST /projects/:id/agent-autonomous-action-queue/:agentId/run` with `includeReadModels: false`. Scheduler status, start, Server Pulse, Hour Pulse, and Day Report also hydrate the queue when the backend tick returns it. Explicit `Sync Agent Queue` uses the same user-visible timeout budget as the control sync, and running an Autonomous Run Control action now awaits a fresh Agent queue read before the UI exposes the next Agent action button, preventing stale C-side queue state from hiding a backend-runnable A-side action. The UI renders the backend run receipt, direct `Agent Action Output Nodes` for returned submissions/evidence/reviews/review responses, `autonomous-action-decision/v1` summaries for Run Control, Agent Queue, Collaboration Intent, and Flow Graph attachments, chat/timeline proof exits, plus a visible backend resource route/event id row for each product node. Transcript-only output rows show the backend transcript route instead of a resource route. The browser Harness covers this path in `npm run ui:manager-backend`, so the C-side control is no longer a local worklog inference.

React also consumes `autonomous-run-control/v1` in the Backend Worker Station. Manager Dashboard, Manager Ready Package, Scheduler status/tick/start, Agent write receipts, and `Sync Agent Queue` all hydrate the same `autonomousRunControl` state; the station renders status, next action lane, Manager/Agent ready counts, worker queued proof, scheduler tick route, next runnable actions, gates, run receipts, direct `Run Control Output Nodes` for delegated submissions/evidence/reviews/review responses/result messages, and bounded loop receipts. Workspace project catalog sync, Backend Worker status checks, scheduler start/stop, aggregate Manager/Proof/Cockpit/Queue syncs, `Server Pulse`, and `Sync State` require an injected or saved backend target plus a backend-writable real project before probing `/projects`, worker routes, or project read-model routes, while save-URL flows can still pass the explicit target override. Ready next actions can be executed one at a time or through the bounded loop route, then the UI refreshes Manager, Agent, transcript, timeline, and proof submodels. Bounded Autopilot sessions now use the scheduler-owned lane as the main C-side continuation control: the station's `Scheduler Tick` calls `/workers/autonomous/tick` with `tickAutopilotSessions`, stores the returned Autopilot worker receipt, shows the `/workers/autopilot/due` route path and `scheduler-autopilot-controls/v1` state, and keeps direct session tick as a manual diagnostic fallback. After Autopilot commands, React consumes the returned `readModels` routes for session, operating-loop, cycle-consistency, Flow, Proof, timeline, event, and Agent queue refresh instead of trusting browser-local session state. The Agent Focus Workspace now renders `Control Runs` from the backend Agent Dashboard and can show the latest `autonomous-run-control-action-run/v1` receipt with chat/timeline proof exits. This is the UI bridge from fragmented C-side queue panels to one A/C-side operating surface.

Agent Focus Workbench options also follow the Agent Dashboard boundary. Backend-online real projects must have `GET /projects/:id/agents/:agentId/dashboard` before the Workbench treats tasks, review links, revision targets, evidence dependencies, or task proof exits as Agent-owned context. If that dashboard is missing, React renders `agent-workbench-backend-dashboard-required-*`, disables Workbench writes, and does not fall back to `activeProject.tasks`, `activeProject.submissionReviews`, `activeProject.agentSubmissions`, or `activeProject.evidenceSearches` for evidence, artifact, draft, review, revision, or final-deliverable submission context. The write functions enforce the same rule below the button layer: stale event paths produce a failed non-proof Workbench receipt instead of calling backend Agent write routes with browser-local context. Offline/demo projects can still use the visible fallback.

Private-pilot handoff and closeout now have the same C-side receipt proof. `npm run agents:product-team:private-pilot:launch-handoff` stops the generic product-team acceptance Harness after the backend proves launch approval is the next required private-pilot step but before approval/export/customer-visible receipts are written. `npm run ui:manager-private-pilot` prepares that handoff state in an isolated `.tmp/product-team-acceptance/<run-id>` store/runtime checkpoint, applies a bounded preparation timeout, starts the browser against that file-backed store, syncs the real backend project, records Manager/security launch approvals, requests and approves the evidence export, records the download audit, clicks the four Manager Ready Package private-pilot receipt buttons, and verifies the backend Manager Flow Graph plus Readiness Proof Map carry the final acceptance-report evidence. Launch approval POST now honors `includeReadModels: false` as a receipt-first command and returns explicit launch approval, evidence export, production audit, and private-pilot go-live refresh routes instead of embedding the large Manager package. This gate protects the C/A boundary: a manager click must become an A-side receipt with proof, not a frontend state mutation.

Kickoff meeting generation now carries `kickoff-generation-provenance/v1` on the meeting and evidence packet. The contract separates `deterministic-validation`, `model-provider-rehearsal`, `model-provider-backed`, and `development-fallback` modes, keeps `productionClaim: "blocked"` until production provider controls are independently approved, and is rendered in the initiation meeting/result proof cards. Approval persists the same provenance into project initiation, kickoff charter evidence, and Manager Dashboard `Kickoff Generation Source`, so the formal project view can still prove whether the meeting came from deterministic Harness logic, model rehearsal, provider-backed generation, or development fallback. This lets the product use deterministic Harnesses and local model-provider rehearsals without presenting them as production provider-backed meetings.

### 5.5 Local Backend Bridge Read Models

The local backend now exposes two bridge read models before a real managed database adapter exists:

- `GET /projects/:id/persistence-adapter-plan` returns `managed-persistence-adapter-plan/v1`, including the `managed-persistence-adapter-contract/v2` method list for schema creation, transactional imports, table reads, event/audit/replay/checkpoint appends, checksum verification, RLS verification, backup/restore, shadow reads, cutover commit, and rollback.
- `GET /projects/:id/persistence-adapter-dry-run` returns `managed-persistence-adapter-dry-run/v1`, executing the cutover checks against the current snapshot and migration dry-run through `src/agents/managedPersistenceAdapter.js` by default. The adapter facade exposes `managed-persistence-adapter-status/v1` from `MANAGED_PERSISTENCE_ADAPTER_DRIVER`, `MANAGED_PERSISTENCE_DATABASE_URL`, `MANAGED_PERSISTENCE_HTTP_ENDPOINT`, `ADAPTER_GATEWAY_HTTP_ENDPOINT`, and `MANAGED_PERSISTENCE_REQUIRE_REAL_ADAPTER`. The default `local-shadow` driver implements the same method names as the production-facing contract and emits `managed-persistence-adapter-execution-receipt/v1` rows for connect, schema creation, import batches, checksum verification, relation verification, RLS verification, backup creation, restore, shadow-read comparison, and rollback. When `MANAGED_PERSISTENCE_ADAPTER_DRIVER=http-json` and an endpoint is configured, the API async handler sends the current project snapshot, table plans, migration dry-run summary, adapter contract, and shadow-read plan to `POST /persistence/dry-run` on the private gateway, then returns `adapterExecution.schemaVersion = managed-persistence-adapter-gateway-execution/v1` with the gateway `managed-persistence-adapter-execution-receipt/v1`. `postgres` is still a future real driver; all dry-run paths keep `productionCutoverReady` false until real database cutover is separately approved. The migration table plan scopes `evidence_sources` by `projectId + evidenceSearchId + id`, because provider/local source ids are only unique inside a single evidence search. The dry-run verifies critical table coverage for projects, messages, events, tasks, Agent states, submissions, evidence searches, submission reviews, project membership policies, replay records, security audit rows, provider usage/eval rows, worker runs, and read-model checkpoints; it also checks shadow-read parity, transaction rollback, backup/restore readiness, audit-stream continuity, RLS coverage, and read-model checkpoint parity.

`src/agents/adapterGatewayClient.js` defines the shared `http-json` private adapter gateway contract used by the async persistence and queue dry-run routes. A gateway must expose:

- `GET /health` returning `adapter-gateway-health/v1` with `managed-persistence-adapter-contract/v2` and `worker-queue-adapter-contract/v1` capabilities.
- `GET /state` returning `adapter-gateway-state-summary/v1` with storage adapter, persistence dry-run, table-record, queue-row, lease, and receipt summaries.
- `POST /persistence/dry-run` returning `managed-persistence-adapter-execution-receipt/v1`.
- `POST /worker-queue/dry-run` returning `worker-queue-adapter-execution-receipt/v1`.

`npm run adapters:gateway` starts local mock gateways and validates the contract shape. It also configures the service to `http-json`, creates a backend project, and proves the project API dry-run routes return gateway execution receipts. It is a deployment-prep contract test, not a substitute for a real managed database or durable queue cutover.

`src/agents/adapterGatewayStore.js` defines the storage adapter contract behind the runnable gateway. It exposes `adapter-gateway-storage-adapter-status/v1`, async-compatible `readState`, `writeState`, and `summary` methods, with current `json-file`, `memory`, and `postgres` / `postgres-compatible` drivers. `json-file` persists imported shadow table records, queue rows, queue leases, dead-letter rows, and dry-run receipt summaries to `ADAPTER_GATEWAY_STORE`; `memory` is available for ephemeral validation. The Postgres-compatible driver exposes `adapter-gateway-postgres-schema-plan/v1`, redacted `ADAPTER_GATEWAY_POSTGRES_URL` status, `ADAPTER_GATEWAY_POSTGRES_SCHEMA`, query-bound execution status, schema/upsert/snapshot write operations for project summaries, table records, queue rows, queue leases, dead letters, dry-run receipts, and state snapshots, plus `adapter-gateway-postgres-readback/v1` for snapshot checksum and table/queue count parity. It is a gateway storage boundary rehearsal until a real managed Postgres client, real database readback, backup/restore drill, RLS enforcement, monitoring, and cutover approval are added. `ADAPTER_GATEWAY_STORAGE_DRIVER` selects the driver.

`src/agents/adapterGatewayServer.js` is the runnable local private gateway reference implementation. It exposes `GET /health`, `GET /state`, `POST /persistence/dry-run`, `POST /worker-queue/dry-run`, and `POST /attestations/managed-production-control`; supports bearer auth through `ADAPTER_GATEWAY_AUTH_TOKEN`; writes through the storage adapter; and keeps `productionCutoverReady: false`. `GET /projects/:id/adapter-gateway-preflight` returns `adapter-gateway-preflight/v1`: in local-shadow mode it records that no external gateway is required for the current private-pilot rehearsal, and in `http-json` mode it calls the live gateway `GET /health` and `GET /state`, checks managed-persistence and worker-queue capabilities, exposes storage/queue state metadata without secrets, and feeds the deployment preflight gateway gate while still keeping production cutover blocked. The attestation route is fail-closed: it signs `adapter-gateway-managed-production-control-attestation/v1` with `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET` or `PRODUCTION_ATTESTATION_SIGNING_SECRET` only when the gateway storage adapter is query-bound Postgres and latest readback parity is true; otherwise it returns `adapter-gateway-managed-production-attestation-blocked`. `npm run adapters:gateway-server` starts that process, and `npm run adapters:gateway-server:validate` verifies bearer rejection, the shared client contract, project API dry-run integration, live project API gateway preflight, the storage adapter status contract, persisted table records, persisted queue rows, and persisted lease state. `npm run adapters:gateway-postgres-store:validate` binds the Postgres-compatible store to a fake query function and proves that the same private gateway and project API dry-run paths emit schema-plan, table-record, queue-row, lease, dry-run receipt, snapshot write, readback parity operations, and a signed managed-production control attestation whose HMAC payload matches `production-evidence-integrity-audit/v1`. This process gives the project a deployable adapter boundary before the storage engine is replaced with a real managed database and durable queue.

### 5.6 API and HTTP Server

The API layer is HTTP-shaped and can be mounted behind a real backend.

Main files:

- `src/agents/agentProjectApi.js`
- `src/agents/agentProjectHttpServer.js`
- `scripts/agent-project-server.mjs`

Important route groups:

- `/llm/status`
- `/llm/test`
- `/search/status`
- `/search/test`
- `/kickoff-meetings`
- `/projects/:id/chat`
- `/projects/:id/meeting`
- `/projects/:id/autonomous-cycle`
- `/projects/:id/worker-queue`
- `/projects/:id/submissions`
- `/projects/:id/agents/:agentId/submissions`
- `/projects/:id/evidence-searches`
- `/projects/:id/agents/:agentId/evidence-searches`
- `/projects/:id/submissions/:submissionId/reviews`
- `/projects/:id/submission-reviews`
- `/projects/:id/agents/:agentId/work-cycle`
- `/workers/autonomous/due`
- `/workers/agents/due`
- `/workers/autopilot/due`
- `/workers/queue-snapshot`
- `/projects/:id/manager-dashboard`
- `/projects/:id/manager-flow-graph`
- `/projects/:id/readiness-proof-map`
- `/projects/:id/mvp-readiness`
- `/projects/:id/persistence-snapshot`
- `/projects/:id/persistence-migration-plan`
- `/projects/:id/persistence-migration-dry-run`
- `/projects/:id/persistence-adapter-plan`
- `/projects/:id/persistence-adapter-dry-run`
- `/projects/:id/pilot-launch-readiness`
- `/projects/:id/deployment-preflight`
- `/projects/:id/production-launch-audit`
- `/projects/:id/production-launch-gap-register`
- `/projects/:id/production-launch-control-center`
- `/projects/:id/production-launch-evidence-dossier`
- `/projects/:id/production-operations-readiness`
- `/projects/:id/production-operations-control-receipts`
- `/projects/:id/production-deployment-control-receipts`
- `/projects/:id/production-security-control-receipts`
- `/projects/:id/launch-approvals`
- `/projects/:id/private-pilot-go-live-readiness`
- `/projects/:id/private-pilot-release-candidates`
- `/projects/:id/private-pilot-launch-runs`
- `/projects/:id/private-pilot-launch-health-checks`
- `/projects/:id/project-evidence-archive`
- `/projects/:id/artifact-quality-audit`
- `/projects/:id/evidence-quality-audit`
- `/projects/:id/evidence-index-readiness`
- `/projects/:id/budget-alert-readiness`
- `/projects/:id/error-reporting-readiness`
- `/projects/:id/evidence-source-review-workflow`
- `/projects/:id/operations-readiness`
- `/projects/:id/provider-readiness`
- `/projects/:id/provider-controlled-run`
- `/projects/:id/provider-eval-runs`
- `/projects/:id/security-boundary`
- `/projects/:id/security-access-audit`
- `/projects/:id/security-audit-stream`
- `/projects/:id/transcripts`
- `/projects/:id/tasks/:taskId/evidence`

### 5.7 Local Workspace Runtime

The local runtime binds a project to a workspace folder and can expose:

- project memory folders
- workspace file list/read/write/delete
- explicitly allowed command execution
- local runtime project archive output
- backend `project-evidence-archive/v1` read model for manager-verifiable project evidence bundles
- backend `artifact-quality-audit/v1` read model for generic submission coverage, proof readiness, review/revision/final closure, generated draft quality status, and calibrated production-quality blockers
- backend `evidence-quality-audit/v1` read model for decision-ready evidence quality/source-safety/proof-route checks
- backend `evidence-index-readiness/v1` read model for local evidence/artifact indexability, proof routes, redaction, and managed vector-store production blockers
- backend `budget-alert-readiness/v1` read model for local budget/request headroom, provider usage ledger proof, and managed alert-routing production blockers
- backend `error-reporting-readiness/v1` read model for local log streams, alert rules, recovery runbook, incident-drill status, and managed observability production blockers
- backend `evidence-source-review-workflow/v1` read model for reviewer-visible source review queues, proof routes, and production source-governance blockers

Main file:

- `src/agents/localProjectRuntime.js`

Command execution must remain disabled by default and allowlisted when enabled.

### 5.8 Model Provider Layer

The model provider is BYOK-oriented and currently optional.

Main file:

- `src/agents/modelProvider.js`
- `src/agents/searchProvider.js`
- `src/agents/secretVault.js`

Design direction:

- `model-provider-adapter-manifest/v1` documents the supported local adapter styles: OpenAI-compatible chat completions, OpenAI, Anthropic messages, and Gemini generate-content
- deterministic fallback remains available for validation
- configured model calls enrich kickoff meetings and runtime intent
- configured search calls can populate evidence-search sources through a private search gateway
- provider failures should not erase saved meetings or project state
- production deployments must manage secrets outside browser state; local pilots can use the envelope secret-vault contract, while public production still needs managed KMS/Secret Manager
- provider status endpoints must never return API keys or raw secrets

`npm run agents:model-provider-adapter` validates this adapter boundary without external provider calls. It reads the adapter manifest, checks OpenAI-compatible request/response mapping through a fake fetch, verifies Anthropic and Gemini adapter styles are selectable, confirms missing-key calls fail closed before network access, and ensures status/manifest responses do not leak plaintext keys. This closes the local MVP "pluggable LLM adapter" requirement while keeping production LLM rollout blocked on managed KMS, centralized provider audit, calibrated eval datasets, retry/circuit policy, alerting, and incident runbooks.

The search provider is intentionally generic. `SEARCH_PROVIDER=deterministic` supports validation without external network calls. `SEARCH_PROVIDER=http-json` can call a private BYOK search gateway that returns `sources` or `results`; the backend stores normalized source packets in `evidenceSearches`. The same provider can now be configured at runtime from Secret Vault: sealing `search.endpoint` calls `setEndpoint(..., 'local-secret-vault', 'http-json')`, sealing `search.apiKey` calls `setApiKey(..., 'local-secret-vault')`, and `/search/status` exposes only redacted endpoint/key-source metadata.

React Settings now treats BYOK, deployment, health, and model routing as backend-owned runtime boundaries. The Deployment tab renders `settings-deployment-runtime-boundary`, reads `settings-runtime-readiness/v1` from `GET /settings/runtime-readiness` or `GET /projects/:id/settings-runtime-readiness`, and points to backend worker/provider/deployment routes instead of offering browser-only environment or endpoint selectors. It also exposes the editable local backend API target through `settings-deployment-backend-url-input` / `settings-deployment-save-backend-url`, reusing the Backend Worker Station URL store; saving a new target clears stale Provider, Vault, startup, and Health read models, marks the backend target as configured, then immediately syncs the saved target instead of waiting for a later tab refresh. The editable field is draft-only until Save, so provider/runtime/Health/startup/Workspace/Initiation/Manager auto-syncs use `backendStation.baseUrl` and never use the unsaved draft URL. Settings/Workspace/Initiation auto-syncs only run after a backend URL has been injected or saved, so the first-run UI does not auto-probe the default `http://127.0.0.1:8787` before the user chooses the active backend. Manual Sync runtime, Sync startup, Quick Check, Workflow Smoke, and footer Test Connection controls use the same configured-target guard. The Models tab renders `settings-model-runtime-boundary`, reads model status from `/llm/status`, and renders the same Settings Runtime Readiness contract instead of offering browser-only model/routing controls. The Health tab renders `settings-health-readiness/v1` from `GET /settings/health-readiness` for Quick Check rows instead of synthesizing browser-side health results; Workflow Smoke remains the explicit deeper `POST /settings/workflow-smoke` probe path because it can issue provider requests and create a probe project. The Keys tab renders `settings-provider-boundary`, reads `/llm/status` and `/search/status`, can run `/llm/test` and `/search/test`, and only displays redacted provider metadata such as configured/enabled flags, `runtimeEnabled`, `enabledSource`, redacted endpoints, `apiKeySource`, `hasApiKey`, and secret-vault readiness/key id. `GET /local-mvp-startup-readiness` exposes the global backend startup preflight (`local-mvp-startup-readiness/v1`) for this UI path: it aggregates Secret Vault readiness, model/search runtime state, provider-vault redaction, project catalog readability, next action, and validation commands before the UI claims a local MVP session can start. Any real credential, endpoint, model-routing, retry, or concurrency changes must happen through backend environment, provider policy, adapter gateway, the local secret-vault contract, or a future managed KMS/Secret Manager flow; React must not persist raw provider keys or treat static model selectors as real provider configuration.

Startup and Settings health readiness also participate in the project proof surface. Workspace Hub renders `workspace-local-mvp-startup-readiness` from `GET /local-mvp-startup-readiness` before Start Initiation, and the Project Initiation flow renders `initiation-startup-readiness-gate` with Sync/Settings actions while disabling every Start Kickoff and Approve/Create command surface until the current configured backend target reports `readyForFirstProjectRun=true`. Entering Project Initiation auto-syncs the startup route once for the configured active backend target, so a user who just completed Settings does not have to discover a second manual Sync before kickoff, while a user who has not chosen a backend target is sent to Settings instead of seeing a failed default probe. `refreshLocalMvpStartupReadiness`, `startInitiationMeetingSession`, and `approveInitiationProject` repeat that configured-target guard, so stale readiness from another target and hidden/legacy controls fail closed as well. Settings renders the same startup route in the provider setup flow, Manager Ready Package embeds `localMvpStartupReadiness` and `settingsHealthReadiness`, Readiness Proof Map exposes `localMvpStartupReadinessRoutes` and `settingsHealthReadinessRoutes`, and the focused startup/health validators assert those routes so first-run blockers cannot remain as Settings-only messages.

React Settings Privacy now has one concrete backend-owned write path instead of browser-only toggles. The tab renders retention mode, provider log mode, and evidence-export approval controls from `activeProject.projectSettings.privacyPolicy`; changes call `PUT /projects/:id/project-settings` with a `project-privacy-policy/v1` payload. The backend preserves existing language when a privacy-only update is sent, stores the policy in the file-backed project record, and emits project settings audit, timeline, and event-ledger proof. `npm run agents:project-settings:privacy` validates this contract without launching a browser. This is local MVP policy evidence only: managed KMS, centralized audit retention, provider-side policy enforcement, and evidence-export retention/deletion controls remain P2 blockers.

React Settings Integrations now has concrete backend-owned Usage Budget and Agent tool-grant write paths. The Usage Budget panel renders daily provider budget and hourly provider request presets from `activeProject.projectSettings.providerBudgetPolicy`; changes call `PUT /projects/:id/project-settings` with `project-provider-budget-policy/v1`. The External Tools panel renders provider-test, model kickoff/intent/artifact-draft, and search-evidence grants from `activeProject.projectSettings.toolGrantPolicy`; changes call the same route with `project-tool-grant-policy/v1`. The backend stores both policies with project settings audit/timeline/event proof and merges them into the effective `provider-control-policy/v1` for that project. `project-settings/v1` also returns `project-integration-capabilities/v1`, which classifies provider budget and Agent tool grants as backend-backed editable controls, and classifies Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting as backend-backed read-only route contracts rather than missing backend APIs. Those read-only rows point to `/projects/:id/adapter-gateway-preflight`, `/projects/:id/provider-readiness`, `/projects/:id/evidence-index-readiness`, `/projects/:id/budget-alert-readiness`, and `/projects/:id/error-reporting-readiness`, respectively. `adapter-gateway-preflight/v1` proves the local/private adapter rehearsal path while production proxy/webhook delivery stays blocked until private gateway delivery, signing, retries, and dead-letter storage exist. MCP remains governed through Provider Readiness and Agent tool grants until a managed tool registry and runtime identity exist. `evidence-index-readiness/v1` indexes local evidence searches, source snapshots, provider receipts, Agent submissions, and artifact storage proofs for local MVP inspection, checks redaction, and keeps production vector storage blocked. `budget-alert-readiness/v1` computes local daily budget and hourly request headroom from the project provider budget policy plus `project.providerUsageLedger`, checks redaction, exposes proof routes, and keeps production centralized alert routing blocked. `error-reporting-readiness/v1` maps Operations Readiness log streams, alert rules, recovery runbook, incident drill, and local error-signal counts into a Settings-readable contract while centralized logs/traces/incident ownership remain blocked. `GET /projects/:id/settings-integration-readiness` returns `settings-integration-readiness/v1`, aggregating the editable policy rows plus those route-backed readiness contracts into one Settings packet with row schemas, route readiness, action-required counts, checksums, validation commands, Manager Ready Package embedding, and Readiness Proof Map routes. React renders that aggregate contract instead of editable fake integration fields, backend-missing warnings, or a route list with no current readiness state; before Workspace or Integrations capability contracts sync, summary gaps render as `not synced` instead of `0`. Settings Keys separates draft entry from persistence through `settings-provider-readiness/v1`: `GET /settings/provider-readiness` and `GET /projects/:id/settings-provider-readiness` tell React whether provider secret draft fields may unlock, whether `Seal` can run, the next backend action, Vault/provider routes, and redaction status. Provider secret draft fields unlock after the saved backend target exists; `Seal` persists only through `/secret-vault/seal` and remains disabled until backend Secret Vault readiness reports `canSealSecrets: true`. Provider secrets are not persisted in browser storage. The Secret Vault badge is three-state: `sync required` before a status read, `backend unreachable` when the status route fails, and `vault required` only after the backend confirms the Vault is missing or not ready. The Settings footer no longer presents a global no-op Save button; backend-backed controls save on change through their route handlers, and unavailable controls remain route-contract blockers. `GET /projects/:id/provider-readiness` and `GET /projects/:id/provider-controlled-run` consume the project-level budget and tool grants, so the Manager sees the same budget headroom and Agent tool decisions that provider calls will enforce. `npm run agents:settings-provider-readiness` validates the Settings readiness route, blocked/ready Vault states, project-scoped route, and secret redaction. `npm run agents:evidence-index-readiness` validates the local index route and production vector-store blocker. `npm run agents:budget-alert-readiness` validates the local budget alert route and production alert-routing blocker. `npm run agents:error-reporting-readiness` validates the local error reporting route and production observability blocker. `npm run agents:settings-integration-readiness` validates the aggregate Integrations readiness route, Manager Ready Package / Readiness Proof Map inclusion, route-backed rows, and production-blocked status. `npm run agents:settings-contracts` runs the low-write aggregate Settings backend gate across local MVP startup readiness, health, runtime, provider-readiness, integration-readiness, search endpoint/key Vault binding, privacy, provider budget, Agent tool grants, integration capability, and workspace capability contracts before heavier browser validation. `npm run agents:project-settings:provider-budget` validates the budget write path, `npm run agents:project-settings:tool-grants` validates tool-grant persistence plus the `agent-tool-grant-missing` controlled-run denial when search evidence access is removed, and `npm run agents:project-settings:integrations` validates the capability rows, audit proof, event ledger, and file-backed persistence. Production cost alerting, task-scoped authorization, managed runtime identity, centralized usage audit, provider incident controls, managed policy storage, managed alert routing, centralized logs/traces, managed tool registry, production adapter gateway, and managed vector memory remain P2 blockers.

Focused Settings/readiness validation scripts use process-scoped `.tmp` roots so parallel or aggregate validation does not delete another check's file store, runtime directory, Vault records, or workspace proof.

Manager Proof Map renders Settings readiness route cards for Local MVP Startup, Settings Health, Settings Provider, Settings Runtime, and Settings Integrations. Those cards consume `localMvpStartupReadinessRoutes`, `settingsHealthReadinessRoutes`, `settingsProviderReadinessRoutes`, `settingsRuntimeReadinessRoutes`, and `settingsIntegrationReadinessRoutes`, expose backend source badges plus `Sync Proof Map`, open the relevant Settings tab, and fail closed when a summary exists without route proof.

Error reporting readiness now participates in the same Manager proof surface as other launch gates. Manager Ready Package embeds `errorReportingReadiness`, Readiness Proof Map exposes `errorReportingReadinessRoutes` plus `errorReportingReadinessSummary`, and `npm run agents:error-reporting-readiness` asserts that local log/error proof is route-backed without claiming production observability.

Budget alert readiness is also part of the Manager proof surface: Manager Ready Package includes `budgetAlertReadiness`, Readiness Proof Map exposes `budgetAlertReadinessRoutes`, and `npm run agents:budget-alert-readiness` verifies those formal proof exits in addition to the Settings route.

React Agent Focus evidence search now follows the same provider-first boundary. The Workbench posts `useProvider: true` to `POST /projects/:id/agents/:agentId/evidence-searches` so a configured deterministic or private `http-json` provider creates the evidence sources, provider receipt, source snapshots, timeline proof, event proof, and transcript proof. If the provider is unavailable, the UI only records a backend `manual-source-record` when the Manager/Agent supplied an explicit source URL; without provider output or an explicit URL, it fails closed and does not create an `agent-note` evidence row from browser draft text.

Evidence source normalization now also computes `evidence-source-safety/v1`: every stored source receives `sourceSafetyLevel`, `sourceSafetyScore`, `sourceSafetySignals`, prompt-injection signal counts, secret-pattern signal counts, and a `sourceSafetyJudgement`. The aggregate evidence search stores `sourceSafetySummary`, which is exposed through task evidence, Manager Dashboard, Manager Flow Graph attachments, Readiness Proof Map, persistence snapshot rows, and provider readiness. The screening is provider-agnostic and checks URL scheme, local/private hosts, credentialed URLs, sensitive query parameters, raw secret-looking content, and prompt-injection language.

`getProviderReadiness` and `GET /projects/:id/provider-readiness` expose the real-provider rollout gate as `provider-readiness/v1`. It reads the redacted model/search provider status, deterministic validation provider, provider-backed evidence-search provenance, local checksummed source snapshots, provider receipts, proof routes, security-boundary redaction state, provider control policy, retry policy, circuit-breaker policy, local secret-vault seal/open/rotation status, provider usage/cost ledger, evidence source-safety summary, and production rollout controls. The service facade evaluates provider calls against `provider-control-policy/v1`: allowed providers/models, per-project hourly request limits, daily budget cents, Agent tool grants, retry attempts/backoff, and provider circuit state. Provider-backed evidence search writes `evidence-provider-receipt/v1` plus one `evidence-source-snapshot/v1` per source, and model/search provider calls write `provider-usage` events plus `project.providerUsageLedger` rows with policy decision, retry metadata, circuit-breaker metadata, outcome, cost estimate, receipt id, and evidence ids. Autonomous Agent worker evidence searches use the same audit shape under the local `agent-autonomous-worker` provider: every worker-generated evidence row carries a provider receipt, source snapshots, safe internal proof-route source metadata, source-review readiness, evidence custody rows, persistence rows, and project-archive contents. The persistence snapshot exports those rows as `evidence_provider_receipts`, `evidence_source_snapshots`, `provider_usage_ledger`, and, after shadow replay/control receipt submission, `provider_eval_runs` and `production_provider_control_receipts`, including retry/circuit, eval proof, and production control proof columns, and exports source-safety fields through `evidence_searches` / `evidence_sources`. The read model may mark the local provider contract ready for an MVP pilot, but it always keeps `readyForProduction: false` until managed KMS/Secret Manager, revocation, immutable managed source/provider audit storage, centralized alerting, and real-provider incident handling exist.

`getProviderControlledRun` and `GET /projects/:id/provider-controlled-run` expose `provider-controlled-run/v1`, the private-pilot run policy dry-run above Provider Readiness. It does not issue provider calls. Instead, it evaluates the model/search operations that a controlled BYOK pilot would need: provider health checks, kickoff model support, intent parsing, model artifact draft generation, and evidence search. Each row records provider configured/enabled status, operation id, Agent id, allowlist/tool-grant decision, daily budget and hourly request headroom, retry/circuit state, estimated cost, existing usage-ledger proof, and whether that operation is allowed for the controlled private-pilot run. The aggregate gates require Provider Readiness, operation policy decisions, budget/rate headroom, model/search usage proof, model human-review boundary, evidence source-review governance, redaction boundary, and a production-overclaim guard. Manager Ready Package embeds it, Readiness Proof Map exposes `providerControlledRunRoutes`, Security Boundary lists the route policy, and Manager UI renders the plan. It can set `readyForPrivatePilotRun: true` locally while keeping `readyForProductionRun: false` until real provider eval runs, managed provider audit storage, centralized cost alerting, production incident runbooks, and calibrated human release policy exist.

`getProviderEvalRunWorkflow`, `recordProviderEvalRun`, and `GET|POST /projects/:id/provider-eval-runs` expose `provider-eval-run-workflow/v1` plus persisted `provider-eval-run/v1` receipts. `GET` shows whether the controlled-run operation plan has been replayed against existing usage-ledger proof. `POST` records a no-call `shadow-replay-from-provider-usage-ledger` run, checks critical `model:artifact-draft` and `search:evidence` proof, preserves policy/circuit, human-review, evidence-governance, redaction, and production-overclaim gates, then writes timeline/event proof. Manager Ready Package embeds the workflow; Manager Flow Graph adds provider-eval monitoring nodes; Readiness Proof Map exposes `providerEvalRunRoutes`; Security Boundary lists the GET/POST route policy; persistence snapshot exports `provider_eval_runs`. It can mark private-pilot provider eval ready locally while keeping `readyForProduction: false` until real provider eval datasets, managed eval storage, centralized provider cost alerts, incident runbooks, and calibrated release policy exist.

`getProductionProviderControlReceiptWorkflow`, `recordProductionProviderControlReceipt`, and `GET|POST /projects/:id/production-provider-control-receipts` expose `production-provider-control-receipt-workflow/v1` plus persisted `production-provider-control-receipt/v1` rows. The workflow derives the local provider contract from Provider Readiness, Provider Controlled Run, and the latest Provider Eval Run, then lists required production provider control ids, latest verified receipt per control, missing controls, proof ids, timeline/event ids, backend routes, and checksum. `POST` accepts runtime-platform or security-admin evidence for provider allowlists, budgets/rate limits, Agent tool grants, retry/circuit breakers, provider audit/cost ledger, encrypted secret-vault proof, source safety review, source snapshot/provider receipts, model-output quality review, real-provider eval, managed provider audit/eval storage, centralized cost alerting, calibrated release policy, and incident runbooks; each control row stores status, evidence id/route/checksum, owner role, completion time, and redacted detail. Recording a receipt writes timeline/event proof, feeds Production Launch Gap Register and Production Launch Control Center inputs, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionProviderControlReceiptRoutes`, lists the route policy in Security Boundary, renders `backend-production-provider-control-receipts-snapshot`, and exports `production_provider_control_receipts` rows through persistence/migration/dry-run contracts. Passing all receipt controls can set the provider rollout domain ready, but the wider launch audit/control center still owns the public production go/no-go.

`getProductionDeploymentControlReceiptWorkflow`, `recordProductionDeploymentControlReceipt`, and `GET|POST /projects/:id/production-deployment-control-receipts` expose `production-deployment-control-receipt-workflow/v1` plus persisted `production-deployment-control-receipt/v1` rows. The workflow derives the deployment contract from Deployment Preflight, adapter dry-runs, access hardening, custody, queue, and operations proof, then lists required deployment control ids, latest verified receipt per control, missing controls, proof ids, timeline/event ids, backend routes, and checksum. `POST` accepts runtime-platform, operations-owner, or security-admin evidence for enforced access, replay protection, audit fail-closed behavior, scheduler autostart, real persistence adapter, managed evidence custody, real queue adapter, environment promotion audit, rollback/smoke test, deployment change approval, and production domain/TLS; each control row stores status, evidence id/route/checksum, owner role, completion time, and redacted detail. Recording a receipt writes timeline/event proof, feeds Production Launch Gap Register and Production Launch Control Center inputs, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionDeploymentControlReceiptRoutes`, lists the route policy in Security Boundary, renders `backend-production-deployment-control-receipts-snapshot`, and exports `production_deployment_control_receipts` rows through persistence/migration/dry-run contracts. Passing all receipt controls can set the deployment domain ready, but the wider launch audit/control center still owns the public production go/no-go.

`getPrivatePilotGoLiveReadiness` and `GET /projects/:id/private-pilot-go-live-readiness` expose `private-pilot-go-live-readiness/v1`, the Manager-facing command view for the private-pilot lifecycle. It derives stage rows from generic delivery proof, release approvals, evidence handoff, provider eval, deployment/operations/security preflight, release candidate, launch run, post-launch health, customer acceptance, and production operations hardening receipts. The contract returns active phase, next action, go-live readiness, customer-acceptance readiness, production overclaim guard, proof ids, timeline/event ids, backend routes, and checksum. Manager Ready Package embeds it; Manager Flow Graph adds the `private-pilot-go-live-readiness` command node; Readiness Proof Map exposes `privatePilotGoLiveRoutes`; Security Boundary lists the GET route policy; Manager UI renders the stage panel. It can mark private-pilot go-live and acceptance state locally while keeping `readyForProduction: false` until the broader production launch audit and managed controls pass.

`getProductionLaunchGapRegister` and `GET /projects/:id/production-launch-gap-register` expose `production-launch-gap-register/v1`, the Manager-facing action register for remaining public-production launch work. It derives deduplicated gap rows from `production-launch-audit/v1`, `public-production-startup-readiness/v1` environment setup rows, `production-evidence-integrity-audit/v1`, deployment preflight production controls, production operations readiness controls, provider readiness controls, evidence custody controls, artifact quality controls, and submission review governance controls. Each row returns id, label, domain, owner, severity, status, action, route, validation command, env vars, proof ids, and checksum; the aggregate returns domain rows, next action, upstream checksums, proof ids, timeline/event ids, backend routes, summary counts, and checksum. Manager Ready Package embeds it; Manager Flow Graph adds the `production-launch-gap-register` decision node and carries `publicProductionEnvironmentSetupOpenCount` in the node attachment; Readiness Proof Map exposes the same count on `productionLaunchGapRoutes` and `productionLaunchGapSummary`; Security Boundary lists the GET route policy; Manager UI renders the gap register panel, including an Env Setup count from backend data. It is read-only and keeps `readyForProduction: false` until real managed infrastructure, identity, KMS, provider, audit, observability, incident, restore, approval controls, and explicit managed-production evidence close.

`getProductionLaunchControlCenter` and `GET /projects/:id/production-launch-control-center` expose `production-launch-control-center/v1`, the Manager-facing public-production release control view. It aggregates `production-launch-audit/v1`, `production-launch-gap-register/v1`, private-pilot go-live readiness, global `public-production-startup-readiness/v1`, production operations control receipts, production deployment control receipts, production security control receipts, production provider control receipts, production evidence integrity audit, launch approvals, deployment preflight, provider readiness, security boundary, evidence custody, and artifact quality into control rows, blocked rows, owner rows, stage rows, next action, proof ids, timeline/event ids, backend routes, upstream checksums, summary counts, and checksum. Manager Ready Package embeds it; Manager Flow Graph adds both the `production-launch-control-center` decision node and a separate `public-production-startup-readiness` blocker node wired into that control center; Readiness Proof Map exposes `productionLaunchControlCenterRoutes`; Security Boundary lists the GET route policy; Manager UI renders the control center panel. It is read-only, never writes approvals or receipts, and keeps `productionDecision: no-go` plus `readyForProduction: false` until every upstream production gate is closed by real managed-control evidence, explicit managed-production evidence integrity, and global public-startup readiness.

`getProductionLaunchEvidenceDossier` and `GET /projects/:id/production-launch-evidence-dossier` expose `production-launch-evidence-dossier/v1`, the Manager-facing launch evidence package. It aggregates `production-launch-audit/v1`, `production-launch-gap-register/v1`, `production-launch-control-center/v1`, `production-evidence-integrity-audit/v1`, private-pilot go-live readiness, deployment/security/provider/operations readiness, and production receipt workflows into a manifest, four control-domain rows, open gap rows, evidence-tier summary, launch state, proof ids, timeline/event ids, backend routes, summary counts, and checksum. Manager Ready Package embeds it; Manager Flow Graph adds the `production-launch-evidence-dossier` node fed by the control center, evidence integrity audit, and gap register; Readiness Proof Map exposes `productionLaunchEvidenceDossierRoutes`; Security Boundary lists the GET route policy; Manager UI renders `backend-production-launch-evidence-dossier-snapshot`. It is read-only and keeps `productionDecision: no-go` plus `readyForProduction: false` until real managed-production evidence and every launch control close.

`getProductionEvidenceIntegrityAudit` and `GET /projects/:id/production-evidence-integrity-audit` expose `production-evidence-integrity-audit/v1`, the read-only evidence-integrity layer above production control receipts. It reads production operations, deployment, security, and provider receipt records, derives the latest receipt per required control, and classifies each row as `missing`, `local-rehearsal`, `external-unattested`, or `managed-production`. Receipt control rows preserve `evidenceEnvironment` plus `attestationId` / `attestationRoute` / `attestationChecksum` / `attestationSignature`; an explicit `managed-production` environment without a valid signature from `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET` or `PRODUCTION_ATTESTATION_SIGNING_SECRET` remains `external-unattested`. The audit returns control rows, domain rows, proof ids, timeline/event ids, backend routes, summary counts, and checksum, is embedded in Manager Ready Package, gates `production-launch-audit/v1`, opens/closes the matching `production-launch-gap-register/v1` row, adds a Manager Flow Graph decision node, exposes Readiness Proof Map `productionEvidenceIntegrityRoutes`, is covered by Security Boundary/access policy, and renders as `backend-production-evidence-integrity-audit-snapshot` in the Manager UI. The Manager UI production launch/control/evidence snapshots carry source badges and proof-model sync actions so backend-backed, backend-required, and demo/offline states remain visibly distinct. It can mark the evidence-integrity audit ready only when every required production control has explicit managed-production evidence and signed control-plane attestation; it does not replace the broader launch audit/control center or real infrastructure deployment.

On backend-online real projects, the React Manager Ready Package no longer synthesizes production launch/control/evidence/receipt shapes from nearby package summaries when a standalone read model is absent. It renders `backend-model-missing` / `backend-required` for the missing `production-launch-gap-register`, `production-launch-control-center`, `production-evidence-integrity-audit`, `production-launch-evidence-dossier`, and production operations/deployment/security/provider receipt workflows; derived fallback rows remain limited to offline or explicit demo/sample projects.

`getBrainstormLayer` and `GET /projects/:id/brainstorm-layer` expose `brainstorm-layer/v1`, the read-only brainstorm aggregate above generic `brainstorm-board` submissions. It derives rows from Agent submissions, parses visible alternatives, links discovery reports, evidence searches, evidence packets, downstream decision/delivery artifacts, Manager Flow Graph nodes, Readiness Proof Map proof ids, timeline ids, event ids, backend routes, gates, summary counts, and checksum. Manager Dashboard and Manager Ready Package embed the model, Manager Flow Graph adds a `brainstorm-layer` aggregate node with synthesis edges, Readiness Proof Map exposes `brainstormLayerRoutes`, Security Boundary/access policy covers Manager/security-admin/observer reads, and Manager UI renders `backend-brainstorm-layer-snapshot`. It proves private-pilot brainstorm traceability without adding research-only fields or claiming production-grade facilitation.

`getAgentDashboard` now also embeds `agent-brainstorm-contribution/v1` for the route-scoped Agent. The model is derived from that Agent's generic `brainstorm-board` submissions, parsed alternatives, submission proof ids, timeline/event ids, direct evidence/downstream links when present, project-level evidence/downstream follow-through counts, and backend routes back to the Agent dashboard, submissions, evidence searches, Manager Flow Graph, and Manager `brainstorm-layer`. It keeps the Agent's subjective initiative visible in the personal dashboard while leaving the Manager aggregate as the source of cross-team synthesis.

`getPrivatePilotReleaseCandidateWorkflow`, `recordPrivatePilotReleaseCandidate`, and `GET|POST /projects/:id/private-pilot-release-candidates` expose the final private-pilot release-candidate freeze contract. `GET` returns `private-pilot-release-candidate-workflow/v1` with blocker/prerequisite gates derived from production launch audit, pilot launch readiness, launch approvals, project evidence export package, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage. `POST` records a checksummed `private-pilot-release-candidate/v1` receipt when those gates pass, freezes Manager Ready Package, MVP/pilot/deployment readiness, production launch audit, project evidence archive/export package, provider eval run, operations, persistence adapter, and worker queue adapter checksums, and writes timeline/event proof. Manager Ready Package embeds the workflow; Manager Flow Graph adds release-candidate decision nodes; Readiness Proof Map exposes `privatePilotReleaseCandidateRoutes`; Security Boundary lists the GET/POST route policy; persistence snapshot exports `private_pilot_release_candidates`. It can mark a private-pilot release candidate ready while keeping `readyForProduction: false` until real managed identity, persistence, queue, KMS, provider eval, centralized audit, deployment, and operations controls exist.

`getPrivatePilotLaunchRunWorkflow`, `recordPrivatePilotLaunchRun`, and `GET|POST /projects/:id/private-pilot-launch-runs` expose the controlled private-pilot launch run contract. `GET` returns `private-pilot-launch-run-workflow/v1` with launch gates derived from the frozen release candidate, production launch audit, evidence handoff package, project archive, provider eval, deployment preflight, operations runbook/incident drill, security boundary, Flow Graph, and Proof Map. `POST` records a checksummed `private-pilot-launch-run/v1` receipt when those gates pass, freezes release-candidate, launch audit, evidence, provider eval, deployment, operations, persistence adapter, and worker queue adapter checksums, and writes timeline/event proof. Manager Ready Package embeds the workflow; Manager Flow Graph adds launch-run decision nodes; Readiness Proof Map exposes `privatePilotLaunchRunRoutes`; Security Boundary lists the GET/POST route policy; persistence snapshot exports `private_pilot_launch_runs`. It can mark a controlled private-pilot launch run ready while keeping `readyForProduction: false` until real production environment promotion, rollback, centralized audit, incident ownership, and managed infrastructure exist.

`getPrivatePilotLaunchHealthCheckWorkflow`, `recordPrivatePilotLaunchHealthCheck`, and `GET|POST /projects/:id/private-pilot-launch-health-checks` expose the post-launch private-pilot health contract. `GET` returns `private-pilot-launch-health-check-workflow/v1` with gates derived from the latest launch run, operations readiness, worker queue adapter dry-run, persistence adapter dry-run, security boundary, provider eval workflow, project evidence archive, Flow Graph, Proof Map, and production overclaim guard. `POST` records a checksummed `private-pilot-launch-health-check/v1` receipt when blocker gates pass, freezes launch-run plus operations/security/provider/evidence/persistence/queue checksums, and writes timeline/event proof. Manager Ready Package embeds the workflow; Manager Flow Graph adds monitoring nodes; Readiness Proof Map exposes `privatePilotLaunchHealthCheckRoutes`; Security Boundary lists the GET/POST route policy; persistence snapshot exports `private_pilot_launch_health_checks`. It can mark private-pilot monitoring ready while keeping `readyForProduction: false` until centralized observability, alert routing, on-call ownership, managed incident systems, and real restore drills exist.

`getPrivatePilotAcceptanceReportWorkflow`, `recordPrivatePilotAcceptanceReport`, and `GET|POST /projects/:id/private-pilot-acceptance-reports` expose the customer private-pilot acceptance closeout contract. `GET` returns `private-pilot-acceptance-report-workflow/v1` with gates derived from release candidate, launch run, post-launch health, evidence handoff, generic product-team delivery proof, operations/security/provider proof, Flow Graph, Proof Map, and production overclaim guard. `POST` records a checksummed `private-pilot-acceptance-report/v1` when blocker gates pass, freezes release/launch/health/evidence/Flow Graph/Proof Map checksums, binds the launch run and health receipt, and writes timeline/event proof. Manager Ready Package embeds the workflow; Manager Flow Graph adds decision nodes; Readiness Proof Map exposes `privatePilotAcceptanceReportRoutes`; Security Boundary lists the GET/POST route policy; persistence snapshot exports `private_pilot_acceptance_reports`. It can mark customer private-pilot acceptance ready while keeping `readyForProduction: false` until public production managed controls, centralized observability, on-call ownership, incident systems, and restore drills exist.

`getProductionOperationsReadiness` and `GET /projects/:id/production-operations-readiness` expose `production-operations-readiness/v1`, the operations hardening read model above private-pilot acceptance. It derives local proof gates from the acceptance report, post-launch health receipt, operations incident drill, security audit stream, provider eval shadow replay, persistence adapter dry-run, worker queue adapter dry-run, and production launch audit. It separately derives production control gates from environment-backed centralized logs/metrics/traces, alert routing, on-call ownership, managed incident system, real restore-drill receipt, centralized audit retention, and managed database/queue cutover readiness. The model also includes `production-operations-managed-production-evidence/v1`, which rechecks the operations receipt rows against the same signed managed-production attestation rule used by `production-evidence-integrity-audit/v1`; local/test receipts can make operations controls ready, but they still show `managedProductionEvidence.readyForManagedProductionOperationsEvidence: false`. Manager Ready Package embeds the contract; Manager Flow Graph adds a monitoring node; Readiness Proof Map exposes `productionOperationsReadinessRoutes`; Security Boundary lists the GET route policy; Manager UI renders `backend-production-operations-readiness-snapshot`. The model can set `readyForPrivatePilotOperations: true` after customer acceptance while keeping the stricter managed-production evidence signal blocked until signed control-plane attestations exist.

Provider eval and the private-pilot release, launch, health, and acceptance POST routes now accept `includeReadModels: false`. In that mode they return the checksummed receipt, updated workflow, timeline/event proof, and explicit refresh routes for provider/launch read models, Manager Ready Package, Flow Graph, Proof Map, production operations readiness, and related launch read models instead of embedding the full Manager Ready Package in every write response. The acceptance Harness uses this contract and then performs authoritative standalone reads at the closeout points.

`getProductionOperationsControlReceiptWorkflow`, `recordProductionOperationsControlReceipt`, and `GET|POST /projects/:id/production-operations-control-receipts` expose `production-operations-control-receipt-workflow/v1` plus persisted `production-operations-control-receipt/v1` rows. The workflow lists the required production control ids, latest verified receipt per control, missing controls, proof ids, timeline/event ids, backend routes, and checksum. `POST` accepts security-admin or operations-owner evidence for centralized logs, metrics, traces, alert routing, on-call ownership, managed incident records, real restore drill, managed persistence cutover, managed worker queue cutover, and centralized audit retention; each control row stores status, evidence id/route/checksum, owner role, completion time, and redacted detail. Recording a receipt writes timeline/event proof, updates Production Operations Readiness, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionOperationsControlReceiptRoutes`, lists the route policy in Security Boundary, renders `backend-production-operations-control-receipts-snapshot`, and exports `production_operations_control_receipts` rows through persistence/migration/dry-run contracts. Passing all receipt controls sets `readyForProductionOperationsControls: true`; it only sets full `readyForProductionOperations: true` when the private-pilot operations proof is also ready, and the wider `production-launch-audit/v1` still decides whether the product can go public.

`getProductionSecurityControlReceiptWorkflow`, `recordProductionSecurityControlReceipt`, and `GET|POST /projects/:id/production-security-control-receipts` expose `production-security-control-receipt-workflow/v1` plus persisted `production-security-control-receipt/v1` rows. The workflow lists required managed security control ids, latest verified receipt per control, missing controls, proof ids, timeline/event ids, backend routes, and checksum. `POST` accepts security-admin evidence for managed identity provider, service identity boundary, managed KMS/Secret Manager, database-backed RBAC, centralized security audit, and session replay hardening; each control row stores status, evidence id/route/checksum, owner role, completion time, and redacted detail. Recording a receipt writes timeline/event proof, updates the Security Boundary production-security status, feeds Production Launch Gap Register and Production Launch Control Center inputs, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionSecurityControlReceiptRoutes`, lists the route policy in Security Boundary, renders `backend-production-security-control-receipts-snapshot`, and exports `production_security_control_receipts` rows through persistence/migration/dry-run contracts. Passing all receipt controls can set the security domain ready, but the wider launch audit/control center still owns the public production go/no-go.

### 5.9 Secret Redaction Boundary

`src/agents/secretRedaction.js` provides the current prototype redaction boundary for provider status, provider errors, evidence sources, Agent submissions, workspace artifact drafts, review comments, task source refs, and event-ledger payloads.

`src/agents/secretVault.js` provides the local BYOK secret-vault contract. It uses Web Crypto AES-GCM with PBKDF2-derived envelope keys to seal/open provider secret records in local/private pilots, and can rehearse local key rotation by decrypting with the current envelope key and resealing records under a new key id. Rotation emits `secret-vault-rotation-receipt/v1` with record counts, checksums, and `plaintextExposed: false`; readiness responses never return key material or plaintext secrets. `createLocalSecretVault` and `createSecretVaultFromEnv` expose `secret-vault-status/v1` with provider, key id, readiness, encrypted record count, raw secret record count, rotation support, and latest rotation receipt metadata only. `SECRET_VAULT_RECORDS_FILE` lets the local backend persist encrypted records and reload them on restart; `SECRET_VAULT_RECORDS_JSON` remains the static preload path. `agentProjectService` injects this status into Security Boundary and Provider Readiness, and provider status can show `apiKeySource: local-secret-vault` without exposing the key.

Settings provider/runtime readiness now enters the same formal proof surface as the other Settings backend contracts. Manager Ready Package embeds `settingsProviderReadiness` and `settingsRuntimeReadiness`, its summary exposes API-field editability, Seal availability, local runtime readiness, and production-blocked status, and Readiness Proof Map exposes `settingsProviderReadinessRoutes` plus `settingsRuntimeReadinessRoutes`.

The file-backed backend now exposes the same local vault boundary as API routes: `GET /secret-vault/status`, `GET /secret-vault/records`, `POST /secret-vault/seal`, and `POST /secret-vault/rotate`. The status route returns redacted readiness, the records route returns safe metadata without ciphertext or plaintext, the seal route returns `secret-vault-seal-receipt/v1`, and the rotate route returns `secret-vault-rotation-receipt/v1` plus refreshed safe status/record metadata. The React Settings Keys tab now uses the seal route for model key, search key, and search endpoint entry only when `/secret-vault/status` reports ready, clears plaintext after the backend receipt, and refreshes `/secret-vault/records` plus project-scoped `/projects/:id/provider-vault-bindings` when a project is active; sealing a provider key flips the local runtime intent from disabled to enabled, and sealing `search.endpoint` makes the local search provider target `http-json` with `endpointSource: local-secret-vault`. The API entry fields stay disabled before saved-backend plus Vault readiness, while the `Seal` buttons remain disabled until the backend can persist secrets. The Keys panel also exposes a `Backend URL` shortcut to the Deployment backend target editor, and the Settings browser gates verify the shortcut opens the active `agents:server` URL before provider credentials are sealed. Model calls can become callable immediately when no provider policy blocks them. Search becomes callable after a deterministic provider or vault-backed endpoint is configured, and `/search/test` proves the configured gateway path. Enforced access limits the route to `security-admin` and `runtime-platform` roles. If `SECRET_VAULT_ENABLED` or `SECRET_VAULT_KEY` is missing, the tab renders a backend-required boundary and disables seal buttons. Settings provider/runtime sync reads `/projects/:id/settings-provider-readiness` and `/projects/:id/settings-runtime-readiness` for an active project before falling back to global routes. Settings Privacy, Workspace, and Integrations are backend boundary views: global/project language, local privacy posture, provider budget, Agent tool-grant policy, workspace policy, local workspace binding, runtime contract rules, backend-derived meeting summaries, project memory readiness, and Integration readiness routes are the current local MVP project settings/read-model surfaces, while managed memory, production proxy/webhook delivery, managed vector adapters, centralized alerting, managed workspaces, and production observability remain production blockers. Settings project policy controls do not optimistic-write browser-local project settings for backend-online real projects: Privacy, provider budget, workspace policy, and Agent tool grants wait for the backend `project-settings/v1` receipt before updating the visible project snapshot or Settings draft, while offline/demo fallback can still mutate locally. Settings backend-backed controls also require the configured-project guard before they are clickable: privacy policy, workspace policy, workspace bind, memory readiness, meeting summaries, integration readiness, integration capability sync, provider budget, and Agent tool grants stay disabled and fail closed until the backend target is saved/injected and the active project is backend-writable. Local workspace binding is not a browser-local project setting: React calls `POST /projects/:id/workspace/bind` with `workspacePath` and optional `createIfMissing`, then displays the backend receipt and `GET /projects/:id/local-runtime` readback from `project.localRuntime.workspacePath`. `project-settings/v1` now includes `project-workspace-policy/v1` plus `project-workspace-capabilities/v1`, which records global interface language as browser-local; project language/interface density/default visibility/autosave cadence as backend-backed project settings; local workspace binding as a backend-backed editable local-runtime control through `/projects/:id/workspace/bind`; runtime contract rules as a backend-backed, non-editable `runtime-contract-freeze/v1` route through `GET /projects/:id/runtime-contracts`; long-term memory readiness as a backend-backed, non-editable `project-memory-readiness/v1` route through `GET /projects/:id/memory-readiness`; and meeting summaries as backend-backed through `GET /projects/:id/meeting-summaries`. `project-memory-readiness/v1` aggregates project state, transcript messages, meeting summaries, evidence/artifact index proof, and the managed persistence adapter plan without enabling user-editable managed memory; Manager Ready Package includes that model and Readiness Proof Map exposes `projectMemoryReadinessRoutes` so memory readiness is not a Settings-only indicator. `meeting-summaries/v1` is derived from backend transcript, timeline, and event-ledger evidence rather than browser-local meeting notes.

`npm run agents:server` is the runnable local backend path for this boundary. `scripts/agent-project-server.mjs` loads `.env*` files, sets a default local `SECRET_VAULT_RECORDS_FILE` under `.tmp/agent-secret-vault-records.json` when one is not provided, builds `createSecretVaultFromEnv`, opens any persisted model/search key records plus search endpoint records, and passes the hydrated providers into `createAgentProjectHttpServer`; `npm run agents:server:validate` starts that process on an ephemeral port with `SECRET_VAULT_ENABLED=true` and `SECRET_VAULT_KEY`, calls `/secret-vault/status`, seals a test `model.apiKey`, checks `/secret-vault/records`, verifies `/llm/status` now reports `hasApiKey: true`, `apiKeySource: local-secret-vault`, `runtimeEnabled: true`, and `enabled: true`, restarts the server against the same encrypted records file, verifies rehydration preserves callable model status, and removes the temporary store. `npm run agents:local-mvp-startup-readiness` verifies the new global startup route in both blocked and Vault-ready states, including route linkage, next action, redaction, and `readyForProduction: false`. `npm run agents:public-production-startup-readiness` verifies `public-production-startup-readiness/v1` through `GET /public-production-startup-readiness`, including enforced-access gates, `managed-identity-startup-readiness/v1`, env-only identity endpoint rejection without signed managed-production identity evidence, `production-cost-control-startup-readiness/v1`, env-only provider budget/audit/alert rejection without signed managed-production cost-control evidence, `production-data-governance-startup-readiness/v1`, env-only retention/deletion/export/residency rejection without signed managed-production data-governance evidence, `production-traffic-startup-readiness/v1`, env-only domain/traffic/health/approval/rollback rejection without signed managed-production traffic-control evidence, `managed-secret-manager-readiness/v1`, env-only Secret Manager/KMS rejection for the local vault, `managed-infrastructure-cutover-readiness/v1`, env-only database/queue rejection without managed-production cutover proof, `production-operations-startup-readiness/v1`, endpoint-only or unsigned observability/incident/restore/audit rejection without matching managed-production attestation signatures, provider redaction, adapter gateway attestation, Settings runtime, Manager Ready Package, Production Launch Control Center, Manager Flow Graph blocker node, Readiness Proof Map, and Manager UI linkage, and secret/env redaction while keeping `readyForPublicProduction: false` on local/private-MVP infrastructure. `npm run launch:infra` now includes that same public-production startup blocker contract before adapter gateway and Postgres-compatible storage rehearsals, so the P2 infrastructure command cannot pass while silently dropping the explicit public-production no-go check. `npm run launch:public-production:no-go` runs `scripts/validate-public-production-no-go.mjs`, reuses the infrastructure rehearsal, then reads the in-memory public startup readiness model and the redacted operator report to assert both still return public-production no-go. `npm run agents:settings-health-readiness` verifies Quick Check reads `settings-health-readiness/v1`, moves from Vault-required to provider-setup guidance, sees a sealed model provider without leaking plaintext, and still keeps provider tests and production readiness blocked until the full local provider setup is complete. `npm run agents:settings-runtime-readiness` verifies `settings-runtime-readiness/v1`, including global and project-scoped routes, model/search runtime rows, provider-vault redaction, local persistence/worker queue adapter rows, deployment preflight linkage, validation commands, and `readyForProduction: false` without launching a browser or calling providers. `npm run agents:search-provider:vault-endpoint` covers the parallel search path by sealing `search.endpoint` and `search.apiKey`, calling `/search/test` against a local mock gateway, verifying no plaintext/ciphertext appears in Vault record metadata, restarting, and confirming endpoint/key rehydration. These gates prevent the Settings UI from drifting into a state where it exposes provider inputs but the documented backend startup path cannot actually seal secrets, bind and enable the running provider, or survive a local restart.

`public-production-startup-readiness/v1` now embeds `managed-secret-manager-readiness/v1`, `managed-infrastructure-cutover-readiness/v1`, `production-traffic-startup-readiness/v1`, `production-operations-startup-readiness/v1`, `production-environment-setup/v1`, and `public-production-action-plan/v1`. The managed secret readiness object reports provider readiness, managed endpoint/key configuration, matched attestation id/checksum presence, raw-secret exposure, approved env var names, and missing groups without returning env values; the local AES-GCM vault remains blocked even when managed Secret Manager/KMS env names are present. The managed infrastructure cutover object reports database/queue driver, local-shadow status, managed configuration presence, real-adapter requirement, cutover proof, route, validation command, and missing env groups without returning connection strings or queue tokens; database URL and queue endpoint env names alone remain blocked without managed-production cutover receipts. The traffic startup object reports production domain/TLS, traffic gateway, health check, release approval, rollback smoke test, and traffic-control evidence separately from required managed-production evidence receipt ids/checksums/signatures; URL, health-check, approval, and rollback env names remain blocked until the HMAC signature matches `managed-production-control-attestation-signature/v1` for the `public-production-startup` scope. The operations startup object reports observability, audit retention, incident response, and restore-drill configuration separately from required managed-production evidence receipt ids/checksums/signatures; logs, metrics, traces, paging, incident, restore, and audit endpoints plus unsigned receipt ids/checksums remain blocked until the HMAC signature matches `managed-production-control-attestation-signature/v1` for the `public-production-startup` scope. The setup matrix is an operator-facing, redacted setup contract: each row groups a production domain such as access control, managed secrets, managed persistence, durable queue, provider runtime, adapter gateway attestation, production traffic, observability, or incident response; lists required env var names and any-of groups; reports which names are configured without returning values; points to the relevant route and validation command; and keeps `readyForPublicProduction=false` until the existing public startup gates pass. The action plan is generated by the backend from blocked setup rows; `report-public-production-readiness.mjs` consumes that backend plan and extends it with redacted managed environment preflight rows while preserving route, validation command, and required env-name context. Manager UI renders the same rows in `backend-production-environment-setup-matrix` and `backend-public-production-action-plan` from the backend package instead of deriving launch setup state in the browser.

`GET /access-control-policy` exposes the global `access-control-policy/v1` read model through `agentProjectService.getAccessControlPolicy()`. It documents the canonical signed request payload, signed identity headers, replay request-id contract, audit fail-closed behavior, project membership route, identity-session route, and related public-production/security audit routes. This route is intentionally read-only and never returns `AGENT_ACCESS_SIGNING_SECRET` or any configured secret value; it exists so deployment operators can wire clients and gateways to the same access contract that `npm run agents:production-access-control` validates.

`GET /managed-identity-policy` exposes the global `managed-identity-policy/v1` read model through `agentProjectService.getManagedIdentityPolicy()`. It is the operator contract for the second public-production blocker after signed access: provider metadata env names (`PRODUCTION_IDENTITY_PROVIDER`, `PRODUCTION_IDENTITY_ISSUER`, `PRODUCTION_IDENTITY_JWKS_URI`), service identity env names (`PRODUCTION_SERVICE_IDENTITY_AUDIENCE`, `PRODUCTION_SERVICE_IDENTITY_SUBJECT`), managed-production evidence env names, the attestation payload shape for `controlId=managed-identity-provider` and `domain=security`, related routes, and `npm run agents:production-managed-identity`. It reports configured and missing env var names only. It never returns issuer/JWKS URL values, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /managed-secret-manager-policy` exposes the global `managed-secret-manager-policy/v1` read model through `agentProjectService.getManagedSecretManagerPolicy()`. It is the operator contract for the managed Secret Manager/KMS blocker after managed identity: managed provider capability requirements, endpoint/key env groups, attestation id/checksum pairs, readiness summary, related routes, and `npm run agents:production-managed-secrets`. It reports env var names and readiness booleans only. It never returns Secret Manager endpoints, KMS key ids, attestation ids/checksums, raw secret records, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /managed-persistence-policy` exposes the global `managed-persistence-policy/v1` read model through `agentProjectService.getManagedPersistencePolicy()`. It is the operator contract for the managed database persistence blocker after managed secrets: adapter env names, database/gateway env groups, signed cutover evidence env names, readiness summary, the attestation payload shape for `controlId=managed-persistence-cutover` and `domain=operations`, related routes, and `npm run agents:production-managed-persistence`. It reports env var names, driver/source labels, and readiness booleans only. It never returns database URLs, endpoint values, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /managed-worker-queue-policy` exposes the global `managed-worker-queue-policy/v1` read model through `agentProjectService.getManagedWorkerQueuePolicy()`. It is the operator contract for the durable queue/scheduler blocker after managed persistence: queue adapter env names, queue/gateway env groups, scheduler lease/retry/dead-letter capability requirements, signed cutover evidence env names, readiness summary, the attestation payload shape for `controlId=managed-worker-queue-cutover` and `domain=operations`, related routes, and `npm run agents:production-managed-worker-queue`. It reports env var names, driver/source labels, local-MVP retry constants, and readiness booleans only. It never returns queue endpoints, gateway endpoints, tokens, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /production-provider-controls-policy` exposes the global `production-provider-controls-policy/v1` read model through `agentProjectService.getProductionProviderControlsPolicy()`. It is the operator contract for the production provider/BYOK cost-control blocker after managed queue: production budget/rate-limit env names, usage audit env groups, cost alert env groups, signed cost-control evidence env names, readiness summary, the attestation payload shape for `controlId=production-cost-controls` and `domain=provider`, related routes, and `npm run agents:production-provider-controls`. It reports env var names and readiness booleans only. It never returns provider audit endpoints, cost ledger endpoints, alert routing endpoints, tokens, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /production-data-governance-policy` exposes the global `production-data-governance-policy/v1` read model through `agentProjectService.getProductionDataGovernancePolicy()`. It is the operator contract for the production data governance blocker after provider controls: retention/residency env names, deletion job env names, export storage env names, signed data-governance evidence env names, readiness summary, the attestation payload shape for `controlId=production-data-governance` and `domain=data-governance`, related routes, and `npm run agents:production-data-governance`. It reports env var names and readiness booleans only. It never returns deletion/export endpoints, tokens, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /production-traffic-policy` exposes the global `production-traffic-policy/v1` read model through `agentProjectService.getProductionTrafficPolicy()`. It is the operator contract for the production traffic/rollback blocker after data governance: production domain/TLS env names, traffic gateway and health check env names, release approval env names, rollback runbook/smoke-test env names, signed traffic-control evidence env names, readiness summary, the attestation payload shape for `controlId=production-traffic-control` and `domain=deployment`, related routes, and `npm run agents:production-traffic-controls`. It reports env var names and readiness booleans only. It never returns domain values, gateway URLs, health URLs, rollback URLs, routing tokens, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /production-customer-acceptance-policy` exposes the global `production-customer-acceptance-policy/v1` read model through `agentProjectService.getProductionCustomerAcceptancePolicy()`. It is the operator contract for the customer production acceptance blocker after traffic cutover: customer acceptance policy env names, success criteria env names, threshold env names, approval env names, rollback criteria env names, signed customer-acceptance evidence env names, readiness summary, the attestation payload shape for `controlId=customer-production-acceptance-policy` and `domain=customer-acceptance`, related routes, and `npm run agents:production-customer-acceptance`. It reports env var names and readiness booleans only. It never returns policy values, threshold values, approval values, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`GET /production-operations-policy` exposes the global `production-operations-policy/v1` read model through `agentProjectService.getProductionOperationsPolicy()`. It is the operator contract for the production operations blocker after traffic/customer acceptance: observability env groups, alert routing env groups, on-call and incident env names, restore-drill env names, centralized audit-retention env names, signed operations evidence env names, readiness summary, related routes, and `npm run agents:production-operations-startup`. It reports env var names and readiness booleans only. It never returns observability endpoints, alert routing keys, incident endpoints, audit sinks, receipt ids/checksums, signatures, `AGENT_ACCESS_SIGNING_SECRET`, or `MANAGED_PRODUCTION_ATTESTATION_SIGNING_SECRET`.

`public-production-startup-readiness/v1` also embeds `production-customer-acceptance-startup-readiness/v1`. The customer acceptance startup object reports policy, success criteria, threshold, approver role, approval id, rollback criteria, signed receipt evidence, attestation signature status, missing env var names, route, validation command, and checksum. The gate id is `customer-production-acceptance-policy`, and it remains blocked unless the receipt id/checksum/signature matches the same managed-production attestation contract for domain `customer-acceptance`; private-pilot acceptance reports and env-only customer policy configuration cannot promote public traffic.

`npm run ui:settings-agents-server` extends that proof into the customer-facing UI. It builds `dist/`, starts `scripts/agent-project-server.mjs` with a real local Secret Vault, seeds a backend project through `POST /projects/initiate`, syncs the project catalog, opens that project, and then verifies Settings against project-scoped backend routes. The browser gate checks Keys can type provider values, shows the local Secret Vault startup contract with the PowerShell `agents:server` command, seals model key plus search endpoint/key through `/secret-vault/seal`, Health exposes `/settings/health-readiness`, `/local-mvp-startup-readiness`, and `/projects`, Deployment and Models read `settings-runtime-readiness/v1`, Integrations reads `/projects/:id/settings-integration-readiness` plus its evidence/provider/adapter/budget/error-reporting routes, Workspace reads `/projects/:id/memory-readiness` plus `/projects/:id/meeting-summaries`, and the same `agents:server` vault exposes encrypted metadata only. It then edits Workspace interface/default-visibility/autosave controls, Privacy retention/provider-log/export-approval controls, Integrations budget/tool-grant controls, polls `GET /projects/:id/project-settings` for backend readback, verifies the saved budget feeds `GET /projects/:id/provider-controlled-run`, and checks timeline plus event-ledger `project-settings-updated` proof. It also checks `/llm/status`, `/search/status`, and `/search/test` against a local mock gateway. It deliberately avoids failure screenshots so the gate stays low-write while still catching Settings/backend integration regressions.

The runtime binding is intentionally narrow. `modelProvider.js` and `searchProvider.js` expose `setApiKey(...)`, and `searchProvider.js` also exposes `setEndpoint(...)` for the `http-json` gateway target. `agentProjectService.sealSecretVaultRecord` calls these only when the sealed record is recognized as `model.apiKey`, `search.apiKey`, or `search.endpoint` by name, scope, and secret target metadata. This makes the current backend process usable immediately after Settings seal while keeping persisted storage limited to the encrypted vault record. Local restart rehydration is supported by the encrypted records file plus the same `SECRET_VAULT_KEY`; production still requires managed Secret Manager/KMS, revocation, audit, and formal rehydration policy before public rollout.

`GET /provider-vault-bindings` and `GET /projects/:id/provider-vault-bindings` expose the bridge between provider runtime status and the local vault. The response is `provider-vault-bindings/v1`: it lists model/search binding rows, search endpoint binding fields, safe vault record metadata, redaction scan status, backend proof routes, and a checksum. Provider Readiness embeds this model so a Manager can see whether model/search providers are actually backed by local vault records instead of only seeing `hasApiKey: true`. The route never returns API keys, master keys, plaintext, or ciphertext; public production still requires managed KMS/Secret Manager binding receipts, revocation, scoped provider credentials, and access audit.

Agent provider calls now carry this bridge into their own evidence. Model-backed artifact drafts, runtime model-intent calls, provider-backed evidence searches, worker provider-evidence cycles, `provider_usage_ledger` rows, and `evidence-provider-receipt/v1` receipts include `provider-vault-binding-proof/v1` or its checksum/route. This lets Manager Flow Graph, Proof Map, operations recovery, and later Settings surfaces trace a concrete Agent action back to the provider/vault boundary that authorized it, without storing raw keys in the project record.

This is not a replacement for encrypted secret storage or scoped credentials. It is a minimum persistence guard: obvious API keys, bearer tokens, secret-bearing URL query params, and secret-looking submission text are redacted before records are written to the JSON store, workspace artifact files, group-chat proof, timeline proof, or ledger payloads.

Production deployments still need managed KMS/Secret Manager storage, real rotation/revocation workflows, access control, audit policy, and provider-specific request isolation. Until those exist, BYOK remains private-MVP-grade rather than public-production-grade.

### 5.10 Access Control Boundary

`src/agents/accessControl.js` provides a prototype access decision layer for the backend API. Local demos remain backward-compatible in `prototype-open` mode. When a request sets `x-hofs-access-mode: enforced`, the API evaluates `x-hofs-role`, `x-hofs-agent-id`, and `x-hofs-user-id` before dispatching the route. If `AGENT_ACCESS_SIGNING_SECRET` is configured, enforced requests must also include `x-hofs-signed-at` and `x-hofs-signature`; the backend verifies an HMAC-SHA256 payload covering method, route path, mode, role, Agent id, user id, timestamp, and optional request id before role policy is evaluated. If an API instance sets `requireSignedRequestIds` or the server sets `AGENT_ACCESS_REPLAY_PROTECTION=true`, signed enforced requests must include `x-hofs-request-id`, and the API rejects duplicate request ids from the same actor within the signature freshness window. The file-backed Node backend stores these accepted request ids in `accessReplayRecords`, so a reused signed request id is still rejected after a local backend restart; custom in-memory prototypes fall back to an API memory cache. If an API instance sets `failClosedOnAuditError` or the server sets `AGENT_ACCESS_AUDIT_FAIL_CLOSED=true`, enforced project access returns `503 access-audit-write-failed` instead of dispatching the route whenever the access audit sink is missing or throws. If an API instance sets `requireProjectMembership`, project-scoped enforced requests then pass through a `project-membership-policy/v1` check for manager/security/observer/runtime user ids, project Agent ids, Reviewer Agent ids, optional Agent runtime user bindings, and revoked user/Agent lists. The local backend exposes `GET|PUT /projects/:id/membership-policy`, persists the active policy plus revision audit in project state, and exports normalized `project_membership_policies` / `project_membership_grants` persistence rows.

`GET|POST /projects/:id/identity-sessions` adds the local runtime credential bridge used by the MVP Harness. `issueIdentitySession` creates an `identity-session/v1` row, returns the bearer token once, stores only `tokenHash`, checksum, role/user/Agent binding, expiry, scope, and revocation metadata in project state, then writes timeline and event-ledger proof. `x-hofs-session-token` can be supplied on project routes; `createAgentProjectApi` verifies the token hash, converts it into enforced actor headers, still runs project membership policy checks, and records the verified session id/status on `securityAccessAudit` plus the backend audit stream. `revokeIdentitySession` marks a session revoked and future requests with that token fail closed before dispatch. The persistence snapshot exports `identity_sessions` rows, migration seed order includes the table, and Security Boundary / Manager Ready Package summarize active, revoked, and expired sessions. This is still a local/private-MVP credential contract, not a production identity provider or managed service-account system.

Security boundary writes now use the same receipt-first command contract as Agent and launch writes. `PUT /projects/:id/membership-policy`, `POST /projects/:id/identity-sessions`, and `POST /projects/:id/identity-sessions/:sessionId/revoke` honor `includeReadModels: false`; in that mode they return the policy/session/revocation receipt plus explicit refresh routes for Security Boundary, Security Access Audit, Security Audit Stream, membership policy, identity sessions, provider readiness, and Manager read models instead of embedding full Manager Ready Package or Security Boundary snapshots.

Current enforced-mode coverage:

- Manager and security-admin project control/read routes
- Agent self-scoped dashboard/work/submission/evidence routes
- Reviewer Agent review writes with reviewer identity matching
- runtime-platform worker and queue routes
- observer read-only routes without sensitive export permission
- security-admin-only persistence snapshot export
- optional signed access-header verification for enforced-mode identity claims, including unsigned/tampered request rejection and signed HTTP scheduler worker calls
- optional signed request replay protection with `x-hofs-request-id`, file-backed local replay records, duplicate request denial across backend restart, memory fallback for custom prototypes, and signed scheduler request ids
- optional audit fail-closed behavior for enforced project routes, returning `503 access-audit-write-failed` when a required audit record cannot be persisted before dispatch
- optional project membership policy verification for project-scoped routes, including persisted policy reads/writes, signed-but-nonmember manager denial, Agent runtime binding denial, non-Reviewer review denial, revoked runtime denial, policy revision audit, and membership policy/grant persistence rows
- local identity-session verification through `x-hofs-session-token`, including one-time token return, token-hash-only persistence, Manager/security-admin issue/list/revoke routes, verified-session access audit rows, revoked-token fail-closed behavior, `identity_sessions` persistence rows, and migration seed coverage
- persisted access-decision audit rows in project state, `GET /projects/:id/security-access-audit`, `GET /projects/:id/security-audit-stream`, `security-access` event-ledger events, store-level audit-stream records with sequence/checksum and tamper-evident hash-chain proof, an append-only JSONL audit sink (`AGENT_SECURITY_AUDIT_LOG`, defaulting to `${AGENT_PROJECT_STORE}.security-audit.jsonl`), and the `security_access_audit` plus `security_audit_stream` persistence tables

This is a backend contract, not a production identity provider. The HMAC header reduces role-header spoofing in local/private deployments once a signing secret exists, file-backed replay protection reduces short-window duplicate request reuse across local backend restarts, fail-closed audit writes stop audited access from proceeding silently when the audit sink fails, the persisted project membership policy reduces project-boundary mistakes in private MVP pilots, and local identity sessions make runtime credential use auditable without storing raw tokens. Production still needs first-party user sessions, service identity issuance, rotation/revocation, durable shared replay storage for multi-instance deployments, managed project membership tables, invitation/revocation flows, database row-level authorization, centralized immutable security audit logs, alerting, and recovery playbooks.

## 6. Backend Harness and Reliability

The current Harness verifies that the architecture is not only documented.

Low-write manager scenario contract:

```bash
npm run agents:scenario:contract
```

This aggregates the Manager chat command, Agent-to-Agent message, marketplace Agent contract, Agent Workbench, and timeline-action contracts. Use it first when checking the core manager scenario path under a low disk-write budget.

Long regression command:

```bash
npm run agents:scenario
```

It validates:

- durable kickoff meeting session create/read/clarify/approve
- Leader and Reviewer governance
- role clarification and Leader campaign transcript evidence
- group-chat assignment and acknowledgement
- Agent-to-Agent messaging
- peer handoff
- meeting and Google Chat change requests
- autonomous project cycle
- independent per-Agent worker cycle
- due-worker processing and skipping
- file-backed persistence and restart
- HTTP server request path
- scheduler status/tick/start/stop
- manager dashboard, flow graph, proof map, transcripts, task evidence, and Agent dashboard reads

Product-team acceptance command:

```bash
npm run agents:product-team:core
npm run agents:product-team:core:file-backed
npm run agents:product-team:research-sample
npm run agents:product-team:local-mvp
npm run agents:mission-runner:startup
npm run agents:mvp-readiness:operator-actions
npm run agents:product-team:cycle-consistency
npm run agents:product-team:private-pilot:release
npm run agents:product-team:private-pilot:launch
npm run agents:product-team:private-pilot:health
npm run agents:product-team:private-pilot:acceptance
npm run agents:product-team:private-pilot:focused
npm run agents:product-team:private-pilot:ops-readiness
npm run agents:product-team:production-ops-controls
npm run agents:product-team:production-deployment-controls
npm run agents:product-team:production-security-controls
npm run agents:product-team:production-provider-controls
npm run agents:product-team:production-evidence-integrity
npm run agents:product-team:production-launch-governance
npm run agents:product-team:private-pilot
npm run agents:product-team
```

It uses Research Project as a validation sample for the general product-team system, then verifies the generic submission chain. Use `npm run agents:product-team:core` for the stable low-write local MVP product-team proof, `npm run agents:product-team:research-sample` when the question is specifically whether the Research sample still proves the generic product-team chain instead of drifting into paper/thesis/manuscript protocol, `npm run agents:mission-runner:startup` when the question is whether the first user mission can start through Product Team Mission Runner and hand off to A-side autonomy through the real Node HTTP server, `npm run agents:mvp-readiness:operator-actions` when the question is whether a Manager readiness action can become runnable A-side Agent output through the real Node HTTP server, `npm run agents:product-team:core:file-backed` when broader file-backed persistence/security/provider/worker coverage is required, and `npm run agents:product-team:cycle-consistency` when the question is whether signed C/A autonomous cycles remain receipt-, Flow-, Proof-, Ready Package-, and persistence-consistent. Use `npm run agents:product-team:private-pilot:focused` for the lowest-write P1 backend confidence check across handoff, release candidate, controlled launch, health, and customer acceptance without browser/build cost. Use the staged private-pilot gates when you need the shortest proof for a specific customer-visible launch phase: `:release` freezes the release candidate, `:launch` records the controlled launch run, `:health` records post-launch health, `:acceptance` records customer acceptance, and `:ops-readiness` verifies the production-operations bridge after acceptance without recording production-control receipts. Use `npm run agents:product-team:production-ops-controls`, `npm run agents:product-team:production-deployment-controls`, `npm run agents:product-team:production-security-controls`, `npm run agents:product-team:production-provider-controls`, `npm run agents:product-team:production-evidence-integrity`, and `npm run agents:product-team:production-launch-governance` for the shortest P2 receipt, evidence, and launch-governance projection checks before the full hardening rehearsal; the operations/deployment/security/provider receipt gates, evidence-integrity gate, and launch-governance gate are focused backend contract validators, not full private-pilot replays. The evidence-integrity gate must keep missing-signature and invalid-signature managed-production claims as `external-unattested` until the HMAC control-plane attestation matches the backend verification payload. `npm run agents:product-team:private-pilot` is the customer-acceptance alias, and `npm run agents:product-team` is only for the full production-hardening rehearsal.

Launch tier gate command:

```bash
npm run launch:gates
npm run agents:production-access-control
npm run agents:production-managed-identity
npm run agents:production-managed-secrets
npm run agents:production-managed-persistence
npm run agents:production-managed-worker-queue
npm run agents:production-provider-controls
npm run agents:production-data-governance
npm run agents:production-traffic-controls
npm run agents:production-customer-acceptance
npm run agents:production-operations-startup
npm run launch:infra
npm run launch:public-production:no-go
```

`docs/LAUNCH_READINESS_GATES.md` defines the operational gate matrix for P0 local backend MVP, P1 customer private pilot, and P2 public production. The validator checks that the matrix stays tied to package scripts, generic product-team evidence contracts, and explicit production blocker domains instead of becoming a stale planning note. `npm run agents:production-access-control` is the focused P2 access-control gate: it proves missing enforced mode/signing/replay/audit fail-closed config surfaces as `setup-access-control`, proves strict access config clears only that setup action, exercises the backend API for signed request enforcement, project membership, replay rejection, Agent self-scope, observer write denial, persisted security audit rows, hash-chain audit stream readiness, fail-closed audit-sink behavior, and verifies `GET /access-control-policy` exposes the operator contract without leaking signing secrets. `npm run agents:production-managed-identity` is the focused P2 managed identity gate: it starts from strict access-control env, proves `setup-managed-identity` is the next action, rejects env-only identity provider/service identity configuration or invalid signatures, verifies redaction, accepts only matching signed managed-production identity evidence while keeping public production blocked, and verifies `GET /managed-identity-policy` exposes the operator identity contract without leaking identity URLs, checksums, signatures, or signing secrets. `npm run agents:production-managed-secrets` is the focused P2 managed Secret Manager/KMS gate: it starts after access and identity are ready, proves `setup-managed-secrets` is the next action, rejects env-only/local-vault setup, rejects raw secret exposure, accepts only a managed provider with rotation/access audit, endpoint/key configuration, and matched attestation id/checksum evidence, and verifies `GET /managed-secret-manager-policy` exposes the operator secret/KMS contract without leaking endpoints, key ids, attestations, checksums, raw secrets, or signing secrets. `npm run agents:production-managed-persistence` is the focused P2 managed database persistence gate: it starts after access, identity, and secrets are ready, proves `setup-managed-persistence` is the next action, rejects database URL env-only setup, rejects invalid cutover signatures, verifies redaction, accepts only real adapter configuration plus signed managed-production cutover evidence, and verifies `GET /managed-persistence-policy` exposes the operator persistence contract without leaking database URLs, endpoint tokens, checksums, signatures, or signing secrets. `npm run agents:production-managed-worker-queue` is the focused P2 managed durable queue gate: it starts after persistence is ready, proves `setup-managed-worker-queue` is the next action, rejects queue endpoint env-only setup, rejects invalid cutover signatures, verifies redaction, accepts only real adapter configuration plus signed managed-production queue cutover evidence, and verifies `GET /managed-worker-queue-policy` exposes the operator queue/scheduler contract without leaking queue tokens, checksums, signatures, or signing secrets. `npm run agents:production-provider-controls` is the focused P2 provider/BYOK startup gate: it starts after provider runtime and managed infrastructure are ready, proves `setup-production-cost-controls` is the next action, rejects missing production budget/rate policy, usage audit, cost alert routing, or signed evidence, verifies endpoint redaction, accepts only signed managed-production cost-control evidence, and verifies `GET /production-provider-controls-policy` exposes the operator provider controls contract without leaking audit endpoint tokens, alert routing tokens, checksums, signatures, or signing secrets. `npm run agents:production-data-governance` is the focused P2 data governance startup gate: it starts after provider controls are ready, proves `setup-production-data-governance` is the next action, rejects missing retention/residency, deletion job, export storage, or signed evidence, verifies endpoint redaction, accepts only signed managed-production data-governance evidence, and verifies `GET /production-data-governance-policy` exposes the operator data governance contract without leaking deletion/export tokens, checksums, signatures, or signing secrets. `npm run agents:production-traffic-controls` is the focused P2 traffic/rollback startup gate: it starts after data governance is ready, proves `setup-production-traffic` is the next action, rejects missing production domain/TLS, traffic gateway, health check, release approval, rollback smoke test, or signed evidence, verifies endpoint redaction, accepts only signed managed-production traffic-control evidence, and verifies `GET /production-traffic-policy` exposes the operator traffic/rollback contract without leaking traffic/health/rollback tokens, checksums, signatures, or signing secrets. `npm run agents:production-operations-startup` is the focused P2 operations startup gate: it starts after customer production acceptance is ready, proves `setup-observability` and `setup-incident-response` are the remaining actions, rejects endpoint/env-only observability, alerting, incident, restore, or audit setup without signed evidence, verifies endpoint redaction, accepts only signed managed-production operations evidence, and verifies `GET /production-operations-policy` exposes the operator operations contract without leaking observability, alert, incident, audit, checksum, signature, or signing-secret values. `npm run launch:infra` runs the P2 infrastructure rehearsal by chaining the gate validator, public-production startup readiness blocker contract, focused production access-control contract, focused managed identity startup contract, focused managed Secret Manager/KMS startup contract, focused managed database persistence startup contract, focused managed durable queue startup contract, focused production provider/BYOK controls startup contract, focused production data governance startup contract, focused production traffic/rollback startup contract, focused production operations startup contract, public-production operator report validation, managed environment preflight validation, shared adapter gateway contract check, reference private gateway server check, Agent HTTP server dry-runs through an env-configured gateway endpoint, Postgres-compatible storage boundary check, and project-level managed infrastructure cutover attestation bridge; it is necessary evidence for managed persistence / queue readiness work, but it still keeps public production blocked until every required managed infrastructure receipt and launch approval exists. `npm run launch:public-production:no-go` is the operator-facing decision command: it runs the infrastructure rehearsal and then asserts `public-production-startup-readiness/v1` plus `public-production-readiness-operator-report/v1` still return blocked/no-go. `npm run agents:public-production-readiness-report` is a read-only CLI adapter over `public-production-startup-readiness/v1`; it emits `public-production-readiness-operator-report/v1` with blocked setup rows, backend routes, validation commands, next action, required env var names, an embedded redacted `managed-environment-preflight-report/v1` summary, and `public-production-action-plan/v1` while omitting configured values. `npm run agents:public-production-readiness-report:validate` protects that report from env-value leakage and from treating partial/env-only setup as production-ready or managed-environment-ready. `npm run agents:managed-environment-preflight` emits `managed-environment-preflight-report/v1`, classifying the configured adapter gateway, managed persistence adapter, durable queue adapter, and attestation signing boundary as missing/local/external without printing values. It does not call the network by default; `node scripts/report-managed-environment-preflight.mjs --check-network` probes the configured external gateway `/health` and `/state` and still reports only schema/capability/storage-driver summaries.

`GET /projects/:id/production-infrastructure-rehearsal` exposes the same readiness boundary inside the product as `production-infrastructure-rehearsal/v1`. It aggregates adapter gateway preflight, managed persistence plan/dry-run, worker queue adapter plan/dry-run, deployment preflight, production deployment control receipts, operations readiness, production operations readiness, and the production launch gap register into one Manager-readable backend contract. The model can mark local/private infrastructure rehearsal visible or ready, but it keeps `readyForProduction` false until those domains carry real managed-production cutover evidence. The contract now includes `managedCutoverGates` plus `managedCutoverSummary`, which normalize the gateway, managed persistence, managed queue/cron, deployment, operations/recovery, and gap-closure cutover gates into production-ready counts, receipt-ready counts, blocked gate ids, and the next cutover gate. Verified `production-operations-control-receipt/v1` rows for `managed-persistence-cutover` and `managed-worker-queue-cutover` now project into the matching infrastructure domain rows and managed cutover gates as `cutoverReceiptReady`, so database/queue blockers can clear from the infrastructure rehearsal without closing unrelated launch gates. Verified `production-deployment-control-receipt/v1` rows also project into the `deployment-preflight` domain row and `deployment-cutover` gate as `deploymentReceiptReady`, bind the deployment workflow checksum into upstream evidence, and clear that infrastructure blocker while the broader launch audit/control center remains no-go. Readiness Proof Map also exposes `productionInfrastructureRehearsalRoutes` / `productionInfrastructureRehearsalSummary`, and Manager Flow Graph emits a `production-infrastructure-rehearsal` node with upstream infrastructure routes so the Manager can trace local rehearsal proof without confusing it for public-production readiness.

`POST /projects/:id/managed-infrastructure-cutover-attestations` is the write-side bridge for that boundary. The service calls the configured private adapter gateway, requires `managedProductionAttestation.ready` plus project-specific persistence and worker-queue dry-run receipt evidence in the gateway state, requests signed control-plane attestations for `managed-persistence-cutover` and `managed-worker-queue-cutover`, records them through `production-operations-control-receipt/v1`, and returns `managed-infrastructure-cutover-attestation-run/v1`. Missing gateway configuration, blocked gateway health/state, missing dry-run receipt evidence, or attestation unreadiness returns a fail-closed run and does not write production operations evidence. The route is listed in Security Boundary/access policy for runtime-platform, security-admin, and operations-owner use.

The React Manager Ready Package consumes the same contract as executable C-side controls. Its release, launch, health, and acceptance buttons are gate-disabled until the backend workflow reports readiness, then POST to the matching receipt route with `includeReadModels: false` and refresh Manager Dashboard, Ready Package, Flow Graph, Timeline/Event, and launch submodels from backend reads. The browser Harness asserts these controls exist and remain disabled on incomplete real backend projects.

- kickoff meeting
- role clarification and Leader campaign
- evidence search record
- evidence quality judgement and source quality signals
- discovery-report submission
- brainstorm-board submission
- evidence-packet submission
- product-brief submission
- decision-proposal submission
- formal changes-requested submission review
- risk-review submission
- revision-note submission
- implementation-plan submission
- revision lineage from requested-changes review to revision-note and final-deliverable
- final-deliverable submission
- formal accepted submission review
- Manager Dashboard submission, evidence-search, and review summaries
- Manager Flow Graph submission, evidence, and review nodes
- Readiness Proof Map submission, evidence-search, and review routes
- Task Evidence submission, evidence-search, and review links
- Agent Dashboard owned submissions, evidence searches, and reviews
- Group Chat transcript submission, evidence-search, and review messages
- Event Ledger `agent-submission`, `evidence-search`, and `submission-review` events
- Search provider status/test path without exposing secrets
- Secret fixture injection and persistence scanning across the file store and workspace artifact files
- Manager Dashboard / Manager Flow Graph / Readiness Proof Map revision lineage, including superseded submissions and resolved review obligations
- real HTTP backend scheduler tick for the same acceptance project, including scheduler status, project/Agent due-worker processing, Manager Dashboard operations evidence, Manager Ready Package operations evidence, and Manager Flow Graph runtime monitoring proof
- worker API response stability: `/workers/autonomous/due` and `/workers/agents/due` reuse per-project Manager Dashboard / Manager Ready Package read models inside one response, and the local HTTP server drains idle/keep-alive sockets during shutdown so validation and desktop backend restarts do not stall on retained fetch connections
- service-instance read-model caching: `agentProjectService` caches Manager Dashboard, Manager Flow Graph, Manager Ready Package, MVP/pilot/deployment/production-launch readiness, security/provider/operations readiness, persistence adapter dry-runs, and worker queue adapter dry-runs for the same project state, then clears the cache on project/message/meeting writes. This keeps heavy proof packages repeatable enough for long manager and product-team Harness runs without changing the API contracts.
- MVP readiness gate through `GET /projects/:id/mvp-readiness` and Manager Ready Package, separating local-pilot readiness from production blockers such as secret vault/RBAC, managed persistence, queue/cron infrastructure, real provider rollout, and observability/recovery. The read model also exposes `operatorActions` and a routed `nextShortestPath` with scope, owner, method, `apiPath`, `runApiPath`, status, and production-blocker flags; React renders those actions in the Manager Ready Package rather than deriving launch guidance locally. Core-gap actions can include `autopilot-delivery-target-control/v1` with target stage, target step, preferred lane, artifact/review controls, checksum, and `submitWorkArtifactOn: "always"` when an artifact must be created. The `draft-review-revision` and `final-deliverable-accepted` gaps are decomposed by current proof state into draft product brief, requested-changes review, linked revision response, final deliverable submission, and final acceptance targets. `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run` persists `mvp-readiness-operator-action-run/v1` receipts with checksum, timeline log, event ledger entry, Manager Flow Graph node, and Readiness Proof Map route; runnable core-gap receipts are projected into Collaboration Intent Queue and `Autonomous Run Control` as `run-mvp-readiness-target`, and the queue run endpoint can execute those receipts into Agent Autonomous Queue through Autonomous Run Control while returning the resulting Agent submission/review/evidence proof. Stale target steps are dropped after each gap step closes and production-hardening receipts remain non-autonomous.
- private pilot launch package through `GET /projects/:id/pilot-launch-readiness` and Manager Ready Package, returning `pilot-launch-readiness/v1` with private-pilot go/no-go, production no-go, evidence routes, failed gates, production blockers, and a launch packet checksum aggregated from the backend readiness contracts
- deployment preflight through `GET /projects/:id/deployment-preflight` and Manager Ready Package, returning `deployment-preflight/v1` with backend store/audit sink, scheduler, access-control hardening, signed/replay/audit-fail-closed flags, secret-vault readiness, provider policy, adapter status, adapter gateway preflight, operations readiness, blocker gates, warnings, production controls, and a checksum
- production launch audit through `GET /projects/:id/production-launch-audit` and Manager Ready Package, returning `production-launch-audit/v1` with private-pilot and production decisions, private-pilot gates, private-pilot handoff gates, production gates, audit-integrity gates, evidence routes, `project-evidence-handoff-summary/v1`, production blockers, next-shortest-path guidance, and a checksum while keeping production `no-go` until real managed controls exist. When core private-pilot gates pass but `project-evidence-export-package/v1` is not download-audited, `nextShortestPath.scope` remains `private-pilot-handoff`; once the package is ready, it shifts to `production-hardening`
- project evidence archive through `GET /projects/:id/project-evidence-archive` and Manager Ready Package, returning full standalone `project-evidence-archive/v1` redacted contents plus a Manager Ready Package manifest-only snapshot with the same status, route, gates, counts, and checksums. The full archive covers manifest checksums, integrity gates, transcript channels, Agent submissions, final deliverables, evidence searches, submission reviews, revision lineage, Flow Graph nodes, Proof Map routes, timeline logs, event ledger rows, readiness summaries, persistence summary, and worker recovery summary. Its `artifact-storage-proofs` manifest row and `artifact-storage-proof-coverage` integrity gate must prove storage/workspace proof coverage for every Agent submission. Its `transcript-proof-coverage/v1` block and `transcript-proof-coverage` integrity gate must prove Agent submission, evidence search, evidence source review, submission review, and provider-backed Autopilot coordination message ids are present in backend transcripts or archived proof, so C-side chat proof exits cannot pass from browser-only history. The same expected/archived/missing transcript coverage is mirrored into Readiness Proof Map as `transcriptProofCoverageRoutes` / `transcriptProofCoverageSummary` for daily Manager monitoring before archive handoff. Proof Map transcript proof coverage cards fail closed with source badges and `Sync Proof Map` actions when the summary exists but the route array is absent. It is a private-pilot/customer handoff proof contract, not a production export/download system until the local approval contract is paired with encrypted storage, retention enforcement, download audit storage, and data residency controls
- evidence quality audit through `GET /projects/:id/evidence-quality-audit` and Manager Ready Package, returning `evidence-quality-audit/v1` with evidence rows, source rows, source quality, source safety, provider provenance, Proof Map routes, decision gates, required production controls, and checksum. The archive manifest and production launch audit evidence routes now include this route so evidence readiness is verifiable without reading every search row
- submission review workflow through `GET /projects/:id/submission-review-workflow` and Manager Ready Package, returning `submission-review-workflow/v1` with generic review rounds, requested changes, linked revision responses, accepted final deliverable proof, proof routes, local closure gates, production controls, and checksum. The product-team Harness verifies standalone API, real HTTP, Manager Ready Package summary, Manager Flow Graph closure node, Readiness Proof Map route, Security Boundary policy, enforced security-admin read, and Manager UI snapshot while keeping production review governance blocked
- product-team delivery trace through `GET /projects/:id/product-team-delivery-trace` and Manager Ready Package, returning `product-team-delivery-trace/v1` with a generic end-to-end delivery chain: kickoff meeting, Agent self-marketing, brainstorm layer, evidence quality, generated draft, review/revision, accepted final deliverable, and proof surfaces. The trace is not research-specific; it gives the C-side Manager one backend-backed view of whether the A-side Agent team has moved from intent to accepted deliverable. Manager Flow Graph adds a `product-team-delivery-trace` aggregate node connected to brainstorm, evidence/search, submission, review/revision, and final-deliverable nodes. Readiness Proof Map exposes `productTeamDeliveryTraceRoutes`; it now also exposes `productTeamAcceptanceChainRoutes` / `productTeamAcceptanceChainSummary`, which flatten the same generic stages into one C/A acceptance route with stage rows, proof ids, timeline ids, event ids, `readyForGenericProductTeamAcceptance`, `readyForBsideProductTeamRun`, and explicit production blockers. Security Boundary lists the route, Manager UI renders `backend-product-team-delivery-trace-snapshot`, Manager Proof Map renders `proof-map-product-team-acceptance-chain`, and backend-online real projects render a `backend-model-missing` trace panel if the model is absent instead of deriving a fake trace. Proof Map submission-review route cards fail closed with source badges and `Sync Proof Map` actions when `submissionReviewWorkflowRoutes` is absent from an otherwise loaded backend Proof Map; Proof Map product-team route cards fail closed the same way when `productTeamDeliveryTraceRoutes` or `productTeamAcceptanceChainRoutes` are absent. The product-team Harness verifies standalone API, real HTTP, Ready Package summary, Flow Graph, Proof Map, and UI coverage while keeping `readyForProduction: false`
- project zero-to-autonomy report through `GET /projects/:id/zero-to-autonomy-report` and Manager Ready Package, returning `project-zero-to-autonomy-report/v1` with the same stage taxonomy used by the redacted operator report. The first stage id is `settings-byok-seal`, not the older `settings-byok-readiness`, so Settings/BYOK proof means the user typed provider values, sealed them through the backend Secret Vault, and bound the running provider without browser-secret persistence. The remaining customer-facing first-run chain covers local startup readiness, kickoff/self-marketing, workspace binding, C/A handoff, autonomous Agent output, provider evidence/source review, brainstorm/draft/review/revision/final delivery, generic artifact coverage, Manager proof surfaces, Project Evidence Archive coverage, proof ids, timeline/event ids, backend routes, redaction status, and production blockers. The report summary must include `providerUsageCount` for search/model provider usage proof rows and `providerReceiptCount` for provider receipt proof rows, and the provider evidence/source review stage must carry those proof ids with the source-review proof ids. Manager Flow Graph adds a `provider-usage-audit` node that carries provider usage/receipt attachments, links evidence searches to custody, and feeds the `zero-to-autonomy-report` aggregate node after Product Team Delivery Trace; zero-to-autonomy stage attachments retain proof/timeline/event ids. Readiness Proof Map exposes `zeroToAutonomyReportRoutes` / `zeroToAutonomyReportSummary` with `providerUsageProofIds`, `providerReceiptProofIds`, and provider audit routes to Provider Readiness, Evidence Source Review Workflow, and Evidence Custody Readiness; Manager UI renders backend-required cards for both Ready Package and Proof Map instead of deriving a local overall readiness row. This report can mark local/private-pilot readiness for a real generic product-team run, but must keep `readyForPublicProduction: false` until managed identity, KMS/Secret Manager, persistence, queue/cron, provider cost controls, observability, audit, recovery, and customer production acceptance are complete
- evidence source review workflow through `GET|POST /projects/:id/evidence-source-review-workflow` and Manager Ready Package, returning `evidence-source-review-workflow/v1` with reviewer-visible source review items, review queue, submitted `evidence-source-review/v1` decisions, local `evidence-source-snapshot/v1` / `evidence-provider-receipt/v1` anchors, quality/source-safety signals, proof routes, decision gates, production controls, and checksum. The product-team Harness now posts Reviewer approvals, verifies source-review Flow Graph nodes, Proof Map source-review routes, archive decision records, source snapshot/provider receipt rows, enforced reviewer identity matching, and `evidence_source_reviews` / `evidence_source_snapshots` / `evidence_provider_receipts` persistence rows while keeping production source governance blocked
- evidence custody readiness through `GET /projects/:id/evidence-custody-readiness` and Manager Ready Package, returning `evidence-custody-readiness/v1` with source-snapshot rows, provider-receipt rows, Reviewer source-decision coverage, local custody gates, production controls, backend routes, proof ids, timeline/event links, and checksum. Manager Flow Graph emits an `evidence-custody-readiness` node plus edges from evidence searches to custody; Readiness Proof Map exposes `evidenceCustodyRoutes`; Project Evidence Archive includes the custody manifest entry and rows. This proves local/private-pilot custody for product-team evidence while production remains blocked until managed immutable object storage, signed custody access, retention/deletion jobs, and centralized custody audit exist
- project evidence export governance through `GET|POST /projects/:id/project-evidence-exports` and Manager Ready Package, returning `project-evidence-export-workflow/v1` and persisting `project-evidence-export/v1` rows for request, approval, rejection, and download-audit actions. Requests pin the archive checksum generated for that request, private-pilot handoff requires Manager plus security-admin approval, each row carries retention/data-residency/download-audit metadata plus a checksum, and records are mirrored into timeline, event ledger, Manager Flow Graph, Readiness Proof Map `projectEvidenceExportRoutes`, security route policy, and the `project_evidence_exports` persistence/migration seed path. After approvals, a download-audit action returns `project-evidence-export-package/v1` and `GET /projects/:id/project-evidence-exports/:exportRequestId/package` reads it back; the package descriptor includes archive manifest checksums, current/request archive checksums, checksum-drift explanation, watermark metadata, retention/data-residency metadata, package gates, and the download-audit receipt while keeping `downloadUrlIssued: false`. This is local/private-pilot export governance; production export remains blocked until encrypted object storage, signed expiring download URL issuance, watermark enforcement, retention deletion jobs, centralized download audit storage, and data-residency controls are implemented
- private-pilot go-live readiness through `GET /projects/:id/private-pilot-go-live-readiness` and Manager Ready Package, returning `private-pilot-go-live-readiness/v1` with delivery, governance, handoff, provider eval, preflight, release candidate, launch run, health, acceptance, and production-hardening stage rows. It exposes active phase, routed next action, proof ids, timeline/event ids, Manager Flow Graph command node, Readiness Proof Map `privatePilotGoLiveRoutes`, Security Boundary policy, Manager UI snapshot, and Harness coverage while keeping public production blocked by the broader launch audit
- `GET /projects/:id/launch-operations-overview` returns `launch-operations-overview/v1`, and Manager Ready Package embeds the same read model. The UI renders Private MVP Launch Package status, private-pilot acceptance, launch approvals, evidence package/download audit, public-production no-go, production blocker count, Public Production Next Steps, next action, and proof routes from that backend model, which in turn consumes `private-pilot-go-live-readiness`, `project-evidence-export-workflow`, `production-launch-control-center`, `production-launch-audit`, and `public-production-startup-readiness`. The read model includes `privateMvpLaunchPackage` as `private-mvp-launch-package-boundary/v1`, pointing to `private-mvp-launch-package/v1`, `npm run agents:private-mvp-launch-package`, and `npm run agents:private-mvp-launch-package:validate` while preserving the forbidden public-production claim. It also includes `publicProductionNextSteps` rows with Manager-readable owner/action/why-blocked/validation-command/route/run-route fields derived from backend blocker rows. `POST /projects/:id/launch-operations-overview/public-production-next-steps/:stepId/run` records `launch-operations-next-step-run/v1` into `launchOperationsNextStepRuns`, timeline, event ledger, Manager Flow Graph, and Readiness Proof Map. This receipt proves Manager acknowledgement of the next blocker; it does not close or promote public production readiness.
- production launch gap register through `GET /projects/:id/production-launch-gap-register` and Manager Ready Package, returning `production-launch-gap-register/v1` with deduplicated production gap rows, domain/owner/action routing, upstream checksums, proof ids, timeline/event ids, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchGapRoutes`, Security Boundary policy, Manager UI snapshot, and Harness coverage while keeping `readyForProduction: false`
- production launch control center through `GET /projects/:id/production-launch-control-center` and Manager Ready Package, returning `production-launch-control-center/v1` with release gate rows, blocked rows, owner routing, stage rows, global public-production startup readiness, operations/deployment/security/provider control state, managed-production evidence integrity state, upstream checksums, proof ids, timeline/event ids, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchControlCenterRoutes`, Security Boundary policy, Manager UI snapshot, and Harness coverage while keeping `productionDecision: no-go`
- production launch evidence dossier through `GET /projects/:id/production-launch-evidence-dossier` and Manager Ready Package, returning `production-launch-evidence-dossier/v1` with launch manifest entries, four production control domains, open gap rows, evidence-tier summary, proof ids, timeline/event ids, Manager Flow Graph dossier node, Readiness Proof Map `productionLaunchEvidenceDossierRoutes`, Security Boundary policy, Manager UI snapshot, and Harness coverage while keeping `productionDecision: no-go`
- production evidence integrity audit through `GET /projects/:id/production-evidence-integrity-audit` and Manager Ready Package, returning `production-evidence-integrity-audit/v1` with operations/deployment/security/provider control evidence tiers, domain rows, proof ids, timeline/event ids, backend routes, summary counts, Manager Flow Graph node, Readiness Proof Map `productionEvidenceIntegrityRoutes`, Security Boundary policy, Manager UI snapshot, and Harness coverage. The focused `npm run agents:product-team:production-evidence-integrity` validator uses the backend file-backed API plus signed membership routes to require local receipts to remain `local-rehearsal`, unsigned `evidenceEnvironment: "managed-production"` claims to remain `external-unattested`, and signed managed-production receipts to upgrade the audit only when managed-production evidence is both explicit and signed. Broader HTTP and browser coverage remains in the heavier product-team and UI gates
- private-pilot release candidate freeze through `GET|POST /projects/:id/private-pilot-release-candidates` and Manager Ready Package, returning `private-pilot-release-candidate-workflow/v1` and persisting `private-pilot-release-candidate/v1` rows after launch approvals, production launch audit private-pilot gates, handoff package download audit, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage are ready. The receipt freezes readiness/audit/evidence/provider/adapter checksums and mirrors proof into timeline, event ledger, Manager Flow Graph, Readiness Proof Map `privatePilotReleaseCandidateRoutes`, security route policy, Manager UI, and the `private_pilot_release_candidates` persistence/migration path. This is a private-pilot release-candidate receipt; production release remains blocked until real managed identity, persistence, queue, KMS, provider eval, centralized audit, deployment, and operations controls are implemented
- private-pilot launch run activation through `GET|POST /projects/:id/private-pilot-launch-runs` and Manager Ready Package, returning `private-pilot-launch-run-workflow/v1` and persisting `private-pilot-launch-run/v1` rows after a release candidate is frozen and launch audit, handoff package, archive/provider/proof/deployment/operations/security gates remain ready. The receipt freezes release-candidate, audit/evidence/provider/deployment/operations/adapter checksums and mirrors proof into timeline, event ledger, Manager Flow Graph, Readiness Proof Map `privatePilotLaunchRunRoutes`, security route policy, Manager UI, and the `private_pilot_launch_runs` persistence/migration path. This is a controlled private-pilot activation receipt; public production go-live remains blocked until real environment promotion, rollback automation, centralized audit, incident ownership, and managed infrastructure controls are implemented
- private-pilot post-launch health through `GET|POST /projects/:id/private-pilot-launch-health-checks` and Manager Ready Package, returning `private-pilot-launch-health-check-workflow/v1` and persisting `private-pilot-launch-health-check/v1` rows after a launch run exists and operations/security/provider/evidence/persistence/queue/proof gates remain healthy. The receipt freezes launch-run and post-launch health checksums, mirrors proof into timeline, event ledger, Manager Flow Graph monitoring nodes, Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, security route policy, Manager UI, and the `private_pilot_launch_health_checks` persistence/migration path. This is a private-pilot monitoring receipt; public production observability remains blocked until centralized logs, metrics, alert routing, on-call ownership, managed incidents, and restore drills exist
- private-pilot customer acceptance closeout through `GET|POST /projects/:id/private-pilot-acceptance-reports` and Manager Ready Package, returning `private-pilot-acceptance-report-workflow/v1` and persisting `private-pilot-acceptance-report/v1` rows after release candidate, launch run, post-launch health, evidence handoff, product-team delivery proof, operations/security/provider proof, Flow Graph, and Proof Map gates remain ready. The receipt freezes release/launch/health/evidence/Flow Graph/Proof Map checksums, mirrors proof into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map `privatePilotAcceptanceReportRoutes`, security route policy, Manager UI, and the `private_pilot_acceptance_reports` persistence/migration path. This is a customer private-pilot acceptance report; public production remains blocked until managed infrastructure, centralized observability, on-call ownership, incident systems, and restore drills exist
- production operations readiness through `GET /projects/:id/production-operations-readiness` and Manager Ready Package, returning `production-operations-readiness/v1` after customer acceptance. It aggregates acceptance, post-launch health, local incident drill, security audit-stream, provider eval, persistence adapter, queue adapter, and launch audit proof into local proof gates, then exposes production control gates for centralized logs/metrics/traces, alert routing, on-call ownership, managed incident records, real restore drill receipts, centralized audit retention, and managed database/queue cutover approval. Manager Flow Graph emits a production operations node, Readiness Proof Map exposes `productionOperationsReadinessRoutes`, Security Boundary lists the route policy, Manager UI renders the snapshot, and `npm run agents:product-team:private-pilot:ops-readiness` verifies the private-pilot-to-production-hardening bridge while keeping `readyForProductionOperations: false` until production controls exist
- production operations control receipts through `GET|POST /projects/:id/production-operations-control-receipts` and Manager Ready Package, returning `production-operations-control-receipt-workflow/v1` and persisting `production-operations-control-receipt/v1` rows. Receipts carry verified control ids, evidence routes/checksums, actor metadata, timeline/event proof, Flow Graph nodes, Proof Map `productionOperationsControlReceiptRoutes`, Security Boundary route policy, Manager UI state, and `production_operations_control_receipts` migration rows. The focused product-team Harness records a full receipt set through a lightweight backend write, proves standalone readback/proof projection, and verifies unsigned external ops receipts stay `external-unattested`; the full/private-pilot Harness verifies operations readiness can pass after customer acceptance while the broader production launch audit remains `no-go`
- production deployment control receipts through `GET|POST /projects/:id/production-deployment-control-receipts` and Manager Ready Package, returning `production-deployment-control-receipt-workflow/v1` and persisting `production-deployment-control-receipt/v1` rows. Receipts carry verified deployment control ids, evidence routes/checksums, actor metadata, timeline/event proof, Flow Graph nodes, Proof Map `productionDeploymentControlReceiptRoutes`, Security Boundary route policy, Manager UI state, and `production_deployment_control_receipts` migration rows. The product-team Harness records a full receipt set after deployment preflight proof and verifies the deployment domain can pass while the broader production launch control center remains `no-go`
- production security control receipts through `GET|POST /projects/:id/production-security-control-receipts` and Manager Ready Package, returning `production-security-control-receipt-workflow/v1` and persisting `production-security-control-receipt/v1` rows. Receipts carry verified managed identity/KMS/RBAC/audit/replay controls, evidence routes/checksums, actor metadata, timeline/event proof, Flow Graph nodes, Proof Map `productionSecurityControlReceiptRoutes`, Security Boundary route policy, Manager UI state, and `production_security_control_receipts` migration rows. The product-team Harness records a full receipt set after operations receipts and verifies the security domain can pass while the broader production launch control center remains `no-go`
- production provider control receipts through `GET|POST /projects/:id/production-provider-control-receipts` and Manager Ready Package, returning `production-provider-control-receipt-workflow/v1` and persisting `production-provider-control-receipt/v1` rows. Receipts carry verified provider allowlist/budget/tool-grant/retry/circuit/audit/cost/secret/source/model/eval/storage/alert/release/incident controls, evidence routes/checksums, actor metadata, timeline/event proof, Flow Graph nodes, Proof Map `productionProviderControlReceiptRoutes`, Security Boundary route policy, Manager UI state, and `production_provider_control_receipts` migration rows. The product-team Harness records a full receipt set after provider eval and verifies the provider rollout domain can pass while the broader production launch control center remains `no-go`
- C-side Manager UI buttons can now record production operations, deployment, security, and provider control rehearsal receipts from the same backend workflows with `includeReadModels: false`. The generated control rows deliberately carry `evidenceEnvironment: "local-rehearsal"` and local rehearsal evidence routes, so they validate UI-to-backend command reliability and Flow Graph/Proof Map projection without satisfying the managed-production evidence-integrity gate.
- launch approval workflow through `GET|POST /projects/:id/launch-approvals` and Manager Ready Package, returning checksummed `launch-approval-workflow/v1` with aggregate proof ids, timeline ids, event ids, mode summaries, and persisted `launch-approval/v1` records with release mode, decision, approver role/id/name, reason, linked audit checksum, and record checksum. Private pilot requires Manager plus security-admin approval; production requires Manager, security-admin, and operations-owner. The access/membership layer now treats operations-owner as a first-class project role for production approval. Approval records and the workflow checksum are mirrored into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map `launchApprovalRoutes`, production launch audit gates, Production Launch Control Center proof, and the `launch_approvals` migration seed path
- local identity-session contract through `GET|POST /projects/:id/identity-sessions`, `POST /projects/:id/identity-sessions/:sessionId/revoke`, and `x-hofs-session-token`, proving one-time token return, token-hash-only project persistence, verified session access decisions, revoked-token fail-closed behavior, Security Boundary summary fields, Manager Ready Package route exposure, `identity_sessions` persistence rows, migration seed coverage, and absence of raw session tokens from the file store
- production persistence snapshot through `GET /projects/:id/persistence-snapshot`, exporting the current project into normalized table records, foreign-key refs, checksums, access replay rows, and integrity checks for the future managed database migration
- managed persistence migration plan through `GET /projects/:id/persistence-migration-plan`, deriving Postgres-compatible table plans, seed batches, RLS/security guidance, cutover steps, and verification gates from the current persistence snapshot
- managed persistence dry-run verification through `GET /projects/:id/persistence-migration-dry-run`, simulating the adapter import contract and checking seed coverage, row counts, checksum preservation, primary-key uniqueness, relation integrity, RLS guidance, and migration-plan gates before a real database adapter is introduced
- managed persistence adapter cutover verification through `GET /projects/:id/persistence-adapter-plan` and `GET /projects/:id/persistence-adapter-dry-run`, proving adapter method coverage, local shadow adapter execution receipts, membership/replay/audit/provider/worker/read-model table coverage, shadow-read parity, transaction rollback, backup/restore readiness, RLS coverage, audit-stream continuity, and read-model checkpoint parity before the JSON/file store is replaced
- http-json adapter gateway contract verification through `npm run adapters:gateway`, proving shared health, persistence receipt, queue receipt, and backend project API dry-run gateway execution against local mock gateways before a real private adapter gateway is deployed
- runnable private adapter gateway verification through `npm run adapters:gateway-server:validate`, proving bearer auth, shared client contract compatibility, backend project API dry-run integration, live project API gateway preflight, storage adapter status, persisted table records, persisted queue rows, persisted leases, and receipt summaries against `src/agents/adapterGatewayServer.js` / `src/agents/adapterGatewayStore.js`
- production worker queue snapshot and adapter verification through `GET|POST /workers/queue-snapshot`, `GET|POST /projects/:id/worker-queue`, `GET /projects/:id/worker-queue-adapter-plan`, and `GET /projects/:id/worker-queue-adapter-dry-run`, exporting project, Agent, and Autopilot-session due rows, management priority, idempotency keys, lease keys, run routes, concurrency policy, retry/dead-letter policy, execution receipts, adapter methods, local shadow queue adapter execution receipts, `worker-queue-adapter-snapshot-parity/v1`, and lease/dispatch/ack/recovery gates for future queue/cron infrastructure
- operations readiness through `GET /projects/:id/operations-readiness`, combining worker-run observability, queue idempotency/lease checks, queue adapter dry-run status, security audit-stream ordering/hash-chain verification, persistence recovery source integrity, migration dry-run status, managed persistence adapter cutover dry-run status, provider usage/eval/source audit recovery metrics, proof-surface replayability, alert-rule drafts, a recovery runbook, and `operations-incident-drill/v1` rehearsal receipts into one backend read model. Provider usage ledger rows, provider eval rows, source snapshots, provider receipts, event proof, persistence export counts, and secret-vault boundary state now feed Operations Readiness metrics, alert rules, recovery steps, and incident-drill receipts before autonomous model/search calls resume
- provider readiness, controlled-run planning, shadow eval receipts, and production provider control receipts through `GET /projects/:id/provider-readiness`, `GET /projects/:id/provider-controlled-run`, `GET|POST /projects/:id/provider-eval-runs`, and `GET|POST /projects/:id/production-provider-control-receipts`, proving redacted provider status, deterministic validation provider, provider-backed evidence provenance, source-safety review, proof routes, leak scanning, provider control policy, retry/circuit-breaker policy, local secret-vault seal/open/rotation status, provider usage/cost ledger rows, provider operation policy dry-run, provider eval shadow replay rows, budget/rate headroom, human-review/evidence-governance boundaries, enforced access-policy coverage, and explicit production provider controls for real LLM/search rollout
- security boundary snapshot through `GET /projects/:id/security-boundary` and Manager Ready Package, exporting route policies, signed replay-protection metadata, audit fail-closed metadata, project membership policy metadata, local secret-vault metadata, audit-stream hash-chain metadata, sensitive-field coverage, provider status boundaries, redaction scan counts, and explicit auth/RBAC/secret-vault production blockers
- enforced access-control checks through `src/agents/accessControl.js`, including allowed own-Agent dashboard access, rejected cross-Agent dashboard/submission access, rejected reviewer identity mismatch, rejected observer persistence export, optional signed identity-header verification, optional signed request replay protection, optional audit fail-closed rejection, persisted project-membership policy verification with revocation, persisted security access audit rows, backend audit-stream rows with sequence/checksum/hash-chain proof, append-only JSONL audit file proof, event-ledger proof, `project_membership_policies`/`project_membership_grants`/`access_replay_records`/`security_access_audit`/`security_audit_stream` persistence export, migration-plan readiness gates, migration dry-run gates, and HTTP header propagation for the same decisions
- operations readiness checks for local operations gates, alert-rule drafts, recovery steps, `operations-incident-drill/v1` receipts, audit stream ordering/hash-chain verification, persistence recovery metrics, and migration dry-run metrics

Persona validation:

```bash
npm run skills:check
npm run skills:blend
npm run skills:dist
```

Current local validation covers the canonical persona skill package rather than app-local copies: `npm run skills:check` validates all 40 `skills/hall-of-fame-personas/source/personas/{slug}` packages and 55 deterministic ranking regression cases; `npm run skills:blend` validates that all 40 registered personas expose loadable Skill documents and that persona + professional skill blending works across the same registry; `npm run skills:dist` runs the self-contained source -> registry -> mindframe -> per-persona dist packaging -> cleanup -> privatize audit -> regression pipeline and produces 40 distributable `dist/skills/{slug}/SKILL.md` packages without external-tool fingerprints. This proves M1 roster/schema/regression/dist integrity for the private MVP path, while production still needs real-model style consistency evaluation and formal Skill version/release policy.

Frontend validation:

```bash
npm run ui:manager-demo
npm run ui:manager-backend:core
npm run ui:manager-backend:core:dev
npm run ui:manager-backend:real-user-chain
npm run ui:manager-backend:real-user-chain:dev
npm run ui:manager-backend:proof-navigation
npm run ui:manager-backend:private-pilot-panels
npm run ui:manager-backend:production-controls
npm run ui:manager-provider-proof
npm run ui:manager-backend:legacy-full
npm run build
```

`npm run ui:manager-backend:core` is the fast C/A control-loop browser gate. It starts an empty local backend, loads the visibly labeled Manager Demo sample fixture, verifies the compatibility seed before backend read-model sync, requires Manager Ready Package to render the public-production startup readiness blocker and `/public-production-startup-readiness` route, runs a real Manager Scenario Walkthrough action and requires `Manager Action Output Nodes`, runs an Autonomous Run Control action and requires direct output nodes, runs an Agent Autonomous Queue action and requires delegated output nodes, advances an Autopilot session through the scheduler-owned due-worker lane, and requires backend run/session receipts plus Manager Flow Graph, Readiness Proof Map, and delegated Agent Dashboard proof. `npm run ui:manager-backend:core:dev` runs the same gate against an already running Vite dev server to reduce build writes during C/A control-loop mock replacement. `npm run ui:manager-backend:real-user-chain` is the focused real-user browser stage split out of the long Manager backend UI chain; it reuses the zero-to-autonomy agents-server harness to verify Settings provider sealing, kickoff approval, C/A handoff, generic artifact submissions through accepted final deliverable, Flow Graph, Proof Map, transcript, timeline, event proof, Manager UI readback, and duplicate React key warning absence on proof-row surfaces. `npm run ui:manager-backend:real-user-chain:dev` runs that same stage against an already running Vite dev server to reduce build writes during mock replacement. `npm run ui:manager-backend:proof-navigation` is the focused Manager proof-navigation stage; it verifies provider evidence receipts, Manager Flow Graph provider-evidence attachments, Readiness Proof Map provider routes, and the click-through into backend Group Chat transcript proof. `npm run ui:manager-backend:private-pilot-panels` is the focused private-pilot Manager stage; it verifies launch approvals, project evidence export handoff, release candidate, launch run, launch health, acceptance report, Manager Flow Graph, and Readiness Proof Map evidence after C-side receipt clicks. `npm run ui:manager-backend:production-controls` is the focused production-control Manager stage; it clicks local rehearsal receipts for operations, deployment, security, and provider controls, verifies Flow Graph and Proof Map projection, and confirms the production evidence-integrity and public-startup gates still keep public launch no-go. The longer `npm run ui:manager-backend` remains the broader browser-chain gate for initiation, workbench submissions, reviews, revisions, final acceptance, Agent queue refresh after C-side control actions, and Manager-visible transcript proof coverage.

`npm run ui:manager-provider-proof` is the focused browser proof link for provider-backed autonomous evidence. It keeps the expensive full UI chain out of ordinary provider-proof work while still verifying that the Manager can open a Flow Graph provider-evidence attachment and land on the exact backend Group Chat transcript proof.

## 7. Current Status and Gaps

### Already covered in prototype

- persona skill registry
- deterministic Agent collaboration runtime
- durable kickoff session protocol
- Leader/Reviewer governance
- group-chat routing and receipts
- autonomous project and Agent workers
- file-backed local backend
- HTTP-shaped API
- explicit Agent submission records and artifact writing
- explicit artifact revision lineage across submissions, reviews, superseded drafts, and resolved obligations
- explicit evidence-search records and source packets
- explicit submission-review records for accepted and changes-requested verdicts
- BYOK-safe search provider status, local secret-vault seal/open/rotation validation, and deterministic provider-backed evidence search validation
- provider readiness contract for local MVP provider validation, including provider control policy, retry/circuit-breaker policy, and usage/cost ledger rows, without claiming production LLM/search rollout readiness
- prototype secret redaction for provider status/errors, submissions, evidence sources, review notes, persisted store records, and workspace artifact files
- production persistence snapshot contract for normalized records, checksums, relation checks, and event-ledger sequence validation
- managed persistence adapter dry-run contract for database cutover readiness, including local shadow adapter execution receipts, critical table coverage, shadow-read parity, rollback, backup/restore, RLS, audit-stream continuity, and read-model checkpoint gates
- production worker queue snapshot and adapter dry-run contracts for due project/Agent rows, idempotency, leases, priority ordering, retry/dead-letter policy, execution receipts, queue adapter execution receipts, adapter methods, dispatch coverage, snapshot parity, and recovery routes
- operations readiness contract for local observability/recovery gates, alert-rule drafts, managed persistence adapter metrics, queue adapter dry-run metrics, worker receipt/dead-letter metrics, audit/persistence/migration metrics, a recovery runbook, and checksummed incident drill receipts
- security boundary snapshot contract for route policy coverage, sensitive-field coverage, local secret-vault status, audit-stream hash-chain status, redaction scan status, provider secret exposure status, and security production blockers
- project evidence archive contract for redacted manager/customer handoff bundles with manifest checksums across transcripts, submissions, final deliverables, evidence, reviews, revision lineage, Flow Graph, Proof Map, timeline, event ledger, readiness, persistence, and worker recovery evidence
- submission review workflow contract for review rounds, requested changes, linked revision responses, accepted final deliverable proof, Manager Flow Graph closure node, Proof Map route, access policy, UI snapshot, and local/private-pilot review closure gates
- Manager Proof Map consumption of submission review workflow and product-team delivery trace routes, with browser-verified chat/timeline proof exits for requested-change closure, revision response, final acceptance, and generic delivery-chain completion
- private-pilot go-live readiness contract for stage rows, active phase, next action, release/launch/health/acceptance proof chain, Manager Flow Graph command node, Proof Map route, access policy, UI snapshot, and production-overclaim guard
- evidence custody readiness contract for local source snapshot/provider receipt/source review custody rows, Flow Graph custody nodes, Proof Map custody routes, archive manifest coverage, and managed-storage production blockers
- prototype enforced access-control contract for role capabilities, Agent self-scope, Reviewer identity matching, runtime worker identity, sensitive export denial, and access-decision audit replay
- manager dashboard and flow graph projections
- event ledger and proof-map surfaces
- Harness coverage for the manager scenario and product-team acceptance sample

### Still needed for product-grade implementation

- richer flow graph editing, filtering, and version comparison
- richer interactive submission review controls, reviewer diff views, calibrated reviewer policy, and version comparison beyond the current read-model closure proof
- production queue/cron infrastructure with durable leases, managed dead-letter storage, replay tooling, and recovery drills
- database-backed persistence with implemented managed adapter, real shadow-read comparison, backup/restore drills, rollback drills, and database RLS enforcement
- production identity provider, signed runtime identities, project membership storage, database row-level permissions, and workspace permissions
- managed BYOK secret isolation, KMS-backed encrypted secret storage, rotation, revocation, and access audit
- stronger security review for redaction bypasses, prompt injection, cross-Agent data contamination, and provider-specific secret handling
- real external search gateway adapters, calibrated source-quality policy, production human source-review workflow, and managed immutable provider/source audit storage
- LLM retry, validation, and cost controls
- long-running observability and recovery
- production-grade export/download service for project archives, including encrypted object storage, signed expiring download URLs, retention deletion, centralized download audit, watermarking, data residency controls, and externalized approval/audit policy enforcement

## 8. Design Rule

Every new Agent capability should answer three questions:

1. What intent or obligation caused this action?
2. What artifact or decision did the Agent produce?
3. Where can the manager verify the proof?

If the answer cannot be represented in chat, timeline, event ledger, task evidence, Agent dashboard, or flow graph, the feature is not yet operationally complete.
