# Agent Consciousness Architecture Audit

This audit maps the product objective to concrete implementation evidence.

## Requirement Coverage

### 1. Meeting communication frames

Requirement: In meetings, every participant must know what to say. Kickoff meetings should clarify what each person will do. Recurring meetings should clarify progress and delivery timing.

Evidence:

- `MEETING_PROTOCOLS.kickoff` defines the Lead frame and member frame.
- `MEETING_PROTOCOLS.sync` defines the recurring sync frame.
- `startAgentSession`, `routeDirectorDirective`, and `runRoundtableExchange` apply those frames when producing Agent turns.
- The dashboard renders kickoff and recurring sync frames in `Governance & Speech Protocol`.

Status: covered.

### 2. Lead-led governance

Requirement: Every team starts with one Lead. The Lead discusses and decides work coordination. The mode must be explicit.

Evidence:

- `createAgentNetwork` selects governance for the team.
- `chooseGovernance` respects a Director-confirmed `isLeader` marker before falling back to inferred Lead selection, then chooses a Reviewer.
- The Lead coordinates all non-lead Agents.
- The Reviewer reviews the Lead.
- The dashboard renders `Lead decides` and `Reviewer challenges`.
- `evaluateCollaborationState` checks that Lead and Reviewer exist and are separate.

Status: covered.

### 3. Long-running autonomous station operation

Requirement: Agents must work over hours/days without user interference and without turning everything into explicit user-driven process.

Evidence:

- `planAutonomousWorkCycle` computes hourly/daily private work and public publishing.
- `evaluateAutonomousSchedule` determines due status, waiting reason, due time, and next run time for enabled projects.
- `advanceAutonomousProjectCycle` writes the cycle back into project state: logs, task touches, progress, autonomous ledger, scheduler ledger, per-Agent state, last run time, and next run time.
- `agentStates` persists each Agent's manager relation, inbox, open obligations, current plan, owned tasks, private worklog, status, and last active time.
- The app scheduler checks due cycles for enabled projects and records whether a cycle came from the scheduler, a manual pulse, or the manager demo seed path.
- The dashboard exposes `Hour Pulse` and `Day Report` for manual verification.
- The dashboard Team panel surfaces each Agent's independent state and management relation.
- Project and chat state persist through browser `localStorage` in the prototype.

Status: covered for local prototype runtime; production backend can replace storage and scheduler while keeping the same contract.

### 4. Multi-Agent speaking logic

Requirement: The system must define how multiple Agents decide who speaks.

Evidence:

- `buildIntentions` ranks Agent meeting speakers.
- `selectTargets` routes Director directives to explicit targets or top-ranked Agents.
- `processWorkCommunication` ranks work-stream responders by attention score.
- `readCommunication` marks each Agent as `speak`, `read`, or `ignore`.
- `buildAgentChatReplies` adapts work-stream speaking decisions into UI chat replies.

Status: covered.

### 5. Reading logic for work communication

Requirement: Since Agents do not call meetings themselves during work, their communication happens inside the work stream. They must know how to read other Agents' messages.

Evidence:

- `readCommunication` scores direct mentions, Lead broadcasts, managed peer messages, capability matches, and ambient signals.
- It extracts obligations for action, review, and unblock cases.
- `processWorkCommunication` exposes diagnostics for every Agent reading a message.
- Autonomous cycles preserve non-ignored communication diagnostics in the ledger.
- Autonomous cycles also write relevant communication diagnostics into each Agent's inbox.
- The dashboard shows recent communication diagnostics from the autonomous ledger.

Status: covered.

### 6. Perfect collaboration invariants

Requirement: Collaboration must not silently drift. Tasks, blockers, risks, deadlines, and Director escalations need governance.

Evidence:

- `evaluateCollaborationState` checks Lead presence, Reviewer presence, Lead/Reviewer separation, ownerless tasks, blocked task ownership, Reviewer visibility for risk signals, and collaboration evidence in cycles.
- The dashboard renders `Collaboration Health`, status, and each check.
- `evaluateManagerScenarioReadiness` checks the requested manager scenario directly, including kickoff approval, role clarification, hearing edges, Leader election, Leader assignments, autonomous work, scheduler evidence, timeline progress, group chat evidence, peer handoff, War Room meeting change source, Google Chat change source, and owner plan sync.
- The readiness audit also checks that evidence-bearing tasks link back to their chat messages and timeline log ids.
- The readiness audit treats group-chat evidence as durable project state, not only as the latest visible chat window, so older kickoff and assignment evidence remains valid after long autonomous runs.
- Event-ledger readiness now separates the retained sequence window from replay coverage. The local backend keeps a larger retained window and can recover kickoff/change/handoff/autonomous replay coverage from durable project records after older events are pruned, so a restarted file/HTTP backend does not drop from `manager-ready` simply because A-side workers produced more proof events.
- The dashboard renders `Manager Scenario Readiness` with pass/fail detail for those scenario-specific requirements.
- The dashboard also renders `Manager Proof Map`, which turns each readiness check into a direct proof route to kickoff chat, group chat, change proof, management proof, timeline proof, or the relevant dashboard evidence surface.
- The backend service/API now exposes the same evidence routing through `getReadinessProofMap` and `GET /projects/:id/readiness-proof-map`, returning typed `proofKind`, `apiPath`, chat proof ids, timeline log ids, task ids, event ids, and Agent ids for every readiness check. It also exposes `roleNegotiationRoutes`, `selfMarketingRoutes`, and `projectMemoryReadinessRoutes` so role clarification, self-nomination, Leader campaign proof, and memory readiness proof are API-visible without reconstructing dashboard rows or Settings state.
- The backend service/API also exposes `getManagerDashboard` and `GET /projects/:id/manager-dashboard`, an aggregate manager read model containing readiness, proof-map routes, transcript summaries, timeline/event summaries, 24/7 operations rows, Agent management mesh rows, kickoff flow, assignment flow, change flow, handoffs, and task evidence rows.
- React now treats `GET /projects/:id/manager-flow-graph` as the authoritative Manager Flow Graph for backend-online real projects. The local `manager-flow-graph/fallback` renderer is limited to offline or explicit demo/sample projects; otherwise the UI shows a `backend model missing` state instead of presenting synthetic nodes as evidence. The browser Harness now intercepts a real initiated project's Flow Graph route with an empty backend model, requires `manager-flow-backend-required`, and verifies no `manager-flow-node-*` fallback nodes render.
- React now labels Manager governance read-model provenance across Live Command Center, Scenario Walkthrough, Action Playbook, Use Case Audit, and Sync Protocol Audit. Backend-online real projects show `backend-backed` or `backend model missing`; offline/demo fallback rows show `demo data`. This keeps C-side governance controls from visually overclaiming A-side backend coverage.
- React now applies the same provenance labels to the core C/A Ready Package autonomy panels: Product Team Delivery Trace, Product Team Operating Loop, Team Collaboration Diagnostics, Runtime Contracts, and Autonomous Cycle Consistency. Those panels can render backend-required missing models for real projects, but they must not visually masquerade as live backend collaboration proof.
- React now applies that Ready Package provenance boundary to the generic acceptance-chain panels as well: Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Evidence Quality Audit, Evidence Source Review Workflow, and Evidence Custody Readiness. Backend-online real projects show backend-required missing models for absent contracts, while Brainstorm Layer summary fallback is limited to offline/demo mode.
- Agent evidence-search, submission, and artifact-draft write routes now support `includeReadModels: false`, returning lightweight backend receipts with Manager/Agent read-model refresh routes. The product-team Harness requires this path before separately verifying Manager Dashboard, Manager Ready Package, Manager Flow Graph, Readiness Proof Map, Agent Dashboards, archive, and persistence rows, so frontend mock replacement can move to explicit backend refreshes instead of local synthetic rows.
- Autonomous Agent worker evidence now has a controlled provider bridge. `runAgentWorkCycleWithProviderEvidence` and the async `POST /projects/:id/agents/:agentId/work-cycle` path with `useProviderEvidenceSearch: true` call the configured search provider through provider policy, Agent tool grants, budget/rate limits, retry/circuit state, provider-vault binding checks, and provider usage ledger recording, then inject the provider-backed sources into the same evidence-search/submission contracts used by ordinary worker output. Agent Autonomous Action Queue rows now expose `providerEvidenceSearchPlanned` and `requestBodyTemplate.useProviderEvidenceSearch` for evidence-packet rows when a search provider is enabled, and `runAgentAutonomousActionQueueItemWithProviderEvidence` persists provider-backed evidence, provider usage, `autonomous-provider-preflight/v1`, and the Agent Queue run receipt together. Evidence-quality Autopilot session ticks and `/workers/autopilot/due` now route through the same async provider-evidence branch, linking provider usage, provider receipts, provider preflight checksum/action, evidence-search ids, Agent Queue run proof, `autonomous-run-control-action-run/v1`, loop receipts, `autonomous-run-control-session-tick/v1`, and an `autopilot-provider-evidence` group-chat coordination message; the HTTP scheduler uses the async API and forwards provider-evidence controls for scheduler-owned continuation; React Backend Worker Station extracts and renders the provider evidence receipt beside the Autopilot worker receipt; Manager Flow Graph attaches the provider receipt plus transcript route to the tick node and links it to the evidence-search node; Readiness Proof Map exposes the evidence-search/provider-usage/transcript routes on session tick routes; explicitly requested evidence targets can also land as project-level evidence/submission nodes when no single task owns the target. `npm run agents:scenario` verifies service/API/UI source coverage with the deterministic provider, provider receipt ids, submission source refs, worker ledger links, Agent Queue run links, Autopilot tick/due-worker links, Flow Graph links, Proof Map routes, backend Transcript proof, provider-vault proof, autonomous preflight proof, and provider usage rows.
- Agent submission writes now emit checksummed `agent-artifact-storage-proof/v1` records for manual, generated-draft, and worker-submitted artifacts. The proof follows the artifact into submission receipts, group-chat/timeline/event evidence, Flow Graph attachments, Readiness Proof Map submission routes, Artifact Quality Audit gates, Project Evidence Archive manifest/contents, and production persistence snapshot rows. Project Evidence Archive now also exposes an `artifact-storage-proof-coverage` integrity gate plus storage/workspace proof counts so the private-pilot handoff package proves every submission has a backend artifact proof. `npm run agents:product-team` now fails if submitted artifacts lose storage proof readiness, so the C-side Flow Graph cannot quietly regress to frontend-only artifact nodes.
- Project Evidence Archive now also exports `transcript-proof-coverage/v1` and an archive integrity gate that verifies Agent submission, evidence-search, evidence-source-review, submission-review, and provider-backed Autopilot coordination message ids are present in backend transcript messages or archived proof. Readiness Proof Map mirrors the same coverage through `transcriptProofCoverageRoutes` / `transcriptProofCoverageSummary`, including expected, archived, missing, and per-kind counts. The product-team Harness checks the Proof Map route, HTTP archive summary, standalone archive manifest, full archive contents, and zero missing proof ids, while the manager scenario Harness now checks provider-backed Autopilot transcript proof directly; C-side chat proof exits for real projects cannot be satisfied only by browser-local chat history.
- The backend service/API now exposes `getProductTeamDeliveryTrace` and `GET /projects/:id/product-team-delivery-trace`. The read model joins kickoff meeting proof, role self-marketing, brainstorm alternatives, evidence quality, generated drafts, review/revision closure, accepted final deliverable, and proof surfaces into `product-team-delivery-trace/v1`. Manager Ready Package embeds the trace, Manager Flow Graph adds a `product-team-delivery-trace` aggregate node connected to brainstorm, evidence/search, submission, review/revision, and final-deliverable nodes, Readiness Proof Map exposes `productTeamDeliveryTraceRoutes`, Security Boundary covers the route, and React renders the trace panel for backend-backed projects. If the backend is online and the trace is absent, React renders a `backend-model-missing` trace panel with fetch guidance instead of hiding the missing contract. This closes a C/A observability gap by giving the Manager one backend contract for the full autonomous product-team chain instead of forcing them to infer it from scattered panels.
- React Manager Proof Map now consumes `submissionReviewWorkflowRoutes`, `productTeamDeliveryTraceRoutes`, and `productTeamAcceptanceChainRoutes` as first-class proof cards. The browser Harness opens chat and timeline proof exits, so requested-change reviews, linked revision responses, accepted final deliverables, generic delivery-stage closure, and the full C/A product-team acceptance chain are visible as backend evidence rather than summary-only UI claims. The chain exposes `readyForGenericProductTeamAcceptance` and `readyForBsideProductTeamRun` while keeping `readyForProduction: false`, preserving the boundary between private-MVP autonomy and public-production readiness.
- The backend service/API now exposes `getProductTeamOperatingLoop` and `GET /projects/:id/product-team-operating-loop`. The read model joins Product Team Delivery Trace, Autonomous Run Control, Agent Autonomous Action Queue, Manager Action Queue, Worker Queue Snapshot, Readiness Proof Map, timeline ids, and event ids into `product-team-operating-loop/v1`. It is the C/A coordination summary for whether a generic product team can continue: Manager continuation route, Agent strategy selection, delivery closure, artifact/review/revision proof, scheduler/worker continuation, and production autonomy blockers. It also derives `product-team-customer-agent-handoff-execution/v1` from Product Team Mission Runner handoff plus Autopilot session/tick/run/submission ledgers, so Manager UI and Flow Graph can distinguish session-started handoff from A-side tick/run/output proof. Security Boundary lists the route, Manager Flow Graph now adds a visible `product-team-operating-loop` aggregate node connected to Product Team Delivery Trace, Autonomous Run Control, Agent strategy nodes, and C/A handoff execution attachment, Readiness Proof Map exposes `productTeamOperatingLoopRoutes`, React `Sync Proof Models` fetches/renders it with backend-required missing state, `ui:manager-backend` and `ui:manager-mission-runner` verify the browser panel, standalone route, and Flow Graph detail attachments, and `npm run agents:product-team:research-sample` reads it through the real HTTP server before accepting the Research validation sample.
- The backend service/API now exposes `getTeamCollaborationDiagnostics` and `GET /projects/:id/team-collaboration-diagnostics`. The read model turns overall scheduling into `team-collaboration-diagnostics/v1`: kickoff, Lead/Reviewer governance, Agent self-marketing, group chat transcript proof, owned tasks, brainstorm/evidence handoff, artifact/review/revision/final closure, operating-loop continuation, Flow Graph, Proof Map, timeline, and event evidence. Manager Ready Package embeds it, Manager Flow Graph adds `team-collaboration-diagnostics`, Readiness Proof Map exposes `teamCollaborationDiagnosticRoutes`, Security Boundary lists the route, React proof-model sync renders the panel with backend-required missing state, and the product-team Harness verifies API, HTTP, Flow Graph, and Proof Map coverage while preserving public-production blockers for provider/BYOK, managed queue/cron, durable audit, cost controls, acceptance thresholds, and incident recovery.
- The backend service/API now exposes `getRuntimeContracts` and `GET /projects/:id/runtime-contracts`. The read model freezes the local MVP runtime contract set as `runtime-contract-freeze/v1`: Agent submissions, artifact storage proof, evidence search/source/provider proof, Reviewer review, revision/final delivery, transcripts, timeline, event ledger, Manager Flow Graph, Readiness Proof Map, Product Team Operating Loop, and Team Collaboration Diagnostics. Manager Ready Package embeds it, Manager Flow Graph adds `runtime-contract-freeze`, Readiness Proof Map exposes `runtimeContractFreezeRoutes`, Security Boundary lists the route, React proof-model sync renders the panel with backend-required missing state, and the product-team Harness verifies API, HTTP, Flow Graph, and Proof Map coverage while keeping production runtime approval blocked for real provider/BYOK, managed database, managed queue/cron, durable audit, cost controls, acceptance policy, and incident recovery.
- The backend service/API now exposes `getAutonomousCycleConsistency` and `GET /projects/:id/autonomous-cycle-consistency`. The read model checks N-step local autonomy as `autonomous-cycle-consistency/v1`: action receipts, bounded loop receipts, Autopilot tick receipts, missing child run references, timeline/event proof, Worker Queue dead-letter state, Product Team Operating Loop, Team Collaboration Diagnostics, Runtime Contract Freeze, Manager Flow Graph, and Readiness Proof Map must remain consistent across at least three autonomous steps. Manager Ready Package embeds it, Manager Flow Graph adds `autonomous-cycle-consistency`, Readiness Proof Map exposes `autonomousCycleConsistencyRoutes`, Security Boundary lists the route, React proof-model sync renders the panel with backend-required missing state, the Research validation sample runs an HTTP three-step loop, and `npm run agents:product-team:cycle-consistency` repeats it through signed membership access before verifying API, Flow Graph, Proof Map, Ready Package, persistence, and UI source coverage. This closes the local MVP gap for "can the team keep moving coherently by itself" without claiming public 24/7 production autonomy.
- Product Team Operating Loop now exposes A-side initiative as first-class backend data through `agent-autonomous-initiative/v1` rows. The rows are derived from Agent strategy decisions and preserve selected action, intent, rationale, target artifact type, review/evidence targets, backend run route, and proof/timeline/event ids. Agent Autonomous Action Queue, the Operating Loop `agentSide.initiativeRows`, Agent action Flow Graph nodes, and the Operating Loop aggregate node all consume the same row shape, and the product-team Harness verifies API, HTTP, and Flow Graph coverage. This closes the gap where Agent "initiative" could be visible only as selected action labels or frontend copy.
- Agent initiative now survives backend scheduling boundaries. Worker Queue Snapshot Agent rows copy the `agent-autonomous-initiative/v1` projection into queue metadata and request bodies through `agentInitiativeId`, target artifact type, strategy intent, and Agent action run route. Autonomous Run Control run/loop receipts freeze the executed initiative for Agent-lane steps, and Autopilot session tick receipts freeze the candidate Agent initiative ids and artifact targets even when lane selection chooses Manager or scheduler work. The manager scenario Harness verifies queue rows, scheduler/autopilot receipts, and loop receipts preserve that proof.
- Autopilot session ticks now survive the same worker/persistence/adapter boundary. `autonomousRunControlSessionTickLedger` rows export `worker-execution-receipt/v1` with `sessionId`, idempotency, lease, and direct tick recovery route; `worker_runs` persistence rows keep that session linkage; Worker Queue Snapshot, queue adapter plan/dry-run, Operations Readiness metrics, and the private adapter gateway import the `autopilot-session` lane with project and Agent workers. The manager scenario Harness now writes an active Autopilot session to the file-backed store, restarts the service, resumes the session through `/workers/autopilot/due`, verifies persisted tick execution receipts, and checks adapter/operations metrics. `npm run agents:scenario`, `npm run agents:product-team:cycle-consistency`, `npm run adapters:gateway`, `npm run adapters:gateway-server:validate`, `npm run adapters:gateway-http:validate`, and `npm run adapters:gateway-postgres-store:validate` verify the local C/A scheduling lane without claiming managed production queue cutover.
- Sync Protocol Audit now has its own backend read boundary through `getSyncProtocolAudit` and `GET /projects/:id/sync-protocol-audit`. The Manager Dashboard and Ready Package still embed the audit, but React Backend Worker Station can fetch it directly through `Sync Protocol`, render a route-backed `backend-sync-protocol-audit-snapshot`, and the API/HTTP/browser Harnesses verify Leader assignment and change-request protocol rows from the standalone endpoint. This keeps C-side governance checks from depending on bundled dashboard data when the A-side collaboration protocol needs to be audited independently.
- Manager Live Command Center now has the same standalone C-side read path. `GET /projects/:id/manager-command-center` remains embedded in Manager Dashboard/Ready Package, but React Backend Worker Station can fetch it directly through `Sync Command`, render `backend-manager-command-center-snapshot`, and the browser Harness verifies the route-backed next-action, attention, Agent-readiness, and run-route surface. This keeps the primary C-side control room inspectable without requiring a full aggregate dashboard refresh.
- Manager Scenario Walkthrough now has its own standalone C-side refresh path in React as well as backend service/API coverage. `GET /projects/:id/manager-scenario-walkthrough` remains the authoritative guided story model and run steps still delegate to Action Queue receipts, but Backend Worker Station can fetch the read model through `Sync Walkthrough`, render `backend-manager-scenario-walkthrough-snapshot`, and the browser Harness verifies the route, completed/runnable counts, next gap, and next run route before a Manager relies on embedded dashboard state.
- Manager Use Case Audit now has a standalone C-side route and Proof Map linkage. `GET /projects/:id/manager-use-case-audit` remains embedded in Manager Dashboard/Ready Package, while Readiness Proof Map exposes `managerUseCaseAuditRoutes` / `managerUseCaseAuditSummary` with Manager Dashboard, Action Queue, Flow Graph, transcript/timeline/event proof, and production-blocker linkage. The manager scenario, browser backend, and product-team Harnesses verify this route-backed story coverage so backend-online real projects do not rely on `fallbackManagerUseCaseAudit` rows.
- Collaboration Intent Queue now has a standalone C/A route for the meeting intent protocol and queue system. `GET /projects/:id/collaboration-intent-queue` returns `collaboration-intent-queue/v1` rows for kickoff self-marketing, role negotiation, group-chat attention, Manager next action, Agent autonomous initiatives, MVP readiness targets, and review/revision/final closure. `POST /projects/:id/collaboration-intent-queue/:intentId/run` executes runnable rows by delegating to Autonomous Run Control, Agent Autonomous Action Queue, or Manager Action Queue, then persists `collaboration-intent-run/v1` with delegated receipt ids, proof/timeline/event ids, and checksum. The API response returns delegated Agent submission, artifact, evidence search, submission review, and review-response outputs when present, so the queue action can be inspected as a product node rather than only as a control receipt; scheduler delegates lift representative due-Agent sweep outputs to the top-level response. Manager Ready Package embeds it, Manager Flow Graph renders `collaboration-intent-queue` with protocol/Agent initiative/run-ledger/production-boundary attachments, Readiness Proof Map exposes `collaborationIntentQueueRoutes`, React Backend Worker Station consumes it through `Sync Intent Queue`, renders returned `Intent Output Nodes` with chat/timeline proof exits, Manager Proof Map renders direct chat/timeline proof exits, and the product-team plus focused browser Harnesses verify route, run receipt, proof, timeline, event, Agent initiative, Flow Graph node/detail, Proof Map card, returned Agent Submission output nodes after a real browser click, and production-blocker coverage. The core product-team Harness now specifically requires one Mission Handoff intent to delegate through Autonomous Run Control, one generic Agent initiative intent to delegate through Agent Autonomous Action Queue into an Agent run receipt plus submitted work artifact, one Group Chat attention intent to delegate through Manager Action Queue into transcript-visible result messages, one Manager next-action intent to delegate through Manager Action Queue into a Manager action receipt with result messages, and the MVP readiness target chain to execute brainstorm, evidence, draft, review, revision, final-deliverable, and final-acceptance targets through Collaboration Intent Queue.
- React now consumes that Agent write contract through the Agent Focus Workbench. Evidence, typed artifact submission, and draft+submit controls call backend Agent routes with lightweight receipts, then refresh Manager Dashboard, Manager Ready Package, Manager Flow Graph, and Agent Dashboard through standalone GET routes without first PUT-ing the stale browser project snapshot back to the backend. Revision-note and final-deliverable submissions can now link `respondsToReviewId`, `revisesSubmissionId`, and final `supersedesSubmissionIds` directly from that Workbench. React also consumes the submission review contract from Manager Dashboard submission rows: Reviewer verdicts, comments, and requested changes POST to the backend review route and refresh authoritative Manager/Agent proof models afterward. `npm run ui:manager-backend` verifies the same path on a real initiated project by submitting Turing brainstorm work, running evidence search, submitting a generated product-brief draft, recording Curie's review, submitting a linked revision-note and final-deliverable, accepting the final deliverable, and checking Manager/Flow/Proof/Agent read models. `npm run agents:product-team` now also statically requires this real browser Harness to keep the same generic chain covered.
- React Manager write commands now attempt the configured backend command route even if the local Backend Worker Station status is stale or temporarily marked offline. Local fallback is considered only after the actual command fails, and projects already seen in backend catalog/project/Manager Dashboard sync remain backend-required instead of falling back locally. The same guard suppresses both the browser-local one-minute autonomous loop and direct local autonomous-cycle helper calls for backend-synced real projects. Scenario Control Center's 24/7 pulse now uses the same backend-first pulse wrapper instead of branching on the frontend online status label. Manager Command Center, Scenario Walkthrough, Action Playbook, Use Case Audit, Manager Flow Graph Sync/Confirm/Supersede controls, Agent Focus Workbench, Reviewer composer, Agent-to-Agent message, per-Agent Pulse, Autonomous Run Control, bounded Autopilot session controls, and Agent Autonomous Queue run buttons use the configured project/backend command target rather than the frontend online label as their availability gate, so a stale status display cannot hide the controls that should attempt backend receipts. `npm run ui:manager-backend` verifies this by submitting a custom dual-channel Manager change after multiple backend read-model syncs, then requiring the backend snapshot to contain the unified change record, owner current plan, team sync, and immediate owner work-pulse evidence.
- Manager Command Center run-next, Scenario Walkthrough run, Action Queue run, chat, meeting, change-request, autonomous-cycle, per-Agent work-cycle, project due workers, Agent due workers, and HTTP scheduler ticks now also honor `includeReadModels: false`. The manager scenario Harness requires those lightweight receipts plus standalone refresh routes, proving that frontend mock replacement can move Manager/worker flows to receipt-first backend writes while preserving authoritative read-model verification. React also suppresses its browser-local one-minute autonomous loop and direct local autonomous-cycle helper for backend-online real projects, leaving real 24/7 work to server worker routes while preserving local cycles only for offline, demo/sample, or explicit development fallback projects.
- War Room meeting discussion is now covered by the same backend proof boundary. `POST /projects/:id/meeting` creates `meeting-agent-turn/v1` rows with the shared `runRoundtableExchange` runtime, persists them as `war-room-meeting-agent-turn` transcript messages, writes `meeting-agent-turn` timeline logs, applies them to Agent state, and records event-ledger ids. React War Room session start, terminal input, and Roundtable playback prefer those backend turns over local simulation for backend projects; if a backend-required real project returns no Agent turns, local simulation is blocked and a backend error is shown. The manager scenario and product-team static gates verify service/API exposure, timeline ids, event ids, transcript rows, backend-first session start, missing-turn fail-closed behavior, and UI consumption, so visible Agent meeting speech cannot regress into frontend-only animation without failing validation.
- Autonomous Run Control now has a dedicated deferred read-model contract for C/A scheduling receipts. Action, bounded loop, session start/tick/pause, and `/workers/autopilot/due` responses return session, active tick/pause, scheduler tick, due-worker, Agent queue, Product Team Operating Loop, Autonomous Cycle Consistency, Flow Graph, Proof Map, timeline, event, and transcript routes when `includeReadModels: false`. React consumes those routes through `refreshAutonomousRunControlReadModels`, so Manager-side Autopilot controls no longer infer session freshness from browser-local memory after scheduler commands.
- Autonomous Run Control now closes the same receipt loop for C-side orchestration: `POST /projects/:id/autonomous-run-control/:actionId/run` persists `autonomous-run-control-action-run/v1`, returns delegated output objects when the run produces submissions/evidence/reviews/review responses/artifacts/result messages, Manager Dashboard/Ready Package/Flow Graph/Readiness Proof Map expose the run, and `GET /projects/:id/agents/:agentId/dashboard` exposes the delegated Agent's control-run rows, timeline ids, event ids, proof ids, and route back to `/autonomous-run-control`. `POST /projects/:id/autonomous-run-control/run-loop` adds a bounded loop receipt (`autonomous-run-control-loop-run/v1`) with max step count, stop reason, and child run receipt ids, then exposes it through Manager Dashboard, Manager Ready Package, Flow Graph, and Readiness Proof Map. `GET /projects/:id/autonomous-run-control/sessions`, `POST /sessions/start`, `POST /sessions/:sessionId/tick`, and `POST /sessions/:sessionId/pause` add bounded Autopilot session receipts (`autonomous-run-control-session/v1`) and tick receipts (`autonomous-run-control-session-tick/v1`) that link to child loop/run receipts and carry participating Agent ids, task ids, action ids, and action lanes. Session start and tick receipts now also carry `autopilot-delivery-target/v1` snapshots from Product Team Delivery Trace, including ready/missing stage counts and the next missing stage; tick execution converts that stage into `autopilot-delivery-target-control/v1`, injects the target artifact/evidence/review controls into the delegated request body, and child loop/run receipts record `autonomous-run-control-target-selection/v1` candidate scores for lane choice. The same target state, target execution payload, and target-selection proof are exposed by Dashboard rows, Flow Graph attachments, and Proof Map routes. The session/tick ledgers are included in read-model cache signatures, Dashboard rows, Ready Package summaries, Flow Graph nodes, and Proof Map routes so the C-side cannot show stale, lane-free, or target-free autonomy proof after a tick. The Harness verifies service/API/UI source coverage plus a service-level start/tick/pause path through Dashboard, Flow Graph, and Proof Map with lane ownership, Agent ownership when an Agent lane runs, delivery-target progress, and target controls present in delegated run request bodies; the browser Harness verifies the Manager can run the control action, see direct `Run Control Output Nodes` with chat proof, run a bounded loop, and let the executing Agent later prove delegated action receipts from its own dashboard.
- Product Team Mission Runner now provides the one-call generic project startup path: `POST /product-team-missions` creates/approves kickoff or reuses an existing customer-approved kickoff, confirms Leader/Reviewer/next actions, preserves Manager-edited `selectedTeamIds`, starts bounded Autopilot, optionally runs the first A-side tick, and persists `product-team-mission-run/v1`; `GET /projects/:id/product-team-missions` reads the receipts. React initiation approval now calls this route for real backend projects and verifies the mission receipt reused the persisted meeting session, so C-side project start, meeting approval, confirmed roster, and A-side autonomy startup are one auditable contract. The receipt now embeds `product-team-customer-agent-handoff/v1`, which freezes the approved team, Leader/Reviewer, session/tick status, target stage, action lanes, next A-side routes, and proof checksum. The receipt appears in Manager Dashboard `productTeamMissionRuns`, Manager Flow Graph `product-team-mission-run` nodes plus C/A handoff attachments, Product Team Operating Loop `customerSide.handoff` and derived `customerSide.handoffExecution`, Collaboration Intent Queue `customer-agent-handoff-intent`, and Readiness Proof Map `productTeamMissionRunRoutes`, with routes to transcripts, Delivery Trace, Operating Loop, Collaboration Intent Queue, Runtime Autonomy Status, Autonomous Run Control sessions/ticks, and child loop/run receipts. The product-team core Harness and browser backend Harness verify this path, including the focused Mission Runner browser gate that renders Handoff Exec/Runs/Outputs from backend data. The stronger real-user zero-to-autonomy gate starts the real `agents:server`, seals provider endpoint/key in Settings, then proves the same C/A handoff plus required generic artifact submissions are visible in Manager UI submissions, Artifact Quality Audit, Flow Graph, Proof Map, transcript, timeline, and event-ledger traceability, proving Research remains a mission brief sample rather than a custom workflow.
- Local MVP startup readiness now has a single backend preflight. `GET /local-mvp-startup-readiness` returns `local-mvp-startup-readiness/v1`, aggregating Settings Provider Readiness, Secret Vault status, redacted model/search runtime status, provider vault bindings, project catalog readability, startup routes, validation commands, next action, and production blockers. This gives Settings and first-run flows one authoritative answer for "can the user type keys, can the backend seal them, and can a real product-team mission start?" without letting the browser infer readiness from local UI state. Manager Ready Package embeds `localMvpStartupReadiness`, Readiness Proof Map exposes `localMvpStartupReadinessRoutes`, and the contract can pass local setup gates while keeping `readyForProduction: false`.
- Settings Health Quick Check now has a backend contract. `GET /settings/health-readiness` returns `settings-health-readiness/v1`, with backend API, startup readiness, Secret Vault, model/search provider status, explicit provider-test routes, worker status route, project catalog, validation commands, and production blockers. React exposes the Health tab and reads this route for Quick Check instead of synthesizing diagnostic rows in the browser; Manager Ready Package embeds `settingsHealthReadiness`, Readiness Proof Map exposes `settingsHealthReadinessRoutes`, and Workflow Smoke remains explicit because it can spend provider calls and create a probe project.
- Settings Runtime Readiness now has a backend contract for Deployment and Models. `GET /settings/runtime-readiness` and `GET /projects/:id/settings-runtime-readiness` return `settings-runtime-readiness/v1`, with model/search runtime rows, provider-vault binding proof, worker status route, persistence adapter row, worker queue adapter row, deployment preflight linkage, validation commands, and production blockers. Manager Ready Package embeds `settingsRuntimeReadiness`, Readiness Proof Map exposes `settingsRuntimeReadinessRoutes`, and React Deployment/Models read this route instead of presenting frontend-inferred runtime readiness from scattered status calls.
- Settings Workspace capability rows now come from backend project settings. `GET/PUT /projects/:id/project-settings` includes `project-workspace-policy/v1` plus `project-workspace-capabilities/v1`, marking global interface language as `browser-local`; project language, interface density, default visibility, autosave cadence, runtime contract rules, long-term memory readiness, and meeting summaries as `backend-backed`. Runtime contract rules are non-editable in Settings and point to `GET /projects/:id/runtime-contracts`, which returns `runtime-contract-freeze/v1`. Long-term memory is non-editable in Settings and points to `GET /projects/:id/memory-readiness`, which returns `project-memory-readiness/v1` over project state, transcripts, meeting summaries, evidence/artifact index proof, and persistence adapter planning while managed memory remains production-blocked. Manager Ready Package now includes `projectMemoryReadiness`, and Readiness Proof Map exposes `projectMemoryReadinessRoutes` so the memory contract is inspectable from the formal Manager proof surfaces. `meeting-summaries/v1` derives rows from backend transcript, timeline, and event-ledger proof. React Settings renders `settings-workspace-policy-controls`, `settings-workspace-capability-contract`, `settings-workspace-memory-readiness`, and `settings-workspace-meeting-summaries` instead of a prose-only placeholder, and `npm run agents:project-settings:workspace` verifies workspace policy, runtime-contract route proof, memory-readiness route proof, Manager Ready Package proof, Readiness Proof Map proof, audit, transcript-derived meeting summary, timeline, event-ledger, and file-backed persistence proof.
- Runtime Autonomy Status now provides the aggregate C/A runtime recovery contract: `GET /projects/:id/runtime-autonomy-status` joins Mission Runner startup, Autopilot session/tick receipts, action/loop receipts, Worker Queue Snapshot recovery, queue adapter dry-run, persistence snapshot, Product Team Operating Loop, Autonomous Run Control, scheduler status/tick routes, and `/workers/autopilot/due`. Readiness Proof Map exposes `runtimeAutonomyStatusRoutes`, Manager Flow Graph renders `runtime-autonomy-status`, Security Boundary lists the route policy, and the product-team core plus Research validation gates verify the standalone API, proof route, graph node, Ready Package embedding, worker recovery attachment, scheduler recovery links, and production boundary. It proves local/private-MVP runtime autonomy only; unattended public production remains blocked by managed queue/cron, distributed leases, BYOK/provider controls, centralized observability, immutable audit, and incident recovery.
- Production operations, deployment, security, and provider control receipt POST routes now also honor `includeReadModels: false`. The manager scenario and product-team Harnesses verify each route returns the auditable receipt/log plus explicit Manager, launch-control, infrastructure rehearsal, operations, deployment/preflight, adapter gateway, evidence-integrity, and receipt-workflow refresh routes without embedding large Manager read models in the write response. React consumes those route receipts through `refreshReceiptReadModels`, so production-control rehearsal clicks refresh Proof Map, Flow Graph, transcript, timeline/event, Ready Package submodels, and launch-control surfaces from backend reads before falling back to full package sync.
- React now syncs a backend project catalog through `GET /projects`, opens backend-online projects through `GET /projects/:id`, and limits browser snapshot writes to guarded sample/dev fallback paths. Manager read-model refreshes, scheduler start, Server Pulse, Agent pulse/message, Manager Command Center, Scenario Walkthrough, Action Queue, and Flow Graph confirmation no longer pre-write the stale browser project snapshot; the remaining `PUT /projects/:id` paths are guarded `Save Project` or compatibility seed branches. Compatibility seeding is now single-adoption for a backend-seen project: React records `lastProjectSyncProjectId` and backend read-model project ids, then suppresses repeat sample snapshot `PUT` after any successful backend sync. `ui:manager-backend:core` asserts that a Manager Demo seed can happen at most once and that later Manager Action, Autonomous Run Control, Agent Queue, and Autopilot scheduler controls do not reseed after proof is written. Backend Worker Station `Start` now lets the server-owned immediate tick write both `manager-ui-scheduler-start-pulse` and `http-autonomous-scheduler-startup-agents` proof before the UI reads back project/Manager/queue state, preventing a parallel browser pulse from overwriting Agent worker ledger receipts. The pending Start state records only `startPending` metadata and resets `lastStartedRunImmediately` until the backend response arrives, so the C-side cannot show a fabricated worker result during scheduler startup.
- React now scopes Agent Dashboard caches, Agent Workbench drafts, Agent Message drafts, Reviewer composer drafts, Manager Change Intake drafts, Leader Assignment composer drafts, Group Chat input, War Room meeting input, active chat channel selection, Chat proof focus, Timeline proof focus, and selected Timeline detail by project before any backend write or Manager monitor view can read them. This keeps reusable personas and C-side command inputs from carrying another project's local UI state into evidence, artifact, review, group-chat, meeting, change-ledger, timeline, transcript, event proof, or proof-monitoring focus.
- The compatibility seed and visible `Save Project` action are now scoped to sample fixture or explicit development fallback projects. For real projects, a missing backend read fails closed with a visible `Backend project missing; local seed suppressed` state, and browser snapshot save is disabled instead of PUT-ing the active browser snapshot. The full `ui:manager-backend` browser Harness now asserts the disabled Save Project state immediately after real initiation approval. This keeps backend Agent submission, review, Autopilot session, worker, and Flow Graph ledgers authoritative after the C-side has started using backend receipts.
- Persona Skill validation now has npm-facing gates instead of relying on manual Python commands. `npm run skills:check` validates 40 canonical persona source packages and 55 deterministic ranking regression cases through the skill package scripts; `npm run skills:blend` validates persona + professional skill blending across the 40 registered personas; `npm run skills:dist` runs source validation, registry compilation, mindframe generation, per-persona dist packaging, external-fingerprint cleanup/audit, and regression in one self-contained pipeline, producing 40 `dist/skills/{slug}/SKILL.md` packages. This keeps The Pantheon roster, task matching, roundtable planning, Agent prompt assembly, and distributable persona artifacts tied to `skills/hall-of-fame-personas/` rather than app-only persona definitions.
- React chat proof navigation now treats backend transcripts as authoritative for backend-online real projects. Before focusing a chat proof, `openProjectChatProof` syncs `GET /projects/:id/transcripts` and the target channel route using no-store backend requests; if the backend transcript does not contain the requested proof id, local recovered proof rows and cached browser-chat proof are suppressed, proof navigation fails closed, and the UI records a backend-required proof error. Group Chat Transcript Index follows the same rule: it labels the source, renders `backend-transcript-index-required` for real backend projects with no transcript read model, and suppresses browser-recovered message/proof counts until the backend transcript route succeeds. Manager Dashboard submission rows expose direct chat/timeline proof exits, and `ui:manager-backend` verifies those real submission proof buttons. `recoverProofMessages` remains available for offline, sample fixture, imported legacy, or explicit development fallback projects.
- React now fails closed for backend-online real project chat, meeting, Manager change intake, Leader assignment, and manual Hour/Day pulse command failures. Those paths surface the backend error and stop browser-local runtime mutation; local runtime fallback remains available only for offline mode, explicit demo/sample projects, or the development-only local runtime fallback flag. This prevents C-end controls from creating fake A-end collaboration proof when the backend command did not commit.
- React full Ready Package proof-model sync now also pulls standalone MVP, pilot, deployment, evidence archive/export, private-pilot lifecycle, production launch audit/gap/control/dossier/evidence-integrity, launch approval, operations, production control receipt, provider, persistence adapter, worker queue adapter, and security boundary read models. The Manager UI prefers those standalone models over embedded Ready Package snapshots, so launch/control panels can move away from summary-shaped frontend fallback without losing visibility.
- The service now caches heavyweight read models for the same project/message/provider/security signature and clears that cache on project, message, and kickoff-meeting writes. This keeps repeated Manager Ready Package, deployment preflight, production launch audit, persistence adapter dry-run, operations readiness, and queue adapter dry-run reads reliable enough for the manager scenario and product-team Harnesses while preserving the same backend contracts.
- The backend service/API now exposes `getMvpReadiness`, `GET /projects/:id/mvp-readiness`, and `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run`. This read model separates local product-team MVP readiness from production readiness: it requires the full generic acceptance loop and backend worker proof before marking `readyForLocalPilot`, while keeping production blockers visible for secret vault/RBAC, managed persistence, queue/cron infrastructure, real provider rollout, and observability/recovery. It now also exposes `operatorActions` and a routed `nextShortestPath` with method, owner, API path, run path, and production-blocker flags; missing core-loop actions can generate `autopilot-delivery-target-control/v1` and the POST route records `mvp-readiness-operator-action-run/v1` receipts into the project ledger, timeline, event ledger, Flow Graph, and Readiness Proof Map. Runnable core-gap receipts are projected into Collaboration Intent Queue and Autonomous Run Control as `run-mvp-readiness-target`; executing that action delegates through Agent Autonomous Queue, submits the requested generic artifact/review/evidence node, and then expires the completed target step. Production-hardening receipts preserve blocker context and stay non-autonomous. The product-team Harness verifies early core-gap target handoff, actual target execution across brainstorm board, evidence packet/search, product brief, requested-changes review, linked revision note, final deliverable, and accepted final review with chat/timeline/event/workspace proof, Flow Graph and Proof Map projection, stale target expiry, private-pilot handoff, production-hardening actions, receipt projection, and Manager UI receipt target display.
- The backend service/API now exposes `getPilotLaunchReadiness` and `GET /projects/:id/pilot-launch-readiness`. It exports `pilot-launch-readiness/v1`, embedded in Manager Ready Package, with private-pilot go/no-go, production no-go, evidence route coverage, failed gates, production blockers, and a launch packet checksum. The contract aggregates MVP readiness, proof routes, Manager Flow Graph, artifact/evidence/review workflow, Security Boundary, Provider Readiness, managed persistence, queue adapter, Operations Readiness, and incident drill receipts without claiming production readiness.
- The backend service/API now exposes `getDeploymentPreflight` and `GET /projects/:id/deployment-preflight`. It exports `deployment-preflight/v1`, embedded in Manager Ready Package, with backend store/audit sink status, scheduler env, access-control hardening flags, signed/replay/audit fail-closed settings, secret-vault readiness, provider policy, managed persistence adapter status, worker queue adapter status, adapter gateway preflight, operations readiness, private-pilot deployment blocker gates, warnings, production controls, and a checksum while keeping `productionDeploymentReady: false`.
- The backend service/API now exposes `getProductionLaunchAudit` and `GET /projects/:id/production-launch-audit`. It exports `production-launch-audit/v1`, embedded in Manager Ready Package, with unified private-pilot and production decisions, private-pilot gates, private-pilot handoff gates, production gates, audit-integrity gates, evidence routes, `project-evidence-handoff-summary/v1`, production blockers, next-shortest-path guidance, route policy coverage, and a checksum. The product-team Harness requires private-pilot `go` for the completed acceptance project, requires next-shortest-path to point at `private-pilot-handoff` before the evidence export package has a download-audit receipt, then returns to production hardening after handoff readiness while keeping production `no-go` and failed production gates visible.
- `docs/LAUNCH_READINESS_GATES.md` now turns those readiness models into an operator-facing launch matrix: P0 local backend MVP, P1 customer private pilot, and P2 public production have explicit validation commands, allowed claims, and blocker domains. `npm run launch:gates` verifies the matrix references runnable scripts and preserves the generic product-team positioning, so launch readiness cannot be inferred from a single green Harness or from local rehearsal receipts. `npm run launch:infra` chains the adapter gateway, reference gateway server, Agent HTTP server env-gateway mode, and Postgres-compatible gateway storage validations into one P2 infrastructure rehearsal for managed persistence / queue work while still requiring real managed cutover evidence before production.
- The backend service/API now exposes `getProductionInfrastructureRehearsal` and `GET /projects/:id/production-infrastructure-rehearsal`. It exports `production-infrastructure-rehearsal/v1`, embedded in Manager Ready Package, by aggregating adapter gateway preflight, managed persistence plan/dry-run, worker queue adapter plan/dry-run, deployment preflight, production deployment control receipts, operations readiness, production operations readiness, and production launch gap status. It also emits `managedCutoverGates` / `managedCutoverSummary` so Manager UI and Harness can verify gateway, managed persistence, managed queue/cron, deployment, operations/recovery, and gap-closure cutover gates without deriving production status in React. Verified operations receipts project managed persistence/queue cutover proof into the matching domain rows and cutover gates, and verified deployment receipts project into the `deployment-preflight` domain row and `deployment-cutover` gate as `deploymentReceiptReady` with a workflow checksum. React proof-model sync fetches the standalone route and renders `backend-production-infrastructure-rehearsal-snapshot` plus cutover gate rows in Manager Ready Package instead of stitching infrastructure readiness from separate panels. Readiness Proof Map exposes `productionInfrastructureRehearsalRoutes` / `productionInfrastructureRehearsalSummary`, and Manager Flow Graph emits a `production-infrastructure-rehearsal` node with upstream infrastructure routes. The product-team Harness verifies Manager Ready Package coverage, standalone API/HTTP route coverage, Security Boundary route policy, managed persistence/queue/deployment domain rows and cutover gates, upstream proof routes, Proof Map coverage, Flow Graph coverage, UI source coverage, and `readyForProduction: false` until real managed-production cutover evidence exists.
- The backend service/API now exposes `getLaunchApprovalWorkflow`, `GET /projects/:id/launch-approvals`, and `POST /projects/:id/launch-approvals`. It exports `launch-approval-workflow/v1`, persists checksummed `launch-approval/v1` records, requires Manager + security-admin approval for private pilot, requires operations-owner for production, writes timeline/event-ledger proof, and mirrors release-governance proof into Manager Ready Package, Manager Flow Graph decision nodes, Readiness Proof Map `launchApprovalRoutes`, production launch audit gates, route policy coverage, and the `launch_approvals` persistence/migration seed path.
- The backend service/API now exposes `getProjectEvidenceArchive` and `GET /projects/:id/project-evidence-archive`. It exports full `project-evidence-archive/v1` contents on the standalone route, embeds a manifest-only archive snapshot in Manager Ready Package, redacts sensitive fields, scans the generated full archive for raw secret patterns, and emits manifest checksums for project state, transcripts, Agent submissions, final deliverables, evidence searches, submission reviews, revision lineage, Manager Flow Graph, Readiness Proof Map, timeline, event ledger, readiness summaries, persistence summary, and worker recovery summary. This closes the manager/customer handoff proof surface for private-pilot validation while keeping the dashboard payload responsive and production export/download controls as remaining work.
- The backend service/API now exposes `getEvidenceCustodyReadiness` and `GET /projects/:id/evidence-custody-readiness`. It exports `evidence-custody-readiness/v1` with checksummed custody rows for source snapshots, provider receipts, and submitted source-review decisions; local gates for source snapshot, provider receipt, reviewer decision, persistence table, archive route, and Proof Map visibility; and explicit production controls for immutable managed storage, signed custody access, retention/deletion jobs, and centralized custody audit. Manager Ready Package, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, production launch audit, access policy, and Manager UI all consume the same contract, and the product-team Harness now requires custody nodes/routes/proof links without allowing production storage overclaim.
- The backend service/API now exposes `getProjectEvidenceExportWorkflow`, `getProjectEvidenceExportPackage`, `recordProjectEvidenceExport`, `GET /projects/:id/project-evidence-exports`, `GET /projects/:id/project-evidence-exports/:exportRequestId/package`, and `POST /projects/:id/project-evidence-exports`. It exports `project-evidence-export-workflow/v1`, persists checksummed `project-evidence-export/v1` request/approval/rejection/download-audit records, pins export requests to the generated archive checksum, requires Manager + security-admin approval for private-pilot evidence handoff, writes timeline/event-ledger proof, and mirrors export-governance proof into Manager Ready Package, Manager Flow Graph, Readiness Proof Map `projectEvidenceExportRoutes`, route policy coverage, and the `project_evidence_exports` persistence/migration seed path. After download audit it returns `project-evidence-export-package/v1` with archive manifest checksums, request/current archive checksums, checksum-drift explanation, watermark metadata, retention/data-residency metadata, package gates, and the download-audit receipt. It keeps `readyForProductionExport: false` and `downloadUrlIssued: false` until encrypted object storage, signed download URLs, watermark enforcement, retention deletion, centralized download audit, and data residency controls exist.
- The backend service/API now exposes `getPersistenceSnapshot` and `GET /projects/:id/persistence-snapshot`. It exports a normalized production persistence contract from the current project: table manifests, compact records, foreign-key refs, checksums, event-ledger sequence checks, and relation integrity checks. This does not replace a managed database, but it gives the production persistence blocker a concrete migration target.
- The backend service/API now exposes `getPersistenceMigrationPlan`, `getPersistenceMigrationDryRun`, `GET /projects/:id/persistence-migration-plan`, and `GET /projects/:id/persistence-migration-dry-run`. Together they derive a managed Postgres-style table/seed/RLS plan and simulate the adapter import contract against the current snapshot, including row counts, checksums, primary-key uniqueness, relation integrity, and migration-gate checks before a real database adapter exists.
- The backend service/API now exposes `getPersistenceAdapterPlan`, `getPersistenceAdapterDryRun`, `getPersistenceAdapterDryRunAsync`, `GET /projects/:id/persistence-adapter-plan`, and `GET /projects/:id/persistence-adapter-dry-run`. These read models turn the managed database production blocker into a concrete adapter cutover contract: critical table coverage for membership, replay, audit, provider usage/eval, worker, and read-model records; `managed-persistence-adapter-contract/v2` methods for schema/transaction/import/read/audit/replay/checkpoint/backup/restore/shadow-read/commit/rollback; a configurable adapter facade in `managedPersistenceAdapter.js` with `managed-persistence-adapter-status/v1`; a default local shadow driver that emits `managed-persistence-adapter-execution-receipt/v1`; an async `http-json` gateway path that returns `managed-persistence-adapter-gateway-execution/v1`; and dry-run gates for adapter driver status, gateway health/capability, shadow-read parity, transaction rollback, backup/restore, RLS, audit-stream continuity, and read-model checkpoint parity before replacing the JSON/file store. The status facade exposes `postgres` as a future real-adapter target while keeping `productionCutoverReady: false` until real database execution receipts and cutover approval exist.
- `adapterGatewayClient.js` and `npm run adapters:gateway` now define and validate the shared private `http-json` adapter gateway contract. The mock gateway validation proves the expected `adapter-gateway-health/v1`, `managed-persistence-adapter-execution-receipt/v1`, and `worker-queue-adapter-execution-receipt/v1` shapes, then creates a backend project and verifies the project API dry-run routes can execute through the gateway before a real external database/queue gateway is deployed. `getAdapterGatewayPreflight`, `getAdapterGatewayPreflightAsync`, and `GET /projects/:id/adapter-gateway-preflight` now expose `adapter-gateway-preflight/v1`: local-shadow mode proves the current private-pilot adapter rehearsal path, while `http-json` mode calls live gateway health/state, checks capabilities, exposes storage/queue metadata, feeds deployment preflight, and keeps production cutover blocked. `adapterGatewayStore.js`, `adapterGatewayServer.js`, `npm run adapters:gateway-server`, and `npm run adapters:gateway-server:validate` add a runnable local private gateway process with bearer auth, a pluggable async-compatible storage adapter contract, shadow table record persistence, queue row / lease / dead-letter persistence, client contract validation, project API dry-run integration, and live project API gateway preflight. `npm run adapters:gateway-postgres-store:validate` adds evidence for the Postgres-compatible store boundary: it exposes `adapter-gateway-postgres-schema-plan/v1`, redacts the database URL, binds a fake query function, and proves schema-plan, project summary, table-record, queue-row, queue-lease, dry-run receipt, state-snapshot write, and `adapter-gateway-postgres-readback/v1` snapshot/count parity operations through the same gateway and backend API dry-run path. This is process/API-path and query-shim coverage only; production cutover still requires the same verification against real managed infrastructure with real database readback, backup/restore, RLS, monitoring, and approval.
- The backend service/API now exposes `getWorkerQueueSnapshot`, `getProjectWorkerQueue`, `GET|POST /workers/queue-snapshot`, and `GET|POST /projects/:id/worker-queue`. These routes preview project and Agent worker queues without mutation, including due status, management priority, idempotency keys, lease keys, run routes, concurrency policy, retry state, `worker-queue-retry-policy/v1`, `worker-dead-letter-policy/v1`, completed `worker-execution-receipt/v1` rows, and a derived dead-letter queue. This does not replace production queue infrastructure, but it gives the queue/cron blocker a concrete worker scheduling and recovery contract.
- The backend service/API now exposes `getWorkerQueueAdapterPlan`, `getWorkerQueueAdapterDryRun`, `getWorkerQueueAdapterDryRunAsync`, `GET /projects/:id/worker-queue-adapter-plan`, and `GET /projects/:id/worker-queue-adapter-dry-run`. These read models turn the queue/cron production blocker into a concrete adapter contract covering enqueue, durable lease acquisition, dispatch, execution receipt acknowledgement, retry import, dead-letter recovery, inspection, and `worker-queue-adapter-snapshot-parity/v1` across queue rows, leases, acknowledgements, and dead-letter recovery. `workerQueueAdapter.js` now provides a configurable `worker-queue-adapter-status/v1` facade with a default local shadow driver that emits `worker-queue-adapter-execution-receipt/v1` plus an async `http-json` gateway path that returns `worker-queue-adapter-gateway-execution/v1`; `managed-queue` remains a future real-adapter target while `productionCutoverReady` stays false until real queue execution receipts and cutover approval exist.
- The backend service/API now exposes `getOperationsReadiness` and `GET /projects/:id/operations-readiness`. It combines worker-run observability, queue idempotency/lease checks, queue adapter dry-run verification, managed persistence adapter cutover dry-run verification, execution receipt coverage, retry/dead-letter recovery readiness, security audit-stream ordering/hash-chain verification, persistence recovery source integrity, migration dry-run status, provider usage/eval/source audit recovery, proof-surface replayability, security boundary visibility, alert-rule drafts, a recovery runbook, and `operations-incident-drill/v1` checksummed rehearsal receipts into one local operations contract. Provider usage ledger rows, provider eval receipts, source snapshots, provider receipts, event proof, persistence export counts, and the secret-vault boundary now surface as a recovery gate, alert rule, runbook step, metrics, and incident-drill receipt before autonomous model/search calls resume. This does not replace centralized production observability or incident management, but it makes the observability/recovery blocker concrete and visible in Manager Ready Package.
- The backend service/API now exposes `getProviderReadiness` and `GET /projects/:id/provider-readiness`. It exports `provider-readiness/v1` with redacted model/search status, deterministic validation-provider proof, provider-backed evidence-search provenance, checksummed source snapshots, provider receipts, source-safety summary, provider proof routes, redaction/leak scan status, provider control policy, retry policy, circuit-breaker policy, local secret-vault seal/open/rotation status, provider usage/cost ledger rows, model-backed artifact-draft quality/human-review summary, and explicit remaining production controls for managed KMS, revocation, managed immutable source/provider/model-output audit storage, centralized alerting, calibrated model-output quality evaluation, and real-provider incident handling. The service facade evaluates configured provider calls against allowlists, per-project hourly request limits, daily budget cents, Agent tool grants, retry attempts/backoff, and provider circuit state, then writes `evidence-provider-receipt/v1`, per-source `evidence-source-snapshot/v1`, `provider-usage` events, and `project.providerUsageLedger` rows. Evidence source normalization now writes `evidence-source-safety/v1` plus source snapshot/provider receipt links into evidence searches, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, task evidence, archive contents, and persistence snapshot rows. The local secret-vault boundary is also exposed as top-level runtime routes: `GET /secret-vault/status`, `GET /secret-vault/records`, `POST /secret-vault/seal`, and `POST /secret-vault/rotate`; the Harness verifies redacted status, ciphertext-free record metadata, seal receipts, rotation receipts, no plaintext/master-key leakage through the HTTP backend, immediate model/search runtime binding to `apiKeySource: local-secret-vault` plus `runtimeEnabled: true` after provider keys are sealed, and local restart rehydration from `SECRET_VAULT_RECORDS_FILE`. The provider/vault bridge is now exposed through `getProviderVaultBindings`, `GET /provider-vault-bindings`, and `GET /projects/:id/provider-vault-bindings`; Provider Readiness embeds `provider-vault-bindings/v1`, Security Boundary lists the route policy, and Agent provider execution now writes `provider-vault-binding-proof/v1` or checksum/route evidence into model artifact drafts, runtime model-intent usage, provider-backed evidence receipts, worker provider-evidence receipts, and provider usage rows. The Harness verifies model/search binding rows, proof routes, checksums, receipt/usage linkage, and zero secret leakage through in-process and HTTP backends. Manager Ready Package embeds the read model, enforced access policy covers the provider and vault routes, persistence snapshot exports `evidence_source_snapshots`, `evidence_provider_receipts`, and `provider_usage_ledger` with retry/circuit columns, and MVP readiness points the `production-real-providers` blocker to this route while keeping `readyForProduction: false`.
- The backend service/API now also exposes `getProviderControlledRun` and `GET /projects/:id/provider-controlled-run`. It exports `provider-controlled-run/v1`, a no-call policy dry-run for private-pilot BYOK execution. The plan covers provider health checks, kickoff/intent model support, model artifact drafting, and evidence search, then records provider configured/enabled status, operation id, Agent id, allowlist/tool-grant decision, budget and hourly request headroom, retry/circuit state, estimated cost, provider usage proof, model human-review boundary, evidence governance, redaction state, production controls, and checksum. Manager Ready Package embeds it, Readiness Proof Map exposes `providerControlledRunRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin reads, and Manager UI renders `backend-provider-controlled-run-snapshot`. This closes the gap between "provider readiness exists" and the Manager question: which model/search operations can enter a controlled private-pilot run before real provider rollout.
- Project settings now own local Agent tool grants instead of leaving them as browser labels. `GET/PUT /projects/:id/project-settings` persists `project-tool-grant-policy/v1` with default grants for `provider:test`, `model:kickoff`, `model:intent`, `model:artifact-draft`, and `search:evidence`; writes add project settings audit, timeline, and event-ledger proof. Provider Readiness and Provider Controlled Run merge that policy into `provider-control-policy/v1`, so removing `search:evidence` produces a visible `agent-tool-grant-missing` decision before any provider call. `npm run agents:project-settings:tool-grants` validates API persistence, Flow-proof ledger evidence, file-backed storage, and the controlled-run denial path while production runtime identity and task-scoped authorization remain blocked.
- Project settings now expose Settings Integration capability rows instead of leaving remaining external integrations as ambiguous UI placeholders. `project-settings/v1` returns `project-integration-capabilities/v1`: provider budget and Agent tool grants are `backend-backed` editable rows; Proxy/Webhook points to `GET /projects/:id/adapter-gateway-preflight`; MCP Tools points to `GET /projects/:id/provider-readiness`; Vector Store points to `GET /projects/:id/evidence-index-readiness`; Budget Alerts points to `GET /projects/:id/budget-alert-readiness`; Error Reporting points to `GET /projects/:id/error-reporting-readiness`; those five route-backed rows are `backend-backed`, read-only, and `readyForProduction: false` instead of being shown as missing backend APIs. Managed vector memory, production proxy/webhook delivery, managed MCP registry, production alert routing, and production observability remain explicit production blockers. React Settings renders those rows, and `npm run agents:project-settings:integrations` validates API read/write, timeline/event proof, audit entries, file-backed persistence, and no production overclaim.
- Settings Integration Readiness now has a backend aggregate contract instead of leaving Integrations as a route list. `GET /projects/:id/settings-integration-readiness` returns `settings-integration-readiness/v1`, aggregating provider budget, Agent tool grants, Adapter Gateway preflight, Provider Readiness, Evidence Index readiness, Budget Alert readiness, and Error Reporting readiness into current status rows with action-required counts, checksums, validation commands, and production blockers. Manager Ready Package embeds `settingsIntegrationReadiness`, Readiness Proof Map exposes `settingsIntegrationReadinessRoutes`, React renders `settings-integration-readiness-contract`, and `npm run agents:settings-integration-readiness` validates the aggregate without launching a browser.
- The backend service/API now exposes `getEvidenceIndexReadiness` and `GET /projects/:id/evidence-index-readiness`. It exports `evidence-index-readiness/v1` with route-linked evidence-search, source-snapshot, provider-receipt, Agent-submission, and artifact-storage-proof rows, local gates, redaction scan status, related evidence/artifact/custody checksums, and explicit production blockers for managed vector storage, retrieval retention, and source-custody audit. `npm run agents:evidence-index-readiness` validates the empty-project blocked state, evidence/submission local-ready state, route linkage, file-backed persistence, and URL-token redaction.
- The backend service/API now exposes `getBudgetAlertReadiness` and `GET /projects/:id/budget-alert-readiness`. It exports `budget-alert-readiness/v1` with daily provider budget, hourly provider request, and provider usage ledger rows, local headroom gates, redaction scan status, project settings/provider readiness/provider controlled-run routes, and explicit production blockers for centralized metrics, alert routing, escalation/on-call ownership, and managed usage audit storage. Manager Ready Package embeds `budgetAlertReadiness`, and Readiness Proof Map exposes `budgetAlertReadinessRoutes`, so budget alert proof is visible from the formal Manager proof surfaces. `npm run agents:budget-alert-readiness` validates project budget policy persistence, route linkage, headroom calculation, Manager Ready Package proof, Readiness Proof Map proof, file-backed settings persistence, and no production alert-routing overclaim.
- The backend service/API now exposes `getErrorReportingReadiness` and `GET /projects/:id/error-reporting-readiness`. It exports `error-reporting-readiness/v1` with Operations Readiness-derived local log stream, alert rule, recovery runbook, incident drill, and local error-signal rows, local gates, redaction scan status, route links, and explicit production blockers for centralized logs, distributed traces, incident routing, on-call ownership, retention, and restore-drill evidence. Manager Ready Package embeds `errorReportingReadiness`, Readiness Proof Map exposes `errorReportingReadinessRoutes`, and `npm run agents:error-reporting-readiness` validates route linkage, Settings capability routing, local observability proof, Manager proof-surface coverage, file-backed project persistence, and no production observability overclaim.
- The backend service/API now also exposes `getProviderEvalRunWorkflow`, `recordProviderEvalRun`, and `GET|POST /projects/:id/provider-eval-runs`. It exports `provider-eval-run-workflow/v1` and records no-call `provider-eval-run/v1` shadow replay receipts that bind the controlled-run checksum to existing provider usage-ledger proof. The receipt requires critical `model:artifact-draft` and `search:evidence` proof, preserves policy/circuit decisions plus human-review/evidence/redaction boundaries, writes timeline and event-ledger proof, adds Manager Flow Graph provider-eval nodes, exposes Readiness Proof Map `providerEvalRunRoutes`, lists the route policy in Security Boundary, allows Manager/runtime-platform/security-admin reads/writes, renders `backend-provider-eval-run-workflow-snapshot`, and exports `provider_eval_runs` persistence rows. This closes the next gap between "these operations are allowed" and "we have an auditable private-pilot rehearsal receipt" without pretending real provider production rollout is complete.
- The backend service/API now also exposes `getProductionProviderControlReceiptWorkflow`, `recordProductionProviderControlReceipt`, and `GET|POST /projects/:id/production-provider-control-receipts`. It exports `production-provider-control-receipt-workflow/v1` and records checksummed `production-provider-control-receipt/v1` rows for provider allowlists, budgets/rate limits, Agent tool grants, retry/circuit breakers, provider audit/cost ledger, encrypted secret-vault proof, source safety review, source snapshot/provider receipt proof, model-output quality review, real-provider eval, managed provider audit/eval storage, centralized provider cost alerting, calibrated release policy, and incident runbooks. Receipts bind Provider Readiness, Provider Controlled Run, and Provider Eval Run checksums, write timeline and event-ledger proof, add Manager Flow Graph provider-control receipt nodes, expose Readiness Proof Map `productionProviderControlReceiptRoutes`, list the route policy in Security Boundary, allow runtime-platform/security-admin writes, render `backend-production-provider-control-receipts-snapshot`, update Production Launch Gap Register and Production Launch Control Center provider state, and export `production_provider_control_receipts` persistence rows. `npm run agents:product-team:production-provider-controls` is the focused proof for this slice. This closes the gap between "we rehearsed provider usage locally" and "production provider rollout controls can be submitted as auditable evidence" without bypassing the broader production launch audit/control center.
- The backend service/API now also exposes `getProductionDeploymentControlReceiptWorkflow`, `recordProductionDeploymentControlReceipt`, and `GET|POST /projects/:id/production-deployment-control-receipts`. It exports `production-deployment-control-receipt-workflow/v1` and records checksummed `production-deployment-control-receipt/v1` rows for enforced access, replay protection, audit fail-closed behavior, scheduler autostart, real persistence adapter, managed evidence custody, real queue adapter, environment promotion audit, rollback/smoke test, deployment change approval, and production domain/TLS evidence. Receipts bind Deployment Preflight, adapter dry-runs, access hardening, custody, queue, and operations proof, write timeline and event-ledger proof, add Manager Flow Graph deployment-control receipt nodes, expose Readiness Proof Map `productionDeploymentControlReceiptRoutes`, list the route policy in Security Boundary, allow runtime-platform/operations-owner/security-admin writes, render `backend-production-deployment-control-receipts-snapshot`, update Production Launch Gap Register and Production Launch Control Center deployment state, and export `production_deployment_control_receipts` persistence rows. This closes the gap between "deployment preflight flags exist" and "deployment hardening controls can be submitted as auditable evidence" without bypassing the broader production launch audit/control center.
- The backend service/API now also exposes `getPrivatePilotReleaseCandidateWorkflow`, `recordPrivatePilotReleaseCandidate`, and `GET|POST /projects/:id/private-pilot-release-candidates`. It exports `private-pilot-release-candidate-workflow/v1` and records checksummed `private-pilot-release-candidate/v1` freeze receipts only after launch approvals, production launch audit private-pilot gates, project evidence export package download audit, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage are ready. The receipt freezes Manager Ready Package, MVP/pilot/deployment readiness, production launch audit, project evidence archive/export package, provider eval run, operations, persistence adapter, and worker queue adapter checksums, writes timeline and event-ledger proof, adds Manager Flow Graph release-candidate decision nodes, exposes Readiness Proof Map `privatePilotReleaseCandidateRoutes`, lists the route policy in Security Boundary, allows Manager/security-admin/operations-owner writes, renders `backend-private-pilot-release-candidate-workflow-snapshot`, and exports `private_pilot_release_candidates` persistence rows. This closes the private-pilot release-candidate proof gap without pretending public production controls are complete.
- The backend service/API now also exposes `getPrivatePilotLaunchRunWorkflow`, `recordPrivatePilotLaunchRun`, and `GET|POST /projects/:id/private-pilot-launch-runs`. It exports `private-pilot-launch-run-workflow/v1` and records checksummed `private-pilot-launch-run/v1` activation receipts only after a release candidate is frozen and launch audit, evidence handoff package, archive/provider/proof/deployment/operations/security gates remain ready. The receipt freezes release-candidate, launch audit, evidence, provider eval, deployment, operations, persistence adapter, and worker queue adapter checksums, writes timeline and event-ledger proof, adds Manager Flow Graph launch-run decision nodes, exposes Readiness Proof Map `privatePilotLaunchRunRoutes`, lists the route policy in Security Boundary, allows Manager/security-admin/operations-owner writes, renders `backend-private-pilot-launch-run-workflow-snapshot`, and exports `private_pilot_launch_runs` persistence rows. This turns the release candidate into a controlled private-pilot activation record without pretending public production go-live controls are complete.
- The backend service/API now also exposes `getPrivatePilotLaunchHealthCheckWorkflow`, `recordPrivatePilotLaunchHealthCheck`, and `GET|POST /projects/:id/private-pilot-launch-health-checks`. It exports `private-pilot-launch-health-check-workflow/v1` and records checksummed `private-pilot-launch-health-check/v1` receipts only after a launch run exists and operations, worker queue adapter, persistence adapter, security boundary, provider eval, evidence archive, Flow Graph, and Proof Map gates remain healthy. The receipt freezes launch-run and health checksums, writes timeline and event-ledger proof, adds Manager Flow Graph monitoring nodes, exposes Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, lists the route policy in Security Boundary, allows Manager/security-admin/operations-owner writes, renders `backend-private-pilot-launch-health-check-workflow-snapshot`, and exports `private_pilot_launch_health_checks` persistence rows. This turns launch activation into a monitorable private-pilot state without pretending centralized production observability or on-call controls are complete.
- The backend service/API now also exposes `getPrivatePilotAcceptanceReportWorkflow`, `recordPrivatePilotAcceptanceReport`, and `GET|POST /projects/:id/private-pilot-acceptance-reports`. It exports `private-pilot-acceptance-report-workflow/v1` and records checksummed `private-pilot-acceptance-report/v1` receipts only after release candidate, launch run, post-launch health, evidence handoff, generic product-team delivery proof, operations/security/provider proof, Flow Graph, and Proof Map gates remain ready. The report freezes release/launch/health/evidence/Flow Graph/Proof Map checksums, writes timeline and event-ledger proof, adds Manager Flow Graph decision nodes, exposes Readiness Proof Map `privatePilotAcceptanceReportRoutes`, lists the route policy in Security Boundary, allows Manager/security-admin/operations-owner writes, renders `backend-private-pilot-acceptance-report-workflow-snapshot`, and exports `private_pilot_acceptance_reports` persistence rows. This closes the customer private-pilot acceptance proof gap without pretending public production controls are complete.
- Launch approval and the four private-pilot receipt POST routes now support `includeReadModels: false`. In receipt-first mode approval, release candidate, launch run, launch health, and acceptance writes return the checksummed receipt, updated workflow, timeline/event proof, and explicit Manager Ready Package / Flow Graph / Proof Map / launch, evidence export, audit, and go-live refresh routes instead of embedding a full Manager Ready Package on every write response. This keeps the C-side command path responsive while preserving the A-side backend proof boundary.
- The backend service/API now also exposes `getPrivatePilotGoLiveReadiness` and `GET /projects/:id/private-pilot-go-live-readiness`. It exports `private-pilot-go-live-readiness/v1`, a read-only Manager command view that aggregates generic delivery proof, launch approvals, evidence handoff, provider eval, deployment/operations/security preflight, release candidate, launch run, post-launch health, customer acceptance, and production operations hardening into stage rows, active phase, next action, proof ids, timeline/event ids, backend routes, and checksum. Manager Ready Package embeds the command view, Manager Flow Graph adds a `private-pilot-go-live-readiness` decision node, Readiness Proof Map exposes `privatePilotGoLiveRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders `backend-private-pilot-go-live-readiness-snapshot`. This gives managers a single private-pilot go-live/acceptance status surface while keeping `readyForProduction: false` until broader production controls are independently verified.
- The backend service/API now also exposes `getProductionLaunchGapRegister` and `GET /projects/:id/production-launch-gap-register`. It exports `production-launch-gap-register/v1`, a read-only Manager action register that deduplicates remaining production blockers from production launch audit, production evidence integrity audit, deployment preflight, production operations readiness, provider readiness, evidence custody, artifact quality, and submission review governance into gap rows with domain, owner, severity, action, route, proof ids, timeline/event ids, upstream checksums, next action, and checksum. Manager Ready Package embeds the register, Manager Flow Graph adds a `production-launch-gap-register` decision node, Readiness Proof Map exposes `productionLaunchGapRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders `backend-production-launch-gap-register-snapshot`. This turns "what is still missing before public production?" into an auditable owner-routed list while keeping `readyForProduction: false`.
- The backend service/API now also exposes `getProductionLaunchControlCenter` and `GET /projects/:id/production-launch-control-center`. It exports `production-launch-control-center/v1`, a read-only Manager release control view that aggregates production launch audit, gap register, private-pilot go-live readiness, production operations receipts, production deployment receipts, production security receipts, production provider receipts, production evidence integrity audit, launch approvals, deployment preflight, provider readiness, security boundary, evidence custody, and artifact quality into gate rows, blocked rows, owner rows, stage rows, next action, proof ids, timeline/event ids, upstream checksums, and checksum. Manager Ready Package embeds the control center, Manager Flow Graph adds a `production-launch-control-center` decision node, Readiness Proof Map exposes `productionLaunchControlCenterRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders `backend-production-launch-control-center-snapshot`. This turns "are we allowed to move toward public production?" into a no-go/go control surface while keeping `productionDecision: no-go`.
- The backend service/API now also exposes `getProductionLaunchEvidenceDossier` and `GET /projects/:id/production-launch-evidence-dossier`. It exports `production-launch-evidence-dossier/v1`, a read-only Manager launch evidence package that aggregates production launch audit, gap register, control center, production evidence integrity audit, private-pilot go-live readiness, deployment/security/provider/operations readiness, and production control receipt workflows into manifest entries, four control-domain rows, open gaps, evidence-tier summary, launch state, proof ids, timeline/event ids, event ids, backend routes, summary counts, and checksum. Manager Ready Package embeds the dossier, Manager Flow Graph adds a `production-launch-evidence-dossier` node fed by the control center, gap register, and evidence integrity audit, Readiness Proof Map exposes `productionLaunchEvidenceDossierRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders `backend-production-launch-evidence-dossier-snapshot`. This turns "what evidence package will we show for launch review?" into an auditable dossier while keeping `productionDecision: no-go`.
- The backend service/API now also exposes `getProductionEvidenceIntegrityAudit` and `GET /projects/:id/production-evidence-integrity-audit`. It exports `production-evidence-integrity-audit/v1`, a read-only audit over production operations, deployment, security, and provider control receipts. The audit derives the latest receipt per required control, preserves `evidenceEnvironment`, classifies evidence as `missing`, `local-rehearsal`, `external-unattested`, or `managed-production`, returns control rows, domain rows, proof ids, timeline/event ids, backend routes, summary counts, and checksum, adds Manager Flow Graph / Readiness Proof Map evidence surfaces, lists a Security Boundary/access-policy route, renders `backend-production-evidence-integrity-audit-snapshot` in Manager UI, gates `production-launch-audit/v1`, and opens/closes the matching production gap-register row. This closes the gap between "control receipts exist" and "those receipts are actually managed-production evidence" without allowing local rehearsal receipts to become public-production proof.
- Manager UI fallback auditing now covers the production launch/control/evidence/receipt surface: backend-online real projects render `backend-model-missing` / `backend-required` when standalone production submodels are absent, while derived production fallback rows remain limited to offline or explicit demo/sample projects. This prevents C-side launch panels from inventing A-side backend evidence.
- The backend service/API now also exposes `getProductionOperationsReadiness` and `GET /projects/:id/production-operations-readiness`. It exports `production-operations-readiness/v1`, aggregating private-pilot acceptance, post-launch health, local operations incident drill, security audit stream, provider eval replay, persistence adapter dry-run, worker queue adapter dry-run, and production launch audit into local proof gates and production control gates. Manager Ready Package embeds it, Manager Flow Graph adds a production operations hardening node/edge, Readiness Proof Map exposes `productionOperationsReadinessRoutes`, Security Boundary lists the route policy, enforced access allows Manager/security-admin/runtime-platform/operations-owner reads, and Manager UI renders `backend-production-operations-readiness-snapshot`. `npm run agents:product-team:private-pilot:ops-readiness` now verifies this P1-to-P2 bridge after customer acceptance. This closes the gap between private-pilot customer acceptance and explicit production operations hardening without pretending centralized observability, on-call, incident, restore, audit-retention, or managed infrastructure controls are complete.
- The backend service/API now also exposes `getProductionOperationsControlReceiptWorkflow`, `recordProductionOperationsControlReceipt`, and `GET|POST /projects/:id/production-operations-control-receipts`. It exports `production-operations-control-receipt-workflow/v1` and records checksummed `production-operations-control-receipt/v1` rows for centralized logs, metrics, traces, alert routing, on-call ownership, managed incident records, restore drill receipts, managed persistence/queue cutover evidence, and centralized audit retention. Receipts write timeline and event-ledger proof, update Production Operations Readiness, add Manager Flow Graph receipt nodes, expose Readiness Proof Map `productionOperationsControlReceiptRoutes`, list the route policy in Security Boundary, allow security-admin/operations-owner writes, render `backend-production-operations-control-receipts-snapshot`, and export `production_operations_control_receipts` persistence rows. Verified managed persistence and worker queue cutover receipts now also project into `production-infrastructure-rehearsal/v1` domain rows as `cutoverReceiptReady`, clearing those infrastructure blockers while the broader public-production launch audit can still remain `no-go`. This turns production operations hardening from a checklist into auditable evidence without bypassing the broader production launch audit.
- The backend service/API now also exposes `getProductionSecurityControlReceiptWorkflow`, `recordProductionSecurityControlReceipt`, and `GET|POST /projects/:id/production-security-control-receipts`. It exports `production-security-control-receipt-workflow/v1` and records checksummed `production-security-control-receipt/v1` rows for managed identity provider, service identity boundary, managed KMS/Secret Manager, database-backed RBAC, centralized security audit, and session replay hardening. Receipts write timeline and event-ledger proof, update Security Boundary production-security status, add Manager Flow Graph receipt nodes, expose Readiness Proof Map `productionSecurityControlReceiptRoutes`, list the route policy in Security Boundary, allow security-admin writes, render `backend-production-security-control-receipts-snapshot`, and export `production_security_control_receipts` persistence rows. This turns production security hardening from a checklist into auditable evidence without bypassing the broader production launch audit/control center.
- The backend service/API now exposes `getEvidenceQualityAudit` and `GET /projects/:id/evidence-quality-audit`. It exports `evidence-quality-audit/v1` with redacted evidence-search rows, flattened source rows, quality/source-safety summaries, provider provenance, Proof Map routes, decision gates, production controls, and checksum. Manager Ready Package embeds the audit, Project Evidence Archive adds it to readiness models and manifest, Pilot/Production launch evidence routes include it, enforced access policy covers Manager/security-admin/observer reads, and the Manager UI renders `backend-evidence-quality-audit-snapshot`. This closes the gap between raw evidence-search records and the Manager question: whether the current evidence set is decision-ready.
- The backend service/API now exposes `getEvidenceSourceReviewWorkflow`, `reviewEvidenceSource`, and `GET|POST /projects/:id/evidence-source-review-workflow`. It exports `evidence-source-review-workflow/v1` with reviewer-visible source review items, review queue, source quality/source-safety signals, source snapshot ids/checksums, provider receipt ids, reviewer handoff, latest decisions, local/production decision use, proof routes, gates, production controls, and checksum. `POST` persists `evidence-source-review/v1` decisions and writes group-chat, timeline, event-ledger, Agent Dashboard, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, access policy, and `evidence_source_reviews` persistence proof. Manager Ready Package embeds the workflow, Project Evidence Archive adds workflow, snapshot, receipt, and decision records, Pilot/Production launch evidence routes include it, enforced access policy covers Manager/security-admin/observer reads plus Manager/reviewer-agent/security-admin writes with reviewer identity matching, and the Manager UI renders decision counts plus source snapshot/provider receipt coverage. This closes the gap between automated source scoring and a manager/reviewer-visible source governance queue while keeping production source review blocked until human approval policy and managed immutable source/provider/reviewer audit storage exist.
- The backend service/API now exposes `getSecurityBoundary` and `GET /projects/:id/security-boundary`. It exports route policies, the `access-control-policy/v1` contract, optional signed access-header metadata, optional signed request replay-protection metadata, optional audit fail-closed metadata, optional project membership policy metadata, local `secret-vault-status/v1`, sensitive-field coverage, provider boundary status, redaction scan counts/checksums, persisted access-audit summary/route, backend audit-stream hash-chain summary/route, and explicit identity/RBAC/secret-vault/audit/abuse-control blockers. The API can run in enforced mode from request headers and reject cross-Agent reads/submissions, Reviewer identity mismatches, observer persistence exports, unsigned enforced requests when a signing secret is configured, tampered signed identity headers, duplicate signed request ids when replay protection is enabled, audit-sink failures when fail-closed mode is enabled, and signed-but-nonmember project access before dispatch. File-backed APIs persist accepted signed request ids as `accessReplayRecords`, so duplicate replay is still rejected after a local backend restart. The backend also exposes `GET|PUT /projects/:id/membership-policy`, persists a `project-membership-policy/v1` with role grants, Agent/Reviewer runtime bindings, revoked user/Agent lists, revision metadata, timeline proof, and a `project-membership-policy-updated` event, then enforces that stored policy when `requireProjectMembership` is enabled. Membership-policy writes support `includeReadModels: false`, returning receipt proof plus Security Boundary / audit-stream / membership refresh routes instead of embedding large Manager read models. Each enforced allow/deny decision is written to `securityAccessAudit`, linked into the event ledger as `security-access`, mirrored into the backend security audit stream with sequence/checksum/hash-chain proof, appended to a JSONL audit sink when file-backed, exposed through `GET /projects/:id/security-access-audit` and `GET /projects/:id/security-audit-stream`, and exported through the persistence snapshot as `project_membership_policies`, `project_membership_grants`, `security_access_audit`, and `security_audit_stream`. This does not replace authentication, managed KMS/Secret Manager storage, database-backed authorization, durable shared replay storage for multi-instance production, database row-level authorization, or immutable centralized audit storage with alerting/recovery, but it gives the secret-vault/RBAC/audit production blocker a concrete hardening contract and makes the boundary visible in Manager Ready Package.
- The backend service/API now exposes `GET|POST /projects/:id/identity-sessions` plus `POST /projects/:id/identity-sessions/:sessionId/revoke`. It exports a local `identity-session/v1` runtime credential contract: tokens are returned once, only token hashes/checksums/status/expiry/revocation proof are persisted, `x-hofs-session-token` converts into enforced actor headers, project membership checks still run, verified session decisions are written into `securityAccessAudit` and the backend audit stream, revoked tokens fail closed, Security Boundary and Manager Ready Package summarize active/revoked sessions, and the persistence/migration path exports `identity_sessions`. Identity-session issue/revoke writes support `includeReadModels: false`, returning the one-time token contract or revocation receipt plus explicit identity-session, Security Boundary, Security Access Audit, and Security Audit Stream refresh routes without embedding Manager Ready Package or Security Boundary snapshots. This is local/private-MVP coverage only; production still needs a real IdP/session store/service credential issuer, rotation, audience binding, shared replay controls, centralized audit, and incident response.
- The kickoff portion of that read model includes concrete conversation rows for role clarification, self-nomination, and Leader campaign speeches, so backend clients can inspect the actual meeting turns instead of only seeing counts. Manager Flow Graph now emits proofed role-clarification conversation nodes plus first-class `self-marketing` nodes for role self-nominations and Leader campaign pitches, including transcript proof, event-ledger proof, heard-by participants, and attachments.
- The same read model includes `kickoffExecutionFlow`, connecting charter next actions, Leader assignment evidence, first-pulse scheduler/chat/timeline proof, and 24/7 readiness.
- That aggregate manager read model now includes `agentCommunicationFlow`, which traces Agent-authored messages from sender worklog to target inbox/obligation state and exact transcript proof. The backend now records `agent-message` timeline logs for those Agent-authored messages. Readiness Proof Map mirrors the same rows as `agentMessageRoutes` / `agentMessageSummary`, carrying transcript route, timeline ids, event ids, sender/target Agent ids, delivery state, and `readyForAgentMessageDelivery`; Manager Flow Graph already renders the matching `agent-message-*` communication nodes. This gives Agent-to-Agent collaboration a Dashboard, Timeline/Event, Flow Graph, and Proof Map proof surface instead of only a chat-card view.
- The backend service/API also exposes `getAgentDashboard` and `GET /projects/:id/agents/:agentId/dashboard`, a per-Agent aggregate read model containing the Agent's inbox, obligations, private worklog, current plan, next run, latest worker, owned task evidence, management relationships, management proof logs, relevant chat/timeline/event proof ids, and route hints.
- React Agent Focus now consumes that route as a required backend read model for backend-online real projects. It labels the Agent Dashboard source, shows `agent-focus-backend-dashboard-required-*` when the route has not returned, and keeps browser-local Agent state visible only as project snapshot context rather than as backend Agent Dashboard evidence. The browser Harness asserts the synced Agent Focus source is `backend-backed`.
- The backend service/API now exposes Agent-authored communication through `submitAgentMessage` and `POST /projects/:id/agents/:agentId/message`. The sender is resolved from the URL, target Agents are explicit, receipts use the same group-chat delivery path, target inboxes and sender worklogs update immediately, and `eventLedger` stores the message with `source: agent-to-agent-message`.
- The backend service/API now exposes Agent-authored artifact submission through `submitAgentArtifact` and `POST /projects/:id/agents/:agentId/submissions`. The sender is resolved from the URL, the artifact type is generic to product-team work (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`, etc.), the artifact is written through the local runtime artifact writer when configured, group-chat proof is published, the task evidence links are updated, and `eventLedger` stores an `agent-submission` event.
- The backend service/API now exposes `getBrainstormLayer` and `GET /projects/:id/brainstorm-layer`. It exports `brainstorm-layer/v1`, deriving a Manager-readable brainstorm aggregate from generic `brainstorm-board` submissions, parsed alternatives, linked discovery/evidence/downstream decision artifacts, chat/timeline/event proof, backend routes, gates, summary counts, and checksum. Manager Dashboard and Manager Ready Package embed it, Manager Flow Graph adds a `brainstorm-layer` aggregate node with synthesis edges, Readiness Proof Map exposes `brainstormLayerRoutes`, enforced access covers Manager/security-admin/observer reads, Manager UI renders `backend-brainstorm-layer-snapshot`, and the product-team Harness verifies the standalone API plus HTTP endpoint. This turns brainstorming into a first-class generic product-team layer without adding research-only fields.
- `getAgentDashboard` now embeds `agent-brainstorm-contribution/v1`, so the Agent who submitted a `brainstorm-board` can see their own visible alternatives, submission proof, timeline/event proof, project evidence/downstream follow-through counts, and route back to the Manager brainstorm layer. The Manager aggregate remains the cross-team synthesis surface, while the Agent dashboard now preserves individual initiative and self-positioning evidence. The product-team Harness verifies the Da Vinci brainstorm contribution through both the service API and enforced-mode HTTP Agent Dashboard.
- The backend service/API now exposes Agent-authored artifact draft generation through `generateAgentArtifactDraft` and `POST /projects/:id/agents/:agentId/artifact-drafts`. The route returns `agent-artifact-draft/v1`, gathers task/evidence/prior-submission/review context, uses a local deterministic draft generator when the model provider is disabled or bypassed, gates model execution behind provider operation `model:artifact-draft`, writes provider-usage proof for model-backed drafts, attaches `artifact-draft-quality/v1` gates, marks model-authored drafts as human-review required, and can pass the generated body into the normal `submitAgentArtifact` flow with `submit: true`.
- The backend service/API now exposes `getArtifactQualityAudit` and `GET /projects/:id/artifact-quality-audit`. It exports `artifact-quality-audit/v1` with redacted Agent submission rows, required generic artifact type coverage, title/summary/body readiness, chat/timeline/event/artifact proof links, review/revision/final-deliverable closure, generated-draft quality status, redaction status, local gates, production controls, and checksum. Manager Ready Package embeds the audit, Readiness Proof Map exposes `artifactQualityRoutes`, Project Evidence Archive adds it to readiness models/manifest/integrity gates/full contents, Pilot/Production launch evidence routes include it, enforced access policy covers Manager/security-admin/observer reads, and the Manager UI renders `backend-artifact-quality-audit-snapshot`. This closes the gap between "submissions exist" and the Manager question: whether the generic product-team artifacts are locally ready for private-pilot handoff.
- The backend service/API now exposes `getSubmissionReviewWorkflow` and `GET /projects/:id/submission-review-workflow`. It exports `submission-review-workflow/v1` with generic review rounds, requested changes, linked revision responses, accepted final-deliverable proof, proof routes, timeline/event ids, local gates, production controls, and checksum. Manager Ready Package embeds the workflow, Manager Flow Graph adds a `submission-review-workflow` closure node with review/revision edges, Readiness Proof Map exposes `submissionReviewWorkflowRoutes`, Security Boundary and enforced access policy cover Manager/security-admin/observer reads, and the Manager UI renders `backend-submission-review-workflow-snapshot`. This closes the gap between individual review rows and the Manager question: whether requested changes, revision, and final acceptance closed locally without claiming production reviewer governance.
- The product-team Harness requires a generated `progress-brief` draft to become a first-class submission with artifact file, timeline proof, event-ledger proof, task evidence, Flow Graph submission node, Readiness Proof Map route, archive checksum, Manager/Agent dashboard provenance, and persistence rows. This proves the product-team artifact workflow no longer depends only on hand-written request payloads.
- The project service/API exposes `GET /projects/:id/submissions` and `GET /projects/:id/submissions/:submissionId`, so submitted artifacts are independently queryable project resources rather than only timeline side effects.
- Agent submissions now support explicit revision lineage through `revisesSubmissionId`, `respondsToReviewId`, and `supersedesSubmissionIds`. Linked revisions mark prior submissions `superseded`, resolve matching review obligations, appear in Manager Dashboard revision/superseded counts, create Manager Flow Graph `revision` edges, and expose `revisionRoutes` plus `revisionSummary` in Readiness Proof Map.
- Manager Dashboard includes a submissions summary with counts by review/final-deliverable state, Agent Dashboard exposes owned submissions, Task Evidence returns submissions linked to the task, Manager Flow Graph creates first-class `submission` nodes from `project.agentSubmissions`, and Readiness Proof Map includes `submissionRoutes` plus `submissionSummary`.
- The backend service/API now exposes Agent evidence searches through `recordAgentEvidenceSearch` and `POST /projects/:id/agents/:agentId/evidence-searches`. The record stores query, purpose, provider/search mode, confidence, findings, source refs, task/submission links, chat proof, timeline proof, and an `evidence-search` ledger event. It also computes source quality signals, per-source quality score/level, aggregate `evidenceJudgement`, `qualityScore`, and `qualitySummary`, making evidence judgement a backend contract rather than a prose-only review note. `GET /projects/:id/evidence-searches` and `GET /projects/:id/evidence-searches/:id` make those records independently queryable.
- The backend service/API now exposes formal submission reviews through `reviewAgentSubmission` and `POST /projects/:id/submissions/:submissionId/reviews`. Reviewer verdicts update the linked submission/task, create submitter obligations when changes are requested, publish group-chat review proof, write timeline proof, and append `submission-review` ledger events. `GET /projects/:id/submission-reviews` and `GET /projects/:id/submission-reviews/:id` make review records independently queryable. The Manager Dashboard now provides a C-side review composer on Agent submission rows so this review loop is operational from the product surface, not only from Harness/API calls.
- The backend service/API now exposes marketplace-to-project Agent contracting through `contractProjectAgent` and `POST /projects/:id/agents/contract`. It writes `agent-contract/v1` records, team roster updates, Agent state/worklog proof, an `agent-contracted` timeline log, an `agent-contracted` event-ledger entry, deferred Manager/Agent read-model refresh routes, and a Manager Flow Graph collaboration node. React now uses this route for real projects instead of adding marketplace Agents through browser-only team mutations, and the manager scenario Harness verifies the isolated API path plus Agent Dashboard and Flow Graph proof.
- Manager Dashboard includes evidence-search quality summaries and submission-review summaries, Agent Dashboard exposes owned evidence searches and relevant reviews, Task Evidence returns linked searches/reviews with quality judgement, Manager Flow Graph creates first-class quality-bearing `evidence` and `review` nodes, and Readiness Proof Map includes evidence-search quality routes plus submission-review routes.
- The backend now includes `searchProvider.js` plus `GET /search/status` and `POST /search/test`. Provider status reports configured/enabled/provider/endpoint-redacted metadata without returning API keys and can expose `apiKeySource: local-secret-vault` plus vault readiness metadata. `secretVault.js` seals/opens fake provider keys, rotates them under a new local key id, and exports only `secret-vault-status/v1` plus `secret-vault-rotation-receipt/v1` metadata; `secretRedaction.js` is applied before persisting provider errors, evidence sources, Agent submissions/source refs, review comments, task source refs, artifact drafts, and ledger payloads. The product-team Harness uses the deterministic provider path through `useProvider: true`, injects fake secret fixtures, scans the file-backed store and workspace artifact files, and proves evidence-search can be provider-backed while production deployments can substitute a private `http-json` search gateway.
- React Settings now consumes those provider status/test routes instead of presenting browser-owned deployment, model, BYOK, Proxy/Webhook, MCP, Vector Store, Budget Alerts, or Error Reporting setup. The Deployment tab renders `settings-deployment-runtime-boundary`, syncs `/settings/runtime-readiness` or `/projects/:id/settings-runtime-readiness`, and points to backend worker/provider/deployment readiness routes; the Models tab renders `settings-model-runtime-boundary`, reads `/llm/status`, and renders the same runtime-readiness contract instead of browser-only model selectors; the Keys tab renders `settings-provider-boundary`, syncs `/settings/provider-readiness`, `/settings/runtime-readiness`, `/llm/status`, `/search/status`, `/secret-vault/status`, `/secret-vault/records`, and `/provider-vault-bindings`, exposes `/llm/test` and `/search/test` as backend tests, shows redacted provider/vault fields, and lets users type API values while backend `settings-provider-readiness/v1` controls whether `Seal` is enabled. Manager Ready Package embeds `settingsProviderReadiness` / `settingsRuntimeReadiness` / `settingsIntegrationReadiness`, and Readiness Proof Map exposes `settingsProviderReadinessRoutes` / `settingsRuntimeReadinessRoutes` / `settingsIntegrationReadinessRoutes`. Integrations renders `settings-integration-readiness-contract` from `/projects/:id/settings-integration-readiness`, plus route rows for Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting instead of fake editable adapter/tool/alert/logging controls. The browser still never persists provider secrets. `ui:manager-provider-proof` opens all three settings boundaries in a real browser and rejects a return to frontend token paste or static deployment/model selectors, while the product-team Harness, `agents:project-settings:integrations`, `agents:settings-provider-readiness`, `agents:settings-runtime-readiness`, `agents:settings-integration-readiness`, `agents:budget-alert-readiness`, and `agents:error-reporting-readiness` statically/API-verify the same contract.
- React Agent Focus now uses that provider boundary for manager-triggered evidence searches. The Workbench posts `useProvider: true` first, records a `manual-source-record` only when the user supplied an explicit URL after provider failure, and blocks the old no-source `agent-note` fallback. Static and browser gates now pin this so real initiated projects cannot turn browser draft text into evidence-search proof without a provider result or an explicit source.
- The project dashboard now surfaces that route in each Team row as an `Agent Message` control, so managers can initiate a real backend Agent-to-Agent message, then verify target inbox proof and sender worklog proof from the same screen.
- The project dashboard also renders `Agent Communication Flow`, so the manager can scan Agent-to-Agent communication as a proof chain instead of searching through group chat, Agent inboxes, and worklogs separately.
- The React `Backend Worker Station` now fetches and renders that aggregate as `Backend Manager Snapshot` after online backend actions and through a manual `Sync Manager View` control, so managers can verify the backend's own readiness, proof route, operations, management, assignment, and change counts from the dashboard itself.
- `README.md` documents collaboration invariants.

Status: covered.

## Verification Evidence

Current verification is backend-contract, browser-flow, and persona-package level:

- `npm run build` validates the React/Vite production bundle.
- `npm run skills:check` validates 40 canonical persona packages plus 55 routing regression cases.
- `npm run agents:scenario` validates the Manager scenario data path, backend API, scheduler, Agent worker, kickoff, chat/change, review/revision/final-deliverable, and receipt-first read-model routes.
- `npm run agents:product-team:smoke` validates the generic product-team loop in memory with no browser/server/file-backed store/workspace artifact writes, from kickoff through meeting turns, evidence, brainstorm, draft, review/revision, final delivery, Flow Graph, Proof Map, Agent Dashboard, transcript, timeline, event, and Evidence Index readiness. `npm run agents:real-user-zero-to-autonomy` validates the same customer startup chain against the real `agents:server` process, Secret Vault, sealed model/search providers, HTTP API routes, provider-backed evidence, model-backed draft, all required generic artifact types, Artifact Quality Audit, and proof surfaces without browser/build writes. `npm run agents:product-team:core` validates the fuller file-backed/HTTP generic product-team loop through persistence, security boundary, provider readiness, and worker queue surfaces.
- `npm run agents:product-team:research-sample` validates Research Project as a generic product-team acceptance sample, proving Product Team Delivery Trace, Operating Loop, three-step autonomous consistency, Runtime Autonomy Status, proof surfaces, and production blockers without paper/thesis/manuscript-specific protocol drift.
- `npm run agents:product-team:cycle-consistency` validates signed membership access plus a bounded three-step autonomous loop, `autonomous-cycle-consistency/v1`, Manager Flow Graph, Readiness Proof Map, Ready Package, and persisted loop/run receipts.
- `npm run ui:manager-backend` validates the browser C/A path: Manager commands, backend Agent work, Agent Focus submissions, evidence search, generated draft, Reviewer changes-requested review, linked revision/final deliverable, accepted final review, and backend Manager/Flow/Proof/Agent readback.
- `npm run agents:product-team:private-pilot:ops-readiness` validates the backend P1-to-P2 bridge after customer acceptance, including private-pilot launch approvals, evidence handoff, release candidate, launch run, health check, acceptance report, production-operations-readiness proof, and explicit public-production blockers.
- `npm run ui:manager-private-pilot` validates the browser private-pilot handoff path by preparing an isolated launch-handoff backend store, clicking Manager/security launch approvals, evidence export request/approval/download-audit, release-candidate, launch-run, launch-health, and acceptance-report receipt buttons, then verifying Flow Graph and Proof Map evidence.
- `npm run launch:infra` validates the P2 infrastructure rehearsal boundary: launch gate matrix, shared adapter gateway contract, runnable private adapter gateway server, Agent HTTP server `http-json` gateway mode, and Postgres-compatible gateway storage read/write/readback parity while keeping public production cutover blocked.
- `npm run agents:product-team:production-ops-controls` validates managed-persistence, managed-queue, audit, observability, incident, restore, and recovery operation receipts as production-hardening rehearsal evidence.
- `npm run agents:product-team:production-deployment-controls` validates deployment preflight, rollback, release, environment, and cutover receipts as auditable production-hardening rehearsal evidence.
- `npm run agents:product-team:production-security-controls` validates managed identity, KMS/secret manager, RBAC/RLS, signed access, replay protection, and centralized security-audit receipts across Security Boundary, Proof Map, Flow Graph, and Launch Control.
- `npm run agents:product-team:production-provider-controls` validates provider/BYOK allowlist, budget/rate-limit, tool grant, retry/circuit-breaker, eval/audit storage, cost alerting, release policy, and incident-runbook receipts across provider and launch surfaces.
- `npm run agents:product-team:production-evidence-integrity` validates that local/test production-control receipts remain local rehearsal evidence until explicit `managed-production` evidence upgrades the evidence-integrity audit.
- `npm run agents:product-team:production-launch-governance` validates signed Manager, security-admin, and operations-owner launch approvals through backend membership, with checksum/proof/timeline/event evidence visible in Launch Audit, Control Center, Proof Map, and Flow Graph.
- `npm run launch:gates` validates that the launch matrix stays tied to runnable scripts, generic product-team evidence, and explicit production blockers.

## Remaining Non-Blocking Cleanup

- `runRoomSimulation` still contains unreachable legacy simulation code after an early `return`. It is not executed and does not affect runtime behavior, but should be cleaned when editing the large legacy React file is safer.
- The React scheduler is still prototype-local, but the Node HTTP server now has an optional autonomous scheduler loop backed by the file store. A production backend can replace the interval runner with cron/queue infrastructure and move storage to an append-only ledger table while preserving the same scheduler ledger contract.

## Manager Scenario Coverage

### Kickoff with Leader election

Evidence:

- `createKickoffRoleNegotiation` generates the pre-election role clarification transcript, including "what should I own?" questions and self-nomination statements.
- The project initiation meeting step renders that runtime-generated transcript before the result screen, so the manager sees the meeting itself instead of only the final project record.
- Initiation transcript cards show the peers that heard each turn, and `createKickoffCharter` persists role and Leader hearing edges as durable evidence.
- `approveInitiationProject` appends that transcript to group chat before the Leader election so Agents hear each other before governance is confirmed.
- `createLeaderElection` generates candidate campaign statements and a recommended leader.
- The initiation result screen renders Leader candidates and lets the Director choose the confirmed Leader.
- `approveInitiationProject` writes the confirmed Leader into team state with `isLeader`.
- `createKickoffCharter` persists the approved meeting result, Leader, Reviewer, team roster, next actions, communication rules, and evidence ids.
- Real initiation and manager-demo kickoff chat messages preserve the runtime transcript ids stored in `kickoffCharter.evidence`, and the dashboard exposes a `Kickoff chat proof` jump that highlights those group-chat records.
- `createKickoffCharter` now emits unified ledger events for role questions, self-nominations, Leader campaign speeches, and the final approved charter, so the kickoff meeting is replayable from the backend event stream.
- Group-chat messages now carry `heardBy`, `directTargetIds`, per-Agent `receipts`, and a `visibility` summary, while timeline logs created from those messages preserve `receiptCount` and `directTargetIds`.
- This gives the manager a durable proof trail that the team could hear a message and that directly mentioned Agents saw the request, even when old chat records are later recovered from logs.
- `Group Chat Transcript Index` renders project-scoped channel transcripts on the dashboard with message counts, archived proof recovery counts, latest speaker/message, receipt coverage, direct mentions, and proof jumps into the exact chat transcript.
- `GET /projects/:id/transcripts` and `GET /projects/:id/transcripts/:channelId` expose the same transcript read model through the backend service/API/HTTP stack, including current messages and recovered proof messages rebuilt from kickoff transcripts and timeline logs.
- The React dashboard now syncs those backend transcript routes after backend project load/sync, chat or meeting commands, worker pulses, Agent write receipts, manual transcript sync, and channel changes. Browser chat history remains an offline/cache fallback, while backend-online transcript rows become the preferred client observation surface for Agent collaboration. If the backend transcript read model is missing for a real project, the dashboard shows a backend-required state instead of counting local recovered messages as collaboration proof.
- The same Transcript Index now renders backend-derived collaboration proof rows for evidence searches, Agent submissions, submission reviews, revision submissions, and final deliverables. The live Group Chat stream also renders backend `evidence-search`, `submission`, `submission-review`, and `evidence-source-review` messages as `chat-collaboration-node-*` cards with route text, proof counts, receipts, timeline proof actions, and Flow Graph node actions. `npm run ui:manager-backend` verifies a real initiated project can open evidence-search, final-deliverable, and submission-review chat proof plus Agent-submission timeline proof, see the corresponding live chat collaboration cards, jump from the final-deliverable chat card into the matching Manager Flow Graph node after the full review/revision/final acceptance loop closes, see the backend submission route in the selected Flow Graph detail, and return from that detail to Manager Proof Map.
- Ordinary group-chat messages are applied through `applyChatMessagesToAgentStates`, so direct mentions update the target Agent's inbox/obligations and Agent-authored replies update the author's worklog even when no special assignment/change command was detected.
- Special manager commands use the same source-message path before side effects: live Leader assignment commands, Google Chat change requests, and War Room meeting change requests are written to Agent state and `eventLedger` before assignment/change handlers append their specialized records.
- `agentProjectService.js` now wraps those runtime operations in backend-style project commands. `submitProjectChatMessage`, `submitProjectMeetingMessage`, and `runProjectAutonomousCycle` accept a project plus request payload, return the next project state and messages to publish, and can be used by React, an API route, or a worker without duplicating orchestration logic.
- `agentProjectStore.js` provides the repository seam behind that service. The memory adapter persists hydrated project snapshots, retained messages, and durable kickoff meeting sessions in process, supports `snapshot`, and can be swapped without changing command routing.
- `agentProjectFileStore.js` is the first Node-backed adapter for that seam. It writes the same project/message/kickoff-meeting snapshot to JSON, reloads it after service restart, and keeps post-restart chat and worker-cycle dispatch on the same service contract.
- `agentProjectApi.js` is the HTTP-shaped command layer over the service. It routes durable kickoff meeting session create/read/approve, project chat, War Room meeting input, autonomous worker cycles, messages, snapshots, timeline reads, event-ledger reads, task evidence reads, readiness checks, readiness proof-map reads, and aggregate manager-dashboard reads through `{ method, path, body }` requests, returning `{ status, body }` responses that can be mounted behind a real server.
- Project-scoped command responses also include `managerDashboard`, so a frontend can refresh the backend aggregate view immediately after initiation, chat, meeting, autonomous-cycle, or per-Agent worker commands without issuing a second read first.
- Due-worker processed items and scheduler tick results include the same manager-dashboard snapshot per processed project, so unattended 24/7 backend work still returns manager-readable aggregate evidence with each processed result.
- `agentProjectHttpServer.js` mounts that command layer on a real Node HTTP server, adds scheduler status/tick/start/stop controls, and `npm run agents:server` starts it against a JSON store path so backend endpoints can be exercised outside React.
- Agent state is now independently addressable through backend endpoints: `GET /projects/:id/agents`, `GET /projects/:id/agents/:agentId/dashboard`, `inbox`, `worklog`, `obligations`, and `plan`, plus `POST /projects/:id/agents/:agentId/message` for Agent-authored group-chat publishing. Direct `@agent` messages, Agent-authored target messages, and kickoff Leader assignments carry task ids or source message ids into those inbox resources, while the dashboard endpoint aggregates each Agent's state, worker ledger, owned tasks, management relations, and proof routes.
- Project evidence is now independently addressable through backend endpoints: `GET /projects/:id/timeline`, `GET /projects/:id/events`, `GET /projects/:id/tasks`, `GET /projects/:id/tasks/:taskId`, and `GET /projects/:id/tasks/:taskId/evidence`. The task evidence endpoint resolves linked chat messages, timeline logs, and unified ledger entries from each task's durable evidence ids.
- `createKickoffMeetingSession`, `approveKickoffMeetingSession`, `POST /kickoff-meetings`, and `POST /kickoff-meetings/:id/approve` make the initiation meeting a durable pre-project backend resource. The session stores Director brief, invited Agents, role questions, self-nominations, Leader campaigns, peer-hearing evidence, recommended Leader/Reviewer, manager decision options, and `kickoff-generation-provenance/v1`; approval reuses that saved transcript to create the project, Leader marker, kickoff charter, assignments, first pulse, and manager-dashboard proof. The same provenance is persisted into project initiation and kickoff charter evidence, and Manager Dashboard exposes it as kickoff generation proof after approval. The browser `ui:manager-backend` Harness now asserts the approved project Dashboard renders the provenance card and keeps production claims blocked. The provenance label separates deterministic validation fallback, model-provider rehearsal, provider-backed model generation, and development fallback while keeping production claims blocked.
- `createKickoffProjectFromMeeting` and `POST /projects/initiate` still provide the one-shot kickoff creation flow behind the backend boundary: from a Director brief and team roster they generate role questions, self-nominations, Leader campaigns, the confirmed Leader marker, kickoff charter, Leader assignments, acknowledgements, chat evidence, and the first autonomous pulse.
- `POST /kickoff-meetings/:id/approve` and `POST /projects/initiate` now honor `includeReadModels: false`. Receipt-first responses keep the created project, kickoff/charter/assignment proof, and first-pulse chat proof, then return explicit project snapshot, transcript, timeline/event, Manager, Flow Graph, and Proof Map refresh routes instead of embedding Manager Dashboard / Ready Package snapshots. React initiation approval consumes this path and refreshes from the returned routes before entering the dashboard. Initiation Roundtable free input also uses `POST /kickoff-meetings/:id/clarify`; if the durable meeting session is missing or the command fails, React records a backend error and does not run local room simulation. The manager scenario and product-team acceptance Harnesses verify that refreshed Manager read models still expose first-pulse, Leader, next-action, clarification, and provenance evidence, and statically reject browser-only initiation meeting simulation.
- `runDueProjectAutonomousCycles` and `POST /workers/autonomous/due` provide the backend project scheduler path: they scan persisted projects, run only autonomy-enabled projects whose `nextAutonomousRunAt` is due, publish group-chat work evidence, and return skipped projects without mutating them.
- `evaluateAgentWorkSchedule`, `runDueAgentWorkCycles`, and `POST /workers/agents/due` provide the independent Agent scheduler path: they scan each team member's `nextAgentRunAt`, rank due Agents by management pressure, run selected Agents through `runAgentWorkCycle`, publish per-Agent group-chat proof plus managed-Agent check-ins, update `agentWorkerLedger` with receipt/retry/dead-letter metadata, and return skipped Agents without mutating their state. The manager dashboard mirrors that per-Agent backend state in Team rows with latest inbox, open obligation, worklog, and next-run details instead of only aggregate counts.
- Management pressure is computed from manager/peer-manager relations, open owned tasks, open obligations, management inbox check-ins, peer handoffs, and review sweep signals. The chosen Agent run stores `managementPriority` and `managementReasons` so the scheduler decision is auditable instead of being a hidden queue heuristic.
- `GET /workers/autonomous/status`, `POST /workers/autonomous/tick`, `POST /workers/autonomous/start`, and `POST /workers/autonomous/stop` wrap both due-worker routes in a server-level autonomous loop, so a running backend can continue project-level and Agent-level 24/7 work without relying on an open browser tab. Scheduler status now carries `scheduler-agent-controls/v1` summaries for startup, preserved scheduled, and latest tick controls, so the C-side Manager station can audit whether the A-side loop is actually running with autonomous strategy, submission, Reviewer, and revision-response controls. The worker API now caches per-project manager read models within one response, and the local HTTP server drains keep-alive sockets on close, which keeps the long manager scenario and product-team acceptance Harnesses reliable under repeated local HTTP server starts/stops.
- The project dashboard includes a `Backend Worker Station` panel that checks the configured local backend, lets the manager edit and persist the backend URL, displays scheduler counters, exposes start/stop/status controls, starts backend-owned immediate project/Agent sweeps, can run a backend-backed `Server Pulse` for the active project, and can pull the latest backend project/messages snapshot into the manager UI.
- When that backend station is online, group-chat, Roundtable meeting, and legacy War Room terminal submissions call the backend project command routes first and merge the returned project/messages into the UI. If the command fails on a real project, the UI shows the failure and does not mutate browser-local state; local runtime fallback is limited to offline, explicit demo/sample projects, or the development fallback switches.
- Real initiation approval follows the same backend-first pattern through `POST /projects/initiate`, then merges the returned project and kickoff messages into the UI. If the backend approval command fails, React keeps the manager in the initiation decision step, shows `Backend initiation approval failed`, and does not create a local project unless an explicit development fallback flag is enabled.
- The React initiation approval, group chat, and Roundtable input paths call that service boundary directly, so manager-demo clicks and real kickoff approval exercise the same command facade a future backend will expose. Initiation approval now uses receipt-first backend responses and route-based read-model refresh instead of depending on embedded Manager snapshots.
- Group-chat channel creation now has the same backend-first boundary. `createProjectTranscriptChannel` and `POST /projects/:id/transcripts` record `transcript-channel-created/v1`, append a transcript-visible system message, persist `transcriptChannelReceipts`, write timeline and event-ledger proof, expose the created channel through transcript index/channel routes, and let Manager Flow Graph render it as a collaboration proof node. Readiness Proof Map now exposes the same receipts as `transcriptChannelRoutes` / `transcriptChannelSummary`, so channel creation is inspectable from the Manager proof surface with chat, timeline, event, and route proof. React's group-chat `+` button calls this route for backend projects and only falls back to local rooms for offline/demo/explicit local runtime projects; the product-team Harness verifies API receipt, transcript index/channel, timeline, event, Flow Graph, and Proof Map coverage.
- Projects now maintain an append-only `eventLedger` with retained-window contiguous sequence numbers plus `eventLedgerFirstSequence`, `eventLedgerLastSequence`, and `eventLedgerEventCount` cursors. It aggregates kickoff approval, Leader assignments, peer handoffs, confirmed changes, autonomous scheduler runs, work pulses, management check-ins, and task completions into one backend-oriented event stream.
- `backfillProjectEventLedger` runs during project hydration, so older localStorage/imported projects that only have kickoff transcripts, logs, change records, peer handoffs, and scheduler records are migrated into the same replayable event stream.
- `publishAutonomousCycleChat` is now the App/worker boundary for autonomous work publication: after `advanceAutonomousProjectCycle`, manual pulses, scheduler pulses, initiation first-pulse, and manager-demo seed cycles publish their visible group-chat messages back into Agent state and `eventLedger` as `group-chat-message` records.
- `projectEventReplayProjection` derives replay-stage counts from that event stream, and the dashboard exposes them in `Unified Event Ledger`.
- The React dashboard now refreshes `GET /projects/:id/timeline` and `GET /projects/:id/events` after backend project load/sync, Manager control actions, chat or meeting commands, worker pulses, Agent write receipts, and manual `Sync Timeline`. Browser `logs` / `eventLedger` stay as offline/cache fallback, while backend-online timeline and event read models become the preferred proof surface for workflow progress.
- The React dashboard now also refreshes core Manager Ready Package proof submodels through standalone backend routes for Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Evidence Quality Audit, Evidence Source Review Workflow, and Evidence Custody Readiness. Those standalone models are preferred over embedded Ready Package snapshots, reducing reliance on summary-shaped UI fallback for the research/product-team acceptance proof chain.
- Timeline proof navigation now uses the same backend-required proof boundary as transcript proof navigation. For backend-online real projects, `openProjectTimelineProof` syncs `GET /projects/:id/timeline` and `GET /projects/:id/events`, verifies the requested proof ids are present in backend timeline/event-ledger models, and records `Backend timeline proof missing; local timeline focus suppressed` instead of focusing stale browser timeline state when proof is missing. The manager scenario Harness statically pins this branch alongside the transcript fail-closed branch.
- The same Ready Package sync path can request `includeLaunchControls`, which expands the standalone refresh to the private-pilot and production launch/control routes before rendering Manager Ready Package panels. This keeps production no-go, private-pilot handoff, security, provider, operations, persistence, and queue proof surfaces tied to backend read models rather than browser inference.
- `evaluateManagerScenarioReadiness` requires both event-ledger continuity and replay readiness for the manager-ready path.
- `approveInitiationProject` immediately calls `advanceAutonomousProjectCycle` with `trigger: initiation-approval`, persists the first scheduler ledger entry, moves the project to executing, and posts first-pulse work messages into group chat.
- The project dashboard renders the `Kickoff Charter` so the manager can see what the team agreed to do after the meeting.
- The project dashboard also renders `Kickoff Meeting Flow`, showing role clarification, self-nomination, peer-hearing edges, Leader campaign count, Director confirmation, and the persisted Leader marker with a proof jump to the kickoff chat transcript. It now includes `Conversation Evidence` rows for the actual meeting turns plus per-row proof jumps.
- The project dashboard also renders `Kickoff Execution Flow`, making the transition from meeting decisions to Leader assignments and the first autonomous pulse visible as a single proof chain.
- `Load Sample Fixture` opens the Manager Demo as explicit sample data and seeds the same kickoff transcript, Leader campaign, Director confirmation, and Leader marker in one click for the manager walkthrough.
- The confirmation is posted into the decisions channel and project logs.

Status: covered for the prototype flow.

### Leader assignment through group chat

Evidence:

- `createLeaderAssignmentPackage` converts open project tasks into Leader-authored `@agent` assignment messages.
- Those assignment messages are appended to group chat after project creation.
- Every assignment also produces an immediate assignee acknowledgement with `assignmentReceipt`, showing the mentioned Agent saw the work and started it.
- The assigned task records persist `assignmentMessageId`, `acknowledgementMessageId`, `acknowledgedAt`, and `timelineLogIds`, so the dashboard can connect each task to chat and timeline proof directly.
- Kickoff Leader assignment tasks also persist `source: kickoff-leader-assignment` and `sourceChannelId: main`, so the manager can use the same Chat proof button on the first assigned work after project creation.
- The manager dashboard consolidates those records into `Leader Assignment Flow`, which shows the full chain from group `@assignment` to the assignee's inbox/obligation state, acknowledgement, work pulse, and timeline proof instead of leaving the evidence scattered across task cards.
- The manager dashboard also renders `Agent Management Mesh`, which reads Leader manager links, peer-management links, latest check-ins, and management timeline logs into one proof surface for Agent-to-Agent management.
- Task proof buttons preserve runtime message ids, jump to the source chat or timeline, and automatically land on and highlight the exact proof messages/logs instead of only opening the broad view.
- Chat proof buttons use backend transcript reads first for backend-online real projects. The local proof-recovery layer rebuilds missing proof messages from kickoff transcripts and `log_<messageId>` project logs only for offline, sample fixture, imported legacy, or explicit development fallback projects.
- Assignment logs are inserted into the project log and therefore appear on the project timeline.
- Assignment acknowledgement logs are also inserted into the project log as `assignment-acknowledged`.
- `handleLeaderChatAssignment` covers live group-chat assignment requests after kickoff: the confirmed Leader emits the actual `@agent` message, a new task is created, the mentioned Agent acknowledges immediately, and both events are logged.
- Live assignment, peer handoff, and feature-change tasks carry the same task-level evidence links for request/assignment, acknowledgement or confirmation, owner sync, and timeline logs.
- `submitChatInput` detects live Leader assignment requests, updates project tasks/logs/Agent state, and appends the Leader assignment plus acknowledgement into the active project chat.
- `handlePeerHandoff` covers Agent-to-Agent dependency handoffs after work has started: the requesting Agent emits an `@agent` dependency request, the peer acknowledges immediately, a dependency task is created, `peerHandoffs` records the accepted handoff, and both events are logged.
- The dashboard `Peer Handoffs` panel links each accepted handoff back to peer chat proof and timeline proof for the request and acknowledgement.
- Team state now preserves peer-management relationships with `peerManagedIds` and `peerManagerIds`, separate from the Director-confirmed Leader chain.
- The project dashboard now surfaces Leader-assigned tasks with owner, assignment source, work-pulse count, and timeline-published status.
- The project dashboard also surfaces accepted peer handoffs with requester, target, task, channel, request evidence, and acknowledgement evidence.
- Chat messages carry `projectId`, and the project chat view filters by active project plus channel, so assignments are visible in the correct project without cross-project leakage.

Status: covered.

### 24/7 work with visible progress

Evidence:

- `evaluateAutonomousSchedule`, `advanceAutonomousProjectCycle`, and the app scheduler continue producing logs, autonomous ledger entries, scheduler ledger entries, and next-run timestamps.
- `createAutonomousCycleChatMessages` converts those autonomous cycle events into project-scoped group chat records.
- The app scheduler appends due-cycle chat records even when the user did not manually press `Hour Pulse` or `Day Report`.
- The project dashboard shows last run, next run, trigger source, due time, scheduler reason, and next scheduled time for recent cycles.
- The dashboard also shows whether the local backend worker station is online, offline, running, or ready, so the manager can distinguish browser-local autonomous work from server-backed 24/7 execution. When online, it periodically syncs the active project snapshot back from the backend so server-run autonomous cycles appear in the same chat and timeline views.
- `24/7 Operations Board` consolidates project next/last run, backend worker state, Agent run queue size, and each Agent's next run, latest work, trigger, open obligations, and management priority so continuous work is manager-readable without inspecting raw scheduler ledgers.
- Backend command responses preserve `responses.changeResponse`, so meeting-change discussion animation and group-chat owner confirmations stay available when the server handles the command.
- Backend initiation responses preserve role negotiation, Leader election, kickoff charter, and assignment-package details so the first manager approval can be persisted by the backend without losing the meeting evidence the UI needs.
- Autonomous cycles now move owned tasks through visible work pulses and emit `task-completed` timeline logs when work reaches completion.
- Autonomous cycles also emit management-loop events: Leader check-ins for managed Agents, Reviewer evidence sweeps, and peer-management check-ins for dependency handoffs.
- Management-loop events are mirrored into group chat as `@agent` messages, written to project logs, counted in `autonomousLedger.managementEventCount`, and delivered into the target Agent's inbox.
- Every autonomous cycle records each Agent's fixed work routine, including routine id, label, checklist, and expected artifact, so 24/7 work is typed by role instead of generic activity.
- Per-Agent worker cycles can now submit completed autonomous work through the same Agent submission contract used by manual and generated artifacts. `submitWorkArtifact: true` on a work-cycle, `submitWorkArtifacts: true` on due-Agent workers, or `submitAgentWorkArtifacts: true` on HTTP scheduler ticks creates a submission linked to the worker cycle, task, group-chat message, timeline log, event-ledger proof, and Agent worker ledger. `workArtifactType: "auto"` and scheduler-created submissions infer generic product-team artifact types from task intent while keeping `progress-brief` as fallback; the manager scenario Harness verifies a service worker producing a `brainstorm-board` and an HTTP scheduler tick producing an `evidence-packet`. Evidence-oriented worker tasks also record standard `evidence-search` proof with worker task/chat/timeline/event sources, expose that receipt in work-cycle and due-worker API responses, and link it into the submitted evidence packet. Reviewer workers can also process assigned pending submissions through the standard review contract when `reviewPendingSubmission` / `reviewPendingSubmissions` is set, producing `submission-review` chat, timeline, event-ledger, task-evidence, and worker-ledger proof. Submitter workers can now close `changes-requested` obligations with linked `revision-note` or `final-deliverable` submissions through `respondToReviewObligation` / `respondToReviewObligations`, and forced scheduler ticks filter non-target projects before max-project limits are applied. The manager scenario Harness now isolates the HTTP scheduler artifact/review state and verifies the generic chain from `evidence-packet` plus worker-local evidence search to `changes-requested`, linked `revision-note`, accepted revision, accepted `final-deliverable`, Manager Flow Graph closure, and Readiness Proof Map routes. These behaviors remain opt-in until production review/volume controls exist.
- Per-Agent worker cycles now also have an auditable strategy layer. `useAutonomousStrategy: true` records `agent-autonomous-strategy-decision/v1` with the selected action, rationale, resolved controls, and next step before side effects run. The strategy prioritizes requested-change obligations, pending teammate reviews, completed owned work, management signals, open owned work, and monitor-only states. The decision is persisted in the worker ledger, attached to timeline/event proof, returned by work-cycle and due-worker APIs, and surfaced through Manager Dashboard continuous-work rows / Flow Graph worker-loop summaries.
- The strategy layer now has a backend execution queue. `GET /projects/:id/agent-autonomous-action-queue` returns `agent-autonomous-action-queue/v1` rows for every Agent, including selected action, schedule state, request body template, delegated work-cycle route, proof ids, and checksum. `POST /projects/:id/agent-autonomous-action-queue/:agentId/run` executes one row through the standard Agent work-cycle path, returns a strategy/run receipt plus delegated output objects, and persists `agent-autonomous-action-run/v1` into project ledger, timeline, and event ledger. Manager Dashboard exposes the run rows, Manager Flow Graph renders executed Agent action receipt nodes, Readiness Proof Map exposes `agentAutonomousActionRunRoutes`, and the delegated Agent Dashboard shows its direct queue-run receipts. Agent due-worker API responses and HTTP scheduler tick/start status now include the post-run queue, so scheduler output carries the next A-side action state back to C-side controls. HTTP scheduler start also forwards the full startup body into the immediate Agent sweep, so C-side start controls can request autonomous strategy, submission, reviewer, and revision-response behavior instead of only turning on the interval. Server autostart preserves those controls for interval ticks and the local `agents:server` entrypoint exposes them through explicit env switches. `npm run agents:scenario` verifies the service, API, HTTP scheduler, scheduler-start startup submission, scheduler autostart submission, Manager Dashboard embedding, persisted direct run receipts, Readiness Proof Map `agentAutonomousActionRoutes` / `agentAutonomousActionRunRoutes`, and Manager Flow Graph Agent action/run nodes.
- Agent Queue runs now also persist `autonomous-action-decision/v1`, the generic action-level proof that wraps the selected strategy, Agent initiative, queue checksum, target artifact/review/evidence intent, and optional provider preflight. The decision checksum/action is copied into Agent Queue run receipts, Autonomous Run Control delegated action receipts, Collaboration Intent run receipts, `/workers/autopilot/due` processed rows, Readiness Proof Map run routes, and Manager Flow Graph run attachments. React renders the same decision in Run Control output, Agent Action output, Collaboration Intent output, and Flow Graph attachment details. This closes the gap between "Agent strategy selected an action" and "C-side can audit why this exact execution happened now." The product-team Harness verifies the decision through a Collaboration Intent -> Agent Queue run, a provider-backed Agent Queue run, and an HTTP Autopilot due-worker run; the Manager backend UI Harness verifies the decision is visible after Run Control and Agent Queue executions.
- Bounded Autopilot sessions now have a scheduler-owned worker lane. `POST /workers/autopilot/due` scans active `autonomous-run-control-session/v1` rows, applies `autopilot-session-schedule/v1`, advances due sessions through the normal `autonomous-run-control-session-tick/v1` contract, and returns session tick, loop, run, target-control, and lightweight read-model refresh evidence. Worker Queue Snapshot exposes active sessions as `autopilot-session` rows with idempotency keys, leases, direct tick routes, and the `/workers/autopilot/due` worker route. HTTP `/workers/autonomous/tick` can opt into this lane with `tickAutopilotSessions`; scheduler status records `scheduler-autopilot-controls/v1`, processed/skipped counts, and latest Autopilot receipts. React Backend Worker Station now exposes this as the primary `Scheduler Tick` Autopilot continuation control, stores the returned worker receipt, renders the `/workers/autopilot/due` path, and keeps direct session tick as a manual fallback. This gives C-side operations a controlled bridge from manual session ticks toward unattended operation without claiming production daemon readiness.
- The `Scheduler Tick` UI confirms the backend session ledger before calling `/workers/autonomous/tick`. When the frontend still has a session id but the backend has no active `autonomous-run-control-session/v1`, React creates the session through the same backend start route and only then runs the scheduler-owned due-worker. This keeps C-side Autopilot controls from relying on a browser-only session state after stale seed/sync paths.
- The Manager Dashboard UI now consumes the same queue. `npm run ui:manager-backend` verifies that a backend-online project can sync `Agent Autonomous Queue`, render Agent-selected rows and run routes, execute an enabled row through the backend endpoint, show the run receipt, render `Agent Action Output Nodes` with the returned `Agent Submission` node and output chat proof, and refresh backend read models instead of writing local mock state. The UI now gives explicit Agent queue sync the longer user-visible timeout path and awaits a fresh queue read after Autonomous Run Control runs, so stale C-side queue state cannot hide the next backend-runnable A-side action.
- The React C-side Agent Pulse path now sends the same backend controls instead of a worklog-only pulse: it opts into autonomous strategy, work submission, Reviewer processing, and submitter review-response handling with lightweight receipts, then refreshes the Manager/Agent read models. Scheduler start forwards startup Agent strategy, submission, Reviewer, and revision-response controls to the immediate backend sweep, and the HTTP Harness now requires a startup `brainstorm-board` submission plus `agent-autonomous-strategy-decision/v1` proof. Heavier autonomous review policy remains behind explicit controls.
- The React C-side Server Pulse, Hour Pulse, Day Report, and Manager Action Playbook `fixed-continuous-routines` paths now run `/workers/autonomous/tick` with current-project filters and Agent worker controls, so browser and Manager Action Queue actions prove both project scheduler output and Agent worker output. Hour/Day pass `projectCadence` to preserve the requested cadence, and the tick can forward `useAgentAutonomousStrategy` so A-side workers choose actions from backend state instead of relying only on C-side hints. Action Queue runs persist a `manager-action-scheduler-tick/v1` receipt with project/Agent processed counts and returned Agent worker evidence. React renders `Manager Action Output Nodes` after Action Playbook or Scenario Walkthrough runs, exposing result-message, task, scheduler, submission, evidence, review, and review-response outputs with chat/timeline proof exits, and the browser Harness requires those nodes after a real walkthrough click. Scheduler status/start/tick handlers hydrate the returned Agent autonomous queue, and the browser/service Harnesses wait for project evidence, Agent evidence, and scheduler-tick receipts before accepting the path.
- `team-received-change-sync` now treats Agent inbox entries as consumable worker queue items. Manager readiness can still pass from durable change-ledger, timeline-log, or event-ledger receipts after Agent workers process the inbox, so A-side autonomous progress does not erase C-side governance proof.
- `agentStates.currentPlan.routine` preserves the same routine evidence for the team panel, and peer handoff relations survive autonomous cycle state refreshes.
- Manual `Hour Pulse` and `Day Report` controls exercise the same work cycle.
- Project timeline now derives events from project logs when logs exist, so assignment, work pulse, and change events are visible in the large timeline view.
- Runtime-generated timeline events now use stable readable labels for project approval, Leader confirmation, assignments, acknowledgements, changes, work pulses, daily reports, and task completion.
- `Manager Demo Sample Fixture` on the dashboard gives a direct sample-data path into kickoff chat evidence, War Room meeting changes, Google Chat change simulation, and the evidence timeline.
- `Load Sample Fixture` creates a fully populated scenario using the same runtime functions, including autonomous work cycles and task completion evidence, while the generated project is visibly marked as sample-only.

Status: covered for local prototype runtime.

### Mid-project feature change

Evidence:

- `handleFeatureChangeRequest` creates a discussion sequence: Lead acknowledges, Reviewer challenges risk when present, responsible Agent confirms.
- Roundtable meeting input detects feature-change requests, sends them through the same protocol, mirrors the discussion into project group chat, and animates the Lead/Reviewer/Owner responses back into the meeting transcript.
- The same protocol adds a responsible-owner plan sync after confirmation, so the owner explicitly tells everyone that the change entered their plan.
- That plan sync now updates the responsible owner's `agentStates` entry immediately, including inbox source channel, open obligation, task id, worklog, and `currentPlan` pointing at the accepted change.
- The same plan sync writes `change-sync` receipts into every non-owner Agent's inbox/worklog, and the change ledger records `teamSyncAgentIds`, `teamSyncCount`, and `teamStateSynced`.
- The same protocol returns `changeRecord`, which is stored in `changeLedger` with source channel, owner, reviewer, linked task, confirmation evidence, sync evidence, and plan update text.
- `handleFeatureChangeRequest` also returns a complete updated project state so group chat, Google Chat, and War Room paths persist the same owner-plan state transition.
- `submitChatInput` detects feature-change messages from group chat and invokes that protocol.
- The `Google Chat` channel uses the same path with `google-chat-mention-change-request` source metadata, preserving the channel where the request was made.
- Both War Room meeting input paths detect feature-change messages and invoke the same protocol.
- The confirmed change creates a new task and project log entries, which flow into chat and timeline.
- The project dashboard renders `Change Ledger` so manager review can inspect accepted mid-project changes without searching the raw chat stream.
- Each `Change Ledger` row can jump directly to chat proof and timeline proof for the related discussion, owner confirmation, owner sync, and log entries.
- The visible change stages now include source request, team discussion, owner confirmation, owner plan, and team sync targets, with owner-plan status derived from the owner's Agent state and team names derived from `teamSyncAgentIds`.
- Autonomous cycles read only the active project's recent chat messages when deriving obligations.
- War Room change discussions are mirrored into meeting logs so the live meeting and group chat stay synchronized.

Status: covered.

## Verification Gates

- `npm run agents:scenario` validates the full manager scenario data path and durable readiness checks.
- `npm run agents:local-mvp-startup-readiness` validates the backend startup preflight contract for Settings/API-key entry and first-run readiness without running a browser build. It proves the no-Vault state, local Secret Vault setup state, redacted vault binding after a sealed model key, Manager Ready Package coverage, Readiness Proof Map routing, and the route/validation-command surface the UI should expose.
- `npm run agents:settings-health-readiness` validates the backend Settings health contract without running a browser build. It proves Quick Check rows are backend-owned, Vault/provider setup state is accurate, sealed provider metadata stays redacted, Manager Ready Package coverage, Readiness Proof Map routing, and production readiness remains false.
- `npm run agents:settings-runtime-readiness` validates the backend Settings runtime contract without running a browser build. It proves Deployment/Models runtime rows are backend-owned, project-scoped routes link deployment preflight, Manager Ready Package and Readiness Proof Map include the runtime readiness surface, provider-vault metadata stays redacted, local persistence and worker queue adapter rows are visible, and production readiness remains false.
- `npm run agents:settings-integration-readiness` validates the backend Settings integration aggregate contract without running a browser build. It proves the project-scoped route returns `settings-integration-readiness/v1`, aggregates editable and route-backed integration rows, embeds in Manager Ready Package, exposes Readiness Proof Map routes, points back from project settings capabilities, and keeps production readiness false.
- Product-team validation is now staged for efficient proof. `npm run agents:product-team:smoke` covers the lowest-write in-memory C/A delivery loop plus canonical persona registry access, persona/professional skill-blend self-marketing, and all required generic artifact types (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`) through Flow Graph, Proof Map, transcript, timeline, event ledger, Artifact Quality Audit, and Agent Dashboard proof, `npm run agents:real-user-zero-to-autonomy` covers the low-write real-backend Secret Vault/provider/API zero-to-autonomy loop, `npm run agents:product-team:core` covers the fast file-backed/HTTP generic C/A delivery loop, `npm run agents:product-team:research-sample` runs through the backend/HTTP Product Team Delivery Trace plus Product Team Operating Loop, a three-step autonomous loop, and Runtime Autonomy Status, then asserts the Research validation sample still proves generic kickoff, self-marketing, brainstorm, evidence, draft, review/revision, final-deliverable, proof-surface, Manager-continuation, Agent-strategy, runtime-recovery, and production-blocker stages instead of paper/thesis/manuscript protocol fields, `npm run agents:product-team:cycle-consistency` continues into signed membership access and proves the three-step autonomous consistency route/Flow/Proof/Ready Package/persistence chain, `npm run agents:product-team:private-pilot` continues through launch approvals, governed evidence handoff, release candidate, launch run, health check, and customer acceptance report, `npm run agents:product-team:production-ops-controls`, `:production-deployment-controls`, `:production-security-controls`, `:production-provider-controls`, `:production-evidence-integrity`, and `:production-launch-governance` provide focused P2 hardening gates, and `npm run agents:product-team` keeps the full public-production hardening rehearsal with production control receipts, managed-production evidence integrity, and production launch-governance checks. Each staged file-backed run writes to an isolated `.tmp/product-team-acceptance/<stage>-<pid>` workspace, which keeps stores and audit/replay artifacts from colliding when focused gates run concurrently.
- The same scenario gate now validates the service facade by dispatching a meeting change, Google Chat change, Leader assignment, peer handoff, backend-worker autonomous cycle, independent per-Agent work cycle, strategy-driven submit/review/revision/acceptance worker decisions, due-Agent worker scan, bounded Autopilot due-worker scan, Autopilot queue snapshot rows, and management-priority due-Agent competition through `createAgentProjectService`; it also creates and approves a durable kickoff meeting session, snapshots the memory repository, reloads a fresh service, writes a file-backed repository to disk, restarts it, drives the HTTP-shaped API handler, verifies `POST /kickoff-meetings`, `POST /kickoff-meetings/:id/approve`, `POST /projects/:id/agents/:agentId/work-cycle`, `POST /workers/agents/due`, and the HTTP scheduler path into `POST /workers/autopilot/due`, boots the real Node HTTP server, calls it with `fetch`, creates a project through `POST /projects/initiate`, verifies timeline/event/task evidence HTTP reads, verifies per-Agent worker evidence and persistence, verifies scheduler status/tick/start/stop controls including Agent and Autopilot processed counters, checks dashboard backend worker controls and snapshot sync, restarts that server, and verifies post-reload kickoff session, chat, readiness, worker-cycle dispatch, and due-worker process/skip behavior.
- `npm run agents:product-team` validates the Research Project acceptance sample as a generic product-team workflow, not a research-only protocol. It creates and approves a durable kickoff meeting, checks search-provider status/test without exposing secrets, seals/opens fake provider secrets through the local secret vault, records provider-backed evidence search/source packets with evidence judgement/source quality signals, submits discovery, brainstorm, evidence, brief, decision, review, revision, implementation, and final-deliverable artifacts through the Agent submission API, requests changes on the draft brief, links revisions to the requested-changes review, accepts the final deliverable, verifies artifact files on disk, scans persisted project state and artifact files for fake secret fixtures, boots the real Node HTTP server against the same file store, runs `/workers/autonomous/tick` for the acceptance project, and proves project/Agent worker output reaches Manager Dashboard, Manager Ready Package, Manager Flow Graph monitoring proof, the standalone MVP readiness endpoint, the standalone pilot launch readiness endpoint, the standalone deployment preflight endpoint, the provider readiness endpoint, the security boundary endpoint, the persisted security access-audit endpoint, the backend security audit-stream endpoint, the production persistence snapshot endpoint, the managed persistence migration-plan endpoint, the managed persistence migration dry-run endpoint, the managed persistence adapter plan endpoint, the managed persistence adapter dry-run endpoint, the operations readiness endpoint, the worker queue snapshot endpoint, the worker queue adapter plan endpoint, and the worker queue adapter dry-run endpoint. It also writes the project membership policy through the backend API, verifies policy readback, revision audit, timeline/event proof, `project_membership_policies` and `project_membership_grants` persistence rows, verifies `access_replay_records` after replay protection runs, requires the managed migration plan to include seed order, table plans, RLS guidance, and passing verification gates, requires the migration dry-run to pass adapter contract, row-count, checksum, primary-key, RLS, and plan-gate checks, requires the managed persistence adapter plan and dry-run to pass local adapter execution receipts, critical table coverage, shadow-read parity, rollback, backup/restore, RLS, audit-stream cutover, and read-model checkpoint gates, requires queue adapter plan and dry-run to pass local queue adapter execution receipts, enqueue, idempotency, lease, dispatch, receipt acknowledgement, retry import, queue inspection, and dead-letter recovery gates while keeping production queue cutover blocked for the local shadow driver, requires operations readiness to pass local worker/audit/persistence/migration/database-adapter/queue-adapter/proof/security gates, worker execution receipts, managed persistence adapter operation/import metrics, queue adapter operation/row metrics, retry/dead-letter recovery metrics, audit-stream hash-chain verification, alert rules, a recovery runbook, and `operations-incident-drill/v1` receipts for alert routing, queue recovery, persistence recovery, and proof replay, requires pilot launch readiness to return private-pilot `go`, production `no-go`, ready evidence routes, production blockers, and a checksummed launch packet, requires deployment preflight to pass private-pilot blocker gates while keeping production deployment blocked and listing scheduler/access/adapter production controls, requires provider readiness to pass local provider policy, retry/circuit-breaker policy, local secret-vault status, usage/cost ledger, model artifact-draft quality/human-review gates, and production-control gates while listing remaining production rollout controls, then checks enforced access decisions for security-admin reads, observer export denial, Agent own-dashboard access, cross-Agent dashboard/submission denial, Reviewer identity mismatch denial, signed access-header rejection/allow/tamper cases, signed request-id replay denial in-process and after file-backed API restart, audit fail-closed rejection, signed HTTP scheduler worker calls under replay protection, signed project-membership allow/deny/revocation cases for Manager, Agent runtime, and Reviewer identities, HTTP header propagation, persisted allow/deny audit rows, backend audit-stream sequence/checksum/hash-chain proof, append-only JSONL audit sink rows, `security-access` event-ledger proof, `provider-usage` event-ledger proof, file-backed audit-stream refresh across local API instances, `security_access_audit`, `security_audit_stream`, `provider_usage_ledger`, and worker receipt/retry/dead-letter persistence export rows, superseded submissions and resolved obligations, project submission/search/review list/read endpoints, Manager Dashboard submission/evidence-quality/review/revision counts, Manager Flow Graph proofed role-clarification/self-marketing nodes plus explicit submission/evidence/review nodes, revision edges, attachments, Readiness Proof Map role-negotiation/self-marketing/submission/search/review/revision routes, task evidence linked submissions/searches/reviews, Agent Dashboard owned submissions/searches/reviews, group-chat transcripts plus event ledger submission/search/review proof, the distinction between local-pilot readiness and production blockers, persistence snapshot integrity, and worker queue idempotency/lease/retry rows for project and Agent workers.
- The same product-team Harness now verifies `autonomous-run-control/v1` through the standalone project API route, Manager Dashboard, Manager Ready Package, Manager Flow Graph, and Readiness Proof Map. The control view aggregates Manager action queue, Agent autonomous action queue, scheduler tick route, worker queue snapshot proof, next runnable actions, proof/timeline/event ids, checksum, and production blockers, keeping local/private-pilot autonomy separate from production readiness. The C-side UI Harness now also executes a listed next action through `POST /projects/:id/autonomous-run-control/:actionId/run` and verifies the persisted `autonomous-run-control-action-run/v1` ledger/event proof, Dashboard receipt rows, Flow Graph receipt node, and Proof Map route.
- The same `npm run agents:product-team` Harness now asserts `production-launch-audit/v1` inside Manager Ready Package, the standalone signed project API route, and the real HTTP server route. The completed signed API path requires private-pilot `go`, production `no-go`, zero failed private-pilot gates, visible failed production gates, production blockers, audit-integrity gates, the audit route itself, and next-shortest-path guidance toward production hardening; the HTTP route verifies the same contract surface without production overclaiming.
- The same Harness now records private-pilot Manager and security-admin launch approvals through `POST /projects/:id/launch-approvals`, verifies checksummed `launch-approval-workflow/v1` readback, Manager Ready Package summary fields, Manager Flow Graph release-governance nodes/edges, Readiness Proof Map `launchApprovalRoutes`, production launch audit private-pilot `go`, file-store `project.launchApprovals`, persistence snapshot `launch_approvals` rows, and managed migration seed-order coverage for the new table. The focused `npm run agents:product-team:production-launch-governance` gate then records signed production approvals from Manager, security-admin, and operations-owner after managed-production evidence integrity is ready, verifies operations-owner project membership access, confirms the production approval gate passes in Launch Audit and Control Center proof, and keeps the wider public-production decision `no-go`.
- The same Harness now requests, approves, and records download audit for a governed project evidence export through `POST /projects/:id/project-evidence-exports`, verifies `project-evidence-export-workflow/v1` readback, request archive-checksum pinning, private-pilot Manager/security-admin approval readiness, download-audit gated `project-evidence-export-package/v1` local package readiness, production launch audit `project-evidence-handoff-summary/v1` before/after package readiness, Manager Ready Package summary fields, Manager Flow Graph export-governance nodes, Readiness Proof Map `projectEvidenceExportRoutes`, file-store `project.projectEvidenceExports`, persistence snapshot `project_evidence_exports` rows, and production export/download `no-go` while encrypted storage/download infrastructure is absent.
- The same Harness now verifies `project-evidence-archive/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary fields, and Manager UI snapshot. The archive must include final deliverable content checksums, discovery/decision/implementation submission checksums, transcript submission messages, evidence source packets, accepted review checksums, Flow Graph final-deliverable/discovery/decision/implementation nodes, manifest checksums, all integrity gates passing, zero raw secret leaks, and transcript proof coverage for submission/search/source-review/submission-review messages. The Harness also requires Readiness Proof Map to expose matching transcript coverage before the archive handoff path can pass; production remains blocked because encrypted export storage, retention, download audit, and data residency controls are not implemented.
- The same Harness now verifies `evidence-quality-audit/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary checksum, Project Evidence Archive manifest, Production Launch Audit evidence routes, enforced security-admin access, and Manager UI snapshot. The audit must be decision-ready for the acceptance evidence set, keep `readyForProduction: false`, expose source-safety readiness, preserve proof routes, and list calibrated source-quality policy as remaining production work.
- The same Harness now verifies `artifact-quality-audit/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary checksum, Readiness Proof Map `artifactQualityRoutes`, Project Evidence Archive manifest/contents/integrity gates, Pilot/Production launch evidence routes, enforced security-admin access, and Manager UI snapshot. The audit must cover all required generic product-team artifact types, mark every submission proof-ready and locally quality-ready, preserve generated-draft quality and human-review metadata, and keep calibrated artifact rubrics plus managed output audit storage as production work.
- The same Harness now verifies `submission-review-workflow/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary checksum, Manager Flow Graph closure node, Readiness Proof Map `submissionReviewWorkflowRoutes`, Security Boundary route policy, enforced security-admin access, and Manager UI snapshot. The workflow must preserve review-round proof, close requested changes through linked revision responses, expose accepted final-deliverable proof, and keep calibrated reviewer policy plus immutable output audit storage as production work.
- The same Harness now verifies `provider-controlled-run/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary checksum, standalone route reads, Readiness Proof Map `providerControlledRunRoutes`, Security Boundary route policy, enforced security-admin access, and Manager UI snapshot. The plan must stay a no-call policy dry-run, mark deterministic private-pilot model/search operations runnable, preserve usage-ledger proof for `model:artifact-draft` and `search:evidence`, expose budget/rate headroom plus human-review/evidence/redaction boundaries, and keep real-provider eval plus managed provider audit storage as production work.
- The same Harness now verifies `provider-eval-run-workflow/v1` and `provider-eval-run/v1` through the in-process API and the real HTTP backend. It checks the initial run-needed state, records a shadow replay receipt through `includeReadModels: false`, requires model/search usage proof replay, validates timeline/event proof, lightweight provider/Manager refresh routes, Manager Flow Graph provider-eval nodes, Readiness Proof Map `providerEvalRunRoutes`, Security Boundary route policy, enforced security-admin reads, `provider_eval_runs` persistence rows, file-backed store persistence, and Manager UI snapshot. The receipt must remain `shadow-replay-from-provider-usage-ledger`, omit large Manager snapshots in receipt-first mode, keep `readyForProduction: false`, and preserve real provider eval plus managed eval storage as production work.
- The same Harness now verifies `private-pilot-release-candidate-workflow/v1` and `private-pilot-release-candidate/v1` through the in-process API and the real HTTP backend. It checks the record-needed workflow after handoff package audit, records a freeze receipt, requires launch audit/evidence package/provider eval/deployment/operations/adapter checksum coverage, validates timeline/event proof, Manager Ready Package summary readiness, Manager Flow Graph release-candidate nodes, Readiness Proof Map `privatePilotReleaseCandidateRoutes`, Security Boundary route policy, enforced security-admin reads, `private_pilot_release_candidates` persistence rows, file-backed store persistence, and Manager UI snapshot. The receipt must mark private-pilot release readiness only and keep `readyForProduction: false` while real managed production controls remain absent.
- The same Harness now verifies `private-pilot-launch-run-workflow/v1` and `private-pilot-launch-run/v1` through the in-process API. It checks the launch-needed workflow after release-candidate freeze, records an activation receipt, requires release-candidate/launch audit/evidence package/provider eval/deployment/operations/adapter checksum coverage, validates timeline/event proof, Manager Ready Package summary readiness, Manager Flow Graph launch-run nodes, Readiness Proof Map `privatePilotLaunchRunRoutes`, Security Boundary route policy, enforced security-admin reads, `private_pilot_launch_runs` persistence rows, file-backed store persistence, and Manager UI snapshot. The receipt must mark private-pilot launch readiness only and keep `readyForProduction: false` while public production controls remain absent.
- The same Harness now verifies `private-pilot-launch-health-check-workflow/v1` and `private-pilot-launch-health-check/v1` through the in-process API. It checks the check-needed workflow after launch activation, records a post-launch health receipt, requires launch-run/operations/security/provider/evidence/persistence/queue checksum coverage, validates timeline/event proof, Manager Ready Package summary readiness, Manager Flow Graph health monitoring nodes, Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, Security Boundary route policy, enforced security-admin reads, `private_pilot_launch_health_checks` persistence rows, file-backed store persistence, and Manager UI snapshot. The receipt must mark private-pilot monitoring readiness only and keep `readyForProduction: false` while centralized observability, on-call, managed incident, and restore-drill controls remain absent.
- The same Harness now verifies `private-pilot-acceptance-report-workflow/v1` and `private-pilot-acceptance-report/v1` through the in-process API. It checks the report-needed workflow after launch health, records a customer acceptance report, requires release/launch/health/evidence/Flow Graph/Proof Map checksum coverage, validates timeline/event proof, Manager Ready Package summary readiness, Manager Flow Graph acceptance decision nodes, Readiness Proof Map `privatePilotAcceptanceReportRoutes`, Security Boundary route policy, enforced security-admin reads, `private_pilot_acceptance_reports` persistence rows, file-backed store persistence, migration dry-run import coverage, and Manager UI snapshot. The report must mark customer private-pilot acceptance readiness only and keep `readyForProduction: false` while public production managed controls remain absent.
- The Manager Ready Package UI now exposes backend command buttons for launch approvals, evidence export handoff, and those four private-pilot receipts. `npm run ui:manager-backend` asserts the controls are attached and gate-disabled on an incomplete real backend project, while `npm run ui:manager-private-pilot` prepares the generic product-team launch-handoff store in an isolated `.tmp/product-team-acceptance/<run-id>` store/runtime checkpoint, clicks Manager/security launch approvals, evidence export request/approval/download-audit, release-candidate, launch-run, launch-health, and acceptance-report buttons in the real browser UI, and verifies the backend Manager Flow Graph plus Readiness Proof Map after the customer-visible clicks. `npm run agents:scenario` statically pins the `runBackendPrivatePilotReceipt` receipt-first path so the UI cannot regress to read-only snapshots or browser-local launch state mutation.
- The Manager Ready Package UI now also exposes `Record Rehearsal` backend commands for production operations, deployment, security, and provider control receipt workflows. The browser Harness asserts those controls are visible on the backend-connected Manager path, and source-level Harness checks pin `manager-ui-production-control-receipt` plus `evidenceEnvironment: 'local-rehearsal'` so UI-originated production-control receipts cannot be mistaken for managed-production certification.
- The same Harness now verifies `private-pilot-go-live-readiness/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph command node, Readiness Proof Map `privatePilotGoLiveRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The command view must expose stage rows, active phase, routed next action, private-pilot go-live readiness, customer acceptance readiness, production operations hardening status, receipt proof ids, timeline/event ids, and `readyForProduction: false`.
- The same Harness now verifies `production-launch-gap-register/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchGapRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The gap register must expose owner/domain/action rows, routed next action, upstream proof checksums, private-pilot acceptance state, production operations receipt state, and `readyForProduction: false`.
- The same Harness now verifies `production-launch-control-center/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchControlCenterRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The control center must expose gate rows, blocked rows, owner routing, routed next action, private-pilot acceptance state, production operations/deployment/security/provider receipt state, managed-production evidence integrity state, and `productionDecision: no-go`.
- The same Harness now verifies `production-launch-evidence-dossier/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph dossier node and incoming launch-control/evidence/gap edges, Readiness Proof Map `productionLaunchEvidenceDossierRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The dossier must expose manifest entries, four production control domains, open managed-production evidence gaps, proof/timeline/event routes, and `productionDecision: no-go`.
- The same Harness now verifies `production-evidence-integrity-audit/v1` through the in-process API, the real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph node/edge, Readiness Proof Map `productionEvidenceIntegrityRoutes`, Security Boundary route policy, enforced Manager reads, and Manager UI snapshot. It records local/test receipt batches and requires all verified controls to remain `local-rehearsal` with public production blocked, then records explicit `evidenceEnvironment: "managed-production"` batches and requires every operations/deployment/security/provider domain row to upgrade to managed-production evidence readiness. `npm run agents:product-team:production-evidence-integrity` is the focused proof for this boundary. This proves the evidence integrity layer can distinguish rehearsal proof from deploy-time production proof.
- The same Harness now verifies `production-security-control-receipt-workflow/v1` and `production-security-control-receipt/v1` through the in-process API, Manager Ready Package, Security Boundary, Manager Flow Graph receipt nodes, Readiness Proof Map `productionSecurityControlReceiptRoutes`, Security Boundary route policy, enforced security-admin writes, Manager UI snapshot, persistence snapshot, migration dry-run, and file-backed store. `npm run agents:product-team:production-security-controls` is the focused proof for this slice. The receipt can make the security domain production-security ready, but the control center must still keep public production `no-go` while non-security launch gates remain open.
- The same Harness now verifies `production-operations-readiness/v1` through the in-process API and the focused `npm run agents:product-team:private-pilot:ops-readiness` stage. It requires the read model to pass local/private-pilot operations proof after the customer acceptance report, keep `readyForProductionOperations: false`, expose required production controls such as centralized logs, alert routing, on-call ownership, managed incident system, and real restore drill, preserve proof ids, route `nextShortestPath.scope` to `production-operations-hardening`, mirror proof into Manager Ready Package summary/checksum, Manager Flow Graph production-operations node/edge, Readiness Proof Map `productionOperationsReadinessRoutes`, Security Boundary route policy, enforced security-admin reads, Manager UI snapshot, and receipt-first private-pilot write responses with explicit refresh routes.
- The same Harness now verifies `production-operations-control-receipt-workflow/v1` and `production-operations-control-receipt/v1` through the in-process API. It records a security-admin receipt covering all production operations control ids, checks timeline/event proof, verifies Production Operations Readiness flips to `production-operations-ready`, confirms the broader Production Launch Audit still remains `no-go`, validates Manager Ready Package summary/checksum, Manager Flow Graph receipt nodes/edges, Readiness Proof Map `productionOperationsControlReceiptRoutes`, Security Boundary route policy, persistence snapshot rows, migration-plan seed order, migration dry-run import coverage, file-backed store rows, and Manager UI snapshot.
- The same Harness now verifies `production-deployment-control-receipt-workflow/v1` and `production-deployment-control-receipt/v1` through the in-process API and real HTTP backend. It records runtime-platform deployment receipts covering all production deployment control ids, checks timeline/event proof, verifies deployment state can become ready without changing the broader public-production `no-go`, validates Manager Ready Package summary/checksum, Manager Flow Graph receipt nodes/edges, Readiness Proof Map `productionDeploymentControlReceiptRoutes`, Security Boundary route policy, persistence snapshot rows, migration-plan seed order, migration dry-run import coverage, file-backed store rows, and Manager UI snapshot. The focused `npm run agents:product-team:production-deployment-controls` gate additionally verifies those receipts project into `production-infrastructure-rehearsal/v1`, clear the deployment-preflight infrastructure blocker, preserve the deployment workflow checksum, and keep the broader production decision blocked.
- The same Harness now verifies `evidence-source-review-workflow/v1`, `evidence-source-review/v1`, `evidence-source-snapshot/v1`, and `evidence-provider-receipt/v1` through the in-process API, source-review POST approvals, the real HTTP backend, Manager Ready Package summary checksum/decision/snapshot/receipt counts, Manager Flow Graph source-review and evidence snapshot/receipt nodes, Readiness Proof Map source-review and provider receipt summaries, Project Evidence Archive manifest and decision/snapshot/receipt contents, Production Launch Audit evidence routes, enforced security-admin access, enforced reviewer identity mismatch denial, `evidence_source_reviews`, `evidence_source_snapshots`, and `evidence_provider_receipts` persistence rows, and Manager UI snapshot. The workflow must derive one review item per evidence source, link each item back to evidence proof routes, require submitted Reviewer decisions for local readiness, keep blocked sources at zero for the acceptance path, and retain human source-review policy plus managed immutable source/provider audit storage as production work.
- The same Harness now records local identity-session issuance, Manager Dashboard access through `x-hofs-session-token`, verified-session access audit rows, revocation and revoked-token denial, active-session Security Boundary proof, `identity_sessions` persistence rows, migration seed-order coverage, managed persistence adapter import coverage, append-only audit JSONL coverage, and raw-token absence from the file store. Production identity remains explicitly blocked.
- The same `npm run agents:product-team` Harness now asserts `inspectSnapshotParity(workerQueueSnapshot, projectId)`, a passing `snapshot-parity` gate, `worker-queue-adapter-snapshot-parity/v1`, and queue/lease/dead-letter parity summary fields in the worker queue adapter dry-run.
- The same `npm run agents:product-team` Harness now asserts local secret-vault seal/open plus rotation through `secret-vault-rotation-receipt/v1`, verifies rotated records still open under the new key id, exposes rotation readiness through Security Boundary and Provider Readiness, and scans both old and rotated vault master-key fixtures out of all readiness responses.
- `npm run ui:manager-demo` validates the built manager demo in a real browser path: click `Load Sample Fixture`, assert the sample fixture label, dashboard evidence panels, and `100%` manager-readiness are present, send the Google Chat change, War Room meeting change, Leader assignment, and peer-handoff demo actions, verify the resulting confirmations/syncs/assignment acknowledgement/accepted handoff, open the evidence timeline, and emit a screenshot artifact.
- `npm run ui:manager-backend` validates the backend-connected manager dashboard in a browser path by starting the real Agent project HTTP server, injecting its URL into the UI, running `Check`, `Server Pulse`, `Sync State`, and a Team-row `Agent Pulse`, verifying the backend store contains the UI-driven autonomous cycle, per-Agent worker ledger, and chat evidence, sending online Google Chat and War Room change requests, verifying backend change-ledger persistence, then walking the real initiation approval flow and verifying backend persistence of the kickoff project, charter, event ledger, and first-pulse chat evidence. It now continues after initiation to submit Turing brainstorm work through Agent Focus Workbench, run evidence search, submit a generated product-brief draft, record Curie's Manager-side changes-requested review, submit Turing's linked revision-note and final-deliverable, record Curie's accepted final review, and verify Manager Dashboard / Flow Graph / Proof Map / Agent Dashboard closure evidence.
