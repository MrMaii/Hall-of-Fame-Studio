# Agent Consciousness Architecture

This folder owns the app-facing agent runtime. It is intentionally model-agnostic: today it produces deterministic behavior for the React prototype, and later the same protocol can wrap BYOK LLM calls.

## Core Idea

An Agent is not only a prompt. In this product, an Agent is a working node with five layers:

1. **Identity**: name, role, persona skill, capability lanes.
2. **Mind**: current goal, open obligations, working memory, confidence, attention policy.
3. **Relations**: lead, reviewer, manager, managed peers, lateral peers.
4. **Communication Policy**: when to read, when to stay silent, when to respond, when to escalate.
5. **Work Cycle**: hourly private work pulses and daily public reports.

The Director does not continuously drive the team. The Director starts or changes intent; the Agent network then works through its own communication loop.

## Lead Mode

Every project starts with one Lead. The Lead is selected by task fit plus management score.

The Lead does not micromanage every sentence. The Lead owns:

- agenda and decision framing
- owner/deadline assignment
- cross-agent dependency resolution
- deciding whether an issue needs Director input
- maintaining the project ledger

The Reviewer is selected separately. The Reviewer can challenge the Lead and force evidence or verification before a decision becomes durable.

The default relationship graph is:

- Lead coordinates all non-lead Agents.
- Reviewer reviews the Lead.
- Every Agent can read peers directly.
- Agents only escalate to the Director when a decision changes scope, budget, deadline, acceptance criteria, or risk tolerance.

## Meeting Speech Frames

Agents do not improvise randomly in meetings. Each meeting type gives every participant a required speech frame.

### Kickoff

Lead says:

- goal
- scope
- owners
- first-cycle deadline
- decision log

Each Agent says:

- role
- first artifact
- dependency
- risk
- deadline

The output is a project charter. In the runtime this is stored as `kickoffCharter`, including the approved meeting result, confirmed Leader, Reviewer, team roster, next actions, communication rules, and evidence ids for role negotiation, Leader campaigns, and assignments.
The charter also stores hearing edges for role negotiation and Leader campaigns, so the system can prove that Agent turns were visible to peers rather than isolated one-on-one responses.

### Recurring Sync

Lead says:

- progress map
- blockers
- deadline pressure
- decision queue

Each Agent says:

- done
- doing
- blocked-by
- next-delivery
- confidence

The output is a status ledger.

### Review

Lead says:

- review target
- acceptance bar
- open risks
- owner fixes

Each Agent says:

- finding
- evidence
- severity
- fix owner
- verification

The output is a review verdict.

### Working Discussion

This is the non-ceremonial work channel.

Lead says:

- current objective
- coordination need
- handoff point

Each Agent says:

- signal
- interpretation
- action
- request

The output is work notes or a handoff.

## Work Communication Logic

Agents themselves do not "call meetings" during normal work. They communicate inside the work stream.

For every message, each Agent runs a read pass:

1. Is this a direct mention?
2. Is this from the Lead?
3. Is this from someone I manage?
4. Does this match my capability lane?
5. Does it contain a blocker, handoff, decision, risk, or deadline?

The runtime converts that into an attention score.

- Below 45: ignore for now.
- 45-71: read silently and update working memory.
- 72+: speak if the Agent can improve the plan.
- Direct mention: always speak unless the message is only FYI.

The speaking Agent must produce one of:

- acknowledgement
- correction
- handoff
- blocker report
- evidence request
- deadline negotiation
- decision proposal

This avoids noisy "everyone comments on everything" behavior.

## Long-Running Work

A station can run without user interference by scheduling work cycles.

### Autonomous Run Control

The backend exposes `GET /projects/:id/autonomous-run-control` as `autonomous-run-control/v1`. It also exposes `POST /projects/:id/autonomous-run-control/:actionId/run` for executing a listed next action through the delegated queue/scheduler lane, `POST /projects/:id/autonomous-run-control/run-loop` for a bounded loop over that same surface, `GET|POST /projects/:id/autonomous-run-control/sessions...` for the project-scoped Autopilot session layer, and `POST /workers/autopilot/due` for opt-in scheduler advancement of active bounded sessions.

This read model is the Manager-facing control surface for unattended work. It summarizes:

- the next Manager orchestration action
- the next Agent-selected autonomous action
- the backend scheduler tick route
- queued project/Agent/Autopilot session worker rows
- proof ids, timeline ids, and event ids
- gates that distinguish local/private-pilot autonomy from production autonomy

Manager Dashboard embeds the light control view. Manager Ready Package embeds the worker-queue-backed view. Manager Flow Graph renders it as an execution node, and Readiness Proof Map exposes its proof route. Run receipts use `autonomous-run-control-action-run/v1`, persist into project logs, `autonomousRunControlRunLedger`, and the event ledger, then surface as Dashboard rows, Flow Graph receipt nodes, Proof Map routes, and the delegated Agent's own Dashboard. Direct action responses also expose delegated output objects so React can render `Run Control Output Nodes` for submissions, evidence searches, reviews, review responses, artifacts, or result-message proof immediately under the run receipt. Loop receipts use `autonomous-run-control-loop-run/v1`, persist into `autonomousRunControlLoopLedger`, record each step receipt plus a stop reason, and surface in Manager Dashboard, Manager Ready Package, Manager Flow Graph, and Readiness Proof Map. Autopilot sessions use `autonomous-run-control-session/v1`; explicit ticks and due-worker ticks use `autonomous-run-control-session-tick/v1`, persist into `autonomousRunControlSessionLedger` and `autonomousRunControlSessionTickLedger`, link back to child loop/run receipts, carry the participating Agent ids, task ids, action ids, and action lanes, and attach an `autopilot-delivery-target/v1` snapshot of Product Team Delivery Trace progress. Before each tick runs, the next missing trace stage is translated into `autopilot-delivery-target-control/v1`; that target control is injected into delegated Agent/Manager/scheduler request bodies as the concrete artifact, evidence-search, review, or revision intent, and each loop step records `autonomous-run-control-target-selection/v1` candidate scores for Manager, Agent, or scheduler lanes. Worker Queue Snapshot exposes active sessions as `autopilot-session` rows with idempotency, lease, direct tick, and `/workers/autopilot/due` route contracts, while HTTP scheduler status records `scheduler-autopilot-controls/v1` and Autopilot counters when enabled. Completed session ticks now also export worker execution receipts with `sessionId`, direct tick recovery routes, and `worker_runs` persistence rows; the file-backed backend can reload a running session after restart and resume it through the due-worker; queue adapter plan/dry-run, Operations Readiness metrics, and the private adapter gateway import, lease, dispatch, acknowledge, and parity-check this Autopilot lane alongside project and Agent workers. Operations Readiness also checks the provider side of autonomous work: provider usage ledger rows, provider eval receipts, source snapshots, provider receipts, event proof, persistence counts, and the secret-vault boundary now appear in the recovery gate, alert rules, runbook, and incident drill before autonomous model/search calls resume. Action, loop, session start/tick/pause, and due-worker writes now return a dedicated `readModels` contract when `includeReadModels: false`: sessions, active tick/pause, scheduler tick, Autopilot due-worker, Agent queue, Product Team Operating Loop, Autonomous Cycle Consistency, Flow Graph, Proof Map, timeline, events, and transcripts all have explicit refresh routes. React Backend Worker Station consumes those routes through `refreshAutonomousRunControlReadModels` after Autopilot commands. The scheduler-owned route remains the primary Autopilot continuation path: `Scheduler Tick` calls `/workers/autonomous/tick` with Autopilot enabled, stores the returned worker receipt, and shows the due-worker path; direct session tick remains as an explicit manual fallback. This makes a C-side run button, bounded loop, bounded session, or controlled scheduler tick auditable from the A-side Agent workspace as well as the Manager aggregate, while keeping each autonomous tick tied to the next generic delivery gap instead of a research-specific script.

Agent initiative is part of that audit path. Agent worker queue rows carry the same `agent-autonomous-initiative/v1` projection used by Agent Autonomous Action Queue and Product Team Operating Loop, including `agentInitiativeId`, target artifact type, strategy intent, and run route. Run/loop receipts freeze the executed initiative when an Agent lane runs; session tick receipts freeze the candidate Agent initiative ids and artifact targets visible before lane selection.

Product Team Mission Runner handoff is now part of that same operating-loop audit. `product-team-customer-agent-handoff/v1` records the C-side approved roster, Leader, Reviewer, target stage, and A-side session/tick route. Product Team Operating Loop derives `product-team-customer-agent-handoff-execution/v1` from the persisted Autopilot session, tick receipts, loop/action run receipts, result message ids, and Agent submissions. The Manager can therefore see whether a customer-approved mission has only started an A-side session, has recorded the first tick, or has produced run receipts/output submissions, and the Flow Graph/Collaboration Intent Queue use the same backend summary instead of a frontend-only button state.

### Product Team Mission Runner

`POST /product-team-missions` starts a generic product-team mission from a customer brief. It creates and persists a kickoff meeting or reuses an already persisted kickoff meeting, confirms Leader/Reviewer/next-action decisions, approves only the Manager-confirmed team through `selectedTeamIds`, starts bounded Autopilot, optionally runs the first A-side tick, and records `product-team-mission-run/v1`. The receipt embeds `product-team-customer-agent-handoff/v1` as `customerAgentHandoff`, capturing the C-side approved roster, Leader/Reviewer, A-side session/tick status, target stage, action lanes, and next routes into Product Team Delivery Trace, Product Team Operating Loop, Collaboration Intent Queue, and Autonomous Run Control. `GET /projects/:id/product-team-missions` and `GET /projects/:id/product-team-missions/:missionId` expose those receipts. The receipt is projected into Manager Dashboard `productTeamMissionRuns`, Manager Flow Graph `product-team-mission-run` nodes, Readiness Proof Map `productTeamMissionRunRoutes`, Product Team Operating Loop `customerSide.handoff`, and Collaboration Intent Queue `customer-agent-handoff-intent`, with handoff checksum/status plus links to transcripts, Product Team Delivery Trace, Product Team Operating Loop, Collaboration Intent Queue, Runtime Autonomy Status, Autonomous Run Control sessions/ticks, and child loop/run receipts. React initiation approval now uses this entry point for real backend projects, so the customer-approved kickoff meeting is carried into the C/A autonomy startup instead of being followed by a separate browser-side project start, and project-start refresh immediately pulls the runtime-autonomy recovery route. This is the preferred backend entry point for replacing frontend project-start mocks because it begins with a real backend project and hands the next step to the A-side loop.

### Product Team Operating Loop

`GET /projects/:id/product-team-operating-loop` exposes `product-team-operating-loop/v1`. This is the backend coordination summary for the C-side/A-side loop: Manager continuation, Agent autonomous strategy selection, generic delivery trace closure, scheduler/worker continuation, Proof Map routes, timeline ids, event ids, and production blockers in one redacted read model.

Use it as the operating contract when deciding whether the current project can continue autonomously. It passes local/private-MVP readiness only when the Manager can run a backend control route, the Agent queue exposes strategy-selected rows, the generic delivery trace is complete through final deliverable, submission/review/revision proof routes exist, and the scheduler or worker queue can continue. It never marks production ready; managed queue/cron, durable audit, provider/BYOK policy, cost controls, customer acceptance policy, and incident recovery remain required production controls.

### Team Collaboration Diagnostics

`GET /projects/:id/team-collaboration-diagnostics` exposes `team-collaboration-diagnostics/v1`. This is the backend break-diagnostics contract for overall scheduling: kickoff, Lead/Reviewer governance, Agent self-marketing, group chat transcript proof, owned tasks, brainstorm/evidence handoff, artifact review, revision, final delivery, Flow Graph, Proof Map, timeline, event ledger, and Agent initiative rows.

Use it when the Manager needs to know whether the collaboration chain is actually connected or where it is broken. Manager Ready Package embeds the same contract, Manager Flow Graph renders a `team-collaboration-diagnostics` aggregate node, and Readiness Proof Map exposes `teamCollaborationDiagnosticRoutes`. It can mark local/private-MVP collaboration ready, but production remains blocked until real provider/BYOK policy, managed queue/cron, durable audit, cost controls, customer acceptance thresholds, and incident recovery are in place.

### Event Ledger Replay Recovery

The runtime keeps `eventLedger` as an append-only, sequence-numbered proof stream with a bounded retained window. The local retained limit is `EVENT_LEDGER_RETAINED_LIMIT = 5000`; the summary still reports both retained and total event counts so a Manager can see whether the stream has been pruned.

Long-running Agent work must not lose manager readiness simply because early kickoff events have aged out of the retained window. `projectEventReplayProjection` can rebuild the core replay coverage from durable project records, including `kickoffCharter`, `changeLedger`, `peerHandoffs`, and `autonomousSchedulerLedger`, while preserving the retained ledger's contiguous sequence window. `backfillProjectEventLedger` still uses the retained projection when it needs to migrate legacy projects, so the recovery path does not hide missing retained events during migration. This is the local/private-pilot recovery contract; production still requires managed immutable audit storage and retention controls.

### Collaboration Intent Queue

`GET /projects/:id/collaboration-intent-queue` exposes `collaboration-intent-queue/v1`. This is the backend meeting/group-chat intent queue: kickoff self-marketing, role negotiation, group-chat attention, Manager next action, Agent autonomous initiatives, and review/revision/final closure are ranked into `collaboration-intent-queue-row/v1` rows with queue position, actor, intent, rationale, runnable route, proof ids, timeline ids, and event ids.

Use it when the Manager needs to inspect "who wants to speak or act next, why, and through which backend route" without reading raw chat text. Runnable rows can be executed through `POST /projects/:id/collaboration-intent-queue/:intentId/run`, which delegates to the existing Autonomous Run Control, Agent Autonomous Action Queue, or Manager Action Queue route and persists `collaboration-intent-run/v1` in the project ledger. The write response carries delegated outputs such as Agent submissions, evidence searches, submission reviews, and review-response artifacts when the delegated route produced them, including representative due-Agent sweep outputs from scheduler-backed Autonomous Run Control actions. This lets a C-side operator inspect the A-side product node created by the queue action. Manager Ready Package embeds the model, Manager Flow Graph renders `collaboration-intent-queue` with protocol/Agent initiative/run-ledger/production-boundary attachments, Readiness Proof Map exposes `collaborationIntentQueueRoutes`, and React Backend Worker Station consumes the standalone route through `Sync Intent Queue`. The browser surface renders queue rows, next runnable intent, backend route, run button, run receipt, returned output nodes with chat/timeline proof exits, Manager Proof Map chat/timeline proof exits, and row-level proof actions while production remains blocked until managed queue/cron, durable audit, cost controls, customer acceptance policy, incident recovery, and provider/BYOK policy exist.

War Room meeting speech is also backend-authored for real projects. `POST /projects/:id/meeting` uses `runRoundtableExchange` to create `meeting-agent-turn/v1` rows, persists them as `war-room-meeting-agent-turn` messages, writes `meeting-agent-turn` timeline logs, applies them to Agent inbox/worklog state, and exposes proof/timeline/event ids for transcript, timeline, and event-ledger inspection. React uses this route for legacy War Room session start as well as meeting input, then plays the returned turns before any local room simulation. If a backend-required real project returns no Agent turns, React blocks the local simulation path and surfaces a backend error. The visible meeting discussion is therefore the same data the backend can audit.

### Submission Review and Delivery Proof

Readiness Proof Map also exposes `submissionReviewWorkflowRoutes`, `productTeamDeliveryTraceRoutes`, and `productTeamAcceptanceChainRoutes`. React now renders them as Manager Proof Map cards with direct chat and timeline proof exits, so the C-side Manager can verify requested changes, revision response, accepted final delivery, and the generic C/A product-team acceptance chain without reconstructing the evidence chain from scattered dashboard panels. The acceptance chain keeps `readyForGenericProductTeamAcceptance` and `readyForBsideProductTeamRun` separate from `readyForProduction`, so local/private-MVP autonomy can be inspected without overclaiming public launch readiness.

### Runtime Contract Freeze

`GET /projects/:id/runtime-contracts` exposes `runtime-contract-freeze/v1`. This is the local MVP contract manifest for the runtime surface: Agent submissions, artifact storage proof, evidence search/source/provider proof, Reviewer review, revision/final delivery, transcripts, timeline, event ledger, Manager Flow Graph, Readiness Proof Map, Product Team Operating Loop, and Team Collaboration Diagnostics.

Use it when deciding whether the current backend contracts are stable enough for the Research-as-product validation sample and private MVP work. Manager Ready Package embeds the manifest, Manager Flow Graph renders `runtime-contract-freeze`, and Readiness Proof Map exposes `runtimeContractFreezeRoutes`. It can mark the local MVP contract set frozen, but production remains blocked until real provider/BYOK policy, managed database, managed queue/cron, durable audit, cost controls, customer acceptance policy, and incident recovery are in place.

### Autonomous Cycle Consistency

`GET /projects/:id/autonomous-cycle-consistency` exposes `autonomous-cycle-consistency/v1`. This is the backend consistency proof for bounded autonomous operation: the local MVP must show at least three autonomous steps while child action receipts, loop receipts, timeline logs, event ids, Flow Graph nodes, Readiness Proof Map routes, Worker Queue recovery, Product Team Operating Loop, Team Collaboration Diagnostics, and Runtime Contract Freeze all agree.

Use it when deciding whether the C-side/A-side system can keep moving as a coherent product team after the Manager starts a bounded loop or Autopilot tick. Manager Ready Package embeds the model, Manager Flow Graph renders `autonomous-cycle-consistency`, Readiness Proof Map exposes `autonomousCycleConsistencyRoutes`, and React proof-model sync renders a backend-required missing state when the standalone route is unavailable. It can mark local/private-MVP cycle consistency ready, but production remains blocked until managed queue/cron, distributed leases/idempotency, centralized audit, cost controls, customer acceptance policy, and incident recovery are in place.

### Runtime Autonomy Status

`GET /projects/:id/runtime-autonomy-status` exposes `runtime-autonomy-status/v1`. This is the backend status for "can the C-side/A-side runtime keep going and be recovered?" It aggregates Product Team Mission Runner startup, Autopilot sessions and ticks, action/loop receipts, Worker Queue Snapshot recovery, queue adapter rehearsal, persistence snapshot, Product Team Operating Loop, Autonomous Run Control, Flow Graph, Proof Map, `/workers/autonomous/status`, `/workers/autonomous/tick`, and `/workers/autopilot/due`.

Use it when the Manager needs one route-backed answer about local/private-MVP autonomy instead of scanning separate scheduler, queue, persistence, and proof panels. Readiness Proof Map exposes `runtimeAutonomyStatusRoutes`, Manager Flow Graph renders `runtime-autonomy-status`, and Security Boundary lists the route policy. The Research validation sample reads this route after the HTTP three-step autonomous loop and requires the route, proof links, Ready Package summary, scheduler recovery links, and production blocker to agree. It can mark local/private-MVP autonomy ready, but unattended production autonomy remains blocked until managed queue/cron, distributed leases, managed persistence restore proof, BYOK/provider cost controls, centralized observability, immutable audit, and incident recovery drills are proven.

### Hourly Cycle

Each Agent privately does:

- inspect new messages
- update obligations
- advance the current artifact
- detect blockers
- publish only if something changed

Hourly public frame:

- last observable change
- current task
- blocked signal
- next hour

### Daily Cycle

Each Agent produces a more durable report:

- completed
- planned
- deadline
- risks
- requests

The Lead consolidates daily reports into the project ledger. The Reviewer attaches verification notes when the work touches evidence, quality, risk, security, or acceptance criteria.

The runtime supports this through two layers:

- `planAutonomousWorkCycle`: computes what every Agent should read, work on, publish, or keep private.
- `evaluateAutonomousSchedule`: decides whether an enabled project is due to run, why it is due or waiting, and what the next scheduled time is.
- `advanceAutonomousProjectCycle`: turns that cycle into a project-state update with logs, task touches, task-completion events, progress movement, scheduler evidence, `lastAutonomousRunAt`, and `nextAutonomousRunAt`.
- `publishAutonomousCycleChat`: publishes the visible autonomous cycle messages into group chat semantics, delivering direct management mentions into Agent inboxes/worklogs and appending `group-chat-message` entries to the unified event ledger.

In the local backend, the HTTP server can run the same hourly/daily worker loop on an interval. A production backend can replace the interval runner with a queue or cron process while keeping the same due-worker contract, then optionally call the configured BYOK model provider only for Agent plans that actually need generation.

### Backend Service Boundary

`agentProjectService.js` is the backend-oriented facade over the pure runtime. It is intentionally independent from React and localStorage so an API route, queue worker, or local prototype can call the same contract:

- `createAgentProjectService`: wraps a project repository and exposes backend-style commands for tests, prototype workers, and future API routes.
- `createAgentProjectMemoryStore`: the in-memory repository adapter for tests and browser-adjacent prototypes. It stores hydrated project snapshots, retained project messages, durable kickoff meeting sessions, backend security audit-stream records, and signed access replay records, exposes `saveProject`, `saveKickoffMeeting`, `appendMessages`, `getMessages`, `appendSecurityAuditRecords`, `listSecurityAuditRecords`, `appendAccessReplayRecords`, `listAccessReplayRecords`, and `snapshot`, and can be replaced by another adapter with the same shape.
- `createAgentProjectFileStore`: the Node-backed repository adapter. It persists the same project/message/kickoff-meeting/security-audit-stream/access-replay snapshot to JSON on disk, mirrors security access decisions into an append-only JSONL audit sink, reloads the snapshot, replay records, and audit sink on service restart, and proves the backend command facade can survive process boundaries before a database adapter exists. Security audit-stream rows carry project-local `previousStreamHash` / `streamHash` links so the read model can detect missing, broken, or mismatched local audit chains.
- `createAgentProjectApi`: an HTTP-shaped command handler over the service. It accepts `{ method, path, body }` and returns `{ status, body }` for `GET /kickoff-meetings`, `POST /kickoff-meetings`, `GET /kickoff-meetings/:id`, `POST /kickoff-meetings/:id/approve`, `GET /projects`, `POST /projects/initiate`, `GET /projects/:id`, `GET /projects/:id/messages`, `GET /projects/:id/timeline`, `GET /projects/:id/events`, `GET /projects/:id/tasks`, `GET /projects/:id/tasks/:taskId`, `GET /projects/:id/tasks/:taskId/evidence`, `GET /projects/:id/readiness`, `GET /projects/:id/readiness-proof-map`, `GET /projects/:id/artifact-quality-audit`, `GET /projects/:id/submission-review-workflow`, `GET /projects/:id/evidence-quality-audit`, `GET /projects/:id/mvp-readiness`, `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run`, `GET /projects/:id/pilot-launch-readiness`, `GET /projects/:id/deployment-preflight`, `GET|POST /projects/:id/launch-approvals`, `GET /projects/:id/private-pilot-go-live-readiness`, `GET|POST /projects/:id/private-pilot-release-candidates`, `GET|POST /projects/:id/private-pilot-launch-runs`, `GET|POST /projects/:id/private-pilot-launch-health-checks`, `GET|POST /projects/:id/private-pilot-acceptance-reports`, `GET /projects/:id/production-operations-readiness`, `GET|POST /projects/:id/production-operations-control-receipts`, `GET|POST /projects/:id/production-security-control-receipts`, `GET|POST /projects/:id/identity-sessions`, `POST /projects/:id/identity-sessions/:sessionId/revoke`, `GET /projects/:id/persistence-snapshot`, `GET /projects/:id/persistence-migration-plan`, `GET /projects/:id/persistence-migration-dry-run`, `GET /projects/:id/persistence-adapter-plan`, `GET /projects/:id/persistence-adapter-dry-run`, `GET /projects/:id/operations-readiness`, `GET /projects/:id/provider-readiness`, `GET /projects/:id/provider-controlled-run`, `GET|POST /projects/:id/provider-eval-runs`, `GET /projects/:id/security-boundary`, `GET /projects/:id/security-access-audit`, `GET /projects/:id/security-audit-stream`, `GET|PUT /projects/:id/membership-policy`, `GET /projects/:id/worker-queue`, `GET /projects/:id/worker-queue-adapter-plan`, `GET /projects/:id/worker-queue-adapter-dry-run`, `GET /projects/:id/manager-dashboard`, `GET /projects/:id/agents`, `POST /projects/:id/agents/contract`, `GET /projects/:id/agents/:agentId`, `GET /projects/:id/agents/:agentId/dashboard`, `GET /projects/:id/agents/:agentId/inbox`, `GET /projects/:id/agents/:agentId/worklog`, `GET /projects/:id/agents/:agentId/obligations`, `GET /projects/:id/agents/:agentId/plan`, `POST /projects/:id/agents/:agentId/message`, `POST /projects/:id/agents/:agentId/work-cycle`, `POST /projects/:id/chat`, `POST /projects/:id/meeting`, `POST /projects/:id/autonomous-cycle`, `POST /workers/autonomous/due`, `POST /workers/agents/due`, and `GET|POST /workers/queue-snapshot`. The launch approval route now accepts operations-owner as a first-class signed project role for production approvals and returns a checksummed workflow proof surface.
- The same API layer also exposes `GET /projects/:id/production-launch-gap-register` for the read-only production launch gap register described below, `GET /projects/:id/production-launch-control-center` for the public-production release control view, `GET /projects/:id/production-launch-evidence-dossier` for the launch manifest/evidence package, and `GET /projects/:id/production-evidence-integrity-audit` for the production receipt evidence-tier audit.
- `GET|POST /projects/:id/private-pilot-launch-health-checks`: the post-launch private-pilot health route. `GET` returns `private-pilot-launch-health-check-workflow/v1`; `POST` writes a checksummed `private-pilot-launch-health-check/v1` receipt after the launch run and operations/security/provider/evidence/persistence/queue/proof gates remain healthy.
- `GET|POST /projects/:id/private-pilot-acceptance-reports`: the customer private-pilot acceptance closeout route. `GET` returns `private-pilot-acceptance-report-workflow/v1`; `POST` writes a checksummed `private-pilot-acceptance-report/v1` receipt after release/launch/health/evidence/proof gates remain ready.
- `GET /projects/:id/production-operations-readiness`: the production operations hardening route. It returns `production-operations-readiness/v1`, passing local/private-pilot operations proof after customer acceptance while keeping public production blocked until centralized observability, alert routing, on-call ownership, managed incidents, restore drills, centralized audit retention, and managed database/queue cutover approval exist.
- Provider eval and the private-pilot release, launch, health, and acceptance POST routes accept `includeReadModels: false`. They then return the checksummed receipt/workflow plus explicit provider, Manager Ready Package, Flow Graph, Proof Map, launch submodel, and production-operations refresh routes instead of embedding full Manager read models in the write response.
- `GET|POST /projects/:id/production-operations-control-receipts`: the production operations evidence receipt route. `GET` returns `production-operations-control-receipt-workflow/v1`; `POST` writes checksummed `production-operations-control-receipt/v1` rows for centralized observability, alerting, on-call, incident, restore, audit-retention, and managed cutover controls.
- `GET|POST /projects/:id/production-security-control-receipts`: the production security evidence receipt route. `GET` returns `production-security-control-receipt-workflow/v1`; `POST` writes checksummed `production-security-control-receipt/v1` rows for managed identity, service identity, managed KMS, database RBAC, centralized security audit, and session replay hardening controls.
- `GET /projects/:id/production-launch-audit` is the unified release audit route. It is also embedded in Manager Ready Package as `production-launch-audit/v1`.
- `GET|POST /projects/:id/launch-approvals` is the release approval and change-management route. It returns `launch-approval-workflow/v1`; POST writes checksummed `launch-approval/v1` records with release mode, decision, approver role/id/name, reason, and linked audit checksum. Private pilot requires Manager plus security-admin approval; production additionally requires operations-owner. Records are persisted to `project.launchApprovals`, timeline, event ledger, Manager Flow Graph, Readiness Proof Map, Manager Ready Package, production launch audit, and the `launch_approvals` persistence/migration seed path.
- Autonomous proof read models include `GET /projects/:id/product-team-operating-loop`, `GET /projects/:id/team-collaboration-diagnostics`, `GET /projects/:id/collaboration-intent-queue`, `GET /projects/:id/runtime-contracts`, and `GET /projects/:id/autonomous-cycle-consistency`; these routes are the current C/A coordination, diagnostics, meeting/chat intent queue, contract-freeze, and N-step consistency surfaces for replacing frontend mock inference.
- Project-scoped command responses such as kickoff initiation/approval, chat, meeting, autonomous-cycle, and per-Agent work-cycle include `managerDashboard` in the response body by default, so compatibility clients can update the aggregate backend view immediately after a command. Frontend mock replacement clients can send `includeReadModels: false` to receive a lightweight receipt with `readModels.included: false` plus standalone project, transcript, Manager, Proof Map, and Agent refresh routes instead.
- Due-worker processed items from `POST /workers/autonomous/due`, `POST /workers/agents/due`, and scheduler ticks include `managerDashboard` by default. They also honor `includeReadModels: false`, returning per-project or per-Agent refresh routes without computing full Manager Ready Package snapshots inside the write response.
- `createFileBackedAgentProjectApi`: combines the API handler with the Node file store so a future backend process can start from a JSON snapshot, handle commands, persist results, and restart without losing Agent state.
- `createAgentProjectHttpServer`: mounts the API handler on a real Node HTTP server with JSON request/response handling, CORS headers for access-control, signed-access, and signed request-id headers, and an optional autonomous scheduler controller for `GET /workers/autonomous/status`, `POST /workers/autonomous/tick`, `POST /workers/autonomous/start`, and `POST /workers/autonomous/stop`.
- `npm run agents:server`: starts that local backend on `AGENT_PROJECT_HOST`/`AGENT_PROJECT_PORT` with `AGENT_PROJECT_STORE` pointing at the JSON store file. `AGENT_SECURITY_AUDIT_LOG` can override the append-only JSONL audit sink path; otherwise it defaults to `${AGENT_PROJECT_STORE}.security-audit.jsonl`. Set `AGENT_AUTONOMOUS_SCHEDULER=1` to start the backend worker loop automatically, `AGENT_AUTONOMOUS_INTERVAL_MS` to adjust the polling interval, and explicit unattended Agent controls such as `AGENT_AUTONOMOUS_AGENT_STRATEGY=true`, `AGENT_AUTONOMOUS_AGENT_SUBMISSIONS=true`, `AGENT_AUTONOMOUS_AGENT_REVIEWS=true`, `AGENT_AUTONOMOUS_AGENT_REVIEW_RESPONSES=true`, and `AGENT_AUTONOMOUS_ARTIFACT_TYPE=auto` to let autostart/interval ticks request strategy decisions, submission nodes, Reviewer processing, and review-response loops. These switches are local/private-pilot controls; production still needs managed queue, budget, review, and incident controls. `AGENT_ACCESS_CONTROL_MODE=enforced` requires access-control headers by default on API routes, `AGENT_ACCESS_SIGNING_SECRET` requires valid signed identity headers for enforced requests, `AGENT_ACCESS_REPLAY_PROTECTION=true` requires signed request ids with one-use replay protection persisted in the file store, and `AGENT_ACCESS_AUDIT_FAIL_CLOSED=true` rejects enforced project access when the audit write fails before dispatch.
- `createKickoffMeetingSession` and `approveKickoffMeetingSession`: split initiation into a durable meeting resource and a later manager approval. A session stores the Director brief, invited Agents, role-clarification questions, self-nominations, Leader campaign turns, peer-hearing evidence, recommended Leader/Reviewer, selectable decision options, and `kickoff-generation-provenance/v1`. The provenance contract labels deterministic validation, model-provider rehearsal, provider-backed model generation, or development fallback while keeping `productionClaim: blocked` until production provider controls are approved. Free-form Initiation Roundtable input uses the same `POST /kickoff-meetings/:id/clarify` route as explicit role-question answers, so Manager clarifications are added to the durable session instead of becoming browser-only meeting turns. Approval reuses the saved transcript to create the project, Leader marker, kickoff charter, assignments, first autonomous pulse, and the session/project link.
- `createKickoffProjectFromMeeting`: creates a project from a Director brief and team roster, generates role clarification, self-nomination, Leader campaign, Director-confirmed Leader marker, kickoff charter, Leader assignments, acknowledgements, and the first autonomous work pulse.
- `contractProjectAgent` and `POST /projects/:id/agents/contract`: add or refresh a marketplace Agent as a real project team member. The route writes an `agent-contract/v1` record, timeline log, `agent-contracted` event-ledger entry, Agent state/worklog proof, Manager Flow Graph collaboration node, and deferred Manager/Agent read-model refresh routes when `includeReadModels: false` is used.
- `submitProjectChatMessage`: accepts Director/group-chat/Google Chat text, resolves `@agent`/`@all`, persists the source message into Agent state and `eventLedger`, routes feature changes, Leader assignments, peer handoffs, or ordinary Agent replies, and returns the updated project plus messages to publish.
- `createProjectTranscriptChannel` and `POST /projects/:id/transcripts`: create generic backend-backed collaboration channels instead of browser-only rooms. The write path records `transcript-channel-created/v1`, publishes a channel-created system message, stores `transcriptChannelReceipts`, writes timeline and event-ledger proof, exposes the channel in `GET /projects/:id/transcripts` / `GET /projects/:id/transcripts/:channelId`, lets Manager Flow Graph render the channel as a transcript proof node, and projects the receipt into Readiness Proof Map `transcriptChannelRoutes` / `transcriptChannelSummary`. React's group-chat `+` control uses this contract for real backend projects while keeping local rooms restricted to offline/demo fallback.
- `submitProjectMeetingMessage`: accepts War Room meeting input, persists the Director source message, then routes meeting feature changes through the same confirmation/owner-sync protocol.
- `submitAgentMessage`: accepts one Agent as the message author and optional `targetAgentIds`, publishes the Agent-authored message through the same group-chat receipt path, writes direct targets into their inbox/obligations, writes the sender's worklog, records an `agent-message` timeline log, appends a ledger event with `source: agent-to-agent-message`, projects the message into Readiness Proof Map `agentMessageRoutes` / `agentMessageSummary`, and returns publishable chat messages plus manager/Agent dashboard proof.
- `runProjectAutonomousCycle`: runs `advanceAutonomousProjectCycle`, publishes visible Agent work back to group chat, and appends those chat messages to Agent state and the unified ledger.
- `runAgentWorkCycle`: advances one named Agent as an independently callable worker. It reads that Agent's current plan and owned task, writes private worklog evidence, publishes a visible group-chat progress message, sends management check-ins to any managed or peer-managed Agents, appends timeline/event-ledger proof, updates task evidence ids, and records the run in `agentWorkerLedger` with idempotency, lease, retry state, dead-letter state, and `worker-execution-receipt/v1` checksum metadata. When the caller sets `submitWorkArtifact: true`, a completed owned task is also submitted through `submitAgentArtifact` as a standard Agent work node; `workArtifactType: "auto"` infers the generic product-team artifact type from task intent (`brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `implementation-plan`, `final-deliverable`, or fallback `progress-brief`). Evidence-oriented completed tasks also call the standard `recordAgentEvidenceSearch` contract, creating an `evidence-search` chat/timeline/event proof row and linking it into the submitted `evidence-packet`. Due-Agent workers and HTTP scheduler ticks expose the matching opt-in as `submitWorkArtifacts` / `submitAgentWorkArtifacts`, default scheduler submissions to task-inferred artifact types, and keep autonomous output behind an explicit control until production review and volume policy are ready. When the caller sets `reviewPendingSubmission: true`, the worker finds a pending submission assigned to that Reviewer and records a standard `submission-review` via `reviewAgentSubmission`; due-Agent workers and scheduler ticks expose the plural `reviewPendingSubmissions` flag for controlled queue processing. When the caller sets `respondToReviewObligation: true`, the submitting Agent finds an open `changes-requested` review obligation and submits a linked `revision-note` or `final-deliverable`; due-Agent workers and scheduler ticks expose this as `respondToReviewObligations` for autonomous revision loops.
- Autonomous worker evidence searches are auditable local provider-backed evidence, not loose worklog notes. Worker-generated evidence rows use the `agent-autonomous-worker` provider and include `evidence-provider-receipt/v1`, checksummed `evidence-source-snapshot/v1` records, safe internal proof-route source metadata, source-review readiness, evidence custody rows, archive contents, Flow Graph/Proof Map routes, and persistence rows. This lets continuous Agent work keep growing after scheduler ticks without breaking Manager inspection or provider-readiness coverage.
- `runAgentWorkCycleWithProviderEvidence` and `POST /projects/:id/agents/:agentId/work-cycle` with `useProviderEvidenceSearch: true` let an autonomous Agent worker call the configured search provider before it records the standard evidence-search node. The bridge runs through provider policy, Agent tool grants, budget/rate gates, retry/circuit state, provider-vault binding checks, and provider usage ledger recording; the resulting provider sources and `evidence-provider-receipt/v1` are injected into the same `recordAgentEvidenceSearch` and `submitAgentArtifact` path. `GET /projects/:id/agent-autonomous-action-queue` now marks evidence-packet rows with `providerEvidenceSearchPlanned` and `useProviderEvidenceSearch` when a search provider is enabled, and the async queue run path executes those rows through `runAgentAutonomousActionQueueItemWithProviderEvidence`. Autopilot session ticks and `/workers/autopilot/due` now also have an async provider-evidence branch for evidence-quality targets, so action, loop, and session tick receipts can link provider usage, provider receipts, evidence-search ids, Agent Queue run proof, and an `autopilot-provider-evidence` group-chat coordination message. Before those autonomous provider-backed steps run, `autonomous-provider-preflight/v1` records the call/degrade/queue/pause decision from provider readiness, queue ownership, provider-vault binding, policy, and circuit gates; its checksum/action is copied into provider usage, Agent Queue run receipts, Autopilot action/loop/tick receipts, and due-worker processed rows. The HTTP scheduler uses the async API for Autopilot due-work and forwards provider-evidence controls, and React Backend Worker Station renders the resulting provider evidence receipt next to the Autopilot worker receipt. Manager Flow Graph attaches the provider receipt and transcript route to the Autopilot tick node and links the tick to the evidence-search node; Readiness Proof Map exposes the same evidence-search/provider-usage/transcript routes on session tick routes. Explicit evidence targets can create project-level evidence/submission nodes when no single task owns the target. This is the current A-side to C-side proof bridge for autonomous research/evidence work: the Manager sees provider-backed evidence, worker ledger links, submission source refs, Provider usage rows, Flow Graph/Proof Map routes, Agent Queue run receipts, Autopilot session receipts, transcript proof, and lightweight API receipts instead of a separate manual search artifact.
- The React Agent Focus Workbench uses the same boundary for manager-triggered evidence. It attempts the provider-backed `evidence-searches` route first. If no provider result is available, it can record only an explicit source URL as `manual-source-record`; it no longer turns a blank browser draft into an `agent-note` evidence source. This keeps Research-sample evidence as a test of the generic product-team provider/source contract rather than a front-end note generator.
- `useAutonomousStrategy: true` adds a backend `agent-autonomous-strategy-decision/v1` before an Agent worker acts. The strategy checks open requested-change obligations, pending teammate submissions, completed owned work, management signals, open owned tasks, and monitor-only states; it records the selected action, rationale, resolved controls, and next step in `agentWorkerLedger`, the worker timeline log/event payload, Agent work-cycle responses, due-worker processed rows, Agent Dashboard latest worker data, Manager Dashboard continuous-work rows, and Manager Flow Graph worker-loop summaries. This is the engineering version of Agent subjectivity: the system records why an Agent chose an action without claiming literal consciousness.
- `GET /projects/:id/agent-autonomous-action-queue` exposes those strategy decisions as a standalone backend queue. Each row contains the Agent, selected action, rationale, schedule state, run route, delegated work-cycle route, request body template, proof ids, and checksum. `POST /projects/:id/agent-autonomous-action-queue/:agentId/run` executes the selected row through the normal Agent work-cycle path and returns an `agent-autonomous-action-queue-item-run` receipt plus delegated output objects when the run produced submissions, artifacts, evidence searches, submission reviews, or review-response artifacts. The direct run now also emits `autonomous-action-decision/v1`, preserving the selected strategy, initiative, queue checksum, target artifact/review/evidence intent, and optional provider preflight as the generic "why this Agent acted now" proof. That decision checksum/action is copied into `agent-autonomous-action-run/v1`, Autonomous Run Control delegated receipts, Collaboration Intent receipts, due-worker summaries, Readiness Proof Map routes, and Manager Flow Graph run attachments. The direct run persists `agent-autonomous-action-run/v1` into `agentAutonomousActionRunLedger`, project timeline, and event ledger, then exposes it through Manager Dashboard, Manager Flow Graph, Readiness Proof Map, and the delegated Agent Dashboard. Manager Dashboard embeds the queue, Readiness Proof Map exposes `agentAutonomousActionRoutes` and `agentAutonomousActionRunRoutes`, and Manager Flow Graph renders each row plus executed run receipts as auditable Agent action nodes.
- Agent due-worker responses and HTTP scheduler tick/start status now carry the post-run Agent autonomous action queue, so C-side scheduler controls can immediately show the A-side next-action state after a backend sweep. React `Sync Agent Queue` uses the same visible sync budget as the Autonomy Control read, and after an Autonomous Run Control action the UI awaits a fresh Agent queue read before enabling the next Agent run button. HTTP scheduler start and server autostart both forward the same startup/tick controls into the Agent sweep, including autonomous strategy, submission, Reviewer, and revision-response controls. Scheduler status also returns `scheduler-agent-controls/v1` summaries for startup, scheduled interval, and latest tick controls, giving the Manager side a direct audit of whether background work is running in strategy/submission/revision-response mode.
- Agent autonomous queue rows now include `agent-autonomous-initiative/v1` projections. Each initiative row turns the strategy decision into a Manager-readable intent packet with selected action, rationale, target artifact type, review/evidence targets, run route, and proof/timeline/event ids. `getProductTeamOperatingLoop` exposes these rows under `agentSide.initiativeRows`, and Manager Flow Graph attaches them to both Agent action nodes and the Product Team Operating Loop aggregate node so A-side initiative is route-backed rather than inferred from UI copy.
- The React Manager Dashboard consumes this route through `Sync Agent Queue` and an `Agent Autonomous Queue` panel, and scheduler status/tick/start refreshes hydrate the same queue when it is returned by the backend. The panel shows the backend-selected action, next step, route, and run button for each Agent; running a row uses the backend queue endpoint, renders the returned receipt, renders `autonomous-action-decision/v1` as the Agent action rationale, renders direct `Agent Action Output Nodes` with chat/timeline proof exits for returned Agent product nodes, and refreshes Manager/Agent read models. Run Control, Collaboration Intent, and Flow Graph attachment views render the same decision when they delegate into Agent work. This keeps C-side autonomy controls on the backend contract instead of local mock inference.
- `submitAgentArtifact`: lets one Agent submit a typed artifact as a first-class project record. The service writes an artifact through the configured artifact writer, publishes a submission message into group chat, writes a timeline log, appends an `agent-submission` ledger event, links the record to the task evidence, updates the submitting Agent dashboard/worklog, and exposes the submission to Manager Dashboard, Manager Flow Graph, Readiness Proof Map, and project submission routes. React Manager Dashboard submission rows now expose direct chat/timeline proof buttons for those records.
- `GET /projects/:id/submissions`, `GET /projects/:id/submissions/:submissionId`, and `POST /projects/:id/agents/:agentId/submissions`: expose the generic submission contract for artifact types such as `discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, and `final-deliverable`. These routes are product-team primitives; the Research Project scenario uses them only as a validation sample, not as a research-only protocol.
- `getBrainstormLayer` and `GET /projects/:id/brainstorm-layer`: export `brainstorm-layer/v1`, the read-only brainstorm aggregate above generic `brainstorm-board` submissions. It parses visible alternatives, links discovery/evidence/downstream decision and delivery artifacts, returns proof ids, timeline/event ids, gates, backend routes, summary counts, and checksum, embeds in Manager Dashboard and Manager Ready Package, adds a `brainstorm-layer` aggregate node plus synthesis edges to Manager Flow Graph, exposes Readiness Proof Map `brainstormLayerRoutes`, is covered by enforced access for Manager/security-admin/observer reads, and renders `backend-brainstorm-layer-snapshot` in Manager UI.
- `getAgentDashboard` embeds `agent-brainstorm-contribution/v1` for the route-scoped Agent. The personal dashboard derives rows from that Agent's `brainstorm-board` submissions, parsed alternatives, proof/timeline/event ids, direct evidence/downstream links where present, project evidence/downstream follow-through counts, and a route back to the Manager `brainstorm-layer`, so the Agent's own initiative is inspectable without creating a research-only artifact surface.
- `generateAgentArtifactDraft` and `POST /projects/:id/agents/:agentId/artifact-drafts`: return `agent-artifact-draft/v1` from task, evidence-search, prior-submission, and review context. With `submit: true`, the generated body is immediately persisted through the standard Agent submission contract, so Flow Graph, Task Evidence, Readiness Proof Map, timeline, event ledger, artifact files, archive, and persistence snapshots see it as a normal product-team work node. The submitted record preserves `artifactDraft` provenance with draft id, source, checksum, proof context, local/model status, `artifact-draft-quality/v1` status, and model human-review requirement for Manager Dashboard and Agent Dashboard. The local generator keeps fallback deterministic; provider-backed drafting is gated by `model:artifact-draft`, covered by deterministic model-provider Harness proof, and surfaced in Provider Readiness through the `model-artifact-draft-quality` gate plus `model-output-quality-review` production control.
- `getArtifactQualityAudit` and `GET /projects/:id/artifact-quality-audit`: export `artifact-quality-audit/v1`, the Manager-facing audit for whether submitted product-team artifacts are ready for private-pilot handoff. It aggregates redacted Agent submissions, required generic artifact type coverage, title/summary/body readiness, chat/timeline/event/artifact proof links, review/revision/final-deliverable closure, generated draft quality status, redaction status, local gates, required production controls, and checksum. Manager Ready Package embeds the full audit, Readiness Proof Map exposes `artifactQualityRoutes`, Project Evidence Archive includes it in readiness models, manifest, integrity gates, and full contents, and the archive now gates one-to-one `agent-artifact-storage-proof/v1` / workspace proof coverage for every Agent submission before customer handoff. Pilot/Production launch evidence routes link to the same proof, and enforced access policy allows Manager/security-admin/observer reads.
- `getSubmissionReviewWorkflow` and `GET /projects/:id/submission-review-workflow`: export `submission-review-workflow/v1`, the Manager-facing closure model for generic submission review rounds. It aggregates submitted artifacts, review verdicts, requested changes, linked revision responses, final-deliverable acceptance, proof routes, timeline/event ids, local gates, production controls, and checksum. Manager Ready Package embeds the workflow, Manager Flow Graph adds a `submission-review-workflow` closure node, Readiness Proof Map exposes `submissionReviewWorkflowRoutes`, Security Boundary lists the route policy, enforced access allows Manager/security-admin/observer reads, and Manager UI renders the workflow snapshot while keeping calibrated production review governance blocked.
- `getProductTeamDeliveryTrace` and `GET /projects/:id/product-team-delivery-trace`: export `product-team-delivery-trace/v1`, the Manager-facing trace that joins kickoff, Agent self-marketing, brainstorm, evidence, generated draft, review/revision, accepted final deliverable, and proof surfaces into one generic product-team delivery chain. Manager Ready Package embeds it, Manager Flow Graph adds a `product-team-delivery-trace` aggregate node with edges from brainstorm/evidence/submission/review/final-deliverable proof nodes, Readiness Proof Map exposes `productTeamDeliveryTraceRoutes`, Security Boundary lists the route policy, Manager UI renders `backend-product-team-delivery-trace-snapshot`, and the product-team Harness verifies API/HTTP/Ready Package/Flow Graph/Proof Map/UI coverage. This is the C-side answer to "did the autonomous A-side team actually move from intent to accepted product output?" without making Research Project a special-case runtime.
- Artifact revisions are explicit: `submitAgentArtifact` accepts `revisesSubmissionId`, `respondsToReviewId`, and `supersedesSubmissionIds`. A linked revision marks prior submissions `superseded`, resolves matching review obligations for the original submitter, adds revision metadata to the artifact/submission/timeline payload, exposes `revisionRoutes` and `revisionSummary` in Readiness Proof Map, and creates `revision` edges in Manager Flow Graph.
- `recordAgentEvidenceSearch`: lets an Agent record a generic evidence/source search as a first-class project resource. The record stores query, purpose, provider/search mode, confidence, sources, findings, task/submission links, group-chat proof, timeline proof, and ledger proof. It also computes provider-agnostic evidence judgement: source `qualityScore`, `qualityLevel`, `qualitySignals`, aggregate `evidenceJudgement`, and `qualitySummary`; source safety judgement: `sourceSafetyLevel`, `sourceSafetyScore`, `sourceSafetySignals`, `sourceSafetyJudgement`, and aggregate `sourceSafetySummary`. Manager Dashboard summarizes evidence quality and source safety, Agent Dashboard exposes owned evidence searches, Task Evidence includes linked searches with judgement, Manager Flow Graph creates quality/safety-bearing `evidence` nodes, Readiness Proof Map returns evidence-search routes with quality and source-safety judgement, and Evidence Quality Audit turns the set into a decision-readiness contract.
- `getEvidenceQualityAudit` and `GET /projects/:id/evidence-quality-audit`: export `evidence-quality-audit/v1`, the Manager-facing audit for whether current evidence can support a product decision. It aggregates redacted evidence rows, flattened source rows, source quality and source-safety summaries, provider provenance, Proof Map routes, decision gates, required production controls, and checksum. Manager Ready Package embeds the full audit, Project Evidence Archive includes it in readiness models and manifest, Pilot/Production launch evidence routes link to it, and enforced access policy allows Manager/security-admin/observer reads.
- `getProjectEvidenceArchive` and `GET /projects/:id/project-evidence-archive`: export `project-evidence-archive/v1`, the private-pilot/customer handoff bundle. The archive includes `transcript-proof-coverage/v1`, counting Agent submission, evidence-search, evidence-source-review, submission-review, and provider-backed Autopilot coordination `messageId` proof against backend transcript/channel archived proof. Its `group-chat-transcripts` manifest row and `transcript-proof-coverage` integrity gate fail if a critical work-node chat proof is missing from backend transcripts, so Flow Graph and Proof Map chat exits cannot silently fall back to browser-only history for real projects. The same coverage is exposed in Readiness Proof Map as `transcriptProofCoverageRoutes` / `transcriptProofCoverageSummary`, giving C-side monitoring a backend transcript health route without waiting for customer handoff export.
- `recordAgentEvidenceSearch`, `recordAgentEvidenceSearchWithProvider`, and `GET|POST /projects/:id/agents/:agentId/evidence-searches`: persist provider-backed evidence with `evidence-source-snapshot/v1` records for each source and `evidence-provider-receipt/v1` for provider calls. These records carry request/result/source checksums, retry/circuit metadata, source-safety fields, and links into provider usage ledger, Task Evidence, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, and normalized `evidence_source_snapshots` / `evidence_provider_receipts` persistence rows. This is local/private-pilot audit proof; production still requires managed immutable source/provider storage and retention controls.
- `getEvidenceSourceReviewWorkflow`, `reviewEvidenceSource`, and `GET|POST /projects/:id/evidence-source-review-workflow`: export `evidence-source-review-workflow/v1` and persist `evidence-source-review/v1` decisions. `GET` derives one source review item per evidence source with reviewer handoff, latest decision, review queue, quality/source-safety signals, local source snapshot ids/checksums, provider receipt ids, local/production decision use, proof routes, gates, production controls, and checksum. `POST` records a Reviewer decision for a source, publishes a group-chat message, writes timeline/event-ledger proof, updates the evidence search, task evidence, Agent Dashboard, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, and `evidence_source_reviews` persistence rows. Enforced access policy allows Manager/security-admin/observer reads and Manager/reviewer-agent/security-admin writes with reviewer-agent identity matching; production remains blocked until calibrated human source-review approvals, managed source/provider receipt storage, and managed reviewer audit storage exist.
- `reviewAgentSubmission`: lets a Reviewer accept a submission or request changes as a durable review record. The review updates the linked submission and task, publishes a group-chat review message, writes a `submission-review` timeline/event record, adds revision obligations to the submitter when changes are requested, and creates Manager Flow Graph `review` nodes. The React Manager Dashboard now consumes this route through a submission-row review composer with `includeReadModels: false`, then refreshes Manager/Agent read models from backend routes instead of creating local review rows.
- `GET /projects/:id/evidence-searches`, `GET /projects/:id/evidence-searches/:evidenceSearchId`, `POST /projects/:id/agents/:agentId/evidence-searches`, `GET /projects/:id/submission-reviews`, `GET /projects/:id/submission-reviews/:reviewId`, and `POST /projects/:id/submissions/:submissionId/reviews`: expose the generic evidence/search and review lifecycle behind the same backend boundary as submissions.
- `searchProvider.js`: exposes a BYOK-safe search provider boundary. `GET /search/status` returns provider/configuration state without raw secrets, `POST /search/test` runs a provider health search, and `POST /projects/:id/agents/:agentId/evidence-searches` can pass `useProvider: true` to populate normalized source packets from the configured provider. The deterministic provider supports Harness validation without external network calls; `http-json` is reserved for a private search gateway. Settings can now configure that gateway through backend Vault receipts: `search.endpoint` binds through `setEndpoint(..., 'local-secret-vault', 'http-json')`, `search.apiKey` binds through `setApiKey(..., 'local-secret-vault')`, and the status route reports only redacted endpoint/key source metadata.
- `secretVault.js`: exposes the local BYOK secret-vault contract. It can seal/open provider secrets with Web Crypto AES-GCM envelope records for private pilots, persist encrypted records through `SECRET_VAULT_RECORDS_FILE`, preload records through `SECRET_VAULT_RECORDS_JSON`, rehearse key rotation with `secret-vault-rotation-receipt/v1`, returns `secret-vault-status/v1` with readiness, encrypted/raw record counts, rotation support, and latest rotation metadata, and never returns key material or plaintext secrets through readiness responses. The backend API now exposes this boundary through `GET /secret-vault/status`, `GET /secret-vault/records`, `POST /secret-vault/seal`, and `POST /secret-vault/rotate`; record lists omit ciphertext/plaintext, writes return seal/rotation receipts, and enforced access limits these routes to security/runtime roles. When the sealed record is recognized as `model.apiKey`, `search.apiKey`, or `search.endpoint`, `sealSecretVaultRecord` opens the record in memory and calls the running provider's `setApiKey(...)` or `setEndpoint(...)`, so Settings can make the current backend process usable without persisting plaintext outside the vault. `scripts/agent-project-server.mjs` also opens persisted model/search key records plus search endpoint records at startup, so local restarts can rehydrate providers when the same `SECRET_VAULT_KEY` is supplied. `SECRET_VAULT_ENABLED` / `SECRET_VAULT_KEY` configure the local vault; production should replace this with managed KMS or a secret manager.
- `getProviderVaultBindings`, `GET /provider-vault-bindings`, and `GET /projects/:id/provider-vault-bindings`: expose `provider-vault-bindings/v1`, the redacted bridge between model/search provider status and local secret-vault records. It shows whether each provider is vault-backed, whether search endpoint is vault-backed, deterministic/no-key, missing, vault-record-ready but provider-not-configured, or using a non-vault key source; returns safe record ids/names/key ids/scopes only; emits gates, proof routes, redaction scan status, and checksum; and is embedded in Provider Readiness. This gives C-side Manager surfaces and A-side runtime controls one shared fact about whether provider capability is actually backed by the backend vault boundary.
- `getLocalMvpStartupReadiness` and `GET /local-mvp-startup-readiness`: expose `local-mvp-startup-readiness/v1`, the backend startup preflight used before the UI claims a real local MVP session can start. It aggregates Settings Provider Readiness, model/search runtime status, Secret Vault status, provider vault bindings, project catalog readability, backend routes, validation commands, next action, redaction gates, and production blockers. It can mark Settings API entry as available, provider setup as required, or the first local project run as ready; it always keeps `readyForProduction: false` until managed KMS/Secret Manager, managed persistence, managed queue/cron, real provider rollout, centralized observability, and incident recovery are in place. Manager Ready Package embeds `localMvpStartupReadiness`, and Readiness Proof Map exposes `localMvpStartupReadinessRoutes`.
- `getSettingsHealthReadiness` and `GET /settings/health-readiness`: expose `settings-health-readiness/v1`, the backend-owned Settings Quick Check contract. It returns rows for backend API, Local MVP startup readiness, Secret Vault, model/search provider status, explicit `/llm/test` and `/search/test` routes, worker status route, project catalog, validation commands, and production blockers. It is intentionally read-only and does not spend provider calls or create probe projects; Workflow Smoke remains the explicit deeper UI path for request and Agent probe validation. Manager Ready Package embeds `settingsHealthReadiness`, and Readiness Proof Map exposes `settingsHealthReadinessRoutes`.
- `getSettingsRuntimeReadiness`, `GET /settings/runtime-readiness`, and `GET /projects/:id/settings-runtime-readiness`: expose `settings-runtime-readiness/v1`, the backend-owned Deployment/Models runtime contract. It returns rows for model runtime, search runtime, provider-vault binding proof, worker status route, local persistence adapter, worker queue adapter, and deployment preflight linkage, plus validation commands and production blockers. Manager Ready Package embeds `settingsRuntimeReadiness`, Readiness Proof Map exposes `settingsRuntimeReadinessRoutes`, and React Deployment/Models get one backend contract instead of inferring runtime readiness from scattered status calls.
- Agent provider execution paths now attach `provider-vault-binding-proof/v1` to the concrete work evidence. `generateAgentArtifactDraft`, runtime model-intent enrichment, `recordAgentEvidenceSearchWithProvider`, and `runAgentWorkCycleWithProviderEvidence` pass the binding proof into provider usage rows and evidence-provider receipts. The Agent can still fail closed or fall back through existing provider policy/circuit controls, but every model/search usage row has a checksum/route back to the vault binding contract that was visible before the call.
- `getProviderReadiness` and `GET /projects/:id/provider-readiness`: export the real-provider rollout gate. The read model returns `provider-readiness/v1` with redacted model/search status, deterministic validation-provider proof, provider-backed evidence-search provenance, source-safety summary, provider proof routes, leak-scan status, `provider-control-policy/v1`, retry policy, circuit-breaker policy, local secret-vault seal/open/rotation status, provider usage/cost ledger rows, model-backed artifact-draft quality/human-review summary, and the remaining production controls for managed KMS, revocation, managed audit storage, centralized alerting, calibrated model-output quality evaluation, and real-provider incident handling. The service facade evaluates configured provider calls against allowlists, per-project hourly request limits, daily budget cents, Agent tool grants, retry attempts/backoff, and provider circuit state, then writes `provider-usage` events plus `project.providerUsageLedger` rows. Manager Ready Package embeds this model, and MVP readiness points the `production-real-providers` blocker to this route without claiming production readiness.
- React Settings consumes the same backend provider boundary. The Deployment tab renders `settings-deployment-runtime-boundary`, fetches `settings-runtime-readiness/v1`, and exposes backend worker/provider/deployment routes instead of browser-only environment or endpoint selectors. The Models tab renders `settings-model-runtime-boundary`, reads model/runtime policy evidence from `/llm/status`, and renders the same runtime-readiness contract instead of browser-only model lists. The Integrations tab can sync `settings-integration-readiness/v1` from `/projects/:id/settings-integration-readiness`, showing provider budget, Agent tool grants, Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting as one backend-owned readiness packet instead of a route list or fake editable controls. The Keys tab renders `settings-provider-boundary`, fetches `/settings/provider-readiness`, `/settings/runtime-readiness`, `/llm/status`, `/search/status`, `/secret-vault/status`, `/secret-vault/records`, and `/provider-vault-bindings`, can run `/llm/test` and `/search/test`, and shows only redacted configured/enabled/key-source/vault metadata. `settings-provider-readiness/v1` tells React whether API fields can be typed, whether `Seal` can run, which backend action is required, and which route proves it. Manager Ready Package embeds `settingsProviderReadiness` and `settingsIntegrationReadiness`, and Readiness Proof Map exposes `settingsProviderReadinessRoutes` and `settingsIntegrationReadinessRoutes`. Key entry posts only to `/secret-vault/seal`; after a model/search key or endpoint seal, the backend binds the running provider and subsequent `/llm/status` / `/search/status` calls report `apiKeySource: local-secret-vault` or `endpointSource: local-secret-vault`. It intentionally does not save provider keys in the browser; model-routing, retry, concurrency, and production KMS changes belong to backend environment, provider policy, adapter gateway, the local secret-vault contract, or future managed KMS/Secret Manager flows.
- `getProviderControlledRun` and `GET /projects/:id/provider-controlled-run`: export `provider-controlled-run/v1`, the controlled private-pilot BYOK run plan. It does not call providers; it evaluates provider health checks, kickoff/intent model support, model artifact drafting, and evidence search against provider configured/enabled status, allowlists, Agent tool grants, daily budget, hourly request headroom, retry/circuit state, estimated cost, existing usage-ledger proof, model human-review boundaries, evidence governance, redaction status, and production controls. Manager Ready Package embeds the contract, Readiness Proof Map exposes `providerControlledRunRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin reads, and Manager UI renders the operation plan. It can mark a local private-pilot controlled run ready while keeping `readyForProductionRun: false` until real provider eval runs, managed provider audit storage, centralized cost alerting, production incident runbooks, and calibrated release policy exist.
- `getProviderEvalRunWorkflow`, `recordProviderEvalRun`, and `GET|POST /projects/:id/provider-eval-runs`: export `provider-eval-run-workflow/v1` and persisted `provider-eval-run/v1` receipts. The write path records a no-call `shadow-replay-from-provider-usage-ledger` run, links the controlled-run checksum, replays critical `model:artifact-draft` and `search:evidence` proof from `project.providerUsageLedger`, writes timeline/event proof, adds Manager Flow Graph provider-eval monitoring nodes, exposes Readiness Proof Map `providerEvalRunRoutes`, lists the route in Security Boundary, allows Manager/runtime-platform/security-admin access, renders Manager UI status, and exports `provider_eval_runs` persistence rows. With `includeReadModels: false`, the route returns provider/Manager refresh routes instead of embedding large Manager snapshots. It can mark private-pilot provider eval ready locally while keeping production blocked until real provider eval datasets, managed eval storage, centralized cost alerts, incident runbooks, and calibrated release policy exist.
- `getPrivatePilotReleaseCandidateWorkflow`, `recordPrivatePilotReleaseCandidate`, and `GET|POST /projects/:id/private-pilot-release-candidates`: export `private-pilot-release-candidate-workflow/v1` and persisted `private-pilot-release-candidate/v1` freeze receipts. The write path requires launch approvals, production launch audit private-pilot gates, project evidence export package download audit, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage. It freezes Manager Ready Package, MVP/pilot/deployment readiness, production launch audit, project evidence archive/export package, provider eval run, operations, persistence adapter, and worker queue adapter checksums, writes timeline/event proof, adds Manager Flow Graph release-candidate decision nodes, exposes Readiness Proof Map `privatePilotReleaseCandidateRoutes`, lists the route in Security Boundary, allows Manager/security-admin/operations-owner writes, renders Manager UI status, and exports `private_pilot_release_candidates` persistence rows. It can mark a private-pilot release candidate ready locally while keeping production blocked until real managed identity, database, queue, KMS, provider eval, centralized audit, deployment, and operations controls exist.
- `getPrivatePilotGoLiveReadiness` and `GET /projects/:id/private-pilot-go-live-readiness`: export `private-pilot-go-live-readiness/v1`, the read-only Manager command view for the private-pilot lifecycle. It derives stage rows, active phase, next action, proof ids, timeline/event ids, backend routes, and checksum from delivery proof, launch approvals, evidence handoff, provider eval, deployment/operations/security preflight, release candidate, launch run, post-launch health, customer acceptance, and production operations hardening receipts. Manager Ready Package embeds the model, Manager Flow Graph adds a `private-pilot-go-live-readiness` command node, Readiness Proof Map exposes `privatePilotGoLiveRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders the stage panel while keeping `readyForProduction: false`.
- `getProductionLaunchGapRegister` and `GET /projects/:id/production-launch-gap-register`: export `production-launch-gap-register/v1`, the read-only Manager action register for remaining public-production launch work. It deduplicates blockers from production launch audit, production evidence integrity audit, deployment preflight, production operations readiness, provider readiness, evidence custody, artifact quality, and submission review governance into gap rows with domain, owner, severity, action, route, proof ids, timeline/event ids, upstream checksums, next action, and checksum. Manager Ready Package embeds the model, Manager Flow Graph adds a `production-launch-gap-register` decision node, Readiness Proof Map exposes `productionLaunchGapRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders the gap register while keeping `readyForProduction: false`.
- `getProductionLaunchControlCenter` and `GET /projects/:id/production-launch-control-center`: export `production-launch-control-center/v1`, the read-only Manager control center for public-production launch decisions. It aggregates production launch audit, gap register, private-pilot go-live readiness, production operations receipts, production deployment receipts, production security receipts, production provider receipts, production evidence integrity audit, launch approvals, deployment preflight, provider readiness, security boundary, evidence custody, and artifact quality into control rows, blocked rows, owner rows, stage rows, next action, proof ids, timeline/event ids, upstream checksums, and checksum. Manager Ready Package embeds the model, Manager Flow Graph adds a `production-launch-control-center` decision node, Readiness Proof Map exposes `productionLaunchControlCenterRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders the control center while keeping `productionDecision: no-go`.
- `getProductionLaunchEvidenceDossier` and `GET /projects/:id/production-launch-evidence-dossier`: export `production-launch-evidence-dossier/v1`, the read-only Manager evidence package for public-production launch review. It aggregates production launch audit, gap register, control center, production evidence integrity audit, private-pilot go-live readiness, deployment/security/provider/operations readiness, and production control receipt workflows into a manifest, four control-domain rows, open gap rows, evidence-tier summary, launch state, proof ids, timeline/event ids, event ids, backend routes, summary counts, and checksum. Manager Ready Package embeds the model, Manager Flow Graph adds a `production-launch-evidence-dossier` node fed by the control center, gap register, and evidence integrity audit, Readiness Proof Map exposes `productionLaunchEvidenceDossierRoutes`, Security Boundary lists the route policy, enforced access allows Manager/runtime-platform/security-admin/observer reads, and Manager UI renders the dossier while keeping `productionDecision: no-go`.
- `getProductionEvidenceIntegrityAudit` and `GET /projects/:id/production-evidence-integrity-audit`: export `production-evidence-integrity-audit/v1`, the read-only evidence-tier audit above operations, deployment, security, and provider production control receipts. It derives the latest receipt per required control, preserves receipt `evidenceEnvironment`, classifies evidence as `missing`, `local-rehearsal`, `external-unattested`, or `managed-production`, and returns control rows, domain rows, proof ids, timeline/event ids, backend routes, summary counts, and checksum. Manager Ready Package embeds the model, `production-launch-audit/v1` consumes it as a production gate, `production-launch-gap-register/v1` opens or closes the matching managed-production evidence gap from it, Manager Flow Graph adds a `production-evidence-integrity-audit` decision node, Readiness Proof Map exposes `productionEvidenceIntegrityRoutes`, Security Boundary/access policy lists the GET route, enforced access allows Manager/runtime-platform/security-admin/operations-owner/observer reads, and Manager UI renders `backend-production-evidence-integrity-audit-snapshot`. The audit can become production-evidence-ready only when every required production control has explicit managed-production evidence; it does not replace broader launch approval or real infrastructure proof.
- Backend-online real Manager UI sessions now treat missing production launch/control/evidence/receipt read models as `backend-model-missing` / `backend-required`. The React dashboard only derives production fallback rows for offline or explicit demo/sample projects, so C-side release panels cannot claim an A-side backend contract that was not produced.
- `getPrivatePilotLaunchRunWorkflow`, `recordPrivatePilotLaunchRun`, and `GET|POST /projects/:id/private-pilot-launch-runs`: export `private-pilot-launch-run-workflow/v1` and persisted `private-pilot-launch-run/v1` activation receipts. The write path requires a frozen release candidate plus launch audit, audited evidence handoff package, archive/provider/proof/deployment/operations/security readiness, runbook, and incident-drill coverage. It freezes release-candidate, launch audit, evidence, provider eval, deployment, operations, persistence adapter, and worker queue adapter checksums, writes timeline/event proof, adds Manager Flow Graph launch-run decision nodes, exposes Readiness Proof Map `privatePilotLaunchRunRoutes`, lists the route in Security Boundary, allows Manager/security-admin/operations-owner writes, renders Manager UI status, and exports `private_pilot_launch_runs` persistence rows. It can mark a controlled private-pilot launch run ready locally while keeping public production blocked until real environment promotion, rollback, centralized audit, incident ownership, and managed infrastructure controls exist.
- `getPrivatePilotLaunchHealthCheckWorkflow`, `recordPrivatePilotLaunchHealthCheck`, and `GET|POST /projects/:id/private-pilot-launch-health-checks`: export `private-pilot-launch-health-check-workflow/v1` and persisted `private-pilot-launch-health-check/v1` post-launch health receipts. The write path requires a launch run receipt plus operations, queue adapter, persistence adapter, security, provider eval, evidence archive, Flow Graph, and Proof Map health. It freezes launch-run and health checksums, writes timeline/event proof, adds Manager Flow Graph monitoring nodes, exposes Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, lists the route in Security Boundary, allows Manager/security-admin/operations-owner writes, renders Manager UI status, and exports `private_pilot_launch_health_checks` persistence rows. It can mark private-pilot monitoring ready locally while keeping public production blocked until centralized observability, alert routing, on-call ownership, managed incidents, and restore drills exist.
- `secretRedaction.js`: provides the prototype persistence guard for obvious API keys, bearer tokens, secret-bearing URLs, provider errors, evidence source summaries, Agent submission text/source refs, review comments, task source refs, workspace artifact drafts, and ledger payloads. This keeps the local store and proof surfaces from retaining raw BYOK-style fixtures, but it does not replace managed secret storage, revocation, or production access control.
- `accessControl.js`: provides the prototype backend access decision layer. The API defaults to `prototype-open` for local demo compatibility; requests can set `x-hofs-access-mode: enforced` plus `x-hofs-role`, `x-hofs-agent-id`, and `x-hofs-user-id` to prove Manager/Agent/Reviewer/runtime/security-admin/observer decisions before route dispatch. When the API is created with `signingSecret` or the server is started with `AGENT_ACCESS_SIGNING_SECRET`, enforced requests must also include `x-hofs-signed-at` and `x-hofs-signature`; unsigned or tampered identity claims are rejected before role policy evaluation, and the HTTP scheduler signs its own runtime worker calls. When `createAgentProjectApi` receives `requireSignedRequestIds` or the server receives `AGENT_ACCESS_REPLAY_PROTECTION=true`, signed enforced requests must include a signed `x-hofs-request-id` that the file-backed backend stores as an `accessReplayRecords` entry and accepts only once within the freshness window; custom memory-only APIs still use the API memory cache fallback. When `createAgentProjectApi` receives `failClosedOnAuditError` or the server receives `AGENT_ACCESS_AUDIT_FAIL_CLOSED=true`, enforced project access returns `503 access-audit-write-failed` if the audit sink is missing or throws before route dispatch. When `createAgentProjectApi` receives `requireProjectMembership`, project-scoped enforced requests check a `project-membership-policy/v1` record from `projectMemberships`/`projectMembershipResolver` or, by default, from the persisted project state written by `GET|PUT /projects/:id/membership-policy`. That policy covers manager/security/observer/runtime user ids, team Agent ids, Reviewer Agent ids, Agent runtime user bindings, Reviewer runtime user bindings, and revoked user/Agent lists. Enforced mode checks Agent self-scope, Reviewer identity match, runtime worker access, security-admin-only sensitive exports, optional replay protection, optional audit fail-closed behavior, and optional project membership. Enforced decisions are persisted by `recordAccessDecision` into `securityAccessAudit`, linked into the event ledger as `security-access` events, mirrored into the backend security audit stream with sequence/checksum/hash-chain proof, exposed through `GET /projects/:id/security-access-audit` and `GET /projects/:id/security-audit-stream`, and exported in the persistence snapshot as `project_membership_policies`, `project_membership_grants`, `security_access_audit`, and `security_audit_stream`.
- `evaluateAgentWorkSchedule` and `runDueAgentWorkCycles`: scan each project's team for Agents whose `nextAgentRunAt` is due, rank due Agents by management pressure, run only the selected independent Agent workers, persist processed projects/messages, and report skipped Agents whose personal cadence is still waiting or whose priority lost to the per-project cap.
- `agentManagementPriority`: folds each Agent's manager relation, peer-manager relation, open owned tasks, open obligations, management inbox signals, peer handoffs, and review sweep signals into a priority score and human-readable reasons. Those reasons are returned by `POST /workers/agents/due`, written into `agentWorkerLedger`, and echoed in the visible Agent work message.
- `runDueProjectAutonomousCycles`: scans persisted projects, uses `evaluateAutonomousSchedule` to separate due from not-due projects, runs only due autonomous cycles, persists processed projects, and reports skipped projects without mutating them.
- The HTTP scheduler controller wraps both due-worker routes with status, manual tick, start, and stop controls, so a running backend can keep autonomy-enabled projects and independently due Agents moving without a browser tab. Scheduler status includes project processed/skipped counts and Agent processed/skipped counts. `POST /workers/autonomous/start` forwards its full startup body into the immediate sweep when `runImmediately` is set, so Manager UI start controls can request Agent autonomous strategy, standard submission nodes, Reviewer processing, and review-obligation responses through the same backend contract as manual ticks. Server autostart uses the same path and preserves the startup controls for later interval ticks, allowing `npm run agents:server` to run an unattended local/private-pilot loop without falling back to browser-local mutation. When a tick specifies `forceProjectIds` or `forceAgentProjectIds`, non-target projects are filtered before max-project limits are applied; this keeps manager-directed pulses focused on the intended project instead of letting unrelated due work consume the slot. Worker API responses reuse per-project Manager Dashboard / Manager Ready Package read models within the same request, and the local HTTP server drains idle/keep-alive sockets during `close()` so validation runs and desktop backend restarts do not hang on retained fetch connections.
- The project dashboard renders a `Backend Worker Station` panel. It checks the configured backend URL (`VITE_AGENT_BACKEND_URL`, default `http://127.0.0.1:8787`), lets the manager edit and persist that URL, shows online/offline scheduler counters, exposes start/stop/status controls, starts backend-owned immediate project and Agent sweeps, can sync the current project to the backend for a `Server Pulse`, and can pull the backend project/messages snapshot back into the manager UI with `Sync State`.
- The same station now pulls `GET /projects/:id/manager-dashboard` after online status checks, syncs, backend chat/meeting commands, Server Pulse, and Agent Pulse. It also exposes `Sync Manager View` for a manual aggregate-only refresh. The resulting `Backend Manager Snapshot` shows readiness score, proof-route count, transcript proof count, operations Agents, management checks, assignment rows, change rows, and open task count so the manager can see the backend's aggregate view without leaving the dashboard.
- When the backend station is online, project group-chat and War Room meeting submissions use backend project commands first, then merge the returned project/messages into the UI. If the backend command fails on a real project, the UI shows the failure and does not mutate browser-local state; local runtime fallback is limited to offline, explicit demo/sample projects, or the development fallback switches.
- Real initiation approval also uses the backend kickoff endpoint when the backend station is online. The returned project, kickoff messages, role negotiation, Leader election, charter, and assignment package are merged into the UI. If approval fails, the React flow now stays in the initiation decision step, shows the backend failure, and does not create a local project unless an explicit development fallback flag is enabled.
- `getTimeline`, `getEventLedger`, `listTasks`, `getTask`, and `getTaskEvidence`: expose the manager's backend read model for project logs, replayable ledger events, task ownership, and task-level chat/log/event proof.
- `listAgentStates` and `getAgentState`: expose each Agent as an independently queryable backend resource, including inbox, obligations, current plan, fixed routine, and private worklog.
- `getAgentDashboard`: returns the per-Agent aggregate read model for one Agent: identity, state, inbox, obligations, worklog, current plan/routine, next run, latest worker, owned tasks with evidence routes, owned submissions, owned evidence searches, relevant submission reviews, management relationships, management proof logs, relevant chat/timeline/event proof ids, and backend route hints. This is the Agent-level counterpart to `getManagerDashboard`.
- `evaluateReadiness`: audits the persisted project and service message window against the manager-ready scenario checks.
- `getReadinessProofMap`: turns each readiness check into a backend evidence route with `proofKind`, `apiPath`, chat proof ids, timeline log ids, task ids, event ids, and Agent ids so the manager UI and future API clients can prove every pass/fail condition without duplicating front-end mapping logic.
- `getManagerDashboard`: returns the manager-ready aggregate read model for one project: readiness, proof map, transcript index, latest messages, event-ledger/timeline summary, 24/7 operations rows, Agent management mesh, Agent communication flow, kickoff meeting flow with conversation rows, kickoff execution flow, Leader assignment flow, change flow, peer handoffs, submission summaries, evidence-search summaries, submission-review summaries, task evidence rows, and backend route hints.
- `agentProjectService` keeps a service-instance cache for heavyweight read models with the current project/message/provider/security signature. The cache covers Manager Dashboard, Manager Flow Graph, Manager Ready Package, readiness/preflight/launch-audit packages, persistence adapter dry-runs, operations readiness, and worker queue adapter dry-runs, and it is cleared whenever the service writes project state, messages, or kickoff meetings. This is a reliability/performance guard for long Harness runs and repeated backend UI refreshes; it does not change the route contracts.
- `getMvpReadiness` and `GET /projects/:id/mvp-readiness`: return the product-team MVP launch gate. It marks the core project as `mvp-local-candidate` only when kickoff governance, self-marketing, brainstorm, evidence judgement, draft-review-revision, final delivery, proof surfaces, backend worker proof, and Agent dashboards are all covered. It also emits `operatorActions` plus a routed `nextShortestPath` with scope, owner, method, API path, run path, and production-blocker flags so C-side clients can move from local MVP closure to private-pilot handoff or production hardening from the backend contract. Missing core-loop actions can carry `autopilot-delivery-target-control/v1`, which maps the Manager's C-side readiness choice to a target stage, target step, preferred Agent/worker lane, artifact or review controls, target checksum, `submitWorkArtifactOn: "always"` for artifact creation, and `run-mvp-readiness-target` route. The draft/review/revision/final-deliverable gaps are decomposed according to current proof into product brief drafting, requested-changes review, linked revision response, final deliverable submission, and final acceptance. `runMvpReadinessOperatorAction` and `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run` record the Manager's selected readiness action as `mvp-readiness-operator-action-run/v1`, then mirror it into timeline, event ledger, Flow Graph, Proof Map, Collaboration Intent Queue, and Autonomous Run Control when the target is still open. Running `run-mvp-readiness-target` delegates through Agent Autonomous Queue and returns the generated Agent submission/review/evidence record, run receipt, timeline/event ids, Flow Graph node, and Proof Map route for the missing generic stage. It separately keeps `readyForProduction: false` while production blockers remain and never makes production-hardening receipts autonomous.
- `getPilotLaunchReadiness` and `GET /projects/:id/pilot-launch-readiness`: return the private pilot launch package. The same `pilot-launch-readiness/v1` contract is embedded in Manager Ready Package and exposed as a standalone route. It aggregates MVP readiness, Manager proof routes, Manager Flow Graph, submissions/evidence/reviews, Security Boundary, Provider Readiness, managed persistence adapter dry-run, worker queue adapter dry-run, Operations Readiness, and incident drill receipts. It can return `privatePilotDecision: go` for a completed local acceptance project, but keeps `productionDecision: no-go` until real production controls replace the local contracts.
- `getDeploymentPreflight` and `GET /projects/:id/deployment-preflight`: return the private pilot deployment preflight contract. The same `deployment-preflight/v1` contract is embedded in Manager Ready Package and exposed as a standalone route. It checks backend store/audit sink, scheduler env, access-control hardening flags, signed request/replay/audit fail-closed settings, local secret-vault readiness, provider policy, managed persistence adapter status, worker queue adapter status, adapter gateway preflight, operations readiness, and production controls. It can mark `privatePilotDeploymentReady` when blocker gates pass, but keeps `productionDeploymentReady: false`.
- `getProductionLaunchAudit` and `GET /projects/:id/production-launch-audit`: return the unified release audit contract. The same `production-launch-audit/v1` contract is embedded in Manager Ready Package and exposed as a standalone route. It aggregates MVP readiness, pilot launch readiness, deployment preflight, security/provider/operations readiness, project evidence handoff readiness, evidence routes, production blockers, audit-integrity checks, next-shortest-path guidance, and a checksum. A completed acceptance project can return `privatePilotDecision: go`, but `nextShortestPath.scope` remains `private-pilot-handoff` until the governed evidence export package has a download-audit receipt; production remains `no-go` until real managed identity, persistence, queue, provider, audit, and operations controls pass.
- `getProjectEvidenceArchive` and `GET /projects/:id/project-evidence-archive`: return the full manager/customer handoff evidence bundle. The standalone route exposes full `project-evidence-archive/v1` redacted contents, while Manager Ready Package embeds a manifest-only snapshot with the same status, route, gates, counts, and checksums. The full archive redacts sensitive fields, produces manifest checksums, scans for raw secret patterns, and includes project state, transcript channels, Agent submissions, final deliverables, evidence searches, evidence quality audit, submission reviews, revision lineage, Manager Flow Graph, Readiness Proof Map, timeline, event ledger, readiness summaries, persistence summary, and worker recovery summary. It can be ready for manager handoff/private-pilot evidence while still keeping `readyForProduction: false`.
- `getEvidenceCustodyReadiness` and `GET /projects/:id/evidence-custody-readiness`: return the local evidence custody contract. `evidence-custody-readiness/v1` aggregates local `evidence-source-snapshot/v1`, `evidence-provider-receipt/v1`, and submitted `evidence-source-review/v1` decisions into checksummed custody rows with proof ids, timeline/event links, backend routes, local gates, and required production controls. Manager Ready Package, Manager Flow Graph, Readiness Proof Map, Project Evidence Archive, production launch audit, access policy, and Manager UI consume the same model. It can pass local/private-pilot custody for the acceptance project, but keeps `readyForProduction: false` until immutable managed storage, signed custody access, retention/deletion execution, and centralized custody audit exist.
- `getEvidenceIndexReadiness` and `GET /projects/:id/evidence-index-readiness`: return the local evidence/artifact index contract. `evidence-index-readiness/v1` maps evidence searches, source snapshots, provider receipts, Agent submissions, and artifact storage proofs into route-linked rows with checksums and redaction checks. Settings Integrations uses this route for the Vector Store row, proving local MVP indexability without claiming managed vector memory, retrieval retention, or source-custody audit readiness.
- `getProjectMemoryReadiness` and `GET /projects/:id/memory-readiness`: return the local project memory readiness contract. `project-memory-readiness/v1` maps project state, transcripts, backend-derived meeting summaries, evidence/artifact index rows, and the managed persistence adapter plan into route-linked readiness rows, keeps output redaction-clean, and leaves user-editable managed memory production-blocked until managed database/vector adapters, retention/deletion policy, restore drills, and customer export controls exist. Settings Workspace uses this route for the Long-Term Memory row instead of showing a missing backend API or fake memory toggle, and Manager Ready Package plus `GET /projects/:id/readiness-proof-map` expose the same memory readiness route through `projectMemoryReadiness` / `projectMemoryReadinessRoutes` so it is part of the formal proof surface.
- `getBudgetAlertReadiness` and `GET /projects/:id/budget-alert-readiness`: return the local budget alert contract. `budget-alert-readiness/v1` reads `project-provider-budget-policy/v1` plus `project.providerUsageLedger`, computes daily budget and hourly request headroom, exposes route-linked local gates, checks redaction, and keeps centralized metrics, notification routing, escalation/on-call ownership, and managed usage audit storage as production blockers. Settings Integrations uses this route for the Budget Alerts row instead of rendering a fake editable alert control, and Manager Ready Package plus `GET /projects/:id/readiness-proof-map` expose the same contract through `budgetAlertReadiness` / `budgetAlertReadinessRoutes`.
- `getErrorReportingReadiness` and `GET /projects/:id/error-reporting-readiness`: return the local error reporting contract. `error-reporting-readiness/v1` reads Operations Readiness log streams, alert rules, recovery runbook, incident drill, and local error-signal counters, exposes route-linked local gates, checks redaction, and keeps centralized logs, distributed traces, managed incident routing, on-call ownership, retention, and restore-drill evidence as production blockers. Settings Integrations uses this route for the Error Reporting row instead of rendering a fake editable logging control. Manager Ready Package embeds `errorReportingReadiness`, and Readiness Proof Map exposes `errorReportingReadinessRoutes`, so local error proof is included in the Manager-facing launch evidence surface.
- `getSettingsIntegrationReadiness` and `GET /projects/:id/settings-integration-readiness`: return the aggregate Settings Integrations contract. `settings-integration-readiness/v1` reads `project-settings/v1`, `project-integration-capabilities/v1`, provider readiness, adapter gateway preflight, evidence index readiness, budget alert readiness, and error reporting readiness, then returns row schemas, current status, action-required counts, checksums, validation commands, and explicit production blockers. Manager Ready Package embeds `settingsIntegrationReadiness`, and Readiness Proof Map exposes `settingsIntegrationReadinessRoutes`, so Integrations readiness is visible from the same formal proof surfaces as other launch gates.
- `getProjectEvidenceExportWorkflow`, `getProjectEvidenceExportPackage`, `recordProjectEvidenceExport`, and `GET|POST /projects/:id/project-evidence-exports`: return and mutate the private-pilot evidence export governance contract. `GET` returns `project-evidence-export-workflow/v1`; `POST` writes checksummed `project-evidence-export/v1` request, approval, rejection, or download-audit records. Each request pins the archive checksum generated for that request, records retention/data-residency/download-audit metadata, requires Manager plus security-admin approval for private-pilot handoff, and keeps `readyForProductionExport: false`. After approvals, `POST` with `action: "download-audit"` returns `project-evidence-export-package/v1`; `GET /projects/:id/project-evidence-exports/:exportRequestId/package` reads the local package descriptor back. The descriptor includes archive manifest checksums, request/current archive checksums, checksum-drift explanation, watermark metadata, retention/data-residency metadata, package gates, and a download-audit receipt while keeping `downloadUrlIssued: false`. Export governance proof is mirrored into Manager Ready Package, Manager Flow Graph, Readiness Proof Map `projectEvidenceExportRoutes`, timeline, event ledger, route policy, and the `project_evidence_exports` persistence/migration path.
- `getPrivatePilotReleaseCandidateWorkflow`, `recordPrivatePilotReleaseCandidate`, and `GET|POST /projects/:id/private-pilot-release-candidates`: return and mutate the private-pilot release-candidate freeze contract. `GET` returns `private-pilot-release-candidate-workflow/v1`; `POST` writes a checksummed `private-pilot-release-candidate/v1` receipt only after launch approvals, production launch audit private-pilot gates, project evidence export package download audit, project evidence archive, provider eval shadow replay, deployment preflight, operations readiness, and security route coverage are ready. The receipt freezes Manager Ready Package, MVP/pilot/deployment readiness, production launch audit, project evidence archive/export package, provider eval run, operations, persistence adapter, and worker queue adapter checksums, then mirrors proof into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map `privatePilotReleaseCandidateRoutes`, route policy, Manager UI, and the `private_pilot_release_candidates` persistence/migration path. It can mark a private-pilot release candidate ready while keeping production blocked until real managed identity, database, queue, KMS, provider eval, centralized audit, deployment, and operations controls exist.
- `getPrivatePilotLaunchRunWorkflow`, `recordPrivatePilotLaunchRun`, and `GET|POST /projects/:id/private-pilot-launch-runs`: return and mutate the controlled private-pilot launch run contract. `GET` returns `private-pilot-launch-run-workflow/v1`; `POST` writes a checksummed `private-pilot-launch-run/v1` receipt only after the release candidate, launch audit, evidence handoff package, archive, provider eval, deployment preflight, operations runbook/incident drill, security boundary, Flow Graph, and Proof Map gates are ready. The receipt freezes release-candidate, audit/evidence/provider/deployment/operations/adapter checksums, then mirrors proof into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map `privatePilotLaunchRunRoutes`, route policy, Manager UI, and the `private_pilot_launch_runs` persistence/migration path. It can mark a controlled private-pilot launch run ready while keeping production blocked until real environment promotion, rollback, centralized audit, incident ownership, and managed infrastructure controls exist.
- `getPrivatePilotLaunchHealthCheckWorkflow`, `recordPrivatePilotLaunchHealthCheck`, and `GET|POST /projects/:id/private-pilot-launch-health-checks`: return and mutate the post-launch private-pilot health contract. `GET` returns `private-pilot-launch-health-check-workflow/v1`; `POST` writes a checksummed `private-pilot-launch-health-check/v1` receipt only after the launch run, operations runbook/incident drill, queue adapter dry-run, persistence adapter dry-run, security boundary, provider eval, project archive, Flow Graph, and Proof Map gates remain healthy. The receipt freezes launch-run and health checksums, then mirrors proof into timeline, event ledger, Manager Flow Graph monitoring nodes, Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, route policy, Manager UI, and the `private_pilot_launch_health_checks` persistence/migration path. It can mark private-pilot monitoring ready while keeping production blocked until centralized observability, alert routing, on-call ownership, managed incidents, and restore drills exist.
- `getPrivatePilotAcceptanceReportWorkflow`, `recordPrivatePilotAcceptanceReport`, and `GET|POST /projects/:id/private-pilot-acceptance-reports`: return and mutate the customer private-pilot acceptance closeout contract. `GET` returns `private-pilot-acceptance-report-workflow/v1`; `POST` writes a checksummed `private-pilot-acceptance-report/v1` only after release candidate, launch run, post-launch health, evidence handoff, generic product-team delivery proof, operations/security/provider proof, Flow Graph, and Proof Map gates remain ready. The report freezes release/launch/health/evidence/Flow Graph/Proof Map checksums, binds launch run and health receipts, then mirrors proof into timeline, event ledger, Manager Flow Graph decision nodes, Readiness Proof Map `privatePilotAcceptanceReportRoutes`, route policy, Manager UI, and the `private_pilot_acceptance_reports` persistence/migration path. It can mark customer private-pilot acceptance ready while keeping production blocked until managed infrastructure, centralized observability, on-call ownership, incident systems, and restore drills exist.
- `getProductionOperationsReadiness` and `GET /projects/:id/production-operations-readiness`: return the production operations hardening read model. `production-operations-readiness/v1` derives local proof gates from private-pilot acceptance, post-launch health, local operations incident drills, security audit-stream proof, provider eval replay, persistence adapter dry-run, queue adapter dry-run, and production launch audit. It separately derives production control gates for centralized logs/metrics/traces, alert routing, on-call ownership, managed incident records, real restore-drill receipts, centralized audit retention, and managed database/queue cutover approval. Manager Ready Package, Manager Flow Graph, Readiness Proof Map `productionOperationsReadinessRoutes`, Security Boundary route policy, and Manager UI all consume the same contract; it can mark private-pilot operations ready while keeping production operations blocked.
- `getProductionOperationsControlReceiptWorkflow`, `recordProductionOperationsControlReceipt`, and `GET|POST /projects/:id/production-operations-control-receipts`: return and mutate the production operations evidence receipt contract. `GET` returns `production-operations-control-receipt-workflow/v1` with required control ids, verified/missing controls, latest receipt, proof ids, timeline/event ids, backend routes, and checksum. `POST` writes checksummed `production-operations-control-receipt/v1` rows for centralized logs/metrics/traces, alert routing, on-call, incident system, restore drill, managed persistence cutover, managed queue cutover, and centralized audit retention evidence. Receipts mirror proof into timeline, event ledger, Manager Flow Graph receipt nodes, Readiness Proof Map `productionOperationsControlReceiptRoutes`, route policy, Manager UI, and `production_operations_control_receipts` persistence/migration rows. They can clear operations hardening while the broader production launch audit still owns the public go/no-go.
- `getProductionDeploymentControlReceiptWorkflow`, `recordProductionDeploymentControlReceipt`, and `GET|POST /projects/:id/production-deployment-control-receipts`: return and mutate the production deployment evidence receipt contract. `GET` returns `production-deployment-control-receipt-workflow/v1` with required deployment control ids, verified/missing controls, latest receipt, proof ids, timeline/event ids, backend routes, and checksum. `POST` writes checksummed `production-deployment-control-receipt/v1` rows for enforced access, replay protection, audit fail-closed behavior, scheduler autostart, real persistence adapter, managed evidence custody, real queue adapter, environment promotion audit, rollback/smoke test, deployment change approval, and production domain/TLS evidence. Receipts mirror proof into timeline, event ledger, Manager Flow Graph receipt nodes, Readiness Proof Map `productionDeploymentControlReceiptRoutes`, route policy, Manager UI, Production Launch Gap/Register inputs, Production Launch Control Center inputs, and `production_deployment_control_receipts` persistence/migration rows. They can clear the deployment hardening slice while the broader production launch audit/control center still owns the public go/no-go.
- `getProductionSecurityControlReceiptWorkflow`, `recordProductionSecurityControlReceipt`, and `GET|POST /projects/:id/production-security-control-receipts`: return and mutate the production security evidence receipt contract. `GET` returns `production-security-control-receipt-workflow/v1` with required managed security control ids, verified/missing controls, latest receipt, proof ids, timeline/event ids, backend routes, and checksum. `POST` writes checksummed `production-security-control-receipt/v1` rows for managed identity provider, service identity boundary, managed KMS/Secret Manager, database-backed RBAC, centralized security audit, and session replay hardening evidence. Receipts mirror proof into timeline, event ledger, Manager Flow Graph receipt nodes, Readiness Proof Map `productionSecurityControlReceiptRoutes`, route policy, Manager UI, Security Boundary production status, Production Launch Control Center inputs, and `production_security_control_receipts` persistence/migration rows. They can clear the security hardening slice while the broader production launch audit/control center still owns the public go/no-go.
- `getProductionProviderControlReceiptWorkflow`, `recordProductionProviderControlReceipt`, and `GET|POST /projects/:id/production-provider-control-receipts`: return and mutate the production provider rollout evidence receipt contract. `GET` returns `production-provider-control-receipt-workflow/v1` with required provider control ids, verified/missing controls, latest receipt, local provider contract status, proof ids, timeline/event ids, backend routes, and checksum. `POST` writes checksummed `production-provider-control-receipt/v1` rows for provider allowlists, budgets/rate limits, Agent tool grants, retry/circuit breakers, provider audit/cost ledger, encrypted secret-vault proof, source safety review, snapshots/provider receipts, model-output quality review, real-provider eval, managed audit/eval storage, centralized cost alerting, release policy, and incident runbooks. Receipts mirror proof into timeline, event ledger, Manager Flow Graph receipt nodes, Readiness Proof Map `productionProviderControlReceiptRoutes`, route policy, Manager UI, Production Launch Gap/Register inputs, Production Launch Control Center inputs, and `production_provider_control_receipts` persistence/migration rows. They can clear the provider rollout slice while the broader production launch audit/control center still owns the public go/no-go.
- `getPersistenceSnapshot` and `GET /projects/:id/persistence-snapshot`: export the current project into a normalized production persistence contract. The snapshot includes table manifests, record counts, compact records, foreign-key refs, checksums, access replay rows, event-ledger sequence checks, and relation issues so a future managed database migration can be validated before replacing the JSON file store.
- `getPersistenceMigrationPlan` and `GET /projects/:id/persistence-migration-plan`: derive a managed database migration plan from the persistence snapshot. The plan includes Postgres-compatible record-envelope DDL, seed batches, table RLS guidance, critical-table coverage, verification gates, blockers, and a cutover checklist for replacing the JSON/file store with managed persistence.
- `getPersistenceMigrationDryRun` and `GET /projects/:id/persistence-migration-dry-run`: simulate the minimum managed persistence adapter contract against the snapshot and migration plan. The dry-run verifies schema-plan coverage, seed order, imported row counts, checksum preservation, primary-key uniqueness, relation integrity, RLS guidance, and migration-plan gates before a real managed Postgres adapter is introduced.
- `getPersistenceAdapterPlan` and `GET /projects/:id/persistence-adapter-plan`: export the managed database cutover bridge. The plan defines `managed-persistence-adapter-contract/v2` methods for schema creation, transactions, batch import, table reads, event/audit/replay/checkpoint writes, checksum and relation verification, RLS verification, backup/restore, shadow-read comparison, cutover commit, and rollback. It requires critical table coverage for project state, messages, events, tasks, Agent states, submissions, evidence searches, reviews, project membership policies, access replay rows, security audit rows, provider usage/eval rows, worker runs, and read-model checkpoints.
- `managedPersistenceAdapter.js`: provides the configurable managed persistence adapter facade. It exports `managed-persistence-adapter-status/v1` from `MANAGED_PERSISTENCE_ADAPTER_DRIVER`, `MANAGED_PERSISTENCE_DATABASE_URL`, `MANAGED_PERSISTENCE_HTTP_ENDPOINT`, `ADAPTER_GATEWAY_HTTP_ENDPOINT`, and `MANAGED_PERSISTENCE_REQUIRE_REAL_ADAPTER`. The default `local-shadow` driver is not a database driver, but it executes the production-facing method names against normalized records and emits `managed-persistence-adapter-execution-receipt/v1` data for connect, schema creation, import, checksum verification, relation verification, RLS verification, backup creation, restore, shadow-read comparison, and rollback. `http-json` can execute through the async private gateway dry-run path; `postgres` still needs a future real driver. All paths must keep production cutover blocked until real database approval exists.
- `getPersistenceAdapterDryRun`, `getPersistenceAdapterDryRunAsync`, and `GET /projects/:id/persistence-adapter-dry-run`: simulate the managed database adapter cutover before replacing the file store. In default mode the service executes the local shadow adapter. In `http-json` mode the API async handler sends the current project snapshot, migration plan, migration dry-run summary, adapter contract, and shadow-read plan to the private gateway and returns `managed-persistence-adapter-gateway-execution/v1`. Manager Ready Package and Operations Readiness embed this read model while production still requires a real managed database adapter, real backup/restore drills, real shadow reads, and a cutover approval process.
- `adapterGatewayClient.js`: defines the shared `http-json` private adapter gateway contract used by managed persistence and worker queue adapters. It validates `GET /health`, `GET /state`, `POST /persistence/dry-run`, and `POST /worker-queue/dry-run` responses, requiring `adapter-gateway-health/v1`, `adapter-gateway-state-summary/v1`, `managed-persistence-adapter-execution-receipt/v1`, and `worker-queue-adapter-execution-receipt/v1`.
- `getAdapterGatewayPreflight`, `getAdapterGatewayPreflightAsync`, and `GET /projects/:id/adapter-gateway-preflight`: expose `adapter-gateway-preflight/v1` for Manager Ready Package and deployment preflight. Local-shadow mode proves the private-pilot adapter rehearsal path is explicit without requiring a gateway endpoint. `http-json` mode calls the configured private gateway's health and state endpoints, checks advertised managed-persistence and worker-queue capabilities, surfaces storage/queue metadata without secrets, and keeps both `productionCutoverReady` and `readyForProduction` false.
- `getProductionInfrastructureRehearsal` and `GET /projects/:id/production-infrastructure-rehearsal`: expose `production-infrastructure-rehearsal/v1`, the product-facing summary of the P2 infrastructure rehearsal boundary. It aggregates adapter gateway preflight, managed persistence plan/dry-run, worker queue adapter plan/dry-run, deployment preflight, production deployment control receipts, operations readiness, production operations readiness, and production launch gaps into one route with domain rows, upstream checksums, a next action, and explicit production blockers. It also emits `managedCutoverGates` and `managedCutoverSummary`, giving the Manager one backend-owned view of gateway, managed persistence, managed queue/cron, deployment, operations/recovery, and gap-closure cutover gates, including receipt-ready counts, blocked gate ids, and the next cutover gate. Verified operations receipts can clear managed persistence/queue cutover domain blockers and matching cutover gates, and verified deployment receipts can clear the `deployment-preflight` domain blocker and `deployment-cutover` gate through `deploymentReceiptReady`; the broader production launch audit/control center still owns public-production go/no-go. Readiness Proof Map mirrors this contract through `productionInfrastructureRehearsalRoutes` / `productionInfrastructureRehearsalSummary`, and Manager Flow Graph emits a `production-infrastructure-rehearsal` proof node that links the rehearsal to deployment, persistence, queue, operations, and gap-register routes.
- `adapterGatewayStore.js`: defines the storage adapter contract behind the runnable gateway. It exposes `adapter-gateway-storage-adapter-status/v1`, async-compatible `readState`, `writeState`, and `summary`, with current `json-file`, `memory`, and `postgres` / `postgres-compatible` drivers selected by `ADAPTER_GATEWAY_STORAGE_DRIVER`. The Postgres-compatible driver exposes `adapter-gateway-postgres-schema-plan/v1`, redacts `ADAPTER_GATEWAY_POSTGRES_URL`, uses `ADAPTER_GATEWAY_POSTGRES_SCHEMA`, and can execute schema/upsert/snapshot write operations plus `adapter-gateway-postgres-readback/v1` snapshot/count parity through a bound query function while still blocking production cutover.
- `adapterGatewayServer.js`: provides the runnable local private adapter gateway reference process. It exposes `GET /health`, `GET /state`, `POST /persistence/dry-run`, and `POST /worker-queue/dry-run`, can require bearer auth, persists imported shadow table records, queue rows, queue leases, dead-letter rows, and receipt summaries through `adapterGatewayStore.js`, and returns gateway execution receipts with `productionCutoverReady: false`. It is the process-level bridge between the backend API dry-run routes and a future real managed database/queue implementation. `npm run adapters:gateway-postgres-store:validate` proves the Postgres-compatible store can emit schema-plan, table-record, queue-row, lease, receipt, snapshot writes, and readback parity through that same bridge.
- `getWorkerQueueSnapshot`, `getProjectWorkerQueue`, `GET|POST /workers/queue-snapshot`, and `GET|POST /projects/:id/worker-queue`: export a no-mutation queue preview for project and Agent workers. Rows include due/waiting status, management priority, idempotency key, lease key, direct run route, due-worker route, concurrency policy, retry state, and expected receipt metadata. The snapshot also includes `worker-queue-retry-policy/v1`, `worker-dead-letter-policy/v1`, completed `worker-execution-receipt/v1` rows, and a derived dead-letter queue so production cron/queue infrastructure can preserve the same scheduling and recovery semantics.
- `workerQueueAdapter.js`: provides the configurable worker queue adapter facade. It exports `worker-queue-adapter-status/v1` from `WORKER_QUEUE_ADAPTER_DRIVER`, `WORKER_QUEUE_HTTP_ENDPOINT`, `ADAPTER_GATEWAY_HTTP_ENDPOINT`, and `WORKER_QUEUE_REQUIRE_REAL_ADAPTER`. The default `local-shadow` driver executes the production-facing method names against due worker rows and emits `worker-queue-adapter-execution-receipt/v1` data for enqueue, lease acquisition, dispatch, execution receipt acknowledgement, retry import, dead-letter storage/recovery, queue inspection, and `worker-queue-adapter-snapshot-parity/v1` parity across queue rows, leases, acknowledgements, and dead-letter recovery. `http-json` can execute through the async private gateway dry-run path; `managed-queue` still needs a future real driver.
- `getWorkerQueueAdapterPlan`, `getWorkerQueueAdapterDryRun`, `getWorkerQueueAdapterDryRunAsync`, `GET /projects/:id/worker-queue-adapter-plan`, and `GET /projects/:id/worker-queue-adapter-dry-run`: export the production queue/cron adapter bridge. The plan defines adapter methods for enqueue, lease acquisition, dispatch, execution receipt acknowledgement, retry, dead-letter, recovery, inspection, and snapshot parity inspection. The dry-run executes the local shadow adapter by default; in `http-json` mode the API async handler sends the project queue snapshot to the private gateway and returns `worker-queue-adapter-gateway-execution/v1` without replacing the local scheduler.
- `getOperationsReadiness` and `GET /projects/:id/operations-readiness`: export the local operations readiness contract. The read model checks worker-run observability, queue idempotency/lease data, queue adapter dry-run status, managed persistence adapter dry-run status, execution receipt coverage, retry/dead-letter recovery readiness, security audit-stream ordering/hash-chain verification, persistence recovery source integrity, migration dry-run status, proof-surface replayability, security boundary visibility, alert-rule drafts, a recovery runbook, and `operations-incident-drill/v1` rehearsal receipts. Each incident drill receipt carries a checksum and proves one local recovery phase such as alert routing, write freeze, persistence verification, queue recovery, audit-chain review, or Manager proof-surface replay. Manager Ready Package embeds this model while production still requires centralized logs/metrics/alerts, incident ownership, a real managed database adapter, durable queue leases, managed dead-letter storage, and backup/restore drills against real infrastructure.
- `getProviderReadiness` / `getProviderControlledRun` / `getProviderEvalRunWorkflow` / `getProductionProviderControlReceiptWorkflow` and `GET /projects/:id/provider-readiness` / `GET /projects/:id/provider-controlled-run` / `GET|POST /projects/:id/provider-eval-runs` / `GET|POST /projects/:id/production-provider-control-receipts`: export the local provider readiness, controlled-run, shadow-eval, and production provider control receipt contracts. They can pass for the deterministic validation-provider path used by the generic product-team Harness when kickoff generation provenance, provider policy, retry/circuit-breaker policy, local secret-vault status, usage ledger, source-safety checks, controlled operation policy decisions, budget/rate headroom, model/search usage proof, shadow replay receipts, production provider receipts, and human-review boundaries are present, while keeping the broader public-production launch blocked until every non-provider launch gate is also closed.
- `getProjectMembershipPolicy`, `setProjectMembershipPolicy`, and `GET|PUT /projects/:id/membership-policy`: persist and read the active `project-membership-policy/v1` record from project state. Updates create a membership audit entry, timeline log, and `project-membership-policy-updated` event-ledger event. The persistence snapshot exports the active policy into `project_membership_policies` and normalized grant/revocation rows into `project_membership_grants`.
- `getIdentitySessions`, `issueIdentitySession`, `verifyIdentitySession`, `revokeIdentitySession`, `GET|POST /projects/:id/identity-sessions`, and `POST /projects/:id/identity-sessions/:sessionId/revoke`: implement the local runtime credential contract. Issuance returns an `identity-session-token/v1` token once, persists only `identity-session/v1` token hashes/checksums/status/expiry/revocation proof, accepts `x-hofs-session-token` on project routes, records verified session ids in access audit rows and backend audit-stream records, and exports `identity_sessions` in the persistence snapshot and migration seed order. Membership-policy writes, identity-session issuance, and identity-session revocation accept `includeReadModels: false`; they then return the security receipt plus explicit Security Boundary, Security Access Audit, Security Audit Stream, membership-policy, identity-session, provider-readiness, and Manager refresh routes instead of embedding large Manager/Security read models. This is for private MVP validation; production still needs a real IdP/session store/service credential system.
- `getSecurityBoundary` and `GET /projects/:id/security-boundary`: export the security boundary read model. It returns route policies for manager, Agent, Reviewer, runtime, persistence, queue, membership policy, identity sessions, and security routes; the `access-control-policy/v1` contract including signed-header, replay-protection, audit fail-closed, project-membership, and identity-session contracts; active membership policy summary; local identity-session active/revoked/expired summary plus public rows; access-audit summary, audit-stream hash-chain summary, and routes; sensitive-field coverage for provider config, submissions, evidence searches, reviews, messages, events, artifacts, and identity-session hashes; provider status boundaries; local secret-vault status; redaction scan counts/checksums; and explicit production blockers for identity, project RBAC, managed KMS/secret rotation, immutable access audit logs, and abuse controls. Manager Ready Package embeds this model and MVP readiness points the secret-vault/RBAC blocker to this route.
- `getSecurityAccessAudit` and `GET /projects/:id/security-access-audit`: return `security-access-audit/v1`, including persisted access decisions, denied rows, actor/route/status fields, linked `security-access` event ids for replay, and a backend audit-stream summary.
- `getSecurityAuditStream` and `GET /projects/:id/security-audit-stream`: return `security-audit-stream/v1`, including store-level access decision records, append-order sequence numbers, stream checksums, `previousStreamHash` / `streamHash` links, hash-chain readiness/mismatch counts, storage metadata, append-only JSONL sink path when file-backed, and the `security_audit_stream` migration target. This is still prototype storage, not immutable centralized production logging.

The React initiation approval, chat input, and Roundtable input now use this service boundary, so the manager demo and the future backend API exercise the same orchestration path. Initiation approval is the stricter project-creation path: backend failure surfaces as a visible initiation error instead of silently creating a frontend/local fallback project, while explicit development fallback remains available for local debugging. Scenario validation also snapshots the store, rebuilds a fresh service, writes the file-backed store to disk, reloads it, drives the HTTP-shaped API handler, boots the real Node HTTP server, calls it with `fetch`, creates and approves a durable kickoff meeting session, creates a project through `POST /projects/initiate`, restarts it, and proves that kickoff, chat, meeting, Leader assignment, readiness, and autonomous worker dispatch still work after reload.

The current React app already exercises the same contract:

- Projects can set `autonomy.enabled` and `autonomy.cadence`.
- The workspace periodically checks whether a project is due for its next cycle.
- The project dashboard exposes `Hour Pulse` and `Day Report` controls for immediate verification.
- Cycle output is written into project logs, task state, task-completion logs, progress, `lastAutonomousRunAt`, `nextAutonomousRunAt`, scheduler ledger records, and project-scoped group chat records.
- Each cycle also writes `agentStates`: per-Agent manager relation, inbox, open obligations, current plan, owned task ids, private worklog, status, and last active timestamp.
- Each autonomous cycle now emits management-loop evidence: Leader `management-check-in` events for managed Agents with open tasks, Reviewer `review-sweep` events for evidence continuity, and peer `peer-management-check-in` events for accepted dependency handoffs.
- These management events are written to the timeline, counted in the autonomous ledger, delivered to the target Agent's inbox, and mirrored to group chat as visible `@agent` check-ins.
- Each Agent has a fixed work routine derived from its role capability. Autonomous cycles store the routine label, checklist, and expected artifact in both `agentStates.currentPlan.routine` and `autonomousLedger.agentPlans`.
- The manager dashboard renders a `Fixed Work Routines` matrix for every Agent, showing routine label, expected artifact, checklist, current focus, next evidence step, and the latest worker/worklog source.
- Approved kickoff meetings write a structured `kickoffCharter` so the project has a durable starting agreement before autonomous work begins.
- The backend now also supports durable pre-approval kickoff meeting sessions. `POST /kickoff-meetings` stores the meeting transcript and manager decision options before a project exists; `POST /kickoff-meetings/:id/approve` turns that saved session into the normal project, charter, Leader marker, assignments, first pulse, and manager-dashboard proof.
- The project dashboard exposes a `Sample Fixture Path` for explicit demo/sample projects; it jumps directly to kickoff chat evidence, a Google Chat change-request simulation, a live meeting-change path through the Roundtable room, and the evidence timeline.
- The same path can prefill a live Leader assignment request; submitting it makes the confirmed Leader post the actual `@agent` assignment and makes the assignee acknowledge immediately.
- The project dashboard renders the `Kickoff Charter` with meeting result, Leader, Reviewer, next actions, and communication rules.
- The `Kickoff Charter` keeps runtime transcript ids for role clarification, self-nomination, Leader campaign, assignment, and acknowledgement messages, and exposes a `Kickoff chat proof` jump that lands back on those group-chat records.
- The dashboard also renders `Kickoff Meeting Flow`, showing role clarification questions, self-nominations, peer-hearing edges, Leader campaign count, Director confirmation, persisted Leader marker, confirmed team count, and a `Kickoff meeting proof` jump back to the exact group-chat evidence.
- `Kickoff Meeting Flow` includes `Conversation Evidence` rows for role questions, self-nominations, and Leader campaigns, including speaker, role, text, heard-by count, and a direct `Conversation proof` jump.
- Manager Flow Graph now turns kickoff self-nominations and Leader campaign turns into first-class `self-marketing` nodes, with transcript proof, event-ledger proof, heard-by participants, attachments, and edges into role analysis or Leader election. Role-clarification turns are also emitted as proofed kickoff conversation nodes.
- Frontend clients must treat `GET /projects/:id/manager-flow-graph` as the authoritative Flow Graph read model. React may render `manager-flow-graph/fallback` only for offline or explicit demo/sample projects; backend-online real projects must show a missing-model state instead of synthesizing flow nodes.
- Frontend clients that create Agent work should prefer lightweight write receipts during normal interaction: send `includeReadModels: false` to Agent evidence-search, submission, and artifact-draft routes, then refresh `GET /projects/:id/manager-dashboard`, `GET /projects/:id/manager-ready-package`, `GET /projects/:id/manager-flow-graph`, or `GET /projects/:id/agents/:agentId/dashboard` explicitly. The response includes `readModels.included: false` and the exact refresh routes. This prevents the UI from depending on mock refresh assumptions while preserving backend-backed proof.
- Frontend clients must cache Agent Dashboard read models by `projectId + agentId`. A persona can work in many projects; `GET /projects/:id/agents/:agentId/dashboard` must not be reused across projects when showing obligations, evidence searches, submissions, reviews, or autonomous run receipts.
- Frontend clients must also scope Agent Workbench, Agent Message, and Reviewer composer drafts by project before they can write through backend routes. The draft text is local UI state, but submitting it creates backend evidence, artifacts, messages, or reviews, so it must not follow the persona across projects.
- Manager Change Intake and Leader Assignment composer drafts follow the same project-scoped rule before they call chat or change-request routes. These are local form states, but submitting them writes C-side command proof into group chat, change ledger, timeline, and event ledger.
- Group Chat input, War Room meeting input, and the active chat channel are also project-scoped local UI state. They can write backend transcripts, meeting records, timeline logs, and event-ledger proof, so switching projects must not carry unsent text or a previous channel selection into another project's backend commands.
- Chat proof focus, Timeline proof focus, and selected Timeline detail are project-scoped Manager monitoring state. They do not create backend records, but they determine which proof ids the operator is inspecting, so clients must not carry them across projects.
- The C-side Agent Pulse controls now use the same receipt-first backend contract. `runBackendAgentPulse` sends `useAutonomousStrategy`, `submitWorkArtifact`, `reviewPendingSubmission`, and `respondToReviewObligation` to `POST /projects/:id/agents/:agentId/work-cycle`, then refreshes the relevant Manager/Agent read models. The pulse still sends reviewer hints for compatibility, but the backend strategy can decide whether this run should prioritize review response, teammate review, completed-work submission, management response, continued work, or monitoring. Scheduler start forwards `useAgentAutonomousStrategy`, `submitAgentWorkArtifacts`, and `respondToReviewObligations` into the immediate Agent sweep; scheduler-created work submissions default to task-inferred artifact types rather than always using `progress-brief`.
- The C-side Server Pulse, Hour Pulse, Day Report, and Manager Action Playbook `fixed-continuous-routines` run now call `/workers/autonomous/tick` instead of only running `/projects/:id/autonomous-cycle`. They force-filter the current project, run the Agent due-worker path with `forceAgentRun: true`, ask workers to use autonomous strategy, submit artifacts, let Reviewers use `agentReviewVerdict: "auto"` for first-pass change requests versus revision acceptance, and let submitters answer open review obligations. Hour/Day pass `projectCadence` so the project worker keeps hourly or daily semantics while sharing the same scheduler boundary. The Action Queue receipt stores `manager-action-scheduler-tick/v1`, project/Agent processed counts, and returned Agent worker evidence. Manager Action Queue and Scenario Walkthrough responses now also carry delegated output objects into React, where `Manager Action Output Nodes` expose result messages, task/scheduler nodes, Agent submissions/evidence/reviews, and chat/timeline proof exits. The browser and service Harnesses verify these controls write both project scheduler evidence and Agent worker evidence.
- Readiness treats change-sync inbox rows as consumable Agent work, not permanent C-side proof. `team-received-change-sync` can be satisfied by durable change ledger, timeline log, or event-ledger receipts after Agent workers consume their inboxes, so autonomous A-side progress does not erase manager governance state.
- Frontend clients that run Manager or worker actions should use the same receipt-first pattern for Manager Command Center run-next, Scenario Walkthrough run, Action Queue run, chat, meeting, change-request, autonomous-cycle, Agent message/work-cycle, Autonomous Run Control action/loop/session commands, Autopilot due-worker, and due-worker routes. These routes keep full read models by default for compatibility, but `includeReadModels: false` avoids recomputing large Manager snapshots inside the write response and returns refresh routes instead.
- Browser project snapshot seeding is a compatibility-only path. After a project has been successfully read from the backend through `GET /projects/:id` or a backend read model, React records the backend-seen project id and suppresses repeat sample snapshot `PUT /projects/:id` calls; subsequent Manager Action, Autonomous Run Control, Agent Queue, and Autopilot commands must advance through backend receipts and refresh routes instead of re-adopting stale browser state.
- Project initiation clients can use the same pattern for `POST /projects/initiate` and `POST /kickoff-meetings/:id/approve`: the response keeps the created project, meeting/charter/assignment proof, and first-pulse chat proof, while `readModels` points to project snapshot, transcripts, timeline/events, Manager Dashboard, Ready Package, Flow Graph, and Readiness Proof Map refresh routes.
- Provider eval and production operations, deployment, security, and provider control receipt POST routes also support the receipt-first contract. With `includeReadModels: false`, they return the receipt/log plus refresh routes for provider readiness/eval surfaces, Manager Dashboard, Manager Ready Package, Manager Flow Graph, production launch audit/gap/control/dossier, production evidence integrity, the specific receipt workflow, and its upstream readiness/preflight/provider/security read model instead of embedding the large Manager Ready Package in the write response. React exposes gated Manager Ready Package commands for these receipt lanes; UI-generated production-control evidence is always marked `local-rehearsal` so C-side operators can test the command path without upgrading public-production evidence integrity.
- Provider eval, launch approval, and private-pilot release, launch, health, and acceptance receipt routes follow the same C/A contract. React exposes gated Manager Ready Package buttons for `POST /provider-eval-runs`, `/launch-approvals`, `/private-pilot-release-candidates`, `/private-pilot-launch-runs`, `/private-pilot-launch-health-checks`, and `/private-pilot-acceptance-reports`; each button waits for the backend workflow readiness flag, posts a receipt with `includeReadModels: false`, stores the returned receipt, and refreshes Dashboard, Ready Package, Flow Graph, Timeline/Event, provider, and launch/go-live submodels from backend reads.
- Security boundary writes follow the same receipt-first rule. `PUT /membership-policy`, `POST /identity-sessions`, and `POST /identity-sessions/:sessionId/revoke` can return the policy/session receipt plus explicit security refresh routes, keeping C-side security controls responsive while preserving audit, event-ledger, and token-hash-only backend proof.
- Marketplace Agent contracts use the same backend-first rule. React posts `POST /projects/:id/agents/contract` with `includeReadModels: false`, then refreshes Manager Dashboard, Manager Flow Graph, Timeline/Event, and the contracted Agent Dashboard instead of adding a browser-only team member row.
- The React Agent Focus Workspace now consumes that contract through an Agent Workbench. Its evidence, typed submission, and draft+submit controls call the backend Agent routes with `includeReadModels: false`, record the returned action receipt, and then refresh Manager/Agent read models through GET routes. The refresh helper intentionally does not PUT the stale browser project snapshot before reading, so backend-created nodes stay authoritative. Revision-note and final-deliverable submissions can now link `respondsToReviewId`, `revisesSubmissionId`, and final `supersedesSubmissionIds` directly from the Workbench.
- For backend-online real projects, Agent Focus also treats `GET /projects/:id/agents/:agentId/dashboard` as the required personal read model. The UI labels the Agent Dashboard source and renders a backend-required missing state with a manual sync action when that route has not returned, while local Agent state cards remain only project snapshot context.
- The React Manager Dashboard also consumes the submission review contract from each Agent submission row. Reviewer verdicts, comments, and requested changes are POSTed to `/projects/:id/submissions/:submissionId/reviews` with lightweight receipts, then the UI refreshes Manager Dashboard, Ready Package, Flow Graph, Timeline/Event, and relevant Agent dashboards from backend routes. The `npm run ui:manager-backend` browser Harness now verifies this after real initiation by submitting Turing brainstorm work, running evidence search, generating a product-brief draft, recording Curie's changes-requested review, submitting Turing's linked revision-note and final-deliverable, accepting the final deliverable, and checking Manager/Flow/Proof/Agent read models.
- `GET /projects/:id/readiness-proof-map` exposes `roleNegotiationRoutes`, `selfMarketingRoutes`, and `projectMemoryReadinessRoutes`, so backend clients can verify role negotiation, Agent self-marketing, and memory readiness without reconstructing kickoff transcripts or Settings state manually.
- The dashboard also renders `Kickoff Execution Flow`, connecting charter next actions to Leader assignment rows, first autonomous pulse evidence, and 24/7 readiness. Managers can jump from this block to assignment chat proof or first-pulse timeline proof.
- The project initiation meeting step renders the runtime-generated kickoff transcript directly, including the Director brief, Agent role questions, self-nominations, and Leader campaign turns before the manager confirms the result.
- Each initiation transcript card shows which peers heard that turn, and the kickoff charter stores those hearing edges as durable evidence.
- `createKickoffCharter` also emits ledger events for each kickoff role question, self-nomination, and Leader campaign speech, so the unified event stream can replay the meeting instead of only storing the final charter.
- Runtime group-chat messages now carry durable receipt metadata: `heardBy`, `directTargetIds`, per-Agent `receipts`, and a `visibility` summary. Assignment, handoff, change discussion, autonomous work, and Director-authored messages all use this shape.
- Group chat rendering shows named `Heard by` and `Direct target` recipients beside receipt counts, so the manager can see which Agents heard a broadcast and which Agent was directly @mentioned.
- The dashboard renders `Group Chat Transcript Index`, which lists every project channel with message count, archived proof recovery count, latest speaker/message, receipt coverage, direct mentions, and an `Open transcript` proof jump. It also renders collaboration proof rows for backend evidence searches, Agent submissions, submission reviews, linked revisions, and final deliverables, each with direct chat/timeline proof exits back into the same transcript and ledger contracts. The live Group Chat stream treats backend `evidence-search`, `submission`, `submission-review`, and `evidence-source-review` messages as work-node cards rather than ordinary chat bubbles, preserving route text, proof counts, receipts, timeline proof actions, and a direct Flow Graph node action inside the conversation. Selected Manager Flow Graph node details now expose the matched Readiness Proof Map/API route and a Proof Map return action, so a Manager can trace a chat collaboration card into the graph and back to the backend proof route.
- The backend exposes the same transcript read model through `GET /projects/:id/transcripts` and `GET /projects/:id/transcripts/:channelId`, including current messages, recoverable proof ids, and archived proof messages when the current chat buffer no longer contains them.
- The React dashboard now consumes that backend transcript model when the backend is online. Project load/sync, chat or meeting commands, worker pulses, Agent write receipts, manual transcript sync, and channel changes refresh the transcript index/channel route, then merge server messages into the local buffer as cache instead of treating browser chat history as the only collaboration source.
- Timeline logs created from chat messages preserve `receiptCount` and `directTargetIds`, so proof recovery and readiness audits can still prove who saw a message after the visible chat buffer is trimmed.
- Timeline detail panels now surface source channel, receipt count, and direct-target Agent names for runtime events, so assignment and change evidence remains auditable from the big timeline view.
- Ordinary group-chat messages use `applyChatMessagesToAgentStates`: direct `@agent` receipts are delivered into that Agent's inbox and obligations, while Agent-authored replies are written into the author's private worklog. This keeps normal chat stateful even when it is not a Leader assignment, handoff, or feature change.
- The backend exposes Agent-authored group-chat publishing through `POST /projects/:id/agents/:agentId/message`. This makes Agent-to-Agent communication a first-class server route: the sender is fixed by the URL, target Agents are explicit, receipts are durable, target dashboards show the inbox proof, and the sender dashboard shows the worklog proof.
- The manager dashboard exposes the same backend route in each Team row as an `Agent Message` control. A manager can pick a target Agent, edit the message, send it through the backend, and immediately see the target inbox proof plus sender worklog proof without leaving the dashboard.
- The dashboard also renders `Agent Communication Flow`, which consolidates Agent-authored messages into sender, targets, receipt count, target inbox state, target obligation state, sender worklog state, and an exact `Agent chat proof` jump.
- Project language, local MVP privacy posture, local provider budget guardrails, Agent provider tool grants, Settings integration capability rows, and Settings workspace capability rows are backend project settings. `GET/PUT /projects/:id/project-settings` exposes `project-settings/v1`; writes persist the language used by Agent/read-model output, `project-privacy-policy/v1` retention/provider-log/model-training/export-approval policy, `project-provider-budget-policy/v1` daily-budget/hourly-request policy, `project-tool-grant-policy/v1` provider-test/model/search default grants, `project-workspace-policy/v1` interface-density/default-visibility/autosave-cadence policy, `project-integration-capabilities/v1` backend-backed integration rows, and `project-workspace-capabilities/v1` browser-local/backend-backed rows, then create audit, timeline, and event-ledger proof. React Settings uses this route for backend-online real projects instead of changing language, privacy, usage budget, external tool grants, workspace policy, or integration readiness only in browser state. Provider Readiness and Provider Controlled Run consume the project provider budget and tool grant policies as the effective provider control policy for that project. The Proxy/Webhook row points to `/projects/:id/adapter-gateway-preflight` for local/private adapter rehearsal readiness, the MCP Tools row points to `/projects/:id/provider-readiness` for provider/tool governance readiness, the Vector Store row points to `/projects/:id/evidence-index-readiness` for local evidence/artifact index readiness, the Budget Alerts row points to `/projects/:id/budget-alert-readiness` for local budget/request headroom readiness, and the Error Reporting row points to `/projects/:id/error-reporting-readiness` for local log/alert/runbook readiness. `GET /projects/:id/settings-integration-readiness` aggregates those rows into `settings-integration-readiness/v1` for the Settings UI, Manager Ready Package, and Readiness Proof Map. Those five integration rows are backend-backed but read-only in Settings, so the UI does not show missing-backend warnings for available readiness contracts and also does not expose fake editable controls. Workspace marks global interface language as browser-local; project language, interface density, default visibility, autosave cadence, runtime contract rules, long-term memory readiness, and meeting summaries as backend-backed. Runtime contract rules are read-only in Settings and point to `/projects/:id/runtime-contracts`, exposing `runtime-contract-freeze/v1` for Agent autonomy/submission/evidence/review/transcript/Flow/Proof contracts without claiming production approval. Long-term memory is read-only in Settings and points to `/projects/:id/memory-readiness`, exposing `project-memory-readiness/v1` without enabling managed memory writes. Meeting summaries derive from backend transcripts, timeline logs, and event-ledger proof; managed summary provenance, transcript retention, human review policy, managed vector memory, production proxy/webhook delivery, managed MCP registry, production alert routing, production observability, runtime contract approval, and managed memory remain production blockers until adapter/operations/persistence routes exist. These settings are not production compliance evidence until managed security, operations, provider, cost-alerting, task-scoped authorization, runtime identity, evidence-export controls, managed alert routing, centralized logs/traces, production adapter gateway, managed tool registry, managed vector storage, and managed workspace policy exist.
- Special manager commands now persist the Director's source message first. Leader assignment commands, Google Chat change requests, and War Room meeting change requests enter Agent inbox/worklog state and the unified event ledger before their runtime handlers create tasks, discussions, confirmations, and sync records.
- Projects maintain a unified append-only `eventLedger` alongside specialized records. Kickoff approval, Leader assignment, peer handoff, confirmed changes, autonomous scheduler runs, work pulses, management check-ins, and task completions all append sequence-numbered events.
- `appendProjectEvents` tracks `eventLedgerFirstSequence`, `eventLedgerLastSequence`, and `eventLedgerEventCount`, so sequence numbers keep increasing even when the retained browser window is trimmed to the latest events.
- `backfillProjectEventLedger` migrates older project shapes into that same stream by rebuilding events from kickoff transcripts, kickoff charter evidence, timeline logs, change ledger records, peer handoffs, and autonomous scheduler records during project hydration.
- `projectEventReplayProjection` derives replay-stage counts from the ledger: kickoff speeches, assignments, changes, peer handoffs, autonomous runs, management events, and completions.
- The project dashboard exposes this `Unified Event Ledger` with retained/total counts, sequence range, and replay-stage counts; the readiness audit checks event-ledger continuity and replay readiness so future backend persistence can map it directly to a project events table.
- The backend API exposes the same evidence as read resources: `/timeline` returns project logs, `/events` returns the append-only ledger plus replay summary, and `/tasks/:taskId/evidence` resolves the task's source chat messages, timeline logs, and ledger entries.
- The React dashboard now consumes the backend timeline/event read models when the backend is online. Project load/sync, Manager control actions, chat or meeting commands, worker pulses, Agent write receipts, and manual `Sync Timeline` refresh `GET /projects/:id/timeline` plus `GET /projects/:id/events`, then merge server logs/ledger into project state for the Manager Flow Graph timeline and Unified Event Ledger.
- The React dashboard also refreshes core Manager Ready Package proof submodels through standalone backend routes. `Sync Proof Models`, Agent write receipts, Manager control actions, and worker pulses can refresh Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Evidence Quality Audit, Evidence Source Review Workflow, and Evidence Custody Readiness, and the UI prefers those standalone models over embedded package snapshots.
- Those acceptance-chain panels now expose read-model provenance in the Manager UI. Backend-online real projects render backend-required missing models when Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Evidence Quality Audit, Evidence Source Review Workflow, or Evidence Custody Readiness is absent; summary-shaped Brainstorm fallback is limited to offline/demo mode.
- The manager dashboard exposes backend worker status beside the autonomous project loop, so the user can tell whether 24/7 work is currently browser-local, connected to the local backend, or offline. When the backend is online, the dashboard periodically pulls the active project snapshot and merges returned chat messages so server-run cycles remain visible in the manager view.
- The dashboard renders a `24/7 Operations Board` directly under the autonomous loop. It summarizes project next/last run, backend worker state, Agent run queue size, and each Agent's next run, latest work, worker trigger, open obligations, and management priority.
- Starting the backend scheduler from `Backend Worker Station` first persists the active project to the backend store, requests an immediate first worker tick, publishes a current-project autonomous pulse with `trigger: manager-ui-scheduler-start-pulse`, runs the per-Agent startup sweep with `trigger: http-autonomous-scheduler-startup-agents`, then silently syncs project state after backend proof exists and shows `Immediate Start`, running state, latest processed projects, latest processed Agents, and worker message count. The UI no longer sends a parallel browser autonomous-cycle after Start, so backend Agent ledger proof is not overwritten by a stale C-side project snapshot.
- Each Team row exposes a backend-backed `Agent Pulse` button when the backend station is online. It calls `POST /projects/:id/agents/:agentId/work-cycle`, so a manager can wake one Agent independently and see that Agent's latest inbox item, open obligation, worklog entry, next Agent run, timeline proof, chat proof, latest management priority, priority reasons, and `agentWorkerLedger` update without ticking the whole project.
- The backend also exposes `GET /projects/:id/agents/:agentId/dashboard`, so a manager client can pull one Agent's inbox, obligations, private worklog, owned task evidence, management mesh, worker ledger, and proof routes without reconstructing that view from multiple endpoints.
- Agent Team rows include direct proof jumps from latest inbox, open obligation, and worklog details back to the exact group-chat message or timeline evidence, so the manager can verify that a mention was received, accepted as work, and published as progress without manually searching chat history.
- The dashboard renders `Agent Management Mesh`, a consolidated view of the Director-confirmed Leader chain, peer-management relationships, managed Agents, latest management check-ins, and management timeline proof jumps.
- The backend scheduler tick also calls `POST /workers/agents/due`, so Team-row Agent work is not limited to manual clicks; each Agent can be woken by its own due timestamp while the manager watches Agent run/skip counters in `Backend Worker Station`. When multiple Agents are due, managed Agents with open work, inbox check-ins, peer-manager pressure, or review signals are processed before lower-priority due Agents.
- Backend-connected chat and meeting commands return runtime response details, including change discussions and owner confirmations, so the War Room animation and group-chat evidence continue to work when the server is the command source.
- Backend-connected initiation returns the same role negotiation, Leader election, kickoff charter, and Leader assignment evidence that the local runtime creates, so the project is backend-persisted from the first approval moment.
- The real initiation result screen now has an explicit `Director Decisions` gate before project creation: it shows the confirmed team roster, lets the manager select the final Leader from the campaign slate, shows the Leader marker that will be persisted, and summarizes the first execution plan before approval.
- Confirming a real initiation project immediately runs the first hourly autonomous work pulse, writes scheduler evidence with `trigger: initiation-approval`, moves the project to executing, and mirrors the first pulse back into group chat.
- The project dashboard renders a `Change Ledger` with change source, owner, reviewer, confirmation status, linked task, and owner plan sync.
- Each `Change Ledger` item renders the mid-project change as five visible stages: source request, team discussion, owner confirmation, owner plan update, and team sync count. It also names the synchronized Agents, with chat proof including discussion, confirmation, and sync messages.
- Each `Change Ledger` row can jump directly to the discussion/confirmation/sync chat proof and the related timeline log proof.
- Active task rows show the responsible owner, Leader assignment marker, source channel, work-pulse count, timeline publication marker, and task-level evidence ids for assignment/request messages, acknowledgement or owner-confirmation messages, owner sync messages, and timeline logs.
- Task rows include direct jumps back to the source chat channel and the evidence timeline when those proofs exist; the destination view automatically lands on and highlights the exact proof messages or timeline logs.
- The dashboard now also renders `Leader Assignment Flow`, a manager-readable proof chain for Leader-assigned work: `Group @Assignment`, `Assignee Inbox`, `Acknowledgement`, `Work Pulse`, and `Timeline Proof`. Each row derives the inbox/work evidence from the assigned Agent's private state and offers direct chat/timeline proof jumps.
- Chat proof jumps recover archived proof messages from `project.initiation` transcripts and `project.logs` only for offline, sample, imported legacy, or explicit development fallback projects. For backend-online real projects, the React dashboard first syncs `GET /projects/:id/transcripts` plus the target channel route through no-store backend requests before opening a chat proof; if the backend transcript does not contain the proof id, local recovery and cached browser-chat proof are suppressed, proof navigation fails closed, and the UI records a backend-required proof error instead of fabricating or trusting a local message.
- Kickoff Leader assignments are stored as `kickoff-leader-assignment` tasks from the main group channel, so the first tasks created after the initiation meeting have the same Chat proof navigation as live assignments and change requests.
- Team rows show each Agent's independent state, including manager/managed relationship, peer-managed dependency relationship, status, current plan, inbox count, and worklog count.
- `Peer Handoffs` shows accepted Agent-to-Agent dependency requests with requester, target, task, channel, request evidence, and acknowledgement evidence, with direct jumps to the peer chat proof and timeline proof.
- Confirmed feature changes update the responsible owner's Agent state immediately: the source channel enters inbox, the change task enters obligations and task ids, and `currentPlan` points at the accepted change before the next autonomous cycle.
- Confirmed feature changes also write `change-sync` receipts into every non-owner Agent's inbox/worklog, and `changeLedger` records the synchronized Agent count.
- The global dashboard exposes `Load Sample Fixture`, which opens the Manager Demo as explicit sample data using the same runtime functions: kickoff role negotiation, Leader election, Director confirmation, Leader `@agent` task assignment, War Room meeting change request, Google Chat change request, autonomous work cycles, task completion, and timeline evidence. The generated project is labeled `sample-fixture` so it is not treated as the real project creation path.
- Chat messages are project-scoped with `projectId`; channel views and autonomous message reading only use the active project's messages so manager-demo evidence does not bleed into other projects.
- Group chat mention parsing resolves targets against the active project team, so full names with spaces such as `@Alan Turing`, ids such as `@turing`, and `@all` route to the intended Agents.
- Project state, chat messages, and the autonomous ledger still have browser `localStorage` as an offline/cache fallback for the prototype. Backend-online project list/load, group-chat transcript reads, timeline/event reads, and core proof-submodel reads are now moving to the file-backed API: React can sync `GET /projects`, open `GET /projects/:id`, refresh `GET /projects/:id/transcripts`, channel transcript routes, `GET /projects/:id/timeline`, `GET /projects/:id/events`, and standalone brainstorm/artifact/review/evidence/custody routes. Browser snapshot writes are limited to explicit demo/sample or development-fallback projects through guarded `Save Project` or a local-only compatibility seed that first checks whether the backend already has the project. Full Ready Package proof-model sync also reads standalone pilot/launch/production-control/provider/operations/persistence/queue/security routes and prefers those models over embedded package snapshots. For backend-online real projects, chat, meeting, Manager change intake, Leader assignment, manual Hour/Day pulse failures, and direct browser snapshot save attempts now stop at the visible backend error instead of mutating browser-local runtime state; fallback is restricted to offline mode, explicit demo/sample projects, or the development-only `__HOF_ALLOW_LOCAL_RUNTIME_FALLBACK__` / `hall_of_fame_studio.dev_local_runtime_fallback.v1` / `VITE_HOFS_ALLOW_LOCAL_RUNTIME_FALLBACK=true` switches. A production backend should replace the remaining browser-first mutation paths with append-only project ledger and transcript tables using the same cycle shape.
- The project dashboard also renders the current Lead, Reviewer, kickoff frame, and recurring sync frame so the team governance model is visible instead of hidden inside runtime code.
- The project dashboard renders `Manager Scenario Readiness`, a scenario-specific audit score that checks the requested manager workflow end to end: kickoff approval, role clarification, peer hearing, Leader election, Leader assignments, Agent states, autonomous work, timeline progress, group chat evidence, peer handoffs, War Room meeting changes, Google Chat changes, and owner plan sync.
- `Manager Scenario Readiness` derives group-chat evidence from durable project records such as kickoff assignment ids, handoff records, change ledger discussion ids, and autonomous ledger entries, so long chat histories do not erase readiness proof.
- `Manager Proof Map` turns every readiness check into a direct evidence route. Kickoff checks jump to kickoff chat proof, assignment and change checks jump to source chat proof, autonomous/progress checks jump to timeline proof, and Agent state/management checks jump to their dashboard or timeline proof surfaces.
- The backend exposes the same proof map through `GET /projects/:id/readiness-proof-map`, so Manager Scenario Readiness is now auditable from service/API/HTTP clients, not only from the React dashboard.
- `GET /projects/:id/manager-use-case-audit` is also a standalone C-side governance route. Readiness Proof Map exposes `managerUseCaseAuditRoutes` and `managerUseCaseAuditSummary`, linking use-case coverage back to Manager Dashboard, Manager Action Queue, Manager Flow Graph, transcript/timeline/event proof, and a production-blocker boundary.
- The backend also exposes `GET /projects/:id/manager-dashboard`, an aggregate manager read model that packages readiness/proof-map status, transcript summary, operations board rows, Agent management mesh rows, kickoff/assignment/change flows, peer handoffs, and task evidence into a single response for future manager clients.

The autonomous ledger stores:

- cycle id
- cadence
- run timestamp
- trigger, due timestamp, scheduler reason, and next run timestamp
- lead id
- published event count
- per-Agent priority, read count, obligation count, and publish status
- per-Agent status derived from the persisted `agentStates`
- per-Agent fixed work routine id, label, checklist, and expected artifact
- communication diagnostics showing which Agent read, spoke, ignored, or accepted an obligation, with attention score and reason

The scheduler ledger stores one entry per manual or automatic cycle so a manager can see whether a run came from the page scheduler, the Director's manual pulse, or a seeded demo path.

## Collaboration Invariants

The system should preserve these rules:

- No ownerless task.
- No decision without a recorded reason.
- No blocker without an owner.
- No deadline change without Lead visibility.
- No risk acceptance without Reviewer visibility.
- No Director interruption unless the team cannot decide inside its authority.

The runtime exposes `evaluateCollaborationState` to check these rules against the current project. The dashboard renders the resulting collaboration health score and each failed check so broken coordination is visible before it becomes silent drift.

## Runtime Entry Points

- `createAgentNetwork`: creates Agents, governance, and relations.
- Confirmed team `isLeader` markers are treated as authoritative by network governance before inferred Lead selection.
- `createKickoffRoleNegotiation`: produces kickoff role-clarification and self-nomination turns before Leader election.
- `createKickoffCharter`: turns the approved kickoff, Leader election, and Leader assignments into a durable project charter.
- `createLeaderElection`: produces Leader campaign statements and a recommended Leader.
- `createLeaderAssignmentPackage`: turns open tasks into Leader-authored `@agent` assignment messages plus immediate assignee acknowledgements, receipt metadata, and timeline logs.
- `isLeaderAssignmentRequest` and `handleLeaderChatAssignment`: detect live group-chat assignment requests, let the confirmed Leader emit the actual `@agent` assignment, create the task, record the assignee acknowledgement, update Agent state, and publish both logs to the timeline.
- `isPeerHandoffRequest` and `handlePeerHandoff`: detect Agent-to-Agent dependency handoffs, create the dependency task, record requester and target messages, update peer-management state, write a handoff ledger entry, and publish both logs to the timeline.
- `startAgentSession`: starts a meeting/session with a protocol frame.
- `routeDirectorDirective`: routes explicit Director instructions.
- `runRoundtableExchange`: produces meeting-style Agent turns.
- `processWorkCommunication`: reads a work-stream message and decides who should speak.
- `planAutonomousWorkCycle`: plans hourly/daily autonomous work and reports.
- `evaluateAutonomousSchedule`: checks due status and next-run timing for enabled autonomous projects.
- `advanceAutonomousProjectCycle`: applies an autonomous cycle to project state.
- `createAutonomousCycleChatMessages`: converts autonomous work cycles into project-scoped group chat records.
- `publishAutonomousCycleChat`: publishes those records back into Agent state and the unified event ledger for manual pulses, scheduled pulses, initiation first-pulse, and manager demo seeding.
- `isFeatureChangeRequest`: detects English and Chinese feature-change requests.
- `handleFeatureChangeRequest`: turns group chat, Google Chat, or meeting change requests into discussion, confirmation, task, owner sync, change ledger, and timeline records.
- It also returns a fully updated project state with owner plan sync persisted into `agentStates`.
- `evaluateCollaborationState`: checks whether the team still satisfies collaboration invariants.
- `evaluateManagerScenarioReadiness`: checks whether the requested manager demo scenario is fully evidenced and manager-ready.
- `buildAgentChatReplies`: adapts work communication into UI chat messages.
- `agentProjectService.js`, `agentProjectStore.js`, `agentProjectFileStore.js`, `agentProjectApi.js`, and `agentProjectHttpServer.js`: wrap the runtime into backend-style project operations: create kickoff projects from a Director brief/team roster, hydrate/replace project state, submit chat, submit War Room meeting input, run autonomous worker cycles, scan due projects and management-prioritized due Agents for the backend scheduler, expose project timeline/event/task evidence, retain message history, snapshot/reload repository state, persist a Node JSON store, expose HTTP-shaped commands, run the optional autonomous scheduler loop in a real local HTTP server, and evaluate manager readiness.

## Verification

- `npm run agents:scenario` validates the manager scenario data path in Node: kickoff role clarification, self-nomination, visible initiation meeting transcript, structured kickoff charter, Leader election, confirmed `isLeader` governance, kickoff Leader `@agent` assignment, live group-chat Leader assignment, Agent-to-Agent message publishing, Agent-to-Agent peer handoff, assignee acknowledgement, War Room meeting change discussion, Google Chat change discussion, change ledger, responsible-owner sync, autonomous scheduler due checks, autonomous work progress, autonomous group chat records, per-Agent state persistence, task completion, autonomous work submission, Reviewer requested-changes review, Submitter linked revision response, Reviewer revision acceptance, scheduler-produced final-deliverable submission and acceptance, timeline logs, backend kickoff project creation, backend service dispatch for chat/meeting/worker operations, project timeline/event/task-evidence/readiness-proof-map/manager-dashboard/agent-dashboard HTTP reads, HTTP scheduler status/tick/start/stop controls, targeted scheduler project filtering, dashboard backend worker station controls and snapshot sync, repository snapshot/reload continuity, file-backed persistence/restart continuity, API command routing/restart continuity, real HTTP server request/restart continuity, due-worker processing/skipping, and 100% manager-readiness audit coverage.
- Use `npm run agents:product-team:smoke` for the lowest-write in-memory generic product-team C/A proof, including canonical persona registry access, persona/professional skill-blend self-marketing, and all required generic artifact types from discovery through final-deliverable, `npm run agents:real-user-zero-to-autonomy` for the low-write real `agents:server` API proof with Secret Vault/provider seal plus the same required generic artifact coverage and proof surfaces over HTTP, `npm run ui:real-user-zero-to-autonomy` for the browser proof that Settings, kickoff approval, C/A handoff, Manager UI submission readback, and Artifact Quality Audit show those same required generic artifacts, `npm run agents:product-team:core` for the fast file-backed/HTTP generic product-team loop, `npm run agents:product-team:research-sample` when specifically validating that the Research sample still proves the generic Product Team Delivery Trace rather than paper/thesis/manuscript protocol, `npm run agents:product-team:private-pilot` for the customer-visible private-pilot acceptance chain, and `npm run agents:product-team` for the full production-hardening rehearsal. Staged product-team gates write to isolated `.tmp/product-team-acceptance/<stage>-<pid>` workspaces, so focused gates can run in parallel without sharing the same file-backed store.
- `npm run launch:gates` validates `docs/LAUNCH_READINESS_GATES.md`, the cross-functional gate matrix for P0 local backend MVP, P1 customer private pilot, and P2 public production. It keeps the launch language tied to runnable scripts, generic product-team proof contracts, and explicit production blocker domains.
- `npm run agents:local-mvp-startup-readiness` validates `local-mvp-startup-readiness/v1` through the HTTP-shaped API. It checks the backend-required state without Vault, the provider-setup-required state with local Secret Vault enabled, redacted provider-vault binding proof after sealing a model key, and the route/command list the UI should use before allowing a first real product-team run.
- `npm run agents:settings-health-readiness` validates `settings-health-readiness/v1` through the HTTP-shaped API. It checks the no-Vault state, Vault-ready provider setup state, sealed model provider state, backend route coverage, production-blocked summary, and plaintext secret redaction without launching a browser or calling external providers.
- `npm run agents:settings-provider-readiness` validates `settings-provider-readiness/v1` through the HTTP-shaped API. It checks API-field editability, Vault-required and Vault-ready states, project-scoped route, Manager Ready Package / Readiness Proof Map inclusion, production-blocked summary, and plaintext secret redaction without launching a browser or calling external providers.
- `npm run agents:settings-runtime-readiness` validates `settings-runtime-readiness/v1` through the HTTP-shaped API. It checks the no-Vault state, Vault-ready runtime guidance, project-scoped route, Manager Ready Package / Readiness Proof Map inclusion, provider-vault redaction, persistence/worker-queue adapter rows, deployment preflight linkage, production-blocked summary, and plaintext secret redaction without launching a browser or calling external providers.
- `npm run agents:settings-integration-readiness` validates `settings-integration-readiness/v1` through the HTTP-shaped API. It checks provider budget, Agent tool grants, adapter preflight, provider readiness, evidence index, budget alert, and error reporting rows, confirms Manager Ready Package / Readiness Proof Map inclusion, checks the project settings capability route points to the aggregate, and keeps public production blocked without launching a browser or calling external providers.
- `npm run launch:infra` runs the P2 infrastructure rehearsal for managed persistence and managed queue/cron work. It chains the launch gate validator, shared adapter gateway contract check, reference private adapter gateway server check, Agent HTTP server dry-runs through an env-configured adapter gateway endpoint, and Postgres-compatible gateway storage boundary check while still keeping public production blocked until real managed infrastructure receipts exist.
- The product-team Harness also verifies `production-infrastructure-rehearsal/v1` through Manager Ready Package, the standalone backend route, Security Boundary route policy, managed persistence/queue domain rows, upstream route checks, Readiness Proof Map routes, Manager Flow Graph proof nodes, and explicit production-blocked status.
- `npm run agents:product-team` validates the generic product-team acceptance sample. It creates and approves a durable kickoff meeting, records an Agent evidence search through the deterministic provider, submits `discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, and `final-deliverable` artifacts through `POST /projects/:id/agents/:agentId/submissions`, generates and submits a backend-authored `progress-brief` through `POST /projects/:id/agents/:agentId/artifact-drafts`, requests changes on the draft brief, links the revision note and final deliverable back to the requested-changes review, and accepts the final deliverable through `POST /projects/:id/submissions/:submissionId/reviews`. The Agent evidence-search, submission, and artifact-draft writes now run with `includeReadModels: false`, proving that frontend clients can submit work through lightweight backend receipts and then refresh authoritative Manager/Agent read models from standalone routes instead of using mock rows. The gate verifies provider status/test without secret exposure, seals/opens fake provider keys through the local secret vault, injects fake secret fixtures into provider configuration/source refs/submission text, scans the persisted file store plus workspace artifact files, boots the real Node HTTP server against that store, runs a scheduler tick through `/workers/autonomous/tick`, and checks that project and Agent worker output is visible in Manager Dashboard, Manager Ready Package, Manager Flow Graph monitoring proof, MVP readiness, provider readiness, evidence quality audit, project evidence archive, production persistence snapshot, managed persistence migration plan, managed persistence migration dry-run, operations readiness, security boundary, security access audit, backend security audit stream, worker queue snapshot, worker queue adapter plan, and worker queue adapter dry-run. It also writes private-pilot Manager and security-admin launch approvals through `POST /projects/:id/launch-approvals`, verifies `launch-approval-workflow/v1` readback, Manager Flow Graph release-governance edges, Readiness Proof Map `launchApprovalRoutes`, production launch audit private-pilot `go`, production launch audit `private-pilot-handoff` next-shortest-path before package audit, `evidence-quality-audit/v1` decision gates/source safety/proof routes/checksum through in-process API and real HTTP, `project-evidence-archive/v1` manifest checksums / evidence quality audit / final deliverable / transcript / evidence / review / Flow Graph / raw-leak gates, governed project evidence export request and Manager/security-admin approvals through `POST /projects/:id/project-evidence-exports`, `project-evidence-export-workflow/v1` private-pilot handoff readiness, download-audit gated `project-evidence-export-package/v1` local package readiness, production launch audit handoff readiness after package audit, Manager Flow Graph export-governance nodes, Readiness Proof Map `projectEvidenceExportRoutes`, and `launch_approvals` plus `project_evidence_exports` persistence/migration rows. It also writes a project membership policy through `PUT /projects/:id/membership-policy`, verifies persisted policy/audit/event proof, checks enforced access decisions for security-admin reads including evidence quality audit, observer export denial, Agent own-dashboard access, cross-Agent dashboard/submission denial, Reviewer identity mismatch denial, signed access-header rejection/allow/tamper cases, signed request-id replay denial in-process and after file-backed API restart, audit fail-closed rejection, signed HTTP scheduler worker calls under replay protection, project membership allow/deny/revocation cases for Manager, Agent runtime, and Reviewer identities, persisted audit rows, backend audit-stream rows with sequence/checksum/hash-chain proof, append-only JSONL audit sink rows, `security-access` event-ledger proof, `provider-usage` event-ledger proof, `project_membership_policies`, `project_membership_grants`, `access_replay_records`, `security_access_audit`, `security_audit_stream`, `provider_usage_ledger`, and worker receipt/retry/dead-letter persistence rows, migration-plan seed order/RLS/verification gates, migration dry-run adapter contract/import/checksum/primary-key gates, queue adapter enqueue/idempotency/lease/dispatch/receipt/dead-letter dry-run gates, operations readiness gates/alerts/recovery runbook plus `operations-incident-drill/v1` receipts, worker recovery metrics, provider readiness gates/production controls, provider retry/circuit-breaker policy, local secret-vault status, provider policy/usage/cost rows, model draft quality/human-review readiness, HTTP header propagation, artifact files, generated artifact-draft submission/quality proof, evidence quality judgement/source signals, proofed role-clarification/self-marketing graph nodes, resolved review obligations, superseded draft submissions, group-chat submission/search/review messages, timeline logs, event-ledger events, task evidence links, Agent Dashboard owned submissions/evidence/reviews, Manager Dashboard submission/evidence/review/revision/evidence-quality summaries, Manager UI evidence quality audit snapshot, Manager Flow Graph submission/evidence/review/revision nodes and edges, Readiness Proof Map role-negotiation/self-marketing/submission/evidence/review/revision routes, local-pilot readiness without production overclaiming, persistence snapshot integrity, and worker queue idempotency/lease/retry rows.
- The same product-team Harness now requires every Agent-submitted artifact to carry `agent-artifact-storage-proof/v1` with content checksum, storage status, proof checksum, and workspace-file proof. It verifies those proofs through submission receipts, Manager Dashboard rows, Manager Flow Graph attachments, Readiness Proof Map submission routes, `artifact-quality-audit/v1`, Project Evidence Archive manifest/contents, and production persistence snapshot rows.
- The same product-team Harness now issues a manager identity session, uses `x-hofs-session-token` to read the Manager Dashboard without signed headers, verifies `securityAccessAudit` and the backend audit stream store the verified session id, revokes that token and confirms fail-closed behavior, keeps a second active security-admin session for Security Boundary proof, checks `identity_sessions` persistence/migration/adapter coverage, and scans the file store to ensure raw session tokens are never persisted.
- The product-team Harness also requires `launch-approval-workflow/v1` and `production-launch-audit/v1` in Manager Ready Package, the standalone signed API routes, and the real HTTP server routes. The completed signed API path asserts private-pilot approvals from Manager and security-admin, private-pilot audit `go`, production `no-go`, zero failed private-pilot gates, visible failed production gates, production blockers, audit-integrity checks, approval/audit routes, and next-shortest-path guidance toward production hardening; the HTTP route verifies the same contract surface without production overclaiming.
- The same Harness now verifies `artifact-quality-audit/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Readiness Proof Map `artifactQualityRoutes`, Project Evidence Archive manifest and contents, Pilot/Production launch evidence routes, security route policy, and Manager UI snapshot. The audit must pass local artifact quality gates for all generic submission types, preserve generated-draft quality and human-review metadata, and keep calibrated artifact rubrics plus managed output audit storage as production blockers.
- The same Harness now verifies `submission-review-workflow/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph closure node, Readiness Proof Map `submissionReviewWorkflowRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The workflow must close requested changes through linked revision responses, preserve accepted final-deliverable proof, and keep calibrated production reviewer governance plus immutable output audit storage as blockers.
- The same Harness now verifies `provider-controlled-run/v1`, `provider-eval-run-workflow/v1`, and `provider-eval-run/v1` through in-process API, real HTTP backend, lightweight provider/Manager refresh routes, Readiness Proof Map `providerControlledRunRoutes` / `providerEvalRunRoutes`, Manager Flow Graph provider-eval nodes, Security Boundary route policy, enforced security-admin reads, persistence rows, file-backed store persistence, and Manager UI snapshots. The controlled plan must keep run mode as `policy-dry-run-no-provider-call-issued`; the eval receipt must stay `shadow-replay-from-provider-usage-ledger`, omit large Manager snapshots in receipt-first mode, preserve model/search provider usage proof, expose budget/rate/headroom and critical replay coverage, and keep real-provider eval plus managed provider/eval audit storage as production blockers.
- The same Harness now verifies `production-provider-control-receipt-workflow/v1` and `production-provider-control-receipt/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Readiness Proof Map `productionProviderControlReceiptRoutes`, Manager Flow Graph receipt nodes, Security Boundary route policy, enforced runtime-platform reads/writes, persistence rows, file-backed store persistence, migration dry-run import coverage, and Manager UI snapshot. The receipt must preserve provider readiness, controlled-run, and eval-run checksums, verify all provider rollout control ids, mark only the provider domain production-ready, and keep the broader Production Launch Control Center `no-go`. `npm run agents:product-team:production-provider-controls` is the focused gate for this slice.
- The same Harness now verifies `private-pilot-release-candidate-workflow/v1` and `private-pilot-release-candidate/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Readiness Proof Map `privatePilotReleaseCandidateRoutes`, Manager Flow Graph release-candidate decision nodes, Security Boundary route policy, enforced security-admin reads, persistence rows, file-backed store persistence, and Manager UI snapshot. The freeze receipt must preserve launch audit, evidence export package, provider eval, deployment, operations, persistence adapter, and queue adapter checksums, mark private-pilot release readiness only, and keep production blocked.
- The same Harness now verifies `private-pilot-go-live-readiness/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph command node, Readiness Proof Map `privatePilotGoLiveRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The command view must expose stage rows, active phase, routed next action, private-pilot go-live readiness, customer acceptance readiness, and `readyForProduction: false`.
- The same Harness now verifies `production-launch-gap-register/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchGapRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The gap register must expose owner/domain/action rows, routed next action, upstream proof checksums, private-pilot acceptance state, and `readyForProduction: false`.
- The same Harness now verifies `production-launch-control-center/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph decision node, Readiness Proof Map `productionLaunchControlCenterRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The control center must expose gate rows, blocked rows, owner routing, routed next action, operations/deployment/security/provider control state, managed-production evidence integrity state, private-pilot acceptance state, and `productionDecision: no-go`.
- The same Harness now verifies `production-launch-evidence-dossier/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph dossier node, Readiness Proof Map `productionLaunchEvidenceDossierRoutes`, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The dossier must expose manifest entries, four production control domains, open managed-production evidence gaps, proof/timeline/event routes, and `productionDecision: no-go`.
- The same Harness now verifies `production-evidence-integrity-audit/v1` through in-process API, real HTTP backend, Manager Ready Package summary/checksum, Manager Flow Graph node/edge, Readiness Proof Map `productionEvidenceIntegrityRoutes`, Security Boundary route policy, enforced Manager reads, and Manager UI snapshot. It first records local/test operations/deployment/security/provider receipts and requires them to remain `local-rehearsal` with `readyForProduction: false`; it then records explicit `evidenceEnvironment: "managed-production"` receipt batches and requires every domain row to upgrade to managed-production evidence readiness. `npm run agents:product-team:production-evidence-integrity` is the focused gate for this public-production evidence boundary. This protects the public-production launch path from treating local rehearsal receipts as production certificates.
- The same Harness now verifies the production launch governance slice through `npm run agents:product-team:production-launch-governance`. After managed-production evidence integrity is ready, it records signed production approvals from Manager, security-admin, and operations-owner, requires `launch-approval-workflow/v1` to carry aggregate checksum/proof/timeline/event ids, verifies the production approval gate passes in Launch Audit, confirms Production Launch Control Center and Readiness Proof Map see approval readiness, and keeps the broader public-production decision `no-go`.
- The same Harness now verifies `private-pilot-launch-run-workflow/v1` and `private-pilot-launch-run/v1` through in-process API, Manager Ready Package summary/checksum, Readiness Proof Map `privatePilotLaunchRunRoutes`, Manager Flow Graph launch-run decision nodes, Security Boundary route policy, enforced security-admin reads, persistence rows, file-backed store persistence, and Manager UI snapshot. The activation receipt must preserve release-candidate, launch audit, evidence export package, provider eval, deployment, operations, persistence adapter, and queue adapter checksums, mark private-pilot launch readiness only, and keep production blocked.
- The same Harness now verifies `private-pilot-launch-health-check-workflow/v1` and `private-pilot-launch-health-check/v1` through in-process API, Manager Ready Package summary/checksum, Readiness Proof Map `privatePilotLaunchHealthCheckRoutes`, Manager Flow Graph monitoring nodes, Security Boundary route policy, enforced security-admin reads, persistence rows, file-backed store persistence, and Manager UI snapshot. The health receipt must preserve launch-run, operations, security, provider eval, evidence archive, persistence adapter, and queue adapter checksums, mark private-pilot monitoring readiness only, and keep production observability/on-call controls blocked.
- The same Harness now verifies `private-pilot-acceptance-report-workflow/v1` and `private-pilot-acceptance-report/v1` through in-process API, Manager Ready Package summary/checksum, Readiness Proof Map `privatePilotAcceptanceReportRoutes`, Manager Flow Graph acceptance decision nodes, Security Boundary route policy, enforced security-admin reads, persistence rows, file-backed store persistence, migration dry-run import coverage, and Manager UI snapshot. The acceptance report must preserve release, launch, health, evidence, Flow Graph, and Proof Map checksums, mark customer private-pilot acceptance readiness only, and keep public production managed controls blocked.
- The same Harness now verifies `production-operations-readiness/v1` through in-process API, Manager Ready Package summary/checksum, Readiness Proof Map `productionOperationsReadinessRoutes`, Manager Flow Graph production-operations node/edge, Security Boundary route policy, enforced security-admin reads, and Manager UI snapshot. The read model must pass local/private-pilot operations proof after the customer acceptance report, expose failed production control gates, route next work to `production-operations-hardening`, and keep `readyForProductionOperations: false`.
- The same Harness now verifies `production-operations-control-receipt-workflow/v1` and `production-operations-control-receipt/v1` through in-process API. It checks the receipt-needed workflow after customer acceptance, records verified evidence for all production operations controls, requires timeline/event proof, updates Manager Ready Package summary/checksum, flips `production-operations-readiness/v1` to operations-ready, keeps the broader production launch audit `no-go`, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionOperationsControlReceiptRoutes`, lists the Security Boundary route policy, imports `production_operations_control_receipts` through persistence snapshot/migration dry-run, persists rows in the file store, and renders the Manager UI snapshot.
- The same Harness now verifies `production-deployment-control-receipt-workflow/v1` and `production-deployment-control-receipt/v1` through in-process API and real HTTP backend. It checks the receipt-needed workflow, records verified evidence for all production deployment controls, requires timeline/event proof, updates Manager Ready Package summary/checksum, marks only the deployment domain production-ready, keeps the broader production launch audit/control center `no-go`, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionDeploymentControlReceiptRoutes`, lists the Security Boundary route policy, imports `production_deployment_control_receipts` through persistence snapshot/migration dry-run, persists rows in the file store, and renders the Manager UI snapshot.
- The same Harness now verifies `production-security-control-receipt-workflow/v1` and `production-security-control-receipt/v1` through in-process API. It checks the receipt-needed workflow, records verified evidence for all managed security controls, requires timeline/event proof, updates Security Boundary to production-security ready, keeps the broader production launch audit/control center `no-go`, adds Manager Flow Graph receipt nodes, exposes Readiness Proof Map `productionSecurityControlReceiptRoutes`, lists the Security Boundary route policy, imports `production_security_control_receipts` through persistence snapshot/migration dry-run, persists rows in the file store, and renders the Manager UI snapshot. `npm run agents:product-team:production-security-controls` is the focused gate for this slice.
- `npm run adapters:gateway` starts mock `http-json` adapter gateways, validates the shared external adapter gateway contract, then verifies the project API dry-run routes can execute through the gateway. It should pass before wiring a real private adapter gateway, but it does not prove production cutover by itself.
- `npm run adapters:gateway-server` starts the local private adapter gateway reference process, and `npm run adapters:gateway-server:validate` verifies bearer auth, shared gateway contract compatibility, project API dry-run integration, live project API gateway preflight, storage adapter status, persisted table records, persisted queue rows, persisted leases, and receipt summaries.
- `npm run adapters:gateway-postgres-store:validate` verifies the Postgres-compatible gateway storage boundary with a query-bound shim; it proves schema/upsert execution and snapshot/count readback parity coverage but not real managed database cutover readiness.
- `npm run ui:manager-demo` builds the production bundle, serves `dist/` through a local static server, clicks `Load Sample Fixture`, verifies the Manager Demo is visibly labeled as sample data, verifies the manager dashboard contains autonomous work, the backend worker station and state sync controls, unified event-ledger replay counts, kickoff charter, change ledger, peer handoffs, `100%` manager-readiness, then clicks and sends the manager demo action path for Google Chat change, War Room meeting change, Leader assignment, and peer handoff, verifies the resulting confirmations/syncs/assignment acknowledgement/accepted handoff, opens the evidence timeline, and writes `dist/manager-demo-ui-validation.png`.
- `npm run ui:manager-private-pilot` builds the production bundle, prepares the generic product-team acceptance store through the `private-pilot-launch-handoff` stage in an isolated `.tmp/product-team-acceptance/<run-id>` store/runtime checkpoint, opens that real backend project in the Manager UI, records Manager/security launch approvals, requests and approves the evidence export, records the download audit, clicks the four private-pilot Ready Package receipt buttons, and verifies the backend Manager Flow Graph plus Readiness Proof Map contain the acceptance-report proof produced by those C-side clicks.
- `npm run ui:manager-backend` builds the production bundle, starts a real Agent project backend on a dynamic local port, injects that URL into the manager dashboard, verifies `Check`, `Server Pulse`, and `Sync State` from the `Backend Worker Station`, confirms the backend store persisted the manager demo project plus backend-published chat messages, sends a real Agent-to-Agent message from a Team row, sends Google Chat and War Room change requests through the online UI, verifies backend change-ledger persistence, then walks the real initiation flow and verifies backend persistence of the new kickoff project, charter, event ledger, and first-pulse chat evidence. After initiation, it submits Turing brainstorm work, runs source-backed evidence search, submits a generated product-brief draft, records Curie's changes-requested review from Manager Dashboard, submits Turing's linked revision-note and final-deliverable, records Curie's accepted final review, and verifies Manager Dashboard / Manager Flow Graph / Readiness Proof Map / Agent Dashboard closure evidence.
- `npm run build` verifies the React/Vite app still compiles with the same runtime.
