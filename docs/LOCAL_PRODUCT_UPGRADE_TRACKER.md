# 本地产品 P0-P2 升级清单

更新时间：2026-07-17

当前清单统计：284 项，284 项已验证，0 项处理中。

最新完整 Dashboard 外层与 P2 收尾核验：P2-190 将原完整 Dashboard 外层、场景装饰、项目概览容器、内容加载边界和项目工具加载边界移动到 `src/project/ProjectDashboardAdvancedView.jsx`。Content Layout 继续位于 Tool Launcher 之前；Manager 可用条件、完整视图对象、全部回调、写入、证明操作、调度操作、可用性判断和禁用规则继续由 `App.jsx` 提供，未重写业务行为。按项目主管要求集中运行两项最终检查：第一项覆盖 5 项受影响契约、正式构建、发布保护和 700 KB 预算，全部通过；第二项真实完整 Dashboard 流程 166.7 秒通过，覆盖 Group Chat、Manager、Manager Flow Graph、Agent、Autopilot 和四种电脑宽度，Autopilot 回执为 7.963 秒。外层源文件为 1,540 字节，独立构建文件为 1,852 字节；主页面构建文件为 657,688 字节，低于 700,000 字节强制预算且构建不再出现大文件警告；`App.jsx` 为 22,364 个非空行。P2-01、P2-02、P2-33 与 P2-190 全部完成。

最新完整 Dashboard 内容外层总装配职责拆分核验：P2-189 将原 `project-paper` 容器、Top Panels 加载边界、Manager Body 插槽、Recent Commit Line 加载边界及原显示顺序移动到 `src/project/ProjectDashboardContentLayout.jsx`。Top Panels 与 Recent Commit Line 继续分别按需加载并保持原顺序；Manager 可用条件、完整 Manager Body、全部视图数据、按钮回调、证明操作、调度操作、写入、可用性判断和禁用规则继续由 `App.jsx` 提供，未移动、未重写。保护测试先复现新布局文件不存在，再完成最小移动；全量测试发现六项旧源码位置检查，只更新到新的实际加载位置，功能要求没有降低。首次真实项目流程发现新的按需布局缺少外层加载边界并进入界面恢复页；新增回归要求，在 `App.jsx` 中为该按需布局补充加载状态后，相同真实流程通过，本地发布检查也固定检查该边界。576 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通用户、普通项目和完整 Dashboard 三组操作分别用时 19.9 秒、34.0 秒和 115.8 秒，合计 169.7 秒；Autopilot 回执为 4.084 秒。共同装配源文件为 908 字节，独立构建文件为 1,153 字节；主页面构建文件为 658,758 字节，比 P2-188 减少 100 字节，`App.jsx` 为 22,374 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 主体总装配职责拆分核验：P2-188 将 Manager Core Panels、Work Loop Panels、Backend Station Region 与 Manager Collaboration Body 的原显示顺序及 Backend Station 加载边界移动到 `src/project/ProjectDashboardManagerBody.jsx`。四个原组件继续分别按需加载并保持原顺序；Manager 可用条件继续由 `App.jsx` 提供。全部数据对象、按钮回调、证明操作、调度操作、写入、可用性判断和禁用规则继续由 `App.jsx` 提供，未移动、未重写。保护测试先复现新 Manager Body 文件不存在，再完成最小移动；全量测试发现 24 项旧源码位置检查，只更新到新的实际加载位置，功能要求没有降低。575 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.364 秒、33.519 秒和 165.054 秒，合计 218.937 秒，Autopilot 回执为 14.528 秒。共同装配源文件为 1,006 字节，独立构建文件为 1,355 字节；主页面构建文件为 658,858 字节，比 P2-187 减少 590 字节，`App.jsx` 为 22,378 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 后台工作站外层区域总装配职责拆分核验：P2-187 将原 `backend-worker-station` 外层区域、P2-186 Station Content 与 Backend Scheduler Controls 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendStationRegion.jsx`。新区域继续先加载 Station Content、再加载 Scheduler Controls，保留原标识和样式；Manager Collaboration Body 继续位于该区域之后。工作站全部数据、调度图标、回调、禁用规则和写入操作继续由 `App.jsx` 提供，未移动、未重写。保护测试先复现新区域文件不存在，再完成最小移动；全量测试和两套发布检查发现五项旧源码位置或 JSX 写法检查，只更新到新的实际加载位置与对象参数写法，功能要求没有降低。574 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 21.021 秒、35.905 秒和 169.321 秒，合计 226.247 秒，Autopilot 回执为 14.003 秒。共同装配源文件为 934 字节，独立构建文件为 1,215 字节；主页面构建文件为 659,448 字节，比 P2-186 减少 410 字节，`App.jsx` 为 22,390 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 后台工作站内容总装配职责拆分核验：P2-186 将已独立的 Worker Station Panels、Manager Ready Package Snapshot 与 Backend Read Model Panels 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendStationContent.jsx`。三个已有共同组件继续分别按需加载并保持 Worker Station、Ready Package、Backend Read Model 的原顺序；`backend-worker-station` 外层区域和 Backend Scheduler Controls 继续由 `App.jsx` 提供，调度控制仍位于总装配之后。地址保存、部署设置、基础设施演练、Ready Package 操作、意图运行、提交复核、自主运行、Agent 行动、证明操作、同步、可用性判断和禁用规则均未移动、未重写。保护测试先复现新共同文件不存在，再完成最小移动；五项旧保护检查只更新组件加载位置与顺序检查。573 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.311 秒、32.950 秒和 145.286 秒，合计 198.547 秒，Autopilot 回执为 14.513 秒。共同装配源文件为 962 字节，独立构建文件为 1,284 字节；主页面构建文件为 659,858 字节，比 P2-185 减少 446 字节，`App.jsx` 为 22,395 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 后台读取总装配职责拆分核验：P2-185 将已独立的 Manager Backend Snapshot Panels 与 Backend Activity Panels 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendReadModelPanels.jsx`。两个已有共同组件继续分别按需加载并保持 Snapshot 在前、Activity 在后；Backend Scheduler Controls 继续位于该总装配之后。意图运行、提交复核、自主运行、Agent 行动、聊天与 Timeline 证明、按钮回调、可用性判断和禁用规则继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；四项旧保护测试只更新组件加载位置与顺序检查。572 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 19.843 秒、35.037 秒和 134.199 秒，合计 189.079 秒，Autopilot 回执为 3.820 秒。共同装配源文件为 694 字节，独立构建文件为 1,015 字节；主页面构建文件为 660,304 字节，比 P2-184 减少 210 字节，`App.jsx` 为 22,402 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager Ready Package 外层职责拆分核验：P2-184 将 Manager Ready Package 的原显示条件、快照容器、标题、Core Panels 与 Operational Panels 顺序及加载状态移动到 `src/project/ProjectDashboardManagerReadyPackageSnapshot.jsx`。两个已有共同组件继续分别按需加载并保持原顺序；所有 Ready Package 模型、路由、来源标记、审批、回执、证明操作、可用性判断和禁用规则继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；两项旧保护测试只更新组件加载位置。第一次全量测试发现 P2-183 的顺序保护仍在 `App.jsx` 中寻找已经移动的新快照内部标记，确认实际组件顺序没有变化后，只将检查位置更新到新文件；重新执行 571 项测试全部通过。产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.038 秒、34.718 秒和 139.632 秒，合计 194.388 秒，Autopilot 回执为 8.665 秒。共同装配源文件为 970 字节，独立构建文件为 1,259 字节；主页面构建文件为 660,514 字节，比 P2-183 减少 530 字节，`App.jsx` 为 22,404 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager Worker Station 区域职责拆分核验：P2-183 将 Backend Worker Station Status、可选 Production Infrastructure Rehearsal 和两个证明缺失提示的原显示顺序、条件、加载状态与参数连接移动到 `src/project/ProjectDashboardManagerWorkerStationPanels.jsx`。两个原组件继续分别按需加载并保持原顺序；Backend Worker Station 外层区域和其后的 Manager Ready Package 保持在 `App.jsx` 原位置。后端地址编辑与保存、打开部署设置、基础设施演练运行、证明条件、可用性判断和禁用规则继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；三项旧保护测试只更新组件加载位置与对象参数写法，原功能要求没有降低。570 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.398 秒、33.514 秒和 106.130 秒，合计 160.042 秒，Autopilot 回执为 4.023 秒。共同装配源文件为 1,730 字节，独立构建文件为 1,866 字节；主页面构建文件为 661,044 字节，比 P2-182 减少 970 字节，`App.jsx` 为 22,413 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 后台活动区域职责拆分核验：P2-182 将 Manager Read Model Summary Panels、Autonomous Run Control、Agent Autonomous Action Queue、Latest Backend Work 四个现有连续区域，以及其后的后台错误提示的共同显示顺序、两个条件、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerBackendActivityPanels.jsx`。四个原组件继续分别按需加载并保持原显示顺序；自主运行的启动、暂停、取消、循环、Scheduler Tick、直接 Tick 和操作运行，Agent 队列运行，聊天与 Timeline 证明跳转、可用性判断和禁用规则继续由 `App.jsx` 提供，没有重写；Backend Scheduler Controls 保持原父级位置。职责保护测试先复现新共同文件不存在，再完成最小移动；六项旧保护测试、两项发布检查和前端边界检查只更新加载位置与对象参数写法，原功能要求没有降低。569 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.3 秒、33.6 秒和 165.5 秒，合计 219.4 秒，Autopilot 回执为 11.750 秒。共同装配源文件为 1,890 字节，独立构建文件为 2,167 字节；主页面构建文件为 662,014 字节，比 P2-181 减少 1,118 字节，`App.jsx` 为 22,421 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 后台快照区域职责拆分核验：P2-181 将 Manager Snapshot Execution Panels、Manager Compatibility Proof Panels 和 Manager Submission Route Panels 三个现有连续区域的共同显示条件、原容器、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerBackendSnapshotPanels.jsx`。三个原组件继续分别按需加载并保持原显示顺序；意图运行、聊天证明、Timeline 证明、提交复核写入、草稿更新、证明跳转、可用性判断和禁用规则继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；九项旧保护测试、两项发布检查和前端边界检查只更新加载位置与对象参数写法，原功能要求没有降低。568 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸。三组真实操作分别用时 20.2 秒、33.6 秒和 166.0 秒，合计 219.8 秒，Autopilot 回执为 19.480 秒。共同装配源文件为 1,141 字节，独立构建文件为 1,427 字节；主页面构建文件为 663,132 字节，比 P2-180 减少 534 字节，`App.jsx` 为 22,431 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager Ready Package 运行区域职责拆分核验：P2-180 将 Project Evidence Export Workflow、Launch Readiness Panels、Pilot Operations Panels、Local Readiness Panels 和 Provider Security Panels 五个现有连续区域的共同布局、证据导出可选显示条件、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerReadyPackageOperationalPanels.jsx`。五个原组件继续分别按需加载并保持原显示顺序；证据导出四项操作、发布审批、试运行和生产控制回执、MVP 操作、Provider Eval 回执、证明模型同步、可用性判断与禁用规则继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；全量测试发现十七项旧组件测试仍要求对象参数使用原 JSX 写法，前端边界与本地 MVP 发布检查也保留同类旧定位，只更新源码定位文本，原组件内容、来源、路由、写入、证明和禁用要求没有降低。567 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作，三组真实操作合计 192.3 秒，Autopilot 回执为 8.842 秒。共同装配源文件为 1,742 字节，独立构建文件为 1,961 字节；主页面构建文件为 663,666 字节，比 P2-179 减少 912 字节，`App.jsx` 为 22,435 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager Ready Package 核心区域职责拆分核验：P2-179 将 Launch Operations Overview、Manager Ready Package Summary、Coordination Panels、可选 Collaboration Intent Queue Snapshot、Runtime Panels 和 Evidence Panels 六个现有连续区域的共同布局、可选显示条件、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerReadyPackageCorePanels.jsx`。六个原组件继续分别按需加载并保持原显示顺序；Launch Operations 运行、intent 运行、聊天证明、Timeline 证明、节点流证明和证明模型同步函数继续由 `App.jsx` 提供，没有重写。暂停时的中间代码曾因参数对象只转换一部分而无法构建；确认根因后完成同一最小移动，新职责保护测试从失败变为通过。全量测试首次发现八项原内部组件测试仍要求三个共同文件直接由 `App.jsx` 加载，只将加载位置检查更新到新共同文件，所有原组件内容、来源、路由、证明和操作要求继续保留。566 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 146.1 秒，Autopilot 回执为 4.056 秒。共同装配源文件为 2,029 字节，独立构建文件为 2,285 字节；主页面构建文件为 664,578 字节，比 P2-178 减少 1,253 字节，`App.jsx` 为 22,445 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 管理协作主体区域职责拆分核验：P2-178 将 Unified Event Ledger、Kickoff Collaboration Panels、Collaboration Operations Panels、Manager Proof Map 和 Coordination Team Panels 五个现有外层区域的共同布局、统一事件记录显示条件、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerCollaborationBody.jsx`。五个原组件继续分别按需加载并保持原显示顺序；P2-176 的十二个证明路由继续作为 Manager Proof Map 内部内容由 `App.jsx` 构造，事件同步、治理同步、Manager 同步、群聊记录同步、Cockpit 同步、证明跳转、Agent 工作台、Agent 消息、Agent Pulse 和 Agent Dashboard 同步函数也继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；十项旧保护测试从只检查 `App.jsx` 改为同时检查新外层文件、原共同文件和原组件，功能要求没有降低。第一次完整测试发现五项立项与群聊记录内部组件测试仍要求其共同文件直接由 `App.jsx` 加载，只更新加载位置检查后重新通过。565 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 233.3 秒，Autopilot 回执为 19.724 秒。共同装配源文件为 1,707 字节，独立构建文件为 2,067 字节；主页面构建文件为 665,831 字节，比 P2-177 减少 1,015 字节，`App.jsx` 为 22,456 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 协作与团队区域职责拆分核验：P2-177 将 Collaboration Health、Sample Fixture Path、Leader Assignment Flow 和 Team Workspace Panels 四个现有区域的共同布局、两个条件显示、加载状态和参数连接移动到 `src/project/ProjectDashboardCoordinationTeamPanels.jsx`。四个原组件继续分别按需加载并保持原显示顺序；协作诊断同步、聊天证明、Timeline 证明、Manager Dashboard 同步、Agent 工作台、Agent 消息、Agent Pulse 和 Agent Dashboard 同步函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；六项旧保护测试从只检查 `App.jsx` 改为同时检查共同文件和原组件，功能要求没有降低。完整测试首次发现两项旧测试仍要求 Team Workspace Panels 直接由 `App.jsx` 加载，更新检查位置后通过；本地 MVP 发布清单随后发现负责人分配条件和示例步骤条件的旧文件位置检查，只将检查范围更新到共同文件中的同一条件。564 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 182.6 秒，Autopilot 回执为 5.729 秒。共同装配源文件为 2,043 字节，独立构建文件为 2,295 字节；主页面构建文件为 666,846 字节，比 P2-176 减少 1,076 字节，`App.jsx` 为 22,471 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager Proof Map 证明路由区域职责拆分核验：P2-176 将 Transcript Proof Coverage、Transcript Channel Routes、Channel Pin Routes、Message Pin Routes、Reply Routes、Mention Routes、Attachment Routes、Member Presence Routes、Agent Message Routes、Agent Contract Routes、Collaboration Intent Queue 和 Submission Review Workflow 十二个现有证明子区域的共同布局、十二个条件显示、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerProofRoutePanels.jsx`。十二个原组件继续分别按需加载并保持原显示顺序；证明同步、聊天证明、Timeline 证明、Agent 详情打开和 Agent Dashboard 同步函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；八项旧保护测试和一项兼容保护测试从只检查 `App.jsx` 改为同时检查共同文件和原组件，功能要求没有降低。第一次完整 Dashboard 真实检查发现可选 Agent Contract 摘要为空时，主程序提前创建参数并读取 `agentIds`，进入本地界面恢复页；新增空数据回归测试后，将 Agent Contract、Collaboration Intent Queue 和 Submission Review Workflow 三组参数恢复为对应后台数据存在时才创建，修复后完整 Dashboard 重新通过。563 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，修复后三组真实操作合计 197.0 秒，Autopilot 回执为 13.384 秒。共同装配源文件为 6,112 字节，独立构建文件为 6,141 字节；主页面构建文件为 667,922 字节，比 P2-175 减少 4,103 字节，`App.jsx` 为 22,486 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 协作与管理区域职责拆分核验：P2-175 将 Change Flow、Agent Communication Flow、Agent Management Mesh 和 Manager Scenario Readiness 四个现有区域的共同布局、两个条件显示、加载状态和参数连接移动到 `src/project/ProjectDashboardCollaborationOperationsPanels.jsx`，并只在用户进入完整 Dashboard 且 Manager 区域可用时加载。四个原组件继续分别按需加载并保持原显示顺序；Cockpit 同步、Readiness Proof Map 同步、Management Sync、聊天证明和 Timeline 证明函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；四项旧保护测试从只检查 `App.jsx` 改为同时检查共同文件和四个原组件，功能要求没有降低。本地发布清单首次发现一项检查仍要求旧条件文本，确认功能数据和后端优先规则仍存在后，只将检查更新为共同文件中的同一条件。562 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 198.4 秒，Autopilot 回执为 15.403 秒。共同装配源文件为 1,970 字节，独立构建文件为 2,180 字节；主页面构建文件为 672,025 字节，比 P2-174 减少 1,160 字节，`App.jsx` 为 22,555 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 立项与群聊记录区域职责拆分核验：P2-174 将 Governance & Speech Protocol、Kickoff Charter、Kickoff Meeting Flow、Kickoff Execution Flow 和 Group Chat Transcript Index 五个现有区域的共同布局、三个条件显示、加载状态和参数连接移动到 `src/project/ProjectDashboardKickoffCollaborationPanels.jsx`，并只在用户进入完整 Dashboard 且 Manager 区域可用时加载。五个原组件继续分别按需加载并保持原显示顺序；治理同步、Manager 同步、群聊记录同步、聊天证明、Timeline 证明、记录打开和频道切换函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；五项旧保护测试从只检查 `App.jsx` 改为同时检查共同文件和五个原组件，功能要求没有降低。561 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 211.2 秒，Autopilot 回执为 13.809 秒。共同装配源文件为 2,316 字节，独立构建文件为 2,528 字节；主页面构建文件为 673,185 字节，比 P2-173 减少 1,514 字节，`App.jsx` 为 22,568 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 工作循环区域职责拆分核验：P2-173 将 Autonomous Work Loop、24/7 Operations Board、Continuous Work Loop 和 Fixed Work Routines 四个现有区域的共同布局、加载状态和参数连接移动到 `src/project/ProjectDashboardWorkLoopPanels.jsx`，并只在用户进入完整 Dashboard 且 Manager 区域可用时加载。四个原组件继续分别按需加载并保持原显示顺序；自动 Pulse、Agent Pulse、Manager Cockpit 同步、聊天证明和 Timeline 证明函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；四项旧保护测试从只检查 `App.jsx` 改为同时检查共同文件和四个原组件，功能要求没有降低。560 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 222.1 秒，Autopilot 回执为 17.368 秒。共同装配源文件为 1,641 字节，独立构建文件为 1,954 字节；主页面构建文件为 674,699 字节，比 P2-172 减少 1,242 字节，`App.jsx` 为 22,590 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard Manager 核心区域职责拆分核验：P2-172 将 Manager Command Centers、Scenario Walkthrough、Action Playbook、Action Run Ledger、Scenario Trail、Sync Protocol Audit、Manager Use Case Audit 和 Manager Composers 八个现有区域的共同布局、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerCorePanels.jsx`，并只在用户进入完整 Dashboard 且 Manager 区域可用时加载。八个原组件继续分别按需加载并保持原显示顺序；运行下一项、场景步骤运行、操作清单运行、Manager 同步、场景记录同步、协议检查同步、用例检查同步、负责人分配和变更提交函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；全量测试首次发现两项兼容检查仍只在 `App.jsx` 查找已移动的原组件，检查范围更新为共同文件和原组件后通过，功能要求没有降低。559 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 223.8 秒，Autopilot 回执为 17.819 秒。共同装配源文件为 3,233 字节，独立构建文件为 3,583 字节；主页面构建文件为 675,941 字节，比 P2-171 减少 2,775 字节，`App.jsx` 为 22,605 个非空行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Dashboard 顶部区域职责拆分核验：P2-171 将原项目标题、项目摘要、Agent 工作状态三个现有区域的共同布局、加载状态和参数连接移动到 `src/project/ProjectDashboardTopPanels.jsx`，并只在用户进入完整 Dashboard 时加载。三个原组件继续分别按需加载，原左侧区域继续包含其后的全部 Manager 内容；会议、Group Chat、Timeline、节点流图、返回简洁页面、Manager 同步、Agent Pulse 和后台读取函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现新共同文件不存在，再完成最小移动；三个旧保护测试从只检查 `App.jsx` 改为同时检查共同文件和三个原组件，功能要求没有降低。558 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 247.9 秒，Autopilot 回执为 19.949 秒。共同装配源文件为 1,295 字节，独立构建文件为 1,569 字节；主页面构建文件为 678,716 字节，比 P2-170 减少 715 字节，`App.jsx` 为 22,640 行。P2-01、P2-02 与 P2-33 继续处理。

最新完整 Timeline 页面职责拆分核验：P2-170 将节点分类、后端图选择、离线备用图构建、缩放筛选、节点布局、证明关联、提交地址、关联人员和关系图等只读计算与显示参数连接移动到 `src/project/ProjectTimelineRouteView.jsx`，并只在用户进入完整 Timeline 与节点流图时加载。原 `AdvancedProjectTimeline.jsx` 页面、节点确认、后端节点流同步、聊天证明、Timeline 证明、提交记录定位、intent 操作和所有写入函数继续由 `App.jsx` 提供，没有重写。职责保护测试先复现旧检查只读取 `App.jsx` 的失败，再把检查范围精确扩展到新页面文件，功能要求没有降低。557 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、Timeline 和刷新恢复；普通用户七种窗口尺寸和四种显示缩放通过；完整 Dashboard 完成 Group Chat、Manager、节点流图、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，三组真实操作合计 206.2 秒，Autopilot 回执为 4.923 秒。完整 Timeline 源文件为 37,560 字节，独立构建文件为 50,512 字节；主页面构建文件为 679,431 字节，比 P2-169 减少 21,025 字节，`App.jsx` 为 22,649 行。P2-01、P2-02 与 P2-33 继续处理。

最新完整群聊页面职责拆分核验：P2-169 将完整 Group Chat 的证明卡片、搜索结果、成员状态、频道分类、置顶、回复、提及、附件索引、回执摘要、消息合并判断和显示参数连接移动到 `src/project/ProjectChatRouteView.jsx`，并只在用户进入完整 Group Chat 时加载。发送、频道创建与切换、搜索、成员同步、附件、回复、提及、置顶、证明定位、输入状态和后端写入函数继续由 `App.jsx` 提供，原 `AdvancedProjectChat.jsx` 页面与操作没有重写。首次完整 Dashboard 检查发现新页面没有接收 `transcriptSearchResults`，进入完整 Group Chat 时触发恢复页；回归测试先复现失败，补齐参数后重新构建和完整检查通过。旧发布清单仍查找已经移动的普通群聊、设置和模型输入代码，检查范围已改为对应独立文件，没有放宽功能要求。556 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程完成立项、会议、群聊、时间线和刷新恢复；普通工作区七种窗口尺寸和四种显示缩放通过；完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，162.5 秒通过，Autopilot 回执为 14.652 秒。完整 Group Chat 独立构建文件为 43,276 字节；主页面构建文件为 700,456 字节，比 P2-168 减少 8,809 字节，`App.jsx` 为 23,344 行。P2-01、P2-02 与 P2-33 继续处理。

最新会议页面职责拆分核验：P2-168 将普通会议的显示参数连接和只读状态计算移动到 `src/meeting/ProjectSimpleMeetingRouteView.jsx`，将完整会议的显示参数连接、发言人、队列、时间和座位位置计算移动到 `src/meeting/AdvancedMeetingRoomRouteView.jsx`。会议发送、语音识别、退出、完成、计时、后端写入判断和原 `renderProjectMeeting` 入口继续由 `App.jsx` 提供。首次构建发现只移动显示参数会让主页面增加 81 字节且 `App.jsx` 增加 5 行，因此没有接受该结果；继续移动不写数据的会议派生计算后，主页面与主文件实际下降。保护测试先失败后通过。555 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。真实普通项目流程完成会议发送、返回项目、群聊、时间线和刷新恢复；普通工作区七种窗口尺寸和四种显示缩放通过；完整 Dashboard 继续完成完整会议、Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，146 秒通过，Autopilot 回执为 16.947 秒。普通会议和完整会议独立构建文件分别为 4,200 与 18,930 字节；主页面构建文件为 709,265 字节，比 P2-167 减少 975 字节，`App.jsx` 为 23,583 行。P2-01、P2-02 与 P2-33 继续处理。

最新人才页面职责拆分核验：P2-167 将人才市场的分类、搜索、签约状态和部署信息计算原样移动到 `src/scenes/AgentMarketRouteView.jsx`，将人才详情的档案、技能、证据信息和签约状态计算原样移动到 `src/scenes/AgentDossierRouteView.jsx`。两个页面只在用户进入对应页面时加载；打开档案、返回立项、继续立项、关闭档案和开始签约的函数继续由 `App.jsx` 提供。保护测试先失败后通过；构建预算检查首次仍查找旧场景文件名称，更新为检查新的按需页面文件后通过，没有放宽主页面预算。554 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。真实用户检查实际打开人才市场、人才档案和签约项目选择，并通过七种窗口尺寸和四种显示缩放；真实普通项目流程通过；完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，149.5 秒通过，Autopilot 回执为 4.859 秒。人才市场和人才详情独立构建文件分别为 9,234 和 13,198 字节；主页面构建文件为 710,240 字节，比 P2-166 减少 1,103 字节，`App.jsx` 为 23,624 行。P2-01、P2-02 与 P2-33 继续处理。

最新立项页面职责拆分核验：P2-166 将立项流程的现有页面外壳、启动状态提示、六步导航、五个步骤组件装配、会议入口、生成来源和立项摘要原样移动到 `src/onboarding/ProjectInitiationFlowView.jsx`，并只在用户进入立项时加载。文件夹选择、工作区创建、会议启动与提交、团队确认和项目批准函数继续由 `App.jsx` 提供；五个原步骤组件继续分别按需加载。保护测试先失败后通过；全量测试首次发现旧会议本地化检查仍只在 `App.jsx` 中查找已移动的生成来源文字，检查范围修正后 553 项自动测试全部通过。产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。普通工作区七种窗口尺寸与四种显示缩放通过；真实普通项目流程完成保存位置、立项会议、项目创建、群聊、时间线和刷新恢复；完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，92.1 秒通过，Autopilot 回执为 5.419 秒。立项页面独立构建文件为 10,918 字节；主页面构建文件为 711,343 字节，比 P2-165 减少 7,270 字节，`App.jsx` 为 23,663 行。P2-01、P2-02 与 P2-33 继续处理。

最新工作区职责拆分核验：P2-165 将普通项目列表与高级工作区的现有显示、启动就绪判断、项目来源标识和工作区统计原样移动到 `src/workspace/WorkspaceView.jsx`，并只在用户进入工作区时加载。创建项目、打开项目、打开设置、同步项目目录、同步运行状态和启动经理示例的函数继续由 `App.jsx` 提供；普通项目列表与 `AdvancedWorkspaceView` 的操作和显示顺序保持不变。保护测试先失败后通过。552 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单和代码差异检查通过。普通工作区七种窗口尺寸与四种显示缩放通过；真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过；完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，145 秒通过，Autopilot 回执为 13.847 秒。工作区独立构建文件为 7,853 字节；主页面构建文件为 718,613 字节，比 P2-164 减少 6,287 字节，`App.jsx` 为 23,833 行。P2-01、P2-02 与 P2-33 继续处理。

最新设置窗口拆分核验：P2-164 将完整设置窗口的现有显示、派生状态和七个设置区域装配原样移动到 `src/settings/SettingsModalView.jsx`，并只在用户打开设置时加载。账户登录与管理、模型和搜索密钥、本地服务、运行检查、隐私、工作区和工具设置全部保留；实际保存、密钥封存、账户操作、工作区绑定和检查函数继续由 `App.jsx` 提供。真实设置流程使用隔离本地账户完成模型密钥、搜索密钥、Secret Vault、运行检查、隐私、工作区、费用限制和完整工作检查，54.5 秒通过。真实检查同时修正了验收脚本在英语界面下仍查找中文模型说明和按钮的问题。551 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。普通工作区七种窗口尺寸与四种显示缩放通过；真实普通项目流程通过；完整 Dashboard 135.7 秒通过，Autopilot 回执为 8.311 秒。设置窗口独立构建文件为 23,994 字节；主页面构建文件为 724,900 字节，比 P2-163 减少 20,315 字节，`App.jsx` 为 24,073 行。P2-01、P2-02 与 P2-33 继续处理。

最新静态样式拆分核验：P2-163 将 `App.jsx` 中 20,959 字节的原静态样式逐字移动到 `src/styles/legacyApp.css`，并由应用入口在共享样式之后直接加载。Dashboard、节点流图、Group Chat、会议、Timeline、intent 路由、动画和全部操作没有重写；运行时不再创建重复的 `<style>` 元素。保护测试先失败后通过，550 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。普通工作区七种窗口尺寸与四种显示缩放通过；真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过；完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作并检查四种 Dashboard 尺寸，167.8 秒通过，Autopilot 回执为 17.006 秒。主页面构建文件为 745,215 字节，比 P1-66 减少 21,096 字节，`App.jsx` 为 24,917 行。P2-01、P2-02 与 P2-33 继续处理。

最新适配核验：P1-66 为完整 Dashboard 增加 1440×1100、1280×720、1024×768 和 960×540 四种电脑可用宽度的真实检查。首次检查在 960×540 复现 19 像素横向滚动；页面现在明确禁止该横向滚动，并在小于 1280 像素时使用更紧凑的外边距、内边距和内容间距，1440 像素及以上继续使用原间距。Dashboard 页面结构、节点流图、Group Chat、会议、Timeline、intent 路由和全部操作保持不变。修复后四种完整 Dashboard 宽度、普通工作区七种窗口尺寸、100%/125%/150%/200% 四种系统缩放、真实普通项目流程及完整 Group Chat、Manager、Agent、Autopilot 操作均通过。完整 Dashboard 检查 209.8 秒通过，Autopilot 回执为 19.831 秒。549 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 766,311 字节，`App.jsx` 为 25,544 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-162 将完整 Dashboard 中 Active Threads 与 Team 两个现有面板的共同装配按原位置移动为独立 915 字节按需文件。两个原面板继续分别按需加载，原双列布局、加载状态和参数连接全部保留；任务证明、聊天证明、Timeline 证明、Manager 同步、Agent 工作区、消息、Artifact、Evidence、Pulse 及全部操作函数继续由 `App.jsx` 提供。保护测试先失败后通过。真实普通项目流程继续覆盖立项、会议、群聊、时间线和刷新恢复并通过；真实完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，85.8 秒通过，Autopilot 回执为 6.133 秒。547 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。独立构建文件为 1,186 字节，主页面构建文件为 766,239 字节，`App.jsx` 为 25,544 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-161 将普通项目群聊页面装配按原位置移动为独立 915 字节按需文件。原 `ProjectChatPanel` 继续单独按需加载；频道、消息、输入、发送状态、恢复状态和回调参数保持原顺序。消息筛选、频道切换、输入处理、发送、重载、后台同步和完整 Group Chat 继续由 `App.jsx` 提供。保护测试先失败后通过；真实普通项目流程继续覆盖立项、会议、群聊、时间线和刷新恢复并通过。真实完整 Dashboard 继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，85.3 秒通过，Autopilot 回执为 4.434 秒。546 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。独立构建文件为 924 字节，主页面构建文件为 766,785 字节，`App.jsx` 为 25,551 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-160 将普通项目会议页面装配按原位置移动为独立 3,327 字节按需文件。会议舞台、会议记录和输入面板继续各自按需加载；返回项目、发送、语音、输入状态、设置操作和完整会议入口全部保留，操作函数继续由 `App.jsx` 提供。真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过。完整 Manager Backend Core UI 初次检查发现 Agent 自主行动点击后 5 秒内偶发没有运行提示；组件现在点击时立即显示当前 Agent 并禁止重复点击，原后端执行、回执和证明逻辑不变。修复后真实完整 Dashboard 176.8 秒通过，Autopilot 回执为 16.816 秒。545 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。会议独立构建文件为 2,922 字节，主页面构建文件为 766,841 字节，`App.jsx` 为 25,553 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-159 将 Manager Read Model Snapshots 与 Manager Action Queue Snapshot 两个连续只读区域的装配代码按原位置移动为独立 2,033 字节按需文件。两个原组件继续各自按需加载，管理状态、来源标识、行动队列摘要、统计、下一操作、地址和显示顺序全部保留；完整 Autonomous Run Control 及其所有执行函数和禁用规则继续留在 `App.jsx`。首次真实检查发现 Agent 操作后的后台读取已经开始时不能被原取消函数停止，导致开始 Autopilot 会话 15 秒内没有回执；现在用户操作会取消等待中和执行中的后台项目读取，保护测试先失败后通过。真实 Manager Backend Core UI 重新运行，继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，109.6 秒通过，Autopilot 回执为 4.113 秒。544 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和构建体积检查通过。独立构建文件为 1,781 字节，主页面构建文件为 768,568 字节，`App.jsx` 为 25,578 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-158 将 Submission Workspace 与 Manager Read Model Routes 两个连续显示区域的装配代码按原位置移动为独立 2,798 字节按需文件。两个原组件继续各自按需加载，提交地址、草稿地址、证据地址、复核地址、提交记录、复核编辑、证明操作、管理接口地址和显示顺序全部保留；复核写入、草稿更新、聊天证明、Timeline 证明和三项禁用规则继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待提交地址和管理接口地址出现，并继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，160.2 秒通过，Autopilot 回执为 12.815 秒。542 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 768,744 字节，`App.jsx` 为 25,563 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-157 将无完整 Ready Package 时的 Collaboration Intent Queue 兼容显示与 Transcript Proof Coverage 两个连续显示区域的装配代码按原位置移动为独立 2,536 字节按需文件。两个原组件继续各自按需加载，原条件、来源标识、状态、统计、意图运行、聊天证明、Timeline 证明、对话证明打开、地址和显示顺序全部保留；四类操作回调和全部禁用判断继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待对话证明覆盖出现，并继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，114.5 秒通过，Autopilot 回执为 4.061 秒。541 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 769,371 字节，`App.jsx` 为 25,572 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-156 将 Backend Manager Snapshot 与 Product Team Mission Runner 两个连续显示区域的装配代码按原位置移动为独立 3,894 字节按需文件。两个原组件继续各自按需加载，经理摘要统计、来源标识、任务状态、运行地址、六项操作、六项禁用条件和显示顺序全部保留；执行意图、聊天证明、Timeline 证明、节点定位及全部禁用判断继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待经理摘要出现，并继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，126.2 秒通过，Autopilot 回执为 5.033 秒；专门创建 Product Team Mission 的真实 Mission Runner 流程 111.5 秒通过。540 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 769,887 字节，`App.jsx` 为 25,582 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-155 将 Manager Ready Package 内 Provider Eval Run Workflow、Evidence Custody Readiness、Security Boundary 与 Package route 三个连续面板和地址显示的装配代码按原位置移动为独立 2,908 字节按需文件。三个原组件继续各自按需加载，供应商评估运行、证据保管、安全边界、来源标识、Record Eval 操作、证明同步、本地路由和显示顺序全部保留；真实保存命令、四项禁用条件、来源计算、证明同步按钮和三项可用性判断继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已明确等待这三个面板与三个来源标识出现，并继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，140.7 秒通过；Autopilot 回执为 5.562 秒。539 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 770,609 字节，`App.jsx` 为 25,596 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-154 将 Manager Ready Package 内 Pilot Launch Readiness、Deployment Preflight、MVP Readiness、Operations Readiness、Provider Readiness 与 Provider Controlled Run 六个连续本地就绪组件的装配代码按原位置移动为独立 5,040 字节按需文件。六个原组件继续各自按需加载，发布决策、发布前检查、本地 MVP 操作、运行就绪、供应商就绪、受控运行、来源标识、状态、统计、本地路由和显示顺序全部保留；MVP 操作函数、来源计算和五项可用性判断继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已明确等待这六个面板与三个来源标识出现，并继续检查 P2-153 的 11 个面板，再完成 Group Chat、Manager、Agent 与 Autopilot 操作，188.2 秒通过；Autopilot 回执为 18.872 秒。538 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 771,648 字节，`App.jsx` 为 25,622 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-153 将 Manager Ready Package 内 Private Pilot Workflow Panels、Production Operations Readiness、四组 Production Control Receipts、Production Launch Audit 与 Launch Approval Workflow 八个连续组件的装配代码按原位置移动为独立 6,880 字节按需文件。八个原组件继续各自按需加载，四项私有试运行工作流、生产就绪状态、四组生产控制、发布审计、发布批准、来源标识、统计、Record 按钮、本地路由和显示顺序全部保留；两类保存函数、可用性判断和按钮禁用规则继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已明确等待 11 个面板与 5 个来源标识出现，再继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，201.8 秒通过；Autopilot 回执为 11.824 秒。537 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 773,879 字节，`App.jsx` 为 25,676 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-152 将 Manager Ready Package 内 Private Pilot Go-Live Readiness、Production Infrastructure Rehearsal、Public Production Startup Readiness 与 Production Launch Proof Panels 四个连续发布就绪显示区的装配代码按原位置移动为独立 5,647 字节按需文件。五个原组件继续各自按需加载，状态、统计、来源标识、证明同步、本地路由、公开生产摘要、演练回执和显示顺序全部保留；基础设施演练操作、来源计算、同步函数和禁用规则继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待并确认七个发布与生产证明来源标识出现，再继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，133.7 秒通过；Autopilot 回执为 4.3 秒。536 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 776,714 字节，`App.jsx` 为 25,756 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-151 将 Manager Ready Package 内 Project Evidence Archive、Brainstorm Layer、Artifact Quality Audit、Submission Review Workflow、Evidence Quality Audit、Evidence Index Readiness 和 Evidence Source Review Workflow 七个连续证据与复核面板的装配代码按原位置移动为独立 7,675 字节按需文件。七个原面板继续各自按需加载，状态、统计、来源标识、Sync Proof Models 操作、本地路由、聊天证明、Timeline 证明和显示顺序全部保留；后端模型、来源计算、同步函数和两个证明跳转函数继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待并确认七个后端来源标识出现，再继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，177.6 秒通过；Autopilot 回执为 17.6 秒。535 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 779,308 字节，`App.jsx` 为 25,805 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-150 将 Manager Ready Package 内 Runtime Contracts、Autonomous Cycle Consistency、Runtime Autonomy Status、Zero-to-autonomy Report 和 Product Team Delivery Trace 五个连续运行状态面板的装配代码按原位置移动为独立 6,446 字节按需文件。五个原面板继续各自按需加载，状态、统计、来源标识、Sync Proof Models 操作、本地路由、聊天证明、Timeline 证明、节点流定位和显示顺序全部保留；后端模型、来源计算、同步函数及三个证明跳转函数继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待并确认五个后端来源标识出现，再继续完成 Group Chat、Manager、Agent 与 Autopilot 操作，127.2 秒通过；Autopilot 回执为 6.1 秒。534 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 783,102 字节，`App.jsx` 为 25,907 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-149 将 Manager Ready Package 内 Product Team Operating Loop、Planner / Executor / Reviewer 和 Team Collaboration Diagnostics 三个连续状态面板的装配代码按原位置移动为独立 2,596 字节按需文件。三个原面板继续各自按需加载，状态、统计、来源标识、Sync Proof Models 操作、本地路由和显示顺序全部保留；后端模型、来源计算和同步函数继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接等待并确认三个后端来源标识出现，再继续完成聊天、Manager、Agent 与 Autopilot 操作，138 秒通过；Autopilot 回执为 8.9 秒。533 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 786,110 字节，`App.jsx` 为 25,983 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-148 将 Manager Ready Package 顶部完整统计区按原位置移动为独立 19,005 字节按需文件。Status、Score、MVP、私有试运行、生产、发布审批、发布审计、证据、提交复核、协作意图、运行契约、自动运行、供应商、运维和工作区统计的原顺序、原格式化函数与原数据来源全部保留；该区域没有写入按钮，所有格式化函数继续由 `App.jsx` 提供。真实 Manager Backend Core UI 已直接确认 Pilot Launch、Evidence Archive、Intent Queue、Runtime Contracts 和 Transcript Channels 显示，并继续完成聊天、Manager、Agent 与 Autopilot 操作，135.4 秒通过；Autopilot 回执为 6.4 秒。532 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。主页面构建文件为 787,707 字节，`App.jsx` 为 26,022 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-147 将 Backend Worker Station 的本地连接与同步状态按原位置移动为独立 9,905 字节按需文件。后端状态、Online/Offline 标识、地址输入、Save URL、8 项调度统计、Immediate Start、Agent 与 Autopilot 控制摘要、项目和 Manager 各类同步时间、部署设置操作、禁用规则和最后操作状态全部保留；保存地址和打开部署设置继续调用 `App.jsx` 中原函数。真实 Manager Backend Core UI 实际点击 Check 并等待 Online，115.4 秒通过。531 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和构建体积检查通过。主页面构建文件为 804,579 字节，`App.jsx` 为 26,127 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-146 将 Manager Ready Package 内完整 Collaboration Intent Queue 按原位置移动为独立 13,577 字节按需文件。原 Proof Map 简版组件和无 Ready Package 时的兼容组件继续保留，没有覆盖或合并。完整面板的状态、来源标识、就绪状态、10 项统计、下一条可运行意图、最多 8 条队列记录、最近运行回执、聊天证明操作、时间线证明操作、Run intent 操作、全部禁用规则、失败状态、自主行动决定、六类输出记录和本地工作流路由全部保留。真实 Manager Mission Runner UI 实际点击 Run intent 并显示 Agent Submission 输出，107.5 秒通过。530 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和构建体积检查通过。主页面构建文件为 813,522 字节，`App.jsx` 为 26,241 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-145 将 Manager Ready Package 内完整 Submission Review Workflow 按原位置移动为独立 4,967 字节按需文件。原 Proof Map 简版组件继续保留，没有覆盖或合并。完整面板的状态、来源标识、证明同步操作、8 项统计、最多 4 条复核记录、复核与提交地址、响应地址、聊天证明入口、时间线证明入口、禁用规则和本地工作流路由全部保留。真实 Manager Backend UI 完整验收 1,342.5 秒通过。529 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。主页面构建文件为 826,355 字节，`App.jsx` 为 26,485 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-144 将 Launch Approval Workflow 按原位置移动为独立 4,825 字节按需文件。状态、来源标识、私有试运行就绪状态、4 项统计、最多 3 条批准记录、经理批准和安全批准两个按钮、全部批准身份、原因、来源、禁用规则及本地路由全部保留；按钮继续调用 `App.jsx` 中原保存函数。真实浏览器从本地账户和完整 Dashboard 开始，实际点击两次批准并继续完成证据导出、Private Pilot 与四组 Production Controls，185.1 秒通过。528 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。主页面构建文件为 830,695 字节，`App.jsx` 为 26,553 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-143 将 Production Launch Audit 按原位置移动为独立 3,722 字节按需文件。私有试运行与生产决策、13 项统计、最多 3 条失败门禁或生产阻断项、关联地址和本地审计路由全部保留；该区域没有保存按钮，也没有移动任何写入逻辑。真实浏览器连续完成 Private Pilot 与四组 Production Controls，159.5 秒通过，生产控制变化后的审计状态正常显示。527 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。主页面构建文件为 835,234 字节，`App.jsx` 为 26,633 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-142 将 Production Provider Control Receipts 按原位置移动为独立 4,003 字节按需文件。Production Provider 状态、Provider Eval 前置条件、8 项统计、最多 4 条未验证控制、Record Rehearsal 按钮、本地演练身份、工作流键、回执键、禁用规则和本地路由全部保留；按钮继续调用 `App.jsx` 中原保存函数。真实浏览器连续完成 Private Pilot 与四组 Production Controls，159.5 秒通过；Provider 控制保存、状态更新和按钮禁用正确。526 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。主页面构建文件为 838,620 字节，`App.jsx` 为 26,674 行；P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-141 将 Production Security Control Receipts 按原位置移动为独立 3,999 字节按需文件。Production Security 状态、8 项统计、最多 4 条未验证控制、Record Rehearsal 按钮、本地演练身份、工作流键、回执键、禁用规则和本地路由全部保留；按钮继续调用 `App.jsx` 中原保存函数。真实浏览器连续完成 Private Pilot 与 Production Operations、Deployment、Security、Provider 全部控制操作，152.6 秒通过；安全控制保存后的页面状态在约 0.2 秒内更新。525 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。主页面构建文件为 842,306 字节，`App.jsx` 为 26,734 行；P2-01、P2-02 与 P2-33 继续处理。

最新稳定性与职责拆分核验：P1-65 修复了 Private Pilot 最终验收后 Production Operations 按钮仍为灰色，以及四组 Production Controls 保存后页面还要等待约 23–30 秒的问题。最终验收和四组控制保存接口现在直接返回页面所需的最新工作流；页面不再为当前状态同时读取全部详细模型。真实浏览器从本地账户、工作区和完整 Dashboard 开始，连续完成 Private Pilot、Production Operations、Production Deployment、Production Security 和 Production Provider 全部操作，192.4 秒通过；四组生产控制保存后的页面更新时间约 0.2–0.3 秒。P2-140 将 Production Deployment Control Receipts 按原位置移动为独立 4,043 字节按需文件，原按钮、参数、禁用规则、统计、列表和本地路由全部保留。524 项自动测试、产品构建、原 Dashboard 功能保护、Agent Manager 场景、四组生产控制契约、前端边界、本地发布清单和构建体积检查通过。主页面构建文件为 845,986 字节，`App.jsx` 为 26,789 行；构建仍有大文件警告，P2-01、P2-02 与 P2-33 继续处理。

最新稳定性核验：P1-64 修复了 Private Pilot 连续操作中下一步按钮保持灰色、顶部状态停留在旧值，以及一次操作后大量本地读取造成长时间等待的问题。保存操作现在直接返回当前状态和受影响的下一步状态，页面优先使用该结果；不会再在每次操作后自动启动完整 Ready Package 读取，也不会同时请求全部详细模型。真实浏览器从本地账户、工作区和完整 Dashboard 开始，连续完成负责人批准、安全批准、证明导出申请、两次批准、下载审计、Release Candidate、Launch Run、Launch Health Check 和 Acceptance Report，81.3 秒通过；各次写入后的页面状态更新时间约 0.2–0.3 秒，没有请求暂停错误。523 项自动测试、产品构建、原 Dashboard 功能保护、Agent Manager 场景、前端边界、本地发布清单、构建体积检查和代码差异检查通过。主页面构建文件为 849,358 字节，`App.jsx` 为 26,836 行；构建仍有大文件警告，P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-139 将 Production Operations Control Receipts 面板按原位置移动为独立 4,039 字节按需文件。状态、8 项统计、最多 4 条未验证控制、Record Rehearsal 按钮、本地演练身份、工作流键、回执键、禁用规则和本地路由全部保留；按钮继续调用 `App.jsx` 中原后端记录函数。专项长浏览器流程暴露其验收脚本仍按旧流程操作：缺少隔离本地账户、强制等待已被自动同步替代的按钮、要求普通项目页显示内部 ID、未点击“查看完整项目控制台”。这些验收步骤已修复且有保护测试；流程随后进入完整 Dashboard，但在约 22 分钟外层上限前未结束，因此不记为通过。生产运行控制后端契约、前端按钮契约、Agent Manager 场景、前端边界、521 项自动测试、产品构建、原 Dashboard 功能保护、本地发布清单和代码差异检查通过。主页面构建文件为 848,686 字节，`App.jsx` 为 27,386 行。P2-01、P2-02 与 P2-33 继续处理，长流程耗时继续诊断。

最新职责拆分核验：P2-138 将 Production Operations Readiness 只读状态面板按原位置移动为独立 4,399 字节按需文件。状态标签、14 项统计、最多 4 条失败门禁、门禁路由和 Production Operations 路由全部保留；没有增加、删除或改写任何操作按钮。519 项自动测试、产品构建、原 Dashboard 功能保护、产品团队完整验收、前端边界检查、本地发布清单与代码差异检查通过，其中完整验收用时 378.8 秒。主页面构建文件为 852,469 字节，`App.jsx` 为 27,440 行。P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-137 将 Release Candidate、Launch Run、Launch Health Check 与 Acceptance Report 四个 Private Pilot 工作流面板按原位置移动为独立 16,539 字节按需文件。四组状态、统计、Record 按钮、运行回执、工作流键、回执键、路由、来源标识和禁用规则全部保留；写入继续调用 `App.jsx` 中原函数。首次真实流程发现独立组件缺少图标导入并立即修复；随后一次 Autopilot 回执等待未完成，保留请求路径和耗时诊断后，相同真实经理核心流程连续两次通过，自动执行回执分别在 4.3 秒和 14.3 秒出现，完整流程分别用时 119.2 秒和 167.4 秒。518 项自动测试、产品构建、原 Dashboard 功能保护、完整经理界面检查、前端边界检查、本地发布清单与代码差异检查通过。主页面构建文件为 856,518 字节，`App.jsx` 为 27,482 行。P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-136 将 Production Launch Gap Register、Production Launch Control Center、Production Launch Evidence Dossier 与 Production Evidence Integrity Audit 四个连续证明面板按原位置移动为独立 13,926 字节按需文件。四组状态、统计、列表、来源标识、证明模型同步按钮与路由全部保留；同步按钮继续调用 `App.jsx` 中原函数，路由继续由 Manager Ready Package 提供。首次真实流程发现旧构建仍包含已移除的无用属性并进入恢复界面，重新生成最新构建后同一流程 160.1 秒通过。517 项自动测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单与代码差异检查通过。主页面构建文件为 872,678 字节，`App.jsx` 为 27,738 行。P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-135 将 Public Production Startup Readiness 完整详情按原位置移动为独立 24,622 字节按需文件。公开生产摘要继续独立加载；Managed Identity、Cost Control、Data Governance、Traffic、Customer Acceptance、Managed Secrets、Infrastructure Cutover、Production Operations、失败门禁、Environment Setup、Action Plan、验证命令、必需环境变量与公开启动路由全部保留。516 项自动测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 147.1 秒真实经理核心流程通过。主页面构建文件为 885,820 字节，`App.jsx` 为 27,939 行。P2-01、P2-02 与 P2-33 继续处理。

最新职责拆分核验：P2-134 将 Launch Operations Overview 按原位置移动为独立 6,594 字节按需文件，保留私有试点与公开生产状态、下一行动、Private MVP Launch Package、Public Production Next Steps、Record 按钮、运行回执、统计、路由和阻断项；运行函数、禁用规则及路由来源继续由 `App.jsx` 提供。515 项自动测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 91.9 秒真实经理核心流程通过。主页面构建文件为 909,990 字节，`App.jsx` 为 28,332 行。P2-01、P2-02 与 P2-33 继续处理。

本次稳定性核验：P1-62 Agent 自主行动偶发无输出与 P1-63 Autopilot Scheduler Tick 偶发无回执已经修复。点击后页面会立即显示正在运行，单次本地重任务允许最多等待 60 秒，验收等待与产品超时一致；新的用户操作会取消尚未开始的后台详细刷新，Agent 行动与 Autopilot 会话的非必要详细读取延后 15 秒并可取消，Scheduler Tick 使用专用本地路由且不会附带无关项目和 Agent 扫描。真实经理核心流程连续两次通过，分别用时 177.5 秒与 159.8 秒；514 项自动测试全部通过、0 项失败，产品构建、前端边界检查、本地发布清单与代码差异检查通过。主页面构建文件为 916,093 字节，`App.jsx` 为 28,417 行；原 Dashboard、节点流图、Group Chat、会议、Timeline 和 intent 路由保护检查继续通过。P2-01、P2-02 与 P2-33 仍未完成。

最新追加核验：P2-130 至 P2-133 已将 Deployment Preflight、简版与完整 Production Infrastructure Rehearsal 以及 Public Production Startup 顶部摘要按原位置拆为独立 4,390、4,816、6,474 与 2,595 字节按需文件；发布前统计、失败门禁、基础设施演练、托管切换证明操作和公开生产启动 15 项统计全部保留。当前产品构建通过，513 项自动测试全部通过、0 项失败；主页面构建文件为 915,775 字节，`App.jsx` 为 28,386 行。真实经理核心流程首次在后续 Agent 行动输出等待处超时一次，立即复跑后 137.7 秒通过；原 Dashboard、节点流图、Group Chat、会议、Timeline 和 intent 路由保护检查继续通过。

最新核验：中英文完整界面 17 步验证已通过，覆盖首次使用、设置、人才市场、项目、群聊、立项、实时切换和项目语言覆盖。经理演示首次打开会先完成本地项目准备，再读取账户权限、时间线、事件、聊天记录和管理数据；Scenario Trail、Manager Requirement Matrix 与 Sync Protocol Audit 读取均已纳入同一准备任务。Manager Flow Graph 时间线证明跳转现在使用节点在画布中的绝对位置计算焦点，异步节点尚未出现时会在 5 秒内按条件继续查找，找到节点后再等待位移动画稳定和节点持续可见。真实 Mission Runner 浏览器流程已通过本地账户、工作区准备、立项会议、项目创建、完整 Dashboard、协作意图执行、聊天证明、时间线证明和节点流定位；真实提交与复核流程完成提交生成、聊天证明、时间线证明、复核填写、复核回执、修订任务、再次提交和最终状态检查。Scenario Control Center、Manager Live Command Center、Manager Use Case Audit、Private Pilot Go-Live Readiness、Pilot Launch Readiness、MVP Readiness、Operations Readiness、Provider Readiness、Provider Controlled Run、Provider Eval Runs、Evidence Custody Readiness、Security Boundary、Backend Manager Snapshot 顶部统计摘要、Product Team Mission Runner、兼容协作意图队列、兼容对话证明覆盖区、提交复核工作区、兼容管理状态路由、六个管理状态面板、Manager Action Queue 兼容摘要、Latest Backend Work 只读结果区、后端调度控制显示区、Agent Autonomous Queue 与 Autonomous Run Control 显示区已按原显示和原操作独立按需加载。当前产品构建通过，自动测试 509 项通过、0 项失败；主页面构建文件为 931,454 字节，`App.jsx` 为 28,580 行。原 Dashboard、节点流图、Group Chat、会议、Timeline 和 intent 路由保护检查继续通过。

## 原始 Dashboard 恢复后的重新核验

2026-07-15 继续采用“只移动、不重写”的方式，把立项说明、保存位置、组建团队、会议准备和立项结果五个非 Dashboard 页面，以及 Dashboard 内的需求证明、Leader 分配、变更提交、自动工作循环、24/7 Operations Board、Continuous Work Loop、Fixed Work Routines、Manager Scenario Readiness、Agent Management Mesh、Collaboration Health、Leader Assignment Flow、Active Threads、Recent Commit Line、项目工具入口、Sample Fixture Path、Transcript Proof Coverage、Transcript Channel Routes、Transcript Channel Pin Routes、Transcript Pin Routes、Transcript Reply Routes、Transcript Mention Routes、Transcript Attachment Routes、Transcript Member Presence Routes、Agent Message Routes、Agent Contract Routes、Collaboration Intent Queue、Submission Review Workflow、Runtime Contracts、Autonomous Cycle Consistency、Planner / Executor / Reviewer、Team Collaboration Diagnostics、Runtime Autonomy Status、Zero-to-autonomy Report、Product Team Operating Loop、Product Team Delivery Trace、Project Evidence Archive、Brainstorm Layer、Artifact Quality Audit、Evidence Quality Audit、Evidence Index Readiness、Evidence Source Review Workflow 和 Project Evidence Export Workflow 改为按需加载。原 Dashboard、节点流图、Group Chat、会议、Timeline 和 intent 路由均未改写；原功能保护检查、真实经理演示整链、真实项目立项到刷新恢复流程、产品构建和 467 项自动测试全部通过。经理演示还修复了项目持久化时没有明确保留当前界面语言的问题，浏览器检查改为按中英文与稳定操作标识验收，不再把中文界面误报为功能缺失。

2026-07-13 恢复了原有项目 Dashboard、节点流图、群聊和意图处理功能。恢复操作同时使一部分 2026-07-11 的界面简化与性能结论失效，因此下方较早的“已验证”记录只能作为当时版本的记录，不能直接代表当前版本。

2026-07-14 已重新通过：首次本地账户、四字符字母数字密码、模型供应商选择、项目立项、项目会议、项目群聊、工作记录、刷新恢复、原节点流图、管理场景、七种窗口尺寸、四种显示缩放，以及 426 项自动测试。完整管理界面浏览器整链流程已经从简洁工作区进入原完整 Dashboard，并继续完成 Manager 同步、Leader Assignment、Server Pulse、Hour/Day Pulse、Agent 详情、Agent Workbench、证据、草稿、复核和最终管理证明，1,142.7 秒内通过。

本次重新补回：侧边栏中文与按钮名称、六个立项步骤中文、五种工作类型中文、保存位置中文、技术地址默认收起、示例数据入口默认收起。普通工作区、普通项目首页、普通群聊、普通会议和普通工作记录默认使用简洁界面；用户主动进入完整工作区或完整项目控制台后，原 Dashboard、完整时间线、节点流图、Group Chat 和意图模块全部保留。

本轮修复了立项收尾覆盖新会议记录、交互请求误用 5 秒短超时、会议发送前重复读取完整项目、项目和消息重复写两次完整磁盘快照的问题。恢复原 Dashboard 后发现的详细数据集中读取问题也已修复：普通界面不再自动读取完整控制台数据，详细项目读取限制为最多 2 项同时执行，相同读取会共用结果，聊天和会议提交不进入这个等待队列。真实项目会议现在会立即显示发言，并显示处理、完成、失败或超时状态。

普通项目的“查看工作记录”现在只显示最近的用户工作记录，并隐藏内部路由、内部数据来源和系统运行账户。普通项目首页也按真实时间显示最新记录，并支持后端实际使用的 `log` 内容字段，不再显示旧记录或空白记录。完整项目控制台中的原节点流时间线没有删除，仍按原入口提供全部节点、关系和控制项。

本轮进一步完成了真实用户从空白状态到最终交付的本地浏览器验证：账户、模型、项目、完整 Dashboard、工作区绑定、团队执行、调查证据、草稿、复核、修改、最终交付和页面证明全部通过。集成设置按当前交互自动同步；管理证明刷新按项目合并重复请求，不再因连续写入产生多批重复读取，也不再把上线控制项混入普通证明刷新。

本轮还修复了三个会阻断真实使用的问题：已登录本地管理员首次创建项目被安全审计错误拒绝、事件列表与校验信息不同步导致完整界面崩溃、本地管理员启动调度后内部工作进程被本地身份规则拒绝。三个问题都有独立复现测试，并已通过完整管理界面真实流程。

本轮延迟处理继续修复：聊天、会议、Leader、在线 Manager、Agent 自主动作、Server Pulse、Hour/Day Pulse、Agent Pulse 和用户主动 intent 同步允许本地重任务最多等待 60 秒；用户操作会取消尚未开始的后台详细刷新，页面切换也会取消旧页面刷新。写入成功后，Agent Workbench 会先显示真实后端回执，再延后 5 秒顺序读取详细证明，避免用户已经完成操作但界面仍长时间没有结果。后台静默状态检查超时时不再把已经确认在线的本地服务错误显示为离线。

仍未完成：按需加载后主界面构建文件由约 1.58 MB 降至 957,816 字节，构建仍报告大文件警告。`App.jsx` 当前 29,014 行。下方 P2 明细已经更正，不再把删除原 Dashboard、流程图、群聊或主文件降至 12,100 行、362–490 KB 的旧结果作为当前证据。继续拆分必须保留原功能并避免大范围重写。

允许状态：`未处理`、`处理中`、`已修复`、`已验证`、`暂时无法完成`

## P0 严重问题

| 编号 | 状态 | 用户结果 | 验证证据 |
|---|---|---|---|
| P0-01 | 已验证 | 旧数据升级后仍能正常启动 | 真实 44MB 数据、2 个项目、升级事务已确认，`npm run dev` 前后端均启动 |
| P0-02 | 已验证 | 已有本地数据不会阻止整个系统启动 | 真实旧数据、全新临时数据、升级失败恢复测试均通过 |
| P0-03 | 已验证 | 修改数据格式前存在可恢复备份 | `.tmp/pre-upgrade-safety-20260711-192100/manifest.json` |
| P0-04 | 已验证 | 升级写入前自动保存原始版本并检查数据 | 迁移原始存档和校验测试通过 |
| P0-05 | 已验证 | 升级失败后自动恢复并保留失败目标文件 | 旧未确认事务恢复测试与真实数据恢复通过 |
| P0-06 | 已验证 | 单个损坏项目不会影响其他项目 | 项目补充失败隔离测试通过 |
| P0-07 | 已验证 | 启动错误使用普通中文并提供修复操作 | 真实关闭隔离后端后，普通页面显示中文恢复提示、数据保留说明、“重新检查”和“打开恢复设置” |
| P0-08 | 已验证 | 后端失败时前端仍可打开恢复页面 | 损坏主文件和备份时，后端失败而 Vite 继续运行 |
| P0-09 | 已验证 | 用户消息发送后立即显示 | 真实会议页发送后立即出现原文；后端拒绝时原文仍保留 |
| P0-10 | 已验证 | 每条消息显示提交、保存、处理、完成和失败状态 | 状态测试通过；真实页面已看到正在提交、AI正在处理、本轮已完成、提交失败和可重试 |
| P0-11 | 已验证 | AI回复可停止、取消、重试和跳过 | 真实会议逐项验证：停止和取消后后端晚返回不会重新启动；成员发言时可跳过；失败或取消后原文可恢复重试 |
| P0-12 | 已验证 | AI回复期间仍可继续输入 | 真实会议连续发送两条消息：输入框保持可用，第一条明确显示“已由新消息替代”，第二条正常完成 |
| P0-13 | 已验证 | 后端失败时仍可安全退出会议 | 实际停止 8790 后端后，5180 前端继续运行；点击退出后返回项目仪表盘 |
| P0-14 | 已验证 | 所有用户可进入的会议发送行为一致 | 普通项目会议与立项会议均使用共享会议页面；旧 War Room 无任何用户入口，入口测试通过 |
| P0-15 | 已验证 | AI无回复或超时后结束等待并允许重试 | 本地无响应服务真实测试：20秒后自动结束，原文保留，显示“等待本地服务超时”，重新发送按钮可用 |
| P0-16 | 已验证 | 默认由负责人简洁回答，其他成员按需要参与 | 前后端统一选择规则：默认负责人、明确 `@成员` 单人、明确 `@all` 全员，服务层测试通过 |
| P0-17 | 已验证 | 重启后消息和项目状态仍然存在 | 隔离后端重启后重新同步项目并进入会议，最近用户消息和AI回复重新显示 |
| P0-18 | 已验证 | 现有恢复工具能够实际备份并恢复 | `npm run local:recovery:drill` 通过 |
| P0-19 | 已验证 | 本地服务提供普通启动状态检查 | 未登录状态真实访问 `/health` 返回 200，仅包含本地运行状态，不暴露项目或用户数据 |
| P0-20 | 已验证 | 保存本地数据不会导致 Windows 界面进程退出 | 已复现 Vite 监听 `.tmp` 临时文件时报 `EBUSY`；忽略后端数据目录后连续保存，5180/8790 端口保持运行 |
| P0-21 | 已验证 | 单个界面渲染异常不会只留下空白页 | 新增本地界面恢复页，明确说明数据仍在本机，提供重新加载和默认收起的错误详情；在模型设置空字段异常中实际触发并验证，修复异常后首次入口恢复正常 |
| P0-22 | 已验证 | 设置页清理后仍能正常打开 | 自动浏览器检查发现并修复设置页缺少项目作用域读取函数的问题；390×844 真实浏览器重新打开模型设置成功，不再进入界面恢复页 |
| P0-23 | 已验证 | 立项会议可以正常结束 | 真实立项操作发现“回答来源”标签覆盖结束操作，已改为不接收点击并移开控制区；真实浏览器重新验证可以结束会议并进入创建项目页面 |
| P0-24 | 已验证 | 刷新后自动恢复并可重新打开本地项目 | 修复无本地账户令牌时不读取开放项目目录、项目卡点击调用已删除函数的问题；真实项目创建后刷新，项目数量、名称和打开操作全部恢复 |
| P0-25 | 已验证 | 全新用户先创建本地账户，再进入立项 | 修复首次账户引导组件存在但首页没有显示的问题；全新隔离环境会先显示本地服务检查、账户创建、模型配置和第一项目四个步骤，未登录点击创建项目会留在账户步骤并显示中文说明 |
| P0-26 | 已验证 | 已登录本地管理员可以直接创建第一个项目 | 修复新项目尚未存在时，写入前安全审计错误调用“读取现有项目”并返回 503 的问题；独立接口测试和真实管理界面项目创建通过 |
| P0-27 | 已验证 | 完整控制台连续同步后不会因事件校验信息过期而崩溃 | 事件列表、校验值、起止编号和总数现在作为同一份后端结果更新；复现测试、完整管理界面、Group Chat、管理动作和 Agent 自主执行通过 |
| P0-28 | 已验证 | 本地管理员启动自动运行后，内部工作进程可以继续执行 | 调度器只在内存中把已验证的本地会话交给内部工作请求；未登录仍被拒绝，已登录调度测试和真实自动运行会话回执通过 |

## P1 普通用户体验

| 编号 | 状态 | 用户结果 | 验证证据 |
|---|---|---|---|
| P1-01 | 已验证 | 完成全部普通用户页面和按钮清点 | 首次使用、工作区、项目首页、会议、群聊、时间线、设置和人才市场均已完成真实页面检查；普通模式无测试入口和无用途按钮 |
| P1-02 | 已验证 | 普通模式不显示内部测试功能 | 普通工作区和项目首页不再显示示例载入、内部路由、后台读模型和同步控制；高级信息按需打开 |
| P1-03 | 已验证 | 删除无用途按钮 | 普通工作区删除示例载入按钮，会议删除内部意图面板，项目页和时间线只保留用户工作入口 |
| P1-04 | 已验证 | 重复操作合并并标明对象 | 普通项目首页统一提供会议、群聊、时间线入口；每个项目卡和成员操作都标明具体对象 |
| P1-05 | 已验证 | 不可用操作说明原因和处理方法 | 会议停止、取消、跳过、重试，群聊发送、附件和频道创建均说明何时可用及恢复方法 |
| P1-06 | 已验证 | 不存在无名称图标按钮 | 首次使用、工作区、项目首页、会议、群聊、时间线、设置和人才市场主流程均完成可访问名称检查 |
| P1-07 | 已验证 | 不显示含义不明的单字母操作 | 侧边栏用户入口由 `D` 改为“总”，会议头像字母均与完整成员名称同时显示且不是操作 |
| P1-08 | 已验证 | 普通界面使用中文 | 首次使用、桌面导航、工作区、项目首页、会议、群聊、时间线、设置、账户管理和人才市场主操作均使用中文；账户角色、登录、密码、用户创建、停用和项目权限提示已统一 |
| P1-09 | 已验证 | 不显示内部地址、代码和测试字段 | 普通工作区、项目首页、会议和人才签约项目选择不再显示路由、后台地址、项目编号、内部状态、数据源代码、`LOG` 编号、后台会话编号和测试字段 |
| P1-10 | 已验证 | 首次打开自动检测本地服务 | 全新浏览器状态自动完成本地服务检查并显示普通中文结果 |
| P1-11 | 已验证 | 首次打开自动使用默认本地地址 | 未保存地址时真实会议页已自动使用 `127.0.0.1:8787`，不再要求手动填写 |
| P1-12 | 已验证 | 引导本地用户创建或登录 | 全新浏览器真实登录本地账户通过，账户说明明确为本机使用 |
| P1-13 | 已验证 | 引导配置模型 | 首次使用第 3 步提供模型设置，并允许明确选择稍后配置 |
| P1-14 | 已验证 | 引导建立第一个项目和团队 | 首次使用第 4 步直接进入真实项目立项流程 |
| P1-15 | 已验证 | 示例功能不高于真实工作操作 | 普通工作区已删除“Load Sample Fixture”，主要操作为创建项目和进入现有项目 |
| P1-16 | 已验证 | 演示项目始终明确标注 | 演示载入入口已从普通模式移除，高级模式原有示例标签保留 |
| P1-17 | 已验证 | 真实数据、缓存和演示数据明确区分 | 普通模式仅显示本地项目与“本地项目已同步”；示例和内部缓存信息不参与普通工作区统计 |
| P1-18 | 已验证 | 明确显示数据是否已保存到本地 | 首次使用明确说明数据只在本机；工作区显示“本地项目已同步” |
| P1-19 | 已验证 | 项目首页只显示七类主要信息 | 真实项目首页只显示目标、进度、当前工作、待确认、最近结果、团队和继续操作 |
| P1-20 | 已验证 | 证明、日志和高级控制按需展开 | 内部证明、日志和高级运行控制收进“查看高级运行信息” |
| P1-21 | 已验证 | 页面按钮数量受控 | 普通工作区仅保留创建、配置、项目卡和高级入口；项目首页仅保留会议、群聊、时间线和高级入口 |
| P1-22 | 已验证 | 标题和空白不妨碍主要工作 | 当前运行截图复核通过，主操作与当前工作在首屏可见 |
| P1-23 | 已验证 | 正文字号可读 | 会议、账户、人才市场、导航和首次使用界面的操作与状态文字已提高到 12px 以上；全局规则保证旧页面中的 8–10px 写法实际显示不低于 12px，七种规定浏览器尺寸复验通过 |
| P1-24 | 已验证 | 文字与背景对比满足可读要求 | 当前运行截图复核通过，普通页面和深色会议页面主要文字均清晰可读 |
| P1-25 | 已验证 | 会议文字不覆盖成员名称 | 桌面端真实会议页面复核通过；小屏幕自动切换为记录与输入布局，响应式布局测试通过 |
| P1-26 | 已验证 | 会议记录宽度、滚动和定位正常 | 真实长会议记录在独立侧栏滚动，输入区与控制区保持固定可用 |
| P1-27 | 已验证 | 用户问题、结论、决定和下一步突出显示 | 真实会议发送含“决定、结论、下一步”的消息后，立即显示“你的问题”和独立“下一步”；AI回复也显示类型与下一步 |
| P1-28 | 已验证 | AI默认简洁并可展开完整说明 | 真实长回复默认收起为四行，“展开完整内容”和“收起完整内容”均操作通过 |
| P1-29 | 已验证 | 键盘、屏幕阅读和焦点状态可用 | 会议输入框真实键盘焦点显示 2px 清晰边框；主要输入和图标按钮具有可读名称；可访问性测试通过 |
| P1-30 | 已验证 | 输入框有标签、错误和提交状态 | 会议输入框有“会议发言”标签，消息显示提交、完成、失败、超时和重试状态 |
| P1-31 | 已验证 | 刷新、切换项目和重启不丢失操作状态 | 登录后重启自动恢复项目目录；会议记录和消息状态恢复验证已通过 |
| P1-32 | 已验证 | 首次立项不显示英文步骤、内部地址和内部状态代码 | 真实首次使用复验发现并修复；立项标题、六个步骤、启动条件、五类工作模式、按钮和摘要均改为普通中文，七种规定浏览器尺寸复验通过 |
| P1-33 | 已验证 | 手机高级设置首屏不再直接显示大量英文和内部诊断字段 | 本地服务标题、说明、按钮和状态均改为普通中文；地址以外的内部接口、状态代码和验证命令收进默认关闭的“查看技术诊断信息”；390×844 真实浏览器复验窗口完整且关闭按钮可见 |
| P1-34 | 已验证 | 首次使用的模型设置页简洁、中文且不会空白 | 模型地址、名称、密钥、保存和测试成为主操作；调查资料搜索与技术诊断默认收起；修复全新账户空状态与浏览器误填用户名问题；删除重复“模型”分类入口，390×844 真实浏览器从首次入口复验通过 |
| P1-35 | 已验证 | 本地免密模型和失败提示可直接理解 | 可勾选“这个本地模型不需要密钥”使用默认 Ollama 等本地接口，勾选后清空密钥草稿且保存按钮可用；真实连接失败复验显示明确中文，不再暴露英文错误 |
| P1-36 | 已验证 | 健康检查页只显示普通用户需要的结果和恢复操作 | 基础检查、完整工作检查、状态、时间和九类结果全部改为中文；模型、搜索、任务和完整流程失败分别给出下一步；接口地址和原始返回默认收进技术诊断；390×844 真实基础检查复验通过 |
| P1-37 | 已验证 | 隐私设置使用中文并明确纯本地边界 | 数据保留、模型日志和导出确认均使用普通中文；无项目时明确提示先创建或选择项目；“不会用于模型训练”单独显示；内部接口与生产状态不再出现在普通页面，390×844 真实复验通过 |
| P1-38 | 已验证 | 工作区设置只保留可执行的本地操作 | 保留界面语言、项目语言、显示密度、默认可见范围、自动保存和本地文件夹绑定；无项目时明确说明原因；内部能力证明、接口地址和只读状态卡已移除，390×844 真实复验通过 |
| P1-39 | 已验证 | 扩展工具页优先显示项目权限和使用限制 | 五项团队能力改为中文并可按项目开关；保留每日费用和每小时调用限制；无项目时明确说明原因；内部接口、能力合同和生产状态默认收进“查看技术诊断信息”，七种窗口尺寸和四种缩放复验通过 |

## P2 稳定性、性能与适配

| 编号 | 状态 | 用户结果 | 验证证据 |
|---|---|---|---|
| P2-01 | 已验证 | 页面显示职责拆分完成，同时保留原 Dashboard 业务控制 | 首次使用、工作区、人才市场、人才详情、项目概览、群聊、会议、时间线、设置、立项步骤和完整 Dashboard 显示装配均已独立；P2-190 将完整 Dashboard 最外层、内容加载边界和项目工具加载边界移至独立按需文件。写入、证明跳转、按钮回调、节点确认、后端同步、intent、可用性判断和禁用规则继续由 `App.jsx` 集中提供，避免重写业务行为。原 Dashboard、节点流图、Group Chat、会议、Timeline 和 intent 路由全部保留。集中自动检查和真实完整 Dashboard 流程通过 |
| P2-02 | 已验证 | 普通页面与完整控制台安全拆分完成 | 普通页面、完整 Group Chat、完整会议、完整 Timeline、Manager Flow Graph、完整 Dashboard 顶部、Manager 主体、后台工作站、内容外层及最外层页面均已独立按需加载。完整 Dashboard 已覆盖 1440×1100、1280×720、1024×768、960×540，并消除紧凑宽度横向滚动。原错误提示、按钮、禁用条件、证明操作、回执、intent 和 Action Playbook/Run Ledger 全部保留；真实完整 Dashboard 流程通过 |
| P2-03 | 已验证 | 会议发送和状态规则一致，完整会议功能保留 | 普通会议使用共享输入、记录和状态组件；完整控制台会议未删除。即时显示、完成、失败、超时、停止和重试测试通过 |
| P2-24 | 已验证 | 普通工作记录与完整节点时间线分开 | 普通项目只加载最近工作记录；从完整项目控制台进入时仍显示原节点、关系和控制项。真实项目流程和经理演示均覆盖两个页面 |
| P2-25 | 已验证 | 手机宽度下高级设置可以导航和关闭 | 高级设置由固定左右栏改为手机纵向布局，分类栏可横向滚动，内容区域独立滚动；390×844 真实浏览器复验窗口位于可视范围内，关闭按钮坐标有效且始终可见 |
| P2-26 | 已验证 | 删除无入口的旧立项和后台操作 | 已删除无入口旧立项页，以及旧时间线备注、旧自动推进、旧立项澄清、旧招募和旧滚轮处理；增加防回归检查，专项检查与构建通过 |
| P2-27 | 已验证 | 模型配置从主文件拆成独立组件 | 新增 `src/settings/LocalModelSettings.jsx`，首次使用与普通设置共用同一模型配置界面；旧模型密钥页、重复模型状态页和无用状态计算已从 `App.jsx` 删除；专项检查、构建与真实浏览器验证通过 |
| P2-28 | 已验证 | 健康检查从主文件拆成独立组件 | 新增 `src/settings/LocalHealthSettings.jsx`，旧健康检查界面和内部接口列表已从 `App.jsx` 删除；真实本地基础检查、专项检查与构建通过 |
| P2-29 | 已验证 | 隐私设置从主文件拆成独立组件 | 新增 `src/settings/LocalPrivacySettings.jsx`，旧隐私界面和内部接口卡片已从 `App.jsx` 删除；专项检查、构建与真实手机尺寸验证通过 |
| P2-30 | 已验证 | 工作区设置完整拆分且没有减少功能 | `src/settings/LocalWorkspaceSettings.jsx` 已连接为按需加载组件；界面语言、项目语言、显示密度、默认可见范围、自动保存、本地文件夹绑定、记忆就绪同步、会议摘要同步和后端能力状态全部保留。七种窗口尺寸、四种缩放、真实设置后端操作、专项边界检查和发布检查通过 |
| P2-31 | 已验证 | 扩展工具设置完整拆分且没有减少功能 | `src/settings/LocalToolsSettings.jsx` 已连接为按需加载组件；五项项目权限、每日费用、每小时次数、后端保存、就绪同步、能力状态和全部技术接口信息均保留，技术信息默认收起。真实设置后端操作、七种窗口尺寸、四种缩放、专项检查和构建全部通过 |
| P1-40 | 已验证 | 工作区和项目首页不再显示乱码 | 所有主要标题、状态、空页面、任务、团队和操作均为正常中文；390×844 真实浏览器复验通过 |
| P1-41 | 已验证 | 立项后半段开始改为普通中文 | 保存位置、组建团队和会议准备页面已完成中文化，内部接口与检查信息默认收起 |
| P1-42 | 已验证 | 立项会议结果页使用普通中文 | 项目名称、工作类型、负责人、复核人、成员、首批工作和创建操作已中文化；真实浏览器从简报、工作区、组队、会议、结果确认到创建项目全流程通过 |
| P1-43 | 已验证 | 首次使用时保留侧边栏，并直接显示保存位置错误 | 首次使用页面继续显示项目工作区、人才市场、创建项目和设置入口；未登录不能进入保存位置步骤；账户会话失效时，保存位置操作会禁用并在页面上直接显示中文处理说明，不再把错误藏在收起区域 |
| P2-32 | 已验证 | 普通工作区和项目首页不显示内部控制，完整控制台可明确进入 | 普通页面只保留创建项目、配置模型、打开项目、会议、群聊和工作记录；用户选择“查看完整项目控制台”后进入保留的完整功能 |
| P2-33 | 已验证 | 首次加载代码预算已经固定并通过 | 主页面构建文件从 1,488,702 字节降至 657,688 字节，减少 831,014 字节；P2-190 外层按需文件为 1,852 字节。生产构建使用 700 KB 明确警告上限，独立发布检查同时强制 `entry < 700,000` 字节，当前构建不再出现大文件警告。原 Dashboard、节点流图、Group Chat、会议、Timeline、intent、按钮、写入和证明动作均保留 |
| P2-41 | 已验证 | 人才签约项目选择不显示内部字段且不影响签约条件 | 签约项目选择已连接独立按需加载组件；已有团队判断、本地合同回退、接口可用性和签约中禁用规则全部保留；真实浏览器验证了打开、选择、关闭和返回流程 |
| P2-42 | 已验证 | 模型设置专业验收与当前简洁设置保持一致 | 隔离验收使用真实本地账户，从普通项目进入保留的完整控制台；模型和搜索密钥输入、保存、清空明文、后端密钥存储、隐私、工作区、费用限制和完整工作检查全部通过，不再要求显示旧技术说明卡或重复模型状态分类 |
| P2-43 | 已验证 | 用户主动同步记忆与会议摘要不会被短等待提前终止 | 浏览器记录确认接口请求已发出但旧 3–3.5 秒上限会提前终止；手动同步等待上限统一改为 10 秒，后台自动刷新仍保持短等待；模型设置专业验收、真实项目流程和全量测试通过 |
| P2-44 | 已验证 | 本地部署设置独立加载且保留全部操作 | 新增 `src/settings/LocalDeploymentSettings.jsx`，保留地址输入、保存、运行时同步、就绪状态和服务路由；删除主文件中的旧重复界面。真实本地服务、七种窗口尺寸、四种缩放、项目全流程、经理演示、发布检查和 389 项测试全部通过；原 Dashboard 未修改 |
| P2-45 | 已验证 | 完整控制台读取不会继续延迟普通聊天和会议 | 详细项目读取最多 2 项同时执行并合并相同请求；聊天和会议提交直接执行。普通聊天或会议完成后只刷新当前界面需要的数据，完整控制台数据仅在完整控制台中自动刷新。真实流程连续两次完成完整 Dashboard、节点图、群聊、会议、时间线和重启恢复，会议 15 秒验收上限内完成 |
| P2-46 | 已验证 | 同一项目连续保存不再重复执行完整数据补全 | 文件存储为已完成数据补全的项目添加仅存在于内存的标记，派生保存直接复用；标记不写入磁盘，重启后仍会正常检查。新增回归测试通过 |
| P2-47 | 已验证 | 旧版 War Room 仅在进入时加载且操作没有减少 | 原开始、结束、参与者选择、会议记录、指令输入和本地部署设置入口全部移入 `src/project/LegacyWarRoomView.jsx`；受保护 Dashboard 源码范围未修改。独立操作保护测试、构建预算、发布清单、389 项测试、经理演示和真实项目流程通过 |
| P2-48 | 已验证 | 侧边栏独立且没有减少项目入口 | 现有 `src/navigation/ProductSidebar.jsx` 已连接；工作区、人才市场、创建项目、项目列表、用户设置、折叠状态和中英文标签全部保留。七种窗口尺寸、四种缩放、可访问性检查、389 项测试和经理演示通过；受保护 Dashboard 源码范围未修改 |
| P2-49 | 已验证 | 设置窗口外壳按需加载且设置能力没有减少 | 分类导航、当前分类内容、后端保存状态、连接检查和关闭操作保留；真实本地后端设置流程通过，模型地址、模型名称和密钥均写入本地加密存储。外壳形成独立 3,196 字节文件，原 Dashboard 源码范围未修改 |
| P2-50 | 已验证 | 完整会议页面仅在进入时加载且会议能力没有减少 | 返回、结束会议、计时、发言意图、意图队列、会议记录、语音输入、发送和本地服务设置入口全部移入 `src/meeting/AdvancedMeetingRoom.jsx`；发送、AI 回复和意图运行逻辑继续复用。真实立项会议、创建项目、群聊、时间线、刷新恢复、经理演示、发布检查和 389 项测试通过；Dashboard 的会议逻辑没有重写 |
| P2-51 | 已验证 | 完整 Group Chat 仅在进入时加载且协作操作没有减少 | 频道切换和创建、返回、频道置顶、消息检索、成员面板、证明定位、回复、提及、消息置顶、附件、输入和发送界面原样移入 `src/project/AdvancedProjectChat.jsx`；状态计算、发送和后端同步逻辑继续复用。完整控制台经理演示、真实项目流程、发布检查和 389 项测试通过；Dashboard、节点流图和意图逻辑没有重写 |
| P2-52 | 已验证 | 完整节点流时间线仅在进入时加载且节点能力没有减少 | 返回、数据来源、缩放、图例、画布拖动、节点、关系线、节点详情、附件、群聊证明、提交记录、证明路径和节点确认界面原样移入 `src/project/AdvancedProjectTimeline.jsx`；节点计算、后端同步、证明定位和确认逻辑继续复用。经理演示、发布检查和 389 项测试通过；节点流和意图逻辑没有重写 |
| P2-53 | 已验证 | 完整工作区仅在主动进入时加载且入口没有减少 | 项目统计、项目列表、创建项目、打开项目、配置模型、启动立项、启动检查、同步、设置和示例项目入口原样移入 `src/workspace/AdvancedWorkspaceView.jsx`；状态和操作逻辑继续复用。独立操作保护测试、构建、真实从零到最终交付流程和 389 项测试通过 |
| P2-54 | 已验证 | 完整 Dashboard 标题仅在进入完整控制台时加载且操作没有减少 | 项目名称、状态、数据来源、进度来源、示例数据提示、召开项目会议、打开项目群聊、查看完整时间线和返回简洁视图原样移入 `src/project/ProjectDashboardHeader.jsx`；项目状态和所有操作逻辑继续由 `App.jsx` 提供。专项保护测试、构建预算、经理演示、真实项目流程、完整管理场景、从零到最终交付流程和 389 项测试通过 |
| P2-55 | 已验证 | 完整 Dashboard 统计和下一步建议仅在进入完整控制台时加载 | 八项项目统计、每项数据来源、下一步建议、来源说明、后端同步按钮、下一步决议和 Agent 接收数量原样移入 `src/project/ProjectDashboardSummary.jsx`；统计计算、建议计算和后端同步继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、经理演示、真实项目流程和 389 项测试通过 |
| P2-56 | 已验证 | 完整 Dashboard 启动来源和 Agent 工作状态仅在进入完整控制台时加载 | 启动生成来源、每名 Agent 的当前工作、技能、固定任务、下一次运行、任务数、证明数、节点流图入口、同步按钮和单 Agent 运行按钮原样移入 `src/project/ProjectDashboardAgentOverview.jsx`；来源数据与操作函数继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、经理演示、真实项目流程、完整管理场景和 389 项测试通过；节点流图、Group Chat、会议和意图逻辑未重写 |
| P2-57 | 已验证 | Manager Action Playbook 仅在进入完整 Dashboard 时加载 | 来源状态、完成/就绪/阻塞/下一步统计、后台缺失提示、同步队列、请求内容、运行接口、运行、再次运行和打开证明全部原样移入 `src/project/ProjectDashboardManagerActionPlaybook.jsx`；队列同步、步骤执行和证明打开函数继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、真实项目流程、完整管理场景和 392 项测试通过 |
| P2-58 | 已验证 | Manager Action Run Ledger 仅在进入完整 Dashboard 时加载 | 管理运行记录、结果消息、任务、提交、产物、证据、复核、调度结果以及聊天、时间线和运行证明入口原样移入 `src/project/ProjectDashboardManagerActionRunLedger.jsx`；执行和证明打开逻辑继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、完整管理界面真实流程、管理场景和 393 项测试通过 |
| P2-59 | 已验证 | Manager Scenario Trail 仅在进入完整 Dashboard 时加载 | 场景完成数量、后端缺失提示、同步操作、每一步状态和 Trail 证明入口原样移入 `src/project/ProjectDashboardManagerScenarioTrail.jsx`；场景数据、同步和聊天/时间线证明跳转继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、真实点击证明并返回 Dashboard、完整管理场景和 393 项测试通过 |
| P2-60 | 已验证 | Manager Scenario Walkthrough 仅在进入完整 Dashboard 时加载 | 完成数量、下一缺口、可重跑步骤、运行回执、后端缺失提示、同步、逐步运行、结果证明和步骤证明入口原样移入 `src/project/ProjectDashboardManagerScenarioWalkthrough.jsx`；场景数据、同步、执行和聊天/时间线证明跳转继续由 `App.jsx` 提供。专项保护测试、发布检查、构建预算、真实 Walkthrough 运行、完整管理场景和 399 项测试通过 |
| P2-61 | 已验证 | Sync Protocol Audit 仅在进入完整 Dashboard 时加载 | 发布、送达、Agent 状态、Timeline、Ledger、聊天证明、时间线证明和同步操作原样移入 `src/project/ProjectDashboardSyncProtocolAudit.jsx`；同步与证明跳转仍由 `App.jsx` 提供。专项保护测试、源边界检查、发布清单、构建和 399 项测试通过 |
| P2-62 | 已验证 | 用户主动同步 intent 队列不会在本地高负载时过早终止 | 手动同步等待由 5 秒提高到 60 秒，静默后台同步仍保持短等待；隔离后端确认 `collaboration-intent-queue/v1` 正常返回，专项测试、完整管理浏览器整链和全量测试通过，intent 模块未删除 |
| P2-63 | 已验证 | 启动本地调度器会先确认，再执行立即工作扫描 | 启动确认与项目/Agent 扫描分开，扫描延后 25 毫秒进入执行；启动请求在繁忙后端下允许等待 30 秒，停止仍为 5 秒。调度器单元测试、HTTP 即时启动、管理场景和全量测试通过 |
| P2-64 | 已验证 | Hour/Day Pulse 在本地高负载时不会被旧 10 秒上限提前终止 | 用户主动 Hour/Day Pulse 与 Server Pulse 等待提高到 60 秒；完整管理浏览器整链已经实际完成 Server Pulse、Hour/Day Pulse 和后续 Agent 工作，专项测试、管理场景、构建和 426 项测试通过 |
| P1-47 | 已验证 | 用户操作完成后立即看到回执，不再等待整套后台证明刷新 | Agent Workbench 的证据、手动来源、产物和草稿写入成功后先更新真实后端回执，再读取详细证明；完整管理浏览器整链通过 |
| P2-65 | 已验证 | 用户操作优先于旧的后台读取 | 聊天、会议、Leader、Manager、Agent 动作和 Pulse 开始前统一取消尚未执行的后台详细刷新；进入或退出项目页面时也取消旧页面刷新，专项合同测试和完整浏览器整链通过 |
| P2-66 | 已验证 | 后台静默状态检查不会把已在线服务误报为离线 | 静默调度状态检查超时会保留最近确认的在线状态；连接状态拥有独立测试标识，专项测试和完整浏览器整链通过 |
| P2-67 | 已验证 | 写入完成后的详细刷新不会继续阻塞操作回执 | 四类重写入和两类 Pulse 的详细读取延后 5 秒，并按顺序执行，新的用户操作与页面切换可以取消；专项测试和完整浏览器整链通过 |
| P2-68 | 已验证 | 立项前四步按需加载且不影响原 Dashboard | 立项说明、保存位置、组建团队和会议准备仅在进入相应步骤时加载；每一步都有独立操作保护测试。真实项目立项、会议、群聊、Timeline、刷新恢复和经理演示整链通过；原 Dashboard、节点流图、Group Chat、会议与 intent 路由保护检查通过 |
| P2-69 | 已验证 | 立项结果页按需加载且创建项目操作不变 | 团队确认、首批工作编辑、增加工作、Leader 选择、会议证据和“生成项目并进入 Dashboard”操作原样移入独立组件；状态更新与后端创建逻辑继续留在原位置。真实项目创建、Dashboard、会议、群聊、Timeline、刷新恢复与经理演示整链通过 |
| P2-70 | 已验证 | Dashboard 需求证明与命令提交区域按需加载且操作不变 | 需求证明、同步 Matrix、Leader 分配、目标 Agent 选择、变更提交和投递模式原样移入独立组件；数据读取、状态更新、后端提交、场景切换和原禁用条件继续留在原位置。保护测试先失败后通过；433 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-71 | 已验证 | Dashboard 自动工作循环按需加载且 Hour/Day Pulse 不变 | 最近运行、下次运行、调度记录、管理事件、通信诊断、Agent 计划、Hour Pulse 和 Day Report 原样移入独立组件；调度状态计算、后端命令、超时和刷新逻辑继续留在原位置。保护测试先失败后通过；434 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-72 | 已验证 | Dashboard 24/7 Operations Board 按需加载且状态与同步不变 | 项目节奏、下次运行、最近运行、后端状态、Agent 队列、Leader 标记、管理优先级和 Sync Cockpit 原样移入独立组件；状态计算、读模型合并、后端同步和刷新逻辑继续留在原位置。保护测试先失败后通过；435 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-73 | 已验证 | Dashboard Continuous Work Loop 按需加载且 Agent 与证明操作不变 | 调度状态、下次项目运行、Agent 固定任务、运行 Agent Pulse、聊天证明、Timeline 证明和 Sync Cockpit 原样移入独立组件；Agent 命令、聊天定位、时间线定位、后端同步、超时和刷新逻辑继续留在原位置。保护测试先失败后通过；436 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-74 | 已验证 | Dashboard Fixed Work Routines 按需加载且每个 Agent 的任务详情与同步不变 | 固定任务名称、产物、检查清单、当前重点、下一项证据、Leader 标识和 Sync Cockpit 原样移入独立组件；任务数据计算、后端同步和禁用条件继续留在原位置。保护测试先失败后通过；437 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-75 | 已验证 | Dashboard Manager Scenario Readiness 按需加载且就绪检查与同步不变 | 就绪百分比、状态、通过数量、全部检查项和 Sync Proof Map 原样移入独立组件；就绪度计算、后端同步和禁用条件继续留在原位置。保护测试先失败后通过；438 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-76 | 已验证 | Dashboard Agent Management Mesh 按需加载且管理关系与证明操作不变 | Leader 关系、同级管理关系、最新 check-in、管理证明、Timeline 证明、Run Management Sync 和 Sync Cockpit 原样移入独立组件；关系计算、时间线定位、后端命令和禁用条件继续留在原位置。保护测试先失败后通过；439 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-77 | 已验证 | Dashboard Collaboration Health 按需加载且协作检查与同步不变 | 协作评分、健康状态、全部检查项和 Sync Proof Models 原样移入独立组件；协作诊断计算、后端读取和禁用条件继续留在原位置。保护测试先失败后通过；440 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-78 | 已验证 | Dashboard Leader Assignment Flow 按需加载且任务分配与证明操作不变 | Assignment Timeline Matrix、Assignment Work Progress Matrix、Leader Assignment 明细、Chat 证明、Timeline 证明和 Sync Cockpit 原样移入独立组件；任务与证明计算、Chat 定位、Timeline 定位、后端同步和禁用条件继续留在原位置。保护测试先失败后通过；441 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-79 | 已验证 | Dashboard Active Threads 按需加载且任务证明操作不变 | 任务状态、负责人、来源、分配证明、接收状态、工作脉冲、Chat 证明、Timeline 证明和 Sync Manager Dashboard 原样移入独立组件；任务证据计算、Chat 定位、Timeline 定位、后端同步和禁用条件继续留在原位置，右侧 Team 列未改。保护测试先失败后通过；442 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-80 | 已验证 | Dashboard Recent Commit Line 按需加载且时间线事件与同步不变 | 最近事件类型、日期、时间、标题、贡献者和 Sync Timeline 原样移入独立组件；事件读取、类型样式、后端同步和禁用条件继续留在原位置。保护测试先失败后通过；443 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-81 | 已验证 | Dashboard 项目工具入口按需加载且场景切换不变 | 工具图标、名称、说明、展开状态、键盘聚焦和进入场景操作原样移入独立组件；场景切换函数、520 毫秒切换过程、旧请求取消和状态更新继续留在原位置。保护测试先失败后通过；444 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-82 | 已验证 | Dashboard Sample Fixture Path 按需加载且演示步骤操作不变 | 步骤编号、名称、说明、按钮动作和切换期间禁用状态原样移入独立组件；显示条件、演示步骤计算和原动作函数继续留在原位置。保护测试先失败后通过；445 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-83 | 已验证 | Dashboard Transcript Proof Coverage 按需加载且同步与聊天证明入口不变 | 已归档证明数、预期证明数、缺失证明数、来源标识、就绪状态、同步按钮和聊天证明按钮原样移入独立组件；证明数据、同步动作和聊天定位函数继续留在原位置。保护测试先失败后通过；446 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-84 | 已验证 | Dashboard Transcript Channel Routes 按需加载且频道证明操作不变 | 频道就绪数、总数、最新频道、接口地址、来源标识、同步按钮、聊天证明按钮和时间线证明按钮原样移入独立组件；频道数据、同步动作、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；447 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-85 | 已验证 | Dashboard Transcript Channel Pin 与 Message Pin Routes 按需加载且固定操作证明不变 | 频道固定和消息固定的就绪数、总数、最新目标、接口地址、来源标识、同步按钮、聊天证明按钮和时间线证明按钮原样移入两个独立组件；摘要数据、同步动作、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；448 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-86 | 已验证 | Dashboard Transcript Reply 与 Mention Routes 按需加载且回复与提及证明不变 | 回复和提及的就绪数、总数、最新消息、接口地址、来源标识、同步按钮、聊天证明按钮和时间线证明按钮原样移入两个独立组件；摘要数据、同步动作、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；449 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-87 | 已验证 | Dashboard Transcript Attachment 与 Member Presence Routes 按需加载且附件与成员状态证明不变 | 附件和成员在线状态的就绪数、总数、最新消息或成员数量、接口地址、来源标识、同步按钮、聊天证明按钮和时间线证明按钮原样移入两个独立组件；摘要数据、同步动作、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；450 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-88 | 已验证 | Dashboard Agent Message 与 Agent Contract Routes 按需加载且消息与合同证明不变 | Agent 消息和合同的就绪数、总数、送达数、工作记录数、启用合同数、接口地址、来源标识、同步按钮、聊天或 Dashboard 证明按钮和时间线证明按钮原样移入两个独立组件；摘要数据、Agent 选择、后端同步、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；451 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-89 | 已验证 | Dashboard Collaboration Intent Queue 与 Submission Review Workflow 按需加载且意图和评审流程不变 | 可运行意图、意图行、Agent 主动事项、提交、复核、修订和最终接受状态，以及来源标识、同步按钮、聊天证明按钮和时间线证明按钮原样移入两个独立组件；摘要数据、状态计算、后端同步、聊天定位和时间线定位函数继续留在原位置。保护测试先失败后通过；452 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-90 | 已验证 | Dashboard Runtime Contracts 与 Autonomous Cycle Consistency 按需加载且运行证明不变 | 合同冻结、失败合同、Schema、产物覆盖、证明、时间线、事件、生产阻断，以及自主循环步骤、运行收据、失败行和死信状态原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；453 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-91 | 已验证 | Dashboard Planner / Executor / Reviewer 与 Team Collaboration Diagnostics 按需加载且职责与协作证明不变 | 角色职责、状态转换、执行 Agent、复核、修订、最终接受、协作评分、就绪行、交接问题、主动事项、聊天记录和证明路由原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；454 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-92 | 已验证 | Dashboard Runtime Autonomy Status 与 Zero-to-autonomy Report 按需加载且自主运行证明不变 | 本地自主状态、生产阻断、任务运行、会话、队列、死信、恢复队列、持久化、证明按钮，以及阶段、产物、供应商、资料决策、证明、时间线、事件和归档泄漏计数原样移入两个独立组件；聊天、时间线、节点流操作和后端路由继续使用原数据与原函数。保护测试先失败后通过；455 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-93 | 已验证 | Dashboard Product Team Operating Loop 与 Product Team Delivery Trace 按需加载且团队运行与交付链路不变 | 交接执行、运行收据、提交产出、Agent 主动事项、目标、运行门槛、交付阶段、头脑风暴、资料搜索、草稿、复核、修订、最终接受和证明统计原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；456 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-94 | 已验证 | Dashboard Project Evidence Archive 与 Brainstorm Layer 按需加载且证明与头脑风暴信息不变 | 归档清单、提交、最终产物、资料搜索、存储证明、工作区文件、来源决策、复核、聊天记录、泄漏检查，以及头脑风暴看板、备选方案、参与者、下游产物、门槛、证明和前三条记录原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；457 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-95 | 已验证 | Dashboard Artifact Quality Audit 与 Evidence Quality Audit 按需加载且质量证明不变 | 产物类型覆盖、质量分数、证明、复核、修订、草稿、失败门槛、生产控制、缺失类型，以及资料来源、强证据、可用证据、来源安全、地址完整度和决策门槛原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；458 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-96 | 已验证 | Dashboard Evidence Index Readiness 与 Evidence Source Review Workflow 按需加载且索引与来源复核证明不变 | 索引行、资料搜索、提交、来源快照、供应商收据、存储证明、地址完整度、失败门槛、生产状态，以及来源复核、待决策项、批准、待处理、自动放行、阻断来源、来源安全和前三条复核记录原样移入两个独立组件；来源标识、同步按钮和后端路由继续使用原数据与原函数。保护测试先失败后通过；459 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-97 | 已验证 | Dashboard Project Evidence Export Workflow 按需加载且四类导出操作不变 | 请求包、经理批准、安全批准和下载审计四个按钮及其禁用条件、请求参数、角色、来源标识、私有试用与生产导出状态、统计和后端路由原样保留；业务回调继续在 `App.jsx` 使用原函数和原请求体，界面移入独立组件。保护测试先失败后通过；460 项自动测试、产品构建、原 Dashboard 功能保护、真实项目主流程和经理演示整链通过 |
| P2-98 | 已验证 | Dashboard Unified Event Ledger 按需加载且事件证明与同步不变 | 保留事件来源、后端缺失提示、同步时间线按钮、保留数量、总数、序号范围、五类统计和最近事件明细；业务数据、后端同步函数和禁用条件继续留在原位置。保护测试先失败后通过；468 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-99 | 已验证 | Dashboard Governance and Speech Protocol 按需加载且治理同步与发言规则不变 | 保留负责人、复核人、负责人发言规则、成员发言规则、后端缺失提示、数据来源和同步治理按钮；后端读取、同步函数和禁用条件继续留在原位置。保护测试先失败后通过；470 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-100 | 已验证 | Dashboard Kickoff Charter 按需加载且章程证明与行动不变 | 保留章程状态、负责人、复核人、候选人数、聊天证明入口、三条后续行动和沟通规则；聊天定位函数和证明编号继续留在原位置。保护测试先失败后通过；470 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-101 | 已验证 | Dashboard Kickoff Meeting Flow 按需加载且完整立项会议证明不变 | 保留六类会议摘要、生成来源、项目说明确认、团队确认、负责人确认、角色问答、听取矩阵、对话记录，以及全部聊天和时间线证明按钮；业务数据、翻译、证明编号提取和定位函数继续留在原位置。保护测试先失败后通过；471 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-102 | 已验证 | Dashboard Kickoff Execution Flow 按需加载且启动、分配与首次工作证明不变 | 保留后端数据缺失提示、Manager Dashboard 同步、四类执行摘要、下一行动确认、Agent 收据、全部 Agent 启动矩阵、启动时间线、任务分配聊天与时间线证明、首次工作轮次聊天与时间线证明；后端同步和证明定位函数继续留在原位置。保护测试先失败后通过；472 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-103 | 已验证 | Dashboard Group Chat Transcript Index 按需加载且频道与协作证明不变 | 保留频道来源、消息总数、可恢复证明数、后端缺失提示、同步聊天记录、频道打开、消息数、归档证明、最新发言人、回执覆盖、直接提及、协作证明后端提示、Manager Dashboard 同步、Chat proof 和 Timeline proof；频道切换状态更新与证明定位函数继续留在原位置。保护测试先失败后通过；473 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P2-104 | 已验证 | Dashboard Change Flow 按需加载且变更受理、解决与台账不变 | 保留 Dual-channel Change Intake Matrix、Change Resolution Matrix、Change Ledger、后端缺失提示、Cockpit 同步、来源消息与回执、团队讨论、负责人确认、计划更新、团队同步、负责人首次工作，以及来源、解决、聊天和时间线证明按钮；证明编号计算、频道选择、后端同步与证明定位继续留在原位置。保护测试先失败后通过；474 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 13,333 字节 |
| P2-105 | 已验证 | Dashboard Peer Handoffs 与 Agent Communication Flow 按需加载且交接与消息证明不变 | 保留 Peer Handoffs、请求与确认编号、来源频道、Peer chat proof、Peer timeline proof、Agent Message Delivery Matrix、Direct Receipt、Target Inbox、Obligation、Sender Worklog、Delivery chat proof 和 Agent chat proof；聊天证明、时间线证明和频道名称继续在 `App.jsx` 使用原计算与原定位函数。保护测试先失败后通过；475 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 7,426 字节 |
| P2-106 | 已验证 | Dashboard Manager Proof Map 按需加载且全部证明入口不变 | 保留设置、核心运行、管理台、治理、输出、产品团队验收、Zero-to-Autonomy Report、Product Team Delivery Trace 和经理就绪检查卡片，以及 Open Settings、各类同步、聊天证明、时间线证明和 Sync Proof Map 操作；证明编号计算、同步请求、设置打开、聊天定位和时间线定位函数继续留在 `App.jsx`。现有 Transcript、Agent、协作意图和提交评审子组件仍按原方式接入。保护测试先失败后通过；476 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 26,161 字节 |
| P2-107 | 已验证 | Dashboard Team 展示区按需加载且 Agent 管理与工作操作不变 | 保留 Agent 状态、优先级、收件箱、义务、工作记录、Agent Workspace、本地后端 Dashboard、控制运行收据、头脑风暴、提交、资料搜索、评审、Agent Workbench、Evidence、Submit、Artifact Draft、管理证明、任务证明、Agent Message 和 Agent Pulse；显示专用状态整理随原界面整体移动，项目数据源、草稿更新、后端请求、聊天定位和时间线定位函数继续由 `App.jsx` 提供。保护测试先失败后通过；477 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 43,369 字节 |
| P2-108 | 已验证 | Dashboard Scenario Control Center 与 Manager Live Command Center 按需加载且控制步骤、同步、运行和证明操作不变 | 保留场景控制步骤、步骤状态、动作入口、Next best action、Run next、Command run proof、Sync Command、启动、工作循环、协作、变更协议、注意事项和变更同步看板，以及聊天与时间线证明按钮；项目数据、后端同步、运行请求、证明编号计算和定位函数继续由 `App.jsx` 提供。保护测试先失败后通过；478 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 30,123 字节；主页面构建文件为 1,047,369 字节，`App.jsx` 为 30,380 行 |
| P2-109 | 已验证 | Dashboard Manager Use Case Audit 按需加载且覆盖检查、同步、运行与证明操作不变 | 保留覆盖计数、后端数据缺失提示、Sync Audit、每条用例的阶段、管理问题、需求覆盖、下一行动、运行路由、Run use case action 和 Use case proof；项目数据、后端同步、运行请求和证明定位函数继续由 `App.jsx` 提供。保护测试先失败后通过；479 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 5,079 字节；主页面构建文件为 1,043,323 字节，`App.jsx` 为 30,319 行 |
| P2-110 | 已验证 | Backend Worker Station 内 Private Pilot Go-Live Readiness 只读快照按需加载且状态、阶段、证明统计与路由不变 | Backend Worker Station 整体 4,921 行，没有整体移动；仅原样移动 52 行只读快照。保留私有试运行状态、来源、阶段、验收阶段、失败阶段、下一行动、负责人、证明、事件、最新启动、最新健康检查、验收报告、校验值、阶段行和本地私有试运行路由。保护测试先失败后通过；480 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 3,718 字节；主页面构建文件为 1,040,325 字节，`App.jsx` 为 30,279 行 |
| P2-111 | 已验证 | Backend Worker Station 内 Pilot Launch Readiness 只读快照按需加载且私有试运行决策、门禁、阻断项与路由不变 | 仅原样移动 43 行只读快照，发布审批按钮、后端命令和数据逻辑没有移动。保留私有试运行决策、生产发布决策、门禁通过数、失败门禁、证明路由、生产阻断项、校验值、下一缺口、阻断行和本地私有试运行发布路由。保护测试先失败后通过；481 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 2,882 字节；主页面构建文件为 1,038,064 字节，`App.jsx` 为 30,246 行 |
| P2-112 | 已验证 | Backend Worker Station 内 MVP Readiness 按需加载且本地试运行状态、操作、回执、阻断项与路由不变 | 原样移动显示区和 Record 按钮；运行函数与后端状态继续由 `App.jsx` 提供。保留本地试运行状态、核心通过数、核心阻断项、生产阻断项、下一行动、操作范围、负责人、请求方法、运行路由、Record 禁用条件、失败回执不生成本地记录的说明、成功回执、阻断行和本地 MVP 路由。保护测试先失败后通过；482 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 4,625 字节；主页面构建文件为 1,034,251 字节，`App.jsx` 为 30,185 行 |
| P2-113 | 已验证 | Backend Worker Station 内 Operations Readiness 只读状态区按需加载且存储、队列、恢复、告警与路由不变 | 仅原样移动只读显示区，数据库、队列、恢复和切换操作没有移动。保留本地运行状态、数据存储适配计划与演练、影子读取、回滚、备份恢复、队列适配计划与演练、快照一致性、租约一致性、工作运行、回执、重试、失败队列、恢复、事故演练、演练回执、告警和本地运行就绪路由。保护测试先失败后通过；483 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 6,994 字节；主页面构建文件为 1,028,026 字节，`App.jsx` 为 30,129 行 |
| P2-114 | 已验证 | Backend Worker Station 内 Provider Readiness 只读状态区按需加载且本地供应商、费用、失败控制、密钥仓库与安全状态不变 | 仅原样移动只读显示区，不读取或显示 API key 明文，也没有移动模型调用逻辑。保留供应商搜索、证据来源、供应商回执、本地控制、使用记录、每日费用、模型草稿质量、人工复核、失败控制、断路状态、重试、加密密钥仓库记录、密钥轮换、来源安全、阻断来源、搜索开关、响应泄漏检查、供应商缺口和本地供应商就绪路由。保护测试先失败后通过；484 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 4,579 字节；主页面构建文件为 1,024,039 字节，`App.jsx` 为 30,076 行 |
| P2-115 | 已验证 | Backend Worker Station 内 Provider Controlled Run 只读状态区按需加载且预算、证明、治理、脱敏、运行计划与路由不变 | 仅原样移动只读显示区，模型运行计划来源、就绪判断和下一段 Provider Eval Runs 操作区继续留在 `App.jsx`，没有移动供应商调用或回执写入逻辑。保留可运行操作数、阻断操作数、门禁、预计费用、每日与每小时余量、模型证明、搜索证明、人工复核、证据治理、脱敏状态、校验值、失败门禁或运行计划行以及本地受控运行路由。保护测试先失败后通过；485 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 3,641 字节；主页面构建文件为 1,021,052 字节，`App.jsx` 为 30,035 行 |
| P2-116 | 已验证 | Backend Worker Station 内 Provider Eval Runs 按需加载且 Record Eval 回执命令、禁用政策、统计、运行行与路由不变 | 原样移动显示区和按钮；真实 `runBackendPrivatePilotReceipt` 调用、回执路由、原因、工作流编号、回执编号、shadow-replay 模式、执行角色、执行编号、来源标记以及四项禁用条件继续留在 `App.jsx`，通过参数传给显示组件。保留评估状态、运行通过数、关键操作回放、操作回放、门禁、证明数、事件数、最新运行、校验值、失败门禁或操作行和本地评估路由。保护测试先失败后通过；486 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 3,921 字节；主页面构建文件为 1,017,907 字节，`App.jsx` 为 29,991 行 |
| P2-117 | 已验证 | Backend Worker Station 内 Evidence Custody Readiness 按需加载且来源、证明同步、保管统计、门禁与路由不变 | 仅原样移动显示区；来源标签的计算和 `managerProofModelSyncButton` 继续留在 `App.jsx`，通过参数传给显示组件。保留本地保管状态、生产状态、门禁、保管记录、来源快照、供应商回执、来源决定、持久化记录、托管存储、生产控制、失败门禁或生产控制行以及保管路由。保护测试先失败后通过；488 项自动测试、产品构建、原 Dashboard 功能保护和连续两轮经理演示整链通过，失败请求为 0。独立按需文件为 3,286 字节；主页面构建文件为 1,015,353 字节，`App.jsx` 为 29,958 行 |
| P2-118 | 已验证 | Backend Worker Station 内 Security Boundary 只读状态区按需加载且访问、审计、身份、密钥仓库、脱敏与路由不变 | 仅原样移动只读显示区，安全边界数据及其读取逻辑继续由 `App.jsx` 提供。保留本地安全状态、生产状态、路由数、敏感数据集、访问政策、审计记录、审计流、审计链、活跃身份会话、会话记录、加密密钥仓库、密钥记录、轮换状态、拒绝记录、明文泄漏、安全阻断项、安全边界路由与身份会话路由。保护测试先失败后通过；489 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 3,022 字节；主页面构建文件为 1,012,943 字节，`App.jsx` 为 29,928 行 |
| P2-119 | 已验证 | Backend Manager Snapshot 顶部统计摘要按需加载且来源、全部统计项与 Backend route 不变 | Backend Manager Snapshot 外层容器继续留在 `App.jsx`；仅原样移动顶部来源、40 项统计卡和 Backend route。Mission Runner、A-side handoff、协作意图、聊天证明、时间线证明、提交、复核和其他后续操作全部留在原位置。保护测试先失败后通过；490 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0。独立按需文件为 5,444 字节；主页面构建文件为 1,008,570 字节，`App.jsx` 为 29,891 行 |
| P2-120 | 已验证 | Product Team Mission Runner 按需加载且六个原操作、状态、回执和证明入口不变 | 仅把原任务运行区显示移动到独立组件；运行 C/A 交接、交接群聊证明、交接时间线证明、任务群聊证明、任务时间线证明和节点流图六个动作及全部禁用规则继续由 `App.jsx` 控制。保护测试先失败后通过；492 项自动测试、产品构建、原 Dashboard 功能保护和真实经理演示整链通过，失败请求为 0。独立按需文件为 9,881 字节；主页面构建文件为 1,000,318 字节，`App.jsx` 为 29,750 行 |
| P2-121 | 已验证 | Manager Ready Package 尚未生成时的兼容协作意图队列按需加载且运行、聊天证明和时间线证明入口不变 | 仅把原兼容队列显示移动到独立组件，完整 Collaboration Intent Queue 保留；运行意图、打开输出聊天证明、打开输出时间线证明和全部禁用规则继续由 `App.jsx` 控制。保护测试先失败后通过；499 项自动测试、产品构建、原 Dashboard 功能保护、真实 Mission Runner 浏览器流程和经理演示整链通过。独立按需文件为 11,717 字节；主页面构建文件为 989,811 字节，`App.jsx` 为 29,564 行 |
| P2-122 | 已验证 | Manager Dashboard 内兼容对话证明覆盖区按需加载且统计、状态、缺失证明提示和 Group Chat 证明入口不变 | 仅把原显示区移动到独立组件；完整 Proof Map 中原 Transcript Proof Coverage 保留，打开 Group Chat 证明的回调和按钮禁用规则继续由 `App.jsx` 控制。保护测试先失败后通过；500 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过。独立按需文件为 2,600 字节；主页面构建文件为 987,856 字节，`App.jsx` 为 29,520 行 |
| P2-123 | 已验证 | Manager Dashboard 提交、证据搜索和复核工作区按需加载且完整操作不变 | 原样保留四条统计路由、提交列表、草稿标记、提交聊天与时间线证明、复核人、复核结论、评论、修改要求、复核按钮、成功与失败回执、证据搜索和复核历史。复核写入、证明跳转和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；501 项自动测试、产品构建、1088 秒真实提交复核完整流程、原 Dashboard 功能保护和经理演示基线通过。独立按需文件为 11,253 字节；主页面构建文件为 977,844 字节，`App.jsx` 为 29,338 行 |
| P2-124 | 已验证 | Manager Dashboard 兼容管理状态路由与六个只读状态面板按需加载且原显示位置不变 | 七条兼容状态路由仍位于原 Manager Dashboard 容器内；Command Center、Scenario Walkthrough、Standalone Trail、Requirement Matrix、Sync Protocol Audit 和 Manager Use Case Audit 六个兼容状态面板仍位于原完整管理组件之前。完整 Command Centers、Scenario Walkthrough、Scenario Trail、Sync Protocol Audit 和 Manager Use Case Audit 组件没有删除或合并。保护测试先失败后通过；503 项自动测试、产品构建、原 Dashboard 功能保护和修复后的真实经理演示通过。两个独立按需文件分别为 3,016 与 9,129 字节；主页面构建文件为 967,724 字节，`App.jsx` 为 29,206 行 |
| P2-125 | 已验证 | Manager Action Queue 兼容只读摘要按需加载且完整行动组件不变 | 原 Complete、Ready、Blocked、Unresolved、Next Action、下一接口和请求内容按原位置移动到独立组件。完整 Manager Action Playbook 与 Manager Action Run Ledger 没有删除、合并或替换，所有运行与证明动作仍使用原函数。保护测试先失败后通过；504 项自动测试、产品构建、原 Dashboard 功能保护和 183.2 秒真实经理演示通过。独立按需文件为 1,787 字节；主页面构建文件为 966,649 字节，`App.jsx` 为 29,184 行 |
| P2-126 | 已验证 | Latest Backend Work 只读结果区按需加载且调度控制不变 | 原触发来源、Projects、Agents、Autopilot 和 Worker Messages 四项结果按原位置移动到独立组件。后面的错误提示、Check、Start、Immediate Start 和 Stop 控制仍留在原 Dashboard，函数和禁用规则没有移动。保护测试先失败后通过；505 项自动测试、产品构建、原 Dashboard 功能保护和 203.6 秒真实经理演示通过。独立按需文件为 2,847 字节；主页面构建文件为 964,515 字节，`App.jsx` 为 29,164 行 |
| P2-127 | 已验证 | 后端调度控制显示区按需加载且全部操作不变 | Check、Start、Stop、Sync State、Seed Sample/Dev、Sync Projects、Sync Manager View、Sync Package、Sync Proof Models、Sync Command、Sync Walkthrough、Sync Trail、Sync Matrix、Sync Protocol、Sync Audit、Sync Cockpit、Sync Queue、Sync Agent Queue、Sync Intent Queue、Sync Timeline 和 Server Pulse 共 21 个按钮，以及 Immediate Start 状态，按原顺序和原文字移动到独立组件。操作函数、参数、禁用规则和状态计算继续由 `App.jsx` 提供。保护测试先失败后通过；506 项自动测试、产品构建、原 Dashboard 功能保护和 146.5 秒真实经理演示通过。独立按需文件为 8,306 字节；主页面构建文件为 957,816 字节，`App.jsx` 为 29,014 行 |
| P2-128 | 已验证 | Agent Autonomous Queue 显示区按需加载且五个 Agent 行、运行操作和证明入口不变 | 仅移动队列来源、Ready、Due、Monitoring、Next、五个 Agent 行、优先级、说明和 Run Agent Action 按钮的显示代码；运行函数、聊天与时间线证明跳转、输出回执和禁用规则继续由 `App.jsx` 提供。保护测试、原 Dashboard 功能保护、508 项全量测试、产品构建、136.5 秒真实经理核心流程和 111.8 秒真实 Mission Runner 流程通过。独立按需文件为 11,591 字节；主页面构建文件为 947,861 字节，`App.jsx` 为 28,858 行 |
| P2-129 | 已验证 | Autonomous Run Control 显示区按需加载且会话、循环、行动和证明操作不变 | Run Loop、Start Session、Scheduler Tick、Direct Tick、Pause、Cancel、下一行动 Run、输出聊天证明和输出时间线证明全部保留原函数、参数、禁用条件、顺序和显示位置。运行回执、循环回执、会话回执、调度回执、Worker 回执、Provider 证据、四项统计、三项下一行动和四项门禁原样保留。保护测试先失败后通过；509 项全量测试、产品构建、原 Dashboard 功能保护和 113.4 秒真实经理核心流程通过。独立按需文件为 17,128 字节；主页面构建文件为 931,454 字节，`App.jsx` 为 28,580 行 |
| P2-130 | 已验证 | Deployment Preflight 只读显示区按需加载且发布前状态与路由不变 | 15 项统计、最多 3 条失败门禁、Deployment Preflight 路由与 Adapter Gateway Preflight 路由按原顺序和原位置移动；该区域没有写入操作。保护测试先失败后通过；510 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查和本地发布清单通过。独立按需文件为 4,390 字节；主页面构建文件为 927,508 字节，`App.jsx` 为 28,536 行 |
| P2-131 | 已验证 | Production Infrastructure Rehearsal 按需加载且托管切换证明操作不变 | 7 项基础设施统计、最多 4 条托管切换门禁、演练路由、请求托管切换证明按钮、证明路由和运行回执按原顺序与原位置移动。运行函数和禁用条件继续由 `App.jsx` 提供。保护测试先失败后通过；511 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 142.4 秒真实经理核心流程通过。独立按需文件为 4,816 字节；主页面构建文件为 923,440 字节，`App.jsx` 为 28,486 行 |
| P2-132 | 已验证 | Manager Ready Package 完整基础设施演练区按需加载且全部状态与证明操作不变 | 11 项统计、最多 6 条托管切换门禁、最多 6 条领域状态、来源标识、演练路由、请求托管切换证明按钮、证明路由和运行回执按原顺序与原位置移动。运行函数、来源计算和禁用条件继续由 `App.jsx` 提供。保护测试先失败后通过；512 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 140.1 秒真实经理核心流程通过。独立按需文件为 6,474 字节；主页面构建文件为 917,794 字节，`App.jsx` 为 28,414 行 |
| P2-133 | 已验证 | Public Production Startup 顶部摘要按需加载且来源、公开上线状态和 15 项统计不变 | 只移动顶部标题、状态、来源标识、公开上线标识和 15 项统计；身份、成本、数据、流量、客户验收、密钥、基础设施与行动计划等详细检查保持原位置。保护测试先失败后通过；513 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和复跑后的 137.7 秒真实经理核心流程通过。独立按需文件为 2,595 字节；主页面构建文件为 915,775 字节，`App.jsx` 为 28,386 行 |
| P2-134 | 已验证 | Launch Operations Overview 按需加载且状态、下一行动、Record 操作、回执、路由与阻断项不变 | 仅移动该显示区；运行函数、禁用规则、路由来源与 Manager Ready Package 外层继续留在 `App.jsx`。保护测试先失败后通过；515 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 91.9 秒真实经理核心流程通过。独立按需文件为 6,594 字节；主页面构建文件为 909,990 字节，`App.jsx` 为 28,332 行 |
| P2-135 | 已验证 | Public Production Startup Readiness 完整详情按需加载且全部生产领域与路由不变 | 原样移动公开生产启动外层详情；摘要继续通过原独立组件加载，身份、成本、数据、流量、客户验收、密钥、基础设施、运维、失败门禁、环境配置、行动计划、验证命令、必需环境变量和路由全部保留。保护测试先失败后通过；516 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 147.1 秒真实经理核心流程通过。独立按需文件为 24,622 字节；主页面构建文件为 885,820 字节，`App.jsx` 为 27,939 行 |
| P2-136 | 已验证 | 四个 Production Launch 证明面板按需加载且状态、统计、来源、同步操作与路由不变 | Gap Register、Control Center、Evidence Dossier 与 Integrity Audit 原样移动；证明模型同步按钮继续调用 `App.jsx` 中原函数，路由继续由 Manager Ready Package 提供。首次真实流程发现旧构建问题，重新构建后同一流程通过；517 项全量测试、产品构建、原 Dashboard 功能保护、前端边界检查、本地发布清单和 160.1 秒真实经理核心流程通过。独立按需文件为 13,926 字节；主页面构建文件为 872,678 字节，`App.jsx` 为 27,738 行 |
| P2-137 | 已验证 | 四个 Private Pilot 工作流面板按需加载且状态、写入操作、回执与路由不变 | Release Candidate、Launch Run、Launch Health Check 与 Acceptance Report 原样移动；Record 操作继续调用 `App.jsx` 中原函数，工作流键、回执键、来源、禁用规则和路由全部保留。首次真实流程发现缺少图标导入并修复；保留请求路径和耗时诊断后，相同真实经理核心流程连续两次通过，自动执行回执分别在 4.3 秒与 14.3 秒出现。518 项全量测试、产品构建、原 Dashboard 功能保护、完整经理界面检查、前端边界检查、本地发布清单和代码差异检查通过。独立按需文件为 16,539 字节；主页面构建文件为 856,518 字节，`App.jsx` 为 27,482 行 |
| P2-138 | 已验证 | Production Operations Readiness 按需加载且状态、统计、失败门禁与路由不变 | 只移动只读显示区，没有增加、删除或改写操作。519 项自动测试、产品构建、原 Dashboard 功能保护和 378.8 秒产品团队完整验收通过。独立按需文件为 4,399 字节；主页面构建文件为 852,469 字节，`App.jsx` 为 27,440 行 |
| P2-139 | 已验证 | Production Operations Control Receipts 按需加载且原操作不变 | 状态、8 项统计、未验证控制、Record Rehearsal 按钮、本地演练身份、工作流键、回执键、禁用规则和本地路由全部保留。后续 P1-65 的完整 Production Controls 页面验收已覆盖此按钮并通过 |
| P2-140 | 已验证 | Production Deployment Control Receipts 按需加载且原操作不变 | 只移动原显示区；Record Rehearsal 继续调用 `App.jsx` 中原函数，身份、工作流键、回执键、禁用规则、8 项统计、未验证控制和本地路由全部保留。专项检查先失败后通过；524 项自动测试、产品构建、原 Dashboard 功能保护、Agent Manager 场景、前端边界、本地发布清单和 192.4 秒完整 Production Controls 页面验收通过。独立按需文件为 4,043 字节；主页面构建文件为 845,986 字节，`App.jsx` 为 26,789 行 |
| P2-141 | 已验证 | Production Security Control Receipts 按需加载且原操作不变 | 只移动原显示区；Record Rehearsal 继续调用 `App.jsx` 中原函数，身份、工作流键、回执键、禁用规则、8 项统计、未验证控制和本地路由全部保留。专项检查先失败后通过；525 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和 152.6 秒完整 Production Controls 页面验收通过。独立按需文件为 3,999 字节；主页面构建文件为 842,306 字节，`App.jsx` 为 26,734 行 |
| P2-142 | 已验证 | Production Provider Control Receipts 按需加载且原操作不变 | 只移动原显示区；Record Rehearsal 继续调用 `App.jsx` 中原函数，Provider Eval 前置条件、身份、工作流键、回执键、禁用规则、8 项统计、未验证控制和本地路由全部保留。专项检查先失败后通过；526 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和 159.5 秒完整 Production Controls 页面验收通过。独立按需文件为 4,003 字节；主页面构建文件为 838,620 字节，`App.jsx` 为 26,674 行 |
| P2-143 | 已验证 | Production Launch Audit 按需加载且所有只读状态不变 | 只移动原显示区；私有试运行与生产决策、13 项统计、失败门禁或生产阻断项、关联地址和审计路由全部保留，没有移动写入逻辑。专项检查先失败后通过；527 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和 159.5 秒完整 Production Controls 页面验收通过。独立按需文件为 3,722 字节；主页面构建文件为 835,234 字节，`App.jsx` 为 26,633 行 |
| P2-144 | 已验证 | Launch Approval Workflow 按需加载且两个原批准操作不变 | 只移动原显示区；状态、来源标识、私有试运行就绪状态、4 项统计、批准记录、经理批准和安全批准按钮、批准身份、原因、来源、禁用规则和本地路由全部保留。两个按钮继续调用 `App.jsx` 中原保存函数。专项检查先失败后通过；528 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和 185.1 秒完整 Production Controls 页面验收通过。独立按需文件为 4,825 字节；主页面构建文件为 830,695 字节，`App.jsx` 为 26,553 行 |
| P2-145 | 已验证 | Submission Review Workflow 完整面板按需加载且证明入口不变 | Manager Ready Package 完整面板原样移动；原 Proof Map 简版组件继续保留。状态、来源标识、证明同步操作、8 项统计、复核记录、复核与提交地址、响应地址、聊天证明入口、时间线证明入口、禁用规则和本地工作流路由全部保留。专项检查先失败后通过；529 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和 1,342.5 秒 Manager Backend UI 完整验收通过。独立按需文件为 4,967 字节；主页面构建文件为 826,355 字节，`App.jsx` 为 26,485 行 |
| P2-146 | 已验证 | Collaboration Intent Queue 完整面板按需加载且原运行与证明操作不变 | Manager Ready Package 完整面板原样移动；原 Proof Map 简版组件和无 Ready Package 时的兼容组件继续保留。状态、来源标识、就绪状态、10 项统计、下一条可运行意图、队列记录、最近运行回执、聊天证明、时间线证明、Run intent、禁用规则、失败状态、自主行动决定、输出记录和本地工作流路由全部保留。专项检查先失败后通过；530 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积检查和 107.5 秒 Manager Mission Runner UI 实际运行通过。独立按需文件为 13,577 字节；主页面构建文件为 813,522 字节，`App.jsx` 为 26,241 行 |
| P2-147 | 已验证 | Backend Worker Station 本地连接与同步状态按需加载且原操作不变 | 只移动原显示区；后端状态、Online/Offline 标识、地址输入、Save URL、8 项调度统计、Immediate Start、Agent 与 Autopilot 控制摘要、项目和 Manager 各类同步时间、部署设置操作、禁用规则和最后操作状态全部保留。保存地址和打开部署设置继续调用 `App.jsx` 中原函数。专项检查先失败后通过；531 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积检查和 115.4 秒 Manager Backend Core UI 实际运行通过。独立按需文件为 9,905 字节；主页面构建文件为 804,579 字节，`App.jsx` 为 26,127 行 |
| P2-148 | 已验证 | Manager Ready Package 顶部完整统计区按需加载且全部状态不变 | 只移动原只读统计显示区；Status、Score、MVP、发布审批、发布审计、证据、提交复核、协作意图、运行契约、自动运行、供应商、运维及工作区统计的顺序、标签、数据来源与格式化结果全部保留。格式化函数仍由 `App.jsx` 提供，该区域没有写入操作。保护测试先失败后通过；532 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积检查、代码差异检查和 135.4 秒真实 Manager Backend Core UI 通过。独立按需文件为 19,005 字节；主页面构建文件为 787,707 字节，`App.jsx` 为 26,022 行 |
| P2-149 | 已验证 | Manager Ready Package 三个协作状态面板的装配代码按需加载且原面板不变 | Product Team Operating Loop、Planner / Executor / Reviewer 与 Team Collaboration Diagnostics 继续使用原独立面板；只移动三者的条件显示、来源标识、同步按钮和路由装配。后端模型、来源计算与同步函数继续由 `App.jsx` 提供。保护测试先失败后通过；533 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 138 秒真实 Manager Backend Core UI 通过。独立按需文件为 2,596 字节；主页面构建文件为 786,110 字节，`App.jsx` 为 25,983 行 |
| P2-150 | 已验证 | Manager Ready Package 五个运行状态面板的装配代码按需加载且原面板不变 | Runtime Contracts、Autonomous Cycle Consistency、Runtime Autonomy Status、Zero-to-autonomy Report 与 Product Team Delivery Trace 继续使用原独立面板；只移动五者的条件显示、来源标识、同步按钮、路由和证明操作装配。后端模型、来源计算、同步函数及聊天、Timeline、节点流证明跳转继续由 `App.jsx` 提供。保护测试先失败后通过；534 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 127.2 秒真实 Manager Backend Core UI 通过。独立按需文件为 6,446 字节；主页面构建文件为 783,102 字节，`App.jsx` 为 25,907 行 |
| P2-151 | 已验证 | Manager Ready Package 七个证据与复核面板的装配代码按需加载且原面板不变 | Project Evidence Archive、Brainstorm Layer、Artifact Quality Audit、Submission Review Workflow、Evidence Quality Audit、Evidence Index Readiness 与 Evidence Source Review Workflow 继续使用原独立面板；只移动七者的条件显示、来源标识、同步按钮、路由和证明操作装配。后端模型、来源计算、同步函数及聊天和 Timeline 证明跳转继续由 `App.jsx` 提供。保护测试先失败后通过；535 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 177.6 秒真实 Manager Backend Core UI 通过。独立按需文件为 7,675 字节；主页面构建文件为 779,308 字节，`App.jsx` 为 25,805 行 |
| P2-152 | 已验证 | Manager Ready Package 四个发布就绪显示区的装配代码按需加载且原组件不变 | Private Pilot Go-Live Readiness、Production Infrastructure Rehearsal、Public Production Startup Readiness 与 Production Launch Proof Panels 继续使用原五个独立组件；只移动条件显示、来源标识、证明同步、路由、公开生产摘要和演练回执装配。基础设施演练操作、来源计算、同步函数和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；536 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 133.7 秒真实 Manager Backend Core UI 通过。独立按需文件为 5,647 字节；主页面构建文件为 776,714 字节，`App.jsx` 为 25,756 行 |
| P2-153 | 已验证 | Manager Ready Package 八个私有试运行与生产控制组件的装配代码按需加载且原组件不变 | Private Pilot Workflow Panels、Production Operations Readiness、四组 Production Control Receipts、Production Launch Audit 与 Launch Approval Workflow 继续使用原八个独立组件；只移动条件显示、来源标识、本地路由和组件参数装配。两类保存函数、可用性判断与按钮禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；537 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 201.8 秒真实 Manager Backend Core UI 通过；真实页面明确等待 11 个面板与 5 个来源标识出现，Autopilot 回执为 11.824 秒。独立按需文件为 6,880 字节，构建文件为 4,756 字节；主页面构建文件为 773,879 字节，`App.jsx` 为 25,676 行 |
| P2-154 | 已验证 | Manager Ready Package 六个本地就绪组件的装配代码按需加载且原组件不变 | Pilot Launch Readiness、Deployment Preflight、MVP Readiness、Operations Readiness、Provider Readiness 与 Provider Controlled Run 继续使用原六个独立组件；只移动条件显示、来源标识、本地路由和组件参数装配。MVP 操作函数、来源计算和五项可用性判断继续由 `App.jsx` 提供。保护测试先失败后通过；538 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 188.2 秒真实 Manager Backend Core UI 通过；真实页面明确等待六个面板与三个来源标识出现，Autopilot 回执为 18.872 秒。独立按需文件为 5,040 字节，构建文件为 4,002 字节；主页面构建文件为 771,648 字节，`App.jsx` 为 25,622 行 |
| P2-155 | 已验证 | Manager Ready Package 三个供应商、证据和安全面板的装配代码按需加载且原组件不变 | Provider Eval Run Workflow、Evidence Custody Readiness 与 Security Boundary 继续使用原三个独立组件，Package route 保持原位置顺序；只移动条件显示、本地路由和组件参数装配。Provider Eval 保存命令与禁用条件、Evidence Custody 来源和证明同步、Security Boundary 来源计算及三项可用性判断继续由 `App.jsx` 提供。保护测试先失败后通过；539 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积、代码差异检查和 140.7 秒真实 Manager Backend Core UI 通过；真实页面明确等待三个面板与三个来源标识出现，Autopilot 回执为 5.562 秒。独立按需文件为 2,908 字节，构建文件为 2,401 字节；主页面构建文件为 770,609 字节，`App.jsx` 为 25,596 行 |
| P2-156 | 已验证 | Backend Manager Snapshot 与 Product Team Mission Runner 的装配代码按需加载且原组件不变 | 两个原组件继续各自按需加载；只移动条件显示、原加载提示、数据组合、显示顺序和组件参数装配。执行意图、聊天证明、Timeline 证明、节点定位及六项禁用判断继续由 `App.jsx` 提供。保护测试先失败后通过；540 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实 Manager Backend Core UI 126.2 秒通过并直接等待经理摘要，Autopilot 回执为 5.033 秒；专门创建 Product Team Mission 的真实 Mission Runner UI 111.5 秒通过。独立按需文件为 3,894 字节，构建文件为 2,867 字节；主页面构建文件为 769,887 字节，`App.jsx` 为 25,582 行 |
| P2-157 | 已验证 | 兼容 Collaboration Intent Queue 与 Transcript Proof Coverage 的装配代码按需加载且原组件不变 | 两个原组件继续各自按需加载；只移动原条件、加载提示、数据组合、显示顺序和组件参数装配。意图运行、聊天证明、Timeline 证明、对话证明打开及全部禁用判断继续由 `App.jsx` 提供。保护测试先失败后通过；541 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实 Manager Backend Core UI 114.5 秒通过并直接等待对话证明覆盖，Autopilot 回执为 4.061 秒。独立按需文件为 2,536 字节，构建文件为 2,068 字节；主页面构建文件为 769,371 字节，`App.jsx` 为 25,572 行 |
| P2-158 | 已验证 | Submission Workspace 与 Manager Read Model Routes 的装配代码按需加载且原组件不变 | 两个原组件继续各自按需加载；只移动原加载提示、数据组合、显示顺序和组件参数装配。复核写入、草稿更新、聊天证明、Timeline 证明和三项禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；542 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实 Manager Backend Core UI 160.2 秒通过并直接等待提交地址与管理接口地址，Autopilot 回执为 12.815 秒。独立按需文件为 2,798 字节，构建文件为 2,335 字节；主页面构建文件为 768,744 字节，`App.jsx` 为 25,563 行 |
| P2-176 | 已验证 | Dashboard 十二个 Manager Proof Map 证明子区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Transcript Proof Coverage、Transcript Channel Routes、Channel Pin Routes、Message Pin Routes、Reply Routes、Mention Routes、Attachment Routes、Member Presence Routes、Agent Message Routes、Agent Contract Routes、Collaboration Intent Queue 和 Submission Review Workflow 的共同布局、十二个条件显示、原加载状态和参数连接移动到 `src/project/ProjectDashboardManagerProofRoutePanels.jsx`；十二个原组件继续分别按需加载并保持原显示顺序。证明同步、聊天证明、Timeline 证明、Agent 详情打开和 Agent Dashboard 同步函数继续由 `App.jsx` 提供。保护测试先失败后通过；八项旧保护测试和一项兼容保护测试已更新且功能要求未降低。第一次完整 Dashboard 检查发现可选 Agent Contract 摘要为空时参数被提前创建并读取 `agentIds`；新增空数据回归测试并恢复三组可选参数的条件创建后重新通过。563 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；修复后三组真实操作合计 197.0 秒，Autopilot 回执为 13.384 秒。源文件为 6,112 字节，独立构建文件为 6,141 字节，主页面构建文件为 667,922 字节，`App.jsx` 为 22,486 个非空行 |
| P2-177 | 已验证 | Dashboard 四个协作与团队区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Collaboration Health、Sample Fixture Path、Leader Assignment Flow 和 Team Workspace Panels 的共同布局、两个条件显示、原加载状态和参数连接移动到 `src/project/ProjectDashboardCoordinationTeamPanels.jsx`；四个原组件继续分别按需加载并保持原显示顺序。协作诊断、聊天证明、Timeline 证明、Manager Dashboard、Agent 工作台、Agent 消息、Agent Pulse 和 Agent Dashboard 同步函数继续由 `App.jsx` 提供。保护测试先失败后通过；六项旧保护测试和两项本地发布条件检查已更新且功能要求未降低。564 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 182.6 秒，Autopilot 回执为 5.729 秒。源文件为 2,043 字节，独立构建文件为 2,295 字节，主页面构建文件为 666,846 字节，`App.jsx` 为 22,471 个非空行 |
| P2-178 | 已验证 | Dashboard 五个管理协作主体外层区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Unified Event Ledger、Kickoff Collaboration Panels、Collaboration Operations Panels、Manager Proof Map 和 Coordination Team Panels 的共同布局、事件记录显示条件、原加载状态和参数连接移动到 `src/project/ProjectDashboardManagerCollaborationBody.jsx`；五个原组件继续分别按需加载并保持原显示顺序。P2-176 证明路由、事件与治理同步、Manager 与 Cockpit 同步、群聊记录同步、证明跳转、Agent 工作台、Agent 消息、Agent Pulse 和 Agent Dashboard 同步函数继续由 `App.jsx` 提供。保护测试先失败后通过；十项旧保护测试已更新且功能要求未降低。565 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 233.3 秒，Autopilot 回执为 19.724 秒。源文件为 1,707 字节，独立构建文件为 2,067 字节，主页面构建文件为 665,831 字节，`App.jsx` 为 22,456 个非空行 |
| P2-179 | 已验证 | Dashboard 六个 Manager Ready Package 核心区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Launch Operations Overview、Manager Ready Package Summary、Coordination Panels、可选 Collaboration Intent Queue Snapshot、Runtime Panels 和 Evidence Panels 的共同布局、可选条件、原加载状态和参数连接移动到 `src/project/ProjectDashboardManagerReadyPackageCorePanels.jsx`；六个原组件继续分别按需加载并保持原显示顺序。Launch Operations、intent、聊天证明、Timeline 证明、节点流证明和证明模型同步函数继续由 `App.jsx` 提供。保护测试先失败后通过；十四项旧保护测试仅更新加载位置和对象参数写法，功能要求没有降低。566 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 146.1 秒，Autopilot 回执为 4.056 秒。源文件为 2,029 字节，独立构建文件为 2,285 字节，主页面构建文件为 664,578 字节，`App.jsx` 为 22,445 个非空行 |
| P2-180 | 已验证 | Dashboard 五个 Manager Ready Package 运行区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Project Evidence Export Workflow、Launch Readiness Panels、Pilot Operations Panels、Local Readiness Panels 和 Provider Security Panels 的共同布局、证据导出可选条件、原加载状态和参数连接移动到 `src/project/ProjectDashboardManagerReadyPackageOperationalPanels.jsx`；五个原组件继续分别按需加载并保持原显示顺序。证据导出四项操作、发布审批、试运行和生产控制回执、MVP 操作、Provider Eval 回执、证明模型同步、可用性判断与禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；十七项旧组件测试和两项发布检查仅更新对象参数源码定位写法，功能要求没有降低。567 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 192.3 秒，Autopilot 回执为 8.842 秒。源文件为 1,742 字节，独立构建文件为 1,961 字节，主页面构建文件为 663,666 字节，`App.jsx` 为 22,435 个非空行 |
| P2-181 | 已验证 | Dashboard 三个 Manager 后台快照区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Manager Snapshot Execution Panels、Manager Compatibility Proof Panels 和 Manager Submission Route Panels 的共同显示条件、原容器、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerBackendSnapshotPanels.jsx`；三个原组件继续分别按需加载并保持原显示顺序。意图运行、聊天证明、Timeline 证明、提交复核写入、草稿更新、证明跳转、可用性判断和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；九项旧保护测试、两项发布检查和前端边界检查仅更新加载位置与对象参数写法，功能要求没有降低。568 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 219.8 秒，Autopilot 回执为 19.480 秒。源文件为 1,141 字节，独立构建文件为 1,427 字节，主页面构建文件为 663,132 字节，`App.jsx` 为 22,431 个非空行 |
| P2-182 | 已验证 | Dashboard 四个 Manager 后台活动区域与错误提示共同按需加载且原组件、条件、顺序和操作不变 | 将 Manager Read Model Summary Panels、Autonomous Run Control、Agent Autonomous Action Queue、Latest Backend Work 及后台错误提示的共同显示顺序、两个条件、加载状态和参数连接移动到 `src/project/ProjectDashboardManagerBackendActivityPanels.jsx`；四个原组件继续分别按需加载并保持原显示顺序。自主运行全部命令、Agent 队列运行、聊天和 Timeline 证明跳转、可用性判断与禁用规则继续由 `App.jsx` 提供，Backend Scheduler Controls 保持原父级位置。保护测试先失败后通过；六项旧保护测试、两项发布检查和前端边界检查仅更新加载位置与对象参数写法，功能要求没有降低。569 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 219.4 秒，Autopilot 回执为 11.750 秒。源文件为 1,890 字节，独立构建文件为 2,167 字节，主页面构建文件为 662,014 字节，`App.jsx` 为 22,421 个非空行 |
| P2-183 | 已验证 | Dashboard Manager Worker Station 区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Backend Worker Station Status、可选 Production Infrastructure Rehearsal 和两个证明缺失提示的原显示顺序、条件、加载状态与参数连接移动到 `src/project/ProjectDashboardManagerWorkerStationPanels.jsx`；两个原组件继续分别按需加载并保持原顺序，Worker Station 外层区域及其后的 Manager Ready Package 保持原父级位置。地址编辑与保存、打开部署设置、基础设施演练运行、证明条件、可用性判断和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；三项旧保护测试仅更新加载位置与对象参数写法，功能要求没有降低。570 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 160.042 秒，Autopilot 回执为 4.023 秒。源文件为 1,730 字节，独立构建文件为 1,866 字节，主页面构建文件为 661,044 字节，`App.jsx` 为 22,413 个非空行 |
| P2-184 | 已验证 | Dashboard Manager Ready Package 外层共同按需加载且原条件、顺序和操作不变 | 将 Manager Ready Package 的原显示条件、快照容器、标题、Core Panels 与 Operational Panels 顺序及加载状态移动到 `src/project/ProjectDashboardManagerReadyPackageSnapshot.jsx`；两个已有共同组件继续分别按需加载并保持原顺序。所有模型、路由、来源标记、审批、回执、证明操作、可用性判断和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；两项旧保护测试和一项 P2-183 顺序保护仅更新加载位置，功能要求没有降低。571 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 194.388 秒，Autopilot 回执为 8.665 秒。源文件为 970 字节，独立构建文件为 1,259 字节，主页面构建文件为 660,514 字节，`App.jsx` 为 22,404 个非空行 |
| P2-185 | 已验证 | Dashboard Manager 后台快照与活动总装配按需加载且原顺序、调度位置和操作不变 | 将已独立的 Manager Backend Snapshot Panels 与 Backend Activity Panels 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendReadModelPanels.jsx`；两个已有共同组件继续分别按需加载并保持 Snapshot 在前、Activity 在后，Backend Scheduler Controls 继续位于总装配之后。意图运行、提交复核、自主运行、Agent 行动、聊天与 Timeline 证明、按钮回调、可用性判断和禁用规则继续由 `App.jsx` 提供。保护测试先失败后通过；四项旧保护测试仅更新加载位置与顺序检查，功能要求没有降低。572 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 189.079 秒，Autopilot 回执为 3.820 秒。源文件为 694 字节，独立构建文件为 1,015 字节，主页面构建文件为 660,304 字节，`App.jsx` 为 22,402 个非空行 |
| P2-186 | 已验证 | Dashboard Manager 后台工作站内容总装配按需加载且原顺序、外层区域、调度位置和操作不变 | 将已独立的 Worker Station Panels、Manager Ready Package Snapshot 与 Backend Read Model Panels 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendStationContent.jsx`；三个已有共同组件继续分别按需加载并保持原顺序，`backend-worker-station` 外层区域和 Backend Scheduler Controls 继续由 `App.jsx` 提供，调度控制仍位于总装配之后。地址保存、部署设置、基础设施演练、Ready Package 操作、意图运行、提交复核、自主运行、Agent 行动、证明操作、同步、可用性判断和禁用规则均未移动。保护测试先失败后通过；五项旧保护检查仅更新加载位置与顺序检查，功能要求没有降低。573 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 198.547 秒，Autopilot 回执为 14.513 秒。源文件为 962 字节，独立构建文件为 1,284 字节，主页面构建文件为 659,858 字节，`App.jsx` 为 22,395 个非空行 |
| P2-187 | 已验证 | Dashboard Manager 后台工作站外层区域总装配按需加载且原结构、顺序、调度操作和后续区域不变 | 将原 `backend-worker-station` 外层区域、Manager Backend Station Content 与 Backend Scheduler Controls 的原显示顺序和加载状态移动到 `src/project/ProjectDashboardManagerBackendStationRegion.jsx`；区域继续先加载 Station Content、再加载 Scheduler Controls，保留原标识和样式，Manager Collaboration Body 继续位于其后。工作站全部数据、调度图标、回调、禁用规则和写入操作继续由 `App.jsx` 提供，未移动、未重写。保护测试先失败后通过；全量测试和两套发布检查发现五项旧源码位置或 JSX 写法检查，只更新到新的实际加载位置与对象参数写法，功能要求没有降低。574 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通用户、普通项目和完整 Dashboard 三组操作分别用时 21.021 秒、35.905 秒和 169.321 秒，合计 226.247 秒；Autopilot 回执为 14.003 秒。源文件为 934 字节，独立构建文件为 1,215 字节，主页面构建文件为 659,448 字节，`App.jsx` 为 22,390 个非空行 |
| P2-188 | 已验证 | Dashboard Manager 主体四个相邻区域共同按需加载且原条件、顺序、加载边界和操作不变 | 将 Manager Core Panels、Work Loop Panels、Backend Station Region 与 Manager Collaboration Body 的原显示顺序及 Backend Station 加载边界移动到 `src/project/ProjectDashboardManagerBody.jsx`；四个原组件继续分别按需加载并保持原顺序，Manager 可用条件继续由 `App.jsx` 提供。全部数据对象、按钮回调、证明操作、调度操作、写入、可用性判断和禁用规则继续由 `App.jsx` 提供，未移动、未重写。保护测试先失败后通过；全量测试发现 24 项旧源码位置检查，只更新到新的实际加载位置，功能要求没有降低。575 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通用户、普通项目和完整 Dashboard 三组操作分别用时 20.364 秒、33.519 秒和 165.054 秒，合计 218.937 秒；Autopilot 回执为 14.528 秒。源文件为 1,006 字节，独立构建文件为 1,355 字节，主页面构建文件为 658,858 字节，`App.jsx` 为 22,378 个非空行 |
| P2-189 | 已验证 | 完整 Dashboard 内容外层总装配按需加载且原布局、顺序、条件和操作不变 | 将原 `project-paper` 容器、Top Panels 加载边界、Manager Body 插槽、Recent Commit Line 加载边界及原显示顺序移动到 `src/project/ProjectDashboardContentLayout.jsx`；Top Panels 与 Recent Commit Line 继续分别按需加载并保持原顺序。Manager 可用条件、完整 Manager Body、全部视图数据、按钮回调、证明操作、调度操作、写入、可用性判断和禁用规则继续由 `App.jsx` 提供，未移动、未重写。首次真实项目流程发现新按需布局缺少外层加载边界并进入界面恢复页；新增回归要求并补充加载状态后，相同流程通过，本地发布检查固定检查该边界。576 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通用户、普通项目和完整 Dashboard 三组操作分别用时 19.9 秒、34.0 秒和 115.8 秒，合计 169.7 秒；Autopilot 回执为 4.084 秒。源文件为 908 字节，独立构建文件为 1,153 字节，主页面构建文件为 658,758 字节，`App.jsx` 为 22,374 个非空行 |
| P2-175 | 已验证 | Dashboard 四个协作与管理区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Change Flow、Agent Communication Flow、Agent Management Mesh 和 Manager Scenario Readiness 的共同布局、两个条件显示、原加载状态和参数连接移动到 `src/project/ProjectDashboardCollaborationOperationsPanels.jsx`；四个原组件继续分别按需加载并保持原显示顺序。Cockpit 同步、Readiness Proof Map 同步、Management Sync、聊天证明和 Timeline 证明函数继续由 `App.jsx` 提供。保护测试先失败后通过；四项旧保护测试已更新且功能要求未降低。本地发布清单发现旧条件文本检查后，只将检查范围更新到共同文件中的同一条件。562 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 198.4 秒，Autopilot 回执为 15.403 秒。源文件为 1,970 字节，独立构建文件为 2,180 字节，主页面构建文件为 672,025 字节，`App.jsx` 为 22,555 个非空行 |
| P2-174 | 已验证 | Dashboard 五个立项与群聊记录区域共同按需加载且原组件、条件、顺序和操作不变 | 将 Governance & Speech Protocol、Kickoff Charter、Kickoff Meeting Flow、Kickoff Execution Flow 和 Group Chat Transcript Index 的共同布局、三个条件显示、原加载状态和参数连接移动到 `src/project/ProjectDashboardKickoffCollaborationPanels.jsx`；五个原组件继续分别按需加载并保持原显示顺序。治理同步、Manager 同步、群聊记录同步、聊天证明、Timeline 证明、记录打开和频道切换函数继续由 `App.jsx` 提供。保护测试先失败后通过；五项旧保护测试已更新且功能要求未降低。561 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 211.2 秒，Autopilot 回执为 13.809 秒。源文件为 2,316 字节，独立构建文件为 2,528 字节，主页面构建文件为 673,185 字节，`App.jsx` 为 22,568 个非空行 |
| P2-173 | 已验证 | Dashboard 四个工作循环区域共同按需加载且原组件、顺序和操作不变 | 将 Autonomous Work Loop、24/7 Operations Board、Continuous Work Loop 和 Fixed Work Routines 的共同布局、原加载状态和参数连接移动到 `src/project/ProjectDashboardWorkLoopPanels.jsx`；四个原组件继续分别按需加载并保持原显示顺序。自动 Pulse、Agent Pulse、Manager Cockpit 同步、聊天证明和 Timeline 证明函数继续由 `App.jsx` 提供。保护测试先失败后通过；四项旧保护测试已更新且功能要求未降低。560 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 222.1 秒，Autopilot 回执为 17.368 秒。源文件为 1,641 字节，独立构建文件为 1,954 字节，主页面构建文件为 674,699 字节，`App.jsx` 为 22,590 个非空行 |
| P2-172 | 已验证 | Dashboard 八个 Manager 核心区域共同按需加载且原组件、顺序和操作不变 | 将 Manager Command Centers、Scenario Walkthrough、Action Playbook、Action Run Ledger、Scenario Trail、Sync Protocol Audit、Manager Use Case Audit 和 Manager Composers 的共同布局、原加载状态和参数连接移动到 `src/project/ProjectDashboardManagerCorePanels.jsx`；八个原组件继续分别按需加载并保持原显示顺序。运行、同步、证明跳转、负责人分配和变更提交函数继续由 `App.jsx` 提供。保护测试先失败后通过；全量测试发现的两项旧文件位置检查已更新且功能要求未降低。559 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 223.8 秒，Autopilot 回执为 17.819 秒。源文件为 3,233 字节，独立构建文件为 3,583 字节，主页面构建文件为 675,941 字节，`App.jsx` 为 22,605 个非空行 |
| P2-171 | 已验证 | Dashboard 标题、摘要和 Agent 工作状态共同按需加载且原组件和操作不变 | 将三个现有区域的共同布局、原加载状态和参数连接移动到 `src/project/ProjectDashboardTopPanels.jsx`；三个原组件继续分别按需加载，后续全部 Manager 内容继续作为同一左侧区域内容。会议、Group Chat、Timeline、返回简洁页面、Manager 同步、Agent Pulse 和后台读取函数继续由 `App.jsx` 提供。保护测试先失败后通过；558 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 247.9 秒，Autopilot 回执为 19.949 秒。源文件为 1,295 字节，独立构建文件为 1,569 字节，主页面构建文件为 678,716 字节，`App.jsx` 为 22,640 行 |
| P2-170 | 已验证 | 完整 Timeline 与节点流图按需加载且节点、证明、同步和 intent 操作不变 | 将节点分类、后端图选择、离线备用图构建、缩放筛选、节点布局、证明关联、提交地址、关联人员、关系图和显示参数连接移动到 `src/project/ProjectTimelineRouteView.jsx`；原 `AdvancedProjectTimeline.jsx` 继续直接使用，节点确认、后端同步、聊天证明、Timeline 证明、提交记录定位、intent 操作和所有写入函数继续由 `App.jsx` 提供。保护测试先失败后通过；557 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程、七种窗口尺寸、四种缩放和完整 Dashboard 通过；三组真实操作合计 206.2 秒，Autopilot 回执为 4.923 秒。源文件为 37,560 字节，独立构建文件为 50,512 字节，主页面构建文件为 679,431 字节，`App.jsx` 为 22,649 行 |
| P2-169 | 已验证 | 完整 Group Chat 按需加载且发送、频道、附件、回复、提及、置顶和证明操作不变 | 将完整 Group Chat 的证明卡片、搜索结果、成员状态、频道分类、置顶、回复、提及、附件索引、回执摘要、消息合并判断和显示参数连接移动到 `src/project/ProjectChatRouteView.jsx`；原 `AdvancedProjectChat.jsx` 由该页面直接使用，发送、频道创建与切换、搜索、成员同步、附件、回复、提及、置顶、证明定位、输入状态和后端写入函数继续由 `App.jsx` 提供。首次完整 Dashboard 检查发现遗漏搜索结果参数并触发恢复页；回归测试先失败，补齐参数后重新检查通过。556 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、两套本地发布清单、构建体积和代码差异检查通过。真实普通项目流程和七种窗口尺寸、四种缩放通过；完整 Dashboard 162.5 秒通过，Autopilot 回执为 14.652 秒。源文件为 19,294 字节，独立构建文件为 43,276 字节，主页面构建文件为 700,456 字节，`App.jsx` 为 23,344 行 |
| P2-168 | 已验证 | 普通会议与完整会议按需加载且发送、语音、退出和完成操作不变 | 将普通会议的显示参数连接和只读状态计算移到 `src/meeting/ProjectSimpleMeetingRouteView.jsx`，将完整会议的显示参数连接、发言人、队列、时间和座位位置计算移到 `src/meeting/AdvancedMeetingRoomRouteView.jsx`；会议发送、语音识别、退出、完成、计时、后端写入判断和原 `renderProjectMeeting` 入口继续由 `App.jsx` 提供。保护测试先失败后通过；555 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。真实普通项目流程通过；普通工作区七种窗口尺寸和四种显示缩放通过；完整 Dashboard 146 秒通过，Autopilot 回执为 16.947 秒。源文件分别为 2,570 和 3,384 字节，独立构建文件分别为 4,200 和 18,930 字节，主页面构建文件为 709,265 字节，`App.jsx` 为 23,583 行 |
| P2-167 | 已验证 | 人才市场与人才详情按需加载且筛选、档案和签约操作不变 | 将人才市场分类、搜索、签约状态和部署信息计算移到 `src/scenes/AgentMarketRouteView.jsx`，将人才详情档案、技能、证据信息和签约状态计算移到 `src/scenes/AgentDossierRouteView.jsx`；打开档案、返回立项、继续立项、关闭档案和开始签约函数继续由 `App.jsx` 提供。保护测试先失败后通过；554 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和构建预算检查通过。真实用户检查打开人才市场、档案和签约项目选择，并通过七种窗口尺寸和四种显示缩放；真实普通项目流程通过；完整 Dashboard 149.5 秒通过，Autopilot 回执为 4.859 秒。源文件分别为 2,577 和 2,481 字节，独立构建文件分别为 9,234 和 13,198 字节，主页面构建文件为 710,240 字节，`App.jsx` 为 23,624 行 |
| P2-166 | 已验证 | 完整立项页面按需加载且文件夹、会议、团队和批准操作不变 | 将立项页面外壳、启动状态提示、六步导航、五个步骤组件装配、会议入口、生成来源和立项摘要原样移动到 `src/onboarding/ProjectInitiationFlowView.jsx`；文件夹选择、工作区创建、会议启动与提交、团队确认和项目批准函数继续由 `App.jsx` 提供。保护测试先失败后通过；553 项自动测试、产品构建、原 Dashboard 功能保护、前端边界和本地发布清单通过。普通工作区七种窗口尺寸和四种显示缩放通过；真实普通项目流程通过；完整 Dashboard 92.1 秒通过，Autopilot 回执为 5.419 秒。源文件为 17,258 字节，独立构建文件为 10,918 字节，主页面构建文件为 711,343 字节，`App.jsx` 为 23,663 行 |
| P2-165 | 已验证 | 普通项目列表与高级工作区按需加载且原操作不变 | 将 `renderDashboardView` 中普通项目列表、高级工作区、启动就绪判断、项目来源标识和工作区统计原样移动到 `src/workspace/WorkspaceView.jsx`；创建项目、打开项目、设置、项目目录同步、运行状态同步和经理示例函数继续由 `App.jsx` 提供。保护测试先失败后通过；552 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单和代码差异检查通过。普通工作区七种窗口尺寸和四种显示缩放通过；真实普通项目流程通过；完整 Dashboard 145 秒通过，Autopilot 回执为 13.847 秒。源文件为 12,568 字节，独立构建文件为 7,853 字节，主页面构建文件为 718,613 字节，`App.jsx` 为 23,833 行 |
| P2-164 | 已验证 | 完整设置窗口按需加载且账户、模型、密钥和项目设置操作不变 | 将设置窗口的现有显示、派生状态及七个设置区域装配原样移动到 `src/settings/SettingsModalView.jsx`；账户、模型、密钥、本地服务、运行检查、隐私、工作区和工具设置继续保留，实际写入函数继续由 `App.jsx` 提供。保护测试先失败后通过；真实设置流程使用隔离本地账户完成模型密钥、搜索密钥、Secret Vault、运行检查、隐私、工作区、费用限制和完整工作检查，54.5 秒通过。551 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。普通工作区七种窗口尺寸和四种显示缩放通过；真实普通项目流程通过；完整 Dashboard 135.7 秒通过，Autopilot 回执为 8.311 秒。源文件为 68,684 字节，独立构建文件为 23,994 字节，主页面构建文件为 724,900 字节，`App.jsx` 为 24,073 行 |
| P2-163 | 已验证 | 原静态样式改为独立 CSS 且 Dashboard 显示和操作不变 | 将 `App.jsx` 内完整静态样式逐字移动到 `src/styles/legacyApp.css`，入口在共享样式之后加载；删除运行时创建 `<style>` 的重复代码，没有新增、删除或改写任何样式规则。保护测试先失败后通过；550 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。普通工作区七种窗口尺寸和四种显示缩放通过；真实普通项目流程通过；完整 Dashboard 167.8 秒通过，Autopilot 回执为 17.006 秒。源 CSS 为 20,959 字节，主页面构建文件为 745,215 字节，`App.jsx` 为 24,917 行 |
| P2-162 | 已验证 | Active Threads 与 Team 的共同装配按需加载且原操作不变 | 两个原面板继续分别按需加载；只移动原双列布局、加载状态和组件参数连接。任务证明、聊天证明、Timeline 证明、Manager 同步、Agent 工作区、消息、Artifact、Evidence、Pulse 及全部操作函数继续由 `App.jsx` 提供。保护测试先失败后通过；547 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过；真实完整 Dashboard 85.8 秒通过，Autopilot 回执为 6.133 秒。独立按需文件为 915 字节，构建文件为 1,186 字节；主页面构建文件为 766,239 字节，`App.jsx` 为 25,544 行 |
| P2-161 | 已验证 | 普通项目群聊页面装配按需加载且完整 Group Chat 不变 | 原 `ProjectChatPanel` 继续单独按需加载；只移动原页面容器和组件参数装配。消息筛选、频道切换、输入处理、发送、重载、后台同步、完整 Group Chat 及其操作函数继续由 `App.jsx` 提供。保护测试先失败后通过；546 项自动测试、产品构建、原 Dashboard 功能保护、无障碍保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过；真实完整 Dashboard 85.3 秒通过，Autopilot 回执为 4.434 秒。独立按需文件为 915 字节，构建文件为 924 字节；主页面构建文件为 766,785 字节，`App.jsx` 为 25,551 行 |
| P2-160 | 已验证 | 普通项目会议页面装配按需加载且完整会议不变 | 会议舞台、会议记录和输入面板继续各自按需加载；只移动原加载提示、页面结构、显示状态和组件参数装配。返回、发送、语音、输入、设置、完整会议入口及其操作函数继续由 `App.jsx` 提供。真实普通项目流程覆盖立项、会议、群聊、时间线和刷新恢复并通过。完整 Manager Backend Core UI 初次检查发现 Agent 自主行动没有立即反馈；组件改为点击时立即显示当前 Agent 并禁止重复点击，原后端执行和证明逻辑不变。保护测试先失败后通过；545 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单、构建体积和代码差异检查通过。真实完整 Dashboard 176.8 秒通过，Autopilot 回执为 16.816 秒。独立按需文件为 3,327 字节，构建文件为 2,922 字节；主页面构建文件为 766,841 字节，`App.jsx` 为 25,553 行 |
| P2-159 | 已验证 | Manager Read Model Snapshots 与 Manager Action Queue Snapshot 的装配代码按需加载且完整自主运行控制不变 | 两个原组件继续各自按需加载；只移动原加载提示、数据组合、显示顺序和组件参数装配。完整 Autonomous Run Control、执行函数和禁用规则继续由 `App.jsx` 提供。首次真实检查发现已经开始的后台读取不能被原取消函数停止；修复后用户操作会取消等待中和执行中的后台项目读取。保护测试先失败后通过；544 项自动测试、产品构建、原 Dashboard 功能保护、前端边界、本地发布清单和构建体积检查通过。真实 Manager Backend Core UI 109.6 秒通过，Autopilot 回执为 4.113 秒。独立按需文件为 2,033 字节，构建文件为 1,781 字节；主页面构建文件为 768,568 字节，`App.jsx` 为 25,578 行 |
| P1-62 | 已验证 | Agent 自主行动点击后立即显示运行状态并在本地重任务完成后显示输出 | 移除了本地服务状态瞬间变化时的无提示退出；运行中 Agent 会立即显示，产品与验收均允许最长 60/65 秒完成；用户操作会取消未开始的后台读取，行动完成后的详细读取延后 15 秒并可取消。专项测试、514 项全量测试、产品构建和连续两次真实经理核心流程通过 |
| P1-63 | 已验证 | Autopilot Scheduler Tick 连续操作时不再长时间无回执 | 会话启动后的详细读取改为延后 15 秒并可由下一操作取消；Scheduler Tick 复用已知运行会话，使用专用 `/workers/autopilot/due` 本地路由，不再附带无关项目与 Agent 扫描；按钮立即显示运行中，最长等待与后端一致。专项测试、514 项全量测试和连续两次真实经理核心流程通过 |
| P1-64 | 已验证 | Private Pilot 连续操作立即显示新状态并启用下一步 | 写入结果直接携带当前与下一步状态，页面不会被旧读取结果覆盖；有写入结果时不再启动全部详细模型读取。真实浏览器连续完成十项批准、导出、发布、运行、健康和验收操作，81.3 秒通过；写入后的页面状态更新约 0.2–0.3 秒，523 项自动测试、产品构建、原 Dashboard 保护、前端边界和本地发布检查通过 |
| P1-66 | 已验证 | 完整 Dashboard 在紧凑电脑宽度和高系统缩放下不再出现横向滚动 | 新增 1440×1100、1280×720、1024×768 和 960×540 四种完整 Dashboard 真实检查。首次检查在 960×540 复现 19 像素横向滚动；修复后小于 1280 像素使用更紧凑间距并明确禁止横向滚动，1440 像素及以上保留原间距。普通工作区七种窗口尺寸、四种系统缩放、真实普通项目流程和完整 Dashboard 全部操作通过；549 项自动测试通过。页面结构、节点流图、Group Chat、会议、Timeline、intent 路由和业务操作未改 |
| P1-65 | 已验证 | Private Pilot 最终验收和四组 Production Controls 保存后立即显示新状态 | 最终验收直接返回下一步 Production Operations 状态；四组生产控制保存直接返回各自最新工作流，不再读取全部详细模型。首次实际页面验收复现灰色按钮，修复后完整流程 242.8 秒通过；进一步修复保存延迟后，同一流程 192.4 秒通过，四组保存后的页面更新时间约 0.2–0.3 秒。524 项自动测试、四组后端契约、产品构建、原 Dashboard 保护和 Agent Manager 场景通过 |
| P1-44 | 已验证 | 项目工具菜单点击不会被自动悬停关闭打断 | 真实经理演示复现了动态内容更新时按钮点击被概览内容拦截；项目工具菜单改为点击或键盘打开，进入工具后仍自动关闭。相同经理演示重新运行并通过 |
| P1-45 | 已验证 | 已登录但还没有项目时可以主动进入工作区 | 首次使用仍默认引导账户、模型和项目设置；完成本地登录后，用户点击“工作区”可以查看空工作区和创建入口，不会继续被首次使用遮罩阻断 |
| P1-46 | 已验证 | 完整控制台连续写入后刷新不会长期停留在旧状态 | 同一项目的证明读取同时只运行一批，期间发生的新变化合并为最多一次补充读取；普通证明刷新不再重复加载上线控制项。真实从零到最终交付流程在 143.3 秒内通过，最终页面显示完整交付、复核闭环和本地证明 |
| P1-48 | 已验证 | Manager Dashboard 和节点流图不再产生重复列表标识 | 根因是自动工作循环在同一轮为不同记录生成相同编号，且本地工作循环条目没有直接公开 Agent 编号。修复后每条自动工作记录有唯一编号和 Agent 归属，本地工作循环条目公开稳定 Agent 身份；测试先失败后通过，经理演示浏览器验证主动拦截重复警告并通过 |
| P1-49 | 已验证 | 英文首次使用和项目页面不再混入中文 | 修复可逆界面翻译、项目语言继承和首次使用验证等待；中英文 17 步真实页面验证全部通过，覆盖首次使用、设置、人才市场、项目、群聊、立项、实时切换和项目语言覆盖 |
| P1-50 | 已验证 | 中文经理演示不会因显示翻译破坏项目记录 | 根因是中文显示转换修改了已签名事件内容；修复为只转换界面响应字段，项目记录原样返回。连续两次中文变更回归测试先失败后通过，中文隔离环境可进入 Dashboard、会议、Group Chat 和节点流图，原项目记录校验错误为 0 |
| P1-51 | 已验证 | 经理演示首次打开不再出现短暂缺失和失败请求 | 根因是本地示例项目尚未写入时，账户权限、时间线、事件、聊天记录和管理数据已经开始读取。现在所有首次读取共用同一个项目准备任务；真实经理演示整链通过，失败 HTTP 请求为 0 |
| P1-52 | 已验证 | Manager Flow Graph 打开、RESET 和证明跳转不再显示空白画布 | 根因是大量并行节点让首批节点全部位于初始可视区域下方，且证明跳转携带证明编号，而旧定位只识别节点编号。首次打开与 RESET 现在把目标节点平移到画面内；节点元素同时携带节点、证明、时间线和事件编号，证明跳转可以定位真实关联节点。三项真实浏览器可见性检查先失败后通过；473 项自动测试、产品构建、原 Dashboard 功能保护和经理演示整链通过，失败请求为 0 |
| P1-53 | 已验证 | 经理演示 Scenario Trail 读取不再早于本地项目准备 | 根因是 Scenario Trail 是同类管理读取中唯一没有等待经理演示项目准备任务的函数，偶发产生 404。现在读取前复用同一准备任务，并加入已有准备时序保护测试；真实经理演示整链通过，失败请求为 0 |
| P1-54 | 已验证 | Manager Flow Graph 时间线证明跳转验收不再在位移动画中间误报空白 | 根因是焦点位置在 180 毫秒后计算，画布随后执行 240 毫秒位移动画；旧验收在动画开始前看到任意节点后立即继续，下一次读取可能正处于移动过程并得到 0。现在必须等待画布变换稳定至少 320 毫秒且节点持续可见；保护测试先失败后通过，连续两轮真实经理演示通过 |
| P1-55 | 已验证 | Manager Requirement Matrix 与 Sync Protocol Audit 不再在示例项目准备前产生 404 | 根因是两个读取与项目写入并行启动，但请求前没有复用现有项目准备任务。现在两个读取都先等待同一准备任务；确定性保护测试先失败后通过，连续两轮隔离经理演示通过，失败请求为 0 |
| P1-56 | 已验证 | Manager Flow Graph 证明跳转在动画期间不会把目标节点移出可见区域 | 根因是旧代码读取动画中的屏幕坐标后继续累加位移，节点较多时会产生过量偏移。现在改为使用节点在画布中的绝对位置和当前缩放比例直接计算目标位移；复现测试先失败后通过，真实经理演示整链通过，目标节点保持可见，失败请求为 0 |
| P1-57 | 已验证 | Manager Flow Graph 在后端节点稍晚到达时仍能完成证据定位 | 根因是旧定位只在进入节点流 180 毫秒后查找一次节点；后端节点在这之后到达时，页面有节点数据但不会再次定位。现在会在最多 5 秒内每 100 毫秒按条件查找，找到节点后立即停止并按绝对画布坐标定位，离开页面时取消等待。保护测试先失败后通过；专项 7 项检查、503 项全量测试、产品构建和修复后的真实经理演示整链通过 |
| P1-58 | 已验证 | 安全访问审计超过 40 条后仍为每条保留事件证明 | 根因是安全审计最多保留 240 条，但返回的事件证明编号只截取 40 条。现在按照保留的审计记录逐条关联事件账本；41 条连续访问决定回归测试先失败后通过，产品验收流程通过 |
| P1-59 | 已验证 | 经理示例项目不会在同一轮读取中重复写入 | 第一次本地示例写入成功后立即记录当前后端和项目的确认状态，后续读取不会在 React 状态刷新前再次写入。真实浏览器主动检查同一示例最多写入一次，经理核心流程 136.5 秒通过 |
| P1-60 | 已验证 | 立项批准等待期间立即显示进度并禁止重复提交 | 点击后立即显示旋转状态和“正在创建项目并启动 Agent 团队”，按钮在完成或失败前不可重复点击；创建完成后进入原项目概览，再由用户打开原完整 Dashboard。真实 Mission Runner 从本地账户、工作区、会议、批准到 Dashboard、聊天、时间线和节点流图在 111.8 秒内通过 |
| P1-61 | 已验证 | 自主运行输出中的单个 Artifact 不再重复显示 | 原 Autonomous Run Control 对同一 Artifact 定义了两条相同输出行，特定运行结果可能产生重复列表标识。现在只保留包含路由、事件和证明编号的完整行；保护测试明确要求每个 Artifact 只定义一次，真实经理核心流程通过 |
| P2-34 | 已验证 | 立项保存位置页面适配手机 | 真实 390×844 浏览器无横向溢出，主要输入和按钮均可到达 |
| P2-35 | 已验证 | 普通工作记录不显示内部流程图，完整流程图保留 | 普通项目显示最近 30 条用户工作记录并过滤内部路由和系统账户；完整控制台继续提供原节点流图和后台同步能力 |
| P2-36 | 已验证 | 项目群聊只保留真实沟通所需操作 | 新群聊页面保留频道、消息、决定、发送状态、恢复提示和输入；真实项目发送后文字在 1 秒内显示，随后返回项目、打开时间线与刷新恢复均通过 |
| P2-37 | 已验证 | 用户界面自动验收与当前产品一致 | 当前主流程浏览器检查逐项覆盖七种规定尺寸的工作区、六类设置、五类工作模式、立项保存位置和横向溢出；七种尺寸全部通过，后端从零到自主工作接口验收独立运行并通过 |
| P2-38 | 已验证 | 发布检查同时保护普通页面和原完整功能 | 当前发布清单检查启动器、独立普通页面、错误恢复、纯本地默认值，以及原时间线、节点图和完整控制台入口，避免再次误删原功能 |
| P2-39 | 已验证 | 普通群聊保持简洁，完整 Group Chat 功能保留 | 普通群聊隐藏内部系统消息和技术状态；完整项目控制台继续提供原消息检索、置顶、回复、提及、附件和证明定位功能 |
| P2-40 | 已验证 | 较大的本地项目创建后可以刷新恢复 | 真实流程测得项目列表读取约 2.34 秒，旧 1.2 秒等待上限会提前终止；上限改为 10 秒后，完整立项、会议、群聊、时间线和刷新恢复连续两次通过 |
| P2-04 | 已验证 | 详细内容只在打开时加载 | 普通项目、时间线和设置不再启动高级读模型请求；打开高级信息后才同步详细内容 |
| P2-05 | 已验证 | 后台同步不会重复或覆盖新状态 | 普通视图同步预算测试、作用域串行化与过期完成抑制测试通过 |
| P2-06 | 已验证 | 等待、超时、重试和错误规则一致 | 会议、模型、搜索和服务商传输均使用有界超时、重试、取消和终止状态测试 |
| P2-07 | 已验证 | 所有失败都有本地可查看记录 | 本地运行状态保存失败类型和时间，设置页可下载去隐私诊断信息 |
| P2-08 | 已验证 | 重复点击不会重复创建工作 | 本地幂等执行、队列回执、重复确认和单次关闭测试通过 |
| P2-09 | 已验证 | 长任务重启后继续或明确恢复 | 本地队列、Autopilot、事件恢复和优雅关闭重启测试通过 |
| P2-10 | 已验证 | 内存、处理器、磁盘和响应时间有基准 | `docs/local-performance-baseline.json` 最新复验：44MB 数据读取 44.21ms、解析 90.08ms、内存约 214MB、健康接口 P95 27.34ms，预算通过 |
| P2-11 | 已验证 | 大量项目、消息、任务和长时间运行可用 | 100 项目、5000 任务、10000 消息写入和重启恢复测试通过 |
| P2-12 | 已验证 | 启动前检查支持的 Node.js 版本 | Node 18 拒绝、Node 20/24 允许，`package.json` 声明 `>=20` |
| P2-13 | 已验证 | 端口占用时自动选择或提供明确操作 | 首选端口被占用时自动选择下一个可用本地端口，冲突测试通过 |
| P2-14 | 已验证 | 普通用户无需命令行即可启动 | 根目录提供 Windows 双击文件和跨平台启动脚本，并自动打开实际界面地址 |
| P2-15 | 已验证 | Windows 本地启动程序可用 | Windows 本机 Node 24 启动验证、前后端健康验证和启动文件检查通过 |
| P2-16 | 已验证 | macOS 和 Linux 有自动兼容测试并标明实机状态 | 启动器包含 macOS `open` 与 Linux `xdg-open` 路径并通过自动检查；当前没有对应设备，已按本目标要求明确记录为未实机验证，不把自动结果表述成实机结果 |
| P2-17 | 已验证 | 七种规定屏幕尺寸主要流程可用 | 内置浏览器逐项设置为 1920×1080、1600×900、1440×900、1366×768、1280×720、1024×768、390×844；立项主流程均无页面横向溢出，主要输入和按钮没有不可达项；390×844 高级设置窗口完整且关闭按钮可见 |
| P2-18 | 已验证 | 100%、125%、150%、200%缩放可用 | 以 1920×1080 物理屏幕为基础，自动浏览器分别使用 100%、125%、150%、200%显示缩放对应的视口与像素密度，工作区、模型设置、关闭操作和横向溢出全部通过；Windows 150%同时有当前实机证据 |
| P2-19 | 已验证 | 中文和空格路径可用 | 中文与空格目录中的项目存档创建、读取和重启恢复测试通过 |
| P2-20 | 已验证 | 非管理员账户与离线环境可用 | 当前 Windows 会话确认 `Elevated=false`；真实项目浏览器流程强制阻止所有非本机网络访问，模型、搜索、立项、会议、群聊、时间线和刷新恢复仍全部通过，且没有产生任何外部地址请求 |
| P2-21 | 已验证 | 磁盘不足、文件损坏、模型不可用和突然关闭可恢复 | ENOSPC、损坏隔离、模型降级、调度器关闭和优雅关闭测试共 12 项通过 |
| P2-22 | 已验证 | 本地错误、运行和恢复记录可查看 | 设置页显示本地服务、模型、账户和数据状态，并提供诊断下载 |
| P2-23 | 已验证 | 诊断导出默认删除密钥和隐私内容 | 导出仅含运行状态、失败类型、平台和数量；凭证、身份、路径、项目内容和消息均不导出，敏感内容测试通过 |

| P2-190 | 已验证 | 完整 Dashboard 最外层按需加载且原结构、顺序、条件和操作不变 | 将原完整 Dashboard 外层、场景装饰、项目概览容器、内容加载边界和项目工具加载边界移动到 `src/project/ProjectDashboardAdvancedView.jsx`；Content Layout 继续位于 Tool Launcher 之前。Manager 可用条件、完整视图对象、全部回调、写入、证明操作、调度操作、可用性判断和禁用规则继续由 `App.jsx` 提供。集中自动检查通过 5 项受影响契约、正式构建、发布保护和 700 KB 预算；真实完整 Dashboard 流程 166.7 秒通过，Autopilot 回执 7.963 秒。源文件 1,540 字节，独立构建文件 1,852 字节，主页面构建文件 657,688 字节，`App.jsx` 为 22,364 个非空行 |

## 最终验收进度

| 编号 | 状态 | 验收要求 | 当前证据 |
|---|---|---|---|
| F-01 | 已验证 | 连续启动 10 次且成功率 100% | 2026-07-11 在 Windows 本机连续执行 10 次隔离启动验证，10 次全部通过；每次使用独立临时账户、项目存档和随机本地端口，真实数据未参与 |
| F-02 | 已验证 | 自动检查与真实完整 Dashboard 检查无失败 | 上一个完整全量检查为 576 项通过、0 项失败。本次按项目主管要求集中运行两项最终检查：Dashboard 升级检查覆盖 5 项受影响契约、正式构建、发布保护和 700 KB 预算，全部通过；真实完整 Dashboard 流程 166.7 秒通过，覆盖 Group Chat、Manager、Manager Flow Graph、Agent、Autopilot 和四种电脑宽度，Autopilot 回执为 7.963 秒 |
| F-03 | 已验证 | 七种屏幕尺寸与四种缩放比例 | 七种规定屏幕尺寸已在真实内置浏览器逐项复验；100%、125%、150%、200%显示缩放已通过独立浏览器环境检查，Windows 150%另有实机验证 |
| F-04 | 已验证 | macOS、Linux、非管理员账户和断网环境 | Windows 非管理员权限与禁止全部外部网络的真实项目流程已验证；macOS、Linux在无对应设备的条件下完成自动兼容检查，并按目标要求明确保留“未实机验证”限制 |

## 五类真实工作验证

| 编号 | 状态 | 验证范围 |
|---|---|---|
| W-01 | 已验证 | 学生学习：目标、团队、计划、练习、检查、长期进度；学习计划与教学安全验收通过 |
| W-02 | 已验证 | 论文写作：题目、资料、提纲、草稿、引用、复核、导出；写作流水线与引用完整性重启验收通过 |
| W-03 | 已验证 | 调查：问题、资料、证据、事实与推测、结论、复查；多假设、矛盾处理、独立复核和安全边界验收通过 |
| W-04 | 已验证 | 技术工作：任务、检查、修改、验证、失败处理、报告；安全证明、回滚和发布收据验收通过 |
| W-05 | 已验证 | 创作艺术：要求、角色、多个方案、选择、修改、版本来源；双重审阅、版权与来源验收通过 |
