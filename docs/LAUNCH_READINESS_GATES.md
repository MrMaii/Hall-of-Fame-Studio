# Launch Readiness Gates

This document is the operational launch gate for Hall of Fame Studio. It keeps the project positioned as a general AI product-team system. Research Project remains a validation sample, not a research-only product surface.

## Gate Summary

| Gate | Target | Decision | Required proof | Not allowed |
| --- | --- | --- | --- | --- |
| P0 - Local Backend MVP | Internal product-team validation on one workstation | Can keep developing and demoing locally | Generic kickoff-to-final-deliverable loop, Agent submissions, evidence, reviews, Flow Graph, Proof Map, Agent Dashboard, persona skill regression, build | Claiming customer readiness or production readiness |
| P1 - Customer Private Pilot | Controlled customer rehearsal with non-sensitive or approved pilot data | Can run a private pilot only with explicit operator oversight | P0 plus launch approvals, project evidence archive/export, release candidate, launch run, health check, customer acceptance report, UI handoff receipts | Public production launch, unattended production daemon, sensitive data without approved controls |
| P2 - Public Production | External production service | Blocked | Managed database, managed queue/cron, real BYOK/provider controls, centralized observability, immutable audit, security operations, customer acceptance policy | Treating local/test/private-pilot receipts as production certification |

## P0 - Local Backend MVP

P0 proves the core product-team loop is real and backend-backed. It is the fastest gate for ordinary development work.

Required commands:

```bash
npm run build
npm run skills:check
npm run skills:blend
npm run agents:scenario:contract
npm run agents:scenario
npm run agents:server:validate
npm run agents:local-mvp-startup-readiness
npm run agents:public-production-startup-readiness
npm run agents:managed-infrastructure-cutover-attestations
npm run agents:settings-health-readiness
npm run agents:settings-runtime-readiness
npm run agents:model-provider-adapter
npm run agents:settings-provider-readiness
npm run agents:settings-integration-readiness
npm run agents:settings-contracts
npm run agents:evidence-index-readiness
npm run agents:budget-alert-readiness
npm run agents:error-reporting-readiness
npm run agents:search-provider:vault-endpoint
npm run agents:project-settings:privacy
npm run agents:project-settings:provider-budget
npm run agents:project-settings:tool-grants
npm run agents:local-tool-grants
npm run agents:prompt-boundary
npm run agents:action-approvals
npm run agents:quality-evaluation
npm run agents:model-degradation
npm run agents:team-formation
npm run agents:delegation-governance
npm run agents:shared-memory
npm run agents:review-handoff
npm run agents:autonomy-governor
npm run agents:learning-program
npm run agents:teaching-safety
npm run agents:academic-writing-pipeline
npm run agents:citation-integrity
npm run agents:investigation-case
npm run agents:investigation-safety
npm run agents:technical-delivery
npm run agents:engineering-security
npm run agents:creative-studio
npm run agents:rights-provenance
npm run agents:fine-grained-authorization
npm run agents:store-migration-safety
npm run agents:local-artifact-storage
npm run agents:local-audit-integrity
npm run agents:local-privacy-lifecycle
npm run agents:local-durable-task-queue
npm run agents:local-idempotent-execution
npm run agents:local-timeout-cancellation
npm run agents:local-scheduling-governance
npm run agents:local-dead-letter-governance
npm run agents:local-rate-concurrency-governance
npm run agents:local-graceful-shutdown
npm run agents:local-event-recovery
npm run agents:local-causal-request-tracing
npm run agents:project-settings:integrations
npm run agents:project-settings:workspace
npm run agents:product-team:smoke
npm run agents:mission-runner:startup
npm run agents:mvp-readiness:operator-actions
npm run agents:product-team:local-mvp
npm run agents:transcript-contracts
npm run agents:transcript-channel-create
npm run agents:transcript-search
npm run agents:transcript-channel-pin
npm run agents:transcript-pin
npm run agents:transcript-reply
npm run agents:transcript-mention
npm run agents:transcript-attachment
npm run agents:transcript-member-presence
npm run agents:timeline-action
npm run agents:agent-workbench-contract
npm run agents:agent-message-contract
npm run agents:agent-contract
npm run agents:manager-chat-command-contract
npm run ui:mock-boundaries
npm run agents:real-user-zero-to-autonomy
npm run agents:real-user-zero-to-autonomy-report
npm run agents:real-user-zero-to-autonomy-report:validate
npm run agents:private-mvp-launch-package
npm run agents:private-mvp-launch-package:validate
npm run agents:launch-operations:private-mvp
npm run agents:product-team:core
npm run agents:product-team:core:file-backed
npm run agents:product-team:research-sample
npm run agents:product-team:cycle-consistency
npm run ui:manager-backend:core
npm run ui:manager-backend:core:dev
npm run ui:manager-backend:real-user-chain
npm run ui:manager-backend:real-user-chain:dev
npm run ui:manager-backend:proof-navigation
npm run ui:manager-backend:private-pilot-panels
npm run ui:manager-backend:production-controls
npm run ui:manager-provider-proof
npm run ui:settings-agents-server
npm run ui:manager-mission-runner
npm run ui:real-user-zero-to-autonomy
npm run launch:local-mvp:check
```

Required product evidence:

- Kickoff meeting, role clarification, self-marketing, Leader/Reviewer governance.
- Brainstorm Layer through generic `brainstorm-board` submissions.
- Search/Evidence Layer with provider receipt, source snapshots, source quality, and source safety.
- Agent submissions for `discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, and `final-deliverable`.
- Reviewer requested-changes review, linked revision response, accepted final deliverable.
- Agent Workbench backend writes for evidence, all required generic artifact submissions (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`), generated draft, review, linked revision, and final delivery, with transcript/timeline/event/Flow Graph/Proof Map proof. React must accept an independently refreshed `readiness-proof-map/v1` after Agent/Reviewer writes even when the aggregate Manager Dashboard refresh is delayed.
- Agent-to-Agent message delivery through backend inbox, sender worklog, transcript, timeline, event ledger, Manager Dashboard, Manager Flow Graph, and Proof Map routes.
- Marketplace Agent contracting through `POST /projects/:id/agents/contract`, so a Manager can bring an Agent from The Pantheon into the project roster with `agent-contract/v1`, Agent Dashboard worklog proof, timeline/event proof, Manager Flow Graph collaboration node, and deferred read-model refresh routes instead of browser-local roster mutation.
- Manager group-chat commands for Leader assignment and change intake through backend transcript, timeline, event ledger, Manager Dashboard, Manager Flow Graph, Readiness Proof Map, and Agent Dashboard proof.
- React backend project commands must refresh Manager Flow Graph and Readiness Proof Map immediately after successful chat, meeting, transcript, or change writes, so C-side proof nodes/routes are visible from backend reads without relying on browser-local message state.
- Autonomous Run Control / Agent Queue continuation receipts must refresh `readiness-proof-map/v1` directly after C/A run actions, loops, sessions, ticks, and due-worker processing, so Proof Map evidence does not depend on aggregate Dashboard refresh.
- Planner / Executor / Reviewer state machine through `planner-executor-reviewer-state-machine/v1`, proving role lanes and handoff transitions from backend proof.
- Manager Dashboard, Manager Flow Graph, Readiness Proof Map, Agent Dashboard, transcript, timeline, and event-ledger proof. Manager governance/action routes for Command Center, Scenario Trail, Scenario Walkthrough, Requirement Matrix, Sync Protocol Audit, Use Case Audit, and Action Queue must appear in both Proof Map routes and Flow Graph nodes.
- Settings Health must expose the read-only `GET /settings/health-readiness` Quick Check and the explicit `POST /settings/workflow-smoke` backend probe, so the first-run check proves a real product-brief Agent submission through Group Chat transcript, Timeline, Event Ledger, Flow Graph, and Proof Map instead of a browser-composed mock.
- Backend Group Chat channel creation through `POST /projects/:id/transcripts`, so a Manager can create a dedicated collaboration room with `transcript-channel-created/v1`, transcript-visible system message, timeline/event proof, Manager Flow Graph node, and Readiness Proof Map route instead of a browser-only chat tab.
- Backend Group Chat transcript search through `GET /projects/:id/transcripts/search`, so Manager search results come from backend transcript/proof messages instead of a disabled UI placeholder or browser-local chat history.
- Backend Group Chat channel pins through `POST /projects/:id/transcripts/:channelId/channel-pin`, so a Manager channel pin creates `transcript-channel-pin/v1` receipt, channel system message, timeline/event proof, Flow Graph node, and Readiness Proof Map route instead of a disabled header placeholder.
- Backend Group Chat message pins through `POST /projects/:id/transcripts/:channelId/pins`, so a Manager pin creates `transcript-pin/v1` receipt, chat system message, timeline/event proof, Flow Graph node, and Readiness Proof Map route instead of a disabled per-message placeholder.
- Backend Group Chat replies through `POST /projects/:id/transcripts/:channelId/replies`, so a Manager reply creates `transcript-reply/v1` receipt, reply transcript message, direct-target receipt proof, timeline/event proof, Flow Graph node, and Readiness Proof Map route instead of a disabled per-message placeholder.
- Backend Group Chat mentions through `POST /projects/:id/transcripts/:channelId/mentions`, so a Manager @mention creates `transcript-mention/v1` receipt, mention transcript message, target Agent inbox proof, timeline/event proof, Flow Graph node, and Readiness Proof Map route instead of a disabled per-message placeholder.
- Backend Group Chat attachments through `POST /projects/:id/transcripts/:channelId/attachments`, so a Manager file attach creates `transcript-attachment/v1` receipt, file transcript message, content checksum proof, timeline/event proof, Flow Graph node, and Readiness Proof Map route instead of a disabled attachment placeholder.
- Backend Group Chat member presence through `GET /projects/:id/transcripts/:channelId/members`, so the member control reads `transcript-member-presence/v1` from backend receipts and exposes Proof Map / Flow Graph coverage instead of a disabled header placeholder.
- Backend Timeline actions through `POST /projects/:id/timeline/actions`, so a Manager note, acknowledgement, completion mark, or edit note on any timeline node creates `timeline-action-receipt/v1`, a new timeline log, an event-ledger row, Flow Graph / Proof Map refresh, and a Flow Graph timeline proof node instead of a no-op comment or completion button.
- Backend scheduler and Agent worker proof, including autonomous strategy, queued Agent action evidence, and the Product Team Operating Loop summary for C/A continuation.
- Runtime Autonomy Status through `GET /projects/:id/runtime-autonomy-status`, so C-side startup, A-side Autopilot/session receipts, worker queue recovery, queue adapter rehearsal, persistence snapshot, Flow Graph, Proof Map, Ready Package, and scheduler recovery routes are visible as one backend contract while unattended public production stays blocked.
- Local MVP startup readiness through `local-mvp-startup-readiness/v1` at `GET /local-mvp-startup-readiness`: it aggregates backend reachability, Secret Vault readiness, model/search runtime status, provider-vault redaction, project catalog readability, next action, and validation commands before the UI claims a user can start a local MVP session. It must appear in Workspace Hub as `workspace-local-mvp-startup-readiness` before Start Initiation, in Manager Ready Package as `localMvpStartupReadiness`, and in Readiness Proof Map as `localMvpStartupReadinessRoutes`. It may mark Settings/provider setup ready, but public production remains blocked by managed secrets, persistence, queue, provider audit, and incident controls.
- Public production startup readiness through `public-production-startup-readiness/v1` at `GET /public-production-startup-readiness`: it is a global backend-only launch preflight for public traffic. It checks enforced access control, signed requests, replay protection, audit fail-closed mode, managed identity provider/service identity evidence, managed Secret Manager/KMS, managed persistence cutover, managed worker queue cutover, provider runtime/redaction, production provider cost controls, production data governance, adapter gateway attestation, production traffic/health/rollback controls, centralized observability, alert routing, on-call ownership, managed incident system, restore drill proof, and centralized audit retention. It embeds `managed-identity-startup-readiness/v1`, which must require identity provider metadata, service identity audience/subject configuration, and a signed managed-production identity receipt; `AGENT_ACCESS_SIGNING_SECRET` or identity endpoint env names alone must remain blocked. It embeds `production-cost-control-startup-readiness/v1`, which must require production provider budget/rate-limit policy, centralized usage/cost audit, cost alert routing, and a signed managed-production cost-control receipt; project-local budgets or cost endpoint env names alone must remain blocked. It embeds `production-data-governance-startup-readiness/v1`, which must require retention policy, deletion job, export storage, data residency, and a signed managed-production data-governance receipt; project-local privacy settings or export/deletion endpoint env names alone must remain blocked. It embeds `production-traffic-startup-readiness/v1`, which must require production domain/TLS, traffic gateway, health check, release approval, rollback smoke test, and a signed managed-production traffic-control receipt; URL, health-check, approval, or rollback env names alone must remain blocked. It embeds `managed-secret-manager-readiness/v1`, which must require managed provider evidence, managed endpoint/key configuration, and a matched attestation id/checksum pair; setting Secret Manager/KMS env names while still using the local AES-GCM vault must remain blocked. It embeds `managed-infrastructure-cutover-readiness/v1`, which must require managed database/queue configuration, `*_REQUIRE_REAL_ADAPTER=true`, and managed-production cutover proof; setting database URLs or queue endpoints while still lacking cutover receipts must remain blocked. It also embeds `production-operations-startup-readiness/v1`, which must require observability, audit retention, incident response, and restore-drill configuration plus signed managed-production evidence receipt ids/checksums/signatures; setting logs/metrics/traces, paging, incident, restore, audit endpoints, or unsigned receipt ids/checksums must remain blocked. It also embeds `production-environment-setup/v1`, a redacted setup matrix with domain rows, env var names, missing any-of groups, next actions, routes, and validation commands, plus `public-production-action-plan/v1`, a backend action manifest generated from blocked setup rows so operators can see the next concrete production actions without leaking env values. Settings runtime readiness, Manager Ready Package, Readiness Proof Map, Manager Flow Graph, Manager UI, and the project-level Production Launch Control Center must link this route; Manager UI must render the matrix and action plan from backend data rather than synthesizing launch setup rows. The route must keep `readyForPublicProduction=false` until those managed production controls exist.
- Customer production acceptance is an explicit embedded startup blocker through `production-customer-acceptance-startup-readiness/v1`. The `customer-production-acceptance-policy` gate must require customer production policy id, success criteria, acceptance threshold, approver role, approval id, rollback criteria, and signed managed-production customer acceptance receipt/checksum/signature evidence; private-pilot acceptance reports or env-only customer policy configuration must remain blocked.
- Production Launch Gap Register must import blocked `production-environment-setup/v1` rows as `public-production-environment-setup` gap rows, so public startup setup gaps appear in the same Manager action register as launch audit, evidence integrity, deployment, operations, provider, custody, artifact, and review blockers. Manager Flow Graph and Readiness Proof Map must carry the same environment setup gap count from backend data.
- Settings health readiness through `settings-health-readiness/v1` at `GET /settings/health-readiness`: Quick Check must read a backend-owned health contract instead of synthesizing rows in the browser. The contract lists backend API, startup readiness, Secret Vault, model/search provider status, explicit test routes, worker status route, project catalog, validation commands, and production blockers without issuing provider calls or creating probe projects. It must appear in Manager Ready Package as `settingsHealthReadiness` and in Readiness Proof Map as `settingsHealthReadinessRoutes`.
- Settings runtime readiness through `settings-runtime-readiness/v1` at `GET /settings/runtime-readiness` and `GET /projects/:id/settings-runtime-readiness`: Deployment and Models must read backend-owned runtime rows for model/search runtime, provider-vault binding proof, worker status route, local persistence adapter, worker queue adapter, deployment preflight linkage, validation commands, and production blockers instead of inferring deployment/model readiness in the browser. It must appear in Manager Ready Package as `settingsRuntimeReadiness` and in Readiness Proof Map as `settingsRuntimeReadinessRoutes`.
- Manager Use Case Audit proof through `GET /projects/:id/manager-use-case-audit` and Readiness Proof Map `managerUseCaseAuditRoutes`, so customer-story coverage is route-backed rather than inferred from frontend fallback rows.
- MVP readiness operator actions through `GET /projects/:id/mvp-readiness` and `POST /projects/:id/mvp-readiness/operator-actions/:actionId/run`, so the Manager sees and records the routed next step for core closure, private-pilot handoff, or production hardening. Runnable core closure receipts must hand an `autopilot-delivery-target-control/v1` target to Collaboration Intent Queue and Autonomous Run Control, and running the target must create the requested Agent submission, evidence, or review proof node across the generic product-team chain. `npm run agents:mvp-readiness:operator-actions` is the focused low-write real Node HTTP server gate for this path. Production-hardening receipts must stay non-autonomous and must not treat local MVP proof or an operator receipt as production readiness.
- Product Team Mission Runner through `POST /product-team-missions` and `GET /projects/:id/product-team-missions`, so a customer goal can become a real backend project with kickoff approval, Leader/Reviewer decisions, bounded Autopilot startup, optional first A-side tick, and `product-team-mission-run/v1` proof in Manager Dashboard, Flow Graph, Readiness Proof Map, Delivery Trace, Operating Loop, Collaboration Intent Queue, Runtime Autonomy Status, and Autonomous Run Control. The Manager-facing React initiation approval must use this route and, when a kickoff meeting already exists, the mission receipt must prove it reused that meeting instead of creating a disconnected project start. This gate proves local/private-MVP mission startup only; production autonomy still requires the P2 controls below.
- `npm run agents:mission-runner:startup` is the focused low-write real Node HTTP server gate for Product Team Mission Runner. It posts `/product-team-missions`, verifies kickoff self-marketing, Leader/Reviewer selection, `product-team-mission-run/v1`, C/A handoff, Runtime Autonomy Status, Collaboration Intent Queue handoff run, first A-side Agent submission output, Manager Flow Graph, and Readiness Proof Map without running browser/build gates.
- Project privacy policy writes are receipt-backed through `project-settings/v1`: retention mode, provider log mode, model-training policy, and evidence-export approval policy must persist in the backend store with project settings audit, timeline, and event-ledger proof while `readyForProduction` remains false until managed security and operations controls exist.
- Project provider budget policy writes are receipt-backed through `project-settings/v1`: daily provider budget and hourly request limits must persist in the backend store, appear in provider settings audit/timeline/event proof, and feed `provider-readiness/v1` plus `provider-controlled-run/v1` budget/headroom calculations while production alerting and centralized cost controls remain P2 blockers.
- Project tool grant policy writes are receipt-backed through `project-settings/v1`: Agent provider-test, model kickoff/intent/artifact-draft, and search-evidence grants must persist in the backend store, appear in settings audit/timeline/event proof, and feed `provider-readiness/v1` plus `provider-controlled-run/v1` Agent tool decisions while production task-scoped authorization and managed runtime identity remain P2 blockers.
- Project integration capability rows are receipt-backed through `project-settings/v1`: `project-integration-capabilities/v1` must mark provider budget and Agent tool grants as backend-backed editable controls, and must mark Vector Store, Proxy/Webhook, MCP Tools, Budget Alerts, and Error Reporting as backend-backed read-only route contracts rather than missing backend APIs. Those read-only rows must point to Evidence Index readiness, Adapter Gateway preflight, Provider Readiness, Budget Alert readiness, and Error Reporting readiness, remain `editable=false`, and keep production readiness blocked until managed adapter, registry, alerting, observability, and operations controls exist.
- Settings integration readiness is backend-owned through `settings-integration-readiness/v1` at `GET /projects/:id/settings-integration-readiness`: Integrations must read one project-scoped contract that aggregates provider budget, Agent tool grants, Proxy/Webhook, MCP Tools, Vector Store, Budget Alerts, and Error Reporting rows with current status, route, schema, checksum, action-required counts, validation commands, and production blockers. It must appear in Manager Ready Package as `settingsIntegrationReadiness` and in Readiness Proof Map as `settingsIntegrationReadinessRoutes`, so the UI cannot present route rows as if they were finished production integrations.
- Project workspace capability rows are receipt-backed through `project-settings/v1`: `project-workspace-policy/v1` persists interface density, default visibility, and autosave cadence, while `project-workspace-capabilities/v1` must mark global interface language as browser-local; project language, interface density, default visibility, autosave cadence, local workspace binding, runtime contract rules, long-term memory readiness, and meeting summaries as backend-backed. Local workspace binding must write through `POST /projects/:id/workspace/bind`, read back through `GET /projects/:id/local-runtime`, and remain local/private-MVP only until managed workspace storage, access policy, quotas, and audit retention exist. Runtime contract rules must point to `GET /projects/:id/runtime-contracts`, expose `runtime-contract-freeze/v1`, remain non-editable from Settings, and keep production runtime approval blocked. Long-term memory must point to `GET /projects/:id/memory-readiness`, expose `project-memory-readiness/v1`, remain non-editable, enter Manager Ready Package as `projectMemoryReadiness`, enter Readiness Proof Map as `projectMemoryReadinessRoutes`, and prove local transcript/meeting/evidence/artifact/persistence-plan memory readiness without claiming managed long-term memory production readiness. `meeting-summaries/v1` must be derived from backend transcript, timeline, and event-ledger proof and must keep production readiness blocked until summary provenance, transcript retention, and human review policy exist.
- Settings Proxy/Webhook exposes backend Adapter Gateway preflight instead of a fake editable control: the Settings row must point to `GET /projects/:id/adapter-gateway-preflight`, which returns `adapter-gateway-preflight/v1` and keeps production adapter cutover blocked until private gateway delivery, signatures, retries, and dead-letter storage exist.
- Settings MCP Tools exposes backend Provider Readiness instead of a fake editable control: the Settings row must point to `GET /projects/:id/provider-readiness`, keeping MCP/tool adapters under provider policy, Agent tool grants, and runtime identity until a managed tool registry exists.
- Settings Vector Store exposes backend Evidence Index readiness instead of a fake editable control: `GET /projects/:id/evidence-index-readiness` must return `evidence-index-readiness/v1`, index local evidence searches, source snapshots, provider receipts, Agent submissions, and artifact storage proofs, and keep managed vector memory as a production blocker.
- Settings Budget Alerts exposes backend Budget Alert readiness instead of a fake editable control: `GET /projects/:id/budget-alert-readiness` must return `budget-alert-readiness/v1`, compute local daily budget and hourly request headroom from the project provider budget policy plus provider usage ledger, enter Manager Ready Package as `budgetAlertReadiness`, enter Readiness Proof Map as `budgetAlertReadinessRoutes`, and keep centralized alert routing/on-call ownership as production blockers.
- Settings Error Reporting exposes backend Error Reporting readiness instead of a fake editable control: `GET /projects/:id/error-reporting-readiness` must return `error-reporting-readiness/v1`, surface local log streams, alert rules, recovery runbook, incident-drill status, and error-signal counts from Operations Readiness, appear in Manager Ready Package as `errorReportingReadiness`, appear in Readiness Proof Map as `errorReportingReadinessRoutes`, and keep centralized logs/traces/incident ownership as production blockers.
- Settings provider/runtime sync must prefer project-scoped readiness when a project is active: `/projects/:id/settings-provider-readiness`, `/projects/:id/settings-runtime-readiness`, and `/projects/:id/provider-vault-bindings` should drive the visible BYOK/Vault state before falling back to global Settings routes.
- Settings provider readiness is backend-owned through `settings-provider-readiness/v1`: `/settings/provider-readiness` and `/projects/:id/settings-provider-readiness` must explain whether provider secret draft fields can be typed, whether `Seal` can run, what backend action is required, and which Vault/provider routes prove it. The Keys tab must keep API key/search endpoint draft entry locked until a backend target is saved, then keep `Seal` locked until the backend Vault is ready, while distinguishing unsynced status, unreachable backend status, and Vault-required setup instead of presenting an unsynced panel as a missing backend API. It must appear in Manager Ready Package as `settingsProviderReadiness` and in Readiness Proof Map as `settingsProviderReadinessRoutes`. The browser must not infer or persist provider secrets as local settings.
- Real-user agents:server API zero-to-autonomy proof on the real local backend: without launching a browser or running a build, the same validation must start `scripts/agent-project-server.mjs`, seal model key plus search endpoint/key through `/secret-vault/seal`, prove `/search/test`, create a product-team mission through `POST /product-team-missions`, record provider-backed Agent evidence, submit all required generic artifact types from discovery through final deliverable, request changes, submit linked revision, accept final deliverable, verify Artifact Quality Audit coverage, and verify Manager Flow Graph, Readiness Proof Map, Product Team Delivery Trace, Group Chat transcript, Agent Dashboard, Evidence Index, timeline, and event ledger traceability.
- Real-user Settings backend target entry plus provider seal and zero-to-autonomy browser proof on the real local backend: the same session must set the active backend API target through Settings Deployment, save model key plus search endpoint/key through backend Vault receipts, prove search can return evidence, approve kickoff, run the C/A handoff, expose an Agent submission node plus all required generic artifact nodes from discovery through final deliverable, verify Artifact Quality Audit coverage, and show those backend submissions in the Manager UI with Flow Graph, Proof Map, Product Team Delivery Trace, transcript, timeline, and event-ledger traceability.

`npm run agents:product-team:smoke` is the low-write core-chain smoke gate. It uses the in-memory backend API, does not launch a browser/server, and does not write workspace artifact files. It verifies the generic kickoff, canonical persona registry access, persona/professional skill-blend self-marketing, Leader/Reviewer selection, backend meeting turns, evidence search, all required generic artifact types (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`), requested-changes review, linked revision-note, accepted final-deliverable, Planner / Executor / Reviewer state machine, Manager Ready Package embedding, Manager Flow Graph, Readiness Proof Map, Manager governance/action Proof Map routes and Flow Graph nodes for Command Center, Scenario Trail, Scenario Walkthrough, Requirement Matrix, Sync Protocol Audit, Use Case Audit, and Action Queue, Brainstorm Layer, Artifact Quality Audit, Submission Review Workflow, Product Team Delivery Trace, transcript, timeline, event ledger, Agent Dashboard, and Evidence Index readiness. Use it before aggregate or browser gates when disk write volume matters; it does not replace the full file-backed, HTTP, or browser gates.

`npm run agents:product-team:local-mvp` is the current low-write aggregate proof for the local MVP product-team path. It runs startup readiness, the Research-as-sample generic product-team gate, Mission Runner HTTP startup/C/A handoff, marketplace Agent contract roster proof, Agent Workbench artifact/review/final-delivery proof, MVP readiness operator-action execution, zero-to-autonomy operator report validation, Private MVP Launch Package validation, and Launch Operations Private MVP visibility in sequence. Use it when the question is whether the current local MVP core can start, hand off to Agents, contract a marketplace Agent into the roster with proof, produce generic product-team outputs, prove Agent Workbench submissions through review closure, prove Flow Graph / Proof Map coverage, produce a customer/investor-readable private-MVP package, and show that package boundary inside Manager Launch Operations without paying the browser/build or full production-hardening rehearsal cost.

`npm run agents:product-team:core` is the stable default alias for that local MVP product-team proof. `npm run agents:product-team:core:file-backed` preserves the older staged file-backed core runner for broader persistence, security-boundary, provider-readiness, and worker-queue coverage when that heavier proof is explicitly needed.

`npm run agents:transcript-contracts` is the low-write Group Chat aggregate gate. It runs backend transcript search, channel pin, message pin, reply, mention, attachment, and member-presence contracts in one command, proving collaboration controls write or read backend transcript receipts instead of browser-local UI state.

`npm run agents:transcript-search` is the low-write Group Chat transcript search gate. It uses the in-memory backend API to create a generic product-team mission, calls `GET /projects/:id/transcripts/search?query=...&channelId=main`, verifies `transcript-search/v1` result routes and proof ids, and confirms empty searches stay empty instead of synthesizing browser matches. It does not launch a browser/server or write workspace artifact files.

`npm run agents:transcript-channel-pin` is the low-write Group Chat channel pin gate. It uses the in-memory backend API to create a generic product-team mission, pins the main channel through `POST /projects/:id/transcripts/main/channel-pin`, verifies `transcript-channel-pin/v1`, channel pin message proof, channel `channelPins`, Readiness Proof Map routes, and Manager Flow Graph channel pin nodes without launching a browser/server or writing workspace artifact files.

`npm run agents:transcript-pin` is the low-write Group Chat transcript pin gate. It uses the in-memory backend API to create a generic product-team mission, pins a real backend transcript message through `POST /projects/:id/transcripts/main/pins`, verifies `transcript-pin/v1`, timeline/event proof, channel `pinnedMessages`, Readiness Proof Map routes, and Manager Flow Graph pin nodes without launching a browser/server or writing workspace artifact files.

`npm run agents:transcript-reply` is the low-write Group Chat transcript reply gate. It uses the in-memory backend API to create a generic product-team mission, replies to a real backend transcript message through `POST /projects/:id/transcripts/main/replies`, verifies `transcript-reply/v1`, appended reply message proof, channel `replies`, Readiness Proof Map routes, and Manager Flow Graph reply nodes without launching a browser/server or writing workspace artifact files.

`npm run agents:transcript-mention` is the low-write Group Chat transcript mention gate. It uses the in-memory backend API to create a generic product-team mission, mentions a target Agent from a real backend transcript message through `POST /projects/:id/transcripts/main/mentions`, verifies `transcript-mention/v1`, appended mention message proof, target Agent inbox delivery, channel `mentions`, Readiness Proof Map routes, and Manager Flow Graph mention nodes without launching a browser/server or writing workspace artifact files.

`npm run agents:transcript-attachment` is the low-write Group Chat transcript attachment gate. It uses the in-memory backend API to create a generic product-team mission, attaches a validation file through `POST /projects/:id/transcripts/main/attachments`, verifies `transcript-attachment/v1`, content checksum proof, appended file message, channel `attachments`, Readiness Proof Map routes, and Manager Flow Graph attachment nodes without launching a browser/server or writing workspace artifact files.

`npm run agents:transcript-member-presence` is the low-write Group Chat member presence gate. It uses the in-memory backend API to create a generic product-team mission, reads `GET /projects/:id/transcripts/main/members`, verifies `transcript-member-presence/v1`, Agent receipt/authored counts, Readiness Proof Map routes, Manager Flow Graph presence nodes, and React source replacement without launching a browser/server or writing workspace artifact files.

`npm run agents:real-user-zero-to-autonomy` is the low-write real-backend zero-to-autonomy gate. It starts the real `agents:server` process with Secret Vault plus local mock model/search endpoints, seals model key plus search endpoint/key through `/secret-vault/seal`, proves `/search/test`, verifies `/local-mvp-startup-readiness` reports `readyForFirstProjectRun=true` and routes the user into `/product-team-missions`, verifies project-scoped Settings provider/runtime/integration readiness routes after project creation, then drives the generic product-team mission, binds a real local/private MVP workspace through `/projects/:id/workspace/bind`, reads it back through `/projects/:id/local-runtime`, writes and reads a workspace proof file, provider-backed Agent evidence, Reviewer source decisions for every provider-backed source, all required generic artifact types (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`), requested-change review, linked revision-note, accepted final-deliverable, Artifact Quality Audit, ready Project Evidence Archive storage/workspace proof coverage, Flow Graph, Proof Map, Product Team Delivery Trace, Group Chat transcript, timeline, event ledger, Agent Dashboard, and Evidence Index readiness through HTTP API calls. Use it before the browser `ui:real-user-zero-to-autonomy` gate when disk write volume matters; it does not replace the full browser proof.

`npm run agents:real-user-zero-to-autonomy-report` runs that same real-backend API chain and emits `real-user-zero-to-autonomy-operator-report/v1`: a redacted stage report for Settings/BYOK seal, startup readiness, kickoff/self-marketing, workspace binding, C/A handoff, provider evidence/source review, brainstorm/draft/review/revision/final delivery, generic artifact coverage, Manager proof surfaces, Project Evidence Archive, proof routes, provider usage proof rows for search and model calls, provider receipt proof rows, counts, and public-production blocker categories. The backend project must also expose `GET /projects/:id/zero-to-autonomy-report` as `project-zero-to-autonomy-report/v1`; its summary must include `providerUsageCount` and `providerReceiptCount`, and Manager Ready Package, Manager Flow Graph, Readiness Proof Map, and Manager UI must consume that route instead of inventing a browser-local overall readiness row. The Proof Map route must carry provider usage proof ids, provider receipt proof ids, and provider audit routes to Provider Readiness, Evidence Source Review Workflow, and Evidence Custody Readiness. `npm run agents:real-user-zero-to-autonomy-report:validate` proves the report stays local-MVP/private-pilot oriented, keeps `readyForPublicProduction: false`, covers all required generic stages, artifact types, provider usage proof, and provider receipt proof, and does not print validation secrets or vault ciphertext.

`npm run agents:private-mvp-launch-package` emits `private-mvp-launch-package/v1`, a customer/investor-readable package that combines the real-user zero-to-autonomy operator report with the public-production no-go operator report. Its expected ready status is `private-mvp-ready-public-production-blocked`: local backend MVP and controlled private-pilot rehearsal can be demonstrated with proof routes, while public production remains no-go. The package lists allowed claims, forbidden claims, stage rows, generic artifact coverage, provider usage/receipt proof counts, top public-production blocker actions, and validation commands. `npm run agents:private-mvp-launch-package:validate` proves the package preserves generic product-team positioning, includes every required zero-to-autonomy stage and generic artifact type, includes the public no-go validation command, and must not be used as public-production approval. `npm run agents:launch-operations:private-mvp` then proves `launch-operations-overview/v1`, Manager Ready Package, and Readiness Proof Map expose that same Private MVP package boundary for Manager UI consumption.

`npm run agents:product-team:research-sample` is the fastest explicit Research Project validation-sample gate. It runs the same low-write in-memory generic product-team backend chain as the core smoke gate, statically guards the Research-as-sample contract, then proves kickoff, self-marketing, brainstorm, evidence, Reviewer source decisions with no pending source-review queue, draft, review/revision, final deliverable, Flow Graph, Proof Map, Manager governance/action Proof Map routes and Flow Graph nodes, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, ready Project Evidence Archive handoff, artifact storage/workspace proof, and production blockers rather than paper/thesis/manuscript-specific protocol fields. Use `npm run agents:product-team:cycle-consistency` for the separate three-step autonomous consistency proof.

Product-team acceptance stages write isolated temporary stores under `.tmp/product-team-acceptance/<stage>-<pid>` while they run, then clean the run directory by default, including normal interruption signals. Use `HOFS_PRODUCT_TEAM_PRESERVE_TMP=1` only for debugging a failed stage; the normal P0/P1 gates should not leave large acceptance stores behind.

The stage runner defaults to quiet output for low-write validation, including UI harnesses that prepare product-team acceptance stores before opening the browser. Set `HOFS_PROGRESS=1` only when debugging a long-running acceptance stage and needing per-step progress on stderr.

P2 adapter gateway rehearsals clean their gateway/project temp stores by default, including normal interruption signals. Use `HOFS_ADAPTER_GATEWAY_PRESERVE_TMP=1` only when debugging a failed adapter gateway rehearsal.

`npm run agents:product-team:cycle-consistency` is the focused P0 autonomy consistency gate. It continues past the Research sample into signed membership access, runs a bounded three-step autonomous loop, and verifies `autonomous-cycle-consistency/v1`, Manager Flow Graph, Readiness Proof Map, Manager Ready Package, and persisted loop/run receipts without running the full P1/P2 launch-hardening receipt chain.

`npm run ui:manager-provider-proof` is the focused C-side proof gate for provider-backed autonomous evidence. It starts a real local backend with deterministic provider search, advances bounded Autopilot through the scheduler, verifies the provider evidence receipt, opens the Manager Flow Graph node, and clicks back into the backend Group Chat transcript proof.

`npm run ui:settings-agents-server` is the focused Settings gate for the real local backend process. It builds the React UI, starts `npm run agents:server` with Secret Vault env on an ephemeral port, seeds a backend project, syncs the project catalog, selects that project in the UI, then verifies Settings as a backend-backed surface. The browser gate seals model key plus search endpoint/key through `/secret-vault/seal`, verifies `/secret-vault/records` exposes only encrypted metadata, checks Health exposes `/settings/health-readiness`, `/local-mvp-startup-readiness`, and `/projects`, checks Deployment/Models read `settings-runtime-readiness/v1`, checks Integrations reads `/projects/:id/settings-integration-readiness` and its route-backed rows, checks Workspace reads `/projects/:id/memory-readiness` plus `/projects/:id/meeting-summaries`, changes Workspace/Privacy/Budget/Tool Grant controls in the UI, polls `/projects/:id/project-settings` until those changes are durable, and proves `/search/test` can call a user-configured local search gateway. It also verifies the persisted provider budget feeds `/projects/:id/provider-controlled-run` and that project settings writes create timeline plus event-ledger proof. This catches the customer-facing failure mode where Settings appears editable but is not actually tied to a selected project and backend proof routes.

`npm run ui:settings-agents-server:dev` runs the same Settings browser proof against an already running Vite dev server at `http://127.0.0.1:5173` through `--ui-base-url`. Use it while replacing Settings mocks because it skips `vite build`; it does not replace the built `ui:settings-agents-server` launch gate.

`npm run ui:manager-mission-runner` is the focused C/A startup gate for real project creation. It starts its local backend with Secret Vault plus local model/search provider fixtures so the `/local-mvp-startup-readiness` gate is ready before the first approval, drives the browser through kickoff meeting, Manager clarification, Leader and next-action decisions, then approves through `POST /product-team-missions` and verifies the reused-kickoff Mission Runner receipt, Autopilot tick, chat proof, timeline proof, and Manager Flow Graph mission node. Use it when changing initiation approval, Mission Runner receipts, proof exits, or frontend mock replacement around project startup.

`npm run ui:real-user-zero-to-autonomy` is the stronger investor/customer-facing browser gate. It starts the real `agents:server` process with Secret Vault plus local mock model/search endpoints, opens the built React app, seals model key plus search endpoint/key in Settings, proves the search provider returns evidence, verifies Workspace Hub shows `workspace-local-mvp-startup-readiness`, reads `/local-mvp-startup-readiness` directly to require `readyForFirstProjectRun=true` and `nextAction=/product-team-missions`, then starts from Workspace Hub, holds the kickoff meeting, approves the project, verifies project-scoped Settings provider/runtime/integration readiness through backend API, visible Settings UI, and Readiness Proof Map routes, binds a local/private MVP workspace from Settings Workspace through `/projects/:id/workspace/bind` and `/projects/:id/local-runtime`, runs the C/A handoff intent from the standalone Manager Dashboard Collaboration Intent Queue before requiring a full Manager Ready Package sync, records provider-backed Agent evidence through the same configured endpoint, records Reviewer source decisions for every provider-backed source, creates all required generic artifact types (`discovery-report`, `brainstorm-board`, `evidence-packet`, `product-brief`, `decision-proposal`, `risk-review`, `revision-note`, `implementation-plan`, `final-deliverable`), records requested-change and final-acceptance reviews, and verifies those backend-created nodes are traceable and visible through Manager UI submissions, delegated Agent Submission output, Artifact Quality Audit, ready Project Evidence Archive storage/workspace proof readback, Manager Flow Graph, Readiness Proof Map, Project Memory Readiness, Product Team Delivery Trace, Group Chat transcript, timeline, and event ledger without relying on frontend mock state. The same gate fails on duplicate React key warnings so long-running Manager views cannot silently duplicate or drop backend proof rows.

`npm run ui:real-user-zero-to-autonomy:dev` runs the same browser script against an already running Vite dev server at `http://127.0.0.1:5173` through `--ui-base-url`. Use it as a lower-write preflight while replacing mocks because it skips `vite build`; it does not replace the built `ui:real-user-zero-to-autonomy` launch gate.

`npm run ui:manager-backend:core:dev` runs the Manager backend core browser gate against an already running Vite dev server at `http://127.0.0.1:5173` through `--ui-base-url`. Use it as a lower-write C/A control-loop preflight for sample backend adoption, Autonomous Run Control, Agent autonomous action receipts, and Autopilot scheduler receipts; it does not replace the built `ui:manager-backend:core` launch gate.

`npm run ui:manager-backend:real-user-chain:dev` is the same lower-write preflight under the Manager backend validation family. It reuses the zero-to-autonomy agents-server UI harness and the same `--ui-base-url=http://127.0.0.1:5173` target, so C/A Manager-surface work can validate the real-user chain without another build while the built `ui:manager-backend:real-user-chain` gate remains the launch evidence.

`npm run ui:mock-boundaries` is the static frontend mock-replacement guard. It skips browser/build work and checks that real backend projects fail closed for missing transcript/timeline proof, browser seed paths stay limited to sample/dev fallback, and backend-required/source-label states remain present for key Manager and Agent panels.

`npm run agents:server` is the local backend process used by the React UI for real backend-online work. For Settings provider entry, it must be started with `SECRET_VAULT_ENABLED=true` and `SECRET_VAULT_KEY` so `/secret-vault/status` reports ready and the Settings Keys tab can seal model/search secrets through `/secret-vault/seal`. `npm run agents:server:validate` proves the model-key startup path by launching the server on an ephemeral port, sealing a test key, checking safe record metadata, verifying runtime provider binding, restarting against the encrypted records file, verifying provider rehydration, and deleting its temporary store.

`npm run agents:local-mvp-startup-readiness` is the low-write backend startup contract gate. It reads `GET /local-mvp-startup-readiness` with and without a Secret Vault, verifies the response moves from `backend-vault-required` to `provider-setup-required`, confirms the route links `/settings/provider-readiness`, `/secret-vault/status`, `/secret-vault/seal`, `/llm/status`, `/search/status`, `/projects`, and `/product-team-missions`, confirms Manager Ready Package and Readiness Proof Map include startup readiness, and checks startup readiness never exposes plaintext provider secrets or public-production readiness.

`npm run agents:public-production-startup-readiness` is the low-write public production startup contract gate. It reads `GET /public-production-startup-readiness`, verifies the response exposes `public-production-startup-readiness/v1`, confirms current local/private-MVP infrastructure remains blocked for public production, checks concrete blocker gates for access control, managed identity, production cost controls, production data governance, production traffic/rollback controls, customer production acceptance policy, managed secrets, persistence, queue, observability, incident response, restore drill, and audit retention, verifies Settings runtime, Manager Ready Package, Production Launch Control Center, Manager Flow Graph, Readiness Proof Map, and Manager UI link the route, and checks plaintext provider secrets or production env values are never exposed.

`npm run agents:managed-infrastructure-cutover-attestations` is the low-write backend bridge gate for real managed infrastructure evidence. It verifies `POST /projects/:id/managed-infrastructure-cutover-attestations` returns `managed-infrastructure-cutover-attestation-run/v1`, fails closed without a configured adapter gateway, refuses to write receipt evidence before the gateway has query-bound readback parity plus project dry-run receipt evidence, then uses the private adapter gateway to issue signed managed-production attestations for `managed-persistence-cutover` and `managed-worker-queue-cutover`. The route writes those attestations into `production-operations-control-receipt/v1`, projects them into `production-infrastructure-rehearsal/v1`, and lets `production-evidence-integrity-audit/v1` classify those two controls as managed-production evidence while keeping broader public production blocked.

`npm run agents:settings-health-readiness` is the low-write Settings health contract gate. It reads `GET /settings/health-readiness` with and without a Secret Vault, verifies Quick Check rows come from the backend contract, confirms Manager Ready Package and Readiness Proof Map include Settings health readiness, checks provider setup remains action-required until keys are sealed, and confirms plaintext provider secrets and public-production readiness are never exposed.

`npm run agents:settings-runtime-readiness` is the low-write Settings runtime contract gate. It reads `GET /settings/runtime-readiness` and `GET /projects/:id/settings-runtime-readiness` with and without a Secret Vault, verifies Deployment/Models rows come from the backend contract, checks provider-vault metadata remains redacted after sealing a model key, verifies persistence/worker queue adapter and deployment-preflight route linkage, confirms Manager Ready Package / Readiness Proof Map inclusion, and confirms public-production readiness is never exposed.

`npm run agents:model-provider-adapter` is the low-write model-provider adapter contract gate. It verifies `model-provider-adapter-manifest/v1`, confirms the local backend can select OpenAI-compatible, OpenAI, Anthropic, and Gemini adapter styles through the shared `createModelProvider` boundary, proves OpenAI-compatible chat-completion request/response mapping with a local fake fetch, checks missing-key calls fail closed without network access, and verifies provider status never exposes plaintext keys.

`npm run agents:settings-provider-readiness` is the low-write Settings provider readiness contract gate. It calls `/settings/provider-readiness` with and without a local Secret Vault, verifies the backend tells the UI when provider secret drafts can be typed, when `Seal` is locked by missing Vault readiness, and when Seal is enabled after Vault readiness, verifies project-scoped `/projects/:id/settings-provider-readiness`, confirms Manager Ready Package / Readiness Proof Map inclusion, and checks the response never exposes plaintext provider secrets.

`npm run agents:settings-integration-readiness` is the low-write Settings integration readiness gate. It calls `/projects/:id/settings-integration-readiness`, verifies `settings-integration-readiness/v1` aggregates provider budget, Agent tool grants, Adapter Gateway preflight, Provider Readiness, Evidence Index readiness, Budget Alert readiness, and Error Reporting readiness, confirms Manager Ready Package and Readiness Proof Map inclusion, checks the project settings capability route points to the aggregate, and confirms public-production readiness is never exposed.

`npm run agents:settings-contracts` is the low-write Settings backend aggregate gate. It runs local MVP startup readiness, Settings health, runtime, provider-readiness, integration-readiness, search endpoint/key Vault binding, privacy policy, provider budget, Agent tool grants, integration capability, and workspace capability contract gates in one sequence. Use it before heavier browser checks when changing Settings because it proves the backend receipts, provider startup path, read models, proof routes, and production blockers are still aligned without launching Vite or Playwright.

`npm run agents:evidence-index-readiness` is the low-write Evidence Index readiness gate. It starts with an empty backend project, verifies the index is not ready, records one Agent evidence search plus one Agent artifact submission, then verifies `/projects/:id/evidence-index-readiness` becomes locally ready, links evidence/search/submission routes, redacts source URL tokens, and still keeps managed vector storage production-blocked.

`npm run agents:budget-alert-readiness` is the low-write Budget Alert readiness gate. It creates a backend project, writes `project-provider-budget-policy/v1`, verifies `/projects/:id/budget-alert-readiness` returns `budget-alert-readiness/v1`, confirms daily budget and hourly request headroom are computable from backend state, confirms the Settings capability row points to the route, confirms Manager Ready Package and Readiness Proof Map expose the same budget alert proof, and keeps production alert routing blocked.

`npm run agents:error-reporting-readiness` is the low-write Error Reporting readiness gate. It creates a backend project, verifies `/projects/:id/error-reporting-readiness` returns `error-reporting-readiness/v1`, confirms local log streams, alert rules, recovery runbook, and incident-drill status are route-backed from Operations Readiness, confirms Manager Ready Package and Readiness Proof Map include the same readiness route, confirms the Settings capability row points to the route, and keeps centralized production observability blocked.

`npm run agents:search-provider:vault-endpoint` is the focused search-provider configuration gate. It starts the real local backend with no search endpoint in env, starts a local mock search gateway, seals `search.endpoint` and `search.apiKey` through `/secret-vault/seal`, proves `/search/status` becomes `http-json` and vault-backed, calls `/search/test`, verifies the mock gateway received the sealed key, restarts the backend against the encrypted Vault records, and confirms endpoint/key rehydration. This is the minimum proof that Settings can turn search from "saved but unusable" into a real provider boundary.

`npm run agents:project-settings:privacy` is the focused Settings privacy gate. It creates a backend project, writes language through `project-settings/v1`, then writes a privacy-only policy update and verifies language preservation, privacy policy persistence, project settings audit, timeline proof, event-ledger proof, and file-backed store persistence. It is local MVP evidence only; production privacy still requires the P2 security, operations, provider, and evidence-export controls.

`npm run agents:project-settings:provider-budget` is the focused Settings budget gate. It writes `project-provider-budget-policy/v1` through `project-settings/v1`, verifies project settings audit/timeline/event/file-store proof, then reads Provider Readiness and Provider Controlled Run to prove the daily budget and hourly request limit are consumed by the backend provider policy surfaces. It is local MVP budget enforcement evidence only; production cost alerting, provider incident controls, and managed usage audit remain P2 blockers.

`npm run agents:project-settings:tool-grants` is the focused Settings tool-grant gate. It writes `project-tool-grant-policy/v1` through `project-settings/v1`, verifies project settings audit/timeline/event/file-store proof, then reads Provider Readiness and Provider Controlled Run to prove a removed `search:evidence` grant is denied as `agent-tool-grant-missing`. It is local MVP Agent tool authorization evidence only; production still requires managed runtime identity, task-scoped grants, and centralized provider audit.

`npm run agents:local-tool-grants` is the focused temporary authorization gate. It removes baseline `search:evidence`, proves denial before transport, creates one Agent/operation/task-scoped `local-tool-grant-lease/v1`, consumes exactly one Provider attempt, verifies exhaustion and restart persistence, and validates the redacted previous-checksum `local-tool-invocation-receipt/v1` chain. It proves bounded single-machine authorization without claiming hardware-backed immutability or cross-host IAM.

`npm run agents:prompt-boundary` is the focused untrusted-data isolation gate. It records safe and injected evidence, proves critical injection is blocked by Evidence Source Safety, captures a real local model request, verifies safe evidence is citation-addressed under `UNTRUSTED_DATA`, verifies malicious instructions and fake secrets are physically absent, and checks the content-free `local-prompt-boundary-receipt/v1` through API, Manager Ready Package, file persistence, and restart.

`npm run agents:action-approvals` is the focused high-cost and irreversible action gate. It proves the backend derives critical deletion policy rather than accepting caller downgrades, requires distinct Manager and security-admin decisions, rejects unapproved execution, recovers the approved record with valid checksums after file-store restart, claims the exact deletion request before purge, and preserves request/decision/execution-claim proof in the residual-boundary tombstone. This is a single-machine local approval control; it does not claim remote identity federation, hardware-backed non-repudiation, or a cloud approval inbox.

`npm run agents:quality-evaluation` is the focused versioned work-quality regression gate. The backend-owned `local-quality-evaluation-suite/v1` derives one deterministic contract scenario from each of the learning, academic-writing, investigation, technical-delivery, and creative-studio modes. It records candidate/model/prompt/policy version ids, compares criterion-level results with a Manager-approved passing baseline, blocks any pass-to-fail regression even when aggregate scores look healthy, verifies content-free SHA-256 receipts after file-store restart, and keeps `readyForProduction=false`. It evaluates observable team/artifact/review/escalation/evidence contracts; semantic correctness, subjective quality, calibrated human preference, and real-provider output judging remain separate release work.

`npm run agents:model-degradation` is the focused model-output truthfulness gate. It forces every artifact draft into exactly one backend-derived mode: successful `model-provider-output`, failed requested-model `requested-model-fallback`, or deliberate `explicit-local-template`. Requested fallbacks expose only closed reason codes, receive a degraded quality ceiling, and cannot accept or complete themselves even if the caller supplies those states. Content-free `local-model-generation-provenance/v1` receipts retain only ids, quality/release metadata, Provider/model identifiers and SHA-256 proof; Provider error bodies, prompts, outputs, project content and credentials are excluded. The gate proves failed transport, forced independent review, file-store restart, successful local-model transition, and `readyForProduction=false`.

`npm run agents:team-formation` is the focused roster-explainability gate. It covers all five work modes, proving each required role names its capability lane, chosen persona, score, selection rationale, owned/reviewed artifacts and escalation responsibility. The content-minimized `local-team-formation-brief/v1` stores only objective checksum/length and closed need-signal ids, fails closed for insufficient persona supply, and requires full coverage, reviewer independence, an acyclic dependency graph and score >=75 before `delegationReady=true`. Project readback at `GET /projects/:id/team-formation-readiness` keeps the checksum stable after restart without creating another persona source of truth.

`npm run agents:delegation-governance` is the focused operational-delegation gate. It proves a visualization-ready task DAG, simultaneous overdue and dependency-blocked states, accountable owner/reviewer/due-date changes, local project-feed notifications, state-fingerprint deduplication, observer read-only access, file-store restart recovery, checksum tamper detection, and exclusion of task text from governance receipts. The local routes are `GET /projects/:id/delegation-governance`, `POST /projects/:id/tasks/:taskId/delegation`, and `POST /projects/:id/delegation-governance/scan`; no cloud notification service is required.

`npm run agents:shared-memory` is the focused project-knowledge governance gate. It persists cited local memory as immutable `local-project-memory-entry/v1` versions, requires SHA-256-bound project sources, confidence and basis, explicit bounded expiry, and project/team/management/Agent access scope. Revision uses optimistic previous-checksum concurrency; revocation is a separate immutable receipt; identity-filtered reads bypass shared caches. The gate proves Manager and Agent visibility differences, idempotency conflicts, stale revision rejection, expiry, revocation, restart, tamper degradation, and additive `project-memory-readiness/v1` evidence through `GET|POST /projects/:id/shared-memories` without claiming hosted vector or managed production memory.

`npm run agents:review-handoff` is the focused independent-review concurrency gate. It converts every work mode's acceptance-check ids into explicit handoff criteria, binds the handoff to a content-free submission SHA-256 fingerprint, requires a different team Reviewer to acknowledge and claim a bounded lease, increments a fence after lease expiry, rejects stale fences and changed submissions, and accepts only when all required criteria carry proof ids. Completion invokes the real `reviewAgentSubmission` path and binds the resulting review hash; overdue scans create one local escalation receipt. `submission-review-workflow/v1` reports governed versus legacy review rounds without pretending compatibility reviews had lease protection.

`npm run agents:autonomy-governor` is the focused pure-local autonomy control gate. It creates an append-only versioned policy for each project, binds pause/resume/stop commands to the expected policy version and checksum, fences every active project session, and makes stop terminal. The common execution assertion covers unified run-control actions and loops, session start/tick, direct Agent autonomy, and Provider-evidence Agent autonomy; `force=true` cannot bypass it. The five work modes must jointly reject projected wall-clock, step, cost, tool-count, and tool-allowlist violations, while file-backed restart proves the paused/stopped state and SHA-256 receipts remain authoritative.

`npm run agents:learning-program` is the focused student-learning execution gate. A learning project converts a prerequisite syllabus and diagnostic evidence into a bounded local schedule that respects weekly minutes, session length, study days, blackout dates, target date, and target mastery. Practice receipts retain scores, duration, hints, evidence ids and checksums but reject raw learner answers. Mastery requires three recent hint-free passing attempts plus mastered prerequisites; the read model then unlocks the next weak topic or schedules a spaced review when evidence ages. Pace revisions use optimistic plan version/checksum concurrency and preserve compatible prior evidence across file-store restart. Academic-integrity and learner-wellbeing remain explicit capability-42 gates rather than being silently claimed complete here.

`npm run agents:teaching-safety` is the focused local tutoring authorization gate. It derives closed risk signals from transient request text and persists only its SHA-256, length, trusted activity type, evidence ids, reason codes, response authorization, and checksum. Coarse age bands constrain reading level, session length, hints, and child supervision without collecting birth dates. Assignment review requires a real same-learner attempt receipt; current/external facts require a completed source-safe project evidence record. Assessment answers, cheating concealment, child personal data, and urgent wellbeing signals are hard stops. Human resolutions require evidence and never convert the blocked decision into teaching-content authorization. The gate proves restart and raw-text absence but makes no legal, clinical, emergency-response, guardian-contact, or school-policy compliance claim.

`npm run agents:academic-writing-pipeline` is the focused local paper-production gate. An academic-writing project binds a hashed research question, acyclic section plan, claim graph, style guide, and real completed local evidence records into a versioned blueprint. Manuscript drafts and revisions must be genuine `academic-manuscript` submissions with artifact and storage-proof checksums. The assigned reviewer must be independent; each requested change becomes a stable issue id, every issue must be addressed by a newer submission, and only an accepted latest revision with full section and claim coverage can be frozen. Receipts omit the research question, claim prose, and manuscript body, survive file-store restart, and degrade on checksum tampering. Finalization deliberately remains `finalized-awaiting-citation-integrity`: semantic citation support, contradiction, retraction, and staleness checking belong to capability 44.

`npm run agents:citation-integrity` is the focused local semantic citation gate. Every planned claim/source pair must resolve through the capability-43 blueprint to a real project `evidence-source-snapshot/v1`, then receive a checksum-linked assessment from the blueprint's independent citation reviewer. Assessments use closed support and source-status vocabularies, publication/status-check timestamps, locators, and hashes of evidence excerpts and rationales; raw excerpts and rationale text are not persisted. The immutable audit automatically emits blocking findings for missing, irrelevant, uncertain, contradictory, stale, corrected, retracted, or unavailable citations and for claims without active supporting evidence. A corrected assessment appends a version/checksum link rather than overwriting history, and a pass binds the exact final manuscript before the academic pipeline reports `citation-integrity-passed`. This is deterministic local governance over recorded review evidence; it does not query a live retraction registry, determine truth autonomously, detect plagiarism, or make the manuscript publicly production-ready.

`npm run agents:investigation-case` is the focused local case-workflow gate. Investigation-mode roster roles own a versioned, content-minimized case with at least two competing hypotheses. Case evidence must bind a real checksummed source snapshot and the risk reviewer's approved source decision, then records five bounded reliability dimensions and explicit support/contradiction strength against known hypotheses. Acquisition starts an evidence-specific custody chain; verify, transfer, and independent seal events are sequential, time-monotonic, actor-attributed, checksum-linked, and terminal after seal. Opposing evidence automatically creates stable contradiction ids, all require independent resolution, and conclusion confidence is derived from reliability-weighted support/contradiction plus the declared prior rather than caller input. Closure binds the exact conclusion, locks later writes while preserving historical idempotent replay, survives file restart, and degrades on source, resolution, evidence, custody, conclusion, or closure tampering. It is a local traceability/control contract, not legal chain-of-custody, admissibility, investigative authority, live truth verification, or public-production certification.

`npm run agents:investigation-safety` is the focused mandatory investigation authorization gate. A case-bound versioned policy declares one closed authority basis, validated authority evidence, an allowed data-category envelope, retention, and a short decision TTL while retaining non-configurable hard stops. Transient request classification persists only hashes, category/signal ids, target ids, case counts, reason codes, expiry, and checksums. Rule priority fails closed for credentials, doxxing, stalking, impersonation, retaliation, covert surveillance, minors, sensitive categories, undeclared PII, out-of-scope data, missing authority, public-record misuse, external effects, and insufficient/unsealed/contradictory evidence. Human disposition never authorizes the blocked operation; a corrected request must be reevaluated. Allowed decisions are exact-action/target, time-monotonic, unexpired, one-time receipts committed atomically with the evidence, conclusion, or closure write. Capability 45's full API path consumes these decisions, so no legacy write bypass remains. This local policy does not validate lawfulness, consent authenticity, regulatory compliance, or investigative authority.

`npm run agents:project-settings:integrations` is the focused Settings integration capability gate. It writes project settings, verifies `project-integration-capabilities/v1`, confirms provider budget and Agent tool grants are backend-backed editable controls, confirms Proxy/Webhook points to `/projects/:id/adapter-gateway-preflight`, confirms MCP Tools points to `/projects/:id/provider-readiness`, confirms Vector Store points to `/projects/:id/evidence-index-readiness`, confirms Budget Alerts points to `/projects/:id/budget-alert-readiness`, confirms Error Reporting points to `/projects/:id/error-reporting-readiness`, and confirms those five route-backed rows are backend-backed but read-only with production blockers. It prevents the UI from presenting those integrations as either editable fake controls or missing backend APIs.

`npm run agents:project-settings:workspace` is the focused Settings workspace capability gate. It writes project language plus `project-workspace-policy/v1` through `project-settings/v1`, verifies `project-workspace-capabilities/v1`, confirms global interface language is browser-local, confirms project language/interface density/default visibility/autosave cadence are backend-backed, confirms local workspace binding is backend-backed through `POST /projects/:id/workspace/bind` plus `GET /projects/:id/local-runtime`, confirms runtime contract rules are backend-backed and read-only through `GET /projects/:id/runtime-contracts`, confirms long-term memory readiness is backend-backed and read-only through `GET /projects/:id/memory-readiness`, confirms the memory read model appears in Manager Ready Package and Readiness Proof Map, confirms meeting summaries are backend-backed through `GET /projects/:id/meeting-summaries`, and verifies audit, runtime-contract, memory-readiness, transcript-derived meeting summary, timeline, event-ledger, and file-backed persistence proof. It prevents the UI from presenting unfinished workspace controls as editable fake settings.

`npm run agents:timeline-action` is the low-write Timeline action contract gate. It creates a generic backend product-team project, posts both Manager note and acknowledgement actions to `POST /projects/:id/timeline/actions`, verifies `timeline-action-receipt/v1`, redaction for notes, deferred timeline/event refresh routes, persisted timeline logs, event-ledger rows, receipt anchors, and Manager Flow Graph timeline node coverage. Use it when replacing timeline-detail controls or proof-review UI because it proves the Manager can annotate and acknowledge a proof node without creating browser-only state.

`npm run agents:agent-workbench-contract` is the low-write Agent Workbench contract gate. It uses the in-memory backend API to simulate the customer-visible Workbench path: provider evidence search, all required generic artifact submissions, generated product-brief draft submission, Reviewer requested changes, linked revision-note, final-deliverable, and accepted final review. It then verifies deferred read-model refresh routes plus transcript, timeline, event-ledger, Manager Flow Graph, Readiness Proof Map, and Submission Review Workflow coverage without launching a browser or writing workspace artifacts.

`npm run agents:agent-message-contract` is the low-write Agent-to-Agent message gate. It creates a generic backend product-team project, sends a targeted Agent message through `POST /projects/:id/agents/:agentId/message`, and verifies target inbox delivery, sender worklog proof, transcript readback, timeline/event proof, Manager Dashboard delivery rows, Readiness Proof Map `agentMessageRoutes`, Manager Flow Graph message node coverage, and the target Agent Dashboard without launching a browser.

`npm run agents:agent-contract` is the low-write marketplace Agent contract gate. It creates a generic backend product-team project, contracts Ada Lovelace through `POST /projects/:id/agents/contract`, and verifies `agent-contract/v1`, team roster mutation, Agent Dashboard worklog proof, timeline/event proof, Manager Flow Graph `agent-contracted` node coverage, and lightweight read-model refresh routes without launching a browser.

`npm run agents:manager-chat-command-contract` is the low-write Manager group-chat command gate. It posts a Leader assignment and a Google Chat change request through `POST /projects/:id/chat`, verifies lightweight read-model routes, then checks transcript, timeline, event-ledger, assignment matrix, change flow, Readiness Proof Map, Manager Flow Graph, and assignee Agent Dashboard readback without launching a browser.

`npm run agents:scenario:contract` is the low-write Manager scenario aggregate gate. It runs the Manager chat command, Agent-to-Agent message, marketplace Agent contract, Agent Workbench, and timeline-action contract slices so the core C/A scenario proof path can be checked without launching the long legacy `agents:scenario` regression.

`npm run launch:local-mvp:check` is the low-write release checklist for P0/P1 readiness metadata. It does not build, launch browsers, or replace the P0/P1 gates above. It verifies the required package scripts, script entry files, canonical persona Skill bridge, persona Skill entry files, launch-gate language, Settings/backend proof boundary, Settings no-op Save removal, Settings provider readiness wiring, and real-user zero-to-autonomy script wiring before a human spends time on the heavier gates.

P0 is not enough for a customer pilot because evidence handoff, launch receipts, customer acceptance, and operator controls are not required by this tier.

## P1 - Customer Private Pilot

P1 proves a controlled private-pilot handoff can close from both backend and browser paths. This is the current shortest gate for a customer-facing rehearsal.

Required commands:

```bash
npm run launch:private-pilot:check
npm run agents:product-team:private-pilot
npm run ui:manager-private-pilot
```

`npm run launch:private-pilot:check` is the one-command P1 release gate. It runs the backend private-pilot acceptance chain and then the built Manager private-pilot browser handoff gate. Use the two component commands only when isolating a backend or UI failure.

`npm run ui:manager-private-pilot:dev` runs the same Manager private-pilot browser handoff gate against an already running Vite dev server at `http://127.0.0.1:5173` through `--ui-base-url`. It now prepares the browser store through the focused file-backed private-pilot handoff contract, including provider eval, security audit, deployment preflight, launch approval readiness, and release-candidate prerequisites while leaving the browser to record the Manager approval/export/release receipts. Use it as a lower-write preflight while replacing private-pilot UI mocks because it skips `vite build`; it does not replace the built `ui:manager-private-pilot` or `launch:private-pilot:check` gates.

The Manager Ready Package Launch Operations Overview is the reader-facing P1/P2 status header backed by `GET /projects/:id/launch-operations-overview`. It must show Private MVP Launch Package status, private-pilot acceptance, launch approvals, evidence package/download audit, next action, public-production no-go, production blocker count, Public Production Next Steps, and proof routes from backend read models before the detailed launch cards. The next-step rows translate blockers into owner, action, why-blocked, validation-command, route, and run-route fields so a non-technical operator can see why public launch is still blocked. `POST /projects/:id/launch-operations-overview/public-production-next-steps/:stepId/run` records `launch-operations-next-step-run/v1` proof into timeline, event ledger, Manager Flow Graph, and Readiness Proof Map without promoting public production. `ui:manager-private-pilot`, `ui:manager-production-controls`, and `agents:launch-operations:private-mvp` must fail if that route/overview disappears, hides the package boundary, hides public-production next steps, drops the next-step receipt, or overclaims public production.

P1 assumes the P0 split browser gates have already passed. Do not rerun the long Manager backend chain for routine private-pilot signoff unless the change touched the legacy workbench flow directly.

Optional narrower gates when only one phase changed:

```bash
npm run agents:product-team:private-pilot:handoff-focused
npm run agents:product-team:private-pilot:release-focused
npm run agents:product-team:private-pilot:launch-acceptance-focused
npm run agents:product-team:private-pilot:focused
npm run agents:product-team:private-pilot:release
npm run agents:product-team:private-pilot:launch
npm run agents:product-team:private-pilot:health
npm run agents:product-team:private-pilot:acceptance
npm run agents:product-team:private-pilot:ops-readiness
```

`npm run agents:product-team:private-pilot:handoff-focused` is the low-write backend contract for daily handoff work. It runs an in-memory generic product-team chain through evidence archive, governed evidence export request, Manager/security approval, download audit, handoff package route, Manager Ready Package, Flow Graph, and Proof Map without replaying the full private-pilot release/launch/health/acceptance chain. It is a focused debugging gate and does not replace the full P1 release gate.

`npm run agents:product-team:private-pilot:release-focused` is the bounded file-backed backend contract for daily release-candidate work. It reuses the focused handoff chain with a temporary file store, Secret Vault, deterministic provider search/model fixtures, signed access audit, replay proof, membership policy, identity-session proof, provider eval, launch approvals, deployment preflight, and operations readiness, then freezes a `private-pilot-release-candidate/v1` and verifies Manager Ready Package, Flow Graph, and Proof Map traceability. It cleans its temporary store by default, remains a focused debugging gate, and does not replace the full P1 release gate.

`npm run agents:product-team:private-pilot:launch-acceptance-focused` is the bounded file-backed backend contract for daily launch, health, and customer-acceptance work. It reuses the focused release-candidate chain, records `private-pilot-launch-run/v1`, `private-pilot-launch-health-check/v1`, and `private-pilot-acceptance-report/v1`, then verifies Manager Ready Package, Flow Graph, and Proof Map traceability through customer-visible private-pilot acceptance. It cleans its temporary store by default, remains a focused debugging gate, and does not replace the full P1 release gate or browser Manager handoff gate.

`npm run agents:product-team:private-pilot:focused` is the low-write aggregate alias for the P1 backend focused chain. It points to the launch/health/customer-acceptance focused validator, which already imports the release-candidate and handoff focused fixtures, so daily P1 backend confidence can be checked with one command without replaying the browser handoff or the full staged acceptance runner.

Required product evidence:

- P0 evidence remains passing.
- Launch approval workflow has Manager and security-admin approval receipts.
- Project Evidence Archive is ready, redacted, checksummed, and safe for local handoff.
- Project Evidence Export has request, approval, and download-audit receipts.
- Private-pilot release candidate freezes the delivery, evidence, provider-eval, deployment, operations, persistence, and queue checksums.
- Private-pilot launch run, health check, and acceptance report all write timeline/event proof and appear in Flow Graph and Proof Map.
- Browser Manager UI can perform the same handoff through backend receipts instead of frontend mutation.

P1 still requires explicit operator supervision. It does not approve public production, unmanaged sensitive data, or 24/7 unattended production operation.

`npm run agents:product-team:private-pilot:ops-readiness` is the focused bridge from P1 into production hardening. It stops after customer acceptance and verifies `production-operations-readiness/v1`, proving local/private-pilot operations proof is closed while centralized logs, metrics, traces, alert routing, on-call ownership, managed incident records, restore drills, centralized audit retention, and managed database/queue cutover remain explicit P2 blockers. That model also exposes `production-operations-managed-production-evidence/v1`, so a complete local/test operations receipt set is visible as receipt-complete but still blocked as managed-production evidence until signed control-plane attestations exist. Private-pilot release, launch, health, acceptance, and production-control write routes support lightweight receipt responses with deferred read-model refresh routes, including production infrastructure rehearsal, operations readiness, Flow Graph, Proof Map, transcript, timeline, and event routes, so the Harness and React can validate the handoff without repeatedly embedding the full Manager Ready Package.

Latest P1 confirmation on 2026-07-05: `npm run agents:product-team:private-pilot` and `npm run ui:manager-private-pilot` both pass in the current workspace. This verifies the backend private-pilot receipt chain plus the browser Manager handoff path for launch approvals, evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Flow Graph, and Readiness Proof Map. It remains a supervised private-pilot rehearsal, not public production approval.

Latest P1 focused backend confirmation on 2026-07-07: `npm run agents:product-team:private-pilot:focused` passes in the current workspace and is equivalent to the launch-acceptance focused chain, which imports the release-candidate and handoff focused fixtures. This verifies the low-write backend handoff, evidence export/download audit, provider eval, release candidate, controlled launch run, post-launch health, customer acceptance report, Manager Ready Package, Flow Graph, and Readiness Proof Map traceability without running a browser build. It is backend-focused daily evidence and does not replace `npm run launch:private-pilot:check` for full P1 signoff.

Latest P1 lower-write browser confirmation on 2026-07-07: `npm run ui:manager-private-pilot:dev` passes against an already running Vite dev server. The gate uses focused file-backed backend preparation and then verifies the browser Manager path for private-pilot launch approvals, evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Manager Flow Graph, and Readiness Proof Map. It is the current low-write browser preflight for mock replacement work and does not replace the built `ui:manager-private-pilot` or `launch:private-pilot:check` signoff gates.

Latest P1 built browser confirmation on 2026-07-07: `npm run ui:manager-private-pilot` passes. It runs `vite build`, prepares the browser store through the focused file-backed private-pilot handoff contract, then verifies the built Manager UI can record launch approvals, evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Manager Flow Graph, and Readiness Proof Map evidence. The build still emits the known large JS chunk warning.

Latest P1 component signoff on 2026-07-07: `npm run agents:product-team:private-pilot` and `npm run ui:manager-private-pilot` both pass in the current workspace. This proves the two commands that make up `npm run launch:private-pilot:check`; the one-command wrapper was not rerun in the same turn to avoid duplicating a full backend replay plus browser build after both component gates had already passed.

## P2 - Public Production

P2 is blocked until managed controls replace local/private-pilot rehearsal controls.

Required blocker domains:

| Domain | Minimum production evidence required |
| --- | --- |
| Managed persistence | Real database adapter execution receipts, RLS/tenant isolation, backup/restore proof, migration rollback, read-model checkpoint parity |
| Managed queue/cron | Durable queue adapter receipts for project, Agent, and Autopilot-session lanes; lease/ack/retry/dead-letter proof; scheduled worker operation without browser dependency |
| BYOK and providers | Managed KMS/secret rotation, real provider eval dataset, cost/rate limits, retry/circuit policy, incident controls, immutable provider/source/model-output audit |
| Security | Production identity lifecycle, RBAC/RLS enforcement, signed access/replay protection, audit fail-closed behavior, centralized security audit retention |
| Operations | Centralized logs/metrics/traces, alert routing, on-call ownership, managed incident records, restore drills, recovery runbooks |
| Evidence export | Encrypted object storage, signed expiring download URLs, watermark enforcement, retention/deletion jobs, data-residency controls |
| Launch governance | Production operations/deployment/security/provider control receipts marked as managed-production evidence with signed control-plane attestation, not local rehearsal evidence or unattested claims |

Required production-oriented commands before a P2 decision can be considered:

```bash
npm run agents:production-access-control
npm run agents:production-managed-identity
npm run agents:production-managed-secrets
npm run agents:production-managed-persistence
npm run agents:production-managed-worker-queue
npm run agents:production-provider-controls
npm run agents:production-data-governance
npm run agents:production-traffic-controls
npm run agents:production-customer-acceptance
npm run agents:production-operations-startup
npm run launch:infra
npm run launch:public-production:no-go
npm run agents:public-production-readiness-report
npm run agents:public-production-readiness-report:validate
npm run agents:managed-environment-preflight
npm run agents:managed-environment-preflight:validate
npm run adapters:gateway
npm run adapters:gateway-server:validate
npm run adapters:gateway-http:validate
npm run adapters:gateway-postgres-store:validate
npm run agents:product-team:production-ops-controls
npm run agents:product-team:production-deployment-controls
npm run agents:product-team:production-security-controls
npm run agents:product-team:production-provider-controls
npm run agents:product-team:production-evidence-integrity
npm run agents:product-team:production-launch-governance
npm run agents:product-team
```

`npm run agents:production-access-control` is the focused production access-control contract. It verifies that the public-production startup matrix starts with `setup-access-control` when enforced mode, signing secret, replay protection, or audit fail-closed mode is missing; verifies strict access env clears only that setup action while public production stays blocked by later managed controls; exercises the backend API for signed request enforcement, project-membership denial, replay rejection, Agent self-scope, observer write denial, persisted security audit rows, hash-chain audit stream readiness, and fail-closed audit-sink behavior; and verifies `GET /access-control-policy` exposes the operator-readable signed header, replay, membership, identity-session, audit fail-closed, and related-route contract without leaking signing secrets.

`npm run agents:production-managed-identity` is the focused managed identity startup contract. It starts from strict access-control env, verifies the next public-production action is `setup-managed-identity`, rejects identity provider/service identity env names plus invalid signatures, verifies redaction of issuer/JWKS tokens and receipt checksums, then accepts only matching signed managed-production identity evidence. It also verifies `GET /managed-identity-policy` exposes the operator-readable provider metadata env names, service identity env names, managed-production evidence env names, attestation payload shape, validation command, and related-route contract without leaking issuer/JWKS URLs, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only the managed identity setup row; public production remains blocked by later managed secrets, infrastructure, provider, governance, traffic, and operations controls.

`npm run agents:production-managed-secrets` is the focused managed Secret Manager/KMS startup contract. It starts after access control and managed identity are ready, verifies the next public-production action is `setup-managed-secrets`, rejects endpoint/key env names plus attestation names when the provider is still local or missing, rejects managed providers that report raw secret exposure, and accepts only a managed provider with rotation, access audit, endpoint/key configuration, and matched attestation id/checksum evidence. It also verifies `GET /managed-secret-manager-policy` exposes the operator-readable provider capability requirements, endpoint/key env groups, attestation id/checksum pairs, validation command, and related-route contract without leaking endpoint values, key ids, attestation ids/checksums, raw secrets, access signing secrets, or attestation signing secrets. Passing this gate clears only the managed secrets setup row; public production remains blocked by later managed persistence, queue, provider, governance, traffic, and operations controls.

`npm run agents:production-managed-persistence` is the focused managed database persistence startup contract. It starts after access, managed identity, and managed secrets are ready, verifies the next public-production action is `setup-managed-persistence`, rejects missing database/gateway configuration, rejects env-only database URLs even with `MANAGED_PERSISTENCE_REQUIRE_REAL_ADAPTER=true`, rejects invalid cutover signatures, verifies database URL redaction, then accepts only real adapter configuration plus signed managed-production cutover evidence for `managed-persistence-cutover`. It also verifies `GET /managed-persistence-policy` exposes the operator-readable adapter env names, database/gateway env groups, signed cutover evidence env names, attestation payload shape, validation command, and related-route contract without leaking database URLs, endpoint tokens, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only the managed persistence setup row; public production remains blocked by durable queue, provider, governance, traffic, and operations controls.

`npm run agents:production-managed-worker-queue` is the focused managed durable queue startup contract. It starts after access, managed identity, managed secrets, and managed persistence are ready, verifies the next public-production action is `setup-managed-worker-queue`, rejects missing queue/gateway configuration, rejects env-only queue endpoints even with `WORKER_QUEUE_REQUIRE_REAL_ADAPTER=true`, rejects invalid queue cutover signatures, verifies queue endpoint redaction, then accepts only real adapter configuration plus signed managed-production queue cutover evidence for `managed-worker-queue-cutover`. It also verifies `GET /managed-worker-queue-policy` exposes the operator-readable queue adapter env names, queue/gateway env groups, scheduler lease/retry/dead-letter capability requirements, signed cutover evidence env names, attestation payload shape, validation command, and related-route contract without leaking queue endpoint tokens, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only the durable queue setup row; public production remains blocked by provider, governance, traffic, and operations controls.

`npm run agents:production-provider-controls` is the focused production provider/BYOK controls startup contract. It starts after access, managed identity, managed secrets, managed persistence, managed worker queue, and backend provider runtime are ready, verifies the next public-production action is `setup-production-cost-controls`, rejects missing budget/rate policy, usage audit, cost alert routing, or signed evidence, rejects env-only provider cost controls with invalid signatures, verifies usage-audit and cost-alert endpoint redaction, then accepts only production provider budget/rate policy plus centralized usage audit, cost alert routing, and signed managed-production cost-control evidence. It also verifies `GET /production-provider-controls-policy` exposes the operator-readable budget/rate-limit env names, usage audit env groups, cost alert env groups, signed cost-control evidence env names, attestation payload shape, validation command, and related-route contract without leaking audit endpoint tokens, alert routing tokens, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only provider cost-control setup; public production remains blocked by data governance, traffic, operations, and customer policy controls.

`npm run agents:production-data-governance` is the focused production data governance startup contract. It starts after provider controls are ready, proves `setup-production-data-governance` is the next action, rejects missing retention/residency, deletion job, export storage, or signed evidence, rejects env-only data governance with invalid signatures, verifies deletion/export endpoint redaction, then accepts only retention policy, data residency, managed deletion job, export storage, and signed managed-production data-governance evidence. It also verifies `GET /production-data-governance-policy` exposes the operator-readable retention/residency env names, deletion job env names, export storage env names, signed data-governance evidence env names, attestation payload shape, validation command, and related-route contract without leaking deletion/export endpoint tokens, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only data-governance setup; public production remains blocked by traffic, operations, and customer policy controls.

`npm run agents:production-traffic-controls` is the focused production traffic/rollback startup contract. It starts after data governance is ready, proves `setup-production-traffic` is the next action, rejects missing production domain/TLS, traffic gateway, health check, release approval, rollback smoke test, or signed evidence, rejects env-only traffic controls with invalid signatures, verifies traffic/health/rollback endpoint redaction, then accepts only domain/TLS, controlled traffic gateway, health check, release approval, rollback smoke test, and signed managed-production traffic-control evidence. It also verifies `GET /production-traffic-policy` exposes the operator-readable domain/TLS env names, traffic gateway and health check env names, release approval env names, rollback runbook/smoke-test env names, signed traffic-control evidence env names, attestation payload shape, validation command, and related-route contract without leaking traffic/health/rollback endpoint tokens, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only traffic setup; public production remains blocked by operations and customer policy controls.

`npm run agents:production-customer-acceptance` is the focused production customer acceptance startup contract. It starts after traffic controls are ready, proves `setup-customer-production-acceptance` is the next action, rejects missing customer policy, success criteria, threshold, human approval, rollback criteria, or signed evidence, rejects env-only customer acceptance controls with invalid signatures, verifies approval/checksum/signature redaction, then accepts only customer acceptance policy, success criteria, threshold, approval, rollback criteria, and signed managed-production customer-acceptance evidence. It also verifies `GET /production-customer-acceptance-policy` exposes the operator-readable policy env names, success criteria env names, threshold env names, approver/approval env names, rollback criteria env names, signed customer-acceptance evidence env names, attestation payload shape, validation command, and related-route contract without leaking approval values, receipt checksums, signatures, access signing secrets, or attestation signing secrets. Passing this gate clears only customer acceptance setup; public production remains blocked by operations controls and final managed launch controls.

`npm run agents:production-operations-startup` is the focused production operations startup contract. It starts after customer production acceptance is ready, proves `setup-observability` and `setup-incident-response` remain when operations controls are missing, rejects endpoint/env-only observability, alert routing, incident, restore, or audit setup with invalid signatures, verifies observability/alert/incident/audit endpoint redaction, then accepts only centralized observability, alert routing, on-call ownership, managed incident records, restore drill proof, centralized audit retention, and signed managed-production operations evidence. It also verifies `GET /production-operations-policy` exposes the operator-readable observability env groups, alert/on-call/incident env names, restore-drill env names, centralized audit-retention env names, signed operations evidence env names, validation command, and related-route contract without leaking observability, alert, incident, audit, checksum, signature, access signing secret, or attestation signing secret values.

`npm run launch:infra` is the fast P2 infrastructure rehearsal. It runs the launch gate matrix validator, public-production startup readiness blocker contract, focused production access-control contract, focused managed identity startup contract, focused managed Secret Manager/KMS startup contract, focused managed database persistence startup contract, focused managed durable queue startup contract, focused production provider/BYOK controls startup contract, focused production data governance startup contract, focused production traffic/rollback startup contract, focused production customer acceptance startup contract, focused production operations startup contract, redacted public-production operator report validation, real managed environment preflight validation, shared adapter gateway contract validation, reference private adapter gateway server validation, Agent HTTP server validation through an env-configured adapter gateway endpoint, Postgres-compatible gateway storage boundary validation, and the managed infrastructure cutover attestation bridge. The Postgres gateway validation now also proves `POST /attestations/managed-production-control` stays blocked until query-bound Postgres readback parity exists, then issues an HMAC-signed control-plane attestation whose payload matches `production-evidence-integrity-audit/v1`. The managed cutover bridge then verifies the project API can request those signed attestations, write `production-operations-control-receipt/v1` for persistence/queue cutover, and project them into infrastructure/evidence proof without closing unrelated launch gates. These commands are necessary but not sufficient for P2. The production launch audit must still show `readyForProduction: true`, and the production evidence integrity audit must classify required control domains as managed-production evidence.

`npm run launch:public-production:no-go` is the operator-facing public-production decision check. It runs the dedicated `scripts/validate-public-production-no-go.mjs` wrapper, reuses `npm run launch:infra` instead of replaying heavier P1 or full product-team gates, then directly checks `public-production-startup-readiness/v1` and the redacted operator report still return explicit public-production no-go. Use it when the question is "can we open public traffic now"; use the lower-level P2 commands only when isolating the specific blocker domain.

`npm run agents:public-production-readiness-report` is the low-write operator report for the public-production no-go state. It reads the same `public-production-startup-readiness/v1` and `production-environment-setup/v1` backend contracts, embeds the redacted `managed-environment-preflight-report/v1` summary, and emits `public-production-action-plan/v1` with blocked domains, route links, validation commands, next action, required env var names, managed-preflight blocked counts, and network-check status. It intentionally omits configured values. The action plan now aggregates validation commands from every blocked action row, so focused gates such as `npm run agents:production-provider-controls` are visible at the operator-report level rather than only inside one setup row. The JSON and Markdown report formats both expose a top-level validation command section, and the Manager UI renders the same top-level validation command list plus action-plan rows with required env names, route, and validation command so operators can see the concrete missing setup without opening raw JSON. `npm run agents:public-production-readiness-report:validate` proves the report remains blocked under partial/env-only production configuration, keeps managed environment readiness false, keeps the action plan blocked, and does not leak production URLs, Secret Manager endpoints, database credentials, or queue endpoints.

`npm run agents:managed-environment-preflight` is the low-write bridge from rehearsal to real managed infrastructure. It classifies whether the adapter gateway, persistence adapter, queue adapter, and managed-production attestation signing are configured for an external managed environment rather than localhost/local-shadow rehearsal. By default it does not make network calls; run `node scripts/report-managed-environment-preflight.mjs --check-network` only after configuring the real gateway to probe `/health` and `/state`. `npm run agents:managed-environment-preflight:validate` proves local/private endpoints stay blocked and gateway URLs, bearer tokens, database credentials, queue tokens, and attestation secrets are not printed.

Managed persistence and managed queue/cron cutover evidence is now projected through two layers: the adapter dry-run routes prove rehearsal coverage, and verified `production-operations-control-receipt/v1` rows for `managed-persistence-cutover` / `managed-worker-queue-cutover` clear the matching `production-infrastructure-rehearsal/v1` domain blockers and managed cutover gates. `managedCutoverSummary` reports the ready/blocked cutover gate counts and the next blocked gate so C-side operators can see exactly why public production remains blocked. This does not by itself approve P2; remaining deployment, security, provider, evidence integrity, operations, and launch-governance gates must still close with managed-production evidence.

Deployment hardening uses the same projection pattern: verified `production-deployment-control-receipt/v1` rows now bind the deployment receipt workflow checksum into `production-infrastructure-rehearsal/v1`, mark the `deployment-preflight` domain row and `deployment-cutover` managed gate `productionReady`, and expose `deploymentReceiptReady` plus the deployment receipt route. This clears only the infrastructure deployment blocker; launch audit, managed-production evidence integrity, security, provider, operations, and approval gates still control the public-production decision.

Operations, deployment, security, provider, managed-production evidence integrity, and launch governance now have matching focused gates. `npm run agents:product-team:production-ops-controls` proves security-admin writes for production operations receipts, observer write denial, lightweight receipt-first responses, standalone workflow readback, Proof Map routes, Manager Flow Graph nodes, Launch Control routing, file-backed persistence, and Evidence Integrity blocking of unsigned external evidence. It does not replay the full private-pilot acceptance chain or claim full operations-ready status by itself. `npm run agents:product-team:production-deployment-controls`, `npm run agents:product-team:production-security-controls`, and `npm run agents:product-team:production-provider-controls` use the shared focused receipt validator to prove their signed write route, observer denial, lightweight write response, standalone workflow readback, Proof Map route, Flow Graph receipt node, Launch Control routing, Evidence Integrity blocking of unsigned external evidence, and file-backed persistence. `npm run agents:product-team:production-evidence-integrity` proves local/test receipts remain local rehearsal evidence, proves unsigned `evidenceEnvironment: "managed-production"` attestation claims remain `external-unattested`, then requires explicit managed-production receipt evidence with a valid attestation signature before closing the evidence-integrity gap. `npm run agents:product-team:production-launch-governance` then proves Manager, security-admin, and operations-owner can write production launch approvals through signed backend membership, that `launch-approval-workflow/v1` carries checksum/proof/timeline/event evidence, and that Launch Audit / Control Center / Proof Map / Flow Graph see approval readiness while the Control Center still keeps global public-production startup readiness as an explicit no-go row until the managed runtime gates pass.

Latest P2 operations-control confirmation on 2026-07-04: `npm run agents:product-team:production-ops-controls` now runs the focused `scripts/validate-production-operations-controls-contract.mjs` validator instead of replaying the full private-pilot chain. It creates a minimal backend product-team project, persists signed project membership, confirms the receipt-needed workflow, rejects observer writes, records all production operations control receipts through a security-admin signed route with `includeReadModels: false`, verifies standalone workflow readback, Proof Map, Manager Flow Graph, Launch Control Center, Evidence Integrity, and file-backed persistence. Unsigned external ops receipts are classified as `external-unattested`, so this closes only the receipt-contract proof; public production remains blocked until managed-production attestations and the rest of the P2 controls pass.

Latest P2 deployment/security/provider control confirmation on 2026-07-04: `npm run agents:product-team:production-deployment-controls`, `npm run agents:product-team:production-security-controls`, and `npm run agents:product-team:production-provider-controls` now run the shared focused `scripts/validate-production-control-receipt-contract.mjs` validator instead of replaying their full private-pilot stage slices. Each gate creates a minimal backend product-team project, persists signed project membership, rejects observer writes, records all required domain controls through the allowed role with `includeReadModels: false`, verifies standalone workflow readback, Readiness Proof Map, Manager Flow Graph, Production Launch Control Center routing, Evidence Integrity `external-unattested` classification for unsigned external evidence, and file-backed persistence. These gates close only receipt-contract proof for their domain; HTTP, UI, migration, infrastructure projection, and full post-acceptance readiness remain covered by the heavier private-pilot/full hardening gates.

Latest P2 evidence-integrity confirmation on 2026-07-06: `npm run agents:product-team:production-evidence-integrity` passes through the focused `scripts/validate-production-evidence-integrity-contract.mjs` validator instead of replaying the full private-pilot chain. It creates a minimal backend product-team project, writes production operations/deployment/security/provider receipt batches through signed membership routes, verifies local receipts stay `local-rehearsal`, verifies unsigned or invalid-signature managed-production claims stay `external-unattested`, verifies signed managed-production attestations upgrade `production-evidence-integrity-audit/v1`, and checks Launch Audit, Gap Register, Control Center, Proof Map, and Flow Graph projection. This closes only the evidence-classification proof; public production remains blocked by the rest of the P2 managed runtime, governance, observability, incident, restore, and approval controls.

Latest P2 launch-governance confirmation on 2026-07-06: `npm run agents:product-team:production-launch-governance` passes through the focused `scripts/validate-production-launch-governance-contract.mjs` validator instead of replaying the full private-pilot chain. It creates a minimal backend product-team project, writes signed managed-production operations/deployment/security/provider receipts, confirms evidence integrity is ready, records Manager, security-admin, and operations-owner production approvals through signed membership, and verifies `launch-approval-workflow/v1`, Launch Audit, Production Launch Control Center, Readiness Proof Map, and Manager Flow Graph projection, including the independent `public-production-startup-readiness` blocker node. This closes the approval-contract proof only; the Control Center must still keep public production `no-go` while global startup/runtime gates remain blocked.

Latest P2 infrastructure/no-go confirmation on 2026-07-07: `npm run launch:public-production:no-go` passes. This runs the dedicated no-go validator, reuses `npm run launch:infra` to verify the global public-production startup blocker contract, focused production access-control contract, focused managed identity startup contract, focused managed Secret Manager/KMS startup contract, focused managed database persistence startup contract, focused managed durable queue startup contract, focused production provider/BYOK controls startup contract, focused production data governance startup contract, focused production traffic/rollback startup contract, focused production customer acceptance startup contract, focused production operations startup contract, redacted operator report validation, managed environment preflight validation, launch gate matrix, adapter gateway contract, reference gateway server, Agent HTTP server env-gateway mode, Postgres-compatible storage boundary, managed identity startup blocker, production traffic/rollback startup blocker, customer production acceptance startup blocker, and managed infrastructure cutover attestation bridge, then directly confirms the current startup readiness and operator report still say public-production no-go. This is infrastructure rehearsal evidence only; public production still requires real managed identity, managed database, durable queue/cron, KMS/Secret Manager, centralized audit/observability, incident process, traffic cutover/rollback proof, and customer production policy.

Latest current P2 focused confirmation on 2026-07-08: `npm run agents:production-access-control`, `npm run agents:production-managed-identity`, `npm run agents:production-managed-secrets`, `npm run agents:production-managed-persistence`, `npm run agents:production-managed-worker-queue`, `npm run agents:production-provider-controls`, `npm run agents:production-data-governance`, `npm run agents:production-traffic-controls`, `npm run agents:production-customer-acceptance`, `npm run agents:production-operations-startup`, `npm run agents:product-team:production-ops-controls`, `npm run agents:product-team:production-deployment-controls`, `npm run agents:product-team:production-security-controls`, `npm run agents:product-team:production-provider-controls`, `npm run agents:product-team:production-evidence-integrity`, `npm run agents:product-team:production-launch-governance`, `npm run agents:public-production-startup-readiness`, and `npm run agents:managed-infrastructure-cutover-attestations` pass in the current workspace. This reconfirms enforced signed access, replay rejection, membership/Agent-scope access boundaries, the operator-readable `GET /access-control-policy` route, managed identity startup evidence boundaries, the operator-readable `GET /managed-identity-policy` route, managed Secret Manager/KMS provider boundaries, the operator-readable `GET /managed-secret-manager-policy` route, managed database persistence cutover boundaries, the operator-readable `GET /managed-persistence-policy` route, managed durable queue cutover boundaries, the operator-readable `GET /managed-worker-queue-policy` route, production provider/BYOK cost-control startup boundaries, the operator-readable `GET /production-provider-controls-policy` route, production data governance startup boundaries, the operator-readable `GET /production-data-governance-policy` route, production traffic/rollback startup boundaries, the operator-readable `GET /production-traffic-policy` route, production customer acceptance startup boundaries, the operator-readable `GET /production-customer-acceptance-policy` route, production operations startup boundaries, the operator-readable `GET /production-operations-policy` route, production receipt contracts, managed-production evidence classification, signed launch-governance approvals, global public-production no-go startup boundaries, and managed infrastructure attestation bridging without replaying browser gates or claiming public production readiness.

Latest public-production operator report confirmation on 2026-07-07: `npm run agents:public-production-readiness-report:validate` passes. This verifies the redacted operator report remains blocked under partial/env-only production setup, embeds the managed environment preflight no-go summary, emits `public-production-action-plan/v1` with concrete blocked domains/actions/env var names, and does not print configured production URLs, Secret Manager endpoints, database credentials, or queue endpoints.

## Current Evidence

As of the current workspace state, the verified private-MVP evidence is:

Prior validation spot check on 2026-07-06: `npm run agents:product-team:smoke`, the then-current `npm run agents:product-team:core`, `npm run agents:real-user-zero-to-autonomy`, `npm run agents:product-team:research-sample`, `npm run agents:product-team:cycle-consistency`, `npm run skills:check`, `npm run skills:blend`, `npm run agents:settings-contracts`, `npm run agents:transcript-contracts`, `npm run agents:scenario:contract`, `npm run agents:local-mvp-startup-readiness`, `npm run agents:server:validate`, and `npm run build` passed in this workspace. The default `agents:product-team:core` command now points to the stable low-write local MVP aggregate; use `agents:product-team:core:file-backed` when you explicitly need the older file-backed/HTTP core runner. The build still emitted the known large JS chunk warning, so chunk splitting remains a performance follow-up rather than a functional blocker.

Latest built browser confirmation on 2026-07-06: `npm run ui:real-user-zero-to-autonomy` passes as the real-backend browser proof for a fresh user journey from Settings provider seal through kickoff, Settings Workspace local workspace bind/readback, C/A handoff, autonomous Agent action, provider-backed evidence, Reviewer source decisions, generic artifact submissions, review/revision closure, accepted final deliverable, Flow Graph, Proof Map, transcript, timeline, event ledger, Agent Dashboard, Evidence Index, and ready Project Evidence Archive storage/workspace proof readback. The current gate also proves the C/A handoff intent can run from the standalone Manager Dashboard Collaboration Intent Queue before full Ready Package dependency, without local snapshot reseeding, that the delegated Agent Submission output node is visible with chat proof, that the Manager Flow Graph visibly labels the `final-deliverable` submission node, that Manager UI shows Source Decisions, Storage Proofs, and Workspace Files in the Project Evidence Archive, and that the Settings provider/workspace paths remain usable after the Secret Vault startup-command, Tool Grant readback, and workspace bind fixes. This confirms the private-MVP browser path only; public production still requires the P2 managed controls below.

Latest P1 browser confirmation on 2026-07-07: `npm run ui:manager-private-pilot` passes after `vite build`. It prepares the browser store through the focused file-backed private-pilot handoff contract, then verifies the Manager private-pilot handoff path for launch approvals, evidence export request/approval/download audit, release candidate, launch run, health check, acceptance report, Manager Flow Graph, and Readiness Proof Map evidence. The build still emits the known large JS chunk warning; public production remains blocked by the P2 managed controls.

Latest foundation and core-chain validation confirmation on 2026-07-06: `npm run skills:validate`, `npm run skills:check`, `npm run skills:blend`, `npm run agents:product-team:smoke`, `npm run agents:product-team:research-sample`, `npm run agents:scenario:contract`, and the then-current `npm run agents:product-team:core` passed in the workspace. The default `agents:product-team:core` command is now the low-write local MVP aggregate; `agents:product-team:core:file-backed` is the explicit longer file-backed runner. The older `agents:scenario`, `agents:product-team:core:file-backed`, and full staged acceptance runner remain long gates; use targeted low-write gates first when disk write volume matters.

Latest Group Chat transcript confirmation on 2026-07-06: `npm run agents:transcript-contracts` passes in the current workspace. This verifies backend transcript channel creation, search, channel pin, message pin, reply, mention, attachment, and member-presence contracts as one low-write collaboration gate, including transcript receipts, Proof Map routes, Flow Graph nodes, timeline/event proof, and target Agent inbox delivery where applicable. It proves the main collaboration controls are route-backed instead of browser-local chat state.

Latest Timeline action confirmation on 2026-07-06: `npm run agents:timeline-action` passes in the current workspace. This verifies Manager notes, acknowledgements, completion marks, and edit notes write through `POST /projects/:id/timeline/actions`, create `timeline-action-receipt/v1`, and appear in timeline, event-ledger, Manager Flow Graph, and deferred refresh-route proof instead of browser-only comments.

Latest Settings backend aggregate confirmation on 2026-07-06: `npm run agents:settings-contracts` passes in the current workspace. This runs local MVP startup readiness, Settings health, runtime, provider-readiness, integration-readiness, search endpoint/key Vault binding, privacy policy, provider budget, Agent tool grants, integration capability, and workspace capability gates in one low-write sequence. It verifies backend first-run startup readiness, Secret Vault search provider binding and rehydration, backend `project-settings/v1` receipts and readback for privacy policy, provider budget, Agent tool grants, integration capability rows, workspace policy, workspace bind/local-runtime readback, runtime-contract links, memory-readiness links, meeting summaries, audit rows, timeline proof, event-ledger proof, file-backed persistence, Manager Ready Package links, and Readiness Proof Map routes. These are local/private-MVP Settings guarantees; public production still requires managed KMS/Secret Manager, task-scoped runtime identity, centralized audit/observability, production provider controls, and managed workspace storage/access/quota controls.

Latest runtime autonomy confirmation on 2026-07-06: `npm run agents:local-mvp-startup-readiness`, `npm run agents:server:validate`, and `npm run agents:product-team:cycle-consistency` pass. This proves the local MVP startup preflight, Secret Vault startup/seal/readback path, and staged autonomous-cycle consistency route for C/A continuation, Manager Flow Graph, Readiness Proof Map, Ready Package, and persistence proof. `agents:product-team:cycle-consistency` is a longer staged backend gate than the smoke tests, so use it when scheduler/autonomy contracts change.

Latest low-write launch metadata and mock-boundary confirmation on 2026-07-07: `npm run launch:local-mvp:check` and `npm run ui:mock-boundaries` pass in the current workspace; the previous `npm run launch:gates` matrix confirmation remains the broader drift guard. This proves the launch matrix references, P0/P1/P2 required-command wiring, Settings/backend proof boundary, Settings API/workspace input editability guard, Settings receipt-backed save/footer guard, Agent Focus Dashboard guard, Agent Workbench backend-dashboard/write/failure guard, Reviewer/Agent-message/Flow-confirmation backend proof guard, Initiation startup readiness and Mission Runner approval guards, Manager Dashboard standalone C/A handoff queue/output guard, Manager direct-command guard, Manager autonomy cockpit guard, Manager governance/collaboration cockpit guard, Manager Ready Package C/A coordination submodel guard, Manager Ready Package production submodel guard, War Room meeting input fail-closed guard, legacy War Room start/terminal/close guard, Group Chat transcript UI command guard, Settings no-op Save removal, project-scoped Settings browser-gate wiring, Research-as-validation-sample gate wiring, scenario/settings aggregate contract wiring, Group Chat transcript sub-gate wiring, Timeline action gate wiring, zero-to-autonomy validator wiring, and frontend mock-replacement fail-closed boundaries are currently aligned before running the heavier browser/build gates. It is metadata and static-boundary proof only; it does not replace the P0/P1 backend and browser gates listed above.

Latest managed-infrastructure bridge confirmation on 2026-07-06: `npm run agents:managed-infrastructure-cutover-attestations` passes. This proves the project API can fail closed before gateway/readback/project dry-run evidence is ready, then accept signed managed-production persistence and worker-queue cutover attestations through the adapter gateway and project them into operations receipt and infrastructure rehearsal proof without closing unrelated public-production blockers.

Latest full Manager browser regression on 2026-07-03: `node scripts/validate-manager-backend-ui.mjs` passes against the built `dist` bundle after explicitly seeding the Manager Demo sample into the backend and syncing standalone Manager read models. This broad legacy regression proves the old Manager workbench can run in backend-backed mode without relying on frontend fallback rows for scenario trail, walkthrough, requirement matrix, sync protocol, use-case audit, Manager command center, Flow Graph, Proof Map, transcript, timeline, event, Agent action, review/revision/final-deliverable, and collaboration proof surfaces.

Latest real-user API confirmation on 2026-07-07: `npm run agents:real-user-zero-to-autonomy` passes against the real local `agents:server` path with Secret Vault, local mock model/search providers, `/local-mvp-startup-readiness` first-project readiness, product-team mission startup, local workspace bind/read/write proof, provider-backed evidence, Reviewer source decisions, required generic artifact submissions, review/revision closure, accepted final deliverable, Flow Graph, Proof Map, Product Team Delivery Trace, ready Project Evidence Archive storage/workspace proof coverage, transcript, timeline, event ledger, Agent Dashboard, and Evidence Index readback. `npm run agents:real-user-zero-to-autonomy-report:validate` also passes, confirming the same backend chain can be read as a redacted operator report without leaking validation secrets or claiming public-production readiness. `npm run agents:product-team:research-sample` also passes on 2026-07-07, confirming Research remains a validation sample for the same generic product-team chain rather than a paper-specific workflow.

Latest low-write browser preflight on 2026-07-07: `npm run ui:real-user-zero-to-autonomy:dev` passes in the current workspace against a temporary Vite dev server after the route-backed C/A handoff preflight and Recent Commit Line key-stability fixes. It verifies Settings provider seal, kickoff, C/A handoff, Agent submissions, Artifact Quality Audit, Product Team Delivery Trace, Proof Map, Flow Graph, transcript, timeline, event, Agent Dashboard, Evidence Index readback, and no duplicate React key warnings while skipping `vite build`; it remains a preflight and does not replace the built launch gate above.

Latest Settings plus real-user dev browser preflight on 2026-07-08: `npm run ui:settings-agents-server:dev` and `npm run ui:real-user-zero-to-autonomy:dev` pass against a temporary Vite dev server with `HOFS_PLAYWRIGHT_CHROMIUM` pointed at local Microsoft Edge. The scripts now skip unreadable Playwright browser cache directories, so sandboxed Windows runs can use an explicit browser executable. The Settings preflight verifies the first-run footer sequence directly: saved backend target requires Health, Health remains blocked before provider secrets are sealed, and backend-backed saves are shown only after sealed provider credentials and Workflow Smoke pass. This remains a lower-write dev preflight; the built browser gates remain the launch evidence for packaged UI.

Latest packaged Settings plus real-user browser evidence on 2026-07-08: `npm run build`, `node scripts/validate-settings-agents-server-ui.mjs`, and `node scripts/validate-real-user-zero-to-autonomy-agents-server-ui.mjs` pass against the rebuilt `dist` bundle with `HOFS_PLAYWRIGHT_CHROMIUM` pointed at local Microsoft Edge. The build still emits the known large single JS chunk warning. Functionally, this confirms the packaged UI can run the same first-run Settings Health sequence, Secret Vault provider sealing, Workflow Smoke, generic product-team mission startup, C/A handoff, Agent evidence/artifact/review/revision/final-deliverable loop, and Manager proof readback without relying on dev-server-only state.

Latest built real-user browser evidence on 2026-07-07: `npm.cmd run build` and `node scripts/validate-real-user-zero-to-autonomy-agents-server-ui.mjs` passed against the current production `dist` bundle. This confirms the current built UI can run Settings provider seal, kickoff, C/A handoff, autonomous Agent continuation, provider-backed evidence, Reviewer source decisions, required generic artifact submissions, review/revision closure, accepted final deliverable, Manager UI readback, Flow Graph, Proof Map, Product Team Delivery Trace, Group Chat transcript, timeline, event ledger, Agent Dashboard, Evidence Index, Project Memory Readiness, and ready Project Evidence Archive storage/workspace proof without relying on frontend mock state.

Latest consolidated built UI confirmation on 2026-07-07: one controlled `npm.cmd run build` pass followed by `node scripts/validate-settings-agents-server-ui.mjs` and `node scripts/validate-real-user-zero-to-autonomy-agents-server-ui.mjs` passed against the same rebuilt `dist` bundle. The build still emits the known large single JS chunk warning, so chunk splitting remains a performance follow-up rather than a functional blocker.

Latest Settings built browser confirmation on 2026-07-07: `node scripts/validate-settings-agents-server-ui.mjs` passed against the rebuilt production `dist` bundle after `npm.cmd run build`. It verifies the real `agents:server` Settings path for backend URL entry, Secret Vault model/search sealing, redacted Vault records, Health/Deployment/Models/Integrations/Workspace readiness routes, Workspace binding, Privacy export-approval editing, provider budget/tool-grant project-setting writes, provider-controlled run consumption, `/search/test`, timeline proof, event-ledger proof, and Settings Workflow Smoke product-brief output.

Latest Settings lower-write browser preflight remains `npm run ui:settings-agents-server:dev` against an already running Vite dev server. Use it while replacing Settings mocks because it skips `vite build`; it does not replace the built confirmation above.

Latest low-write foundation reconfirmation on 2026-07-07: `npm run skills:validate`, `npm run skills:blend`, `npm run agents:scenario:contract`, and `npm run agents:product-team:smoke` pass. This verifies the 40 persona packages source shape, persona/professional skill blending across registered personas, 55 routing regression cases in the focused scenario contract, Manager chat command / Agent message / marketplace Agent contract / Agent Workbench / timeline-action scenario slices, and the generic in-memory product-team loop without running the longer persona regression, legacy scenario regression, file-backed core chain, or browser gates.

Latest low-write local MVP product-team confirmation on 2026-07-07: `npm run agents:product-team:local-mvp` passes in the current workspace. This reconfirms Settings/local startup contracts, Research as a generic product-team validation sample, Mission Runner startup/C/A handoff, marketplace Agent contract roster proof, Agent Workbench artifact/review/final-delivery proof, MVP readiness operator-action execution into A-side proof, and the zero-to-autonomy operator report / proof package without rerunning browser/build or the older file-backed core runner.

Latest production build evidence on 2026-07-07: `npm.cmd run build` passed with Vite for the current source, producing the `dist` bundle used by the Settings and real-user browser gates above. The known large single JS chunk warning remains a performance follow-up rather than a functional build blocker.

- P0 backend loop: `npm run agents:product-team:core`
- P0 Research validation sample: `npm run agents:product-team:research-sample`
- P0 autonomous cycle consistency: `npm run agents:product-team:cycle-consistency`
- P0 persona supply: `npm run skills:check`
- P0 persona skill blending: `npm run skills:blend`
- P0 manager/backend low-write scenario contract: `npm run agents:scenario:contract`
- P0 manager/backend Harness scenario: `npm run agents:scenario`
- P0 local backend Secret Vault startup: `npm run agents:server:validate`
- P0 local MVP startup readiness: `npm run agents:local-mvp-startup-readiness`
- P0 public-production startup blocker contract: `npm run agents:public-production-startup-readiness`
- P0 Settings health readiness contract: `npm run agents:settings-health-readiness`
- P0 Settings runtime readiness contract: `npm run agents:settings-runtime-readiness`
- P0 model provider adapter contract: `npm run agents:model-provider-adapter`
- P0 Settings provider readiness contract: `npm run agents:settings-provider-readiness`
- P0 Settings backend aggregate contract: `npm run agents:settings-contracts`
- P0 search provider Vault endpoint/key proof: `npm run agents:search-provider:vault-endpoint`
- P0 Agent Workbench backend contract: `npm run agents:agent-workbench-contract`
- P0 Agent-to-Agent message backend contract: `npm run agents:agent-message-contract`
- P0 marketplace Agent contract backend gate: `npm run agents:agent-contract`
- P0 Manager group-chat command backend contract: `npm run agents:manager-chat-command-contract`
- P0 frontend mock boundary static guard: `npm run ui:mock-boundaries`
- P0 browser control loop: `npm run ui:manager-backend:core`
- P0 browser control loop lower-write preflight: `npm run ui:manager-backend:core:dev`
- P0 browser real-user product-team chain: `npm run ui:manager-backend:real-user-chain`
- P0 browser real-user product-team chain lower-write preflight: `npm run ui:manager-backend:real-user-chain:dev`
- P0 browser proof navigation: `npm run ui:manager-backend:proof-navigation`
- P0 browser private-pilot Manager panels: `npm run ui:manager-backend:private-pilot-panels`
- P0 browser production-control Manager panels: `npm run ui:manager-backend:production-controls`
- P0 provider proof browser link: `npm run ui:manager-provider-proof`
- P0 Settings privacy policy backend receipt: `npm run agents:project-settings:privacy`
- P0 Settings provider budget backend receipt: `npm run agents:project-settings:provider-budget`
- P0 Settings tool grant backend receipt: `npm run agents:project-settings:tool-grants`
- P0 temporary tool grant and invocation receipt contract: `npm run agents:local-tool-grants`
- P0 prompt injection quarantine and untrusted-data citation contract: `npm run agents:prompt-boundary`
- P0 unified local approval and irreversible execution contract: `npm run agents:action-approvals`
- P0 versioned five-mode quality regression contract: `npm run agents:quality-evaluation`
- P0 model generation degradation and provenance contract: `npm run agents:model-degradation`
- P0 team formation explainability and gap governance contract: `npm run agents:team-formation`
- P0 delegation DAG overdue blocking reassignment and notification contract: `npm run agents:delegation-governance`
- P0 project shared memory citation version confidence expiry and access-scope contract: `npm run agents:shared-memory`
- P0 review handoff acknowledgement lease fence stale-submission and acceptance contract: `npm run agents:review-handoff`
- P0 project autonomy governor version pause resume terminal-stop cost step and tool contract: `npm run agents:autonomy-governor`
- P0 local learning syllabus diagnostic pace practice mastery and spaced-review contract: `npm run agents:learning-program`
- P0 local teaching age integrity privacy evidence uncertainty and wellbeing contract: `npm run agents:teaching-safety`
- P0 local academic question outline claims draft review revision and finalization contract: `npm run agents:academic-writing-pipeline`
- P0 local claim source support contradiction freshness status and citation integrity contract: `npm run agents:citation-integrity`
- P0 local investigation hypotheses reliability custody contradictions confidence review and closure contract: `npm run agents:investigation-case`
- P0 local investigation authority privacy minimization sufficiency human review and one-time authorization contract: `npm run agents:investigation-safety`
- P0 local requirements implementation tests security review rollback rehearsal and release evidence contract: `npm run agents:technical-delivery`
- P0 local dependency secret permission static analysis remediation exception and release attestation contract: `npm run agents:engineering-security`
- P0 local creative brief iteration critique export quality and acknowledged handoff contract: `npm run agents:creative-studio`
- P0 local asset rights license attribution generation provenance derivative lineage and exact export audit contract: `npm run agents:rights-provenance`
- P0 local fine-grained authorization mutation audit transaction recovery contract: `npm run agents:fine-grained-authorization`
- P0 local store migration archive journal crash recovery validation and explicit rollback contract: `npm run agents:store-migration-safety`
- P0 local canonical artifact inventory retention legal hold and workspace drift contract: `npm run agents:local-artifact-storage`
- P0 local audit checkpoint archive quarantine recovery and recovery-receipt contract: `npm run agents:local-audit-integrity`
- P0 local privacy lifecycle automatic scan exact dual approval journal recovery and residual-boundary contract: `npm run agents:local-privacy-lifecycle`
- P0 local durable three-lane enqueue lease receipt acknowledgement and restart-recovery contract: `npm run agents:local-durable-task-queue`
- P0 local exactly-once commit stable Provider key ambiguity blocking and reconciliation contract: `npm run agents:local-idempotent-execution`
- P0 local durable timeout active cancellation fence Provider ambiguity and workspace child termination contract: `npm run agents:local-timeout-cancellation`
- P0 local UTC interval schedule stable slot clock rollback missed-run coalescing and durable dispatch contract: `npm run agents:local-scheduling-governance`
- P0 local durable dead-letter immutable source exact replay lease receipt recovery and disposition contract: `npm run agents:local-dead-letter-governance`
- P0 local process-safe Provider project actor model tool quota and concurrency admission contract: `npm run agents:local-rate-concurrency-governance`
- P0 local quiesce HTTP scheduler drain forced-timeout durable-recovery and shutdown-receipt contract: `npm run agents:local-graceful-shutdown`
- P0 local event SHA-256 chain verified checkpoint quarantine valid-tail recovery and restart-idempotent receipt contract: `npm run agents:local-event-recovery`
- P0 local safe HTTP trace context unique request span causal graph topology async links and hash-chain receipt contract: `npm run agents:local-causal-request-tracing`
- P0 Settings integration readiness contract: `npm run agents:settings-integration-readiness`
- P0 Settings workspace capability backend receipt: `npm run agents:project-settings:workspace`
- P0 Evidence Index readiness contract: `npm run agents:evidence-index-readiness`
- P0 Budget Alert readiness contract: `npm run agents:budget-alert-readiness`
- P0 Error Reporting readiness contract: `npm run agents:error-reporting-readiness`
- P0 Mission Runner browser startup: `npm run ui:manager-mission-runner`
- P0 real-user zero-to-autonomy API startup: `npm run agents:real-user-zero-to-autonomy`
- P0 real-user zero-to-autonomy operator report: `npm run agents:real-user-zero-to-autonomy-report` and `npm run agents:real-user-zero-to-autonomy-report:validate`
- P0 real-user zero-to-autonomy browser startup: `npm run ui:real-user-zero-to-autonomy`
- P0/P1 low-write release checklist: `npm run launch:local-mvp:check`
- P2 operator public-production no-go check: `npm run launch:public-production:no-go`
- P2 production data governance startup contract: `npm run agents:production-data-governance`
- P2 production traffic/rollback startup contract: `npm run agents:production-traffic-controls`
- P2 production customer acceptance startup contract: `npm run agents:production-customer-acceptance`
- P2 production operations startup contract: `npm run agents:production-operations-startup`
- P2 redacted operator readiness report: `npm run agents:public-production-readiness-report` and `npm run agents:public-production-readiness-report:validate`
- P2 managed environment preflight: `npm run agents:managed-environment-preflight` and `npm run agents:managed-environment-preflight:validate`
- P1 backend private pilot: `npm run agents:product-team:private-pilot`
- P1 production-ops bridge: `npm run agents:product-team:private-pilot:ops-readiness`
- P1 browser handoff: `npm run ui:manager-private-pilot`
- Legacy full Manager browser chain, optional broad regression only: `npm run ui:manager-backend:legacy-full`
- P2 infrastructure rehearsal: `npm run launch:infra` plus `GET /projects/:id/production-infrastructure-rehearsal`

The current correct claim is: local backend-backed private MVP, customer private-pilot rehearsal, and P2 infrastructure rehearsal are verified. Public production remains blocked until real managed-production evidence replaces local/private rehearsal receipts.

The current production-hardening rehearsal evidence is also verified:

- P2 operations controls: `npm run agents:product-team:production-ops-controls`
- P2 deployment controls: `npm run agents:product-team:production-deployment-controls`
- P2 security controls: `npm run agents:product-team:production-security-controls`
- P2 provider controls: `npm run agents:product-team:production-provider-controls`
- P2 managed-evidence classification: `npm run agents:product-team:production-evidence-integrity`
- P2 launch-governance approval contract: `npm run agents:product-team:production-launch-governance`

These P2 focused gates prove the backend contracts, receipt projections, evidence classification, Flow Graph / Proof Map surfaces, and launch-governance approval path. They are production-hardening rehearsal evidence, not public-production certification. The project-level Production Launch Control Center now also carries the global `public-production-startup-readiness` blocker, so a project cannot look production-ready while the runtime environment is still local/private-pilot only. The public production decision remains blocked until those contracts are connected to the real managed database, durable queue/cron, provider/BYOK credentials, centralized audit/observability, incident process, and customer production policy.

## Maintenance Rule

When adding or changing launch-related read models, scripts, or UI controls, update this document and run:

```bash
npm run launch:gates
```

The gate validator checks that the launch tiers remain tied to runnable package scripts, production blockers, and the generic product-team positioning.
